# گزارشِ چت ۱ — ممیزیِ امنیتیِ کد (اقدامِ ۱) — ۱۹/۰۶/۱۴۰۵ (2026-09-09)

> شاخه: `arena/01a0827b-p2` · پوش شده · درخت تمیز · کاملاً مستنداتی (صفر تغییر در کد).

## ۱. جدولِ کامیت‌ها

| # | موضوع | کامیت | پیام |
|---|---|---|---|
| ۱ | وظیفه (پیامِ دقیقِ درخواستی) | `aa9d199` | `docs: add security audit report` |
| ۲ | همین گزارش | (کامیتِ گزارش) | `docs: add chat 1 security audit report` |

## ۲. چه ساخته شد

- `docs/SECURITY_AUDIT_REPORT.md` (تازه، کامل): §۱ ممیزیِ بک‌اند (۱۲ سطرِ یافته با
  فایل:سطر + فهرستِ تمیزهایِ تأییدشده) + §۲ ممیزیِ فرانت‌اند (۳ یافته + تمیزها) +
  §۳ تعیین‌تکلیفِ W1/W2/W3 از برنامهٔ عملیاتی + §۴ محدودیتِ روش + §۵ گیت‌ها.
- **رأی:** ۰ بحرانی، ۰ بالا، ۲ متوسط، ۱۳ پایین، ۱ اطلاعاتی.

## ۳. اصلِ حاکم: زمین‌سازی، نه وانمود

- **خوانده شد، نه حدس زده شد:** کلِ زنجیرهٔ درخواست (`index.js`)، `auth.js` کامل،
  گیت‌هایِ `sync.js`، هر ۶ فایلِ routes، `pull.js`، `policy.js`، `admin.js`،
  `idor.js`، `validate.js` (قوانینِ تایپ) و `scope/tenancy` خط‌به‌خط بازبینی شدند.
- **ادعاهایِ مثبت هم شاهد دارند:** «بدونِ traversal» با allowlist استاتیک و
  regex+فهرستِ restore اثبات شد؛ «بدونِ eval» با grep کلِ `server/`؛ «XSS مهارشده»
  با دسته‌بندیِ هر ۳۲ `innerHTML` + پیمایشِ taint رویِ ۹۷ محلِ `openModal`.
- **W3 بسته شد، نه باز گذاشته شد:** ابهامِ `password` با جست‌وجویِ کلِ درخت
  (صفر خوانشِ `.password`) و اصلِ مستندِ passwordless حل شد — رأیِ «آسیب‌پذیری نیست».
- **شدت‌ها صادقانه‌اند:** دو متوسط (XFF و CSRF) با مهارکننده‌هایِ پابرجایشان
  ثبت شدند تا نه بزرگ‌نمایی شود نه کوچک‌نمایی؛ بقیه دفاع‌درعمق‌اند.

## ۴. راستی‌آزمایی

| بررسی | نتیجه |
|---|---|
| `node tests/smoke.js` | ‏۵۴۷/۵۴۷‏ ✅ |
| `node tools/check-authz.js` | ‏۰‏ (تطبیق کامل) ✅ |
| `node tests/secret-scan.js` | ‏۱۱/۱۱‏ ✅ |
| `npm run lint` | ‏۰ خطا‏ ✅ |

## ۵. Follow-upها (به‌ترتیبِ ارزش)

1. ‏F-AUTH-01‏ (متوسط): قیدِ پروکسیِ اعلام‌شده برایِ XFF — کوچک، پراثر.
2. ‏F-CSRF-01‏ (متوسط): توکنِ CSRF + احرازِ مجدد برایِ delete-account.
3. ‏F-IDOR-01‏ (پایین): یکدست‌سازیِ قلمروِ دبیر (taught در برابرِ مدرسه).
4. بقیهٔ پایین‌ها (F-FILE/F-PULL/F-SQL/F-XSS/...) — هر کدام یک‌خطی تا چندخطی.
