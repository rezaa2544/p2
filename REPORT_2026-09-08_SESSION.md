# گزارشِ نشستِ عامل — پایش

**تاریخ:** ۲۰۲۶-۰۹-۰۸ · **شاخه:** `arena/01a08256-p2` · **کامیتِ مبنا:** `4ca531c`
**مخزن:** https://github.com/rezaa2544/p2
**دستور:** اتصال به مخزن، خواندنِ تک‌تک فایل‌ها، کسبِ درکِ ۱۰۰٪ از سامانه،
آوردنِ ضروری‌ها به ورک‌اسپیس، و اعمالِ فوریِ هر تغییر در گیت‌هاب.

---

## ۱. وضعیتِ اتصال

| مورد | مقدار | توضیح |
|---|---|---|
| مسیرِ محلی | `/home/user/p2` | کلونِ آماده |
| شاخهٔ نشست | `arena/01a08256-p2` | منشعب از `main@4ca531c` |
| remote | `https://github.com/rezaa2544/p2.git` | بدون توکن در `.git/config` |
| عمقِ تاریخچه | ۱ کامیت (shallow) | همهٔ فایل‌ها روی دیسک حاضرند |
| احراز | از پیش تنظیم شده | **توکنِ ارسالی استفاده نشد و ذخیره نشد** |
| پوش | ✅ انجام شد | `bcbb3c1`، `a965137` و این کامیت |

> ⚠️ **اقدامِ امنیتیِ لازم از سوی شما:** توکنی که در متنِ گفتگو فرستاده شد،
> باید از `Settings → Developer settings → Tokens` **باطل** شود. احرازِ محیط
> از پیش برقرار است و نیازی به توکن نیست.

---

## ۲. آنچه خوانده شد (۵۳۵ فایل)

| حوزه | دامنه | سطحِ خواندن |
|---|---|---|
| `server/` | ۲۰ فایل + ۴ middleware + ۶ route | خط‌به‌خط |
| `src/js/` هسته | داده، ماندگاری، ایندکس، مسیریاب، پوسته، بوت | خط‌به‌خط |
| `src/js/` امنیت و همگامی | `27-sync`، `29-pull`، `29-scope`، `30-authz`، `06-login`، `19-actions-core` | خط‌به‌خط |
| `src/js/` نمونه‌ها | `02-demo-data`، `42-self-diagnostics`، `44-sms-notify`، `66-client-features`، `99-theme-loader` | ساختار و سرخط‌ها |
| مجوز | `authz/model.json` + `authz/write-perms.json` | ساختار + بازشماریِ ماشینی |
| ابزار/ساخت/تست | `build.js`، `tools/*`، `tests/run.js`، `scripts/run-all-tests.sh`، workflowها | خط‌به‌خط |
| راهنماهای متنی | `reza/توضیح_کامل_برنامه.md`، `docs/راهنمای_برنامه_نویس.md`، `docs/README.md`، `README.md` | کامل |
| وضعیت/گزارش | `TODO_BEFORE_PRODUCTION`، `REMAINING_WORK_SUMMARY`، `PILOT_READY_SUMMARY`، `FINAL_VERIFICATION_REPORT`، `docs/ROADMAP_FINAL_SCAN_2026-09-08`، `PROJECT_NOTES` (فهرست) | کامل/فهرستی |

فایل‌های حجیمِ غیرکد (`index.html` ۱.۹MB، `USER_GUIDE.html` ۲.۴MB،
`reza/برنامه_نویس/index.html` ۱.۹MB، `server/schema.sql` ۴۰k خط،
`docs/HANDOFF_ARCHIVE.md`، `docs/AI_PROMPT.md`) خروجی یا آرشیو حساب شدند —
خودشان از `src/` و `server/` تولید می‌شوند و خواندنِ کامل‌شان چیزی به درک
نمی‌افزود.

---

## ۳. خروجی‌های این نشست

| # | فایل | محل | وضعیت |
|---|---|---|---|
| ۱ | `AGENT_ONBOARDING.md` | ریشهٔ مخزن | ✅ کامیت `bcbb3c1` |
| ۲ | `reza/درک_عمیق_سامانه_پایش.md` | `reza/` — جایِ درخواستیِ اول | ✅ کامیت `a965137` |
| ۳ | `reza/README.md` | بلوکِ «نقطهٔ شروع» بالای فهرست | ✅ کامیت `a965137` |
| ۴ | `README.md` | بخشِ «🧭 اولین‌بار این‌جایید؟» + پرامپتِ آماده | ✅ کامیت `a965137` |
| ۵ | `REPORT_2026-09-08_SESSION.md` | این فایل | ⏳ کامیتِ جاری |
| ۶ | `REPORT_2026-09-08_OPEN_ITEMS.md` | مواردِ باز با اولویت | ⏳ کامیتِ جاری |
| ۷ | `PAYESH_REPORTS_2026-09-08.html` | بستهٔ تک‌فایلیِ قابلِ دانلود و چاپ | ⏳ کامیتِ جاری |
| ۸ | `payesh-reports-2026-09-08.zip` | در پوشهٔ ورک‌اسپیس (بیرون از مخزن) | 📥 فقط دانلود — طبق `.gitignore` (`*.zip`) وارد گیت نمی‌شود |

---

## ۴. یافته‌های کلیدی (خلاصهٔ فنی)

1. **لایهٔ داده واقعاً بسته است.** `00-data-layer.js` تنها نقطهٔ تماس با
   `localStorage`/`fetch` است و `tests/smoke.js` این را قفل کرده است. این یعنی
   اتصال به پایگاهِ واقعی، بازنویسیِ بدنهٔ چند تابع است نه کلِ برنامه.
2. **دفترچهٔ عملیات (event-log) زیربنای همه‌چیز است** — بازپخشِ وضعیت،
   سابقهٔ تغییرات و صفِ ارسال، هر سه از یک منبع تغذیه می‌شوند. فشردنِ
   خودکار (۳۰k عمل / ۳MB → اسنپ‌شات + ۵۰۰ عمل) از رشدِ بی‌پایان جلو می‌کند.
3. **امنیتِ سرور لایه‌لایه و fail-closed است**: nonceِ هر درخواست، نگهبانِ
   شمردنِ شناسه (۴ مرحله تا ابطالِ نشست)، JWT با الگوریتمِ سخت‌کد‌شده و
   rotation، OTPِ فقط-هش با سقف‌هایِ پیش از بررسیِ وجودِ شماره، و دروازهٔ
   فیلد که محافظت‌شده‌ها (`status`, `school_id`, `user_id`, `role`,
   `national_id`, `phone`) را هرگز از payload عبور نمی‌دهد.
4. **مجوز یک‌منبعی و ماشینی است**: `ACTION_ROLES` + تحلیلِ استاتیک +
   `authz/model.json` → `authz/write-perms.json` → `server/sync.js`، با
   نگهبانِ `build.js --check`. جدولِ کهنه = build قرمز.
5. **تست جدی و جهش‌کِش است**: ۱۰۸ سوئیت + ۴۵ جهش، اجرای سه‌خطی (درجا /
   کپیِ ایزوله / پورت‌سریال) با self-heal و گاردِ درختِ کثیف، حدود ۷ دقیقه.
6. **«آماده» با یک قید**: ۲۸/۲۸ بندِ نقشه‌راه پیاده شده و رگرسیون مؤثر
   ۱۵۰/۱۵۰ است؛ اما سروینگِ درخواست‌ها روی فایلِ JSON است، پرداختِ والد تصمیمِ
   محصول می‌خواهد، و درگاهِ پیامکِ واقعی هنوز وصل نیست.

جزئیاتِ کامل با نشانیِ «فایل:خط» در
[`AGENT_ONBOARDING.md`](AGENT_ONBOARDING.md) و
[`reza/درک_عمیق_سامانه_پایش.md`](reza/درک_عمیق_سامانه_پایش.md).

---

## ۵. ریسک‌ها و مواردِ باز

خلاصه در این جدول؛ شرحِ کامل با اولویت و پیشنهاد در
[`REPORT_2026-09-08_OPEN_ITEMS.md`](REPORT_2026-09-08_OPEN_ITEMS.md).

| اولویت | مورد |
|---|---|
| 🔴 محصول | پرداختِ والد — تنها موردِ بازِ کلِ پروژه |
| 🔴 محصول | تنشِ احتمالیِ paywall با بندِ ۰.۴ (دادهٔ پایه نباید پولی شود) |
| 🟠 استقرار | ذخیره‌سازِ JSON؛ PG آماده است ولی مسیرِ زنده نیست |
| 🟠 امنیت | سقفِ نرخِ ورودِ اکسل نیست؛ `cache.checkRateLimit` بی‌مصرف‌کننده |
| 🟡 کیفیت | `tests/api/*` در رگرسیون سیم‌کشی نشده |
| 🟡 کیفیت | فلیک‌های زمانیِ ۴ سوئیت + ۱۵ سوئیتِ جهش با ارجاعِ کهنه |
| 🟡 پیش از استقرار | حذفِ نرم (`deleted_at`) و تصمیمِ UUID |

---

## ۶. گام‌های پیشنهادیِ بعدی (منتظرِ تأیید)

1. بستنِ تصمیمِ **پرداختِ والد** (و هم‌زمان تعیینِ تکلیفِ بندِ ۰.۴).
2. بردنِ سروینگِ درخواست‌ها به **PostgreSQL** (اسکیما و `db.js` آماده‌اند).
3. سیم‌کشیِ `tests/api/*` در `run-all-tests.sh` + سقفِ نرخِ ورودِ اکسل.
4. رفعِ فلیک‌های زمانی و ارجاع‌های کهنهٔ سوئیت‌های جهش.
5. اتصالِ **درگاهِ پیامکِ واقعی** (گام‌های ۲ تا ۵ در `docs/PLAN_SMS_GATEWAY.md`).

> طبقِ «اصلِ هشتم» (قابلِ استخراج از گزارش‌های دورهای ۸۰+) هیچ‌کدام بدون
> تأییدِ شما آغاز نمی‌شود.

---

## پیوست — دستورهای پرتکرار

```bash
node build.js            # ساختِ index.html + dist/payesh.html
node build.js --check    # انطباقِ بیت‌به‌بیت + مجوزها + مُهرِ راهنما
npm test                 # tests/run.js + tests/smoke.js
node server/seed.js      # ساختِ server/data/payesh.json (نیاز: jsdom)
node server/index.js     # وب + API روی :3000
bash scripts/run-all-tests.sh     # رگرسیونِ کامل (~۷ دقیقه)
```
