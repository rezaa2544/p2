# گزارش بوتِ سشن — چت ۶ (بازیابی هویت + ممیزی وضعیت)

**تاریخ:** ۲۰۲۶-۰۹-۱۲ (UTC) · **شاخهٔ کاری:** `arena/01a09801-p2` · **پایه:** `main` @ `523c1f57e896db4b73f28dc8eb5743399951b31d` (مرجِ PR #147)
**تهیه‌کننده:** چت ۶ (Arena Agent) · **روش:** راستی‌آزماییِ مستقیم روی مخزن + GitHub API + اجرای محلی گیت‌ها

> **قاعدهٔ صداقت (Rule ۱):** هر عددِ این گزارش یا **measured** (با اجرای واقعی در این سندباکس) است یا برچسب
> `NOT-RUN` / `تأیید‌نشده` دارد. هیچ تستی که اجرا نشده، سبز اعلام نشده است.

---

## ۱) تأیید بازیابی هویت

هویتِ «چت ۶» با **شش نقش** بازیابی شد:

| نقش | وضعیت |
|---|---|
| معماری دیتابیس (PostgreSQL) | ✅ فعال |
| مهاجرت‌ها (Migrations) | ✅ فعال — گیت `migrate-helper --check` سبز |
| بهینه‌سازی عملکرد / Query | ✅ فعال (محدود به محیطِ بدون PG) |
| حاکمیت مستندات (Docs Governance) | ✅ فعال — قفل **rc40** راستی‌آزمایی شد |
| ممیزی PRها | ✅ فعال — ۹ PR باز ممیزیِ اولیه شد |
| یکپارچگی ریپازیتوری | ✅ فعال — گیت‌های ساختاری سبز |

**خط قرمزهای پذیرفته‌شده:** صداقت مطلق · مسدودیِ CI · Red-First + Mutation · قفل مستندات · بهداشت ورک‌اسپیس · تحویل bundle/patch هنگام ۴۰۱ · ارائهٔ گزارش در پایانِ هر دور.

---

## ۲) ⚠️ انطباقِ پرامپتِ دست‌بالا با واقعیتِ مخزن (Drift Report)

پرامپتِ دریافت‌شده چند فرضِ **کهنه** داشت. طبق Rule ۱، واقعیت را جایگزین کردم:

| ادعای پرامپت | واقعیتِ راستی‌آزمایی‌شده | حکم |
|---|---|---|
| پایه = مرج‌ها تا PR #133 | آخرین مرج روی `main` = **PR #147** (۲۰۲۶-۰۹-۱۲T23:31Z) | ❌ کهنه |
| فیکس stats-drift در PR #135 مرج شده و rc38 تثبیت شده | **#135 بسته (CLOSED) و مرج‌نشده**؛ قفلِ جاری **rc40** است | ❌ نادرست |
| قفل مستندات `rc38` | `docs/DOCS_FREEZE_v1.0.0-rc40.md` قفلِ جاری (۳۲۲ سند در مانیفست) | ❌ کهنه |
| PR #115 باز و منتظر بازبینی | **#115 مرج‌شده به main** (۲۰۲۶-۰۹-۱۲T15:57Z، `compare …behind_by=0` ⇒ جدِ main) | ❌ کهنه |
| PR #133 مرج‌شده به main | #133 به **شاخهٔ #129** مرج شده (base = `feat/p03-reports-envfix-bounded-cache`) ⇒ روی main نیست؛ همان stackِ #129 | ⚠️ بازخوانی |
| PR #129 باز، منتظر ACCEPT | ✅ **تأیید شد** (تنها آیتمِ کاملاً منطبق) | ✅ |
| PG 18.4 embedded روی پورت 55433 | در این سندباکس **هیچ** باینری/پردازه/لیسنرِ PG نیست؛ `DATABASE_URL` تنظیم نشده | ❌ NOT-RUN |
| دوزیه‌های Flag-1 و Report-Cache روی main | روی main **نیستند**؛ فقط در شاخهٔ #129: `docs/FLAG1_SCOPE_SQL_DOSSIER.md` و `docs/REPORT_CACHE_ISOLATION_DOSSIER.md` | ⚠️ بازخوانی |
| دور ۱ ممیزی انجام شده، دور ۲ در صف | انجام شده؛ ولی اکنون **دورهای ۲/۳ (چت ۸)** هم ثبت شده (PR #138/#139) | ⚠️ کهنه |

**یافتهٔ جدیدِ بحرانی (در پرامپت نبود):** هر **۹ PR باز** در وضعیت `mergeStateStatus = DIRTY` هستند
(تعارض با `main`). یعنی «منتظر ACCEPTِ ناظر» کافی نیست — پیش از ACCEPT باید **rebase/merge از main** انجام شود.

---

## ۳) جدول وضعیتِ باز (Open Items)

### الف) صفِ PRها (۹ باز — همه DIRTY)

| # | عنوان (کوتاه) | base | تغییرات | وضعیت | گام موردنیاز |
|---|---|---|---|---|---|
| **150** | ری‌تارگت M1/M6 در server15-mutations (رفع rot) | main | +8/−4 · 1f | 🟠 DIRTY | rebase + ACCEPT |
| **149** | bh-mut فاز ۲ دستهٔ ۴ (نوار ۱: ۴ فایل، ۲۰ جهش) | main | +651/−584 · 14f | 🟠 DIRTY | rebase + ACCEPT |
| **129** | P0-2 کش کراندار گزارش‌ها + دوزیهٔ flag-1 | main | +2541/−28 · 30f | 🟠 DIRTY · **مسدودکنندهٔ stack** | rebase ⇒ ACCEPT (یک ACCEPT = #129 + #133) |
| **128** | گزارش P1-3 تجمیع گزارش عمومی سمت PG | main | +76/−1 · 3f | 🟠 DIRTY | rebase + ACCEPT |
| **114** | گیت زندهٔ جداسازی tenant روی PG (10/10 + جهش 7/7) | main | +376/−11 · 8f | 🟠 DIRTY | rebase + اجرای PG زنده |
| **91** | Wave 18 — national load testing | main | +2180/−33 · 14f | 🟠 DIRTY | تصمیم (P0-4/P0-5) |
| **82** | DB scale wave10 | main | +3515/−144 · 29f | 🟠 DIRTY | تصمیم/ادغام یا بستن |
| **76** | WAL disk-full drill | main | +892/−2 · 7f | 🟠 DIRTY · superseded | بستن فقط با تأیید مالک (قاعدهٔ ۶) |
| **74** | bug-hunt session 7 (DoS + از دست‌رفتن صف) | main | +853/−57 · 19f | 🟠 DIRTY · منوط به BH-2 | تصمیمِ توکن ⇒ سپس رفع |

### ب) دوزیه‌ها و قیودِ منتظر تصمیم ناظر/مالک

| آیتم | وضعیت | منبعِ شاهد | نیازمند تصمیم |
|---|---|---|---|
| **Flag-1** — `scopedSchools` به SQL | 🟡 دوزیه روی شاخهٔ #129 (روی main نیست) | `docs/FLAG1_SCOPE_SQL_DOSSIER.md` در PR #129 | ACCEPTِ #129 |
| **Tenancy** — `school_id IS NULL` | 🔴 باز | `HANDOFF.md` (چت ۳) · پیش‌نیاز حذف `OR TRUE` | backfill به NOT NULL یا تغییر هم‌زمان JS/SQL |
| **جداسازی کش گزارشی از دادهٔ عملیاتی** | 🟡 دوزیه روی شاخهٔ #129 | `docs/REPORT_CACHE_ISOLATION_DOSSIER.md` در PR #129 | ACCEPTِ #129 + تصمیم MV |
| **BH-mut فاز ۲** (مهاجرت فایل‌های جهش) | 🟠 ۱۲۳ فایل واقعی · ۵۹ مهاجرت‌یافته · **۵۵ باقی‌مانده**؛ ۴۷ فایل روی شاخهٔ `bh-mut/phase2` **بدون PR باز** ⇒ خطر رهاشدگی | `NEXT_ACTIONS.md` ردیف BH-mut | تعیین مالک + باز کردن PR |
| **HO-1** — ارجاع‌های کهنه در `2026-09-13.md` | 🔴 ۱۱ ارجاعِ «تازه» (خروجی امروز) | `node tools/docs-refs-check.js` | رفع یا بازپایه‌سازی baseline |
| **HO-3** — گیت‌های جهش به CI وصل نیستند | 🟡 هیچ ورک‌فلویی سوئیت‌های جهش را صدا نمی‌زند | بررسی `.github/workflows/` | مالک/زیرساخت |
| **BH-2** — تصمیمِ توکنِ `setup-deps.ps1` | 🔴 فقط مالک | `NEXT_ACTIONS.md` | دستور صریح مالک |
| **P0-1** Reporting DB-native · **P0-4** دیتاست ملی · **P0-5** استیجینگ · **P0-6** ریشهٔ OOM | 🟠 باز (P0-3 محلی سبز، CI NOT-RUN) | `NEXT_ACTIONS.md` §P0 | اولویت‌بندی ناظر |
| **BH-6** — بازفعال‌سازی ایندکس ۰۱۲ | ⏸ NOT-ADD تا اثباتِ EXPLAIN | `NEXT_ACTIONS.md` | مهندسی |

---

## ۴) گیت‌های اجراشده — **measured** (همین سشن، محلی)

| گیت | فرمان | نتیجه | وضعیت |
|---|---|---|---|
| واحد/ساختاری | `node tests/run.js` | **35/35** ✅ | measured |
| دودی | `node --expose-gc tests/smoke.js` | **547/547** ✅ (۱ هشدارِ کنسول: `window.scrollTo` در jsdom — بی‌خطر) | measured |
| اسکن راز | `node tests/secret-scan.js` | **11/11** ✅ | measured |
| ساخت | `node build.js --check` | ✅ بیت‌به‌بیت + راهنما + WRITE_PERMS همگام (۳۹۴ اکشن) | measured |
| آینهٔ reza | `node tools/reza-mirror-check.js` | ✅ همهٔ آینه‌ها برابر با `docs/` | measured |
| قفل مستندات | `node tests/docs-freeze-marker.js` | **14/14** ✅ — قفل **rc40** معتبر | measured |
| همگامی آمار | `node tools/docs-stats-sync.js --check` | ✅ ۳۲۳ ریشه + ۳۸ زیرپوشه = ۳۶۱ درخت · تست‌ها ۴۶۵ | measured |
| مهاجرت‌ها | `node tools/migrate-helper.js --check` | ✅ ۰۰۱…۰۱۰ در راهنما ردیف دارند · شمارهٔ بعدی **011** | measured |
| ارجاع‌های مستندات | `node tools/docs-refs-check.js` | ⚠️ exit 0 ولی **۱۱ ارجاعِ کهنهٔ تازه** (baseline ۷۴ تاریخی پوشانده) | measured |

### گیت‌های **NOT-RUN** (دلیلِ صریح)

| گیت | خروجی | دلیل |
|---|---|---|
| `tests/wave23-reports-pg.js` | `⚠ NOT-RUN` (خروجِ خودِ اسکریپت) | `DATABASE_URL` تنظیم نیست؛ `ECONNREFUSED 127.0.0.1:5432`؛ هیچ باینری/پردازهٔ PG در سندباکس |
| `tests/wave3-parity.js` | `0 از 0 (1 skip)` | `DATABASE_URL is not set` ⇒ خود-Skipِ طراحی‌شده |
| CI (GitHub Actions) | — | انسداد بیلینگ (قیدِ پابرجا) ⇒ **ادعای «CI سبز» نمی‌کنم** |
| بنچ مقیاس / دیتاست ملی (P0-4/P0-5) | — | نیازمند استیجینگ و PG؛ در سندباکس مقدور نیست |

> نکتهٔ زیرساختی: برای این دور `npm install` اجرا شد (۱۲۶ بسته، موفق). پوشهٔ `node_modules/`
> در `.gitignore` است و در snapshotِ سندباکس هم مستثناست ⇒ بودجه را مصرف نمی‌کند.

---

## ۵) بهداشتِ ورک‌اسپیس (قاعدهٔ ۵)

| سنجه | مقدار | سقف | حکم |
|---|---|---|---|
| حجم ریپو (بدون `.git`/`node_modules`) | **۳۳ MB** | ۱۰۰ MB مؤثر | ✅ امن |
| فایل‌های تحتِ نظارت git | **۱,۳۱۳** | ۱۰,۰۰۰ | ✅ امن |
| اسنادِ درختِ `docs/` | ۳۶۱ (۳۲۳ ریشه + ۳۸ زیرپوشه) | — | ✅ همگام با مانیفست rc40 |
| درخت کاری | تمیز (بدون تغییرِ کد) | — | ✅ |

---

## ۶) آماده برای بریف

هویت بازیابی شد، وضعیت با شواهد استخراج شد، و هیچ تغییری در کد/مستندات ایجاد نشده است
(تنها خروجیِ این دور همین گزارش است — و به‌عمد **بیرون از `docs/`** نوشته شد تا قفلِ rc40 نشکند).

**پیشنهادِ اولویت برای نخستین بریف (نیازمند تأیید شما):**

1. **رفعِ DIRTYِ ۹ PR باز** (rebase روی `main@523c1f5`) — بدون آن هیچ ACCEPTی ممکن نیست.
2. **ACCEPTِ stack #129 ⇒ #133** (یک تصمیم، دو PR) + تعیین تکلیف دوزیه‌های Flag-1 / Cache-Isolation.
3. **باز کردن PR برای ۴۷ فایلِ مهاجرت‌یافته روی `bh-mut/phase2`** (خطرِ رهاشدگی).
4. **تصمیمِ tenancyِ `school_id IS NULL`** (مسدودکنندهٔ حذفِ `OR TRUE`).
5. **تصمیمِ BH-2 (توکن در `setup-deps.ps1`)** — فقط مالک.

---

### پیوست — فرمان‌های بازتولید

```bash
node tests/run.js                                   # 35/35
node --expose-gc --max-old-space-size=2048 tests/smoke.js   # 547/547
node tests/secret-scan.js                           # 11/11
node build.js --check                               # ✅
node tools/reza-mirror-check.js                     # ✅
node tests/docs-freeze-marker.js                    # 14/14 (rc40)
node tools/docs-stats-sync.js --check               # ✅
node tools/migrate-helper.js --check                # ✅ (بعدی: 011)
node tools/docs-refs-check.js                       # ⚠️ ۱۱ ارجاعِ تازه

# نیازمند PG (در این سندباکس NOT-RUN):
#   DATABASE_URL=postgres://…  node tests/wave23-reports-pg.js
#   DATABASE_URL=postgres://…  node tests/wave3-parity.js
```
