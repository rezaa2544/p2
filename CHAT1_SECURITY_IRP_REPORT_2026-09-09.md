# گزارشِ چت ۱ — طرحِ پاسخ به حوادثِ امنیتی (Incident Response Plan)

- **تاریخ:** ۱۹/۰۶/۱۴۰۵ (2026-09-09)
- **شاخه:** `arena/01a0827b-p2` — رویِ گیت‌هاب به‌روز شد (push شد)
- **کامیتِ کار:** `da31b51` با پیامِ `docs: add security incident response plan`
- **سندِ تحویلی:** `docs/SECURITY_INCIDENT_RESPONSE.md`

---

## ۱. جدولِ کامیت‌ها

| # | کامیت | پیام | محتوا |
|---|--------|------|-------|
| ۱ | `da31b51` | `docs: add security incident response plan` | ۲ فایل (۱۵۵+): سندِ تازه (تیمِ IRT + ۴ سناریویِ P0/P1/P2 + playbook پنج‌فازی + ماتریسِ اطلاع‌رسانی و مهلتِ ۷۲ ساعته) + ورودیِ HANDOFF |

---

## ۲. چه چیزی تحویل شد

سندِ `docs/SECURITY_INCIDENT_RESPONSE.md` با هر ۴ بخشِ خواسته‌شده، زمین‌سازی‌شده رویِ
مکانیکِ واقعیِ ریپو (نه وانمود):

- **§۱.۱ — تیمِ IRT:** ۴ نقش (Coordinator، Lead Engineer، Communication، Legal) با
  مسئولیت و جانشین + قاعدهٔ فراخوان بر اساسِ شدت‌هایِ `RUNBOOKS.md` §۰-۱ + شمارهٔ IR.
- **§۱.۲ — سناریوها:** P0 نشتِ PII، P0 حملهٔ حجمی، P1 دسترسیِ غیرمجاز، P2 بدافزار/XSS —
  هر کدام با نشانه‌هایِ قابلِ مشاهده (نامِ واقعیِ ایونت‌هایِ `audit()`) و اقدام‌هایِ
  گام‌به‌گام + قالبِ ورودیِ Breach Log (قراردادِ ساختِ آن در اولین حادثه).
- **§۱.۳ — playbook:** تشخیص (دستورهایِ `curl`/`journalctl`/`grep` رویِ آدیت + قانونِ
  ده‌برابریِ اعلام)، مهار (جدولِ ۵ اهرم: ابطالِ تکی/سراسری با چرخشِ کلید، دفاعِ شمارشِ
  شناسه، سقف‌ها، ایزوله + فریزِ شواهد)، تحلیل (تایم‌لاین‌نویسی)، بازیابی (ارجاع به
  ران‌بوک‌هایِ §۱-۱/§۱-۳، نه ابداع)، درس‌آموخته (blameless تا ۵ روزِ کاری).
- **§۱.۴ — ارتباطات:** تک‌سخنگو (Comms Lead)، ماتریسِ SEV→مخاطب، قالبِ فارسیِ پیامِ
  کاربری، مهلتِ ۷۲ ساعته با مالکِ حقوقی، و فهرستِ ممنوعه‌ها.

**صداقتِ سندی:** نداشته‌هایِ امروز (WAF، بلاک‌لیستِ IP، هشداردهندهٔ خودکار، Breach Log،
`report-uri` در CSP) در متن صریحاً «نداریم + توصیه/قراردادِ ساخت» ثبت شدند؛ برایِ ملی
به HPA و CDN موجود ارجاع داده شد.

---

## ۳. گیت‌هایِ الزامی (همه سبز)

| گیت | نتیجه |
|---|---|
| `node tests/smoke.js` | 547/547 ✅ |
| `node tools/check-authz.js` | «تطبیق کامل»، خروجیِ ۰ ✅ |

کار مستنداتیِ خالص بود (صفر تغییر در `src/`/`server/`/`tests/`)؛ HANDOFF به‌روز شد.

**وضعیتِ گیت:** کامیتِ `da31b51` رویِ `arena/01a0827b-p2` پوش شد؛ درخت تمیز.
چت ۱ برایِ کارِ بعدی آماده است. ✅
