# infra/observability/.env.observability — فقط الگو (فایلِ واقعی 0600؛ gitignored با .env*)
GRAFANA_USER=admin
GRAFANA_PASSWORD=
# مسیرِ دایرکتوریِ لاگِ برنامه برایِ promtail (پیش‌فرض: server/data در ریپو —
# در میزبانِ واقعی معمولاً /var/log/payesh است)
PAYESH_LOG_DIR=
