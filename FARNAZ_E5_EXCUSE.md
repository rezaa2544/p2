# گزارش E.5 فرناز — پاسخ سریع ولی به اعلان غیبت (چت ۳، آیتم ۵)

## یافتهٔ اصلی (عدم موازی‌کاری)
زنجیرهٔ درخواست→تأیید→موجه از قبل کامل بود (`quick-excuse` روی پرونده → `leaves` pending →
`decideLeave` → `excused` + اعلان به ولی). شکاف واقعی E.5 دو چیز بود: (۱) دکمه کنار **اعلان**
نبود، (۲) اعلان غیبت در زمان اجرا ساخته نمی‌شد (فقط دادهٔ نمایشی). هر دو بدون اکشن جدید
و بدون تغییر authz اضافه شد.

## چه ساخته شد (کلاینت‌فقط)
- `absenceNotifFor`: ساخت اعلان غیبت برای والدین با `ref='att_<id>` و ضدتکرار + هوک در `att-commit`
  (فقط رکوردهای غایب) + `ref` در دادهٔ نمایشی.
- `absenceAttOf`: حل رکورد حضور از روی اعلان (مسیر ref + مسیر fallback ولی↔فرزند↔تاریخ برای دادهٔ قدیمی).
- `absenceExcuseBtn` در لیست اعلان‌ها: دکمهٔ «🕊️ موجه اعلام کنم» (همان `quick-excuse` موجود) فقط برای
  ولی، بج «⏳ در انتظار بررسی مدیر» اگر درخواست pending هست، هیچ‌چیز برای بقیه/رکورد موجه.
- ضدتکرار در `qe-save`: درخواست دوم برای همان روز رد می‌شود.
- `authz/write-perms.json` صرفاً با مولد رسمی بازتولید شد (`att-commit` += مجموعهٔ `notifications`).

## نتایج
| گیت | نتیجه |
|---|---|
| سئوت جدید `tests/excuse.js` (جریان کامل تا تأیید مدیر) | 16/16 ✅ |
| تست جهش `tests/excuse-mutations.js` (شامل نگهبان «ولی وضعیت را عوض نمی‌کند») | 5/5 کشته ✅ |
| رگرسیون E.1–E.4 و `client-features` | سبز / 12/12 ✅ |
| `build --check` / `check-authz` | 0 / 0 ✅ |
| `tests/smoke.js` | 547/547 ✅ |

## نکتهٔ فنی (شفاف)
- جهش M4 (حذف گیت ولی) اول زنده ماند چون `myNotifs` ذاتاً اعلان ولی را به دانش‌آموز نشان نمی‌دهد؛
  تست T12 گیت را مستقیم (با اعلان کاشته‌شده برای دانش‌آموز) پوشش داد و هر ۵ جهش کشته شدند.
- رندر صفحهٔ حضور دست‌نخورده (فقط مسیر ثبت، فقط برای غایب‌ها) — پروب رندر ۱۱ms پابرجاست.
