# گزارشِ چت ۱ — Load Testing Plan (Tier 1، اولویت ۳) — ۱۹/۰۶/۱۴۰۵ (2026-09-09)

> شاخه: `arena/01a0827b-p2` · همه پوش شده · درخت تمیز · صفر تغییر در `src/` و `server/`.

## ۱. جدولِ کامیت‌ها

| # | موضوع | کامیت | پیام |
|---|---|---|---|
| ۱ | Load Testing Plan (وظیفه) | `f4dc106` | `docs: add load testing plan with k6 scenarios` |
| ۲ | اصلاحیهٔ Redis در سندِ Canary | `939aaf9` | `docs: correct redis claims in canary plan` |
| ۳ | همین گزارش | (کامیتِ گزارش) | `docs: add chat 1 load plan report` |

## ۲. یافتهٔ اصلیِ ممیزی (پیش از هر تغییر)

پرامپت فرض می‌کرد سند و سناریوها وجود ندارند؛ ممیزی خلافش را ثابت کرد:

- `docs/LOAD_TESTING_PLAN.md` (۳۲۵ خط، از main/فاز ۵) از قبل §۱-۱ تا §۱-۴ پرامپت را پوشش می‌دهد:
  مقایسهٔ k6/Locust/Gatling، هر ۶ سناریو، نردبانِ مرحله‌ای، SLOها (شاملِ Cache hit ‏> ۸۰٪‏).
- کلِ `tests/performance/` موجود و کامل است: ‏`scenarios/01..06`‏ (هر ۶ فایلِ §۲ پرامپت)،
  ۴ سوئیت، هلپرها، کانفیگ، فیکسچر، `run-benchmarks.sh` (با گاردِ نبودِ k6 و بالا آوردنِ خودکارِ سرورِ دمو).
- تنها خلأِ واقعی نسبت به پرامپت: دستورهایِ §۱-۶ (`perf:login` / `perf:attendance` / `perf:full`) —
  هیچ‌کدام در `package.json` نبودند.

پس به‌جایِ دوباره‌سازی (که موازی‌کاری و واگرایی می‌ساخت): **راستی‌آزماییِ کدِ موجود + سیم‌کشیِ نقطهٔ ورود + §۱۰**.

## ۳. راستی‌آزماییِ انجام‌شده (رویِ همین شاخه)

| بررسی | روش | نتیجه |
|---|---|---|
| پارسِ ESM هر ۱۳ فایلِ `.js` | `vm.SourceTextModule` (بدونِ اجرا — k6 فقط ESM) | ‏۱۳/۱۳‏ ✅ |
| سلامتِ ایمپورت‌ها | تطبیقِ ۶۸ ایمپورتِ نسبی با اکسپورتِ واقعی | ‏۶۸/۶۸‏، ۰ گمشده ✅ |
| واقعی‌بودنِ اندپوینت‌ها | تطبیقِ مسیرهایِ `/api/*` با `server/index.js` (شاملِ `/api/v1/*` فاز ۳) | همه موجود ✅ |
| سیم‌کشیِ npm | `npm run perf:login` بدونِ k6 | گاردِ تمیزِ «k6 یافت نشد» ✅ |
| احرازِ سناریوها | `PAYESH_DEMO_CODE=1` → اکویِ `demo_code` در `auth.js` | واقعی ✅ |
| اثرِ جانبی | grep بازگشتی رویِ `tests/`؛ گاردِ `git status` برایِ `src/` و `server/` | صفر ✅ |

## ۴. دلتایِ پیاده‌سازی (کامیتِ `f4dc106`)

1. `package.json`: سه اسکریپتِ `perf:login` / `perf:attendance` / `perf:full` (لفافِ `run-benchmarks.sh`؛
   آرگومانِ اضافه مثل `--env staging` پاس داده می‌شود).
2. `docs/LOAD_TESTING_PLAN.md` §۱۰ (تازه): جدولِ دستورها، پیش‌نیازها و محیط‌ها، قاعدهٔ «k6 در CI نیست»
   (گیتِ دستیِ پیشِ‌انتشار)، تطبیقِ نردبانِ ۱۰M با §۴/§۱، وضعیتِ راستی‌آزماییِ همین شاخه.
3. `HANDOFF.md`: ورودیِ وظیفه.

## ۵. اصلاحیهٔ Redis (کامیتِ `939aaf9` — کشفِ جنبیِ همین ممیزی)

نسخهٔ اولِ `CANARY_DEPLOYMENT.md` (وظیفهٔ ۳) ادعا می‌کرد «Redis در این معماری نیست».
ممیزی ثابت کرد این ادعا **غلط** است: `server/redis.js` + ‏`server/cache.js`‏ واقعی و سیم‌کشی‌شده‌اند
(ioredis با `REDIS_URL` و fallback حافظه؛ کش در مسیرِ bootstrap و idempotency/ابطالِ sync).
۴ نقطه اصلاح شد (سطرِ C4، اسنیپتِ readiness با `redis-cli ping` مشروط، پاراگرافِ §۱-۲ با برچسبِ اصلاحیه، بولتِ §۴).

## ۶. گیت‌ها (نهایی)

| گیت | نتیجه |
|---|---|
| `node tests/smoke.js` | ‏۵۴۷/۵۴۷‏ ✅ |
| `node tools/check-authz.js` | ‏۰‏ (تطبیقِ کامل) ✅ |
| `node tools/secret-scan.js` | ‏۱۱/۱۱‏ ✅ |
| قانونِ «بدونِ دست‌زدن به `src/` و `server/`» | برقرار ✅ |

## ۷. Follow-upها (خارج از دامنه)

1. راهنمایِ نصبِ داخلِ `run-benchmarks.sh` گزینهٔ غیرواقعیِ `npm install -g k6` را پیشنهاد می‌دهد
   (نصبِ درست در `tests/performance/README.md` §۲) — عمداً دست نخورد.
2. ادعایِ «وابستگیِ خارجیِ صفر» در `DEPLOY.md` §۰ با وجودِ `ioredis` در وابستگی‌ها کهنه است
   (از main؛ ioredis اختیاری با fallback است ولی در `package.json` هست) — نیازمندِ تصمیمِ مالکِ سند.
3. اجرایِ واقعیِ سناریوها (`--vus` پایین رویِ staging) پس از نصبِ k6 رویِ ایستگاهِ آزمون.
