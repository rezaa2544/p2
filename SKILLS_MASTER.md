# 📜 مرجع جامع مهارت‌های مهندسی فوق‌حرفه‌ای

**نسخه:** ۱.۰.۰ | **تاریخ:** ۲۰۲۶-۰۹-۰۸

**هدف:** تبدیل آرنا به یک معمار ارشد، کارشناس امنیت و مهندس نرم‌افزار درجه‌یک (مبتنی بر معماری پروژه پایش).

---

## 🚀 دستورالعمل استفاده (Master Prompt)

**هر بار که می‌خواهید به آرنا دستوری بدهید، این عبارت را ابتدای پرامپت قرار دهید:**

> "با رجوع به فایل `SKILLS_MASTER.md` و رعایت تمام اصول معماری، امنیتی، مقیاس‌پذیری و کدنویسی موجود در آن..."

---

## ۱. اصول معماری و فلسفه طراحی (Architecture & Design)

| اصل | شرح | مثال / پیاده‌سازی |
| :--- | :--- | :--- |
| **Fail-Closed (شکست-بسته)** | در صورت بروز ابهام، خطا یا ورودی ناشناخته، سیستم باید دسترسی یا عملیات را **رد کند** (ببندد)، نه اینکه به‌صورت پیش‌فرض مجوز دهد. | `unknown field` در `server/validate.js` باید خطای `validation_failed` دهد. |
| **Offline-First** | برنامه باید بدون اینترنت کار کند. داده‌ها در سمت کلاینت (IndexedDB/localStorage) با ساختار `Event Log` ذخیره شوند. | تغییرات در `src/js/03-persistence.js` باید از `applyOp` عبور کنند. |
| **Stateless Core** | سرور باید بدون حالت (Stateless) باشد. تمام وضعیت‌های نشست، نرخ و صف باید در Redis یا دیتابیس ذخیره شوند. | استفاده از `server/cache.js` و `server/redis.js` برای توزیع وضعیت. |
| **Tenant Isolation** | داده‌های هر مدرسه `school_id`) باید کاملاً ایزوله باشد. کوئری‌ها همیشه دارای شرط `school_id` باشند. | `server/middleware/scope.js` باید در تمامی روت‌ها اعمال شود. |
| **Single-File Distribution** | خروجی نهایی فقط یک فایل `index.html` است. **هیچ‌گاه** این فایل را دستی ویرایش نکنید. همیشه `node build.js` را اجرا کنید. | تمام تغییرات باید در فایل‌های `src/` اعمال شوند. |

---

## ۲. امنیت پیشرفته (Advanced Security)

| اصل | شرح | پیاده‌سازی الزامی |
| :--- | :--- | :--- |
| **حفاظت در برابر تزریق (XSS/ICS)** | تمام ورودی‌های کاربر در خروجی HTML باید با `esc()` و `escAttr()` پالایش شوند. برای ICS از استاندارد RFC 5545 استفاده کنید. | در `src/js/01-helpers.js` توابع escape وجود دارند. |
| **مدیریت رمز و OTP** | رمزها هرگز به‌صورت `plain text` ذخیره نشوند. کدهای OTP فقط به‌صورت `hash` در `server/data/otp.json` یا Redis ذخیره شوند. | از `crypto.randomInt` و `sha256` استفاده کنید. |
| **Rate Limiting توزیع‌شده** | محدودیت‌های نرخ (IP, Phone, User) نباید محلی (In-Memory) باشند؛ باید از Redis استفاده کنند. | تابع `checkRateLimit` در `server/cache.js` باید برای تمام endpointهای حساس فعال شود. |
| **JWT Hardening** | توکن‌ها باید شامل `iss`, `aud`, `iat`, `exp` و `jti` باشند. کوکی باید `HttpOnly; Secure; SameSite=Lax` باشد. | `server/auth.js` این موارد را اعمال می‌کند. |
| **Secret Management** | هیچ‌گاه توکن یا کلیدی در کد هاردکد نشود. از متغیرهای محیطی `process.env`) استفاده کنید. | `.env` در `.gitignore` است. |

---

## ۳. بک‌اند، دیتابیس و API (Backend & Data)

| اصل | شرح | پیاده‌سازی الزامی |
| :--- | :--- | :--- |
| **Resource-Based API** | از معماری RPC/Sync صرف، به سمت RESTful حرکت کنید. | از `server/routes/*.js` و `server/middleware/pagination.js` استفاده کنید. |
| **Database Abstraction** | لایه دسترسی به داده `server/db.js`) باید از PostgreSQL پشتیبانی کند و در صورت عدم دسترسی به دیتابیس، به حافظه (Fallback) برگردد. | متدهای `query` و `transaction` اجباری هستند. |
| **Query Optimization** | از حلقه‌های `O(n)` در اسکریپت‌های سمت سرور جداً پرهیز کنید. کوئری‌ها باید با ایندکس‌های ترکیبی (Composite Indexes) بهینه شوند. | از `server/schema.sql` برای ایندکس‌گذاری استفاده کنید. |
| **Idempotency** | هر درخواست نوشتن `POST/PUT/PATCH`) باید دارای یک `Idempotency-Key` (مانند `uid`) باشد تا از ثبت تکراری جلوگیری شود. | با استفاده از `server/cache.js` (Idempotency Engine) پیاده‌سازی کنید. |
| **Optimistic Concurrency** | برای داده‌های حساس (نمرات، حضور) از `base_version` برای جلوگیری از بازنویسی هم‌زمان استفاده کنید. | در صورت mismatch، خطای `409 Conflict` بازگردانید. |

---

## ۴. کلاینت، UI/UX و آفلاین (Client & Offline)

| اصل | شرح | پیاده‌سازی الزامی |
| :--- | :--- | :--- |
| **Event Delegation** | برای مدیریت کلیک‌ها از `data-act` و آبجکت `A` استفاده کنید. نام اکشن‌ها باید یکتا باشد تا دچار سایه‌اندازی نشوند. | در `src/js/19-actions-core.js` مدیریت شده است. |
| **Virtual Scrolling** | برای لیست‌های با بیش از ۱۰۰ رکورد، از صفحه‌بندی (Pagination) یا رندر مجازی استفاده کنید تا مرورگر قفل نشود. | پیاده‌سازی در `src/js/04-queries.js` با تابع `paginatedRows`. |
| **IndexedDB Migration** | داده‌ها باید به تدریج از localStorage به IndexedDB مهاجرت کنند. از پرچم `payesh_idb_migrated_v2` برای جلوگیری از اجرای مجدد استفاده کنید. | `src/js/00-migration.js` این وظیفه را دارد. |
| **Render Optimization** | از رندرهای کامل (Full Re-render) پرهیز کنید. تا حد امکان فقط بخش‌های تغییرکرده (Partial Render) را به‌روزرسانی کنید. | از `renderRoute` هوشمندانه استفاده کنید. |
| **Dead-Letter Queue (DLQ)** | عملیات‌های آفلاین که پس از چندین تلاش با خطای پایدار (مانند ۴۰۳ یا ۴۱۳) مواجه می‌شوند، باید به صف «مرده» منتقل شده و از چرخه بی‌نهایت Retry خارج شوند. | در `src/js/27-sync.js` با `SYNC_DEAD_CODES` پیاده‌سازی شده است. |

---

## ۵. تست و تضمین کیفیت (Testing & QA)

| اصل | شرح | پیاده‌سازی الزامی |
| :--- | :--- | :--- |
| **تست جهش (Mutation Testing)** | برای هر رفع باگ یا ویژگی جدید، حتماً تست جهش بنویسید. اگر تغییر کد باعث زنده ماندن یک جهش شود، یعنی تست شما کافی نیست. | ساخت فایل‌های `*-mutations.js` و استفاده از `scripts/run-all-tests.sh`. |
| **Smoke Test** | پیش از هر کامیت، `node tests/smoke.js` باید **۵۴۷/۵۴۷** سبز باشد. | این تست رفتار واقعی UI در `jsdom` را شبیه‌سازی می‌کند. |
| **Authorization Check** | هیچگاه مجوزها را با `WRITE_PERMS` دستی یا `ACTION_ROLES` دستی قاطی نکنید. حتماً `node tools/check-authz.js` را اجرا کنید (خروجی باید ۰ باشد). | `build.js --check` این کار را خودکار می‌کند. |
| **Full Regression** | قبل از Merge به `main`، حتماً `scripts/run-all-tests.sh` را اجرا کنید. تعداد تست‌ها نباید کاهش یابد مگر با دلیل موجه. | این کار معمولاً حدود ۷ تا ۱۰ دقیقه زمان می‌برد. |
| **شمارِ زنده را دستی ننویسید (Single Source of Truth)** | هر عددی که از دیسک قابل شمارش است (شمار تست‌ها، شمار اسناد، شمار ردیف‌های مانیفست) **نباید** دستی در سند تایپ شود؛ با هر سند یا تستِ تازه کهنه می‌شود و تست قرمز می‌کند. این اشتباه سه بار پشتِ سر هم رخ داد (`rc28`→`rc29`→`rc30`) تا اینکه علت برداشته شد. | `node tools/docs-stats-sync.js` اعداد را از دیسک می‌سازد؛ `--check` کهنه‌بودن را با exit 1 گزارش می‌کند؛ `--freeze` مانیفستِ قفلِ جاری را هم بازتولید می‌کند. قاعده: **یا ابزار عدد را می‌سازد، یا عدد در سند نمی‌آید.** |

---

### ۵.۱ جلوگیری از کهنه‌شدنِ اعداد (قاعدهٔ «دیوارِ شماره»)

سه قفلِ پشتِ سر هم (`rc28`، `rc29`، `rc30`) به یک دلیل بسته شدند: کسی سند یا تستی افزود و
شمارهای دستیِ `DOCS_METRICS.md` / `DOCUMENTATION_MAP.md` / `TEST_COVERAGE_REPORT.md` کهنه شد.
این «بی‌دقتی» نبود؛ **طرح ایراد داشت** — عددِ درست فقط در دیسک بود ولی عددِ نوشته‌شده دستی
نگه داشته می‌شد. تا وقتی این دو از هم جدا باشند، کهنه‌شدن قطعی است، نه احتمالی.

| قاعده | چرا |
| :--- | :--- |
| **۱. یا ابزار عدد را می‌سازد، یا عدد در سند نمی‌آید** | عددِ دستی حتماً کهنه می‌شود؛ سؤال «کی» است نه «آیا». |
| **۲. تست باید عددِ انتظار را از دیسک مشتق کند، نه سخت‌کد** | سنجه‌ای که `۲۷۷` را hard-code کرده بود با نامِ خودش («ادعای سند با دیسک سازگار است») در تضاد بود و با هر بامپ کهنه می‌شد. |
| **۳. ابزارِ همگام‌سازی باید `--check` داشته باشد** | نوشتنِ تنها کافی نیست؛ گیت باید بتواند **بدونِ نوشتن** بپرسد «کهنه‌ای؟» و با exit 1 جواب بگیرد. |
| **۴. ابزار باید idempotent باشد** | اجرای دوم نباید دیف بسازد؛ وگرنه «اجرا کن تا سبز شوی» به یک حلقهٔ بی‌پایان تبدیل می‌شود. |
| **۵. ابزار در ابهام باید بایستد، نه حدس بزند** | اگر الگویی صفر یا بیش از یک بار خورد، `docs-stats-sync.js` با exit 2 می‌ایستد و از نوشتن خودداری می‌کند — خراب‌کردنِ بی‌صدا از قرمزِ صادقانه بدتر است. |
| **۶. تصمیمِ انسانی را به ابزار نسپارید** | خودِ بامپِ قفل یک **تصمیم** است (چه چیزی منجمد شود و چرا) و دستی می‌ماند؛ ابزار فقط مانیفست و شمارها را می‌سازد. |

**ممیزیِ جهشِ ابزار** (اثبات اینکه واقعاً می‌گیرد): عددِ دست‌کاری‌شده → exit 1 · سندِ تازهٔ
همگام‌نشده → exit 1 · هشِ خراب در مانیفست → exit 1 · ردیفِ تکراریِ تحتِ مالکیت → exit 2 با
«از نوشتن خودداری شد». در همهٔ موارد، اجرای ابزار وضعیت را به سبز برمی‌گرداند.

### ۵.۲ تستِ رفتاری به‌جای تستِ متنی (قاعدهٔ «سورس را نپا، قرارداد را بپا»)

‏`tests/a11y-regressions.js` قرمز شد چون رشتهٔ `label for="${m[1]}"` را در سورس
می‌گشت؛ وقتی `f()` سخت‌تر شد (پاک‌سازی با `escAttr` + شناختِ jdate) **رفتار درست‌تر شد
و تست شکست**. سنجهٔ متنی هم «املا»ی پیاده‌سازی را می‌پاید هم رفتار را — پس هر بهبودی
در پیاده‌سازی آن را می‌شکند.

| قاعده | چرا |
| :--- | :--- |
| **۱. قرارداد را بسنجید، نه امضایِ سورس را** | سنجهٔ متنی با هر ریفکتورِ درست قرمز می‌شود؛ و بدتر، روی کدی که آن رشته را دارد ولی کار نمی‌کند سبز می‌ماند. |
| **۲. واحدِ واقعی را استخراج و **اجرا** کنید** | ‏`escAttr` از `01-helpers.js` و `f()` از `18-modals.js` با موازنهٔ آکولاد بیرون کشیده و اجرا می‌شوند — نه shim که واگرا شود. |
| **۳. سنجهٔ رفتاری را هم جهش بدهید** | یک سنجهٔ رفتاریِ بد هم می‌تواند ساختگی باشد. جهشِ «`escAttr` حذف شد» سنجهٔ اولِ پاک‌سازی را **سبز** گذاشت و لویش داد. |
| **۴. جهش باید «کشته شود»، نه «اجرا»** | معیار، exit غیرصفر نیست؛ معیار این است که **همان سنجهٔ مرتبط** قرمز شود. |
| **۵. پروبِ دورریختنی را با تست اشتباه نگیرید** | اگر پروبِ موقت خطا داد، همان را گزارش کنید — نه خروجیِ تستِ واقعی را. |

### ۵.۳ جغجغه برای بدهیِ تاریخی (قاعدهٔ «بدهی را قفل کن، رشدش را ببند»)

گاهی یک نقصِ واقعی در صدها جای تاریخی تکرار شده و رفعِ یک‌باره‌اش یا ناممکن است یا
یعنی جعلِ سابقه (گزارش‌های نقطه‌درزمانی). راه‌حل، نادیده‌گرفتن نیست:

| قاعده | چرا |
| :--- | :--- |
| **۱. بدهی را در یک خطِ پایه ثبت کنید** | ‏`tools/docs-refs-baseline.json` — ۱۱۸ ارجاعِ کهنهٔ موجود، با دلیلِ نوشته‌شده در خودِ فایل. |
| **۲. فقط موردِ **تازه** گیت را قرمز کند** | ‏`tools/docs-refs-check.js --check` روی بدهیِ قدیمی سبز است و روی ارجاعِ کهنهٔ تازه exit 1. |
| **۳. کلید را «سند → ارجاع» بگیرید، نه فقط ارجاع** | وگرنه سرایتِ یک ارجاعِ کهنه به سندِ تازه بی‌صدا می‌گذرد. |
| **۴. نویز را با قاعده رد کنید، نه با خطِ پایه** | مسیرهای قالب (`src/js/NN-my-feature.js`، ‏`MODULE.js`، ‏`docs/_export/`) در `TEMPLATE_PATTERNS`‌اند. گذاشتنشان در خطِ پایه، بدهیِ واقعی را پنهان می‌کند. |
| **۵. ابزارِ جغجغه باید خودش تست داشته باشد** | ‏`tests/docs-refs-check.js` رفتار را روی درختِ موقت می‌سنجد — چون ابزاری که ساکت شده باشد سبز می‌ماند. |

**دامی که در همین کار افتاد:** ابزار `ROOT` را از `__dirname` خودش می‌گیرد نه از `cwd`؛
پس تستی که کپیِ ابزار را در درختِ موقت می‌سازد ولی **مسیرِ اصلی** را اجرا می‌کند، مخزنِ
واقعی را می‌سنجد و نتیجهٔ کاذب می‌دهد. هر چهار سنجهٔ رفتاری نخستین نسخه به همین دلیل
«قرمز» شدند در حالی که ابزار سالم بود.

## ۶. مدیریت مخزن و DevOps (Git & CI/CD)

| اصل | شرح | پیاده‌سازی الزامی |
| :--- | :--- | :--- |
| **Commit Conventions** | از استاندارد `Conventional Commits` استفاده کنید: `feat:`, `fix:`, `docs:`, `perf:`, `refactor:`. | مثال: `fix(sync): add chunking to prevent 413 errors`. |
| **Clean History** | از `git reset --hard` با احتیاط استفاده کنید و همیشه قبل از آن یک `git branch backup` بسازید. | از پاک‌سازی تاریخچه با `git-filter-repo` برای حذف فایل‌های حساس استفاده کنید. |
| **Secret Scan** | قبل از Push، حتماً `tools/secret-scan.js` را اجرا کنید تا از نشت توکن‌ها جلوگیری شود. | در صورت وجود هرگونه secret در تاریخچه، عملیات متوقف شود. |
| **PR Merge Gate** | Pull Request فقط زمانی Merge شود که تمام چک‌ها (Checks) سبز باشند، Conflict وجود نداشته باشد و حداقل یک بازبینی (Review) انجام شده باشد. | پس از Merge، شاخه‌های Feature حذف شوند. |

---

## ۷. قدرت تحلیل و عیب‌یابی (Troubleshooting)

| اصل | شرح | پیاده‌سازی الزامی |
| :--- | :--- | :--- |
| **Root Cause Analysis** | هر باگ را با «پروب زنده» (اجرای واقعی) شبیه‌سازی کنید و ریشه را پیدا کنید. به پیام خطا سطحی اکتفا نکنید. | از `console.log` و ابزارهای دیباگ در `jsdom` استفاده کنید. |
| **Performance Profiling** | برای یافتن گلوگاه‌های عملکرد، از `perfSample` و ابزارهای اندازه‌گیری زمان در کد استفاده کنید. | در `src/js/42-self-diagnostics.js` ابزارهای لازم وجود دارد. |
| **Log & Audit** | هرگز اطلاعات حساس (رمز، کد ملی کامل، توکن) را در `console.log` یا `audit.log` چاپ نکنید. | از `server/audit.js` با ماسک‌سازی خودکار استفاده کنید. |
| **Incremental Delivery** | تغییرات بزرگ را به چند مرحله کوچک تقسیم کنید. هر مرحله باید به‌تنهایی قابل تست و بازگشت (Rollback) باشد. | این اصل در فازهای ۱ تا ۷ نقشه راه رعایت شده است. |

---

## 🎯 دستورالعمل نهایی (Habit Formation)

**آرنا، به‌عنوان یک مهندس ارشد:**

۱. پیش از نوشتن حتی یک خط کد، این سند را مرور کن.

۲. در ذهن خود، سناریوی «شکست» (Failure Scenario) را تصور کن و برای آن برنامه‌ریزی کن.

۳. هر تغییری که انجام می‌دهی باید با یک **تست** (Unit/Integration/Mutation) همراه باشد.

۴. به‌یاد داشته باش که این کد قرار است توسط هزاران مدرسه و میلیون‌ها کاربر استفاده شود؛ پس کیفیت و امنیت، اولویت مطلق توست.

**پایان سند.** 🚀
