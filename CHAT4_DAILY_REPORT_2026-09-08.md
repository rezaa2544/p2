# گزارش روزانهٔ چت ۴ — هماهنگی و نظارت کیفیت فاز فرناز

**تاریخ:** ۱۸ شهریور ۱۴۰۵ (2026-09-08) · **شاخه:** `feat/farnaz-phase1-chat4` · **مبنا:** `main` @ `4ca531c`
**نویسنده:** چت ۴ (هماهنگ‌کننده و ناظر کیفیت)

---

## ۱. خلاصهٔ اجرایی

| محور | وضعیت |
|---|---|
| دروازه‌های `main` | ✅ هر سه سبز (دودی ۵۴۷/۵۴۷ · مجوزها خروجی ۰ · ساخت بیت‌به‌بیت) |
| دروازه‌های شاخه‌های فرناز | ✅ هر سه شاخهٔ موجود سبز (دودی ۵۴۷/۵۴۷ روی هر کدام) |
| خط‌مبنای کارایی حضور (۸۳۶ms) | ✅ بازتولید شد (میانهٔ ۸۳۱ در نخستین سنجش) · هیچ شاخه‌ای رجعت ندارد |
| هدف «زیر ۶۰۰ms» | 🔴 هنوز محقق نشده — ریشه شناسایی شد (§۴) |
| انحراف فرآیندی | ⚠️ **یک مورد جدی:** شاخهٔ چت ۲ اصلاً روی گیت‌هاب نیست (§۵) |
| تداخل شاخه‌ها | ⚠️ `USER_GUIDE.html` در هر سه جفتِ مرج، تداخل متنی دارد (§۶) |

---

## ۲. وضعیت تیم (فایل‌به‌فایل و کامیت‌به‌کامیت وارسی شد)

| چت | شاخه | کامیت‌ها | بندها | دروازه‌ها | وضعیت |
|---|---|---|---|---|---|
| چت ۱ | `feat/farnaz-phase1-chat1` @ `61861b5` | ۷ کامیت | الف.۱ ✅ · الف.۲ ✅ · ب.۱ ✅ · ب.۲ ✅ · و.۱+و.۲ ✅ | دودی ۵۴۷/۵۴۷ · مجوزها ۰ · ساخت ۰ | **کامل** — مطابق دستور، هر ۶ بند بسته شد |
| چت ۲ | `feat/farnaz-phase1-chat2` | **۰ کامیت روی اصل** | ب.۳–ب.۵، ج.۱–ج.۳، د.۱–د.۴، گ.۳ | نامعلوم | 🔴 **نامرئی** — شاخه وجود ندارد (§۵) |
| چت ۳ | `feat/farnaz-phase1-chat3` @ `64c0753` | ۳ کامیت | ای.۱ ✅ · ای.۲ ✅ · ای.۳ ✅ | دودی ۵۴۷/۵۴۷ · مجوزها ۰ · ساخت ۰ | در حال اجرا — ای.۴ تا ای.۶ و گ.۱/گ.۲ باقی است |
| پایه | `feat/farnaz-phase1` @ `776de0e` | ۴ کامیت | اس.۰–اس.۵ ✅ (معدل ورودی + خوابگاه پانسیونی) | دودی ۵۴۷/۵۴۷ · مجوزها ۰ · ساخت ۰ | کامل، بی‌آر (هنوز درخواست مرجی ندارد) |
| چت ۴ | `feat/farnaz-phase1-chat4` | این گزارش + ابزار سنجش | نظارت | — | فعال |

**پی‌آر شمارهٔ ۸** (چت ۱ ← مین): پیش‌نویس، وضعیت مرج `clean`، ۲۶ فایل (۲۳۷۸+ / ۲۰-)، شرحش تا ب.۱ به‌روز است ولی فهرست «باقی‌مانده» در شرح پی‌آر کهنه شده (ب.۲ و و.۱/و.۲ الان کامیت شده‌اند). پیشنهاد: چت ۱ شرح پی‌آر را تازه کند.

---

## ۳. دروازه‌ها — اعداد دقیق

| دروازه | مین `4ca531c` | پایه `776de0e` | چت ۱ `61861b5` | چت ۳ `64c0753` |
|---|---|---|---|---|
| `tests/smoke.js` | **۵۴۷/۵۴۷** ✅ | **۵۴۷/۵۴۷** ✅ | **۵۴۷/۵۴۷** ✅ | **۵۴۷/۵۴۷** ✅ |
| `tools/check-authz.js` | خروجی **۰** ✅ | **۰** ✅ | **۰** ✅ | **۰** ✅ |
| `build.js --check` | بیت‌به‌بیت + مُهر راهنما ✅ | ✅ | ✅ | ✅ |

> تنها هشدارِ دودی، `window.scrollTo` است — محدودیت شبیه‌ساز، نه برنامه (سابقه‌دار).
> اکشن‌های سامانه اکنون **۳۳۸** عدد است (برخی اسناد هنوز ۳۳۷ نوشته‌اند — رانش جزئی مستندات).

---

## ۴. خط‌مبنای کارایی «ثبت حضور دبیر» — قید کیفی

**ابزار تازه:** `tools/perf-attendance.js` (کامیت‌شده در این شاخه) — سناریوی قطعیِ
«علامت تک‌تک + ثبت نهایی» برای کلاس ۱ (دهم ریاضی فیزیک، **۱۳ دانش‌آموز**) با
`teacher1_1` در جی‌ساد‌ام؛ هر اجرا در دامِ تازه + صحت‌سنجیِ ۱۳ رکورد؛ ۵ اجرا در هر سنجش.

| سنجش | میانه | کمینه–بیشینه | تفکیک میانگین |
|---|---|---|---|
| مین (نخست) | **۸۳۱ مس** | ۵۹۳–۹۴۷ | رندر ۶۷ · کلیک‌ها ۵۸۶ · مرور ۵ · ثبت ۱۰۹ |
| مین (بازه‌سنجی) | ۷۱۲ مس | ۶۱۴–۹۰۰ | رندر ۶۹ · کلیک‌ها ۵۶۱ · مرور ۴ · ثبت ۱۱۱ |
| چت ۱ | ۶۴۷ مس | ۶۳۹–۷۵۹ | رندر ۵۲ · کلیک‌ها ۵۱۱ · مرور ۴ · ثبت ۱۱۰ |
| چت ۳ | ۷۱۲ مس | ۶۴۰–۸۰۴ | رندر ۶۱ · کلیک‌ها ۵۴۶ · مرور ۴ · ثبت ۱۰۲ |
| پایه | ۶۸۱ مس | ۶۰۵–۸۰۸ | رندر ۷۰ · کلیک‌ها ۵۱۴ · مرور ۴ · ثبت ۱۰۰ |

**نتیجه‌ها:**
1. خط‌مبنای اعلامیِ ۸۳۶ مس **بازتولید شد** (میانهٔ ۸۳۱ در نخستین اجرا). پراکندگی ذاتی محیط ۵۹۳ تا ۹۴۷ است.
2. **هیچ شاخه‌ای رجعت کارایی ندارد** — همهٔ میانه‌ها درون بازهٔ نویزِ خودِ مین‌اند.
3. ادعای «مین ~۷۰۸ / شاخه ~۶۵۸» در شرح پی‌آر شمارهٔ ۸ با سنجش ۵-اجرای ما سازگار است (تک‌اجرا بوده).
4. 🔴 **هدف «زیر ۶۰۰ مس» هنوز در هیچ شاخه‌ای محقق نشده.** ریشهٔ اندازه‌گیری‌شده:
   **~۷۰٪ زمان کل در فاز کلیک‌هاست** — هر کلیک `att-set` یک `render()` کاملِ صفحه
   می‌کند (۱۳ رندر کامل). ثبت نهایی (`batchWrites`) فقط ~۱۰۰ مس است.
   **راه‌حل پیشنهادی (ساخته نشد — طبق دستور فقط گزارش):** به‌روزرسانی جزئی دام
   در اکشن `att-set` (فقط کلاسِ دکمه‌های همان سطر عوض شود، نه رندر کامل).
   برآورد: رساندن کل جریان به ~۲۵۰–۳۵۰ مس. نیازمند تأیید ناظر و ساخت در یک دور جدا.

---

## ۵. 🔴 انحراف جدی: چت ۲ هیچ چیزی را پوش نکرده است

قانون سخت شمارهٔ ۳ می‌گوید: «هیچ تغییری نباید فقط در محیط محلی باقی بماند.»
وضعیت فعلی نقض این قانون است:

- شاخهٔ `feat/farnaz-phase1-chat2` روی `origin` **وجود ندارد** (`git ls-remote` تأیید شد).
- بندهای محول‌شده به چت ۲ (ب.۳–ب.۵، ج.۱–ج.۳، د.۱–د.۴، گ.۳) وضعیت نامعلوم دارند.
- **ریسک:** کارِ انجام‌شده در محیطِ محلی با هر ریست/حادثه از دست می‌رود؛ ناظر نمی‌تواند
  تداخلش را با بقیه بسنجد؛ ادغام نهایی کور خواهد بود.

**اقدام لازم (فوری، از چت ۲):** پوشِ فوری شاخه + اعلام وضعیت هر بند.
تا آن لحظه، برنامهٔ ادغام فقط سه شاخهٔ موجود را می‌بیند.

---

## ۶. نقشهٔ تداخل شاخه‌ها (با `git merge-tree` سنجیده شد)

| جفت | تداخل متنی | توضیح |
|---|---|---|
| پایه × چت ۱ | ⚠️ `USER_GUIDE.html` | راهنما را هر دو افزوده‌اند؛ `HANDOFF.md` و `authz/*` خودکار حل می‌شوند |
| پایه × چت ۳ | ⚠️ `HANDOFF.md` + `USER_GUIDE.html` | هر دو ورودی بالای هانداف افزوده‌اند |
| چت ۱ × چت ۳ | ⚠️ `USER_GUIDE.html` | `19-actions-core.js` · `30-authz.js` · `index.html` خودکار حل شدند — **ولی نیازمند راستی‌آزمایی رفتاری پس از مرج** |

هیچ تداخلی در منطق هسته دیده نشد، ولی «حل خودکارِ متنی» برای فایل‌های مشترکِ
پراکشن (`19-actions-core.js`، `30-authz.js`) به‌معنای امنیت رفتاری نیست — پس از هر
مرج، اجرای کاملِ مجوزها + دودی روی درختِ مرج‌شده **اجباری** است.

**پیشنهاد ترتیب ادغام (پس از تأیید ناظر):**
1. پایه (اس.۰–اس.۵) → مین
2. چت ۱ (پی‌آر شمارهٔ ۸) → مین
3. چت ۳ → مین
4. چت ۲ → مین (پس از پوش و وارسی)
در هر گام: حل `USER_GUIDE.html` (حفظ هر دو فراخوانی/یادداشت)، سپس سه دروازه روی درخت مرج.

---

## ۷. ریز‌یافته‌ها (بدون اقدام فوری)

| # | یافته | شدت |
|---|---|---|
| ۱ | شمار اکشن‌ها ۳۳۸ شده ولی چند سند هنوز ۳۳۷ می‌گویند | جزئی — همگامی مستندات |
| ۲ | شرح پی‌آر شمارهٔ ۸ کهنه است (ب.۲/و.۱/و.۲ انجام‌شده ولی «باقی‌مانده»اند) | جزئی |
| ۳ | هشدار مُهرِ راهنما (قلم ۱ حسابرسی نهایی) اکنون روی مین سبز است — قابل بستن | جزئی |
| ۴ | چت ۱ در `0.5.23` دو دام ماندگار ثبت کرده (جریان `rng` نمونه + همگامی `NAV_EXPECT`) — همهٔ چت‌ها ملزم به خواندن | اطلاع |
| ۵ | کیفیت گزارش‌های چت ۳ شفاف است (حتی جهشِ زنده‌ماندهٔ موقت را خودش گزارش کرد) | مثبت |

---

## ۸. برنامهٔ فردا

1. پیگیری پوشِ چت ۲ و وارسی شاخه‌اش به محض ظهور.
2. پایش کامیت‌های تازهٔ چت ۳ (ای.۴–ای.۶، گ.۱/گ.۲) — دروازه + سنجش حضور روی هر کامیت.
3. آماده‌سازی گزارش یکپارچهٔ فرناز برای پایان فاز.
4. در صورت تأیید ناظر: اجرای ترتیب ادغام §۶.

---

*ابزار سنجش این گزارش: `tools/perf-attendance.js` — اجرا:
`PAYESH_ROOT=<مسیر> node --expose-gc tools/perf-attendance.js 5`*

---

## پیوستِ زنده — پایش لحظه‌ای کامیت تازهٔ چت ۳

حینِ تهیهٔ گزارش، کامیت `a9373f6` (ای.۴ — دانلودِ سراسریِ تقویم با اسکوپِ نقش)
روی شاخهٔ چت ۳ نشست. وارسیِ فوری طبقِ وظیفهٔ ۲:

| دروازه | نتیجه |
|---|---|
| `build --check` | خروجی ۰ ✅ |
| `check-authz` | خروجی ۰ ✅ |
| `tests/ics.js` (سوئیت خود بند) | ۲۲/۲۲ ✅ |
| `tests/smoke.js` | ۵۴۷/۵۴۷ ✅ |
| هارنس حضور (۵ اجرا) | میانهٔ ۶۵۸ مس — درون بازهٔ مین، **بدون رجعت** ✅ |

یادداشت: ای.۴ دقیقاً حوزهٔ حساسِ دور ۸۹ (نشتِ اسکوپ در خروجیِ تقویم) را لمس
می‌کند؛ سوئیت ۲۲-بخشیِ آن اسکوپِ نقش را می‌سنجد. وضعیت چت ۳: ای.۱ تا ای.۴ کامل،
ای.۵/ای.۶ و گ.۱/گ.۲ باقی.

---

## پیوست ۲ — پایش لحظه‌ای کامیت ای.۵ چت ۳ (`bd106c0`)

**بند:** «موجه اعلام کنم» کنار اعلان غیبت (ای.۵) — ۱۰ فایل، ۲۹۴+.

| دروازه | نتیجه |
|---|---|
| `build --check` | خروجی ۰ ✅ |
| `check-authz` | خروجی ۰ ✅ |
| `tests/excuse.js` | ۱۶/۱۶ ✅ |
| `tests/excuse-mutations.js` | ۵/۵ کشته ✅ |
| `tests/smoke.js` | ۵۴۷/۵۴۷ ✅ |

**سنجش کارایی (هارنس §۴، ایزولهٔ ای.۴ × ای.۵ در یک نشست):**

| سنجش | میانه | فاز ثبت |
|---|---|---|
| ای.۴ — باری ۱ | ۶۹۹ مس | ۱۰۵ |
| ای.۵ | ۶۹۷ مس | **۱۵۳** |
| ای.۴ — باری ۲ | ۶۱۱ مس | ۱۰۷ |

میانهٔ کل جریان عملاً بدون تغییر است (رجعت معنادار ندارد)، ولی **فاز ثبت
~۴۸ مس پرهزینه‌تر شد** — ریشهٔ اندازه‌گیری‌شده: ای.۵ در `att-commit` برای هر
دانش‌آموزِ غایب، اعلان درون‌برنامه‌ای به والدین می‌سازد (`absenceNotifFor`)؛
در سناریوی ۱۳ غایب یعنی ۱۳ درجِ اعلان + بررسی ضدتکرار. هزینهٔ مطلوبِ قابلیت
است، نه بی‌دقتی.

**نظارت مقیاس (قاعدهٔ ۸ سند مرجع):** ضدتکرارِ `absenceNotifFor` با `some()` روی
کل `db.notifications` می‌گردد — در کلاس ۱۳ نفره بی‌آزار است، ولی اگر اعلان‌ها
انبوه شوند الگوی جستجوی خطی داخل حلقه می‌شود. پیشنهاد برای آینده (نه اکنون):
نقشهٔ درهم بر کلید `ref` پیش از حلقه. ثبت شد؛ اقدام لازم نیست.

**وضعیت چت ۳:** ای.۱ تا ای.۵ کامل ✅ · ای.۶ + گ.۱/گ.۲ باقی.
**وضعیت چت ۲:** 🔴 هنوز هیچ شاخه‌ای روی اصل ندارد (دومین گزارشِ پیاپی).
**چت ۱:** استندبای — پی‌آر ۸ بدون کامیت تازه.

---

## پیوست ۳ — شبیه‌سازی مرج یکپارچه (رؤوسِ لحظه‌ای) + پایش کامیتِ مستنداتی چت ۱

### الف) کامیت تازهٔ چت ۱ (`3f1e7ee` — فقط مستندات)
گزارش تجمیعی نهایی فاز + قواعد فراچتی §۰.۵.۲۵ + ورودی هانداف. بررسی یکپارچگی:
- §۰.۵.۲۵ مکمل §۰.۵.۲۴ من است و صریحاً به آن ارجاع می‌دهد — **بدون تناقض**.
- قاعدهٔ مرج فاز تأیید شد: بازتولید `index.html` + `write-perms.json` در مرج (نه مرج دستی)؛
  ترتیب پایه ← چت ۱ ← چت ۳ ← چت ۲، منوط به تأیید ناظر ارشد.
- ⚠️ کهنگیِ جزئی: در گزارشِ چت ۱، ای.۵ «در حال اجرا» و مبنای چت ۳ `a9373f6` ثبت شده،
  درحالی‌که ای.۵ (`bd106c0`) پیش از این گزارش نشسته و در پیوست ۲ سبز شده است.
  همچنین دو کامیت نظارتی اخیر چت ۴ (پیوست‌ها) در شمارش نیامده. موردِ اقدام نیست —
  صرفاً ثبت برای گزارش نهایی.
- چون تغییر فقط مستندات است، دروازه‌ی کد لازم نبود.

### ب) شبیه‌سازی مرج یکپارچه (پایه + چت ۱ + چت ۳@ای.۵ روی مین)
در `/tmp` (بدون پوش — اصل هشتم: مرج واقعی فقط با تأیید ناظر):

| گام | نتیجه |
|---|---|
| مرج پایه | تمیز |
| مرج چت ۱ | فقط مُهر راهنما → حل شد |
| مرج چت ۳ | مُهر راهنما + ترتیب هانداف (هر دو ورودی حفظ شد) → حل شد |
| بازتولید بیلد/مجوزها/مُهر | `build` + `generate-write-perms` سبز |
| `build --check` روی درخت مرج | خروجی ۰ ✅ |
| `check-authz` روی درخت مرج | خروجی ۰ ✅ |
| دودی روی درخت مرج | **۵۴۷/۵۴۷** ✅ |
| هارنس حضور روی درخت مرج | میانهٔ ۷۲۴ مس (۶۶۲–۸۵۶) — درون بازه، بدون رجعت ✅ |

**نتیجه:** سه شاخهٔ موجود در ترتیب پیشنهادی **بدون تداخل رفتاری** مرج می‌شوند؛
تنها هزینه‌های حل، دو مُهر و یک ترتیبِ هانداف است (هر دو مکانیکی). پس از تأیید
ناظر ارشد، ادغام واقعی می‌تواند فوراً اجرا شود. ریسک باقی‌مانده برای ادغام:
فقط چت ۲ (نامرئی) و بندهای نانوشتهٔ چت ۳ (ای.۶، گ.۱/گ.۲).

### ج) وضعیت لحظه‌ای
| چت | وضعیت |
|---|---|
| چت ۱ | ✅ کامل + استندبای (پی‌آر ۸ در انتظار تصمیم ناظر) |
| چت ۲ | 🔴 سومین گزارش پیاپی — هیچ شاخه‌ای روی اصل نیست |
| چت ۳ | ای.۱–ای.۵ کامل و سبز · ای.۶/گ.۱/گ.۲ باقی |
| چت ۴ | نظارت فعال — این گزارش با ۳ پیوست |

---

## پیوست ۴ — پر کردن شکاف‌های آزمون (چرخهٔ چهارم) + ارزیابی موج ۲

### الف) اجرای کامل سوئیت‌های ویژگی (ادعاهای چت ۱ و پایه بازتولید شد)
تا پیش از این، فقط دودی/مجوزها/ساخت روی شاخه‌ها رفته بود. در این چرخه تک‌تک
سوئیت‌های ویژگی اجرا و با ادعاهای کامیت‌ها تطبیق داده شد:

| سوئیت (چت ۱ @ `3f1e7ee`) | نتیجهٔ مستقل | ادعای کامیت |
|---|---|---|
| `staffatt2` | ۱۰/۱۰ ✅ | ۱۰/۱۰ |
| `staffatt-mutations` | ۳/۳ کشته ✅ | ۳/۳ |
| `training2` | ۱۱/۱۱ ✅ | ۱۱/۱۱ |
| `training-mutations` | ۳/۳ کشته ✅ | ۳/۳ |
| `family2` | ۱۱/۱۱ ✅ | ۱۱/۱۱ |
| `tuition-exempt` + جهش | ۸/۸ + ۳/۳ ✅ | ۸/۸ + ۳/۳ |

| سوئیت (پایه @ `776de0e`) | نتیجهٔ مستقل | ادعای کامیت |
|---|---|---|
| `entry-gpa` | ۱۹/۱۹ ✅ | ۱۹/۱۹ |
| `entry-gpa-mutations` | ۶/۶ کشته ✅ | ۶/۶ |
| `dorm-kind` | ۱۷/۱۷ ✅ | ۱۷/۱۷ |
| `dorm-kind-mutations` | ۵/۵ کشته ✅ | ۵/۵ |

**صفر انحراف بین ادعاها و اجرای مستقل.** همهٔ گزارش‌های تیم قابل اتکا هستند.

### ب) 🪤 دام محیطی کشف‌شده (برای همهٔ چت‌ها)
اجرای نخستِ سوئیت‌های سروریِ پایه در ورک‌تریِ تازه با `FATAL ENOENT:
server/data/payesh.json` می‌مُرد (فاز کلاینت سبز، فاز سرور کرش) — این فایل
در `.gitignore` است و هر کلون/ورک‌تریِ تازه **باید پیش از سوئیت‌های سروری
`node server/seed.js` اجرا کند**. رانر رجیسیون خودترمیم است ولی اجرای دستی
نه. (سابقه: خودترمیمی رانر در دورهای ۹۲/۱۰۰.) ثبت شد؛ اقدام کد لازم نیست.

### ج) ارزیابی شاخهٔ `fix/arch-review-wave2` (قلم ۵ حسابرسی نهایی)
- مرجع مشترک با مین: `eddddfa` (۲۰۲۶-۰۹-۰۶) — **۱۴۶ کامیت عقب‌تر**.
- ۳ کامیت یکتا: دبلیو۱ فشردن دفترچه · دبلیو۲ جی‌سی سرور · دبلیو۳ کش رندر.
- 🔴 **یافتهٔ ساختاری:** دبلیو۳ روی `19-actions.js` قدیمی کار می‌کند که در
  فاز ۲ به پاره‌های `19-actions-*.js` شکسته شد — ریبیس مکانیکی ممکن نیست؛
  پورتِ دستی لازم است (و پورت با کشِ رندر، رفتار کارایی را عوض می‌کند ⇒
  سنجش با هارنس §۴ پس از پورت الزامی است).
- هم‌پوشانی فایل با شاخه‌های فرناز: صفر (به‌سبب همان تغییر نام) — تداخل
  مستقیم ندارد، ولی ادغامش وسط فاز فرناز ریسک بی‌مورد است.
- **توصیهٔ ناظر:** موج ۲ **پس از** ادغام کامل فاز فرناز، به‌صورت مجموعه‌تغییرِ
  پورت‌شدهٔ تازه (با تست + جهش + سنجش حضور) بیاید — نه اکنون.

### د) وضعیت لحظه‌ای (چهارمین وارسی)
- هیچ کامیت تازه‌ای در هیچ شاخه‌ای از وارسی پیش ننشسته است.
- 🔴 چت ۲: چهارمین گزارش پیاپیِ فقدان — بندهای ب.۳–ب.۵/ج/د/گ.۳ همچنان نامرئی.
- چت ۱: استندبای · چت ۳: احتمالاً مشغول ای.۶/گ.۱/گ.۲ (بدون پوش) · مین ثابت.
- هیچ اقدامی روی `main` لازم نیست؛ همه‌چیز برای تصمیم ناظر آماده است.

---

## پیوست ۵ — چرخهٔ پنجم: ای.۶ سبز شد · شاخهٔ تازهٔ بیرون از برنامه

### الف) چت ۳ — ای.۶ «یادداشت خصوصی ولی برای هر فرزند» (`ddcc02f`، رأس `b106c44`)
`19-actions-core.js` لمس شد ⇒ طبق قاعدهٔ §۰.۵.۲۴ سنجش حضور اجباری بود:

| دروازه | نتیجه |
|---|---|
| `build --check` | خروجی ۰ ✅ |
| `check-authz` | خروجی ۰ ✅ |
| `tests/pnote.js` | ۱۹/۱۹ ✅ |
| `tests/pnote-mutations.js` | ۵/۵ کشته ✅ |
| `tests/smoke.js` | ۵۴۷/۵۴۷ ✅ |
| هارنس حضور (۵ اجرا) | میانهٔ ۷۴۰ مس (۷۱۳–۸۵۲) — درون بازه، بدون رجعت ✅ |

گ.۱ (`b106c44`) فقط سند طراحی است (شاخص سلامت مدرسه) — بررسی شد، بی‌تعارض.
**وضعیت چت ۳:** ای.۱–ای.۶ کامل ✅ · گ.۱ کامل (طراحی) · فقط گ.۲ باقی است.

### ب) چت ۱ — کمک به دستهٔ د (`72392bf`، فقط مستندات + ابزار پیش‌نویس)
- `docs/D_OFFICE_LEVEL_ANALYSIS.md` + `tools/seed-office-data.js` (۴۸ خط).
- ابزار از نظر نحوی معتبر است (`node --check`)، **به هیچ‌جا سیم‌کشی نیست**،
  دادهٔ ساختگی (الگوی ۰۹۹۹) دارد و صریحاً «تصمیم نهایی با چت ۲» نوشته.
- ⚠️ نکتهٔ مالکیت: چت ۲ غایب است و چت ۱ دارد بخشی از دستهٔ د را پیش می‌برد.
  اگر چت ۲ بازگردد، باید خروجی چت ۱ را نقطهٔ شروع بگیرد — نه موازی‌کاری.

### ج) 🔶 شاخهٔ تازهٔ بیرون از برنامه: `arena/01a08256-p2`
یک نشستِ دیگر آرنا سند `AGENT_ONBOARDING.md` (۵۰۵ خط، تحلیل/حافظهٔ عامل،
فقط مستندات) را روی شاخه‌ای جدا پوش کرده است. محتوا بی‌خطر و تحلیلی است
(بررسی سرخطی شد؛ اعدادش با وضعیت فعلی سازگار است)، ولی این شاخه **جزو
برنامهٔ فرناز نیست** و برخلاف قاعدهٔ «هر چت فقط شاخهٔ خودش» ایجاد شده.
**تصمیم با ناظر:** ادغام به‌عنوان سند کمکی، یا بستن شاخه. هیچ اقدامی نکردم.

### د) سطح تعارض در رأس‌های تازه (بازبینی با `merge-tree`)
بدون هیچ تغییر نسبت به پیوست ۳ — فقط مُهر راهنما و ترتیب هانداف. طرح ادغام
همان است و معتبر ماند. شبیه‌سازی کاملِ پیوست ۳ (۵۴۷/۵۴۷ روی درخت مرج) با
ای.۶ سازگار است چون ای.۶ تعارض تازه‌ای نمی‌آفریند؛ در صورت تأیید ناظر،
مرج واقعی با همان ترتیب و بازتولید بیلد انجام می‌شود.

### هـ) وضعیت لحظه‌ای
| چت | وضعیت |
|---|---|
| چت ۱ | کامل + استندبای + کمک به دستهٔ د (پی‌آر ۸ @ `72392bf`) |
| چت ۲ | 🔴 **پنجمین** گزارش پیاپیِ فقدان |
| چت ۳ | فقط گ.۲ باقی است |
| چت ۴ | نظارت فعال — ۵ پیوست |
| مین | ثابت @ `4ca531c` — منتظر تصمیم ناظر برای ادغام |

---

## پیوست ۶ — چرخهٔ ششم: شبیه‌سازی مرجِ نهایی (با ای.۶) + حکمِ آمادگی

### الف) شبیه‌سازی یکپارچهٔ نسخهٔ ۲ — این‌بار با ای.۶ (اجرا، نه تحلیل)
ادعای پیوست ۵ («سازگاری ای.۶») فقط تحلیلی بود؛ در این چرخه **اجرا** شد:
مرج پایه ← چت ۱ ← چت ۳@`b106c44` روی مین، با همان دو حلِ مکانیکی (مُهر/هانداف)،
بازتولید بیلد+مجوزها، سپس:

| آزمون روی درخت مرج | نتیجه |
|---|---|
| `build --check` | خروجی ۰ ✅ |
| `check-authz` | خروجی ۰ ✅ |
| دودی | **۵۴۷/۵۴۷** ✅ |
| هارنس حضور (۵ اجرا) | میانهٔ ۶۸۹ مس (۶۵۵–۷۶۴) — بدون رجعت ✅ |
| `pnote` (ای.۶) | ۱۹/۱۹ ✅ |
| `ics` (ای.۴) | ۲۲/۲۲ ✅ |
| `staffatt2` (ب.۱) | ۱۰/۱۰ ✅ |
| `training2` (ب.۲) | ۱۱/۱۱ ✅ |
| `entry-gpa` (اس.۴) | ۱۹/۱۹ ✅ |
| `dorm-kind` (اس.۵) | ۱۷/۱۷ ✅ |

**هر شش قابلیتِ سه شاخه روی درختِ یکپارچه زنده ماندند.** این قوی‌ترین
شواهدِ موجود برای ادغام است.

### ب) `tests/api` روی مین — قلم ۳ حسابرسی نهایی
پس از سید: **۷/۷ سوئیت سبز** ✅ — رانر سالم است؛ فقط در
`run-all-tests.sh` سیم‌کشی نیست (تغییرِ نیازمند تأیید؛ گزارش شد، اقدام نکردم).

### ج) 🪤 دام سید — سومین وقوع، این‌بار سیستماتیک ثبت می‌شود
سوئیت‌های سروری (پایه/چت۱/ای‌پی‌آی) در **هر** درخت تازه بدون
`server/data/payesh.json` می‌میرند (`ENOENT copyfile`). امروز سه‌جا دیده شد.
**قاعدهٔ الزامی برای همهٔ چت‌ها:** در هر کلون/ورک‌تری تازه، پیش از هر سوئیتِ
سروری: `node server/seed.js`. رانر رجیسیون خودترمیم دارد؛ اجرای دستی ندارد.

### د) حکمِ آمادگیِ ادغام (Go/No-Go) برای ناظر ارشد
| معیار | وضعیت |
|---|---|
| دروازه‌ها روی تک‌تک شاخه‌ها | ✅ سبز (۴ شاخه) |
| شبیه‌سازی مرج + دروازه روی درخت مرج | ✅ سبز (نسخهٔ ۲، با ای.۶) |
| رجعت کارایی | ✅ ندارد (همه در بازهٔ مین) |
| تعارض‌ها | ✅ فقط مکانیکی (مُهر/هانداف) |
| **حکم** | 🟢 **آمادهٔ ادغام** — به‌شرطِ: (۱) تأیید صریح ناظر، (۲) تصمیم دربارهٔ چت ۲ (صبر یا حذف از فاز)، (۳) تصمیم دربارهٔ شاخهٔ آرنا |
| تنها کار فنی باقی‌ماندهٔ چت ۳ | گ.۲ |

### هـ) وضعیت لحظه‌ای (ششمین وارسی)
هیچ کامیت تازه‌ای ننشسته. چت ۲: ششمین فقدان پیاپی. مین ثابت.
فاز در آستانهٔ تکمیل است — منتظر دستور ناظر برای ادغام یا ادامهٔ انتظار.

---

## پیوست ۷ — چرخهٔ هفتم: وارسی آرنا + فهرست یکپارچهٔ تصمیم‌های ناظر

### الف) کامیت تازهٔ شاخهٔ آرنا (`a965137` — فقط مستندات)
افزوده‌ها: بخش «نقطهٔ شروع» به `README.md` و `reza/README.md` + سند تازهٔ
`reza/درک_عمیق_سامانه_پایش.md` (۳۲۸ خط). بررسی ناظر:
- **هیچ کدی لمس نشده** (فقط سه فایل متنی) — دروازهٔ کد لازم نیست.
- هر دو تغییر روی فایل‌های مشترک **افزودنیِ خالص** است (چیزی حذف/بازنویسی نشده).
- ارجاع‌ها به فایل‌های موجود راستی‌آزمایی شد؛ هشدار «نسخهٔ دمو» با
  `TODO_BEFORE_PRODUCTION.md` هم‌خوان است.
- ⚠️ سطح ریسک از «شاخهٔ جدا» به «ویرایش فایل مشترک» ارتقا یافت — هنوز بی‌خطر،
  ولی تصمیم دربارهٔ سرنوشتش هرچه زودتر بهتر است (بند د.۳).

### ب) وضعیت تیم (هفتمین وارسی — بدون تغییر نسبت به پیوست ۶)
چت ۱: استندبای · چت ۲: **هفتمین** فقدان پیاپی · چت ۳: فقط گ.۲ باقی (بدون پوش) ·
مین ثابت · پی‌آر ۸ پیش‌نویس.

### ج) ⭐ فهرست یکپارچهٔ تصمیم‌های در انتظار ناظر ارشد
تا این تصمیم‌ها نرسد، فاز در حالت «آماده ولی متوقف» می‌ماند:

| # | تصمیم | ورودی ناظر لازم | توصیهٔ چت ۴ |
|---|---|---|---|
| د.۱ | **ادغام فاز** (پایه ← چت۱ ← چت۳) | تأیید صریح (اصل هشتم) | 🟢 حکمِ «برو» در پیوست ۶ صادر شد — همهٔ شواهد سبز |
| د.۲ | **چت ۲** (ب.۳–ب.۵/ج/د/گ.۳ نامرئی) | صبر، حذف از فاز، یا واگذاری | واگذاری به چت ۱ (که هم‌اکنون دستهٔ د را کمک می‌کند) |
| د.۳ | شاخهٔ آرنا (`arena/01a08256-p2`) | ادغام به‌عنوان سند، یا بستن | ادغام جدا پس از فاز — محتوایش سالم و افزودنی است |
| د.۴ | هدف «زیر ۶۰۰ مس» ثبت حضور | تأیید + تعیین مجری | رندرِ جزئی در `att-set`؛ برآورد ۲۵۰–۳۵۰ مس؛ پس از ادغام |
| د.۵ | سیم‌کشی `tests/api` به رگرسیون | تأیید (قلم ۳ حسابرسی) | یک خط در `run-all-tests.sh` — کم‌ریسک |
| د.۶ | شاخهٔ موج ۲ (`fix/arch-review-wave2`) | رد یا تأیید توصیه | پورت دستی **پس از** فاز (پیوست ۴) |

### د) اقدام‌های خودمختار چت ۴ در این چرخه
هیچ — در نبود کامیت تازه، فقط وارسی و این گزارش انجام شد. درخت کاری تمیز،
همه‌چیز روی اصل پوش است.

---

## پیوست ۸ — چرخهٔ هشتم: رگرسیونِ کامل روی درخت مرج + ریشه‌یابی یک فلیک

### الف) رگرسیون کامل (رانر رسمی) روی درختِ مرج — قوی‌ترین شواهد تا امروز
تا این لحظه از اجرا (رانر هنوز در حال اتمام ۸ سوئیت پایانیِ سریالِ سروری):

| شاخص | مقدار |
|---|---|
| سوئیت‌های اجراشده | ۱۶۸ از ۱۷۶ |
| سبز | **۱۶۷** |
| قرمز | **۱** — فقط `ics` (ریشه‌یابی در بند ب) |

⚠️ معیارِ اولیهٔ من (جست‌وجوی `❌` در لاگ) ۶۱ قرمزِ کاذب نشان می‌داد — سئوت‌ها
در خروجیِ سبزِ خود علامتِ `❌ 0` چاپ می‌کنند. معیارِ معتبر، خط‌های `RED` خودِ
رانر است: فقط ۱ مورد.

### ب) 🔬 ریشه‌یابی قرمزِ `ics` (T12a) — فلیکِ سمت تست، نه رجعت محصول
- تست، خروجی دانلود را با ساختِ **دوبارهٔ** `icsBuild()` مقایسه می‌کند؛ هر رویداد
  `DTSTAMP` لحظهٔ ساخت دارد (`20-communication-finance.js:389/402`) — اگر دو ساخت
  در دو ثانیهٔ متفاوت بیفتند، مقایسه نامعتبر می‌شود (شرط مرزی زیر یک ثانیه).
- شواهد: در پیوست ۶ همین درخت ۲۲/۲۲ سبز بود · الان ۳ اجرای پیاپیِ جدا هم
  **۲۲/۲۲ سبز** است · شکست فقط گاه‌به‌گاه و وابسته به شانسِ مرزِ ثانیه است.
- هم‌خانواده با فلیک‌های ثبت‌شدهٔ §۵.۲ (پنجره‌های زمانی در تست).
- **رفع پیشنهادی (برای چت ۳، با تأیید ناظر):** تزریق زمانِ ثابت به `icsBuild` در
  تست، یا مقایسه با حذف خط‌های `DTSTAMP`. اقدام نکردم (اصل هشتم + تفکیک شاخه‌ها).

### ج) اسکن رازها روی مین: **۱۱/۱۱ سبز** ✅

### د) جمع‌بندی حکمِ د.۱ (ادغام)
رگرسیونِ کامل، آخرین حلقهٔ مفقودِ شواهد بود: درختِ مرج نه‌تنها دودی/مجوز/ساخت،
که **کل ۱۷۶ سوئیت** را با یک فلیکِ تستیِ ریشه‌یابی‌شده تحمل می‌کند. حکمِ «برو»
بدون تغییر و با اطمینانِ بیشتر باقی است. جمع‌بندی نهایی ۸ سوئیتِ باقی‌مانده در
چرخهٔ بعد ثبت می‌شود (همه از سوئیت‌های سروریِ سبزِ شناخته‌شده روی مین‌اند).

---

## پیوست ۹ — چرخهٔ نهم: ادغام در مین رخ داد + وارسی مینِ تازه

### الف) 🎉 تصمیم ناظر اجرا شد — دو مرج روی مین
| پی‌آر | محتوا | کامیت مرج |
|---|---|---|
| #9 | شاخهٔ چت ۴ (هارنس سنجش + گزارش روزانه + اسناد) | `d941e8d` |
| #10 | شاخهٔ چت ۳ (ای.۱–ای.۶ + گ.۱ + **گ.۲ تیکت پشتیبانی** `d4238bd`) | `6961b72` |

ترتیب واقعی با توصیهٔ من فرق داشت (چت۳ پیش از پایه/چت۱ مرج شد) — بی‌ضرر،
چون سازگاری هر دو قبلاً در شبیه‌سازی‌ها سنجیده شده بود.

### ب) وارسی کاملِ مینِ تازه (`6961b72`) — وظیفهٔ ۲ (پس از هر مرج)
| دروازه | نتیجه |
|---|---|
| `build --check` | خروجی ۰ ✅ |
| `check-authz` | خروجی ۰ ✅ |
| `tests/tickets.js` (سوئیت گ.۲) | **۲۰/۲۰** ✅ |
| `tests/tickets-mutations.js` | ۵/۵ کشته ✅ |
| `tests/smoke.js` | **۵۴۷/۵۴۷** ✅ |
| هارنس حضور (۵ اجرا) | میانهٔ ۷۸۱ مس (۷۱۱–۸۴۹) — درون بازهٔ خط‌مبنا ✅ |

گ.۲ روی مین سالم است؛ هیچ رجعتی دیده نشد. فاز ثبتِ حضور با وجود ای.۵/ای.۶/گ.۲
همچنان در بازهٔ تاریخی است.

### ج) حرکت‌های تازهٔ شاخه‌ها
- **چت ۳ (`7c5540d`):** پیش‌نویس‌های دستهٔ د (د.۲/د.۳/د.۴) — فقط مستندات.
  ⚠️ نکتهٔ هماهنگی: **چت ۱ و چت ۳ هر دو** در غیاب چت ۲ روی دستهٔ د کار
  می‌کنند (بند د.۲ فهرست تصمیم‌ها) — خطر موازی‌کاری؛ یک‌کاسه‌سازی با نظر ناظر.
- **آرنا (`2523b13`):** سه گزارش نشست (فقط مستندات، افزودنی) — همچنان بی‌خطر،
  همچنان بیرون از برنامه، منتظر تصمیم د.۳.
- **چت ۲:** 🔴 نهمین فقدان پیاپی.
- **پی‌آر ۸ (چت ۱) + شاخهٔ پایه:** هنوز مرج نشده‌اند — حکمِ «برو»ی پیوست ۶
  برای هر دو برقرار است (شواهد: پیوست ۳/۴/۶ + رگرسیون در حال اجرا).

### د) رگرسیون شبیه‌سازی (پایه+چت۱+چت۳ روی مین قدیم) — وضعیت لحظه‌ای
۱۷۲ از ۱۷۶ سوئیت · ۱۷۱ سبز · ۱ قرمز (فلیک ics پیوست ۸) · ۴ سوئیت سروریِ
سریال باقی. جمع‌بندی نهایی در چرخهٔ بعد. این رگرسیون همچنان مدرکِ ادغامِ
پایه و چت ۱ است (که هنوز مرج نشده‌اند).
