# گزارشِ گامِ ۵ — نقصِ ۵: ناسازگاریِ پشتیبان با اسنپ‌شات (P2)

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08) · **برنچ:** `fix/behavior-findings`
**کامیتِ این گام:** (پایین، پس از ثبت)

---

## ۱. ریشهٔ دوگانهٔ نقص

پس از فشرده‌سازیِ دفترچه (`t:'snap'` + دم در `log`)، پشتیبان‌گیری/بازیابی
از دو جا می‌شکست:
1. `validateBackup` اسنپ‌شات را «ناقص» می‌دانست (بی `c` + نوعِ ناشناخته)
   ⇒ **هر پشتیبانِ پس‌از-فشرده‌سازی بازیابی‌ناپذیر بود** (متنِ نقص).
2. `restoreBackup` با `applyOp`ِ خام بازپخش می‌کرد که `t:'snap'` را
   نمی‌شناسد ⇒ حتی با دورزدنِ اعتبارسنجی، وضعیتِ فشرده‌شده برنمی‌گشت.
3. **یافتهٔ امنیتیِ حینِ کار (P0):** اسنپ‌شات رکوردِ کاملِ کاربر — با
   **رمزِ متن‌ساده** — را حمل می‌کند و رداکشنِ موجود فقط opهایِ `users`
   را پوشش می‌داد ⇒ فایلِ v3 بدونِ اقدام، قاعدهٔ پنتست («رمز هرگز در
   فایلِ دانلود») را نقض می‌کرد. در همین گام بسته شد (پنهان‌سازی نشد).

## ۲. رفع (`src/js/38-plans-backup.js` — ۵ جایگزینیِ دقیق)

- **فرمتِ v3:** `buildBackup().version` ‏۲ ← ۳ (یعنی «دفترچهٔ دارایِ
  اسنپ‌شات»)؛ دروازهٔ نسخه در `validateBackup` ‏`>2` ← ‏`>3` (فایل‌هایِ
  v1/v2 همچنان پذیرفته می‌شوند — سازگاریِ عقب‌رو).
- **پذیرشِ `t:'snap'`** با بررسیِ شکل (`db` آبجکت + `ids` آبجکت/ناموجود)؛
  بقیهٔ قواعد (مجموعهٔ شناخته‌شده و…) بی‌تغییر.
- **بازپخش با `applyLog`ِ بوت** به‌جایِ `applyOp`ِ خام: اسنپ‌شات + دم +
  پرشِ `__a` + شمارنده‌هایِ تازهٔ حجم (این آخری یک کهنگیِ ضمنیِ پیشین را
  هم درست کرد). مدیریتِ `SYNC_MUTED` با خودِ `applyLog` است.
- **رداکشنِ رمز در اسنپ‌شات:** `db.users[].password` ← ‏`'[redacted]'` —
  همان قاعدهٔ opها. (تبادلِ ازپیش‌موجود: بازیابی، رمزِ رداکت‌شده را
  برمی‌گرداند؛ رفتارِ v2 برایِ opها هم همین بود.)

## ۳. آزمون‌ها

- **تازه:** `tests/backup-snap.js` (فشرده‌سازیِ واقعی با آستانهٔ شمارشی):
  S1 نسخهٔ ۳ + validate سبز · S2 رفت‌وبرگشت از serialize (رکوردِ A فقط از
  مسیرِ اسنپ‌شات — opاش از دم حذف شد تا اثبات شود — + رکوردِ B از دم +
  شمارنده‌هایِ تازه) · S3 امنیت (نبودِ رمز در blob + رداکت پس از بازیابی) ·
  S4 فایلِ دستیِ v2 (اعتبار + بازیابی) · S5 پنج منفی. نتیجه: **18/18** ✅
- **جهش:** `tests/backup-snap-mutations.js` — M1 ردِ اسنپ‌شات ← S1 · M2
  بازپخشِ خام ← S2 · M3 نشتِ رمز ← S3 · M4 نسخهٔ ۲ ← S1 · M5 پذیرشِ
  اسنپ‌شاتِ ناقص ← S5. نتیجه: **5/5 کشته، خطِ پایه سبز، ۰ خطایِ محیطی** ✅

## ۴. دروازهٔ این گام

- [x] `node build.js --check` سبز (authz بی‌تغییر: تطبیقِ کامل)
- [x] `node tests/smoke.js` سبز (**547/547**) — پس از یک ریشه‌یابی (پایین)
- [x] سئوتِ تازه **18/18** + جهش‌ها **5/5**
- [x] مرتبط‌ها: `security` سبز (هر دو نگهبانِ نشتِ پشتیبان) · `compact`
      ‏۱۹/۱۹ · `run.js` ‏۳۴/۳۴
- **ریشه‌یابیِ smoke:** نخست ۵۴۶/۵۴۷ شد («بستهٔ پشتیبان ساختار درست
  دارد») — علت: ادعایِ `version === 2` که با bumpِ دستوریِ نسخه ناسازگار
  بود. smoke (تست، نه محصول) به `=== 3` به‌روز شد و سبز شد. هیچ رفتارِ
  محصولی برایِ سبزکردن دست‌کاری نشد.

**فایل‌هایِ این کامیت:** `src/js/38-plans-backup.js` + `tests/smoke.js` +
‏۲ سئوتِ تازه + `index.html`/`USER_GUIDE.html` + این گزارش.

**گامِ بعد:** گامِ ۶ — نقصِ ۶ (P2، تداخلِ generateP12).
