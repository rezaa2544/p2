# گزارشِ دورِ ۹۷ — بند ۲.۷: نگهبانِ شمردنِ شناسه (enumeration guard) در سطحِ روتر

**تاریخ:** ۱۸/۰۶/۱۴۰ (2026-09-08)
**وضعیت:** ✅ بسته — رجیسیونِ کامل: `TODO:REGRESSION`
**کامیت:** `TODO:COMMIT`

## مسئله

سیاستِ بند ۲.۷ (TODO_BEFORE_PRODUCTION): «کسی که ۵٬۰۰۰ بار 404 گرفته در حال
نقشه‌برداری است. پاسخ تدریجی: ثبت ⇒ تأخیر ۵۰۰ms ⇒ تأخیر ۲s + هشدار ⇒ ابطالِ
نشست. قطعِ فوریِ پیشین نه.»

وضعیتِ پیش از R97: فقط `server/idor.js` (GET /api/students/:id) نگهبانِ
خودش را داشت (شمارِ **درخواست‌ها** در پنجرهٔ ۶۰s + تأخیرِ تدریجی؛ بدونِ
هشدارِ آدیتِ منظم و بدونِ ابطال). بقیهٔ سطحِ /api/* (sync با 403ِ
out_of_scope، مسیرهایِ ناشناس با 404، bell/conflicts) هیچ نگهبانی نداشت.

## انجام شد

**`server/index.js` — نگهبان در سطحِ روتر (یک منبعِ حقیقت):**
- شمارِ **رد‌ها** (نه همهٔ درخواست‌ها): هر پاسخ 401/403/404 به هر نشست
  (jti) در پنجرهٔ ۱۰ دقیقه — از نقطهٔ گذرِ واحدِ `sendJsonCounting` که
  همهٔ ماژول‌هایِ /api/* (sync/idor/bell/admin/sms/conflicts) با آن می‌چرخند.
- چهار مرحله (env، پیش‌فرض):

| مرحله | پیش‌فرض | اثر |
|---|---|---|
| `PAYESH_ENUM_WARN` | ۲۰ | آدیت `enum_warn` |
| `PAYESH_ENUM_SLOW1` | ۱۰۰ | تأخیرِ ۵۰۰ms روی درخواست‌هایِ بعدیِ همان نشست |
| `PAYESH_ENUM_SLOW2` | ۵۰۰ | تأخیرِ ۲s |
| `PAYESH_ENUM_REVOKE` | ۲۰۰ | **ابطالِ نشست** (jti به `__revoked_jti`) + آدیت `enum_revoke` — ادامه = 401 |

- `/api/auth/*` مستثنی است (cooldown و سقف‌هایِ OTP سقفِ خودشان را می‌سازند؛
  R96). شمارنده در حافظه (مثلِ rate-limitها) — **persist نمی‌شود**.
- تأخیر در ابتدایِ درخواست اعمال می‌شود (تنهٔ مهاجم، نه همهٔ کاربران).
- **حذفِ نگهبانِ تکیِ idor.js** — دو لایهٔ تکراری می‌ماند (درسِ R96
  P0-1: یک منبعِ حقیقت). 404هایِ /api/students/:id حالا توسطِ روتر
  شمرده می‌شوند.

## آزمون — `tests/server17.js` بخشِ E (E0–E6)

با آستانه‌هایِ صریحِ پایین (warn=5/slow1=8/slow2=12/revoke=15):
رد‌هایِ کم → بدونِ هشدار · n=5 → `enum_warn` در آدیت · n=8 → درخواستِ
بعدی ≥500ms (سنجیده شد) · n=12 → ≥2s (سنجیده شد) · n=15 → نشست ابطال
(`/me` = 401) · آدیت: `enum_slow`/`enum_slow2`/`enum_revoke` ثبت‌اند.

## باقی‌مانده (پس از R97)

- تصمیمِ **UUID** برایِ شناسه‌ها (بند ۲.۷) — هنوز پیش از نخستین استقرارِ
  واقعی باید گرفته شود (مهاجرتِ بعدی سنگین است).
- بک‌لاگِ R96 دست‌نخورده: read-sync pull/bootstrap (تصمیمِ محصول/عقدهٔ داده)،
  ایندکسِ id، قیدِ مالکیتِ `del`، SameSite Strict (تعادلِ UX).

**نتیجهٔ رجیسیونِ کامل:** `TODO:REGRESSION`
