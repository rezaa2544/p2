# گزارشِ چت ۱ — برنامهٔ عملیاتی امنیتی (GPT-5.6 Sol) — ۱۹/۰۶/۱۴۰۵ (2026-09-09)

> شاخه: `arena/01a0827b-p2` · پوش شده · درخت تمیز · کاملاً مستنداتی (صفر تغییر در کد).

## ۱. جدولِ کامیت‌ها

| # | موضوع | کامیت | پیام |
|---|---|---|---|
| ۱ | وظیفه (پیامِ دقیقِ درخواستی) | `a2248e4` | `docs: add security action plan based on GPT-5.6 Sol analysis` |
| ۲ | همین گزارش | (کامیتِ گزارش) | `docs: add chat 1 security plan report` |

## ۲. چه ساخته شد

- `docs/SECURITY_ACTION_PLAN.md` (تازه، کامل و قابلِ اجرا): §۱.۱ خلاصهٔ تحلیلِ Sol +
  راستی‌آزماییِ درختیِ هر ادعا (با فایل:سطر) + §۱.۲ چهار اقدامِ اولویت‌بندی‌شده با
  گام‌هایِ اجرایی و معیارِ خروج + §۱.۳ جدولِ ابزارها (۴ سطرِ درخواستی + ۲ سطرِ موجودِ
  ثبت‌شده) + §۱.۴ زمان‌بندیِ ۳هفته + §۱.۵ معیارهایِ موفقیت با خطِ پایهٔ امروز +
  §۱.۶ نگاشت به اسنادِ موجود (قرارداد، R96/R98/R99، ران‌بوک‌ها، SAST).

## ۳. اصلِ حاکم: زمین‌سازی، نه وانمود

- **فایلِ مستقلی از تحلیلِ Sol در ریپو نبود** (جست‌وجویِ کلِ درخت منفی) — این در سربرگِ
  سند صریحاً ثبت شد: اولویت‌ها از brief، و همهٔ «موجود/نقص»ها از درخت.
- **تصحیحِ یک ادعایِ brief:** ‏pgcrypto‏ در درخت نیست (هیچ `pgcrypto`/`pgp_*` در
  `migrations/` و `server/`؛ فقط digestهایِ Node) — به‌جایِ تکرارِ «نقطهٔ قوت»،
  «یافت نشد» ثبت و تصمیمِ پذیرش/حذف به اقدامِ ۱ سپرده شد.
- **۷ ضعفِ آدرس‌دار (W1–W7)** از کد استخراج شد، نه از حدس: نبودِ توکنِ CSRF
  (`server/auth.js:113`، فقط SameSite=Lax)؛ درون‌یابیِ `${col}` در `db.js`
  (مهارشده با گیتِ `sync.js:119-120` ولی بدونِ دفاع‌درعمق)؛ نگهداشتِ ردیابی‌نشدهٔ
  `password` رویِ users (هدفِ رأیِ ممیزی، نه ادعایِ آسیب‌پذیری)؛ `jwt.key` رویِ
  دیسک؛ نبودِ THREAT_MODEL؛ Zero Trust ناقص (بک‌لاگِ R96)؛ نبودِ ابزارِ pentest.
- از پیشنهادِ کارِ تکراری پرهیز شد: قوت‌هایِ R96/R98/R99/ESLint عیناً بازنویسی
  نشدند، بلکه به‌عنوانِ «موجودِ تأییدشده» به اقدام‌ها نگاشت شدند.

## ۴. راستی‌آزمایی

| بررسی | نتیجه |
|---|---|
| `node tests/smoke.js` | ‏۵۴۷/۵۴۷‏ ✅ |
| `node tools/check-authz.js` | ‏۰‏ (تطبیق کامل) ✅ |
| `node tests/secret-scan.js` | ‏۱۱/۱۱‏ ✅ |
| `npm run lint` | ‏۰ خطا‏ ✅ |

## ۵. Follow-upها

1. اگر فایلِ اصلیِ تحلیلِ Sol موجود است، در ریپو (مثلاً `docs/`) قرار گیرد تا نسخه‌هایِ بعدیِ
   برنامه مستقیم به آن ارجاع دهند.
2. اقدامِ ۱ با رأیِ W3 شروع شود (ابهامِ `password` رویِ users — بالاترین ارزشِ اطلاعاتی).
3. تصمیمِ pgcrypto (پذیرش برایِ رمزنگاریِ at-rest یا حذفِ رسمی از نقشه) در ممیزی ثبت شود.
