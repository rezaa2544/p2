# گزارشِ چت ۱ — Runbooks عملیات و بازیابی (Tier 3، اولویت ۱۴) — ۱۹/۰۶/۱۴۰۵ (2026-09-09)

> شاخه: `arena/01a0827b-p2` · پوش شده · درخت تمیز · کاملاً مستنداتی (صفر تغییر در کد).

## ۱. جدولِ کامیت‌ها

| # | موضوع | کامیت | پیام |
|---|---|---|---|
| ۱ | وظیفه (پیامِ دقیقِ درخواستی) | `9bcb3af` | `docs: add operational runbooks` |
| ۲ | همین گزارش | (کامیتِ گزارش) | `docs: add chat 1 runbooks report` |

## ۲. چه ساخته شد

- `docs/RUNBOOKS.md` (تازه، کامل و قابلِ اجرا): §۰ شدت‌بندی و مسیرهایِ مشترک +
  §۱-۱ استقرار (پیش‌چک‌ها، تک‌باکس، ملی، سه مسیرِ رولبک) + §۱-۲ عیب‌یابی
  (latency ای‌پی‌آی، CPU دیتابیس در هر دو مسیرِ JSON/PG، OOM ردیس، backlog صفِ sync) +
  §۱-۳ بازیابی (JSON، ‏PG/PITR‏، ‏RDB/AOF‏، بازیابیِ کامل با هدفِ RTO) +
  §۱-۴ مانیتورینگ (موجودیِ امروز + جدولِ آستانه‌ها + کرونِ حداقلی) +
  §۱-۵ امنیت (چرخشِ کلید، revoke، پاسخ به breach) + §۲ برنامهٔ تمرین‌ها.

## ۳. اصلِ حاکم: زمین‌سازی، نه وانمود

هر رویه به مکانیکِ واقعیِ ریپو گره خورد (ایونت‌هایِ `sync_*` در آدیت، پولِ `server/db.js`،
fallback ردیس، dead-letter، بکاپِ superadmin، اثرِ چرخشِ `jwt.key`) و هرچه نیست صریحاً
«شکافِ ثبت‌شده» شد: نبودِ revoke مدیریتی (با رویهٔ break-glass)، نبودِ `/metrics`،
Tracing/Logging در دستِ چتِ ۳/۲، نبودِ فایلِ alert در ریپو. سیاستِ RTO/RPO از
`RELIABILITY_DR_PLAN.md` به ارث رسید، نه بازتعریف.

## ۴. راستی‌آزمایی

| بررسی | نتیجه |
|---|---|
| `node tests/smoke.js` | ‏۵۴۷/۵۴۷‏ ✅ |
| `node tools/check-authz.js` | ‏۰‏ ✅ |
| `node tools/secret-scan.js` | ‏۱۱/۱۱‏ ✅ |

## ۵. Follow-upها

1. ای‌پی‌آیِ `POST /api/admin/revoke` (درمانِ ریشه‌ایِ شکافِ revoke تکی).
2. ترجمهٔ جدولِ آستانه‌هایِ §۱-۴ به `prometheus/alert.rules` پس از ثمرِ چتِ ۲/۳.
3. نخستین drill ماهانهٔ بازیابی رویِ staging + ثبتِ RTO واقعی.
