# مواردِ بازِ پایش — با اولویت، شاهد و پیشنهاد

**تاریخ:** ۲۰۲۶-۰۹-۰۸ · **شاخه:** `arena/01a08256-p2` · **کامیتِ مبنا:** `4ca531c`
**روش:** هر ردیف از خواندنِ مستقیمِ کد/تست/مستندات استخراج شده؛ ستونِ «شاهد»
نشانیِ فایل است. چیزی که دیده نشد، «دیده نشد» نوشته شده — بدون ادعایِ بی‌سند.

---

## ۰. خلاصهٔ یک‌نمایی

| ردیف | اولویت | تعداد |
|---|---|---|
| 🔴 مسدودکنندهٔ محصول | تصمیمِ شما می‌خواهد، نه کد | ۲ |
| 🟠 پیش از استقرارِ واقعی | معماری/امنیت | ۴ |
| 🟡 کیفیت و بهداشتِ مخزن | تست/رفتار | ۵ |
| ⚪ تصمیمِ راهبردیِ زودهنگام | مهاجرت بعدی سنگین است | ۱ |

---

## ۱. 🔴 مسدودکنندهٔ محصول (تصمیم، نه کد)

### ۱.۱ پرداختِ والد
* **شاهد:** `R88b` / `R90` در گزارش‌ها؛ نبودِ هرگونه درگاه در `server/`؛
  `parent_subscriptions` و `subscription_payments` در مدل
  (`authz/model.json`) و در `src/js/23-subscription.js`.
* **وضعیت:** تنها موردِ بازِ کلِ پروژه (گزارشِ R96). اشتراک در کلاینت
  شبیه‌سازی می‌شود و مدیر آن را دستی تغییر می‌دهد.
* **اثر:** بدون آن، درآمدِ B2C عملاً صفر است و paywallِ ولی فقط نمایشی است.
* **پیشنهاد:** تعیینِ مدل (درگاهِ داخلی / کیف پول / ثبتِ دستی توسط مدیر) و
  سپس یک endpointِ ایدمپوتنت با ثبت در آدیت.

### ۱.۲ تنشِ احتمالیِ paywall با بندِ ۰.۴
* **شاهد:** `docs/ROADMAP_FINAL_SCAN_2026-09-08.md` §۳؛
  `PARENT_FREE_ROUTES` در `src/js/07-shell.js` (اشتراک/اعلان/اطلاعیه).
* **وضعیت:** بندِ ۰.۴ می‌گوید «دادهٔ پایه (نمره/حضور/برنامه) هرگز پولی نشود»،
  اما امروز **نمره و حضور پشتِ قفلِ اشتراکِ ولی هستند**.
* **اثر:** یا نقضِ یک قفلِ محصولی است، یا تعریفِ «دادهٔ پایه» در AD ۶۵
  محدودتر از متنِ تحقیق است.
* **پیشنهاد:** یک جمله از شما کافی است: «کارتِ ولی رایگان شاملِ نمره و حضور
  هست یا نه؟» بر اساس پاسخ، یا `PARENT_FREE_ROUTES` گسترش می‌یابد یا بندِ
  ۰٫۴ بازنویسی می‌شود. کد تا پیش از پاسخ تغییر نکند.

---

## ۲. 🟠 پیش از استقرارِ واقعی

### ۲.۱ سروینگ روی فایلِ JSON (نه پایگاهِ داده)
* **شاهد:** `server/index.js` (store = `server/data/payesh.json`)؛
  `server/db.js` آماده با `DATABASE_URL`، اما مسیرِ درخواست‌ها از آن نمی‌گذرد؛
  `FINAL_VERIFICATION_REPORT.md` بند B1 آن را «sidecar» نامیده است.
* **اثر:** برای پایلوت کافی است؛ برای چند نود یا چند هزار مدرسه نه —
  هر نوشتن، کل فایل را بازنویسی می‌کند و نوشتنِ هم‌زمان امن نیست.
* **پیشنهاد:** پیش از استقرار، خواندن/نوشتنِ مستقیم به PG با تراکنش
  (`db.transaction`)؛ فایل فقط برای بکاپ بماند.

### ۲.۲ سقفِ نرخِ ورودِ اکسل
* **شاهد:** `TODO_BEFORE_PRODUCTION.md` §۲.۲ (سطرِ «⚠️ سقفِ نرخِ ورودِ اکسل
  هنوز نیست»)؛ `src/js/34-excel-import.js`؛ سقف‌های موجود فقط در
  `server/auth.js`.
* **اثر:** یک کاربرِ معتبر می‌تواند با وارداتِ پیاپی، هزاران عملیات در صف
  بسازد (سقفِ ۵۰۰ در هر دسته، اما تعدادِ دسته نامحدود).
* **پیشنهاد:** پنجره‌ی لغزانِ «عملیاتِ پذیرفته‌شده به‌ازای نشست» در
  `server/sync.js` (نه فقط در endpoint) + آدیت.

### ۲.۳ `cache.checkRateLimit` بی‌مصرف‌کننده
* **شاهد:** `server/cache.js:128`؛ هیچ فراخوانی بیرون از این فایل
  (`grep -rn checkRateLimit server/` = تعریف فقط).
* **اثر:** نرخ‌محدودیتِ توزیع‌شده روی کاغذ است؛ آنچه هست همان سقف‌های
  فایل‌محورِ `otp.json` است.
* **پیشنهاد:** یا در روتر سیم‌کشی شود (با کلیدِ IP+مسیر)، یا با یادداشتِ
  روشن حذف شود تا «ادعای بی‌پشتوانه» در مستندات نماند.

### ۲.۴ حذفِ نرم (`deleted_at`) پیاده نشده
* **شاهد:** `TODO_BEFORE_PRODUCTION.md` §۲.۸ (تصمیم گرفته شده)؛
  در عمل `applyOp` رکورد را از آرایه حذف می‌کند (`splice`) و
  `server/sync.js` آن را در `__deleted_records` به‌عنوان سنگ‌قبر ثبت می‌کند.
* **اثر:** حذفِ سخت برای سابقهٔ تحصیلی از نظر قانونی/ممیزی ریسک دارد.
* **پیشنهاد:** افزودنِ `deleted_at` به مجموعه‌های سابقه‌ای + فیلتر در همهٔ
  خوانش‌ها + یکتاییِ جزئی (`UNIQUE ... WHERE deleted_at IS NULL`).

---

## ۳. 🟡 کیفیت و بهداشتِ مخزن

| # | مورد | شاهد | پیشنهاد |
|---|---|---|---|
| ۳.۱ | `tests/api/*` در رگرسیون نیست | `grep tests/api scripts/run-all-tests.sh` = ۰ | افزودنِ ۷ سوئیت به خطِ A یا B |
| ۳.۲ | فلیکِ ساعتیِ `homework2` | `OPEN_ITEMS.md` §۵.۲ ردیف ۶ — بین ۲۳:۰۰ تا ۲۳:۵۹ UTC قرمز | ثابت‌کردنِ ساعت در تست (fake clock) |
| ۳.۳ | فلیکِ `bus2`/`bus3` | §۵.۲ ردیف ۴ — پیش از ۰۷:۴۲Z قرمز | همان |
| ۳.۴ | فلیکِ `bell2` | §۵.۲ ردیف ۳ — روی شنبه/پنجشنبه قرمز | همان |
| ۳.۵ | ۱۵ سوئیتِ جهش فایلِ حذف‌شده را می‌خوانند | §۵.۲ ردیف ۱ — ارجاع به `19-actions.js` | به‌روزرسانیِ مسیر به `19-actions-core.js` |

> هر پنج مورد **ایرادِ تست‌اند، نه ایرادِ برنامه** — و تا رفع نشوند، «قرمز»
> در گزارش‌ها معنایِ واقعی ندارد.

---

## ۴. ⚪ تصمیمِ راهبردیِ زودهنگام

### ۴.۱ UUID در برابر شناسهٔ عددیِ پیاپی
* **شاهد:** `TODO_BEFORE_PRODUCTION.md` §۲.۷؛ `nextId()` در
  `src/js/03-persistence.js` و `server/sync.js` (بیشینه + ۱).
* **وضعیت:** نگهبانِ شمردنِ شناسه (R97) پیاده شده و مؤثر است، اما شناسه‌ها
  همچنان قابلِ حدس‌اند.
* **اثر:** مهاجرت پس از نخستین استقرارِ واقعی بسیار سنگین است.
* **پیشنهاد:** تصمیمِ صریحِ «همین شناسه‌ها + نگهبان» در یک AD ثبت شود تا
  بعداً دوباره بحث نشود.

---

## ۵. یادداشتِ روشی

این فهرست **همهٔ** مواردِ باز را در بر نمی‌گیرد؛ فقط آن‌هایی را که در کد یا
مستنداتِ زنده شاهد دارند. برای وضعیتِ کامل:
`TODO_BEFORE_PRODUCTION.md` (بازبینیِ R96) · `docs/OPEN_ITEMS.md` ·
`FINAL_VERIFICATION_REPORT.md` §۷ · `docs/ROADMAP_FINAL_SCAN_2026-09-08.md`.

هر مورد بالا پیش از اجرا نیازمندِ تأییدِ جداگانه است («اصلِ هشتم»).
