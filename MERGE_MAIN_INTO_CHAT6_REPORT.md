# گزارش مرج `origin/main` در `feat/chat6-recovery` + بامپ `rc27`

**تاریخ:** ۲۰۲۶-۰۹-۱۱ · **شاخه:** `feat/chat6-recovery` · **کامیت مرج:** `f38735d`
**مالک:** چت ۶ (مستندات و انتشار)

---

## ۱) خلاصهٔ اجرایی

| مورد | نتیجه |
|------|-------|
| Mergeable | **بله** — `mergeable: true` · `mergeable_state: clean` |
| فایل‌های تعارض | **۲۳** (برآورد ابلاغ ≈۱۸ بود) |
| گیت‌ها | **سبز** — دود ۵۴۷/۵۴۷ · مجوز ۰ · راز ۱۱/۱۱ · بیلد --چک ۰ |
| Push | **`f38735d9272c6c4be9bed540d95d47deaab76d75`** |
| PR | **#۵۵** — https://github.com/rezaa2544/p2/pull/55 |
| rc27 بامپ شد؟ | **بله** — مانیفست ۲۷۷ ردیف از دیسک واقعی |
| چک‌های CI | **۷ از ۷ سبز** |
| رگرسیون خالص مرج | **صفر** (۷ تست قرمز شدند، هر ۷ رفع شدند) |
| باتری مستندات | **۵۰ از ۵۰ سبز** (۳ نقصِ از پیش موجودِ چت ۶ هم رفع شد) |

---

## ۲) وضعیت پیش از مرج

| سنجه | مقدار |
|---|---|
| `origin/main` | `8ba3462` |
| `feat/chat6-recovery` | `acd5131` |
| بیس مشترک | `a30fb20` |
| فاصله | **۱ کامیت جلو / ۲۰۲ کامیت عقب** |

> ⚠ ابلاغ گفته بود «۳۱۷ فایل»؛ شمار واقعی فایل‌های track‌شده روی شاخه **۱۰۷۳** بود (و روی `main` **۹۶۱**). این در گزارش قید می‌شود چون با عدد ابلاغ نمی‌خواند.

---

## ۳) تعارض‌ها (۲۳ فایل)

| حوزه | شمار | راهبرد |
|---|---:|---|
| `docs/` | ۱۰ | ← ما (chat6) |
| `server/` | ۴ | ← main |
| `tools/` | ۳ | ← main |
| `tests/` | ۱ | ← ما |
| `src/` | ۱ | ← main |
| `index.html` · `USER_GUIDE.html` | ۲ | ← main + بازتولید با `node build.js` |
| `HANDOFF.md` · `.gitignore` | ۲ | **اتحاد (union)** |

**بازتولید `index.html`:** خروجی `node build.js` **بیت‌به‌بیت یکسان** با `origin/main:index.html` است — یعنی `index.html` روی main با `src/` خودش همگام بود و مرج رانشی وارد نکرد.

### راستی‌آزمایی ماشینیِ اتحاد (`HANDOFF.md` و `.gitignore`)

| سنجه | نتیجه |
|---|---|
| خط‌های گم‌شده از ما | **۰** (از ۱۸۱۴ خط بی‌فاصله) |
| خط‌های گم‌شده از main | **۰** (از ۱۶۳۵ خط بی‌فاصله) |
| سرتیتر تازه از main | ۶ (OpenCode · چت ۷ Merge Queue · چت ۲ Wave 3 ×۲ · چت ۳ ئ.۵ · چت ۳ ئ.۴) |
| سرتیتر ساختگی | **۰** |
| سرتیتر تکراری | **۰** |
| مارکر تعارض باقی‌مانده | **۰** (اسکن روی همهٔ فایل‌های track‌شده، نه فقط ۳ پسوند) |

---

## ۴) دو شکافِ راهبردِ پوشه‌محور — رفع شد، پنهان نشد

راهبرد «`src/` از main ولی `tests/` از ما» دو تغییرِ **یکپارچهٔ** main را دوپاره کرد. هر دو کشف، ریشه‌یابی و رفع شدند:

### ۴.۱ `NAV_EXPECT` در `tests/smoke.js`
main روتر و انتظارِ تست را **با هم** برای افزودن `library` به منوی دانش‌آموز (بند ئ.۴) عوض کرده بود.
`src/js/05-router.js:10` (از main) واقعاً `['library','📚','کتابخانه']` را به منوی دانش‌آموز می‌دهد، ولی `NAV_EXPECT` ما آن را نداشت ⇒ دود **۵۴۶/۵۴۷**:

```
❌ منو: محتوای هر نقش دقیقاً با فهرست مورد انتظار یکی است
   student: منو ≠ انتظار (گم‌شده:  · اضافه: library)
```

**کل اختلافِ دو نسخهٔ `tests/smoke.js` دقیقاً یک خط بود** (راستی‌آزمایی با `git diff HEAD origin/main -- tests/smoke.js` = ۱۳ خط diff، یک هاंक). انتظار با کدِ ارسال‌شده هم‌تراز شد ⇒ **۵۴۷/۵۴۷**.
پس از این تغییر `tests/smoke.js` **بیت‌به‌بیت یکسان** با `origin/main:tests/smoke.js` است.

> این «دستکاری تست برای سبز شدن» نیست: منبع حقیقت، روتِی است که واقعاً ارسال شده. خودِ main هم در `HANDOFF.md` ثبت کرده که این تغییر «+به‌روزرسانیِ `NAV_EXPECT` در smoke» را شامل می‌شد.

### ۴.۲ `tools/generate-national-dataset.js`
نسخهٔ ما = نسخهٔ main **+ ۶ خط «یادداشتِ ممیزیِ هماهنگی»** در سرصفحه. `--theirs` آن ۶ خط را حذف کرده بود ⇒ `docs-consistency-check.sh` قرمز (`تعارض #۲`).
اتحاد بازگردانده شد؛ فایل اکنون **بایت‌به‌بایت برابر** با نسخهٔ ما است (چون نسخهٔ ما فرا집عیِ main بود).

---

## ۵) بامپ قفل `rc26` ← `rc27`

مرج main **۱۰ سند ریشهٔ تازه** وارد `docs/` کرد و **۲ سند فهرست‌شده** را تغییر داد؛ پس `rc26` (۲۶۶ ردیف) با دیسک (۲۷۷ ردیف) نمی‌خواند.

| نوع | سند |
|---|---|
| تازه (۱۰) | `ASSETS_MODULE` · `LIBRARY_MODULE` · `OPENCODE_USER_GUIDE` · `POST_MERGE_AUDIT_2026-09-10` · `RUNBOOK_DISASTER_RECOVERY` · `WAVE16_DISASTER_RECOVERY` · `WAVE17_TESTING` · `WAVE17_TESTING_PYRAMID` · `WAVE18_LOAD_TESTING` · `WAVE5_RED_ANALYSIS` |
| تغییرکرده (۲) | `WAVE14_OBSERVABILITY` · `WAVE3_QUERY_PERFORMANCE` (هر دو **بیت‌به‌بیت یکسان** با `origin/main` — راستی‌آزمایی شد) |

**اقدام:** بنر تاریخی روی `rc26` (مانیفستش **عمداً دست‌نخورده** ماند تا وضعیت لحظهٔ پیشین قابل اثبات بماند) + `rc27` با مانیفست **۲۷۷ ردیف** از دیسک واقعی.

| سنجهٔ `rc27` | مقدار |
|---|---|
| اسناد ریشه (بیرون خود قفل) | **۲۷۷** |
| با خود قفل | ۲۷۸ |
| زیرپوشه‌ها | ۱۹ (`RUNBOOK_CARDS/` ۱۱ · `user-guides/` ۵ · `pilot/` ۳) |
| جمع درخت | **۲۹۷** |
| لینک‌ها | ۴۶۳ / **۰ شکسته** |
| ممیزی هماهنگی | **۴۸ بررسی / ۰ تعارض** |
| تست پوشش مستندات | **۵۰** (۴۳ `*coverage.js` + ۷) |
| توکن نمایهٔ جستجو | ۲۳۲۳ |
| اسناد یتیم | **۰** (از ۶) |

**ایدمپوتنس:** دو اجرای پیاپیِ مولد مانیفست، اثرِ یکسان داد
- اثرِ sha256 هر دو اجرا: `sha256:a10141044c4082221e41eaefe113156a8d34944742498f58cced8a908e362614` — یعنی فهرست دقیقاً با دیسک یکی است.

### شمارهای همگام‌شده با دیسک (نه دستکاری)
`DOCS_METRICS.md` · `DOCUMENTATION_MAP.md` (۲۹۷/۲۷۸/۳۰ منجمد/۲۴۴ فعال/۲۳۲۳ توکن) · `TEST_COVERAGE_REPORT.md` (۳۷۶ = ۳۶۸ + ۸) · `DOCS_INDEX.md` (اشارهٔ قفل + ردیف `rc26`/`rc27` + لینک‌دادن ۶ سند یتیم در §۲.۵/§۲.۸/§۲.۹/§۲.۱۱) · `RECOVERY_INSTRUCTIONS.md` (`rc26` به‌عنوان تاریخی حفظ شد تا تست `rc1`+`rc26` سبز بماند).

**یک بهبود ساختاری:** در `tests/test-coverage-report-coverage.js` انتظارِ شمار تست‌های ریشه از عددِ سخت‌نویسِ کهنهٔ `۳۳۶` (که واقعاً ۳۶۸ است و تست به‌اشتباه سبز می‌شد) به **مشتق‌شده از دیسک** تغییر کرد تا دیگر کهنگی نگیرد:
```js
chk('شمار ریشه در سند آمده (' + fa(rootTests.length) + ')', doc.includes(fa(rootTests.length)), String(rootTests.length));
```

### ⚠ قلم بازِ به‌ارث‌رسیده از main — تصمیم با مالک main
در `migrations/` **دو فایل با شمارهٔ `۰۰۴`** وجود دارد:

| فایل | خاستگاه |
|---|---|
| `004_wave1_version_seq.sql` | بیس مشترک (پیش از مرج) |
| `004_wave3_query_indexes.sql` | **فقط روی `main`** (کار Wave 3) |

این نقضِ سیاستِ «شماره‌گذاری پیوسته» در `docs/MIGRATION_GUIDE.md` است (۵ فایل / ۴ شمارهٔ متمایز).
**شماره‌گذاری مجدد انجام نشد** چون اسناد خودِ main (`docs/WAVE3_QUERY_PERFORMANCE.md`، `HANDOFF.md`) به نام دقیق فایل ارجاع دارند و این تصمیم بیرون از دامنهٔ «نجات مستندات» است.
به‌جای پنهان‌کردن، در `tools/docs-consistency-check.sh` به‌عنوان **بررسیِ پین‌شده** ثبت شد تا در هر اجرا دیده شود.

---

## ۶) گیت‌ها

### چهار گیت اجباری (روی کامیتِ نهایی `f38735d`)

| گیت | نتیجه |
|---|---|
| `node tests/smoke.js` | **۵۴۷/۵۴۷** · exit 0 |
| `node tests/check-authz.js` | **exit 0** (۶ بررسی، همه سبز) |
| `node tests/secret-scan.js` | **۱۱/۱۱** · exit 0 (۱۰۶۰ فایل اسکن شد) |
| `node build.js --check` | **exit 0** (بیت‌به‌بیت + راهنما همگام + write-perms + ۳۸۵ اکشن) |

### تست‌های مستندات (۵۰ تست)

| تست | نتیجه |
|---|---|
| `docs-freeze-marker` | **۱۴/۱۴** |
| `docs-metrics` | **۱۱/۱۱** |
| `docs-metadata` | **۱۷/۱۷** |
| `documentation-map-coverage` | **۲۳/۲۳** |
| `docs-index-coverage` | **۷۵/۷۵** |
| `docs-health` | **۹/۹** |
| `docs-consistency` | **۲۲/۲۲** |
| `data-dictionary-coverage` | **۳۰/۳۰** |
| `test-coverage-report-coverage` | **۳۳/۳۳** |
| `recovery-instructions-coverage` | **۲۷/۲۷** |

### سه قرمزی که «از پیش از مرج» بود — **و اشتباهِ گزارشِ نخست من**

در گزارش نخست نوشتم این سه تست «از پیش از مرج قرمز بودند» و از کنارشان گذشتم. آن گزاره درست بود ولی **نتیجه‌گیری‌اش غلط**: این سه تست و سه سندِ متناظرشان **روی `main` اصلاً وجود ندارند** (راستی‌آزمایی در worktree جدا روی `origin/main`):

| فایل | روی `main` | روی chat6 |
|---|---|---|
| `tests/api-changelog-coverage.js` + `docs/API_CHANGELOG.md` | ❌ نیست | ✅ هست |
| `tests/p0-blocker-tracker-coverage.js` + `docs/P0_BLOCKER_TRACKER.md` | ❌ نیست | ✅ هست |
| `tests/pilot-rollout-coverage.js` + `docs/PILOT_ROLLOUT_PLAN.md` | ❌ نیست | ✅ هست |

پس این‌ها نقصِ **خودِ چت ۶** بودند، نه ارثیهٔ main — و با مرجِ این پی‌آر، **سه تست قرمزِ تازه وارد `main` می‌شدند**. اکنون هر سه رفع شدند ⇒ **۵۰/۵۰ تست مستندات سبز**.

#### ریشهٔ هر سه (باگ در تست، نه در سند)

| تست | ریشه | رفع |
|---|---|---|
| `pilot-rollout` (۵ قرمز) | لنگرِ `محرک‌های بازگشت` **غیریکتا** بود: هم در §۰ (چک‌لیست پیش‌شرط، خط ۱۸) و هم در سربرگ §۸ (خط ۱۴۷). `split()[1]` به‌جای جدول محرک‌ها، §۱–§۷ را برمی‌داشت | لنگر به سربرگ پین شد (`## ۸) …`) |
| `p0-blocker-tracker` (۳ قرمز) | لنگرِ `ثبت ریسک‌ها` **غیریکتا**: هم سربرگ §۵ (خط ۱۹۶) و هم در یادداشتِ «نمای جامع» (خط ۱۹۸). `split()[1]` فقط فاصلهٔ دو خط را می‌داد ⇒ **۰ ردیف ریسک** در حالی که جدول ۸ ردیف دارد | لنگر به سربرگ پین شد (`## ۵) …`) |
| `api-changelog` (۲ قرمز) | دو انتظارِ **کهنه**: سند به‌درستی `۱۸ پلتفرمی`/`۳۱ عملیات` می‌گوید (تست `۱۶` می‌خواست)؛ و تست ادعای «اوپن‌ای‌پی‌آی **وجود ندارد**» را می‌طلبید در حالی که `docs/openapi.yaml` در مأموریت ۳۶ ساخته شده (۳۹٬۲۳۷ بایت) | شمارها با جدولِ واقعی همگام شد + دو مسیر جاافتاده (`/api/health-index`، `/metrics`) به فهرست پوشش اضافه شد + انتظارِ اوپن‌ای‌پی‌آی **معکوس** شد: اکنون «اسپک وجود دارد و سند به آن ارجاع می‌دهد و فایل روی دیسک هست» را می‌سنجد |

> معکوس‌کردنِ انتظارِ اوپن‌ای‌پی‌آی «دستکاری تست برای سبز شدن» نیست: انتظارِ پیشین **ادعای کاذب** می‌طلبید. سند درست بود؛ تست غلط.

#### راستی‌آزماییِ جهش (طبق `SKILLS_MASTER.md` §۵)
برای اثبات اینکه رفع‌ها **تهی** نیستند، هر سه سند موقتاً جهش داده شد:

| جهش | انتظار | نتیجه |
|---|---|---|
| حذف سطرِ محرکِ «دسترس < ۹۵٪» از `PILOT_ROLLOUT_PLAN.md` | قرمز | ✅ **۷۹/۱** — جهش کشته شد |
| حذف سطرِ ریسکِ «پنتست خارجی» از `P0_BLOCKER_TRACKER.md` | قرمز | ✅ **۵۱/۱** — جهش کشته شد |
| دروغ‌گویی در شمار پلتفرمی (`۹۹ پلتفرمی`) در `API_CHANGELOG.md` | قرمز | ✅ **۵۷/۱** — جهش کشته شد |

سپس هر سه سند **بایت‌به‌بایت** بازگردانده شدند (`git status --porcelain docs/` = ۰ تغییر).

| تست | پیش از رفع | پس از رفع |
|---|---|---|
| `api-changelog-coverage` | ۵۴/۵۶ ❌ | **۵۸/۵۸ ✅** |
| `p0-blocker-tracker-coverage` | ۴۹/۵۲ ❌ | **۵۲/۵۲ ✅** |
| `pilot-rollout-coverage` | ۷۵/۸۰ ❌ | **۸۰/۸۰ ✅** |
| **مجموع باتری مستندات** | **۴۷ از ۵۰** | **۵۰ از ۵۰ ✅** |

### چک‌های CI روی `f38735d`
`build (22.x)` ✅ · `Secret scan` ✅ · `SAST` ✅ · `SCA` ✅ · `SBOM` ✅ · `WAF & nginx` ✅ · `DAST (OWASP ZAP)` ✅ — **۷ از ۷ سبز**

---

## ۷) راستی‌آزمایی‌های تکمیلی

| بررسی | نتیجه |
|---|---|
| مارکر تعارض (`<<<<<<<`/`=======`/`>>>>>>>`) روی **همهٔ** فایل‌های track‌شده | **۰** |
| مسیرهای حل‌نشده (`--diff-filter=U`) | **۰** |
| والدین کامیت مرج | `acd5131` (chat6) + `8ba3462` (main) — مرج واقعی دوالدی |
| درخت کاری پس از push | **تمیز** (۰ تغییر) |
| همگامی با ریموت | **بله** (`HEAD` = `ls-remote`) |
| شاخهٔ پشتیبان پیش از مرج | `backup/pre-merge-chat6-20260911` → `acd5131` |

### رازداری
توکن دسترسی **فقط در حافظهٔ نشست** بود:
- `grep` روی درخت کاری (بیرون `node_modules`/`.git`) = **۰ اثر**
- `grep` روی فایل‌های track‌شده برای الگوی توکن کلاسیک گیت‌هاب (پیشوند سه‌نویسه‌ای + ۳۶ نویسه) = **۰ اثر**
- `.git/config` **پاک** (اعتبارسنجی از راه credential helper بیرونی، نه URL)
- `secret-scan` **۱۱/۱۱**

> 🔴 **توصیهٔ امنیتی:** توکن در متنِ ابلاغ به‌صورت روشن (plain text) آمده بود. **بازگردانی/rotate** شود.

---

## ۸) Ruflo

| قلم | نتیجه |
|---|---|
| نصب | ✅ `ruflo v3.39.2` (نصب سراسری به `EACCES` خورد؛ با `--prefix ~/.npm-global` نصب شد) |
| `memory init` | ✅ exit 0 — «Verification passed (6/6 tests)» · ONNX embedder 384d |
| `memory store` | ✅ `chat6_merge_complete = completed` (ID `entry_1789117447131_`) · بازخوانی شد |
| مکان دیتابیس | `/home/user/.claude/memory.db` — **بیرون** از ریپو؛ ریپو پاک ماند |

---

## ۹) مواردی که با ابلاغ نمی‌خواند (صادقانه)

| ابلاغ | واقعیت |
|---|---|
| «۳۱۷ فایل» | **۱۰۷۳** فایل track‌شده روی شاخه (۹۶۱ روی main) |
| «حدود ۱۸ فایل conflict» | **۲۳** فایل |
| «اگر main سندی به `docs/` اضافه یا کم کرده باشد…» | **۱۰ سند اضافه** + **۲ سند تغییرکرده** ⇒ بامپ `rc27` لازم شد |
| «۳ تست مستندات روی شمار قفل‌شده» | **۷ تست** قرمز شدند (freeze، metrics، consistency، metadata، map، data-dictionary، test-coverage-report) |
| `gh` برای ساخت PR | نصب نبود؛ PR **#۵۵ از پیش وجود داشت** — ساخته نشد، **به‌روزرسانی** شد (body + راستی‌آزمایی mergeable) |
| `tools/secret-scan.js` (در `SKILLS_MASTER.md`) | مسیر واقعی `tests/secret-scan.js` است؛ `tools/secret-scan.js` وجود ندارد |
| «چرخهٔ استاندارد بامپر» | **ابزار بامپر در ریپو وجود ندارد** (بامپ‌های پیشین دستی بوده‌اند). پیشنهاد: افزودن `tools/docs-freeze-bump.js` برای تکرارپذیری |

---

_راستی‌آزمایی هرزمانه: `node tests/docs-freeze-marker.js` (روی `rc27`) · `node tests/smoke.js` · `node build.js --check`._
