# گزارشِ بررسیِ عملی و رفعِ ایرادهایِ خودکارِ PR #2 (Devin AI)

**تاریخ:** ۲۰۲۶-۰۹-۰۸ · **نویسنده:** دورِ ۸۸ (رفعِ بازبینی)
**وضعیتِ PR:** ادغام‌شده در `0acd68e` → بازبینی دوباره ممکن نیست؛ رفع‌ها مستقیم روی `main` نشست.
**کامیت‌هایِ این دور:** `b22a7b6` (کلاینت + تست) · `8e06379` (سرور + تست)

## روش

هیچ ادعایی بدونِ راستی‌آزماییِ زنده اعمال نشد. هر شش ادعا پیش از رفع، یا با پروبِ زندهٔ سرور
(دو سرورِ جدا روی ۸۹۹/۸۹۹۰ — `parent`/`manager`/`student` واقعی) یا با اجرای jsdom روی خودِ
`index.html` تأیید/رد شد. **دو پیشنهادِ خودِ متنِ بازبینی فنی اشتباه بود** و اعمال نشد؛ دلیلش
در ذیلِ همان ردیف ثبت شده است (اصل: «تأییدِ خودکار ≠ دستورِ اجرا»).

## جدولِ احکام (۶ ادعا)

| # | ادعای Devin | حکم | مستندِ زنده |
|---|---|---|---|
| ۱ | چک‌لیستِ «فردا» فقط روزهای ۰–۴ را درسی می‌داند | **واقعی (نیمه)** | کد `day<=4` داشت، درحالی‌که مدرسه `work_days` + روزهایِ جبرانی دارد |
| ۲ | تزریقِ فیلد در خروجی ICS (نامِ درس/اتاق) | **واقعی و جدی** | نامِ بدخواه → خطِ `X-PROP`/`INJ:` مستقل در فیل |
| ۳ | جعلِ `S.__cfRid` در ذخیرهٔ موجه‌سازی | **واقعی (شکافِ کلاینت)** | سرور از پیش `403` می‌دهد (پروب B) — لایهٔ UX کلاینت شکسته بود |
| ۴ | امتحانِ ساعت‌دار به‌عنوانِ رویدادِ تمام‌روز صادر می‌شود | **واقعی** | همهٔ `DTSTART`ها `VALUE=DATE` بودند؛ ساعتِ `09:30` حذف می‌شد |
| ۵ | اعلانِ مدیر در موجه‌سازیِ ولی ثبت نمی‌شود | **واقعی و بحرانی** | پروب A: `ins`ِ notification از والد → `403 out_of_scope` — `inScope` به‌ساختار تمامِ insertهای اعلانِ نقش‌هایِ غیرمدیر را رد می‌کند (رکوردِ اعلان `student_id` ندارد) |
| ۶ | حالتِ چندنقشی: فقط `u.role` دیده می‌شود، نه persona | **واقعی** | اپ مکانیزمِ واقعی دارد: `activePersona()` در `23-subscription.js:61` = `S.persona ‖ S.user.role` — کدِ PR فقط `u.role` را می‌خواند |

## رفع‌ها (به ترتیبِ اولویت)

1. **چک‌لیستِ فردا** — `isWorkDay` (از `46-bell-now.js`) به‌جای `day<=4`؛_FALLBACK امن به ۰–۴ اگر
   تابع موجود نباشد. تست F1 با دو پیکربندیِ کنترل‌شده (`[0,1,2,3,4]` و `[5,6]`) نشان می‌دهد
   علامتِ «روزِ درسی» از تنظیمِ خودِ مدرسه می‌آید.
2. **تزریق ICS** — تابعِ جدید `icsEsc` (RFC 5545): `\` → `\\`، `;` → `\;`، `,` → `\,`،
   خط‌خوردگی → `\n`؛ روی `SUMMARY` و `LOCATION` اعمال.
   > **انحراف از متنِ بازبینی:** پیشنهادِ `esc()` اشتباه است — `esc` فرارِ HTML است و فایلِ ICS را
   > خراب می‌کند؛ فرارِ صحیحِ RFC 5545 پس‌مخثوشی است.
3. **ذخیرهٔ جعلی** — `qe-save` در لحظهٔ ذخیره دوباره `cfCanExcuse(u,rec)` را می‌سنجد (نه فقط
   هنگامِ بازکردنِ مودال). سرور همچنان مرجعِ نهایی است (پروب B: `403 out_of_scope`).
4. **امتحانِ ساعت‌دار** — `DTSTART` شناورِ محلی: `YYYYMMDDTHHMMSS` (بدونِ `Z`)؛ بدونِ ساعت →
   `DTSTART;VALUE=DATE:YYYYMMDD`.
   > **انحراف از متنِ بازبینی:** پیشنهادِ افزودن `Z` اشتباه است — `Z` یعنی UTC و ساعتِ مدرسه
   > (تهران) را ۳:۳۰ جابه‌جا می‌کند. زمانِ شناورِ محلیِ RFC 5545 پاسخِ درست است.
5. **اعلانِ مدیر** — هوکِ **سمتِ سرور** در `server/sync.js` (پسِ حلقهٔ apply، پیشِ markDirty):
   هر `ins leaves` با `status:'pending'` از نقشِ غیرمدیر/سوپرادمین → سرور خود اعلانِ مدیرِ
   همان مدرسه را می‌سازد + `audit('leave_request_notified')`.
   > **انحراف از متنِ بازبینی:** «ثبتِ اعلان در کلاینت» ممکن نیست — پروب A اثبات کرد
   > `inScope` ساختارِ رکوردِ اعلان را از والد رد می‌کند. اعلانِ کلاینتیِ demo-mode حفظ شد
   > (برایِ حالتِ بدونِ سرور تنها مسیر است؛ با سرور به‌عنوانِ opِ ردشده بی‌ضرر می‌ماند).
6. **چندنقشی** — `cfPersona(u)` = `activePersona()` (یعنی `S.persona ‖ u.role`) و گاردِ
   مشترک `cfCanExcuse(u,rec)`.
   > **حاشیهٔ دامنه:** بازبررسیِ گارد فقط شاخه‌هایِ parent/student را دارد (هم‌راستا با گاردِ
   > بازکردن). شاخهٔ manager/teacher پیشنهادشده در متنِ بازبینی از UI در دسترس نیست و سرور
   > آن را `403` می‌کند؛ افزودنش کدِ مردود می‌ساخت.

## تست‌هایِ تازه و تستِ جهش (mutation)

| تست | چیزی که نگه می‌دارد |
|---|---|
| F1 (بازنویسی) | روزِ درسی از `work_days` + جبرانی مدرسه، نه `day<=4` |
| F3 (بازنویسی) | همهٔ `DTSTART`ها یا `VALUE=DATE` خالص یا `HHMMSS` شناور |
| F3-b (تازه) | بارِ بدخواه (`;`، خط‌خوردگی واقعی) نه خطِ مستقل می‌سازد و نه فرارِ RFC5545 دقیق دارد |
| F4-e (تازه) | `qe-save` با `S.__cfRid`ِ جعلی (رکوردِ فرزندِ دیگر) رد می‌شود و شناسه پاک |
| F4-f (تازه) | کاربرِ چندنقشی (student + parent_link) فقط با `S.persona='parent'` ردِ گارد می‌شود |
| S14–S17 (تازه، سرور) | ins بیرونی `403` · ins اعلان از والد `403` · pendingِ فرزند → اعلانِ مدیر **روی disk** · درخواستِ مدیر → اعلان نمی‌سازد |

**تستِ جهش (هر جهش باید تستِ مقابلش را قرمز کند — هر سه تأیید شد):**

| جهش | نتیجه |
|---|---|
| A: `icsEsc` بدونِ فرار | F3-b قرمز ✅ |
| B: حذفِ بازبررسیِ `qe-save` | F4-e و F4-f قرمز ✅ |
| C: خاموشِ هوکِ سرور | S16b قرمز ✅ |

## دروازه‌ها (پس از کامیت)

| دروازه | نتیجه |
|---|---|
| `node build.js` | ✅ (مُهرِ راهنما: feaa0c81e366) |
| `tests/smoke.js` | ✅ 546/546 |
| `tools/check-authz.js` | ✅ 0 |
| `tests/client-features.js` | ✅ 11/11 |
| `tests/server12.js` | ✅ 24/24 |
| `scripts/run-all-tests.sh` (۱۲۵ سویت، v3) | ✅ **125/125** — 436 ثانیه |

## باقی‌مانده (خارج از دامنهٔ این دور — ثبت شد، دست نخورد)

1. **فیلترِ مدرسه در ICS:** `icsForStudent` فقط روی `class_id` فیلتر می‌کند؛ در seed، class id
   میانِ مدرسه‌ها تداخلِ عددی دارد → امتحانِ کلاسِ هم‌شمارهٔ مدرسهٔ دیگر در تقویمِ دانش‌آموز
   می‌آید. رفعِ یک‌خطی: `e.school_id===cls.school_id` در همان فیلتر (دورِ بعد).
2. **شکافِ سیستماتیکِ اعلان:** `insert('notifications')` از تمامِ نقش‌هایِ غیرمدیر به‌ساختار
   توسطِ `inScope` رد می‌شود (رکوردِ اعلان `student_id`/`parent_id` ندارد) — پیشینِ PR#2 و
   فراگیر. راه‌حلِ عمومی یا اسکیمأ رکوردِ اعلان یا اندپوینتِ جدا می‌خواهد.
3. **opِ ردشدهٔ demo:** با سرور، insert اعلانِ کلاینتیِ `qe-save` به‌عنوانِ opِ `rejected`
   در ردیفِ همگام‌سازی دیده می‌شود (بی‌ضرر؛ اعلانِ واقعی را هوکِ سرور می‌سازد). نمایشِ
   opهایِ ردشده به‌جای پنهانشان، آگاهانه حفظ شد.
