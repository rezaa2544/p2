# گزارشِ چت ۱ — تحلیلِ ایستا با ESLint (Tier 3، اولویت ۱۲) — ۱۹/۰۶/۱۴۰۵ (2026-09-09)

> شاخه: `arena/01a0827b-p2` · پوش شده · درخت تمیز · POC (کانفیگ + CI + سند)؛ تنها
> تغییرِ سورس ۲ کامنتِ مجوز است (صفر تغییرِ رفتار).

## ۱. جدولِ کامیت‌ها

| # | موضوع | کامیت | پیام |
|---|---|---|---|
| ۱ | وظیفه (پیامِ دقیقِ درخواستی) | `a36f93e` | `feat(ci): implement eslint static analysis` |
| ۲ | همین گزارش | (کامیتِ گزارش) | `docs: add chat 1 eslint report` |

## ۲. چه ساخته شد

- `eslint.config.js` (تازه): فرمتِ flat (تنها فرمتِ معتبرِ ESLint 10) — بلاک‌هایِ
  فرانت‌اند/node/تست/k6/worker + ‏`ignores` درون‌کانفیگی (آینهٔ `.gitignore`) +
  ۴ قانونِ error + ۱۳ قانونِ warn + خاموشی‌هایِ مستند (دلیلِ هر سطر در کامنتِ خودش).
- `package.json`: پنج devDependency (‏`eslint` • ‏`@eslint/js` • ‏`security` • ‏`n` •
  ‏`globals`) + اسکریپتِ `lint: eslint .`؛ قدمِ `npm run lint` در `Node.js CI`.
- `docs/STATIC_ANALYSIS_SETUP.md` (تازه، کامل): §۰ چهار انحرافِ آگاهانه (§۳ همین گزارش)
  + §۱.۱ نصب + §۱.۲ پیکربندی (جدولِ تصمیمِ هر قانون) + §۱.۳ CI + §۱.۴ آزمون‌ها +
  §۲ شواهدِ پیمایش + §۳ نگهداشت (قراردادِ «صفر خطا»).
- `src/js/42-self-diagnostics.js`: فقط ۲ کامنتِ `eslint-disable-next-line` در ۲ نقطهٔ
  eval دارایِ مجوزِ X2a (+ بازتولیدِ `index.html`، مُهرِ راهنما، `cdn-manifest.json`).

## ۳. اصلِ حاکم: زمین‌سازی، نه وانمود

- **پیکربندی از پیمایش درآمد، نه از حدس:** ۴ دور اسکنِ تمام‌درختی (۳۳۶ فایل) از
  ۹۸۷۶ خطا به **۰ خطا و ۵۹ هشدارِ مستند** رسید؛ هر error/warn/off با شمارِ برخورد
  و دلیل ثبت است و قوانینِ ردشده (مثلِ `eqeqeq` با ۲۸۸ برخوردِ پیشینی) با عدد توجیه شدند.
- **انحراف‌ها تصمیم‌اند، نه فراموشی:** ‏`.eslintrc`‏ (حذف‌شده در v10)،
  ‏`eslint-plugin-node`‏ (متروک → فورکِ زندهٔ `n`)، ‏`eslint-plugin-import`‏
  (بدونِ گرافِ ESM در ریپویِ CJS = مثبتِ کاذب) — هر سه در §۰ سند با دلیل.
- **شکاف‌ها صریح‌اند:** `detect-child-process` الگویِ غالبِ `*Sync` را نمی‌بیند
  (با فیکسچر اثبات شد؛ پوشش با بازبینی)؛ سبک اعمال نمی‌شود (هشدارِ هزارتایی =
  تئاتر)؛ `no-undef` فرانت‌اند را smoke پوشش می‌دهد، نه لینت.
- **`--fix` ادعا نیست، اثبات است:** فیکسچرِ موقتِ `if (!!x)` با دستورِ واقعیِ
  `npm run lint -- --fix` به `if (x)` تبدیل شد؛ سپس فیکسچر پاک شد.

## ۴. راستی‌آزمایی

| بررسی | نتیجه |
|---|---|
| `npm run lint` | ‏۰ خطا، ۵۹ هشدار، خروجِ ۰‏ ✅ |
| `npm run lint -- --fix` (رویِ فیکسچرِ موقت) | رفعِ خودکارِ اثبات‌شده ✅ |
| `node build.js --check` | سبز (باندل + مُهرِ راهنما + مانیفست + مجوزها) ✅ |
| `node tests/smoke.js` | ‏۵۴۷/۵۴۷‏ ✅ |
| `node tools/check-authz.js` | ‏۰‏ (تطبیق کامل) ✅ |
| `node tools/secret-scan.js` | ‏۱۱/۱۱‏ ✅ |

## ۵. Follow-upها

1. بازبینیِ انسانیِ ۲ regex پرهزینه (`tests/client-features.js`، `tools/check-authz.js`) از صفِ warn.
2. مدرن‌سازیِ `url.parse` در `server/pull.js:131` (یادآوریِ `n/no-deprecated-api`).
3. نصبِ `puppeteer` هنگامِ نیاز به اسکرین‌شاتِ دستی (`_shots.js` فعلاً اجراشدنی نیست).
4. بازدیدِ دوره‌ایِ صفِ warn (~۵۹) و ارتقایِ قانون‌هایِ به‌ثمررسیده به error؛ بازبینیِ
   `detect-child-process` پس از انتشارِ نسخهٔ تازهٔ پلاگینِ security.
