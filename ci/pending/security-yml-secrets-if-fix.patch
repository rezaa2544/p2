--- a/.github/workflows/security.yml
+++ b/.github/workflows/security.yml
@@ -88,7 +88,7 @@
           cache: npm
       - run: npm ci
       - name: Generate SBOM (SPDX JSON)
-        run: npm sbom --sbom-format=spdxjson > sbom.spdx.json
+        run: npm sbom --sbom-format=spdx > sbom.spdx.json
       - name: Upload SBOM artifact
         uses: actions/upload-artifact@v4
         with:
@@ -102,14 +102,22 @@
     continue-on-error: true
     steps:
       - name: OWASP ZAP baseline scan
-        if: ${{ secrets.SECURITY_TARGET_URL != '' }}
-        uses: zaproxy/actions-baseline@v0.12.0
+        # NOTE: secrets.* must not be referenced directly in `if:` on this repo —
+        # GitHub rejects the workflow with zero jobs ("workflow file issue").
+        # Verified empirically (bisect on wftest-branch, runs #61–#66, 2026-09-09):
+        # passing the secret via env and testing env.* in `if` works.
+        env:
+          SECURITY_TARGET_URL: ${{ secrets.SECURITY_TARGET_URL }}
+        if: env.SECURITY_TARGET_URL != ''
+        uses: zaproxy/action-baseline@v0.12.0
         with:
           target: ${{ secrets.SECURITY_TARGET_URL }}
           # advisory (continue-on-error) until a stable staging URL is provided.
       - name: DAST skipped (no SECURITY_TARGET_URL)
-        if: ${{ secrets.SECURITY_TARGET_URL == '' }}
+        if: env.SECURITY_TARGET_URL == ''
         run: echo "SECURITY_TARGET_URL not set — DAST baseline skipped (needs a live staging URL)."
+        env:
+          SECURITY_TARGET_URL: ${{ secrets.SECURITY_TARGET_URL }}
 
   # ── WAF (kept from the earlier Security (WAF) workflow) ─────────────
   waf:

