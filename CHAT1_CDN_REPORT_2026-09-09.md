# گزارشِ چت ۱ — CDN Integration (Tier 2، اولویت ۲) — ۱۹/۰۶/۱۴۰۵ (2026-09-09)

> شاخه: `arena/01a0827b-p2` · پوش شده · درخت تمیز · صفر تغییر در `src/` و `server/` ·
> `index.html` بایت‌به‌بایت دست‌نخورده.

## ۱. جدولِ کامیت‌ها

| # | موضوع | کامیت | پیام |
|---|---|---|---|
| ۱ | وظیفه (پیامِ دقیقِ درخواستی) | `59f96d3` | `feat(infra): integrate cdn for static assets` |
| ۲ | همین گزارش | (کامیتِ گزارش) | `docs: add chat 1 cdn report` |

## ۲. یافتهٔ ممیزی و سازگاریِ معماری

پرامپت فرضِ بیلدِ چندفایلی داشت (`app.[hash].js`)؛ ممیزی ثابت کرد بیلد **تک‌فایل** است
(`index.html` + فونتِ data-URI، صفر ساب‌ریسورسِ خارجی). پس به‌جایِ هش در نامِ فایل:

- `node build.js` اثرِ انگشتِ قطعی می‌نویسد: `cdn-manifest.json`
  (`sha256` در دو تکهٔ ۳۲تایی + ‏`seal12`‏ = همان مُهرِ راهنما)؛
- `node build.js --check` مانیفست را هم راستی‌آزمایی می‌کند (سبز ماند)؛
- جریانِ invalidate: بیلدِ تازه ← purge تک‌URLِ `/` ← تطبیقِ sha256 بدنهٔ edge با مانیفست.

## ۳. چه ساخته شد (POC)

- `cloudflare/worker.js` (تازه): passthrough صریحِ `/api/*` (هرگز کش) + ۵دقیقه‌ایِ `/` و
  `/index.html` با must-revalidate + قانونِ immutable یک‌ساله (رزروِ آماده برایِ روزی که بیلد بشکافد).
- `k8s/ingress-cdn.yaml` (تازه): Ingress مبدأ (TLS + روت به `payesh-api:3000`) با هشدارِ
  «کش فقط در ورکر، نه دو لایه».
- `docs/CDN_INTEGRATION_SETUP.md` (تازه): §۰ قیدها + §۱-۱ (Cloudflare پیشنهادی، CloudFront، آروان) +
  §۱-۲ (جدولِ TTL با ستونِ وضعیت) + §۱-۳ (ورکر) + §۱-۴ (هش‌گذاری) + §۱-۵ (۳ تست) + چک‌لیست + §۴.
- `build.js`: بلوکِ `syncManifest` + دو هوک (compute + ‏check‏) — خروجیِ `index.html` عوض نشد.
- `docs/DEPLOY.md`: بولتِ §۹ + راهِ «ج» (پشتِ CDN) در §۴.
- `tests/cdn.js` ‏۵/۵‏ + `tests/cdn-mutations.js` ‏۲/۲‏ کشته.

## ۴. دام‌ها و تصمیم‌ها

| دام | تصمیم |
|---|---|
| هشِ ۶۴تاییِ یک‌تکه secret-scan را به ۱ هیت انداخت (گاردِ `sha256` خطِ اشتباه را چک می‌کند) | دو تکهٔ ۳۲تایی + join — بدونِ دست‌زدن به اسکنر (مستند در کد و سند) |
| origin با `no-cache` سرو می‌دهد | عمداً دست نخورد (خارج از فایل‌هایِ POC + ریسکِ رگرسیون)؛ کش فقط در edge |
| immutable یک‌ساله چیزی برایِ کش ندارد | قانون نوشته و تست شد ولی «رزرو» اعلام شد، نه فعال |

## ۵. راستی‌آزمایی

| بررسی | نتیجه |
|---|---|
| `node build.js --check` (با هش‌گذاریِ تازه) | سبز — ۵ خطِ ✅/OK ✅ |
| `node tests/smoke.js` | ‏۵۴۷/۵۴۷‏ ✅ |
| `node tools/check-authz.js` | ‏۰‏ ✅ |
| `node tools/secret-scan.js` | ‏۱۱/۱۱‏ ✅ |
| `tests/cdn.js` / `tests/cdn-mutations.js` | ‏۵/۵‏ / ‏۲/۲‏ کشته ✅ |
| پارسِ ESM ورکر + پارسِ YAML اینگرس | ✅ |
| `git status` پس از بازسازی (چرخشِ درخت) | فقط فایل‌هایِ وظیفه؛ `index.html` عوض نشد ✅ |

## ۶. Follow-upها

1. باگِ گاردِ hex در `tests/secret-scan.js` (چکِ خطِ قبلی به‌جایِ خطِ هش) — عمداً دست نخورد؛ با مالکِ ابزار هماهنگ شود.
2. استقرارِ ورکر رویِ staging + هر ۳ تستِ §۱-۵ + ثبتِ آستانهٔ عددیِ تستِ ۳ پس از نخستین اندازه‌گیری.
3. تصمیمِ TLS پشتِ CDN (Full strict) هم‌زمان با خریدِ دامنه.
