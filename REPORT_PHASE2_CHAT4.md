# گزارش فاز ۲ — چت ۴: ردیس، چندنمونه‌ای و یکپارچگی داده (P0-13 تا P0-18)

**تاریخ:** ۱۸ شهریور ۱۴۰۵ (2026-09-09) · **شاخه:** `feat/p2-redis-integrity-chat4` · **پایه:** `fadeae6` (main)

## خلاصه

هر شش بندِ خواسته‌شده پیاده‌سازی شد؛ هر بند یک کامیتِ مستقل، یک تستِ مستقل، و
دروازه‌های سبز دارد. هیچ تغییری صرفاً محلی نمانده — همهٔ کامیت‌ها به
`origin/feat/p2-redis-integrity-chat4` پوش شده‌اند. ادغام با تأیید سرپرست.

| بند | کامیت | تست | نتیجه |
|---|---|---|---|
| P0-13 فال‌بک ردیس در تولید ممنوع | `cfec6bf` | `tests/redis-fallback.js` | ۱۰/۱۰ |
| P0-14 قفل توزیع‌شدهٔ اتمیک | `05820f2` | `tests/lock-atomic.js` | ۱۲/۱۲ |
| P0-15 حالت OTP در ردیس | `63e2bbe` | `tests/otp-redis.js` | ۱۶/۱۶ |
| P0-16 شناسهٔ بدون‌برخورد | `1e317f3` | `tests/id-collision.js` | ۱۱/۱۱ |
| P0-17 حذف امن با سنگ‌قبر | `6531e69` | `tests/tombstone.js` | ۲۵/۲۵ |
| P0-18 همزمانی خوش‌بینانه | `1dee995` | `tests/occ.js` | ۱۸/۱۸ |

## جزئیات هر بند

### ۱) P0-13 — فال‌بک ردیس در تولید ممنوع (`cfec6bf`)
- **مسئله:** در تولید وقتی ردیس نبود یا قطع بود، سرور بی‌صدا به حافظهٔ محلی
  فال‌بک می‌کرد؛ حالت بین نمونه‌ها واگرا می‌شد و هیچ‌کس نمی‌فهمید.
- **اجرا:** `redis.init()` در `NODE_ENV=production` بدون `REDIS_URL`/درایور یا با
  ردیسِ غیرقابل‌اتصال `{ok:false}` برمی‌گرداند. `redis.ready()` افزوده شد
  (در تولید فقط با ردیسِ زنده درست است). `cache.init()` نتیجه را منتشر می‌کند؛
  بوتر در `index.js` با شکست، پس از ذخیرهٔ فروشگاه و بستن اتصال‌ها `exit(1)`
  می‌کند. `/api/health` در تولیدِ بدون ردیس ۵۰۳ + `cache:unavailable` می‌دهد.
  فال‌بک حافظه فقط برای توسعه نگه داشته شد.
- **تست پذیرش:** بوت واقعی — تولید بدون ردیس با خروجی غیرصفر می‌میرد و پیام
  شکست ثبت می‌شود؛ توسعه بالا می‌آید و سرویس می‌دهد.

### ۲) P0-14 — قفل توزیع‌شدهٔ اتمیک (`05820f2`)
- **مسئله:** `acquireLock` یک `SET` ساده بود (نه `NX`) — یعنی همیشه «قفل می‌شد»
  و هر دو طرف برنده بودند؛ `releaseLock` هم بی‌قیدوشرط حذف می‌کرد.
- **اجرا:** `redis.setNX` (SET NX EX در ردیس / درج شرطی در حافظه) و
  `redis.compareAndDelete` (اسکریپت لوآ) افزوده شد. `cache.acquireLock` توکن
  یکتا برمی‌گرداند یا تهی؛ `cache.releaseLock(key, token)` فقط با تطبیق توکن
  آزاد می‌کند. قفل منقضی‌شده هرگز توسط صاحب سابق کشته نمی‌شود.
- **تست پذیرش:** دو (و پنجاه) دریافت‌کنندهٔ همزمان → دقیقاً یک برنده؛ توکن
  اشتباه/منقضی بی‌اثر؛ انحصار متقابل دو نمونه.

### ۳) P0-15 — حالت OTP و حدّ‌نرخ‌ها در ردیس (`63e2bbe`)
- **مسئله:** کدها و شمارنده‌ها فقط در `otp.json` بود؛ دو نمونهٔ واقعی حالت
  جدا داشتند و کدِ مصرف‌شده می‌توانست در نمونهٔ دیگر زنده بماند.
- **اجرا:** با ردیس فعال، کلید `payesh:otp:state` منبع حقیقت است: فلاشِ
  سریالی‌شده زیر قفلِ P0-14 با شمارهٔ دنباله (نویسندهٔ کهنه بازنویسی نمی‌کند؛
  فلاش‌ها هم‌جوشی می‌شوند)، خواندنِ دورتر در ورود هر درخواست، ادغامِ اتحادی
  کلیدبه‌کلید، و **سنگ‌قبر** برای حذف کدها. فایل `otp.json` فقط فال‌بک توسعهٔ
  بدون ردیس است (رفتار پیشین دست‌نخورده). فراخوانی‌های `auth.js` `await` شدند
  و سه حذفِ کد به `otp.deleteCode` رفت.
- **باگ‌های گرفته‌شده حین اجرا:** ۱) `adopt`ِ بازنویس، تغییرات فلاش‌نشده را
  پاک می‌کرد → ادغام شد؛ ۲) فلاش بدون قفل، نوشت‌ها را می‌پوشاند → قفل شرطِ
  ورود شد؛ ۳) حفرهٔ بازپخش کدِ مصرف‌شده → سنگ‌قبر.
- **تست پذیرش:** دو نمونه — ب کدِ الف را می‌بیند؛ کد مصرف‌شده در همه‌جا می‌میرد؛
  ۵۰ نوشت موازی بدون تلفات؛ ۱۰۰۰ عملیات همزمان ~۷۰ میلی‌ثانیه.

### ۴) P0-16 — شناسهٔ بدون‌برخورد (`1e317f3`)
- **مسئله:** هر پنج مسیر با «بیشینهٔ فعلی + ۱» شناسه می‌ساختند؛ در درخواست‌های
  همزمان (و قطعاً چندنمونه‌ای) برخورد ممکن بود.
- **اجرا:** ماژول تازهٔ `server/ids.js`: در پستگرسِ فعال `nextval` روی دنبالهٔ
  `payesh_<col>_id_seq` با بوت‌استرپ زیر قفل مشاوره‌ای و `setval` رو به جلو؛
  بدون پستگرس، مکس+۱ زیر میوتکس فرایندی + قفل توزیع‌شده. هر پنج مسیر به
  `await ids.nextId(...)` وصل شدند. دانش‌آموز و کاربر فضای نام مشترک `users`
  دارند (یک آرایهٔ منبع).
- **تست پذیرش:** ۱۰۰ دریافت همزمان → ۱۰۰ شناسهٔ یکتا و بی‌فاصله؛ نمونهٔ دومِ
  شبیه‌سازی‌شده روی دنبالهٔ مشترک، عدد تکراری نمی‌گیرد.

### ۵) P0-17 — حذف امن با سنگ‌قبر (`6531e69`)
- **مسئله:** هر پنج حذف، `splice` خام بود — داده بی‌صدا نابود می‌شد، بدون ردّ
  قابل بازیابی و بدون رویداد برای نمونه‌های دیگر.
- **اجرا:** اسپلایس خام از همهٔ مسیرها حذف شد. `server/delete-service.js` تنها
  درگاه حذف است: نسخهٔ رکورد بالا می‌رود → رکورد کامل با متادیتا
  (حذف‌کننده/زمان/دلیل) در `store.tombstones` بایگانی می‌شود → رویداد
  `<col>.deleted` در `server/outbox.js` (`store.outbox` + جدول `server_outbox`
  در اسکیمای پستگرس، سقف صف ۱۰۰۰) می‌نشیند → سپس خروج از مجموعهٔ زنده +
  `persistOp` + ممیزی. برابری چندقسمتی پشتیبانی می‌شود (حذف دانش‌آموز نقش هم
  می‌سنجد).
- **تست پذیرش:** رفتار واحد + برابری نقش + سقف صندوق + e2e واقعی با نشست
  سوپرادمین: ساخت کلاس → حذف ۲۰۰ → ۴۰۴ → سنگ‌قبر و رویداد در اسنپ‌شات.

### ۶) P0-18 — همزمانی خوش‌بینانه سراسری (`1dee995`)
- **مسئله:** فقط نمره‌ها OCC داشت؛ حضور و غیاب نسخه را بی‌بررسی بالا می‌برد؛
  کلاس/دانش‌آموز/کاربر اصلاً نسخه نداشتند → نوشتِ همزمان = بازنویسی گمشده.
- **اجرا:** `server/occ.js` (`checkOcc`/`bump`) به هر پنج مسیر وصل شد: نوشت با
  نسخهٔ پایهٔ کهنه ⇒ **۴۰۹ + `server_version`** (معادل
  `UPDATE ... WHERE id=$id AND version=$base` با صفر ردیف). پذیرش پایه از هر دو
  فیلد `base_version`/`version`. رکوردهای تازه با `version: 1` زاده می‌شوند.
  کلاینتِ بدون نسخه برای سازگاری با رابط فعلی می‌گذرد ولی بامپ می‌شود.
- **تست پذیرش:** چرخهٔ کامل ۲۰۰ → ۴۰۹ → ۲۰۰ روی کلاس؛ دو نوشتِ همزمان با
  پایهٔ یکسان → دقیقاً یکی ۲۰۰ و یکی ۴۰۹؛ حضور و غیابِ کهنه ۴۰۹ می‌گیرد.

## دروازه‌ها (اجرای نهایی روی `1dee995`)

- **دودی:** ۵۴۷/۵۴۷ ✅
- **مجوزها (`tools/check-authz.js`):** خروجی ۰ ✅
- **سوئیت‌های فاز ۲:** ۹۲/۹۲ (شش فایل) ✅
- **مسیرها/کش/سرور:** کلاس‌ها ۵، نمره‌ها ۵، دانش‌آموزان ۷، کاربران ۵، حضورغیاب ۵،
  بوت‌استرپ ۵، کش ۷، `otp-ratelimit` ۴۹ — همگی سبز ✅

## فایل‌های تغییرکرده

`server/`: `redis.js`, `cache.js`, `index.js`, `otp-store.js`, `auth.js`,
`ids.js` (تازه), `outbox.js` (تازه), `delete-service.js` (تازه), `occ.js` (تازه),
`schema.sql`, `routes/{attendance,classes,grades,students,users}.js`
`tests/`: `redis-fallback.js`, `lock-atomic.js`, `otp-redis.js`,
`id-collision.js`, `tombstone.js`, `occ.js` (همه تازه), `api/cache.test.js`
مستندات: `HANDOFF.md`, `docs/AI_PROMPT.md` (§۰.۵.۲۷)

## یادداشت‌ها و ریسک‌ها

- **سازگاری کلاینت:** در P0-18 کلاینتی که نسخه نفرستد همچنان می‌گذرد تا رابط
  فعلی نشکند؛ برای قفلِ کاملِ «نوشتِ گمشده» باید کلاینت نسخه را بفرستد
  (پیشنهاد: در دور بعدی فرانت `base_version` را از بوت‌استرپ بگیرد و بفرستد).
- **قفل در دسترس نبودن:** اگر قفل یک ثانیه گرفته نشود، فلاشِ حالت به دور بعد
  می‌ماند (داده گم نمی‌شود، فقط دیرتر منتشر می‌شود).
- **دنباله‌های پستگرس:** بوت‌استرپ خودکار است ولی دسترسی `CREATE SEQUENCE` و
  قفل مشاوره‌ای لازم دارد؛ بدون آن به محافظ محلی برمی‌گردیم.
