# گزارش E.2 فرناز — شمارش‌معکوس و یادآوری امتحان (چت ۳، آیتم ۲)

## چه ساخته شد (کلاینت‌فقط)
- **شمارش‌معکوس** در داشبورد خانواده (`examCountdownCard` در `summaryBlock`): نزدیک‌ترین امتحانِ
  کلاس از همان کالکشن `exams` — «۳ روز تا امتحان ریاضی» / «فردا امتحان…» / «امروز امتحان…» + تاریخ، ساعت، اتاق، بارم.
- **یادآوری** در پنجرهٔ ۰ تا ۲ روز مانده (روز امتحان هم پوشش داده می‌شود اگر داشبورد دیر باز شود):
  ۱. پیامک از **صف موجود** (`notifyRequest` با `kind=event` و `source_ref=examrem:<id>`)؛
  ۲. اعلان داخل‌برنامه (`type=exam_remind` + آیکون ⏳) برای دانش‌آموز و همهٔ والدین لینک‌شده.
- **ضدتکرار دوگانه** (چون فراخوان از مسیر رندر است): پیامک با `source_ref` روی دیتابیس،
  اعلان داخلی با نشانِ حافظهٔ محلی. اگر پیامک مدرسه خاموش باشد، فقط اعلان داخلی می‌رود.
- بدون `data-act` جدید ⇒ بدون تغییر در authz؛ هیچ نقشی هاردکد نشد؛ صفحهٔ حضور دست‌نخورده.

## نتایج
| گیت | نتیجه |
|---|---|
| سئوت جدید `tests/examcount.js` | 16/16 ✅ |
| تست جهش `tests/examcount-mutations.js` | 5/5 کشته ✅ |
| رگرسیون E.1 (`tests/tomorrow.js`) | 15/15 ✅ |
| `build --check` / `check-authz` | 0 / 0 ✅ |
| `tests/smoke.js` | 547/547 ✅ |
