# پایش — سامانه هوشمند مدیریت مدرسه

## وضعیت پروژه
- **معماری:** تک‌فایلی، کاملاً آفلاین (`index.html` ~۴۲۷KB)
- **فونت:** Vazirmatn به‌صورت base64 جاسازی‌شده (بدون CDN)
- **لوگو:** جاسازی‌شده (data URI)
- **زبان/جهت:** فارسی، RTL
- **ذخیره‌سازی:** localStorage با الگوی event-log (`log` → `applyOp` → `applyLog`)
- **داده:** دیتای دمو با تولیدکننده‌ی شبه‌تصادفی (`SEED` + `rng()`) — قابل بازتولید

> ⚠️ در نسخه‌ی آپلودشده یک اسکریپت Cloudflare (`/cdn-cgi/challenge-platform`) به انتهای فایل چسبیده بود که از مرورگر تزریق شده بود؛ در ورک‌اسپیس **حذف شد**.

## نقش‌های کاربری (۶ نقش)
| نقش | کلید | توضیح |
|---|---|---|
| مدیر کل سامانه | `superadmin` | نظارت کلان، مدارس، ادارات، تقسیمات کشوری، اشتراک‌ها |
| اداره آموزش‌وپرورش | `edu_office` | داشبورد آماری منطقه، مدارس تحت پوشش |
| مدیر مدرسه | `manager` | کلاس، درس، کاربر، حضورغیاب، نمره، انضباط، امتحانات، مالی |
| دبیر | `teacher` | کلاس‌های من، ثبت نمره/حضور/انضباط، مراقبت امتحان |
| دانش‌آموز | `student` | کارنامه، برنامه، شهریه، مرخصی |
| اولیا | `parent` | پرونده فرزندان، خانواده، شهریه و اقساط، اشتراک (paywall) |

## ماژول‌های پیاده‌شده (routes)
`dashboard` · `schools` · `users` · `classes` · `subjects` · `attendance` · `grades` ·
`discipline` · `schedule` · `announcements` · `record` · `children` · `notifications` ·
`leaves` · `calendar` · `chat` · `tuition` · `mytuition` · `regions` · `exams` ·
`teachers` · `corrections` · `family` · `geo` · `offices` · `officedash` ·
`officeschools` · `subscription` · `adminsubs`

## مجموعه‌های داده (db collections)
`users` `schools` `classes` `subjects` `enrollments` `attendance` `grades` `discipline`
`schedule` `announcements` `notifications` `leaves` `messages` `tuitions` `installments`
`transactions` `exams` `exam_terms` `exam_duties` `corrections` `teacher_schools`
`parent_links` `parent_subscriptions` `parent_verifications` `subscription_payments`
`provinces` `counties` `districts` `offices` `app_settings`

## قابلیت‌های شاخص
- **تقویم جلالی کامل:** `toJalali` / `toGregorian` / `isLeapJ` / `monthMatrix` + کامپوننت date-picker (`jdate`)
- **پیمایش با دکمه Back گوشی:** `S.stack` + `history.pushState` + `goBack()`
- **Paywall اولیا:** `parentLocked()` → `viewLocked()`؛ فقط `subscription`/`notifications`/`announcements` باز است
- **دروازه‌ی احراز ولی:** `parentGate()` + `validNid()` (اعتبارسنجی کد ملی) + `makeNid()`
- **چاپ رسید:** `printReceipt()` — تولید سند مستقل با auto-print
- **تقسیمات کشوری:** استان → شهرستان → منطقه → اداره
- **ریسپانسیو موبایل:** ۳ اندازه‌ی گوشی تست‌شده

## تست‌های انجام‌شده (طبق چت قبلی)
| بخش | نتیجه |
|---|---|
| تک‌فایلی (فازهای ۱۱ تا ۱۶) | ✅ |
| سرور (۱۶ فاز) | ۷۳۹ ✅ / ۰ ❌ |
| رابط کاربری موبایل | ۱۱۴ ✅ / ۰ ❌ (شامل ۷ صفحه‌ی جدید در ۳ اندازه) |
| **مجموع** | **۱۲۲۸ سناریو، بدون خطا** |

## قرارداد کاری
> هر قابلیت جدید **همزمان در هر دو نسخه** (تک‌فایلی و سرور) پیاده و تست می‌شود.

## ساختار ورک‌اسپیس (ساخته‌شده)
فایل تک‌فایلی به ساختار ماژولار تفکیک شد — **بدون هیچ اتلاف**:
`build.js --check` تأیید می‌کند ادغام مجدد بیت‌به‌بیت با اصل یکسان است.

```
payesh/
├── index.html          خروجی توزیعی (تک‌فایلی، آفلاین)
├── build.js            ادغام‌کننده + راستی‌آزما
├── package.json        صفر وابستگی
├── src/                ۲۴ ماژول JS + ۳ فایل CSS + اسکلت HTML
├── scripts/dev-server.js
├── tests/run.js        ۳۱ تست
├── docs/               ARCHITECTURE.md + DEVELOPMENT.md
├── server/             (رزرو برای نسخه سرور)
├── public/  data/  dist/
```

**وضعیت تست‌ها:** ۳۱/۳۱ ✅ | **build:** بیت‌به‌بیت یکسان ✅ | **git:** کامیت اولیه ثبت شد

## تغییرات دور ۲ (انجام‌شده)
1. **فیلترهای جمع‌شونده** — ماژول `25-filters-ui.js`؛ فیلترها پیش‌فرض بسته‌اند، با دکمه «⚙️ فیلترها» باز می‌شوند. تعداد فیلترهای فعال روی دکمه نمایش داده می‌شود. اعمال‌شده در: مدارس، کاربران، دروس، نمرات، انضباط، داشبورد اداره، مدارس تحت پوشش.
2. **حذف دکمه اضافی «غیرفعال کن»** — نشان وضعیت در جدول خودش قابل‌کلیک است (کلاس `.tgl`).
3. **نوع منطقه** — فقط «منطقه» و «روستا» (حذف شهری/عشایری).
4. **حذف «کلاس‌ها» از منوی سوپرادمین** — route هم به داشبورد هدایت می‌شود.
5. **جستجو و فیلتر دروس** — جستجوی نام/کد + فیلتر مقطع/پایه/شاخه/رشته/مدرسه، با گروه‌بندی مقطع › پایه › رشته.
6. **ساختار کتاب‌های درسی** — ماژول `26-curriculum.js`:
   - ابتدایی (۱–۶) و متوسطه اول (۷–۹): کتاب‌ها بر اساس پایه، مشترک
   - متوسطه دوم (۱۰–۱۲): تفکیک بر اساس **شاخه** (نظری/فنی‌وحرفه‌ای/کاردانش) و **رشته** (۱۴ رشته)
   - دکمه «📥 افزودن کتاب‌های استاندارد» با انتخاب آبشاری و پیش‌نمایش زنده

**وضعیت تست:** ۳۱/۳۱ ساختاری + ۷۶/۷۶ دودی (رندر همه صفحات × همه نقش‌ها) ✅

## دور ۳ — لایه offline-first (انجام‌شده)

**اصلاح درک قبلی:** «آفلاین» یعنی دبیر بدون اینترنت کار کند و داده در دستگاه بماند
تا اتصال برقرار شود — نه صرفاً «بدون منبع خارجی».

ماژول `27-sync.js`:
- **صف ارسال** روی `localStorage` (کلید `sms_syncq_v1`) — پس از بستن مرورگر هم باقی می‌ماند
- هر `insert/update/remove` خودکار وارد صف می‌شود (قلاب در `applyOp`)
- `SYNC_MUTED` جلوی ورود داده‌ی دمو و بازپخش لاگ به صف را می‌گیرد
- تشخیص خودکار آنلاین/آفلاین با رویدادهای `online`/`offline`
- **ارسال خودکار** به‌محض وصل شدن اینترنت + **backoff نمایی** (۲s→۵min) در خطا
- نشانگر زنده در نوار بالا: همگام / در صف ‹تعداد› / آفلاین ‹تعداد› / همگام‌سازی… / تعارض
- پنجره‌ی جزئیات: فهرست عملیات معلق، وضعیت، زمان شمسی، دکمه ارسال دستی
- کلید `demoMode` — با اتصال سرور واقعی فقط `SYNC.demoMode=false` و `SYNC.serverUrl` تنظیم شود
- ساختار پاسخ سرور: `{results:[{uid,ok}|{uid,conflict,server,message}]}` — حالت `conflict` آماده است

**وضعیت تست:** ۳۱/۳۱ ساختاری + ۸۹/۸۹ دودی + E2E واقعی مرورگر
(آفلاین → ۲ عملیات در صف → اتصال → ارسال خودکار → داده سالم) ✅

## کارهای بعدی
- [ ] اتصال به سرور واقعی (`SYNC.demoMode=false`)
- [ ] حل تعارض (UI انتخاب نسخه محلی/سرور)
- [ ] Service Worker برای بارگذاری آفلاین خودِ برنامه

## نحوه‌ی اجرا
```bash
cd /home/user/payesh
npm run dev          # سرور توسعه + بازسازی خودکار (پورت ۳۰۰۰)
npm run build        # ساخت فایل نهایی
npm run build:check  # راستی‌آزمایی یکسانی
npm test             # ۳۱ تست
```

> ⚠️ هرگز `index.html` را مستقیم ویرایش نکنید — خروجی build است. در `src/` تغییر دهید.


## دور ۴ — مقیاس ملی (۱۰ میلیون دانش‌آموز / ۵ میلیون کاربر همزمان)

**اصل حاکم:** بار هر مرورگر باید ثابت و مستقل از مقیاس کشور بماند.

### ماژول‌های جدید
- `src/js/28-indexes.js` — لایه ایندکس با باطل‌سازی خودکار از `applyOp`.
  `idxGroup/idxUnique/idxInvalidate/idxReset/idxReport` + ۸ ایندکس آماده + `sortByNameFa`.
- `src/js/29-scope.js` — محدودهٔ دادهٔ هر نقش، `SCOPE_LIMITS`، قرارداد `/api/bootstrap`.

### اصلاحات کارایی
- `04-queries.js`: `byId`, `classOf`, `studentsOfClass`, `teacherClasses` ایندکس‌محور شدند.
- `12-attendance.js`: `studentsOfClass` یک‌بار (نه دو بار) + `Map` کلاس/روز.
- `08-dashboard.js`: نمودار روند از ۱۴ پیمایش به ۱ رسید؛ `avgBySchool` تک‌پیمایشی شد.
- `03-persistence.js`: `saveLog` دیگر خطا را بی‌صدا نمی‌بلعد + `storageFull()`.

### نتایج اندازه‌گیری‌شده
- `viewAttendance` مدرسه ۵٬۰۰۰ نفره: **۶٬۴۲۲ms → ۲ms**
- بنچمارک مقیاس ایران (کلاس ۴۰ نفره، سال ۱۸۰ روزه): همه زیر **۱۰۰ms**؛
  حداکثر ایران (۱٬۵۲۰ نفر) = ۳۵ms، دو برابر حداکثر (۳٬۰۰۰ نفر) = ۷۱ms.
- برش هر نقش: دانش‌آموز **۸KB**، دبیر ۳۵۶KB، مدیر ۱۰۲۹KB (پیش‌تر همه ۴.۵۹MB).
- پایداری: ۱٬۰۰۰ رندر + ۱۰ تغییر داده → ۰ خطا، رشد حافظه ۱.۰MB، اصابت ایندکس ۱۰۰٪.
- امنیت پس از بهینه‌سازی: XSS escape، دامنهٔ دید نقش‌ها دست‌نخورده.

### تست
`node tests/run.js` = ۳۱/۳۱ · `node tests/smoke.js` = **۱۰۵/۱۰۵** (۱۶ تست جدید).
`node build.js --check` بیت‌به‌بیت یکسان. خروجی ۴۹۹.۲KB، ۲۹ ماژول.

### اسناد
- `docs/SCALE_10M.md` — معماری مقیاس ملی
- `docs/CLIENT_CAPACITY.md` — کشش سمت کلاینت
- `docs/CAPACITY.md` — ظرفیت سمت سرور
- `docs/AI_PROMPT.md` — **به‌روزرسانی شد** با بخش‌های ایندکس، محدوده و قواعد مقیاس


## بررسی مخزن گیت‌هاب (github.com/rezaa2544/payesh)

مخزن فقط شامل `school.rar` بود = **نسخهٔ سروری** (React 19 + Vite + Express 5 +
SQLite/PG، ۳۶ صفحه، ۳۳ فایل سرور، ~۱۴٬۶۰۰ خط، ۱۶ فاز تست).

**نتیجهٔ مقایسه:** هیچ قابلیتی جا نیفتاده — فایل مبنای من (نسخه ۹) این‌ها را
از ابتدا نداشت. اما ۱۰ قابلیت در نسخهٔ سروری هست که باید به تک‌فایلی افزوده شود:
چرخهٔ تحصیلی، ویزارد اکسل، طرح‌ها، گزارش‌گیری، فرم‌ها و پیامک، تنظیمات،
صورتحساب، جلسات اولیا، رشد مدرسه، افت تحصیلی.

در مقابل، ۶ قابلیت من دارم که سروری ندارد: فیلترهای جمع‌شونده، کتاب‌های درسی،
offline-sync، لایه ایندکس، محدودهٔ داده، محافظت حافظه.

جزئیات کامل: `docs/GITHUB_COMPARISON.md`

✅ **فایل مرجع پیدا شد** — مخزن دوم `github.com/rezaa2544/p2` شامل
`پایش-سامانه-مدیریت-مدرسه.html` (۵۳۴KB) بود. در `reference/payesh-full-reference.html`
نگه داشته شد.

**مقایسه دقیق:** مرجع ۳۶ تابع `view*` دارد، من ۳۰ — دقیقاً **۶ صفحه** غایب و
**صفر** مورد اضافه (یعنی چیزی حذف نکرده‌ام). لازم برای تکمیل:
۶ تابع view + ۱۴ تابع کمکی + ۱۵ اکشن + ۷ مجموعه داده + ۶ روت.

⚠️ مرجع سه مشکل کارایی دارد که من رفع کرده‌ام (جستجوی خطی حضور و غیاب،
۱۴ پیمایش داشبورد، saveLog بی‌صدا) — هنگام انتقال باید با الگوی ایندکس بازنویسی شود.

## قاعدهٔ دائمی جدید کاربر
پس از **هر** تغییر در برنامه، `docs/AI_PROMPT.md` باید به‌روز شود و نسخهٔ
به‌روزشده در ورک‌اسپیس قرار گیرد. (ثبت شد در دستور نقش، بند ۱۰)


## دور ۵ — ممیزی کامل امنیت و کارایی

### 🔴 باگ بحرانی یافت و رفع شد
`renderRoute()` هیچ بررسی مجوزی نداشت — فقط روی `S.route` سوییچ می‌کرد.
`NAV` تعیین می‌کرد چه چیزی در منو دیده شود، ولی چیزی جلوی تغییر مستقیم
`S.route` را نمی‌گرفت.

**اثبات‌شده با تست واقعی:** دانش‌آموز با `S.route='attendance'` کل لیست کلاس را
دید و با کلیک روی `att-set` برای هم‌کلاسی‌اش **غیبت جعلی ثبت کرد** که وارد
**صف ارسال به سرور** هم شد. همچنین `adminsubs` پنل ۵۱KB سوپرادمین را باز کرد.

**رفع:** ماژول جدید `src/js/30-authz.js`
- `canRoute()` در `renderRoute()` (07-shell.js)
- `canAction()` در دیسپچر کلیک (19-actions.js)
- مجوز از خودِ `NAV` استخراج می‌شود (تک‌منبع حقیقت) + `EXTRA_ROUTES` + `ACTION_ROLES`
- راستی‌آزمایی: ۱۰ دسترسی غیرمجاز مسدود، ۱۹ دسترسی مجاز دست‌نخورده (صفر رگرسیون)

### گلوگاه‌های کارایی رفع‌شده
- `08-dashboard.js` رتبهٔ دانش‌آموز: `O(peers×grades)` → ایندکس. کلاس ۳۲۰ نفره ۱۲.۷ms → **۲.۰ms**
- `19-actions.js` اکشن `att-all`: find خطی → `idxAttByClassDate` + Map
- `11-classes-subjects.js` شبکهٔ کلاس‌ها: filter به ازای هر کارت → `idxEnrollByClass`

### راستی‌آزمایی امنیتی
- XSS: ۴ payload × ۱۶ مجموعه × ۵۰ فیلد، بررسی **DOM واقعی** در ۱۷۴ ترکیب → صفر نشتی
- درس روش‌شناختی: جستجوی متنی در خروجی برای تشخیص XSS **غلط** است (فرم escape‌شده
  را مثبت کاذب می‌شمارد)؛ باید `querySelectorAll` روی DOM واقعی زد
- مرز چندمستأجری، اداره، paywall، جانشینی، پرسونای دوم → همه سالم

### نتایج
`node tests/run.js` = ۳۱/۳۱ · `node tests/smoke.js` = **۱۱۵/۱۱۵** (۱۰ تست امنیتی جدید)
بنچمارک: حداکثر مدرسهٔ ایران (۱٬۵۲۰ نفر) = **۵۴.۳ms** · ۳۸ ترکیب نقش×روت همه زیر ۲۰ms
پایداری: ۱٬۵۰۰ رندر → ۰ خطا، ۱.۳MB رشد حافظه، اصابت ایندکس ۱۰۰٪

### پرامپت
`docs/AI_PROMPT.md` (۹۵۰ خط) به‌روز شد + بخش جدید **«۲.۵ راهنمای خط‌به‌خط»**
شامل جدول «کجا چه چیزی را پیدا کنم» و نمودار توالی اجرای یک کلیک.


## دور ۶ — افزودن چرخهٔ تحصیلی + کشف باگ کارایی درجه‌دوم

### قابلیت جدید: `31-lifecycle.js` (اولین از ۶ صفحهٔ غایب)
پنج تب: ارتقای پایه · انتقالی‌ها · تعارض کد ملی · داده ناقص · بایگانی
۴ مجموعه داده (`student_transfers`, `transfer_requests`, `student_archive`,
`nid_conflicts`) + ۷ اکشن + مجوز فقط مدیر/سوپرادمین.

**کپی مستقیم از مرجع نشد** — با الگوی ایندکس‌شده بازنویسی شد
(`idxGradesByStudent`, `idxAttByStudent`, `idxEnrollByClass`).

**بهبود نسبت به مرجع:** `moveStudent` در مرجع فقط `users.school_id` را عوض
می‌کرد؛ من `school_id` رکوردهای grades/attendance/discipline را هم به‌روز
می‌کنم، وگرنه پرونده در مدرسهٔ جدید دیده نمی‌شد.

### 🔴 باگ کارایی درجه‌دوم (کشف حین تست فارغ‌التحصیلی انبوه)
فارغ‌التحصیل کردن ۴۸۰ دانش‌آموز **۳٬۲۰۰ms** طول می‌کشید و UI قفل می‌شد.

سه علت با اندازه‌گیری تفکیکی پیدا شد (نه حدس):
1. `saveQueue()` در `enqueueOp` — **علت اصلی**؛ هر عملیات کل صف را stringify
   می‌کرد (۲۱۹ms برای ۵۰۰ insert؛ با SYNC_MUTED فقط ۲۴ms)
2. `saveLog()` — همان مشکل روی لاگ
3. `applyOp` ایندکس را **پیش از** جستجو باطل می‌کرد ⇒ `idxById` کل مجموعه
   را در هر نوشتن بازمی‌ساخت

**رفع:** `batchWrites(fn)` در `03-persistence.js` (تودرتو-امن، استثنا-امن،
پوشش هم‌زمان لاگ و صف) + جابه‌جایی ترتیب باطل‌سازی در `applyOp`.

**نتیجه: ۳٬۲۰۳ms → ۱۸۷ms (۱۷ برابر)** با تضمین اینکه هیچ عملیاتی گم نمی‌شود.

### درس روش‌شناختی
دو بار حدس اشتباه زدم (اول saveLog، بعد applyOp) و هر بار فقط ۱.۵× بهبود گرفتم.
وقتی هزینه را **تفکیکی اندازه گرفتم** علت اصلی (saveQueue) در یک نگاه پیدا شد.
درس: پیش از بهینه‌سازی، سهم هر بخش را جداگانه بسنج.

### نتایج
`npm test` = ۳۱/۳۱ + **۱۲۷/۱۲۷** (۱۲ تست جدید)
بنچمارک با lifecycle: معمول ۴۲ms · بزرگ ۵۱ms · حداکثر ایران ۵۹ms
پایداری ۱۵۰۰ رندر: ۰ خطا، ۱.۱MB، اصابت ایندکس ۱۰۰٪

### باقی‌مانده
۵ صفحه: `import` (پیچیده‌ترین، xlsx بومی) · `formssms` · `meetings` · `growth` · `atrisk`


## دور ۷ — سه صفحهٔ تعامل با خانواده

### `32-engagement.js`
- **atrisk** — امتیاز ریسک ۰–۱۰۰ از پنج نشانه (میانگین اخیر، افت، غیبت،
  انضباطی، نمرات زیر ۱۰)؛ سطح high/medium/low؛ اکشن `risk-notify`
- **meetings** — نوبت‌دهی جلسه؛ کارکنان می‌سازند، ولی رزرو می‌کند؛
  دو قاعده: نوبت تکراری ممنوع، هر ولی یک نوبت در روز
- **growth** — کد معرف، نرخ تبدیل، سهم مدرسه، دعوت گروهی اولیا

مجموعه داده `meeting_slots` + ۸ اکشن + مجوز تفکیک‌شده به نقش
(مثلاً `mtg-book` فقط ولی، `mtg-new` فقط کارکنان).

### بهینه‌سازی: getter تنبل برای `reasons`
`atrisk` در بدترین حالت (همهٔ ۱٬۵۲۰ نفر پرخطر) **۸۹ms** بود.
تفکیک هزینه نشان داد محاسبه ۷۱ms و رندر فقط ۱۸ms است — علت: ساخت
رشته‌های فارسی «دلایل» برای همهٔ دانش‌آموزان درحالی‌که فقط ۱۵۰ ردیف
نمایش داده می‌شود. `reasons` به **getter** تبدیل شد ⇒ **۸۹ms → ۳۳ms**.

### درس تکرارشده
باز هم اول حدس زدم مشکل «پیمایش حضور» است. اندازه‌گیری تفکیکی نشان داد
پیمایش فقط ۳ms است و مقصر جای دیگری بود. **همیشه تفکیکی اندازه بگیر.**

### نتایج
`npm test` = ۳۱/۳۱ + **۱۳۴/۱۳۴** (۷ تست جدید)
بنچمارک هر هفت صفحه: معمول ۳۳ms · بزرگ ۴۵ms · حداکثر ایران ۶۲ms
پایداری ۱۵۰۰ رندر: ۰ خطا، ۱.۷MB، اصابت ایندکس ۱۰۰٪

### باقی‌مانده
۲ صفحه: `formssms` (فرم رسمی + پنل پیامک) · `import` (ویزارد اکسل، پیچیده‌ترین)


## دور ۸ — دو صفحهٔ پایانی: همهٔ قابلیت‌های مرجع تکمیل شد

### قاعدهٔ نگارشی جدید کاربر
کاربر گفت متن فارسی و انگلیسی درهم می‌شود و خوانا نیست. از این پس همهٔ
توضیح‌ها فارسی نوشته می‌شود و نام‌های انگلیسی فقط داخل قطعه‌کد، مسیر فایل
یا سلول جدول می‌آیند. این قاعده به‌عنوان بند ۱۲ به دستور نقش پرامپت اضافه شد.

### `33-forms-sms.js` — فرم‌های رسمی و پنل پیامک
سه سند چاپی A4 (لیست نمرات، صورت‌جلسهٔ امتحان، دفتر آمار) و پنل پیامک با
کیف پول جدا برای هر مدرسه. سازندهٔ بدنهٔ سند از عمل چاپ جدا نوشته شد تا
بدون باز کردن پنجره آزمون‌پذیر باشد.

دفتر آمار با یک پیمایش گروه‌بندی می‌کند (در مرجع به‌ازای هر کلاس کل جدول
پیمایش می‌شد). سه قاعده آزموده شد: اعتبار ناکافی مانع ارسال می‌شود، متن
کوتاه رد می‌شود، گیرندگان یکتا و دارای شمارهٔ معتبرند.

### `34-import.js` — ویزارد ورود اکسل
خوانندهٔ بومی xlsx بدون هیچ کتابخانه‌ای: ساختار بایگانی فشرده مستقیم خوانده
و با `DecompressionStream` باز می‌شود. اگر مرورگر پشتیبانی نکند پیام روشن
داده می‌شود و csv کار می‌کند.

نگاشت هوشمند ستون‌ها با یکسان‌سازی نویسه‌های عربی کار می‌کند («كدملي» =
«کد ملی») و ردیف تیتر خودکار پیدا می‌شود. کد ملی متعلق به مدرسهٔ دیگر رد
شده و خودکار در تعارض‌ها ثبت می‌شود — پل میان ویزارد و چرخهٔ تحصیلی.
ثبت ۱۰۰۰ دانش‌آموز در ۲۹۷ میلی‌ثانیه.

### دو خطای آزمون که کشف و اصلاح شد (نه اشکال کد)
۱. `\n` داخل رشتهٔ آزمون در `tests/smoke.js` به خط جدید تبدیل نمی‌شد و
   شش آزمون را می‌شکست. با `String.fromCharCode(10)` حل شد.
۲. جستجوی کاربر با نام «علی رضایی» نفر اشتباه را برمی‌داشت چون دادهٔ نمونه
   دو نفر همنام دارد. با نام یکتا حل شد.
هر دو به جدول اشتباهات رایج پرامپت اضافه شدند.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۱۵۳ از ۱۵۳** · ساخت بیت‌به‌بیت یکسان
بنچمارک هر نُه صفحه: معمول ۳۵ · بزرگ ۴۷ · حداکثر ایران ۷۱ میلی‌ثانیه
پایداری ۱۵۰۰ رندر روی چهار نقش: صفر خطا، ۱.۳ مگابایت، اصابت ایندکس ۱۰۰٪

### وضعیت نهایی
نسخهٔ تک‌فایلی اکنون همهٔ ۳۶ صفحهٔ نسخهٔ سروری را دارد، به‌علاوهٔ شش قابلیتی
که فقط در تک‌فایلی هست. **هیچ صفحهٔ غایبی باقی نمانده است.**


## دور ۹ — پنل سوپرادمین و ساختاردهی کد

### سه قابلیت تازه در `35-superadmin.js`
- **مالی سراسری** (روت `finance`): درآمد اشتراک، شهریه دریافتی و معوق،
  وضعیت اشتراک‌ها، روند شش ماهه، رتبه‌بندی مالی مدارس. همه با یک پیمایش
  روی هر جدول (نه حلقه تودرتو).
- **بازنشانی رمز** (اکشن `pass-reset`): رمز موقت هشت‌نویسه‌ای + اعلان به کاربر.
  مرز امنیتی: سوپرادمین نمی‌تواند رمز سوپرادمین دیگر را عوض کند؛ مدیر فقط
  دانش‌آموز و دبیر و ولیِ مدرسه خودش. بدون این، مدیر می‌توانست کنترل کل
  سامانه را بگیرد.
- **سلامت سامانه** (روت `health`): حافظه، ایندکس، صف همگام‌سازی، حجم داده +
  سه ابزار نگهداری (پشتیبان‌گیری، بازسازی ایندکس، تلاش دوباره).

### یک اشتباه که گرفتم
رشته‌ای که برای افزودن مجوز جایگزین کردم، هم در سطر سوپرادمین و هم مدیر
بود و به مدیر خورد. آزمون نشان داد مدیر به صفحه مالی سراسری دسترسی دارد.
اصلاح شد؛ حالا فقط سوپرادمین دسترسی دارد ولی بازنشانی رمز برای مدیر ماند.

### ساختاردهی کد (خواسته کاربر)
کاربر گفت برنامه‌نویس باید راحت بخش موردنظر را پیدا کند. سه کار انجام شد:

۱. **هشت فایل با نام گنگ تغییر نام یافتند** — نام‌هایی مثل «فاز ۳ تا ۷»
   هیچ چیزی نمی‌گفتند. حالا `20-communication-finance`، `24-edu-office`،
   `31-student-lifecycle` و…
۲. **۱۹ فایل سرفصل استاندارد گرفتند** — عنوان فارسی + یک جمله توضیح.
۳. **`CONTRIBUTING.md` نوشته شد** (۲۴۸ خط) — جدول «کجا چه چیزی را پیدا
   کنم»، دستور گام‌به‌گام افزودن صفحه تازه، قواعد امنیت و کارایی، و
   دام‌های آزمون‌نویسی.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۱۶۱ از ۱۶۱** (۸ آزمون تازه) · ساخت بیت‌به‌بیت یکسان
کارایی صفحات تازه: زیر یک میلی‌ثانیه حتی با ۱۵۲۰ دانش‌آموز

### باقی‌مانده از خلأهای سوپرادمین
سابقه تغییرات · وضعیت و فعالیت مدارس · خروجی اکسل · اطلاعیه سراسری ·
مدیریت پلان فروش · پشتیبان‌گیری کامل (فعلاً فقط دفترچه عملیات)


## دور ۱۰ — سابقه تغییرات و آمار بازدید و بار

### `36-audit-activity.js`

**سابقه تغییرات** (روت `audit`): دفترچه عملیات پیش از این فقط
`{t, c, data}` داشت و هیچ ردی از «چه کسی» نبود. دو کلید `by` و `at`
اضافه شد. نام فارسی ۳۹ مجموعه در `COLL_FA` و هفت مجموعه حساس در
`SENSITIVE`. چهار فیلتر: جستجو، نوع داده، نوع عمل، فقط حساس.

**بازدید و بار** (روت `activity`) — خواسته کاربر:
- کاربران برخط: فعالیت در پنج دقیقه اخیر
- بازدید امروز و روند چهارده روزه
- بار برنامه: زمان واقعی رندر با صدک ۵۰، ۹۰، ۹۹ و کندترین صفحه
- بار سرور: صف ارسال، عملیات یک ساعت اخیر، برآورد مقیاس ملی

### دو نکته که با سنجش تصمیم گرفته شد
۱. **حجم دفترچه:** فقط شناسه و زمان ذخیره شد نه رکورد کامل. سنجش نشان
   داد هر عملیات ۱۸۰ بایت ⇒ ظرفیت ۹۷۱ روز کار برون‌خط. با رکورد کامل
   به چند هفته می‌رسید.
۲. **سربار سنجش زمان:** `renderRoute` به دو تابع شکسته شد. اندازه‌گیری
   نشان داد سربار فقط ۰٫۶۳ میلی‌ثانیه است — ناچیز.

### دو خطا که آزمون گرفت
۱. متغیر محلی `f` (فیلترها) تابع سراسری `f` (سازنده فیلد فرم) را
   پوشاند ⇒ خطای «f is not a function». به `flt` تغییر یافت.
۲. تابع `shortStamp` را فرض کرده بودم وجود دارد؛ نداشت و نوشته شد.

هر دو به جدول اشتباهات رایج پرامپت و CONTRIBUTING اضافه شدند.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۱۷۱ از ۱۷۱** (۱۰ آزمون تازه) · ساخت بیت‌به‌بیت یکسان

### باقی‌مانده از خلأهای سوپرادمین
وضعیت و فعالیت مدارس · خروجی اکسل · اطلاعیه سراسری · مدیریت پلان فروش


## دور ۱۱ — سازگاری با فایل واقعی مدرسه

کاربر تصویر یک فایل اکسل واقعی مدرسه را فرستاد و پرسید آیا با نحوه کار
ما یکی است. **پاسخ: نبود.** ویزارد فقط ۶ ستون از ۳۵ را می‌شناخت.

### چهار ناهماهنگی که رفع شد
۱. فایل واقعی «نام» و «نام خانوادگی» را **جدا** دارد ⇒ فیلدهای
   `first_name` و `last_name` افزوده شد.
۲. معدل به شکل «۱۲/۸۸» نوشته می‌شود (اسلش = ممیز) ⇒ تابع `toNumFa`.
۳. شماره دانش‌آموز و پدر روی هم بازنویسی می‌شدند ⇒ سه شماره جدا.
۴. ستون «ردیف» به اشتباه نگاشت می‌شد ⇒ فهرست `IMP_IGNORE`.

**نتیجه: ۳۴ از ۳۵ ستون** خودکار شناخته می‌شود.

### تب شناسنامه
۳۸ فیلد تازه باید دیده هم می‌شدند. تب «شناسنامه» با هفت بخش افزوده شد
و فیلدهای خالی خودکار حذف می‌شوند تا کارت شلوغ نشود.

### 🔴 اشکالی که ضمن کار پیدا شد
مسیریاب `viewRecord(S.user.id)` را صدا می‌زد ⇒ همیشه پرونده خود کاربر.
مدیر اصلاً نمی‌توانست پرونده دانش‌آموزی را باز کند. تابع `recordTargetId()`
با مرز دسترسی کامل نوشته شد و با هشت آزمون راستی‌آزمایی شد.

### دو اصل دائمی کاربر
در بند ۱۲ دستور نقش پرامپت و بالای CONTRIBUTING ثبت شد:
۱. همگامی مستندات با هر تغییر، در همان نوبت
۲. تفکیک اصولی کد برای برنامه‌نویس تازه‌وارد

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۱۷۸ از ۱۷۸** (۷ آزمون تازه) · ساخت بیت‌به‌بیت یکسان


## دور ۱۲ — ابزارهای تکمیلی سوپرادمین

`37-admin-tools.js` سه خلأ باقی‌مانده را پر کرد:

۱. **وضعیت و فعالیت مدارس** — ستون تازه در فهرست مدارس که نشان می‌دهد
   هر مدرسه چند روز است بی‌استفاده مانده (سبز/نارنجی/قرمز/هرگز).
   از کلید `at` دفترچه عملیات که در دور دهم افزوده شد استفاده می‌کند.
۲. **خروجی اکسل** — چهار صفحه: مدارس، کاربران، مالی، سابقه.
   دو نکته امنیتی: پیشوند BOM برای فارسی، و خنثی‌سازی سلول‌هایی که با
   `=` شروع می‌شوند (تزریق فرمول).
۳. **اطلاعیه سراسری** — دو حالت: یک اطلاعیه برای همه کشور، یا نسخه جدا
   برای هر مدرسه.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۱۸۴ از ۱۸۴** (۶ آزمون تازه)

### باقی‌مانده از خلأهای سوپرادمین
مدیریت پلان فروش · پشتیبان‌گیری کامل (فعلاً فقط دفترچه عملیات)


## دور ۱۳ — پلان فروش و پشتیبان‌گیری (آخرین خلأهای سوپرادمین)

`38-plans-backup.js` دو خلأ باقی‌مانده را پر کرد:

**۱. مدیریت پلان فروش** (روت `plans`): سه پلان با آمار فروش، درآمد و
قیمت معادل ماهانه. سوپرادمین از رابط قیمت‌ها، دوره آزمایشی، سهم مدرسه
و دیوار پرداخت را تغییر می‌دهد. `validatePlanSettings` جلوی مقدار
بی‌معنا را می‌گیرد — از جمله قاعده کسب‌وکاری «سالانه نباید از ماهانه
گران‌تر باشد».

**۲. پشتیبان‌گیری و بازیابی**: پیش از این فقط دکمه دریافت بود و هیچ راهی
برای برگرداندن — یعنی عملاً بی‌فایده. حالا:
- `buildBackup` فقط دفترچه عملیات را ذخیره می‌کند (داده پایه با مولد
  قطعی بازساخته می‌شود) ⇒ حجم ده‌ها برابر کوچک‌تر
- `validateBackup` شش بررسی روی فایل
- `restoreBackup` داده پایه را بازمی‌سازد و عملیات را دوباره اجرا می‌کند

**آزمون واقعی:** داده را عمداً تخریب کردم (حذف اطلاعیه، حذف ۲۰ نمره،
تغییر نام کاربر) و بازیابی همه را برگرداند. فایل خراب هم رد می‌شود
بدون آنکه به داده موجود آسیبی بزند.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۱۹۳ از ۱۹۳** (۹ آزمون تازه)

### وضعیت
**همه نُه خلأ پنل سوپرادمین برطرف شد.**


## دور ۱۴ — اشتراک مشترک پدر و مادر

### تأیید چهار مورد پرسیده‌شده
کاربر پرسید آیا وضعیت مدارس، خروجی اکسل، اطلاعیه سراسری و مدیریت پلان
ساخته شده‌اند. آزمودم: **هر چهار مورد ✅**.

### قاعده جدید کسب‌وکار (خواسته صریح کاربر)
«اشتراک یک‌بار و برای یک سال تحصیلی است. کافی است پدر یا مادر یکی‌شان
بپردازد؛ هر دو دسترسی پیدا می‌کنند.»

**اصل:** اشتراک به دانش‌آموز تعلق دارد، نه ولی. دو تابع تازه:
`studentSubscription(studentId)` و `effectiveParentAccess(parentId)`.
`parentLocked()` حالا از دومی استفاده می‌کند.

### نکته ظریفی که کشف شد
`subOf(id, true)` خودش دوره آزمایشی می‌سازد. اگر اول اشتراک خودِ ولی
بررسی شود، ولی دومی که هم‌ولی‌اش پرداخت کرده بی‌جهت یک دوره آزمایشی
مصرف می‌کند و در آمار «صاحب اشتراک» شمرده می‌شود. پس ترتیب بررسی
عوض شد: اول هم‌ولی، بعد خودش.

### 🔴 اشکال ایندکس که ضمن کار پیدا شد
`byId('users',1130)` مقدار برنمی‌گرداند درحالی‌که کاربر در db بود.
علت: `add()` در داده نمونه مستقیم push می‌کرد و ایندکس را باطل نمی‌کرد.
ایندکس ۱۱۲۹ رکورد داشت و آرایه ۱۱۳۴ — **پنج کاربر نامرئی**.
رفع شد؛ حالا هر ۳۹ مجموعه همگام‌اند.

### نمایش به کاربر
کارتی در صفحه اشتراک نشان می‌دهد چه کسی پرداخت کرده و چرا نیازی به
پرداخت دوباره نیست.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۱۹۹ از ۱۹۹** (۶ آزمون تازه)


## دور ۱۵ — چرخه سال تحصیلی و چیدمان کلاس

### پاسخ به پرسش کاربر: نه، لحاظ نشده بود
کاربر پرسید آیا ارتقای خودکار پایه، چیدمان کلاس و ثبت‌نام شهریور
پیاده شده. آزمودم و یک **خلأ جدی** پیدا کردم:

ارتقای پایه فقط عدد `grade_level` را زیاد می‌کرد و دانش‌آموز در کلاس
پارسالش می‌ماند. با آزمون تأیید شد: دانش‌آموز پایه ۱۱ هنوز در کلاس
«دهم علوم تجربی» بود.

### `39-school-year.js`
- **بستن سال**: مدیر تصمیم می‌گیرد (تیر یا مرداد)
- **یادآوری هر ۷ روز** در فصل پایان سال تا وقتی سال بسته نشده
- **چیدمان کلاس**: دستی (انتخاب از فهرست) یا خودکار
- **دو منطق**: ابتدایی و متوسطه اول کلاس‌محور، متوسطه دوم رشته‌محور
- **کلاس موازی** برای رشته‌های پرجمعیت
- **ثبت‌نام** با تأیید شهریه

### روش مارپیچ برای چیدمان خودکار
اگر ساده تقسیم می‌کردیم یک کلاس همه قوی‌ها را می‌گرفت. با مرتب‌سازی
بر پایه امتیاز و توزیع مارپیچی، میانگین کلاس‌ها نزدیک می‌ماند.
**سنجش: ۸۰ دانش‌آموز در ۲ کلاس ⇒ میانگین ۱۵٫۲۵ و ۱۵٫۲۶ (اختلاف ۰٫۰۱)**

### دو آزمون قدیمی که شکستند و مقاوم شدند
آزمون‌های «سند چاپی» و «فارغ‌التحصیلی» فرض می‌کردند کلاس اول همیشه
دانش‌آموز دارد. آزمون‌های تازه چیدمان را عوض کردند و آن فرض شکست.
حالا کلاسی را انتخاب می‌کنند که واقعاً دانش‌آموز دارد.

### مصرف حافظه
آزمون‌ها با تنظیم پیش‌فرض حافظه تمام می‌کردند. سقف در `package.json`
به ۲ گیگابایت رسید و آزمون چیدمان سبک‌تر شد.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۲۰۹ از ۲۰۹** (۱۰ آزمون تازه)


## دور ۱۶ — اعتبارسنجی دیداری و نوع چیدمان کلاس

### اعتبارسنجی با کادر قرمز (`40-form-validation.js`)
۶۲ نقطه اعتبارسنجی فقط `toast` می‌دادند و کاربر نمی‌فهمید کدام فیلد.
لایه مرکزی ساخته شد: `need` · `needAll` · `invalid`.
رفتار: کادر قرمز + لرزش + پیام زیر فیلد + فوکوس. با تایپ خودکار پاک می‌شود.
**۱۹ نقطه وصل شد.**

ضمن کار کشف شد **کد ملی و موبایل کاربر هیچ اعتبارسنجی نداشتند** — رفع شد.

### نوع چیدمان کلاس
در فرم تعریف کلاس گزینه افزوده شد: کلاس‌محور / رشته‌محور / خودکار.
- کلاس‌محور: بخش رشته پنهان و اختیاری
- رشته‌محور: رشته الزامی، فهرست ۳۱ رشته از منحنی درسی
- خودکار: پایه ۱۰ به بالا رشته‌محور
راهنمای زیر گزینه پویا عوض می‌شود. `grade_level` هم خودکار استنتاج
و ذخیره می‌شود (برای چرخه سال تحصیلی لازم است).

### اصل سوم کاربر
پس از هر تغییر، کد به مخزن گیت‌هاب فرستاده شود:
`https://github.com/rezaa2544/p2.git`
در بند ۱۲ دستور نقش پرامپت و بالای CONTRIBUTING ثبت شد.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۲۱۸ از ۲۱۸** (۹ آزمون تازه)


## دور ۱۷ — جای نوار پیمایش و تقویت اصل گیت‌هاب

### نوار پیمایش به راست منتقل شد
صفحه راست‌به‌چپ است و مرورگر نوار را خودکار سمت چپ می‌گذارد.
راه‌حل: جهت ظرف چپ‌به‌راست، جهت محتوای درونش راست‌به‌چپ.
اعمال شد روی: کل صفحه · منوی کناری · مودال‌ها · ظرف جدول‌ها · زبانه‌ها.
ظاهر نوار هم یکدست شد (تیره در منو، روشن در بقیه).

آزمون با مرورگر مجازی: جهت html برابر ltr، جهت body برابر rtl،
متن فارسی سالم، همه نقش‌ها بدون خطا.

### اصل سوم تقویت شد
کاربر گفت به‌روز بودن مخزن گیت‌هاب یکی از شرط‌های اصلی است، هم‌رده
دو اصل دیگر. در بند ۱۲ دستور نقش پرامپت با جزئیات دستور ارسال ثبت شد.
حالا سه کار همیشه با هم انجام می‌شوند: سند مرجع، راهنمای برنامه‌نویس،
و ارسال به مخزن.

### نتایج
آزمون‌ها: ۳۱ از ۳۱ و **۲۱۸ از ۲۱۸**


## دور ۱۸ — اتصال به گیت‌هاب و ارسال موفق

کاربر توکن دسترسی داد. ارسال انجام شد:
`f08eb5b..387c8fd  HEAD -> main`

**نکته مهم:** مخزن پیش‌تر فقط `New folder.rar` داشت. به‌جای پاک‌کردن
تاریخچه با `--force`، تاریخچه دو طرف **ادغام** شد
(`--allow-unrelated-histories`) تا آن فایل از دست نرود. تأیید شد که
فایل rar هنوز در مخزن هست.

**تأیید محتوا:** ۷۱ فایل شامل ۴۱ ماژول، ۹ سند، ۲ مجموعه آزمون،
`index.html`، `build.js`، `docs/AI_PROMPT.md`، `CONTRIBUTING.md`.
محلی و راه دور کاملاً همگام (`387c8fd4`).

**امنیت:** توکن پس از ارسال از تنظیمات git پاک شد و در هیچ فایلی از
ورک‌اسپیس ذخیره نشده — بررسی و تأیید شد.


## دور ۱۹ — رفع نوار پیمایش صفحه اصلی

کاربر گزارش داد نوار منو درست شده ولی نوار صفحه اصلی هنوز سمت چپ است.

**علت:** گذاشتن `direction:ltr` روی `html` کافی نبود، چون `body` هم
ارتفاع ۱۰۰٪ داشت و هم راست‌به‌چپ بود؛ اسکرول روی خودِ body می‌افتاد
و نوارش به چپ می‌رفت.

**رفع:** اسکرول عمودی از `body` گرفته و به `.main` داده شد:
- `body{ overflow:hidden }`
- `.app{ height:100dvh; overflow:hidden }`
- `.main{ direction:ltr; overflow-y:auto; height:100dvh }`
- `.main > *{ direction:rtl }`

از `100dvh` استفاده شد تا در موبایل با نوار آدرس مرورگر جابه‌جا نشود
(`100vh` به‌عنوان پشتیبان).

آزمون با مرورگر مجازی تأیید کرد: `.main` اسکرول‌پذیر با جهت چپ‌به‌راست،
محتوای درونش راست‌به‌چپ، متن فارسی سالم، همه نقش‌ها بدون خطا.


## دور ۲۰ — پاک‌سازی حجم

کاربر گزارش داد ۱۹ مگابایت داده داریم و احساس می‌کند فایل غیرضروری هست.

### یافته
مقصر اصلی: **`New folder.rar` (۳.۷ مگابایت)** که در **تاریخچه گیت**
مانده بود و `.git` را به ۱۳ مگابایت رسانده بود.

### پیش از حذف، تأیید شد
هر ۳۶ تابع `view*` نسخه مرجع **پیاده شده‌اند** — پس فایل دیگر کاربردی
نداشت. پشتیبان در `/home/user/backup/` گرفته شد.

### نتیجه
| مورد | پیش | پس |
|---|---|---|
| `.git` | ۱۳ مگابایت | **۹۱۹ کیلوبایت** |
| کل پروژه | ~۱۹ مگابایت | **۷.۳ مگابایت** |

حذف‌شده‌ها: `New folder.rar` · `reference/` · `dist/`
الگوهای تازه در `.gitignore`: `*.rar` `*.zip` `*.7z`

### درس مهم
حذف فایل از پوشه کافی نیست — اگر در تاریخچه گیت باشد همچنان حجم
می‌گیرد. `git filter-branch` + `gc --prune=now` لازم است، و سپس
ارسال با `--force` چون تاریخچه بازنویسی شده.

### اصل چهارم کاربر
«حجم همیشه در کمترین حالت ممکن» به بند ۱۲ دستور نقش پرامپت و
بالای CONTRIBUTING اضافه شد.


## دور ۲۱ — پاک‌سازی ورک‌اسپیس و ثبت توکن

### پاک‌سازی ورک‌اسپیس (نه فقط مخزن)
کاربر گفت مدیریت حجم باید در **هر دو جا** رعایت شود.

حذف شد:
- `backup/New folder.rar` (۳.۷MB) — محتوایش در html استخراج‌شده هست
- `payesh/dist/` — خروجی موقت build
- فایل‌های `*.tmp.js`
- کلون‌های موقت `/tmp/ghpayesh` و `/tmp/p2`

**دست‌نخورده ماند:** پوشه `uploads/` (فایل‌های خود کاربر) و
`backup/payesh-full-reference.html` (تنها نسخه مرجع باقی‌مانده).

| پوشه | حجم |
|---|---|
| `payesh` | ۶.۷ مگابایت |
| `uploads` | ۲.۵ مگابایت (فایل کاربر) |
| `backup` | ۵۲۴ کیلوبایت |

### توکن گیت‌هاب در پرامپت ثبت شد
در بند ۱۲ دستور نقش، همراه با دستور کامل ارسال و هشدار امنیتی:
توکن نباید در `.git/config` یا هیچ فایل پروژه ذخیره شود.

### قاعده حفاظتی
فهرست صریح «هرگز حذف نکن» به پرامپت و CONTRIBUTING اضافه شد:
فایل‌های کاربر، کد منبع، آزمون‌ها، اسناد و فایل‌های کلیدی.


## دور ۲۲ — پررنگ‌کردن حاشیهٔ کادرها

کاربر گفت کادرها بهتر دیده شوند. ریشه: رنگ حاشیه `#e4eaf6` با
پس‌زمینهٔ `#f2f6fd` اختلاف روشنایی زیر ۴٪ داشت.

- `--border`: `#e4eaf6` → `#c2cee4`
- `--border-strong` جدید: `#a9b8d4` (حالت اشاره و پنجره)
- ضخامت کارت/فیلد/دکمه: ۱ → ۱.۵ پیکسل
- سر کارت پس‌زمینهٔ متمایز، سر جدول تیره‌تر، خط ردیف‌ها واضح‌تر
- نشان‌ها حاشیهٔ `currentColor` گرفتند
- پشت پنجرهٔ گفتگو تیره‌تر (.5 → .62)

تست‌ها: ۳۱/۳۱ و ۲۱۸/۲۱۸ ✅ — تغییر صرفاً ظاهری بود.
نمونهٔ مقایسه: `/home/user/borders-preview.html`


## دور ۲۳ — شاخه و رشتهٔ مدرسهٔ متوسطه دوم

کاربر خواست در ثبت مدرسهٔ متوسطه دوم، شاخه‌ها گزینه و دسته‌بندی باشند.

`BRANCHES` از قبل بود ولی فقط در کلاس و درس استفاده می‌شد، نه در
ثبت مدرسه.

- دو میدان جدید: `branches` و `fields` (آرایه؛ در بقیهٔ مقاطع خالی)
- `branchPicker()` در `18-modals.js`: سه کارت شاخه + نشان‌های رشته
- **چندانتخابی** چون هنرستان‌ها چند شاخه دارند
- رشته‌ها فقط با تیک شاخه باز می‌شوند
- تغییر مقطع یا برداشتن تیک ⇒ پاک‌سازی خودکار
- اعتبارسنجی: متوسطه دوم بدون شاخه ثبت نمی‌شود
- فهرست مدارس: نمایش شاخه + صافی `sbranch`
- دادهٔ نمونه: ایران‌زمین عمداً دو شاخه دارد
- `$$` به کمک‌ابزارها افزوده شد (نبود!)

تست‌ها: ۳۱/۳۱ و **۲۲۹/۲۲۹** ✅ (۱۱ آزمون جدید)
نمونه: `/home/user/branches-preview.html`


## دور ۲۴ — سرایت شاخهٔ مدرسه به فرم‌های پایین‌دستی

پس از دور ۲۳، اعلام شاخه در فرم مدرسه **بی‌اثر** بود: فرم کلاس هنوز
همهٔ ۱۴ رشته را می‌داد و در دبیرستان نظری می‌شد کلاس مکانیک خودرو
ساخت.

- `schoolBranches()` و `schoolFields()` در `26-curriculum.js`
- بازگشت امن: مدرسهٔ بدون شاخه ⇒ همهٔ رشته‌ها (دادهٔ قدیمی نشکند)
- شش نقطه وصل شد: فرم کلاس، فرم درس، صافی درس‌ها، کتاب استاندارد،
  دو رویداد تغییر شاخه
- `fieldsHere()` تقاطع شاخه با رشته‌های مدرسه
- اعتبارسنجی دولایه: هنگام ذخیره هم بررسی می‌شود
- استثنا: رشتهٔ فعلی رکورد مجاز است (قفل‌نشدن کلاس‌های قدیمی)

**اشکال پیداشده:** در `subject-save` شناسهٔ اعتبارسنجی `c_field` بود
ولی فیلد `s_field` نام دارد — کادر قرمز روی عنصر ناموجود می‌رفت.

تست‌ها: ۳۱/۳۱ و **۲۳۵/۲۳۵** ✅ (۶ آزمون جدید)


## دور ۲۵ — کلاس‌بندی خودکار از اکسل + شبیه‌سازی ظرفیت

### الف) ماژول `41-class-placement.js`
از «رضا احمدی دهم الکترونیک» می‌فهمد: پایهٔ ۱۰، رشتهٔ الکتروتکنیک،
کلاس «دهم الکتروتکنیک». همهٔ دانش‌آموزان با نوشتارهای گوناگون
(«دهم الکترونیک»، «۱۰ الکترونیک»، «دهم برق») در **یک** کلاس می‌افتند.

- جدول نام‌های عامیانه: الکترونیک→الکتروتکنیک، تجربی→علوم تجربی، …
- تطبیق سه‌مرحله‌ای: نام دقیق → پایه+رشته → پایه+شاخهٔ کلاس
- رشتهٔ خارج از شاخهٔ مدرسه هشدار می‌گیرد
- ستون «پایه» به نگاشت افزوده شد
- **رفع باگ:** نام مادر نادیده گرفته می‌شد

### ب) شبیه‌سازی ظرفیت (`docs/CAPACITY_SIM.md`)
سقف ~۲٬۰۰۰ دانش‌آموز در هر مرورگر. مدرسهٔ ۱٬۵۰۰ نفره: ۴۷ms.

سه گلوگاه پیدا و رفع شد:
| گلوگاه | رفع |
|---|---|
| باطل‌سازی ایندکس در هر درج | `idxAppend()` افزایشی |
| `nidOwner` خطی | ایندکس `idxUserByNid` |
| چهار جستجوی خطی در commit | نقشه‌های درهم‌سازی |

**ورود ۲۰۰۰ ردیف: ۱۴٬۲۵۷ms → ۲٬۷۱۹ms (۵.۲×)**

تست‌ها: ۳۱/۳۱ و **۲۴۴/۲۴۴** ✅ (۹ آزمون جدید) — ۴۱ ماژول


## دور ۲۶ — تصحیح رشته، رفع پرش منو، دیاگ سامانه، زنگ‌بندی

### ۱) تصحیح کاربر: الکتروتکنیک ≠ الکترونیک
اشتباه من بود. این دو رشتهٔ مجزا در فنی و حرفه‌ای هستند.
- الکترونیک به `BRANCHES` افزوده شد (حالا ۷ رشتهٔ فنی و حرفه‌ای)
- برق/برق صنعتی/برق ساختمان → الکتروتکنیک
- الکترونیک/مخابرات → الکترونیک

### ۲) رفع پرش منو
`render()` با innerHTML نوار کناری را بازمی‌ساخت و اسکرول صفر می‌شد.
حالا موقعیت پیش از بازسازی برداشته و پس از آن بازگردانده می‌شود.

### ۳) دیاگ سامانه (`42-self-diagnostics.js`)
۱۸ آزمون در ۶ دسته. تشخیص و تعمیر جدا. تعمیر خودکار فقط برای
عیب‌های امن؛ چیزی که قضاوت انسانی می‌خواهد فقط گزارش می‌شود.
هر تعمیر پشتیبان می‌گیرد و در صورت شکست برمی‌گرداند.
نمرهٔ سلامت ۰-۱۰۰. پایش خودکار هر ۵ دقیقه (اختیاری).
**سنجش:** ۱۲ms برای ۱۸ آزمون. تزریق عیب: ۹۰→۶۰→تعمیر→۹۰.

### ۴) شیفت و زمان‌بندی زنگ (`43-bell-schedule.js`)
- میدان `shift` در مدرسه: صبح/بعدازظهر/هر دو
- مدیر ساعت شروع و طول هر زنگ و تفریح را خودش تعریف می‌کند
- سه الگوی آماده به‌عنوان نقطهٔ شروع
- بازگشت امن: مدرسهٔ بدون تعریف، الگوی متناسب شیفتش را می‌گیرد

تست‌ها: ۳۱/۳۱ و **۲۶۰/۲۶۰** ✅ (۱۵ آزمون جدید) — ۴۳ ماژول


## دور ۲۷ — دیاگ دو خانواده‌ای (موتور + داده)

کاربر تصحیح کرد که دیاگ باید **پیکربندی و زیرساخت و موتور برنامه** را
بپوشاند، سپس افزود که **کیفیت دادهٔ کاربر هم بماند**. پس هر دو.

### دسته‌بندی `DIAG_CATS`
| خانواده | تعداد | مسئول رفع |
|---|---|---|
| ⚙️ موتور (`engine`) | ۱۸ آزمون | برنامه‌نویس / خود دیاگ |
| 📋 داده (`data`) | ۱۴ آزمون | مدیر مدرسه |

### ۱۴ آزمون تازهٔ زیرساختی
مسیر بدون نما · مسیر بدون عنوان · ناهماهنگی منو و مجوز · مجموعهٔ
ثبت‌نشده · شمارندهٔ عقب‌مانده · شناسهٔ تکراری · سلامت حافظهٔ مرورگر ·
سلامت دفترچهٔ عملیات · انباشت صف همگام · کندی رندر · توابع حیاتی
گمشده · ترتیب بارگذاری ماژول · پیکربندی زنگ · پیکربندی مدرسه.

دو تای اول واقعاً **هر صفحه را رندر می‌کنند** — گران است ولی همین
گرانی ارزششان را می‌سازد.

### رابط کاربری
سه دکمه: بررسی کامل · فقط موتور · فقط داده.
عیب‌ها به تفکیک خانواده گروه‌بندی می‌شوند.

### سنجش
| مورد | عدد |
|---|---|
| کل آزمون | ۳۲ |
| اجرای کامل | ~۲۳۵ میلی‌ثانیه |
| فقط داده | ~۱۲ میلی‌ثانیه |
| موتور روی داده نمونه | ۱۸ از ۱۸ سالم ✅ |

**دام:** صف همگام‌سازی `SYNC.queue` است نه `syncQueue` — آزمون اولم
نام غلط را فرض کرد و بی‌صدا رد می‌شد.

تست‌ها: ۳۱/۳۱ و **۲۷۴/۲۷۴** ✅ (۱۴ آزمون جدید) — ۴۳ ماژول، ۸۵۲KB


## دور ۲۸ — آماده‌سازی برای سرور واقعی

کاربر تأکید کرد که دمو بماند ولی چند اصل از همین حالا رعایت شود تا
اتصال بعدی به سرور بازنویسی کامل نخواهد.

### ۱. لایهٔ داده `00-data-layer.js`
سه بخش: `Store` (انبار)، `Api` (سرور، فعلاً خاموش)، `Data` (چهار
عمل داده). **۲۴ فراخوانی مستقیم `localStorage` در ۸ فایل → صفر.**

تصمیم: `Data` همگام ماند. چون معماری «اول آفلاین» است و نوشتن به
صف می‌رود، امضا با اتصال سرور هم عوض نمی‌شود؛ ناهمگام کردنش فقط
۴۳ فایل را بی‌دلیل پیچیده می‌کرد.

### ۲. امن‌سازی XSS
`escAttr()` تازه ساخته شد (علاوه بر `=` و بک‌تیک) چون در صفت بدون
هیچ `<` می‌شود صفت تازه چسباند.

| | پیش | پس |
|---|---|---|
| صفت پویای امن | ۶ | **۱۲۱** |
| صفت پویای ناامن | ۱۱۵ | **۰** |

نقطه‌ضعف واقعی متن‌ها نبود، **صفت‌ها** بود.

### ۳. حریم خصوصی داده نمونه
تلفن `0999…` و کد ملی `999…` — هر دو پیشوند در ایران تخصیص نیافته.
عدد تصادفی کافی نبود چون می‌توانست اتفاقاً واقعی شود.

### ۴. سند `TODO_BEFORE_PRODUCTION.md`
چهار مسدودکننده (bcrypt، احراز هویت سرور، پایگاه داده، بررسی نقش
سمت سرور) + شش کار مهم + آزمون پذیرش.

**دام:** اولین آزمون XSS بی‌صدا سبز شد — فهرست کاربران صفحه‌بندی
است و رکورد آزمایشی در صفحهٔ آخر بود. حالا آزمون اول بررسی می‌کند
رکورد واقعاً رندر شده.

تست‌ها: ۳۱/۳۱ و **۲۹۰/۲۹۰** ✅ — ۴۴ ماژول


## دور ۲۹ — بستن چهار شکاف ممیزی امنیتی

کاربر چهار شکاف واقعی را که در دور ۵ پوشش داده نشده بود مطرح کرد.
همه درست بودند.

### ۱. سند `docs/SERVER_SECURITY_CONTRACT.md`
هفت بخش: تکرار کنترل دسترسی سمت سرور، احراز هویت ژتون، اعتبارسنجی
محموله، دادهٔ حساس، رمز عبور، فهرست پذیرش، و آنچه باید در مرورگر
بماند.

**تصمیم کاربر:** کوکی `HttpOnly` + `Secure` + `SameSite=Lax` مبنا
باشد، نه سرآیند `Authorization`. دلیل: ژتون از دید جاوااسکریپت پنهان
می‌ماند، پس XSS نمی‌تواند بدزددش. `Lax` هم CSRF را بدون ژتون
جداگانه می‌بندد.

**مهم‌ترین بند:** `op.by` از مرورگر می‌آید و ادعاست. سرور باید با
`jwt.sub` بسنجدش و اگر نخواند **کل دسته** را رد کند نه فقط آن یک
عملیات.

### ۲. علامت‌گذاری چهار بدهی در کد
`🔴 TODO پیش از اتصال به سرور` در `02-demo-data.js`،
`19-actions.js`، `30-authz.js`، `27-sync.js`. بخش ۰.۵ پرامپت
**پیش از هویت پروژه** آمد تا در ممیزی بعدی از قلم نیفتد.

### ۳. دادهٔ حساس
ارزیابی صادقانه: رمزنگاری لایهٔ برنامه روی HTTPS **لازم نیست** مگر
واسطهٔ نامطمئن در مسیر باشد؛ کلیدی که در مرورگر باشد خودش قابل
استخراج است. آنچه واقعاً لازم است: کد ملی هرگز در نشانی درخواست یا
گزارش سرور نیاید، و در پایگاه داده ستون رمزنگاری‌شده + ستون هش قطعی
برای جست‌وجو داشته باشد.

### ۴. نُه آزمون مرز
**تصمیم کاربر: فقط آزمون و مستندسازی، بدون تغییر کد اجرایی** — چون
همه‌چیز فعلاً دمو است.

سنجش پیش از نوشتن تأیید کرد که دستکاری `S.user.role` از کنسول واقعاً
گارد را دور می‌زند و `insert` مستقیم وارد صف می‌شود. آزمون‌ها همین
را **عمداً تأیید** می‌کنند تا مرز صادقانه ثبت شود.

**دام:** آزمون رمز متن ساده اول شکست خورد چون آزمون «بازنشانی رمز»
پیش‌تر رمز همان دبیر را عوض می‌کند. حالا رکورد تازه می‌سازد.

تست‌ها: ۳۱/۳۱ و **۲۹۹/۲۹۹** ✅


## دور ۳۰ — جهت نوار اسکرول

کاربر گزارش داد نوار افقی از سمت چپ شروع می‌شود و مجبور است دستی
به راست بکشد تا ستون‌های اول جدول را ببیند.

**ریشه:** حیلهٔ `direction:ltr` که برای بردن نوار *عمودی* به لبهٔ
راست لازم است، اشتباهاً روی ظرف‌های *افقی* هم اعمال شده بود. روی
ظرف افقی همین حیله نقطهٔ شروع را به چپ می‌برد.

| اسکرول | جهت درست |
|---|---|
| عمودی | `ltr` روی ظرف + `rtl` روی فرزندان |
| افقی | `rtl` — دست نزنید |

`.table-wrap` و `.tabs` از `ltr` به `rtl` برگشتند. `.diag-items` و
دو ظرف درون‌خطی (اعلان‌ها، صف همگام) کلاس تازهٔ `vscroll` گرفتند
چون عمودی بودند و نوارشان چپ می‌افتاد.

**سنجش:** جهت محاسبه‌شده در `users`/`grades`/`attendance` ⇒ `rtl` ✅

شش آزمون نگهبان افزوده شد، از جمله یکی که فایل‌ها را می‌خواند و
ظرف `max-height`+`overflow` بدون `vscroll` را می‌گیرد — این اشتباه
بی‌صداست و فقط کاربر متوجهش می‌شود.

تست‌ها: ۳۱/۳۱ و **۳۰۵/۳۰۵** ✅


## دور ۳۱ — سنجش عددی IDOR

کاربر پرسید آیا IDOR را فقط مستند کردم یا آزمون هم دارد. بررسی کردم:
**فقط مستند بود.** نُه آزمون دور ۲۹ روی کنترل سطح *نقش* متمرکز
بودند، و IDOR از جنس دیگری است — نقش را عوض نمی‌کنی، شناسه را
می‌شماری. از تور رد شده بود.

### اعداد سنجیده‌شده (دادهٔ نمونه، ۶ مدرسه)

| نقش مهاجم | درون محدوده | خارج از محدوده |
|---|---|---|
| مدیر مدرسه | ۲۷۵ کاربر | **۸۷۰ کاربر** |
| ولی با ۱ فرزند | ۱ دانش‌آموز | **۵۲۶ دانش‌آموز** |
| دبیر با ۳ کلاس | ۳ کلاس · ۹۶۲ نمره | **۴۱ کلاس · ۱۱٬۵۹۲ نمره** |

بدترین ردیف نمره است: دبیر به دوازده برابر آنچه حق دارد می‌رسد.

### چرا توابع محدودهٔ موجود کافی نبودند
`scopeDescriptor()` و `visibleClasses()` فقط در **نماها** صدا زده
می‌شوند. `byId` مستقیم از ایندکس می‌خواند و مهاجم از لایهٔ نما رد
نمی‌شود. آزمون‌های قبلی می‌پرسیدند «تابع محدوده درست می‌گوید؟» نه
«اگر کسی شناسه بشمارد چه بیرون می‌آید؟».

### نکتهٔ تازه در قرارداد سرور
برای رکورد خارج از محدوده `404` نه `403` — چون `403` تأیید می‌کند
رکورد وجود دارد و مهاجم شناسه‌های معتبر را نقشه‌برداری می‌کند.

سه آزمون `IDOR ⚠️` عدد را در خروجی چاپ می‌کنند. روز اتصال سرور
باید **معکوس** شوند.

**دام محیطی:** `node_modules` در snapshot ذخیره نمی‌شود؛ برای
اسکریپت‌های jsdom باید `npm install jsdom --no-save` دوباره اجرا شود.

تست‌ها: ۳۱/۳۱ و **۳۰۹/۳۰۹** ✅


## دور ۳۲ — سه دفاع تکمیلی در قرارداد سرور (فقط مستند)

کاربر سه بند خواست: محدودسازی نرخ ورود، سرآیندهای امنیتی، و هشدار
خودکار شمردن شناسه. همه فقط مستندسازی، بدون تغییر کد.

### بند ۵.۵ — محدودسازی نرخ
سه لایهٔ همزمان (حساب، نشانی شبکه، سراسری). لایهٔ اول بدون دومی
بی‌فایده است: مهاجم یک رمز رایج را روی هزار حساب می‌زند و هیچ
حسابی به آستانه نمی‌رسد.

**دو دام مهم که نوشتم:**
۱. قفل سخت خودش سلاح می‌شود — مهاجم با ۵ تلاش عمداً غلط معلم را
   روز امتحان قفل می‌کند. تأخیر تصاعدی درست است.
۲. نشت از راه زمان پاسخ — «کاربر ناموجود» ۵ms و «رمز غلط» ۲۵۰ms
   یعنی فهرست نام‌های کاربری لو می‌رود. باید هش ساختگی مقایسه شود.

### بند ۵.۶ — سرآیندهای امنیتی
**مسئلهٔ خاص این پروژه:** خروجی تک‌فایلی است و اسکریپت‌ها درون‌خطی،
پس سیاست محتوا به `'unsafe-inline'` نیاز پیدا می‌کند که عملاً
بی‌اثرش می‌کند. راه‌حل: `build.js` باید `nonce` تولید کند — تغییر
واقعی در ابزار ساخت که باید زودتر برنامه‌ریزی شود.

`Referrer-Policy` اینجا اهمیت ویژه دارد چون نشانی‌ها شناسهٔ
دانش‌آموز دارند.

### بند ۵.۷ — هشدار شمردن شناسه
مکمل بند ۱.۲: سنجش مالکیت نشت را می‌بندد ولی مهاجم را نشان نمی‌دهد.
پاسخ تدریجی پنج‌مرحله‌ای، کندسازی نه قطع فوری (مدیر گزارش سالانه
هم صدها رکورد می‌خواند).

**پیشگیری بهتر از درمان:** شناسهٔ UUID اصلاً قابل شمردن نیست — ولی
این تصمیم باید پیش از نخستین استقرار گرفته شود، نه بعد.

آزمون نگهبان هشت کلیدواژهٔ تصمیم‌محور را می‌سنجد (نه فقط وجود واژه).

**دام محیطی تکرارشونده:** ریست محیط `node_modules` را پاک می‌کند؛
`npm install jsdom --no-save` لازم می‌شود.

تست‌ها: ۳۱/۳۱ و **۳۱۰/۳۱۰** ✅


## دور ۳۳ — هشدار تعامل کارایی و سنجش محدوده

کاربر پرسید افزودن سنجش محدوده به `byId` روی بنچمارک‌های سرعت چه
اثری می‌گذارد و آیا باید یادداشت شود. پاسخ: بله.

### بند ۱.۲.۴ قرارداد سرور
سه هشدار:
۱. سنجش مدیر ارزان است (یک مقایسهٔ `school_id`)، ولی سنجش ولی و
   دبیر جست‌وجو می‌خواهد و بدون ایندکس در حلقه درجه‌دوم می‌شود —
   همان الگویی که در ارتقای پایه و ورود اکسل دیده شد.
۲. دو محور جدا: بنچمارک فعلی زمان رندر مرورگر است، سنجش محدوده
   سمت سرور اجرا می‌شود. نباید با هم مقایسه شوند.
۳. 🔴 هرگز برای سرعت سنجش را حذف نکنید — استثنا برای مسیر
   پرترافیک دقیقاً جایی است که نشت رخ می‌دهد.

نکتهٔ اضافه: حافظهٔ نهان محدوده باید با تغییر `parent_links`،
`enrollments` و تخصیص کلاس باطل شود، وگرنه دسترسی منقضی زنده
می‌ماند.

### تصمیم: بدون عدد تخمینی
کاربر تأکید کرد سنجش دقیق در فاز ساخت سرور انجام شود. در سند هم
صریح نوشتم که عدد عمداً نیامده — حدس زدن هزینهٔ کارایی در این
پروژه چند بار اشتباه از آب درآمده.

`docs/CAPACITY_SIM.md` هشدار «تاریخ انقضا» در سرش گرفت.

تست‌ها: ۳۱/۳۱ و **۳۱۱/۳۱۱** ✅


## دور ۳۴ — راه‌حل نشت زمانی (بند ۵.۵.۳.۱)

کاربر خواست راه‌حل نشتی که در بند ۵.۵.۳ توصیف شده بود مشخص شود.

**ریشه:** bcrypt عمداً کند است. وقتی فقط در شاخهٔ «کاربر پیدا شد»
اجرا شود، همان کندیِ محافظ به کانال جانبی تبدیل می‌شود:
۲۵۰ms در برابر ۵ms.

**راه‌حل:** `DUMMY_HASH` از پیش ساخته‌شده در راه‌اندازی سرور. وقتی
کاربر نیست، bcrypt روی آن اجرا و نتیجه دور ریخته می‌شود.

🔴 هش ساختگی باید **همان هزینهٔ** واقعی‌ها را داشته باشد؛ ۱۲ در
برابر ۱۰ یعنی چهار برابر اختلاف و نشت سر جایش.

**چهار دام که نوشتم:**
۱. بهینه‌ساز فراخوانی بی‌استفاده را حذف نکند
۲. بررسی `active` باید بعد از مقایسهٔ رمز بیاید، وگرنه «موجود ولی
   غیرفعال» لو می‌رود
۳. زمان پرس‌وجوی پایگاه داده هم نامتقارن است — ایندکس روی
   `username` لازم است
۴. بازیابی رمز و ثبت‌نام همین نشت را از راه پیام می‌دهند

**ملاحظهٔ تکمیلی:** زمان کف ثابت (۳۰۰ms) مکمل است نه جایگزین —
تأخیر ثابت به‌تنهایی با میانگین‌گیری قابل حذف است.

دو ردیف به فهرست پذیرش و یک آزمون نگهبان با چهار کلید تصمیم‌محور.

**دام محیطی:** سندباکس چند نوبت قطع شد؛ متن آماده در گفتگو نگه
داشته شد تا با برگشت محیط بدون بازنویسی وارد شود.

تست‌ها: ۳۱/۳۱ و **۳۱۲/۳۱۲** ✅


## دور ۳۵ — بستن فاز معماری

کاربر پرسید آیا تصمیم پایه‌ای مانده که تعویقش گران تمام شود.
مرور واقعی سند (نه از حافظه) سه شکاف نشان داد:

| شکاف | وضعیت پیش از این دور |
|---|---|
| شکل شناسه | در بند ۵.۷.۳ گفته شده «زود تصمیم بگیرید» — ولی گرفته نشده بود |
| حل تعارض | در TODO به‌عنوان «کار» بود، نه تصمیم معماری |
| حذف نرم/سخت | **هیچ‌جا مطرح نشده بود** |

### تصمیمات
۱. **شناسهٔ عددی می‌ماند.** UUID جایگزین سنجش مالکیت نیست — خود
   بند ۵.۷.۳ این را گفته بود. سود اضافی کم در برابر مهاجرت.
۲. **تعارض ترکیبی بر پایهٔ نوع داده.** این معماری است چون شکل
   عملیات را تعیین می‌کند: داده حساس `base_version` لازم دارد.
۳. **حذف نرم برای سابقه، سخت برای عملیاتی.** معماری چون هر
   پرس‌وجو `deleted_at IS NULL` می‌خواهد.

دام‌هایی که نوشتم: قید یکتایی باید جزئی باشد وگرنه رکورد حذف‌شده
مانع ثبت دوباره می‌شود · «آخرین نوشتن» بر پایهٔ زمان سرور نه ساعت
دستگاه · نگه‌داشتن تعارض بدون صفحهٔ داوری بی‌فایده است.

### دو مورد عمداً باز
محل نگهداری داده و رضایت والدین — هر دو وابسته به شرایط بیرونی نه
معماری برنامه. برای رضایت هشدار گذاشتم که پیش از نخستین ثبت‌نام
واقعی تصمیم گرفته شود.

### سند تازه
`docs/ARCHITECTURE_DECISIONS.md` — ۴۰ تصمیم قفل‌شده در هشت گروه، هر
کدام با «چرا» و ارجاع به بند. یک وابستگی متقابل ثبت شد: شناسهٔ
عددی به پیاده‌سازی سنجش مالکیت و هشدار شمردن بند است.

تست‌ها: ۳۱/۳۱ و **۳۱۴/۳۱۴** ✅


## دور ۳۶ — بازبینی ورود اکسل و طرح توزیع

کاربر خواست کار روی «ثبت خودکار از اکسل با کلاس‌بندی هوشمند» شروع
شود و شش سؤال طراحی مطرح کرد. بازبینی کد نشان داد **چهار مورد از
شش از قبل کامل است**:

| سؤال | وضعیت واقعی |
|---|---|
| خواندن ورودی | ✅ ۴۱ فیلد، امتیازدهی مترادف ۷۰–۱۰۰ |
| خطای ردیفی | ✅ خطا/هشدار تفکیک‌شده |
| پیش‌نمایش | ✅ جادوگر چهارمرحله‌ای |
| تکراری | ✅ سه حالت + `recordConflict` |
| توزیع | ❌ فقط تطبیق است، توزیع نیست |
| کارایی | ⚠️ درجه‌دوم روی محور کلاس |

### کشف کارایی — با سنجش نه حدس
```
۳۰۰۰ ردیف /   ۹ کلاس →  ۲۱۱ms
۳۰۰۰ ردیف / ۱۵۹ کلاس → ۱۰۰۳ms
```
روی محور ردیف خطی بود (۳۰۰۰ به ۲۰۰ فقط ۱۰× در برابر ۱۵× خطی) و
همین گمراه‌کننده بود. `findClassFor` برای هر ردیف تا چهار `filter`
روی کل فهرست می‌زند.

🔴 **درس ثبت‌شده:** تابع دوورودی دو محور دارد. بنچمارک تک‌محوره
مشکل محور دیگر را پنهان می‌کند. دادهٔ نمونه ۹ کلاس دارد ⇒ نامرئی.

### تصمیمات کاربر برای توزیع
۱. قانون پایه: **ظرفیت باقی‌مانده + تعادل تعداد**
۲. تفکیک جنسیتی: **اختیاری** (مدارس روستایی مختلط‌اند)
۳. خواهر و برادر هم‌کلاس: **اختیاری** (بعضی مدارس جدا می‌کنند)
۴. همه پر: هشدار + انتخاب مدیر — نه ساخت خودکار، نه نقض ظرفیت
۵. تکرارپذیری: **تضمین‌شده**، بدون هیچ عنصر تصادفی

### دام‌هایی که در طرح نوشتم
- شمارندهٔ ثبت‌نام باید در حافظه به‌روز شود وگرنه همه به یک کلاس
- گره‌گشایی قطعی (شناسهٔ کوچک‌تر) وگرنه تکرارپذیری نمی‌شود
- خواهر/برادر فقط با کد ملی والدین، **نه نام خانوادگی**
- خواهر/برادر فقط در همان پایه (دوقلوها)
- ظرفیت بر قانون خواهر/برادر مقدم است
- پارامتر نقشه اختیاری بماند تا فراخوان‌های قدیمی نشکنند

📄 `docs/PLACEMENT_PLAN.md` — ۱۸ ردیف فهرست پذیرش + بخش «آنچه در
این طرح نیست» برای پرهیز از گسترش دامنه.

**منتظر تأیید کاربر پیش از کدنویسی.**


## دور ۳۷ — مرحلهٔ ۱: رفع درجه‌دومی findClassFor

`buildClassIndex(schoolId)` افزوده شد: سه نقشه (`byName`, `byGF`,
`byGrade`) که یک بار ساخته می‌شوند. `findClassFor` پارامتر سوم
**اختیاری** گرفت تا فراخوان‌های قدیمی نشکنند.

**یک فراخوان پنهان پیدا شد:** جز `planPlacement`، تابع
`validateImport` هم `findClassFor` را داخل حلقهٔ ردیف‌ها صدا
می‌زد. اگر فقط اولی درست می‌شد، نیمی از مشکل می‌ماند.

### بنچمارک واقعی (۳۰۰۰ ردیف ثابت)
| کلاس | پیش | پس |
|---|---|---|
| ۹ | ۲۱۱ms | **۱۳۸ms** |
| ۱۵۹ | ۱۰۰۳ms | **۱۳۵ms** (۷٫۴×) |
| ۵۰۹ | — | **۱۳۹ms** |

منحنی صاف شد؛ نوسان ۱۳۵–۱۳۹ نویز است نه رشد.
محور ردیف هم ۳۵٪ سریع‌تر: ۳۰۰۰ ردیف ۲۱۱→۱۳۸ms.

**یک حلقه عمداً ماند:** مرحلهٔ «رشته در نام کلاس هست ولی در field
نیست» جست‌وجوی زیررشته می‌خواهد؛ دامنه‌اش به کلاس‌های همان پایه
محدود شد، نه کل فهرست.

دو آزمون نگهبان: یکی محور کلاس را می‌سنجد (آستانهٔ ۳× چون نویز
jsdom زیاد است)، دیگری سازگاری فراخوان دوآرگومانی.

تست‌ها: ۳۱/۳۱ و **۳۱۶/۳۱۶** ✅


## دور ۳۸ — مرحلهٔ ۲: منطق توزیع

`PLACE_DEFAULTS` (هر دو کلید اختیاری خاموش) · `placeSettings` ·
`isMixedSchool` · `buildOccupancy` · `pickBranch` ·
`branchCandidates` · `hasExplicitSection` · `familyKey`.

### سه دامی که فقط سنجش پیدا کرد
۱. **کلاس بی‌شمارهٔ هم‌نام** — «دهم علوم تجربی» در دادهٔ نمونه
   وجود دارد؛ تطبیق نام روی آن می‌افتاد و همهٔ ۳۵ نفر آنجا
   می‌رفتند. بنچمارک اولم به همین دلیل گمراه‌کننده بود.
۲. **`section` برای رشته‌ای‌ها خالی است** — «دهم علوم تجربی ۳» و
   «دهم تجربی» `pl` یکسان می‌دهند. `hasExplicitSection` روی
   `pl.raw` کار می‌کند و `findClassFor` هنگام چند نامزد شماره را
   تطبیق می‌دهد.
۳. **کارایی: ۷۵۱ms از ۹۰۴ms در `hasExplicitSection`** — حلقه روی
   ۱۵۱ نامزد. حدس اولم `slice()` بود و غلط بود؛ سنجش تفکیکی
   گناهکار را نشان داد. نقشهٔ `byNameDigit` ⇒ ۱۴ms.

### بنچمارک نهایی
| آزمون | نتیجه |
|---|---|
| ۹ نفر / ۳ شعبه | ۳-۳-۳ |
| ۳۵ نفر / ۳×۱۰ | ۱۰-۱۰-۱۰ + ۵ دستی، صفر کلاس نو |
| تکرارپذیری | سه اجرا یکسان ✅ |
| ۳۰۰۰ ردیف / ۱۶۲ کلاس | ۱۸۷ms |

هشت آزمون تازه. آزمون `Math.random` اول شکست خورد چون روی متن
کامنت افتاده بود — حالا کامنت‌ها را حذف می‌کند.

تست‌ها: ۳۱/۳۱ و **۳۲۴/۳۲۴** ✅


## دور ۳۹ — مرحلهٔ ۳: رابط کاربری توزیع

`impCapTable` (جدول ظرفیت با سه نشانهٔ قرمز) · `impManualBlock`
(ردیف‌های جانشده + دکمهٔ افزایش ظرفیت) · `impRulesCard` (سه کلید).
کنش‌های `imp-raise-cap` و `place-rules-save`.

`validateImport` توزیع را هم اجرا می‌کند و `capRows`/`manualRows`
برمی‌گرداند؛ شمارندهٔ اشغال در حافظه به‌روز می‌شود.

### سنجش رندر واقعی (نه فرض)
```
۳۵ ردیف / ۳ شعبهٔ ۱۰تایی:
  ok:35 · auto:30 · manual:5 · کلاس نو: 0
  جدول: هر شعبه 0 +10 = 10/10 ✅
سناریوی ظرفیت ۲ با ۵ نفر:
  over:true · عدد قرمز ✅ · برچسب ✅ · هشدار متنی ✅
کارت قواعد: ۱ checked (فقط توزیع) · disabled در مدرسهٔ تک‌جنسیتی ✅
```

### دو دام
۱. **`validateImport` از `S.user` می‌خواند نه پارامتر** — چهار
   آزمون اول شکست خوردند با «جدول خالی». کمکی `withBranches`
   حالا کاربر را هم می‌گذارد و در `finally` برمی‌گرداند.
۲. **تولیدکنندهٔ کد ملی خودم باگ داشت** — `slice(0,9)` همهٔ
   شماره‌ها را یکسان می‌کرد و ۳۱ ردیف «تکراری» می‌شدند. تشخیص
   تکراری سامانه درست بود؛ ابزار آزمون غلط بود.

پنج آزمون تازه. تست‌ها: ۳۱/۳۱ و **۳۲۹/۳۲۹** ✅


## دور ۴۰ — ارزیابی کلی وضعیت

سنجش واقعی با jsdom روی همهٔ نقش‌ها و صفحات.

| سنجه | نتیجه |
|---|---|
| رندر صفحات | ۸۲/۸۲ سالم، صفر خطا |
| خودآزمایی ۳۲ بررسی | ۲۹ ✅ · ۲ هشدار · ۱ بحرانی · نمره ۷۵ |
| `JSON.stringify(db)` | ۴٫۶۱MB (سقف مرورگر ~۵MB) |
| `index.html` | ۹۰۴KB · ۴۴ ماژول |

روت‌ها: superadmin ۱۹ · manager ۲۵ · teacher ۱۳ · student ۱۰ ·
parent ۱۱ · edu_office ۴.

### شکاف‌های دمو
۱. `meeting_slots` و `bell_schedules` هر دو صفر — صفحه‌ها سالم‌اند
   ولی خالی باز می‌شوند.
۲. رمز همه `123456`.
۳. حجم داده نزدیک سقف حافظهٔ مرورگر.

### دو دام سنجش (اشتباه خودم)
- **`render()` به‌جای `renderRoute()`** ⇒ ۸۲ خطای کاذب با
  `innerHTML` صفر. تابع درست خروجی رشته‌ای می‌دهد.
- **`diagHealthScore()` بدون پارامتر** ⇒ استثنا. امضا:
  `diagHealthScore({critical,warning,info})`.

هیچ تغییری در کد برنامه انجام نشد؛ این دور فقط ارزیابی بود.


## دور ۴۱ — کار ۱: پر کردن دو صفحهٔ خالی

**تشخیص دقیق‌تر از ارزیابی قبل:** صفحهٔ زنگ‌ها **خالی نبود** —
`bellOf()` الگوی پیش‌فرض برمی‌گرداند و ۲۴۹۸ نویسه رندر می‌شد، فقط
هشدار «زمان‌بندی اختصاصی تعریف نشده» داشت. صفحهٔ جلسات اولیا اما
واقعاً خالی بود (۴۶۹ نویسه برای ولی).

`generateP11()` در `43-bell-schedule.js` (نه در `02-demo-data.js`،
چون به `BELL_PRESETS` و کاربران فازهای بعد وابسته است).

| | پیش | پس |
|---|---|---|
| `meeting_slots` | ۰ | **۱۲۰** (۸۰ آزاد · ۴۰ رزرو) |
| `bell_schedules` | ۰ | **۵** (فقط مدارس فعال) |
| صفحهٔ ولی | ۴۶۹ نویسه | **۷۲۹۵** |
| صفحهٔ مدیر | ۵۴۶ | **۱۰۴۳۰** |

⚠️ **دام تاریخ:** نوبت باید در **آینده** باشد. نوبت گذشته قابل
رزرو نیست و صفحهٔ ولی دوباره خالی به نظر می‌رسد. تابع کمکی
`daysAheadISO()` افزوده شد چون `daysAgoISO` فقط گذشته می‌دهد.

بازآزمایی کامل: **۸۲/۸۲ صفحه سالم، صفر خطا.**
حجم داده ۴٫۶۱ ⇐ ۴٫۶۴MB (ناچیز).

سه آزمون نگهبان، از جمله «هیچ نوبتی در گذشته نباشد».
تست‌ها: ۳۱/۳۱ و **۳۳۲/۳۳۲** ✅


## دور ۴۱ — کار ۲: بازسنجی `docs/CAPACITY_SIM.md`

بنچمارک روی کد فعلی دوباره اجرا و با ستون «قبل/بعد» ثبت شد.
مهم‌ترین یافته: ورود ۲٬۰۰۰ ردیف اکسل از ۲٬۷۱۹ms به **۱۲۹ms**
رسیده (۲۱×) — توزیع خودکار کند نکرد، بهینه‌سازی‌های دور ۳۷ کل
مسیر را سریع‌تر کردند.

رفتار درجه‌دومی تازه **سنجیده شد نه حدس زده**: سربار
`separateGender` ۲٫۴٪ و `keepSiblings` صفر.

سنجش محدودهٔ امنیتی هنوز در `byId` نیست (تأیید با خواندن سورس تابع)
⇒ هشدار «تاریخ انقضا» در سند سرجایش ماند، با عدد مبنای ۰٫۷۶µs.

بخش‌های نو: `CAPACITY_SIM.md` ۳.۱–۳.۳ · `CONTRIBUTING.md` «بنچمارک:
چطور فریب نخورید» · `AI_PROMPT.md` ۰.۵.۱۲.


## دور ۴۱ — کار ۳: جمع‌بندی موارد باز

سند تازه `docs/OPEN_ITEMS.md`. فقط موارد شناخته‌شده مرور شد (طبق
خواستهٔ کاربر)، ولی هر ردیف با گرپ روی کد امروز وارسی شد نه از روی
حافظهٔ سند.

**۴ مسدودکننده باز** (عمدی — فاز دمو) · **۶ مهم باز، ۲ تصمیم‌گرفته**
· **۳ آزمون IDOR عمداً وارونه** (روز اتصال سرور معکوس شوند) ·
**۲ مورد بی‌تصمیم** — رضایت والدین مهلتش پیش از نخستین ثبت‌نام واقعی
است، یعنی زودتر از اتصال سرور.

**تنها مورد باز فنی:** حجم `db` = ۴٫۶۴MB مقابل سقف ~۵MB.

چهار ادعای «انجام شده» بازسنجی شد و همه سالم: صفر تخطی لایهٔ داده،
صفر دادهٔ واقعی، XSS امن، ۸۲/۸۲ صفحه.


## دور ۴۲ — گام ۱: هستهٔ صف اطلاع‌رسانی پیامکی

طرح `docs/SMS_NOTIFY_PLAN.md` تأیید شد (۶ پاسخ کاربر: عدد نمره در
پیامک نباشد · نمره پیش‌فرض خاموش · اصلاحیه تابع قاعدهٔ پیام اصلی ·
مهلت ۲۰ دقیقه · پاک‌سازی ۳۰ روزه · غیبت موجه پیامک نشود).

ماژول `src/js/44-sms-notify.js` — ۴۵ ماژول شد.

**سنجش پیش از کد، دو تصحیح آورد:**
| | برآورد | واقعی |
|---|---|---|
| رکورد صف | ۲۵۰ بایت | **۴۳۶** (۱٫۷×) |
| قالب غیبت | ۱ قطعه (فرض) | **۲ قطعه** ⇒ بازنویسی شد به ۱ |

قالب غیبت از «حاضر نبود» (۷۴ نویسه) به «غایب بود» (۶۴) تغییر کرد و
یک‌قطعه‌ای شد — نیمی از هزینهٔ پیامک مدرسه.

**تست جهش:** چهار جهش عمدی زدیم؛ سه‌تا گرفته شد و یکی **نه** —
آزمون `kinds` بی‌معنا بود چون از تابعی استفاده می‌کرد که خودش
ادغام می‌کرد. اصلاح شد و حالا می‌گیرد.

۱۵ آزمون تازه · ۳۱/۳۱ و **۳۴۷/۳۴۷** ✅


## دور ۴۲ — گام ۲: صفحهٔ صف مدیر

مسیر `notifyqueue` در منوی مدیر. صفحه‌ها **۸۲ → ۸۳**.

بخش ۹ ماژول ۴۴ + ۱۰ کنش در `19-actions.js` + سبک‌های `.notify-*`
+ کارت داشبورد + نوار هشدار در `renderShell`.

**سنجش واقعی:** خاموش ۳۵۵ نویسه · روشن و خالی ۸۰۹ · با ۶ پیام
۵۰۰۷ نویسه و ۶ ردیف · XSS: تگ خام صفر، فرارداده‌شده ✅ ·
نوار هشدار ۶/۶ صفحهٔ مدیر · دبیر منع می‌شود · ۸۳/۸۳ صفحه سالم.

**تست جهش — پنج جهش، هر پنج گرفته شد:**
حذف `esc` از متن پیام · برداشتن نوار از پوسته · نمایش هشدار در
حالت خاموش · دادن اجازهٔ تأیید به دبیر · بی‌اثر کردن فیلتر.

**یک آزمون موجود کار خودش را کرد:** «همهٔ مسیرهای منو عنوان دارند»
بلافاصله افتاد چون `TITLES.notifyqueue` را ننوشته بودم.

۱۰ آزمون تازه · ۳۱/۳۱ و **۳۵۷/۳۵۷** ✅


## دور ۴۲ — گام ۳: پیش‌نویس حضور و غیاب

تغییر رفتار: تیک دیگر بی‌درنگ ذخیره نمی‌شود.

**سنجش پایداری پیش‌نویس (شرط صریح کاربر):**
| سناریو | نتیجه |
|---|---|
| تیک در کلاس الف | ۳ تیک، صفر رکورد در پایگاه داده ✅ |
| رفتن به کلاس ب و تیک زدن | ۲ تیک جدا ✅ |
| بازگشت به الف | **۳ تیک سالم** ✅ |
| بستن و باز کردن مرورگر | **۳ تیک بازیابی شد** ✅ |

مسیر کامل با کلیک واقعی DOM: پنجرهٔ مرور ۱۰۷۰ نویسه («۲ غایب،
۱ تأخیر، ۲ حاضر») · ثبت ۵ رکورد · ۳ پیامک · اصلاح درون پنجره
پیام را `cancelled` کرد · پس از ۳۰ دقیقه `pending` ماند.
کارایی: ۰٫۰۸ms هر تیک. ۸۳/۸۳ صفحه سالم.

**تست جهش ۹ مورد — سه شکاف واقعی پیدا شد:**
۱. بازگرداندن `att-set` به نوشتن مستقیم را **نمی‌گرفتیم** (آزمون‌ها
   `attDraftSet` را مستقیم صدا می‌زدند نه از راه دکمه) ⇒ آزمون با
   کلیک واقعی DOM اضافه شد.
۲. حذف `attDraftClear` از ثبت نهایی را **نمی‌گرفتیم**.
۳. 🔴 آزمون «تعویض کلاس» **بی‌صدا رد می‌شد** — `skipTest` به‌خاطر
   دادهٔ آلودهٔ آزمون‌های قبلی همیشه فعال بود. خودکفا شد.

۱۲ آزمون تازه · ۳۱/۳۱ و **۳۷۰/۳۷۰** ✅


## دور ۴۲ — گام ۵: اصلاحیهٔ خودکار

`notifyReconcile` با چهار خروجی: `none`/`created`/`cancelled`/`updated`.

**تصمیم محوری:** مبنا «آخرین پیام ارسال‌شده» است نه «پیام اصلی».
در سناریوی غایب→حاضر→غایب، با مبنای پیام اصلی هیچ اصلاحیه‌ای
ساخته نمی‌شد و خانواده با اطلاع غلط می‌ماند.

**سنجش واقعی (کلیک DOM):**
| سناریو | نتیجه |
|---|---|
| تغییر پس از ارسال | اصلاحیه ساخته شد، پیام اصلی `sent` ماند ✅ |
| رفت‌وبرگشت | اصلاحیهٔ معلق `cancelled`، دومی ساخته نشد ✅ |
| ۵ وارسی پیاپی | ۴ → ۴ رکورد، حلقه نیست ✅ |
| بدون پیام قبلی | `none`، صفر رکورد ✅ |
| پیام معلق (نرفته) | `none` ✅ |
| صف مدیر | اصلاحیه بالای فهرست با نشان 🔴 ✅ |

**تست جهش ۷ مورد — یک شکاف پیدا شد:** گارد پیام تکراری در
`att-commit` هیچ پوششی نداشت (آزمون‌ها تابع را مستقیم صدا می‌زدند
نه از راه دکمه). آزمون با کلیک واقعی اضافه شد و حالا می‌گیرد.

۱۰ آزمون تازه · ۳۱/۳۱ و **۳۸۰/۳۸۰** ✅


## دور ۴۲ — گام‌های ۶ و ۹ (پایان طرح پیامک)

**گام ۶ شکاف واقعی داشت:** `autoSend` فقط برچسب `auto:1` می‌زد و
پیام هرگز خودکار نمی‌رفت. `notifyAutoDue`/`notifyAutoFlush`/
`notifyAutoTick` ساخته و به `render()` وصل شد.

سنجش: خاموش ⇒ صفر سررسید · روشن ولی درون مهلت ⇒ صفر · پس از
مهلت ⇒ ۱ ارسال با کسر درست اعتبار · سقف ۱ قطعه ⇒ صفر ارسال با
دلیل `daily-cap` · محافظ ۳۰ ثانیه‌ای کار می‌کند.

**گام ۹:** `attHistory`/`attHistoryCard` + ستون «تغییرات» در
پروندهٔ دانش‌آموز.

🔴 **باگ جدی کشف‌شده:** دفترچهٔ تغییرات شیءِ زندهٔ رکورد را نگه
می‌داشت، پس `update` گذشته را بازنویسی می‌کرد. سابقه می‌گفت «ثبت
present» در حالی که `absent` ثبت شده بود. **این فقط ماژول تازه را
خراب نمی‌کرد — صفحهٔ سابقهٔ تغییرات از روز اول داده‌های نادرست
نشان می‌داد.** رفع با رونوشت سطحی در `03-persistence.js`.

**اصل پنجم افزوده شد:** «پرامپت امنیت» — بازبینی امنیتی مبتنی بر
شواهد پس از هر تغییر. جزئیات در `docs/AI_PROMPT.md`.

**نکتهٔ محیطی:** اجرای تست دودی به ۶ تا ۱۲ دقیقه رسیده، ولی
`user 13s` در برابر `real 6m31s` نشان می‌دهد پردازنده بیکار است و
مشکل از اشباع میزبان است نه کد (بار سیستم ۶٫۲۸). آزمون ایستا
۳۳/۳۳ و بیلد بیت‌به‌بیت سالم‌اند.

۶ آزمون تازه · آزمون ایستا **۳۳/۳۳** ✅


## دور ۴۲ — بازبینی نهایی: پاسخ سه پرسش

**۱. دامنهٔ باگ `applyOp`:** سراسری بود و رفع هم سراسری است (در
خودِ `applyOp` نه در ماژول). پنج جدول سنجیده شد، همه ✅. بونوس:
صف همگام‌سازی سرور هم درست شد.

**۲. سقف روزانه:** پیام‌ها گم نمی‌شوند و فردا خودکار می‌روند —
سنجیده شد. ولی حق با کاربر بود: **هیچ هشداری به مدیر نبود.**
`notifyCapBanner` + هشدار داشبورد اضافه شد.

**۳. پشتیبان گیت:** `git bundle` در `/home/user/gitbackup/`،
تأییدشده «تاریخچهٔ کامل»، ۱٫۱MB.

۳ آزمون تازه · آزمون ایستا **۳۳/۳۳** ✅


## دور ۴۳ — مرحلهٔ اول: سه قابلیت + بررسی کشف‌پذیری

**#۲ اختیار مدیر:** لغو پس از پنجرهٔ مهلت با نشان `by_manager` و
کارت ردپا. دو گارد: بررسی نقش (نه فقط پارامتر) و محدودهٔ مدرسه.

**#۳ نمودار روند:** ترتیب از `created_at` (سنجش: درج ۱۸،۱۰،۱۴ ⇒
نمودار ۱۰،۱۴،۱۸) و نرمال‌سازی به ۲۰ (۸ از ۱۰ ⇒ ۱۶).

**#۱۱ پیام اداره:** اعلان درون‌سامانه‌ای به مدیران محدوده — نه
پیامک، چون کیف اعتبار مال مدرسه است.

**تست جهش ۶ مورد — یک شکاف:** آزمون نشت اداره بی‌معنا بود چون
انتظارش را از همان تابعی می‌گرفت که می‌سنجید. با ساختن انتظار
مستقل از `officeScopeSchools` رفع شد.

**کشف‌پذیری:** گروه «مدرسه من» با ۱۴ آیتم از ۲۶ آیتم مدیر به
انبار تبدیل شده — تنها موردی که واقعاً بازچینش می‌خواهد. داشبورد
اداره اصلاً مشکلی نداشت.

۸ آزمون تازه · ایستا **۳۳/۳۳** ✅


## دور ۴۳ — بازچینش منوی مدیر

«مدرسه من» (۱۴ آیتم) شکست به **«کار روزانه» (۵)** و
**«راه‌اندازی مدرسه» (۷)**. معیار: بسامد مراجعه.

`meetings` → «نوبت ملاقات اولیا» و همراه `notifyqueue` به گروه
«ارتباطات». بزرگ‌ترین گروه از ۱۴ به ۷ رسید.

**سنجش:** ۲۶ مسیر سالم، صفر گمشده، صفر تکراری · `authz-coverage`
و `title-coverage` هر دو ✅ · **۸۳/۸۳ صفحه** · ایستا ۳۳/۳۳.

**تست جهش ۴ مورد:** `title-coverage` هر دو جهشش را گرفت.
`authz-coverage` دو جهش نخست را **نگرفت** — چون `canRoute` از خود
منو ساخته می‌شود. با دست‌کاری `allowedRoutes` روی مسیری که فقط در
منوست (`bells`) گرفته شد: ۲ ناهماهنگی دقیق.


## دور ۴۴ — مرحلهٔ دوم: ابزارهای دبیر

ماژول `45-teacher-tools.js` (۴۶ ماژول) + جدول `teacher_notes`.

**#۷ سابقهٔ چندساله:** فقط نمرات و حضور (بدون انضباط)، فقط
دانش‌آموز کلاس فعلی دبیر. `generateP12` نمونهٔ سال گذشته ساخت —
بدون آن قابلیت نامرئی می‌ماند (۷۵ دانش‌آموز سابقهٔ دوساله).

**#۵ یادداشت خصوصی:** مدیر می‌بیند نمی‌نویسد · ولی و دانش‌آموز
هرگز · با انتقال دبیر در مدرسه می‌ماند · هشدار حقوقی صریح.

**دو دام کشف‌شده:**
۱. `generateP12` با `insert()` بوت را به **دو دقیقه** برد →
   `add()` برگرداندش به ۴ ثانیه.
۲. حجم `db` به **۵٫۱۰MB** رسید (بالای سقف) → سقف نمونه کم شد،
   حالا **۴٫۷۵MB**.

**تست جهش ۹ مورد:** ۸ گرفته شد. یکی «بی‌اثر» بود چون دفاع دولایه
است؛ با دو جهش عمیق‌تر (ولی به شاخهٔ مجاز اضافه شد) تأیید شد.

۱۲ آزمون تازه · ایستا **۳۳/۳۳** · ۸۳/۸۳ صفحه ✅


## دور ۴۵ — دفترچهٔ تحویل کار

`HANDOFF.md` در ریشه ساخته شد؛ اصل ششم. نخستین ورودی خلاصهٔ کل
۴۴ دور + راهنمای شروع برای احمد (عضو تازهٔ تیم).

قاعده: جدیدترین ورودی بالا · سقف ۲۰ ورودی سپس آرشیو ·
پیام کامیت `docs: update HANDOFF.md with session summary [تاریخ]`.


## دور ۴۶ — خانوادهٔ چندفرزندی (احمد)

`02-demo-data.js`: توزیع `{1:521, 3:1}` → **`{1:309, 2:103, 3:3}`**.
حجم `db` از ۴٫۷۵۵ به **۴٫۶۹۷MB** کم شد (هر ادغام ۲۲۳ بایت آزاد
می‌کند). انتخاب قطعی است نه تصادفی.

`keepSiblings` حالا ۲۶ خانوادهٔ هم‌پدر در مدرسهٔ نخست دارد —
پیش از این هرگز فعال نمی‌شد.

**دام:** دیوار پرداخت آزمون را دروغین می‌انداخت (هیچ ولیِ
دوفرزندی اشتراک نداشت). helper `withSubscribedParent` ساخته شد.

۵ آزمون تازه · ۴ جهش، هر چهار گرفته شد · ۸۳/۸۳ صفحه · ۳۳/۳۳ ایستا.


## دور ۴۷ — تفکیک نقش سوپرادمین (احمد)

۵۱ کنش مدرسه‌محور از سوپرادمین حذف شد (از ۸۸ به ۳۷).
معیار: صفحهٔ آن کنش در منویش هست یا نه.

خط قرمز رعایت شد: مسیر `S.boss` سالم است چون `S.user` در
جانشینی خودِ مدیر می‌شود.

سه آزمون کهنه که انتظار `superadmin=true` داشتند وارونه شدند.

۴ آزمون تازه · ۳ جهش، هر سه گرفته شد · ۸۳/۸۳ صفحه · ۳۳۶/۳۳۶.


## دور ۴۸ — گام ۱ زنگ جاری: هستهٔ زمان (احمد)

ماژول `46-bell-now.js` (۴۷ ماژول). فقط تشخیص، بدون تغییر رفتار.

نگاشت روز `(getDay()+1)%7` — هر هفت روز سنجیده شد ✅.
سه لایهٔ دفاع ساعت: سال معقول · `hasSchedule` · قلاب سرور.

**تست جهش ۷ مورد:** شش‌تا فوری گرفته شد. هفتمی (نادیده‌گرفتن روز
در `teacherNowClass`) **نگرفت** — آزمون تازه‌ای با مقایسهٔ شنبه و
یکشنبه اضافه شد و حالا می‌گیرد.

۱۰ آزمون تازه · ۳۴۲/۳۴۲ · ۳۳/۳۳ ایستا.


## پایان شیفت احمد (دور ۴۸)

گام ۱ طرح زنگ تمام شد (`46-bell-now.js`، ۴۷ ماژول، کامیت
`a899143`). گام ۲ و گسترش دادهٔ نمونه **عمداً** انجام نشد چون
رضا خواسته بود پس از گام ۱ منتظر تأیید بمانم.

وضعیت تحویل: ۳۳/۳۳ ایستا · ۳۴۲/۳۴۲ دودی · ۸۳/۸۳ صفحه ·
بیلد بیت‌به‌بیت · گیت‌هاب همگام · `HANDOFF.md` با چهار ورودی.


## دور ۴۹ — بررسی مدل داده چندمدرسه‌ای

پرسش مهندس امنیت: آیا فرض تک‌مدرسه‌ای قفل شده؟
**پاسخ: نه.** ۳۲ جدول `school_id` دارند، شناسه‌ها سراسری‌اند،
سلسله‌مراتب `province/county/district` روی ۱۰۰٪ مدارس هست.

نقطهٔ قفل فقط **ذخیره‌سازی** است (یک کلید `localStorage`) — نه
مدل داده. برآورد آماده‌سازی مدل: **صفر**.

`org_id` رد شد: افزونگی است و دو منبع حقیقت می‌سازد.

سند: `ARCHITECTURE_DECISIONS.md` ضمیمهٔ ب.
