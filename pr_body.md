**یک‌مأموریت، صفر مرج، صفر کانفلیکت با `main`.**

## چرا این PR هست
پرامپت سشن دو ادعای «تکمیلی» داشت که راستی‌آزمایی **ردشان کرد**: «مرج نهایی #29» (از ۲۰۲۶-۰۹-۱۰ با merge-commit `c7eee5296` در `main` است) و «بلاکرِ فقدان credential» (`ls-remote` exit 0، توکن معتبر، کلون ≡ `origin/main`). پس مأموریتِ واقعی چت ۷ شد **دروازه‌بانی**، نه بازمرج‌کردن.

## آنچه تحویل می‌دهد
- `docs/daily-reports/2026-09-13-chat7.md` — جدول ۱۲ گیت با **دستور + خروجی واقعی + برچسب Measured (Local) / CI: NOT-RUN**؛ نقشهٔ تعارض سه PR باز (worktree ایزوله `/tmp/mergegate` — بدون rebase/force-push روی شاخهٔ دیگران)؛ §۴-الف = بن‌بست `rc40`↔`stats-sync` + توصیهٔ سیاستی «آخرین مرج‌کننده، آمارساز».
- `docs/daily-reports/2026-09-13-chat7-preaudit.md` — ممیزی Fail-Closed (شاهد به‌شاهد).
- `docs/DOCS_HEALTH_REPORT.md` — بازتولید ماشینی: `main` می‌گفت «۰ لینک شکسته / ۳۲۱ سند / ۲۰۲۶-۰۹-۱۲»؛ واقعیت «۱ لینک شکسته / ۳۲۴ سند».

## یافته‌های اصلی
1. `docs/RUNTIME_SECURITY.md:37` → `../monitoring/alert-rules.yml` گمشده است (مسیر صحیح: `infra/observability/`). ریشه: کامیت `89aebdd`. هیچ‌یک از #129/#157/#159 اصلاحش نکرده. سند در قفل `rc40` یخ‌زده ⇒ اصلاح = بامپ قفل = تصمیم مالک/چت ۵.
2. `docs-refs-check.js` با ۱۵ ارجاع کهنهٔ «تازه» **exit 0** می‌دهد ⇒ گیت بی‌دندان. `docs-health.sh` درست `exit 1` می‌دهد ولی **در هیچ workflow‌ای صدا زده نمی‌شود** (grep در `.github/workflows` = صفر).
3. کانفلیکت هر سه PRِ بلاک‌شده **صددرصد در آمارِ مولّد** است؛ #129 **صفر کانفلیکت کد** دارد (۱۴ فایل، +۱۷۵۶/−۲ شامل `src/js/29-pull.js` و ۷ سوئیت تست جدید).
4. #159 با مرج #168 از `MERGEABLE/UNSTABLE` به **`CONFLICTING/DIRTY`** افتاد — رانش واقعی‌اش ۴ کامیت بود نه ۷۹.
5. هر سه PR زیر **بلاک صریح ناظر** در `NEXT_ACTIONS` (دور ۴) هستند ⇒ مرج نکردم؛ فقط بستهٔ تصمیم.

## گیت‌ها روی این شاخه (محلی؛ CI = NOT-RUN به دلیل بیلینگ `RISK-O-007`)
smoke `547/547` · integration `35/35` · secret-scan `12/12` (سندِ ارجاع ۱۱/۱۱ ادعا می‌کرد — رانش کهنه) · `build --check` PASS · check-authz PASS · `docs-freeze-marker` `14/14` · `reza-mirror-check` `21/21` · `docs-consistency` `49/0` · `docs-refs-check` exit 0 با **صفر بدهیِ تازه از فایل‌های این PR** · `docs-stats-sync --check` = **exit 1 عمداً** (رفعش بامپ `rc41` می‌خواهد که در یدِ #159 است — §۴-الف).

## بهداشت ورک‌اسپیس (`docs/WORKSPACE_HYGIENE.md` §۱/§۴/§۷/§۸)
بودجه ۹۶٫۵MB → **۸۹٫۲MB / ۱٬۶۸۴ فایل 🟢** · `.git` ۶۶→۵۸MB (repack با window=50 طبق §۸) · سه worktree و سه ref موقت `origin/pr-*` **پس از تأیید برابری با refsِ زندهٔ origin** حذف شد (`2b7e102`/`3e9e365`/`80c5910`) · `git fsck` سالم · **صفر حذف فایل پروژه** (باندلِ loose و `p2-old` و `dist` در این سندباکس وجود نداشت ⇒ قاعدهٔ ۴: گیتِ حذف باز بود، مصرف نشد).

## مرزها
هیچ تغییری در `policy.js` / `metrics.js` / `inScope` · هیچ force-push · هیچ آینهٔ تازه در `reza/` · هیچ baseline دست‌کاری‌شده · هیچ توکنی در گزارش/کامیت.

**ROLLBACK:** `git revert` همین کامیت‌ها — محتوا فقط سند است.
