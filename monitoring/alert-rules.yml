# DEPRECATED — canonical Prometheus alert catalogue lives at
# infra/observability/alert-rules.yml
#
# Prometheus loads only the canonical file above. This marker intentionally
# contains no active rules, preventing a second catalogue from drifting.
