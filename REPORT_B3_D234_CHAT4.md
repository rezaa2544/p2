# گزارش تجمیعی چت ۴ — بندهای ب.۳، د.۲، د.۳، د.۴

**شاخه:** `feat/b3-d234-chat4` · **مقصد ادغام:** `main` (منتظر تأیید صریح سوپروایزر — اصل هشتم)
**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-09)

## کامیت‌ها

| کامیت | بند | موضوع | تست رفتاری | جهش |
|---|---|---|---|---|
| `d868f3a` | ب.۳ | ارزشیابی ناشناس معلم | ۱۲/۱۲ + ۱۳/۱۳ سرور | ۵/۵ |
| `236e19e` | د.۲ | کارت امتیازی منطقه (۵ بعد + CSV) | ۱۴/۱۴ | ۶/۶ |
| `4266b1d` | د.۳ | اطلاعیهٔ فوری/بحرانی اداره | ۱۳/۱۳ | ۶/۶ |
| `7f7dc3e` | د.۴ | کمبود نیروی انسانی | ۱۶/۱۶ | ۷/۷ |

## گیت‌های سراسری (در نقطهٔ سرِ شاخه)

| گیت | نتیجه |
|---|---|
| `npm run build && build --check` | ✅ بیت‌به‌بیت + تطبیق راهنما |
| `tests/smoke.js` | ✅ ۵۴۷/۵۴۷ (NAV_EXPECT برای منوهای تازه به‌روز شد) |
| `tools/check-authz.js` | ✅ exit 0 — همهٔ اکشن‌های نویسنده در WRITE_PERMS |
| `tests/authz-model.js` | ✅ ۲۵۰/۲۵۰ (با مجموعهٔ تازهٔ `staff_posts`) |
| `tests/server16.js` | ✅ ۳۹/۳۹ (دروازه‌های تازه پروب‌های قدیمی را نشکست) |
| رگرسیون رفتاری ب.۳/د.۲/د.۳ پس از د.۴ | ✅ همه سبز |

## ب.۳ — ارزشیابی ناشناس معلم (پیش‌تر گزارش شد)
ناشناسی کامل: کلیدهای ذخیره‌شده فقط `school_id/teacher_id/criteria/feedback/created_at/id`؛
`created_by` در درج ذخیره نمی‌شود و فیلد جعلی توسط دروازهٔ فیلد رد می‌شود؛
`upd:[] / del:[]` (تغییرناپذیر)؛ پاسخ خطا و آدیت موفق هیچ‌کدام هویت نمی‌برند.
دروازهٔ `server/sync.js` برای `teacher_evaluations` (مالکیت از مسیر مدرسه/دانش‌آموز).

## د.۲ — کارت امتیازی منطقه
- **ابعاد و وزن‌ها:** مالی .۲۰ (درصد پرداخت اقساط) · داخلی .۲۵ (۰٫۶ حضور + ۰٫۴ اشغال ظرفیت) ·
  رشد .۲۰ (نوبت دوم − اول؛ در نبود ترم، نیمه‌بندی تاریخی نمرات؛ امتیاز = ۵۰+Δ×۵) ·
  رضایت .۱۵ (۱۰۰ − سرانهٔ شکایت×۲۰۰) · مدیریت .۲۰ (نسبت دانش‌آموز/کلاس + مدیر فعال +
  فصل امتحان منتشرشده + مانور سالانه).
- **بازنرمال‌سازی:** فقط روی ابعاد دارای داده؛ اگر هیچ داده‌ای نباشد امتیاز `null` و
  برچسب «داده ناکافی» — هرگز صفر ساختگی (جهش‌های مربوطه کشته شدند).
- **بازه‌ها (G.1):** سبز ≥۶۰ · کهربایی ≥۳۵ · قرمز <۳۵ + سیگنال «ضعف در {بعد}».
- **خروجی:** جدول + کارت‌های آماری + `rscore-csv` با ستون «ابعاد بدون داده».
- **دامنه:** `officeScopeSchools`؛ سوپرادمین همه؛ کاملاً خواندنی.

## د.۳ — اطلاعیهٔ فوری/بحرانی
- `severity` ∈ normal/urgent/critical — رکورد قدیمی بدون فیلد = عادی (آزمون سازگاری دارد).
- `office_id`: اطلاعیهٔ اداره برای همهٔ مدارس محدوده؛ مدارس فقط اطلاعیهٔ ادارهٔ خود
  (`officeForSchool` — خاص‌ترین اداره: منطقه > شهرستان > استان) و سوپرادمین همه را می‌بیند.
- نمایش: بنر قرمز/نوار کهربایی، بج «اطلاعیهٔ اداره»، مرتب‌سازی بحرانی > فوری > تازه‌ترین،
  پینِ خودکارِ بحرانی در مدل (پین‌شده ذخیره می‌شود).
- **دروازهٔ سرور (سازگار با پروب‌های موجود):** کارشناس فقط اطلاعیهٔ دارای `office_id`
  ادارهٔ خودش را درج/ویرایش/حذف می‌کند؛ رکورد بدون `office_id` هرگز رد نمی‌شود
  (پروبِ `eo × announcements × ins` فقط `school_id` دارد و سبز می‌ماند).
- اعتبارسنجی کلاینت: مقدار نامعتبر `severity` رد می‌شود (نه ذخیره، نه سقوط).

## د.۴ — کمبود نیروی انسانی
- مجموعهٔ تازهٔ `staff_posts {school_id, subject_id, required}` — ثبت کامل در زنجیرهٔ
  مدل/مجوزها/مهاجرت/سید/اکشن‌روالس/ترتیب ساخت + دروازهٔ دامنهٔ سرور.
- **موجود:** معلمانِ متمایز هر درس از اسلات‌های برنامهٔ هفتگی (نه شمار اسلات).
- **شکاف = نیاز − موجود** (کمتر از صفر نمایش داده نمی‌شود؛ مازاد نشان جدا دارد)؛
  بدون هنجار = «تعریف نشده» — هرگز صفر ساختگی.
- نما: کارت‌های آماری + تجمیع شهرستان/استان + جدول تفکیک مدرسه/درس با رنگ‌بندی شدت؛
  ویرایشگر هنجار برای سوپرادمین و کارشناس اداره (اعتبارسنجی عدد صحیح ۰ تا ۵۰۰).
- گارد نقش سه‌لایه: منو/روت (`canRoute`) + نمای مستقیم (`viewForbidden`) + ACTION_ROLES + سرور.

## درس‌های فنی (در `docs/AI_PROMPT.md` بخش ۶ ثبت شد)
1. هارنس جهان مصنوعی: بدنهٔ تست‌ها از صف آسنکرون اجرا می‌شود — ساخت/بازگردانی دنیا
   باید گام صف باشد، وگرنه پیش از اجرا خنثی می‌شود؛ بکاپ با `.slice()`.
2. `ins` در `/api/sync` بدون `op.id` رد می‌شود؛ آزمون سروری `await __seq` پیش از
   `finally` می‌خواهد؛ پیش از اسپان، پورت‌ها را از پروسه‌های مانده آزاد کنید.
3. `add()` دمو فقط روی مجموعه‌های موجود کار می‌کند — ثبت در `generate()` الزامی است.
4. در `inScope` سرور از `store_get()` استفاده شود (`store` در آن اسکوپ تعریف ندارد).
5. ناهنجاری محیطی: اسنپ‌شات ورک‌اسپیس یک‌بار `.git` را به شاخهٔ قدیمی برگرداند؛
   بازیابی از روی `origin` انجام شد — پیش از هر کامیت، شاخه و سرِ لاگ را چک کنید.

## اقلام باقی‌مانده
- ادغام به `main` منوط به تأیید صریح سوپروایزر (اصل هشتم) است.
- خارج از اسکوپ نسخهٔ جاری (طبق پیش‌نویس‌ها): رسید خواندن اطلاعیه، توزیع پیامکی،
  تعیین هنجار توسط خود مدرسه.
