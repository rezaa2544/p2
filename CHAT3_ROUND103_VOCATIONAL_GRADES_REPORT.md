# گزارش دور ۱۰۳ — چت ۳ جدید: E.1 نمره‌های تئوری/عملی هنرستان

**تاریخ:** 18/06/1405 (2026-09-09)
**شاخه:** `arena/01a08545-p2` (از `2a2b74f` = main)
**وضعیت:** ✅ کامل — پیاده‌سازی، تست، مستندات، کامیت و پوش

---

## ۱) چه چیزی ساخته شد

در هنرستان‌های فنی‌وحرفه‌ای/کاردانش، نمرهٔ هر درس از دو قسمت **تئوری** و
**عملی** تشکیل می‌شود. این نسخه هر دو را به‌صورت فیلدِ جدا در مدلِ `grades`
نگه می‌دارد؛ **نمرهٔ نهایی (`score`) = میانگینِ قسمت‌هایِ پرشده** — پس همهٔ
مصرف‌کننده‌هایِ پیشینِ `score` (معدل، رتبه، هشدارهای افت، کارنامه‌ها، آمار)
بدونِ تغییر درست کار می‌کنند.

### مدل داده (grades)

| فیلد | نوع | معنی |
|---|---|---|
| `theoretical_score` | number ۰–۲۰ / null | نمرهٔ تئوری |
| `practical_score` | number ۰–۲۰ / null | نمرهٔ عملی |
| `is_vocational` | boolean | نمرهٔ رشته‌ایِ هنرستان |
| `score` | number ۰–۲۰ | **نمرهٔ نهایی** = میانگینِ پرشده‌ها (سازگار) |
| `kind` | theory/practical | مدلِ دور ۶۹ — دست‌نخورده |

### رفتارها

- **فرمِ ثبت (دبیر/هنرآموز):** در کلاس‌هایِ مدرسهٔ توانِ `workshop` فیلدِ
  «نوعِ نمره» — «تئوری» (پیش‌فرض): دو فیلدِ جداگانهٔ تئوری/عملی + فیلدِ
  «نمره» پنهان؛ «عملی/کارگاهی»: رفتارِ دور ۶۹ (رکوردِ واحد) با هم‌پیکریِ
  فیلدهای تازه. جابه‌جاییِ فیلدها با تغییرِ نوع (`gradeKindToggle`).
  مدرسه‌های غیرکارگاهی: فرم دست‌نخورده.
- **مدیر:** ستون‌های جداگانهٔ «تئوری» و «عملی» در فهرستِ نمرات (فقط کلاس
  کارگاهی) + ستونِ نمرهٔ کل (همیشه).
- **دانش‌آموز/ولی:** نمایشِ جداگانه در کارنامهٔ پرونده (چیپ‌های تئوری/عملی)
  و کارنامهٔ A4 (ستون‌های جدا + میانگین) — هر دو قالب.
- **سازگاریِ عقب:** رکوردِ کهنهٔ `kind='practical'` (بدونِ فیلدهای تازه)
  از همان `score` تفسیر می‌شود؛ `vocationalParts()` تنها نقطهٔ تفسیر.
- **دادهٔ نمونه:** پایهٔ آخرِ هنرستان یک نمرهٔ ترکیبیِ تئوری ۱۵/عملی ۱۷
  (score=16) — قطعی و تکرارپذیر.
- **دسترسی:** سه فیلد به `authz/model.json` + قواعدِ تازه در
  `server/validate.js` + بازتولیدِ `write-perms.json` — همان نقش‌ها و
  قاعدهٔ «فقط کلاسِ خودش».

## ۲) کامیت‌ها

| # | هش | پیام | محتوای اصلی |
|---|---|---|---|
| ۱ | `7c0aba2` | `feat(grades): E.1 vocational theory/practical parts + is_vocational (round 103)` | ۶ ماژولِ src + index.html (بیلد) + authz (model/write-perms) + server/validate.js + ۲ سوئیتِ تازه + به‌روزرسانیِ workshop2-mutations |
| ۲ | `c7574d8` | `docs: E.1 vocational grades — VOCATIONAL_GRADES.md, AI_PROMPT 0.5.28, ROADMAP, USER_GUIDE, HANDOFF [2026-09-09]` | سندِ ویژهٔ قابلیت + بندهای ۰.۵.۲۸/ROADMAP E.1 ✅/HANDOFF/دو calloutِ USER_GUIDE |

## ۳) دروازه‌ها (همه سبز)

| دروازه | نتیجه |
|---|---|
| `tests/smoke.js` | ✅ **۵۴/۵۴۷** |
| `tools/check-authz.js` | ✅ صفرِ مغایرت (۵۵ اکشنِ نویسنده، همه با برچسب) |
| `tests/secret-scan.js` | ✅ ۱۱/۱۱ |
| `build.js --check` | ✅ تطبیقِ بایت‌به‌بایت (dist 2019.2KB، ۹۱ ماژول) |
| `tests/authz-model.js` | ✅ ۲۴/۲۴۸ |
| `tests/run.js` | ✅ ۳۵/۳۵ |
| `tests/vocational-grades.js` (تازه) | ✅ ۱۴/۱۴ (V0–V13) |
| `tests/vocational-grades-mutations.js` (تازه) | ✅ ۵/۵ جهش کشته |
| `tests/workshop2.js` (رگرسیون) | ✅ ۱۲/۱۲ |
| `tests/workshop2-mutations.js` (رگرسیون) | ✅ ۴/۴ |

**توضیحِ مهندسی (M4):** با معنایِ تازه، جهشِ «نوع هرگز عملی ذخیره
نشود» زودتر — در لحظهٔ ردِّ ذخیره — می‌میرد؛ `expectFail` از «نوعِ نمره
عملی ذخیره نشد» به «نمرهٔ تازه ثبت نشد» به‌روز شد (کشتنِ رفتاری همان
است — رکوردِ عملی ساخته نمی‌شود).

## ۴) پوش

```
git push origin arena/01a08545-p2
 * [new branch] arena/01a08545-p2 -> arena/01a08545-p2
```

تأیید با `git ls-remote`: remote `c7574d8e6f6573d893135154d02c0f3b85c7e15c`
= HEAD محلی. درختِ کاری پاک.
ساختِ PR (درخواستِ شما بود، به تأییدِ سوپروایزر بستگی دارد):
https://github.com/rezaa2544/p2/pull/new/arena/01a08545-p2

## ۵) وضعیتِ کانفلیکتِ پی‌آرهای باز (مرورِ مرحلهٔ ۳)

همهٔ ۵ پی‌آر با mainِ فعلی (`2a2b74f`) فقط **سطح‌بندی‌نویسی** دارند —
کانفلیکتِ منطقی نیست:

| پی‌آر | شاخه | فایل‌های مشترک | ماهیت |
|---|---|---|---|
| #29 (کتابخانه) | `8723222d` | HANDOFF, USER_GUIDE, index.html, 05-router.js, tests/smoke.js | افزودنی — ادغامِ دوباره کافی است |
| #31 (سلامت) | `455b2e94` | HANDOFF | افزودنی؛ چک‌هایش سبز |
| #32 (assets) | `455b2e94` | HANDOFF, USER_GUIDE | افزودنی + بیلد |
| #33 (behavior) | `455b2e94` | HANDOFF, USER_GUIDE | افزودنی + بیلد |
| #34 (کارآموزی) | `455b2e94` | HANDOFF, USER_GUIDE | افزودنی + بیلد |

**پیشنهاد:** هر شاخه با یک `git merge origin/main` ساده (ادغامِ هر دو
سمتِ HANDOFF) + بیلدِ مجدد (USER_GUIDE/index) آمادهٔ ادغام است — بدونِ
تداخلِ منطقی. طبقِ دستور، به شاخه‌هایِ آنها کاری نکرده شد.

## ۶) تصمیم‌های باز (برای سوپروایزر)

1. **زیرمجموعه‌های عملی** (چند پروژهٔ کارگاهی با نمرهٔ جدا): فعلاً یک
   مقدارِ عملیِ تجمیعی؛ اگر خواسته شود فامیلِ جدا لازم است.
2. **وزن‌دهی** (مثلاً تئوری ۶۰٪/عملی ۴۰٪): فعلاً میانگینِ سادهٔ
   پرشده‌ها؛ تغییر فقط در فرمولِ شاخهٔ تئوریِ `grade-save`.
3. **PR این شاخه به main**: طبقِ پیمانِ چت‌ها، ادغام به main فقط با
   تأییدِ صریحِ سوپروایزر.

## ۷) هشدار امنیتی (تکرار — مهم)

در مکالمهٔ چت، یک **PAT گیت‌هاب** پیست شده بود. استفاده‌نشده است ولی
الان **نشتیِ واقعی** محسوب می‌شود — لطفاً هرچه زودتر از
`Settings → Developer settings → Personal access tokens` **لغو (revoke)**
شود و token تازه ساخته شود.
