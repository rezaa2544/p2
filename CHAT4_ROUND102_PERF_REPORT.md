# گزارش دور ۱۰۲ چت ۴ — رفع تعارض پی‌آر ۱۴ + بهینه‌سازی کارایی ثبت حضور

**تاریخ:** ۱۸/۰۶/۱۴۰۵ · **شاخه:** `feat/farnaz-phase1-chat4` @ `99bae51` · **پی‌آر:** #14

---

## گام ۱ — تعارض پی‌آر ۱۴: رفع شد ✅
- شاخه با مین (`5d7fe50`) همگام است (مرج داخلی با راه‌حل «هر دو ورودی» در `HANDOFF.md`).
- GitHub API اکنون: `mergeable: True` — تعارضی نیست. (`mergeable_state: unstable`
  صرفاً وضعیت استاتوس‌چک است، نه تعارض.)
- فورس‌پوش به شاخه با محافظت مخزن رد شد (`stale info`)؛ از مسیر کامیتِ عادی استفاده شد.

## گام ۲ — بهینه‌سازی `att-set`: هدف زیر ۶۰۰ مس **محقق شد** 🎯

| سنجه (هارنس رسمی، میانهٔ ۵ اجرا) | پیش | پس |
|---|---|---|
| کلِ ثبتِ کلاس ۱۳ نفره | ۷۰۶ مس | **۳۶۴ مس** (۳۱۵–۴۲۷) |
| فاز ۱۳ کلیک | ۵۱۴ مس | **۱۳۷ مس** |

**پیاده‌سازی (کامیت `4058e0b`):**
- مسیر داغ `'att-set'` (حاضر/غایب) در `19-actions-core.js` دیگر `render()` کامل
  نمی‌کند؛ `attPartialSync(id)` در `12-attendance.js` فقط «ردیف همان دانش‌آموز +
  شمارنده‌های خلاصه + نوار پیش‌نویس + پابرگ کارت» را بازسازی می‌کند.
- قالب‌ها به توابع مشترک استخراج شدند (`attCtx` · `attRowHTML` · `attSummaryInner` ·
  `attDraftBarHTML` · `attCardFootHTML`) — رندر کامل و به‌روزرسانی جزئی هر دو از
  **همین** قالب‌ها خروجی می‌گیرند (منبع واحد حقیقت؛ خروجی بایت‌به‌بایت یکسان).
- **سوپاپ اطمینان:** با هر تردیدی (نبود ردیف/خطا/نبود تابع پیش‌نویس) مقدار `false`
  برمی‌گردد و فراخوان به `render()` کامل می‌افتد — رفتار هرگز از قبل بدتر نمی‌شود.
  مسیرهای تاخیر/خروج/مودال‌ها دست نخورده‌اند.

## گام ۳ — تست‌ها و اعتبارسنجی: همگی سبز ✅
| آزمون | نتیجه |
|---|---|
| `tests/attpartial.js` (تازه — ۱۰ بررسی: نبود رندر کامل، ردیف/شمارنده/نوار، رفت‌وبرگشت و تعویض تیک، سوپاپ اطمینان، دورریختن، مودال مرور) | **۱۰/۱۰** |
| `tests/attpartial-mutations.js` (تازه — M1 حذف فراخوانی جزئی، M2 حذف بازسازی ردیف، M3 حذف شمارنده‌ها، M4 حذف درج نوار) | **۴/۴ کشته** |
| `node build.js --check` / `check-authz` / اسکن راز | ۰ / ۰ / ۱۱/۱۱ |
| `tests/smoke.js` | **۵۴۷/۵۴۷** |
| `att2`/`att3`/`att4` · `excuse` · `pnote` · `tomorrow` · `examcount` · `goals` | همگی سبز |
| `ics` | ۲۲/۲۲ در ۳ اجرای پیاپی (یک فلیک ثانیه‌ای شناخته‌شده از دور ۸ — سمت تست، بی‌ربط به این تغییر) |

**یافتهٔ فنی ثبت‌شده برای آینده:** صفت روی `#root` نشانهٔ رندر کامل نیست
(`innerHTML` خود ریشه را نگه می‌دارد) — نشانه‌گذاری باید روی عناصر فرزند
(`isConnected`) باشد؛ سئوت تازه بر همین اساس نوشته شد.

## گام ۴ — کامیت‌ها و اسناد
- `4058e0b` — تغییر کارایی + تست‌ها + `HANDOFF.md` + `docs/AI_PROMPT.md` §۰.۵.۲۶
- `99bae51` — بازتولید مُهر `USER_GUIDE.html` (خروجی بیلد)
- قاعدهٔ نظارتی به‌روز شد (§۰.۵.۲۶): **خط‌مبنای تازه ۳۶۴ مس؛ عبور از ۶۰۰ مس = رجعت کارایی ⇒ توقف و گزارش.**

## مشاهده‌های جانبی این چرخه (برای ناظر)
1. چت ۱ و چت ۳ هر دو شاخه‌هایشان را روی مینِ تازه ری‌بیس کردند (فورس‌آپدیت) — کار
  درستی بود؛ پی‌آر ۸ اکنون سرِ به‌روز دارد.
2. چت ۳ تداخل دستهٔ ج را در ری‌بیسِ خود حل کرد: ج.۱/ج.۲ حذف (یکی‌شده با نسخهٔ
  مرج‌شدهٔ چت ۱ در پی‌آر ۱۲) — اما **ج.۳ خودش (`pubrep`) را نگه داشت**؛ این هنوز
  همپوشانی مفهومی با ج.۳ مرج‌شدهٔ پی‌آر ۱۲ (`public2`) دارد. تأیید/رد پی‌آر ۱۳
  با ناظر است؛ توصیه: اگر ج.۳ مرج‌شده کافی است، `pubrep` از پی‌آر ۱۳ بیرون بیاید.
3. چت ۳ هم گزارش گ.۳ (بازبینی راهنما، فقط گزارش) را تحویل داد — هر دو چت این بند را دارند.

**جمع‌بندی:** همهٔ قواعد سخت رعایت شد؛ هدف زیر ۶۰۰ مس با فاصله محقق است؛ پی‌آر ۱۴
بدون تعارض و آمادهٔ تأیید/مرج ناظر است.
