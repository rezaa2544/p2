# گزارشِ گامِ ۶ — نقصِ ۶: تداخلِ generateP12 (P2)

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08) · **برنچ:** `fix/behavior-findings`
**کامیتِ این گام:** (پایین، پس از ثبت)

---

## ۱. ریشهٔ نقص

دو تعریفِ هم‌نامِ `generateP12`: سالِ‌گذشتهٔ دبیر (نمرات/حضور در
`45-teacher-tools.js`) و فاز ۱۲ مشاور (`47-counselor.js`). تعریفِ دوم
اولی را سایه می‌انداخت ⇒ **نمرات و حضورِ سالِ‌گذشته هرگز ساخته
نمی‌شد** (کدِ مرده)؛ فراخوان‌ها (`24` در بوت، `38` در بازیابی) فقط نسخهٔ
مشاور را اجرا می‌کردند. پیمایشِ کلِ `src/js` نشان داد این **تنها**
تکرارِ نامِ تابعِ سطحِ‌بالاست ⇒ نگهبان با آستانهٔ صفر.

## ۲. رفع (۶ نقطه)

- `45`: ‏`generateP12` ← ‏`generatePriorYear` (+ یادداشتِ باستان‌شناسی).
- `47`: یادداشتِ تفکیک در jsdoc (تعریفِ فاز ۱۲ بی‌تغییر).
- فراخوانیِ دوگانه در **بوت** (`24-edu-office.js`) و **بازیابی** (`38`)
  با محافظِ `typeof` (هر دو مستقل‌اند: مجموعه‌هایِ جدا؛ ترتیب بی‌اثر).
- `tests/persist-roundtrip.js`: آینهٔ ترتیبِ بوت هم دوگانه شد (وفاداری).
- `tests/run.js`: نگهبانِ «نامِ تابعِ سطحِ‌بالا یکتاست» (گروهِ تازه).
  (نگهبانِ add/insertِ گامِ ۱ از پیش `generatePriorYear` را مولد می‌دانست.)

## ۳. آزمون‌ها

- **تازه:** `tests/genp12.js` — G1 هر دو تعریف‌اند و متمایز · G2
  نمره/حضورِ سال‌گذشته پس از بوت (متنِ نقص) · G3 مشاور+ارجاع پس از بوت ·
  G4 بازتولیدِ هر دو پس از بازیابیِ پشتیبان. نتیجه: **9/9** ✅
- **جهش:** `tests/genp12-mutations.js` — M1 حذف از بوت ← G2 · M2 حذف از
  بازیابی ← G4 · M3 برگرداندنِ تداخل ← G1 · M4 همان با سئوتِ `run.js` ←
  نگهبانِ یکتایی. نتیجه: **4/4 کشته، خطِ پایه سبز، ۰ خطایِ محیطی** ✅

## ۴. دروازهٔ این گام

- [x] `node build.js --check` سبز (authz بی‌تغییر)
- [x] `node tests/smoke.js` سبز (**547/547** — رشدِ دادهٔ بوت بی‌اثر بود)
- [x] سئوتِ تازه **9/9** + جهش‌ها **4/4**
- [x] مرتبط‌ها: `run.js` **۳۵/۳۵** (نگهبانِ تازه سبز) · `persist-roundtrip`
      ‏۹/۹ · `backup-snap` ‏۱۸/۱۸
- **ریشه‌یابیِ جهش:** M3 نخست «خطایِ محیطی» شد — علت باگِ سئوت بود نه
  محصول: مقایسهٔ مستقیمِ `generateP12!==generatePriorYear` زیرِ M3
  (تعریف‌نشده) `ReferenceError` می‌داد. با اتصالِ‌کوتاهِ `typeof` اصلاح
  شد و M3 قرمزِ تمیز داد و کشته شد.

**فایل‌هایِ این کامیت:** `45/47/24/38` + `run.js/persist-roundtrip` +
‏۲ سئوتِ تازه + `index.html`/`USER_GUIDE.html` + این گزارش.

**گامِ بعد:** گامِ ۷ — دروازهٔ نهایی (رگرسیونِ کامل + به‌روزرسانیِ
‏مستندات + کامیتِ نهایی).
