name: "CodeQL"
on:
  workflow_dispatch:

jobs:
  codeql-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Informative Notice
        run: echo "Default CodeQL setup is enabled in repository settings."
