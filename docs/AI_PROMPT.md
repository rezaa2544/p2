# پرامپت جامع پروژه «پایش»
### سند انتقال کامل بافتار (Context Handover) برای هر مدل هوش مصنوعی

> **نحوه استفاده:** کل این سند را در ابتدای گفتگو به مدل بدهید. پس از خواندن، مدل باید بدون نیاز به هیچ سؤال اضافه‌ای بتواند روی پروژه کار کند.

---

## ۰. دستور نقش (Role Instruction)

```
تو یک مهندس ارشد نرم‌افزار هستی که روی پروژه‌ی «پایش» کار می‌کنی — یک سامانه‌ی
مدیریت مدرسه‌ی فارسی/RTL که به‌صورت تک‌فایلی و آفلاین‌محور توسعه یافته است.

قواعد الزامی:
۱. هرگز index.html را مستقیم ویرایش نکن — آن خروجی build است. فقط در src/ تغییر بده.
۲. هیچ فریم‌ورک، کتابخانه یا وابستگی خارجی (CDN، npm در زمان اجرا) اضافه نکن.
۳. هر متن قابل مشاهده باید فارسی باشد؛ هر ورودی کاربر باید با esc() فرار داده شود.
۴. تاریخ‌ها داخلی ISO میلادی ذخیره و فقط هنگام نمایش به شمسی تبدیل می‌شوند.
۵. تغییر داده فقط از طریق insert/update/remove — نه دستکاری مستقیم db.
۶. پس از هر تغییر: npm run build && npm test — هر دو باید سبز باشند.
۷. پاسخ‌ها به فارسی، مستقیم و بدون تعارف. اگر چیزی را نمی‌دانی، حدس نزن؛ کد را بخوان.
۸. هرگز الگوی جستجوی خطی در مسیر رندر ننویس. اگر داخل map/forEach به find نیاز
   داری، اول با idxGroup/idxUnique ایندکس بساز. این قاعده مقیاس ملی را ممکن می‌کند.
۹. هر داده‌ای که نقش کاربر اجازهٔ دیدنش را ندارد، نباید اصلاً به مرورگر برسد.
   محدوده را در 29-scope.js تعریف کن، نه با فیلتر سمت نمایش.
۱۰. ⭐ قاعدهٔ دائمی کاربر: پس از **هر** تغییر در برنامه، همین سند
    (docs/AI_PROMPT.md) را به‌روز کن و نسخهٔ به‌روزشده را در ورک‌اسپیس بگذار.
    این کار اختیاری نیست و باید در همان نوبت انجام شود.
۱۱. قاعدهٔ دو نسخه: هر قابلیت جدید باید هم در نسخهٔ تک‌فایلی و هم در نسخهٔ
    سروری پیاده و تست شود.
۱۲. ⭐⭐ **دو اصل همیشگی که هرگز نباید فراموش شوند:**

    **اصل یکم — همگامی مستندات:**
    با **هر** تغییری در برنامه، همین سند (`docs/AI_PROMPT.md`) و راهنمای
    برنامه‌نویس (`CONTRIBUTING.md`) باید **در همان نوبت** به‌روز شوند.
    نه بعداً، نه در نوبت بعد. جزئیات ریز هم باید نوشته شود: چرا آن
    تصمیم گرفته شد، چه عددی سنجیده شد، چه دامی وجود دارد.

    **اصل سوم — مخزن گیت‌هاب همیشه به‌روز:**
    ⭐ این یکی از **شرط‌های اصلی** پروژه است، هم‌ردهٔ دو اصل دیگر.
    مخزن `https://github.com/rezaa2544/p2.git` باید **همیشه** آخرین
    وضعیت کد را داشته باشد. پس از **هر** تغییر، این سه کار با هم و
    در همان نوبت انجام می‌شوند:

    ۱. به‌روزرسانی این سند (`docs/AI_PROMPT.md`)
    ۲. به‌روزرسانی راهنمای برنامه‌نویس (`CONTRIBUTING.md`)
    ۳. ارسال به مخزن:
       ```bash
       T=$(cat ~/.payesh_gh_token)          # توکن بیرون از پروژه نگه‌داری می‌شود
       git add -A && git commit -m "شرح تغییر"
       git push "https://${T}@github.com/rezaa2544/p2.git" HEAD:main
       ```
       اگر تاریخچه بازنویسی شده باشد، `--force` لازم است.

    🔴 **توکن هرگز نباید داخل فایل‌های پروژه نوشته شود.**
    گیت‌هاب سازوکار «حفاظت از ارسال» دارد و اگر توکنی در کد باشد
    ارسال را با خطای `GH013` رد می‌کند. این بار همین اتفاق افتاد.
    توکن در `~/.payesh_gh_token` (بیرون از پوشهٔ پروژه) نگه داشته
    می‌شود و در `.gitignore` هم نیست چون اصلاً داخل پروژه نیست.

    پس از ارسال، نشانی مخزن را بدون توکن برگردان:
    `git remote set-url origin https://github.com/rezaa2544/p2.git`

    هیچ‌کدام از این سه را به نوبت بعد موکول نکن.

    **اصل چهارم — کمترین حجم ممکن، در هر دو جا:**
    حجم باید هم در **ورک‌اسپیس** و هم در **مخزن گیت‌هاب** کمینه بماند.
    پس از هر کار، فایل‌های اضافی را پاک کن:
    • فایل‌های موقت آزمون (`*.tmp.js`)
    • خروجی‌های ساخته‌شده (`dist/`)
    • کلون‌های موقت در `/tmp`
    • بایگانی‌های فشرده‌ای که محتوایشان استخراج و پیاده شده

    ⚠️ حذف از پوشه کافی نیست؛ اگر فایل حجیم در **تاریخچهٔ گیت** باشد
    همچنان حجم می‌گیرد و باید با بازنویسی تاریخچه پاک شود.

    🔴 **هرگز این‌ها را حذف نکن:**
    • هر فایلی که **کاربر** فرستاده است (پوشهٔ `uploads/`)
    • کد منبع (`src/`)، آزمون‌ها (`tests/`)، اسناد (`docs/`)
    • `index.html`, `build.js`, `package.json`, `_order.json`
    • `CONTRIBUTING.md`, `PROJECT_NOTES.md`, همین سند
    اگر دربارهٔ فایلی شک داری، **حذفش نکن** و از کاربر بپرس.

    **اصل دوم — تفکیک اصولی کد:**
    ساختار کد باید طوری بماند که برنامه‌نویس تازه‌وارد **بدون پرسیدن**
    بخش موردنظرش را پیدا کند. نام فایل محتوایش را بگوید، هر صفحه یک
    تابع `view` داشته باشد، و هر ماژول سرفصل فارسی داشته باشد.

۱۳. ⭐ قاعدهٔ نگارش: همهٔ توضیح‌ها به کاربر و همهٔ متن این سند باید **فارسی**
    باشد. نام‌های انگلیسی فقط داخل قطعه‌کد، مسیر فایل یا سلول جدول بیایند و
    هرگز وسط جملهٔ فارسی قرار نگیرند، چون متن درهم و ناخوانا می‌شود.
    ✅ درست: «تابع ساخت کیف پول در ماژول `33-forms-sms.js` است.»
    ❌ نادرست: «تابع smsWalletOf در ماژول forms-sms هست که wallet را create می‌کند.»
```

---

## ۰.۵ 🔴 بدهی امنیتی باز — پیش از هر ممیزی بخوانید

> این بخش عمداً پیش از هویت پروژه آمده تا در هیچ ممیزی از قلم
> نیفتد. تا وقتی این چهار بند باز است، سامانه **نباید** با دادهٔ
> واقعی دانش‌آموز، اولیا یا کارکنان اجرا شود.

| # | بدهی | وضعیت | کجا علامت خورده |
|---|---|---|---|
| ۱ | رمز عبور متن ساده | 🔴 باز | `02-demo-data.js` · `19-actions.js` (login) |
| ۲ | احراز هویت کاملاً سمت مرورگر | 🔴 باز | `19-actions.js` (login) |
| ۳ | کنترل دسترسی فقط سمت مرورگر | 🔴 باز | `30-authz.js` |
| ۴ | محمولهٔ همگام‌سازی بدون قرارداد احراز هویت | 🔴 باز | `27-sync.js` (`sendBatch`) |
| ۵ | **IDOR — دسترسی با شمردن شناسه** | 🔴 باز | `04-queries.js` (`byId`) |

هر چهار نقطه در کد با `🔴 TODO پیش از اتصال به سرور` علامت خورده‌اند.
جست‌وجوی `TODO پیش از اتصال` همه را می‌آورد.

### ۰.۵.۱ رمز عبور — جزئیات

امروز: `password:'123456'` متن ساده، و مقایسه با `!==` در مرورگر.

| موضوع | الزام پیش از سرور |
|---|---|
| الگوریتم | bcrypt، هزینهٔ حداقل ۱۲ |
| محل هش | فقط سرور |
| ذخیره | فقط هش — رمز خام هرگز در پایگاه داده یا گزارش |
| مقایسه | تابع زمان‌ثابت کتابخانهٔ bcrypt |
| پیام خطا | «نام کاربری یا رمز نادرست» — هرگز نگویید کدام‌یک |

⚠️ **دام:** bcrypt ورودی بیش از ۷۲ بایت را بی‌صدا می‌بُرد. رمز فارسی
هر حرف را ۲ بایت می‌کند ⇒ ۳۶ حرف به سقف می‌رسد و بقیه نادیده گرفته
می‌شود. پیش از هش، طول بایتی را بسنجید.

⚠️ **دام:** هزینهٔ ۱۲ امروز مناسب است؛ هر دو سال بازبینی شود.

آزمون `یادآور: رمز عبور هنوز متن ساده است` در `tests/smoke.js` این
وضعیت را تثبیت کرده. روزی که رمزها هش شوند آن آزمون شکست می‌خورد —
و همان‌جا باید به آزمون bcrypt تبدیل شود.

### ۰.۵.۲ چرا کنترل دسترسی مرورگر امنیت نیست

`canRoute()` و `canAction()` خوب نوشته شده‌اند و باید بمانند، ولی
هرکس در کنسول `S.user.role = 'superadmin'` بنویسد از هر دو رد
می‌شود. این نقص پیاده‌سازی نیست؛ **ذات کدی است که روی دستگاه کاربر
اجرا می‌شود**.

هیچ گاردی سمت مرورگر این را حل نمی‌کند، چون همان گارد هم از همان
کنسول قابل بازنویسی است. تنها پاسخ واقعی: سنجش دوباره سمت سرور.

مرز را در ذهن نگه دارید:

| لایه | نقش مرورگر | نقش سرور |
|---|---|---|
| کنترل دسترسی | راحتی کاربر | **امنیت** |
| امن‌سازی خروجی (XSS) | **خط مقدم** | کمکی |
| اعتبارسنجی داده | بازخورد فوری | **مرجع نهایی** |

### ۰.۵.۳ محمولهٔ همگام‌سازی

هر عملیات در صف، فیلد `by` دارد که `03-persistence.js` از
`S.user.id` پر می‌کند — یعنی **از مرورگر، پس ادعاست**.

سرور باید `op.by` را با کاربر احراز هویت‌شدهٔ ژتون بسنجد و اگر
نخواند **کل دسته را رد کند**، نه فقط آن یک عملیات.

مبنای تصمیم‌گرفته‌شده برای احراز هویت: **کوکی `HttpOnly` + `Secure`
+ `SameSite=Lax`** حاوی JWT کوتاه‌عمر. دلیل انتخاب کوکی به‌جای
سرآیند `Authorization`: ژتون از دید جاوااسکریپت پنهان می‌ماند، پس
اگر روزی XSS از لایهٔ `esc`/`escAttr` رد شود باز هم قابل سرقت نیست.
`Lax` هم جلوی جعل درخواست بین‌سایتی را می‌گیرد بدون ژتون CSRF
جداگانه.

### ۰.۵.۴ دادهٔ حساس

سامانه دادهٔ **کودکان** را نگه می‌دارد. کد ملی، تلفن و آدرس
پرخطرترین‌اند.

- انتقال: HTTPS اجباری. رمزنگاری لایهٔ برنامه روی TLS **لازم نیست**
  مگر واسطهٔ نامطمئنی در مسیر باشد.
- ذخیره: کد ملی رمزنگاری‌شده در سطح ستون + ستون هش قطعی برای
  جست‌وجو و بررسی یکتایی.
- 🔴 کد ملی و تلفن **هرگز** در نشانی درخواست، گزارش سرور یا پیام
  خطا نیایند. فقط بدنهٔ `POST`.

### ۰.۵.۵ IDOR — پرخطرترین بدهی، با عدد

`byId(collection, id)` در `04-queries.js` هیچ سنجش محدوده‌ای ندارد.
هر کاربر معتبری که شناسه را بشمارد، به دادهٔ خارج از محدوده‌اش
می‌رسد. سنجش‌شده روی دادهٔ نمونه (۶ مدرسه):

| نقش مهاجم | درون محدوده | **خارج از محدوده** |
|---|---|---|
| مدیر مدرسه | ۲۷۵ کاربر | **۸۷۰ کاربر** |
| ولی با ۱ فرزند | ۱ دانش‌آموز | **۵۲۶ دانش‌آموز** |
| دبیر با ۳ کلاس | ۳ کلاس · ۹۶۲ نمره | **۴۱ کلاس · ۱۱٬۵۹۲ نمره** |

⚠️ **چرا توابع محدودهٔ موجود جلویش را نمی‌گیرند:**
`scopeDescriptor()` و `visibleClasses()` فقط در **نماها** صدا زده
می‌شوند — لایه‌ای که مهاجم اصلاً از آن رد نمی‌شود. `byId` مستقیم از
ایندکس می‌خواند. فاصلهٔ میان **اعلام** و **اجرا** همان جایی است که
IDOR زندگی می‌کند.

⚠️ **نکتهٔ سرور:** برای رکورد خارج از محدوده `404` برگردانید نه
`403`. پاسخ `403` تأیید می‌کند رکورد وجود دارد و مهاجم با همین
می‌تواند شناسه‌های معتبر را نقشه‌برداری کند.

سه آزمون `IDOR ⚠️` در `tests/smoke.js` این اعداد را زنده نگه
می‌دارند و در خروجی چاپشان می‌کنند. روزی که سرور وصل شود، هر سه
باید **معکوس** شوند: انتظار برود دسترسی خارج از محدوده صفر باشد.

⚠️ **کارایی و امنیت با هم بازسنجی شوند.** بنچمارک‌های
`docs/CAPACITY_SIM.md` پیش از وجود سنجش محدوده گرفته شده‌اند و پس
از افزودنش بی‌اعتبارند. سنجش مدیر ارزان است (یک مقایسهٔ `school_id`)
ولی سنجش ولی و دبیر جست‌وجو لازم دارد و بدون ایندکس در حلقه
درجه‌دوم می‌شود. 🔴 راه‌حل کندی، ایندکس است نه حذف سنجش — استثنا
برای «مسیرهای پرترافیک» دقیقاً جایی است که نشت رخ می‌دهد.
عدد تخمینی عمداً ثبت نشده؛ در فاز ساخت سرور اندازه گرفته می‌شود.

### ۰.۵.۶ سه لایهٔ دفاعی تکمیلی (فقط مستند، پیاده‌سازی با سرور)

| لایه | نکتهٔ کلیدی که آسان اشتباه می‌شود |
|---|---|
| محدودسازی نرخ ورود | **قفل سخت نگذارید** — مهاجم با ۵ تلاش عمداً غلط، معلم را روز امتحان قفل می‌کند. تأخیر تصاعدی درست است. |
| سرآیندهای امنیتی | خروجی تک‌فایلی است ⇒ سیاست محتوا به `'unsafe-inline'` نیاز پیدا می‌کند که بی‌اثرش می‌کند. `build.js` باید `nonce` تولید کند. |
| هشدار شمردن شناسه | **کندسازی نه قطع** — مدیری که گزارش سالانه می‌گیرد هم صدها رکورد می‌خواند. |

⚠️ **نشت از راه زمان پاسخ:** اگر «کاربر ناموجود» در ۵ میلی‌ثانیه و
«رمز غلط» در ۲۵۰ میلی‌ثانیه پاسخ بگیرد، مهاجم فهرست نام‌های کاربری
معتبر را بدون زدن حتی یک رمز درست استخراج می‌کند.

راه‌حل (بند ۵.۵.۳.۱ قرارداد): وقتی کاربر پیدا نشد، bcrypt را روی
`DUMMY_HASH` از پیش ساخته‌شده اجرا کنید و نتیجه را دور بریزید. هش
ساختگی باید **همان هزینهٔ** هش‌های واقعی را داشته باشد، وگرنه نشت
سر جایش می‌ماند. بررسی `active` هم باید **بعد از** مقایسهٔ رمز
بیاید، نه پیش از آن.

⚠️ **تصمیم زودهنگام دربارهٔ شناسه:** اگر قرار است شناسه‌ها UUID شوند
تا قابل شمردن نباشند، باید **پیش از** نخستین استقرار تصمیم گرفته
شود. مهاجرت پس از پر شدن پایگاه داده سنگین است.

### ۰.۵.۱۱ ارزیابی کلی وضعیت (دور ۴۰)

سنجش واقعی با jsdom، نه ادعا:

| سنجه | نتیجه |
|---|---|
| رندر همهٔ صفحات | **۸۲/۸۲ سالم، صفر خطا** |
| خودآزمایی داخلی | ۲۹ سالم · ۲ هشدار · ۱ بحرانی · نمره **۷۵** |
| حجم `db` سریال‌شده | **۴٫۶۱MB** در برابر سقف ~۵MB مرورگر |
| `index.html` | ۹۰۴KB · ۴۴ ماژول |

⚠️ **مرز حافظه نزدیک است.** دادهٔ نمونه ۴٫۶۱MB است و سقف
`localStorage` حدود ۵MB. امروز مشکلی نیست چون کل `db` ذخیره
نمی‌شود (فقط دفترچهٔ تغییرات) و نمونه هر بار بازتولید می‌شود — ولی
هر افزایش حجم نمونه باید با این عدد سنجیده شود.

🔴 **سه شکاف دمو که با سنجش پیدا شد:**
۱. `db.meeting_slots` و `db.bell_schedules` هر دو **صفر**اند.
   صفحه‌ها کار می‌کنند ولی خالی باز می‌شوند — بد است در نمایش زنده.
۲. رمز همهٔ کاربران `123456` است (خودآزمایی خودش هشدار می‌دهد).
۳. بررسی «دسترسی به حافظهٔ مرورگر» در محیط بدون `localStorage`
   بحرانی گزارش می‌شود.

⚠️ **`renderRoute()` است نه `render()`.** سنجش اول من ۸۲ خطای
کاذب داد چون تابع غلط را صدا زدم و `innerHTML` صفر برگشت.
`diagHealthScore(sum)` هم پارامتر لازم دارد.

### ۰.۵.۱۰ رابط کاربری توزیع — پیاده شد

سه بخش تازه در `34-excel-import.js`:
`impCapTable` · `impManualBlock` · `impRulesCard`.
دو کنش در `19-actions.js`: `imp-raise-cap` · `place-rules-save`.

`validateImport` حالا `capRows`, `manualRows`, `settings` و
شمارنده‌های `auto`/`manual` هم برمی‌گرداند.

⚠️ **مجموع فراتر از ظرفیت سه نشانه دارد، نه یکی:** عدد قرمز،
برچسب «فراتر از ظرفیت»، و هشدار متنی زیر جدول. عدد خاکستری هشدار
واقعی نیست و مدیر ماه‌ها بعد کلاس ۵۵ نفره کشف می‌کند.

⚠️ **`validateImport` مدرسه را از `S.user.school_id` می‌خواند، نه
از پارامتر.** آزمونی که کاربر را تنظیم نکند، جدول ظرفیت **خالی**
می‌گیرد و می‌تواند بی‌صدا سبز شود. کمکی `withBranches()` در
`smoke.js` کاربر را هم می‌گذارد و در `finally` برمی‌گرداند.

⚠️ افزایش ظرفیت فقط با عدد صریح مدیر؛ پس از ثبت، پیش‌نمایش
خودکار بازسازی می‌شود تا نتیجهٔ تازه دیده شود.

### ۰.۵.۹ توزیع خودکار — پیاده شد

توابع تازه در `41-class-placement.js`: `buildClassIndex`,
`buildOccupancy`, `pickBranch`, `branchCandidates`,
`hasExplicitSection`, `placeSettings`, `isMixedSchool`,
`familyKey`, `PLACE_DEFAULTS`.

`planPlacement(rows, schoolId, opts)` حالا `manual` و `occ` هم
برمی‌گرداند.

🔴 **سه دام که فقط با سنجش پیدا شدند — نه با خواندن کد:**

**۱. کلاس بی‌شمارهٔ هم‌نام.** مدرسه‌ای که کلاس «دهم علوم تجربی»
دارد و بعد شعبهٔ ۱ و ۲ اضافه کرده: تطبیق نام دقیق روی کلاس
بی‌شماره می‌افتد و **همه آنجا تلنبار می‌شوند**. حالا وقتی چند
شعبهٔ هم‌رشته هست، نام متعارف انتخاب صریح شمرده نمی‌شود.

**۲. `parsePlacement` برای رشته‌ای‌ها `section` را پر نمی‌کند.**
«دهم علوم تجربی ۳» و «دهم تجربی» `pl` یکسان می‌دهند. پس
`hasExplicitSection` باید `pl.raw` را هم بسنجد، و `findClassFor`
هنگام چند نامزد شماره را تطبیق دهد وگرنه شعبهٔ ۱ را می‌دهد.

**۳. `hasExplicitSection` ۸۳٪ زمان را می‌خورد.** سنجش تفکیکی:
۷۵۱ms از ۹۰۴ms. علت: حلقه روی ۱۵۱ نامزد با `normField`.
راه‌حل: نقشهٔ `byNameDigit` در `buildClassIndex` ⇒ **۱۴ms**.

⚠️ **درس:** حدس اولم `slice()` بود و **غلط** بود. هزینه را
تفکیکی اندازه بگیر — این پنجمین بار است که حدس کارایی در این
پروژه اشتباه از آب درآمد.

| سنجش | نتیجه |
|---|---|
| ۹ نفر / ۳ شعبه | ۳-۳-۳ متعادل |
| ۳۵ نفر / ۳×۱۰ | ۱۰-۱۰-۱۰ + ۵ دستی |
| سه اجرای پیاپی | یکسان ✅ |
| ۳۰۰۰ ردیف / ۱۶۲ کلاس | ۱۸۷ms |

### ۰.۵.۸ کار جاری: توزیع خودکار کلاس‌بندی

📄 طرح کامل: `docs/PLACEMENT_PLAN.md` — **طرح است، هنوز پیاده نشده.**

بازبینی نشان داد ورود اکسل تا حد زیادی **از قبل کامل** است:
۴۱ فیلد با مترادف، تشخیص هوشمند ستون، خطای ردیف‌به‌ردیف با تفکیک
خطا از هشدار، و رکورد تکراری با سه حالت. دامنهٔ کار تازه فقط دو
چیز است:

**۱. توزیع خودکار.** `findClassFor` امروز فقط **تطبیق** می‌کند.
اگر فایل بگوید «دهم تجربی» و سه شعبه باشد، `null` برمی‌گرداند
(شرط `cands.length === 1`). قانون تازه: کم‌جمعیت‌ترین شعبهٔ واجد
شرایط، با احترام به ظرفیت.

**۲. کارایی روی محور تعداد کلاس.**

🔴 **درس روش‌شناختی مهم — بنچمارک تک‌محوره فریب می‌دهد:**

```
۳۰۰۰ ردیف با   ۹ کلاس →  ۲۱۱ms
۳۰۰۰ ردیف با ۱۵۹ کلاس → ۱۰۰۳ms   (۴٫۸× کندتر)
```

روی محور **ردیف** کاملاً خطی بود و سالم به نظر می‌رسید. مشکل روی
محور **کلاس** پنهان بود: `findClassFor` برای هر ردیف تا چهار بار
روی کل فهرست `filter` می‌زند ⇒ `O(ردیف × کلاس)`.

⚠️ **قاعده:** تابعی که دو ورودی دارد دو محور دارد. ثابت نگه داشتن
یکی، مشکل روی دیگری را پنهان می‌کند. با ۹ کلاس دادهٔ نمونه این
کاملاً نامرئی است.

راه‌حل: همان الگوی `commitImport` — نقشهٔ درهم‌سازی یک‌بار پیش از
حلقه.

**سه تصمیم قفل‌شدهٔ توزیع:**

| موضوع | تصمیم |
|---|---|
| تکرارپذیری | همان فایل ⇒ همان نتیجه. بدون `Math.random`؛ امتیاز برابر ⇒ شناسهٔ کوچک‌تر |
| همه پر | «بررسی دستی» — **نه** ساخت خودکار کلاس، **نه** نادیده گرفتن ظرفیت |
| تفکیک جنسیتی و خواهر/برادر | هر دو **اختیاری، پیش‌فرض خاموش** |

⚠️ خواهر و برادر فقط با **کد ملی والدین** تشخیص داده شود، نه نام
خانوادگی — «محمدی» در یک مدرسه ده‌ها نفر است.

### ۰.۵.۷ 🔒 فاز طراحی امنیتی و معماری بسته شد

تصمیمات پایه گرفته و **قفل** شده‌اند. در فاز سرور لازم نیست دوباره
بحث شوند — فقط پیاده شوند.

📄 **خلاصهٔ یک‌صفحه‌ای:** `docs/ARCHITECTURE_DECISIONS.md`

سه تصمیمی که در دور آخر بسته شد:

| تصمیم | انتخاب | اثر معماری |
|---|---|---|
| شکل شناسه | **عددی پیاپی می‌ماند** | UUID جایگزین سنجش مالکیت نیست |
| حل تعارض | **ترکیبی بر پایهٔ نوع داده** | هر عملیات حساس `base_version` لازم دارد |
| حذف | **نرم برای سابقه، سخت برای عملیاتی** | هر پرس‌وجو `deleted_at IS NULL` لازم دارد |

⚠️ دو مورد اخیر **از روز اول** باید باشند. افزودن `base_version` پس
از انباشتن میلیون‌ها عملیات، یا افزودن شرط `deleted_at` پس از نوشتن
صدها کوئری، مهاجرت گسترده است.

⚠️ **تنها وابستگی متقابل فهرست:** شناسهٔ عددی فقط تا وقتی قابل دفاع
است که سنجش مالکیت (بند ۱.۲) و هشدار شمردن (بند ۵.۷) هر دو پیاده
باشند. حذف یکی ⇒ بازبینی شکل شناسه.

**دو مورد عمداً باز:** محل نگهداری داده (وابسته به ارائه‌دهنده) و
رضایت والدین (وابسته به تکلیف حقوقی — 🔴 پیش از نخستین ثبت‌نام
واقعی تصمیم بگیرید).

📄 **قرارداد کامل سرور:** `docs/SERVER_SECURITY_CONTRACT.md`
   (بندهای ۵.۵ نرخ · ۵.۶ سرآیندها · ۵.۷ هشدار شمردن · ۵.۸ معماری)
📄 **فهرست کارهای باقی‌مانده:** `TODO_BEFORE_PRODUCTION.md`

---

## ۱. هویت پروژه

| ویژگی | مقدار |
|---|---|
| **نام** | پایش (Payesh) — سامانه هوشمند مدیریت مدرسه |
| **مخاطب** | مدارس ایران، ادارات آموزش‌وپرورش، دبیران، دانش‌آموزان، اولیا |
| **زبان/جهت** | فارسی، RTL، تقویم شمسی (جلالی) |
| **وضعیت فعلی** | **نسخه دمو** — بدون بک‌اند؛ سرور بعداً وصل می‌شود |
| **پشته** | Vanilla JS، صفر وابستگی زمان اجرا، بدون فریم‌ورک |
| **خروجی** | یک فایل `index.html` (~۴۹۹KB) شامل همه‌چیز |
| **حجم کد منبع** | ~۱۱٬۲۰۰ خط جاوااسکریپت در **۴۴ ماژول** + ۳ فایل شیوه‌نامه |
| **داده نمونه** | ۳۲٬۱۵۷ رکورد در ۳۰ مجموعه (~۴.۶MB JSON در حافظه) |

### دو معنای متفاوت «آفلاین» — مهم است اشتباه نشود

| اصطلاح | معنی در این پروژه |
|---|---|
| **تک‌فایلی / بدون وابستگی** | فونت، لوگو، CSS و JS همه داخل فایل جاسازی شده‌اند؛ هیچ درخواست CDN وجود ندارد |
| **Offline-first** ⭐ | **قابلیت اصلی محصول:** دبیری که در مدرسه اینترنت ندارد، حضور و غیاب می‌زند؛ داده روی دستگاه می‌ماند و به‌محض وصل شدن اینترنت خودکار به سرور می‌رود |

مورد دوم دلیل وجودی لایه‌ی `27-sync.js` است.

---

## ۲. معماری

### ۲.۱ توسعه ماژولار، توزیع تک‌فایلی

```
src/head.html  ─┐
src/styles/*.css├─→  node build.js  ─→  index.html  (تک‌فایلی، قابل توزیع)
src/js/*.js    ─┤                        └─→ dist/payesh.html
src/body.html  ─┘
```

`build.js` فقط **الحاق (concatenation)** انجام می‌دهد — نه ترنسپایل، نه باندلر، نه minify.
به همین دلیل `node build.js --check` می‌تواند تضمین کند خروجی **بیت‌به‌بیت** با نسخه‌ی توزیعی یکسان است.

⚠️ همه‌ی ماژول‌ها در **یک scope مشترک** اجرا می‌شوند (نه ES modules). پس:
- ترتیب در `src/js/_order.json` **حیاتی** است
- نام متغیرها سراسری‌اند — خطر تصادم نام وجود دارد
- `const`/`let` سطح‌بالا روی `window` نمی‌نشینند (نکته‌ی مهم برای تست)

### ۲.۲ ترتیب بارگذاری ماژول‌ها (فعلی)

```
01-helpers          توابع پایه: $, esc, fa, jalali, toast, empty, bar + ثابت‌ها
02-demo-data        موتور تولید داده شبه‌تصادفی (SEED ثابت = 20260901)
03-persistence      applyOp / insert / update / remove + mutation log + قلاب sync
                    + باطل‌سازی خودکار ایندکس + هشدار پرشدن حافظه
04-queries          کوئری‌های مبتنی بر نقش: visibleClasses, visibleSubjects…
05-router           شیء S، NAV، TITLES، go/goBack، مدیریت history
28-indexes          ⭐ لایه ایندکس (پیش از persistence بارگذاری می‌شود)
29-scope            ⭐ محدودهٔ دادهٔ هر نقش + قرارداد bootstrap سرور
30-authz            ⭐ مجوزدهی روت و اکشن (پس از NAV، پیش از shell)
31-student-lifecycle        ⭐ چرخهٔ تحصیلی (پیش از actions بارگذاری می‌شود)
32-atrisk-meetings-growth       ⭐ افت تحصیلی، جلسات اولیا، رشد مدرسه
33-forms-sms        ⭐ فرم‌های رسمی چاپی و پنل پیامک
34-excel-import     ⭐ ویزارد ورود اکسل (خوانندهٔ بومی xlsx)
35-superadmin       ⭐ مالی سراسری · بازنشانی رمز · سلامت سامانه
36-audit-activity   ⭐ سابقهٔ تغییرات · بازدید و بار سامانه
37-admin-tools      ⭐ وضعیت مدارس · خروجی اکسل · اطلاعیهٔ سراسری
38-plans-backup     ⭐ پلان فروش · پشتیبان‌گیری و بازیابی
39-school-year      ⭐ چرخهٔ سال تحصیلی · چیدمان کلاس · ثبت‌نام
00-data-layer       ⭐ لایهٔ داده: Store و Api و Data — تنها دروازهٔ حافظه
40-form-validation  ⭐ اعتبارسنجی دیداری فرم‌ها (کادر قرمز)
41-class-placement  ⭐ کلاس‌بندی خودکار از اکسل (تشخیص پایه و رشته)
42-self-diagnostics ⭐ دیاگ سامانه: ۳۲ آزمون موتور و داده + تعمیر خودکار
43-bell-schedule    ⭐ شیفت مدرسه و زمان‌بندی زنگ‌ها (تعریف با مدیر)
25-filters       ⭐ فیلترهای جمع‌شونده (مشترک همه پنل‌ها)
26-curriculum       ⭐ ساختار مقطع/پایه/شاخه/رشته + کتاب‌های درسی
27-sync             ⭐ لایه offline-first: صف ارسال، تشخیص اتصال، backoff
06-login            صفحه ورود + حساب‌های دمو
07-shell            پوسته: سایدبار، نوار بالا، renderShell + گارد مجوز روت
08-dashboard        داشبورد هر نقش
09-schools          مدیریت مدارس
10-users            مدیریت کاربران
11-classes-subjects کلاس‌ها + دروس (با جستجو و فیلتر و گروه‌بندی)
12-attendance       حضور و غیاب
13-grades           نمرات
14-discipline       پرونده انضباطی
15-schedule         برنامه هفتگی
16-announcements    اطلاعیه‌ها
17-student-record   کارنامه دانش‌آموز
18-modals           پنجره‌های محاوره‌ای + f/inp/sel helpers
19-actions          ⭐ کنترلگر مرکزی رویدادها (event delegation) + گارد مجوز اکشن
20-communication-finance        اعلان، مرخصی، تقویم، گفتگو، شهریه، دفتر مالی، آمار منطقه
21-phase-8          چندمدرسه‌ای، امتحانات، مراقبت، تأیید اولیا، کد ملی
22-jalali-calendar  موتور تقویم شمسی + date-picker
23-phase-10         چندنقشی، اشتراک اولیا، paywall
24-phase-9          تقسیمات کشوری، ادارات، پنل اداره + render() + boot
```

> `24-phase-9` آخرین ماژول است و شامل `render()` و بلوک راه‌اندازی (`setTimeout(...,50)`) می‌شود.

### ۲.۳ جریان اجرا

```
بارگذاری صفحه
      │
      ├─ generate()        ← داده پایه با SEED ثابت (قابل بازتولید)
      ├─ generateExtras()  ← مالی، تقویم، پیام
      ├─ generateP8()      ← امتحانات، کد ملی، چندمدرسه‌ای
      ├─ generateP9()      ← تقسیمات کشوری، ادارات
      ├─ generateP10()     ← اشتراک‌ها
      ├─ loadLog()         ← خواندن تغییرات کاربر از localStorage
      ├─ applyLog()        ← بازپخش روی داده پایه  (SYNC_MUTED=true)
      ├─ initSync()        ← بارگذاری صف ارسال + شنود online/offline
      └─ render()
              │
     ┌────────┴────────┐
 renderLogin      renderShell
                       │
                  renderRoute()   ← switch روی S.route
                       │
                  viewXxx()       ← رشته HTML برمی‌گرداند
                       │
                 کلیک کاربر
                       │
        event delegation روی [data-act]
                       │
                  insert/update/remove
                       │
              applyOp() ──┬─→ log  → localStorage
                          └─→ enqueueOp() → صف همگام‌سازی
                       │
                    render()
```

### ۲.۴ الگوی رندر

رشته‌محور، بدون Virtual DOM:
- هر `viewXxx()` یک رشته HTML برمی‌گرداند
- `$('#root').innerHTML = ...` کل صفحه را بازنویسی می‌کند
- رویدادها با **event delegation** روی `document` مدیریت می‌شوند (`data-act`)
- پس نیازی به اتصال مجدد listener بعد از رندر نیست

**استثنا:** `refreshSyncBadge()` فقط نشانگر همگام‌سازی را جایگزین می‌کند تا رندر کامل لازم نشود.

---

## ۲.۵ راهنمای خط‌به‌خط: کجا چه چیزی را پیدا کنم

این جدول برای وقتی است که می‌خواهی چیزی را تغییر دهی و باید بدانی **دقیقاً کدام فایل و کدام تابع**.

### نقاط ورود و جریان اصلی

| می‌خواهم… | فایل | نشانه |
|---|---|---|
| ببینم برنامه چطور بالا می‌آید | `24-edu-office.js` انتها | `setTimeout(...,50)` |
| صفحه‌ای را به مسیریابی اضافه کنم | `07-shell.js` | `function renderRoute()` → `switch` |
| آیتم منو اضافه کنم | `05-router.js` | `const NAV={` |
| عنوان صفحه بگذارم | `05-router.js` | `const TITLES={` |
| مجوز روت بدهم | `30-authz.js` | `EXTRA_ROUTES` |
| دکمه‌ای را کارا کنم | `19-actions.js` | شیء `A={` داخل شنوندهٔ `click` |
| مجوز اکشن بدهم | `30-authz.js` | `ACTION_ROLES` |
| پوستهٔ صفحه را عوض کنم | `07-shell.js` | `renderShell()` |

### داده

| می‌خواهم… | فایل | نشانه |
|---|---|---|
| مجموعهٔ داده اضافه کنم | `02-demo-data.js` | `db={...}` در `generate()` |
| دادهٔ نمونه بسازم | `02-demo-data.js` | `generate*()` — PRNG قطعی با `SEED=20260901` |
| داده را تغییر دهم | `03-persistence.js` | `insert/update/remove` — **هرگز `db.x.push()`** |
| بفهمم ماندگاری چطور کار می‌کند | `03-persistence.js` | `applyOp` → `log` → `localStorage` |
| کوئری نقش‌محور بنویسم | `04-queries.js` | `visibleClasses/visibleSubjects` |
| ایندکس بسازم | `28-indexes.js` | `idxGroup`/`idxUnique` |
| محدودهٔ داده را تغییر دهم | `29-scope.js` | `scopeDescriptor()` |

### رابط کاربری

| می‌خواهم… | فایل | نشانه |
|---|---|---|
| مودال بسازم | `18-modals.js` | `modalTpl`, `openModal`, `askConfirm` |
| فرم بسازم | `18-modals.js` | `f(label,html)`, `inp`, `sel`, `opt`, `V(id)` |
| فیلتر جمع‌شونده بگذارم | `25-filters.js` | `filterPanel(route, html)` + ثبت در `FILTER_KEYS` |
| تاریخ شمسی نشان دهم | `22-jalali-calendar.js` | `isoToJalali`, `jalaliToIso`, `faD` |
| عدد فارسی کنم | `01-helpers.js` | `fa()` برای عدد · `faD()` برای رشته |
| متن را امن چاپ کنم | `01-helpers.js` | `esc()` |
| پیام موقت نشان دهم | `01-helpers.js` | `toast(msg, 'ok'\|'err'\|'')` |

### قابلیت‌های خاص

| موضوع | فایل |
|---|---|
| حضور و غیاب | `12-attendance.js` |
| نمرات | `13-grades.js` |
| مالی، مرخصی، چت، تقویم | `20-communication-finance.js` (بزرگ‌ترین، ۵۲۰ خط) |
| امتحانات، اولیا، کد ملی | `21-exams-teachers-family.js` |
| اشتراک، چندنقشی، paywall | `23-subscription.js` |
| اداره، تقسیمات کشوری | `24-edu-office.js` |
| کتاب‌های درسی | `26-curriculum.js` |
| همگام‌سازی آفلاین | `27-sync.js` |

### توالی اجرای یک کلیک (برای دیباگ)

```
کلیک کاربر
  └─ 19-actions.js: شنوندهٔ click روی document
       └─ closest('[data-act]') → نام اکشن
            └─ 30-authz.js: canAction(a)  ← اگر false، توقف با toast
                 └─ A[a]() اجرا می‌شود
                      └─ 03-persistence.js: insert/update/remove
                           ├─ 28-indexes.js: idxInvalidate(coll)
                           ├─ log.push + saveLog()
                           └─ 27-sync.js: enqueueOp()  (اگر SYNC_MUTED نباشد)
                      └─ render()
                           └─ 07-shell.js: renderRoute()
                                └─ 30-authz.js: canRoute(S.route) ← اگر false، viewForbidden()
                                     └─ viewXxx() → رشتهٔ HTML
                                          └─ $('#root').innerHTML = ...
```

### سه فایلی که پیش از هر تغییر باید بخوانی

۱. `src/js/_order.json` — ترتیب الزامی؛ ماژول ثبت‌نشده اصلاً وارد build نمی‌شود
۲. `src/js/01-helpers.js` — همهٔ helperها اینجاست؛ دوباره‌نویسی نکن
۳. `src/js/04-queries.js` — کوئری‌های نقش‌محور؛ مرز دسترسی از اینجا می‌گذرد

---

## ۲.۶ ساختار کد برای برنامه‌نویس ⭐

**خواستهٔ کاربر:** «اگر برنامه‌نویسی آمد و خواست تغییری بدهد، باید راحت
بخش موردنظر را پیدا کند.» این قاعده هنگام هر تغییری باید رعایت شود.

### قواعد نام‌گذاری
نام فایل باید **محتوایش** را بگوید، نه تاریخچهٔ ساختش را.
نام‌هایی مانند «فاز ۳ تا ۷» حذف شدند چون هیچ چیزی به تازه‌وارد نمی‌گویند:

| نام قدیمی (گنگ) | نام تازه (گویا) |
|---|---|
| `20-phase-3-7` | `20-communication-finance` |
| `21-phase-8-exams-parents` | `21-exams-teachers-family` |
| `23-phase-10-subscription` | `23-subscription` |
| `24-phase-9-edu-office` | `24-edu-office` |
| `31-lifecycle` | `31-student-lifecycle` |
| `32-engagement` | `32-atrisk-meetings-growth` |
| `34-import` | `34-excel-import` |

### سرفصل استاندارد هر ماژول
هر فایل با این قالب شروع می‌شود — عنوان فارسی و یک جملهٔ توضیح:

```js
/* ═══════════════════════════════════════════════════
   عنوان فارسی ماژول
   یک جمله دربارهٔ اینکه چه می‌کند و چرا اینجاست.
   ═══════════════════════════════════════════════════ */
```

### یک صفحه = یک تابع `view`
هر صفحهٔ برنامه دقیقاً یک تابع با پیشوند `view` دارد، پس یافتنش ساده است:
```bash
grep -rn "function viewGrades" src/js/
```

### سند `CONTRIBUTING.md`
راهنمای کامل برنامه‌نویس در ریشهٔ پروژه است و شامل جدول «می‌خواهم فلان
چیز را عوض کنم، کجا بروم؟»، دستور گام‌به‌گام افزودن صفحهٔ تازه، و
دام‌های شناخته‌شدهٔ آزمون‌نویسی است. **با هر تغییر ساختاری، آن را هم
به‌روز کن.**

---

## ۳. مدل داده

### ۳.۱ سی مجموعه (collections)

```
هسته آموزشی:
  schools, users, classes, subjects, enrollments, schedule

عملیات روزانه:
  attendance, grades, discipline, leaves, announcements, notifications, messages, calendar

مالی:
  tuition_plans, tuitions, installments, transactions

امتحانات:
  exam_terms, exams, exam_duties

روابط و تأیید:
  parent_links, parent_verifications, corrections, teacher_schools

تقسیمات کشوری:
  provinces, counties, districts, offices

کسب‌وکار:
  parent_subscriptions, subscription_payments, app_settings
```

### ۳.۲ فیلدهای کلیدی

**`users`** — همه‌ی نقش‌ها در یک جدول
```js
{ id, school_id, office_id, role, full_name, username, password,
  national_id, father_nid, mother_nid, phone, active,
  subject_id, subject, degree, title, job, created_at }
```

**`schools`**
```js
{ id, name, code, level, gender, capacity, active,
  province_id, county_id, district_id, area_kind,   // 'district' | 'village'
  city, address, phone, landline, created_at }
```

**`subjects`** — «درس» و «کتاب» یکی هستند
```js
{ id, school_id, name, code, weekly_hours,
  grade,   // 'اول'…'دوازدهم' یا '' برای عمومی
  field }  // فقط برای پایه‌های دهم تا دوازدهم
```

**`attendance`**
```js
{ id, school_id, class_id, student_id, date /* ISO */,
  status /* present|absent|late|excused */, note }
```

### ۳.۳ لایه‌ی ذخیره‌سازی: Event Log

داده به‌جای ذخیره‌ی مستقیم، به‌صورت **گزارش تغییرات** نگه داشته می‌شود:

```
داده پایه (تولید با SEED ثابت)  +  لاگ تغییرات (localStorage)  =  وضعیت فعلی
```

```js
applyOp({t:'ins'|'upd'|'del', c:'collection', id, data})
insert(coll, obj)      // id خودکار + ثبت در لاگ + ورود به صف sync
update(coll, id, patch)
remove(coll, id)
resetAll()             // پاک کردن لاگ + صف → بازگشت به حالت اولیه
```

**پرچم `SYNC_MUTED`** — حیاتی: هنگام تولید داده نمونه و بازپخش لاگ فعال می‌شود
تا ۳۲ هزار رکورد دمو وارد صف ارسال به سرور نشوند.

**کلیدهای localStorage:**
```
sms_log_v1       لاگ تغییرات
sms_session_v1   نام کاربری جلسه فعال
sms_boss_v1      حساب واقعی هنگام جعل هویت (impersonation)
sms_persona_v1   پنل فعال در حالت چندنقشی
sms_syncq_v1     صف ارسال به سرور
sms_syncmeta_v1  زمان آخرین همگام‌سازی
```

---

## ۴. نقش‌ها، پنل‌ها و روابط

### ۴.۱ شش نقش

| نقش | کلید | دامنه دید |
|---|---|---|
| مدیر کل سامانه | `superadmin` | همه‌چیز، همه مدارس |
| اداره آموزش‌وپرورش | `edu_office` | مدارس محدوده‌ی خود (استان/شهرستان/منطقه) — **فقط آمار تجمیعی** |
| مدیر مدرسه | `manager` | فقط مدرسه‌ی خودش (شامل معاون) |
| دبیر | `teacher` | فقط کلاس‌هایی که تدریس می‌کند + مدارس مشترک |
| دانش‌آموز | `student` | فقط پرونده‌ی خودش |
| اولیا | `parent` | فقط فرزندان تأییدشده — **پشت paywall** |

### ۴.۲ دامنه دید — دو لایه

```js
// لایه ۱: منو  →  NAV[role] تعیین می‌کند چه چیزی دیده شود
// لایه ۲: داده →  توابع فیلترکننده

visibleClasses()   // superadmin: همه | manager: مدرسه خود
                   // teacher: teacherClasses(id) | student: کلاس خودش
visibleSubjects()  // superadmin: همه | بقیه: مدرسه خود
myAnnouncements()  // بر اساس school_id + audience
officeScopeSchools(o, filters)  // اداره: بر اساس province/county/district
```

### ۴.۳ ارتباط بین پنل‌ها — نمودار جریان

```
┌─────────────┐  تعریف مدرسه + حساب مدیر
│ superadmin  │──────────────────────────────┐
│             │  تعریف اداره + کارشناس       │
│             │──────────┐                   │
│             │          │                   │
│  ورود به پنل مدرسه (impersonation)         │
│  S.boss نگه داشته می‌شود → بازگشت          │
└──────┬──────┘          │                   │
       │                 ▼                   ▼
       │          ┌─────────────┐     ┌─────────────┐
       └─────────→│ edu_office  │←────│   manager   │
                  │ آمار تجمیعی │     │ مدیر مدرسه  │
                  │ (بدون داده  │     └──┬───┬───┬──┘
                  │  فردی)      │        │   │   │
                  └─────────────┘        │   │   │
                     تعریف کلاس/درس/کاربر│   │   │ ابلاغ مراقبت امتحان
              ┌─────────────────────────┘   │   └──────────┐
              ▼                             │              ▼
      ┌──────────────┐   ثبت حضور/نمره/انضباط        ┌──────────┐
      │   teacher    │──────────────┐        │       │ exam     │
      │              │              │        │       │ duties   │
      │ دبیر مشترک   │              ▼        ▼       └──────────┘
      │ چند مدرسه    │        ┌──────────────────┐
      └──────────────┘        │     student      │
                              │  کارنامه، شهریه  │
                              └────────┬─────────┘
                                       │ parent_links
                                       │ (اتصال با کد ملی)
                                       ▼
                              ┌──────────────────┐
                              │      parent      │
                              │ 🔒 پشت paywall   │
                              │ تأیید فرزندان    │
                              │ درخواست اصلاح ───┼──→ corrections → manager
                              └──────────────────┘
```

### ۴.۴ سازوکارهای ویژه‌ی ارتباطی

**۱. جعل هویت (Impersonation)**
سوپرادمین می‌تواند وارد پنل مدیر یک مدرسه شود:
```js
S.boss = S.user;  S.user = mgr;   // نوار هشدار بالای صفحه نمایش داده می‌شود
// بازگشت با 'stop-imp'
```

**۲. چندنقشی (Multi-role)**
یک شخص می‌تواند همزمان دبیر و ولی باشد:
```js
panelsOf(u)        // فهرست پنل‌های در دسترس
activePersona()    // S.persona || S.user.role
panelPicker()      // صفحه انتخاب پنل هنگام ورود
```
اتصال با **کد ملی** انجام می‌شود: اگر `student.father_nid === user.national_id`
آن شخص خودکار پنل اولیا هم می‌گیرد (`linkAsParent`).

**۳. تأیید فرزندان (Parent Gate)**
پس از اتصال خودکار، ولی باید تأیید کند:
```
pending → ولی «بله فرزند من است» → confirmed
        → ولی «فرزند من نیست»   → rejected + ثبت در corrections → مدیر مدرسه
```

**۴. دبیر مشترک بین مدارس**
`teacher_schools` — دبیر می‌تواند در چند مدرسه تدریس کند.
سیستم **تداخل زمانی** را تشخیص می‌دهد (`overlapP`) و مانع ابلاغ متناقض می‌شود.

**۵. Paywall اولیا**
```js
parentLocked()  // فقط اگر persona==='parent' && paywall فعال && اشتراک منقضی
// در renderRoute:
if(parentLocked() && !['subscription','notifications','announcements'].includes(S.route))
  return viewLocked();
```
اولین ورود → مهلت تست رایگان خودکار (پیش‌فرض ۲۱ روز، قابل تنظیم در پنل سوپرادمین).

---

## ۵. برنامه درسی (ماژول `26-curriculum.js`)

قاعده‌ی کلیدی که باید رعایت شود:

| مقطع | پایه‌ها | تفکیک کتاب |
|---|---|---|
| ابتدایی | اول تا ششم | فقط بر اساس **پایه** — مشترک بین همه |
| متوسطه اول | هفتم تا نهم | فقط بر اساس **پایه** — مشترک بین همه |
| **متوسطه دوم** | دهم تا دوازدهم | **بر اساس شاخه و رشته** |

**شاخه‌ها و رشته‌ها (۱۴ رشته):**
```
نظری          : ریاضی فیزیک، علوم تجربی، ادبیات و علوم انسانی، علوم و معارف اسلامی
فنی و حرفه‌ای : الکتروتکنیک، مکانیک خودرو، کامپیوتر، حسابداری، ساختمان، گرافیک
کاردانش       : تعمیر لوازم خانگی، طراحی دوخت، تأسیسات، صنایع غذایی
```

**فرمول متوسطه دوم:** `کتاب‌ها = عمومی مشترک پایه + تخصصی رشته`

**API:**
```js
LEVELS                       // ['ابتدایی','متوسطه اول','متوسطه دوم']
GRADES_OF_LEVEL[level]       // آرایه پایه‌ها
BRANCHES                     // {شاخه: [رشته‌ها]}
levelOfGrade(grade)          // 'دهم' → 'متوسطه دوم'
needsField(level)            // آیا رشته لازم است؟
branchOfField(field)         // 'ریاضی فیزیک' → 'نظری'
booksFor(grade, field)       // → [[نام, ساعت], …]
fieldsOfBranch(branch)
```

در UI دکمه‌ی «📥 افزودن کتاب‌های استاندارد» با انتخاب آبشاری و پیش‌نمایش زنده وجود دارد.

---

## ۵.۵ چرخهٔ تحصیلی (ماژول `31-student-lifecycle.js`) ⭐

پنج تب در روت `lifecycle` (فقط مدیر و سوپرادمین):

| تب | کار |
|---|---|
| `promotion` | نمای پایان سال هر کلاس + دکمهٔ اجرای ارتقا |
| `transfers` | درخواست‌های انتقال ورودی/ارسالی + سابقه |
| `conflicts` | کد ملی‌هایی که در مدرسهٔ دیگری فعال‌اند |
| `incomplete` | پرونده‌های ناقص با درجهٔ اهمیت |
| `archive` | بایگانی فارغ‌التحصیلان به تفکیک سال |

### قواعد نظام آموزشی ایران
```
ابتدایی ۱–۶ · متوسطه اول ۷–۹ · متوسطه دوم ۱۰–۱۲
پایه ۶ و ۹  = پایان مقطع  ⇒ status='awaiting_transfer'
پایه ۱۲     = فارغ‌التحصیلی ⇒ بایگانی + active=0
سایر پایه‌ها = grade_level+1
```

### API
```js
yearCode()                    // '1404-1405' (سال از مهر شروع می‌شود)
yearTitle()                   // '۱۴۰۴-۱۴۰۵'
isTerminal(g)                 // ۶/۹/۱۲ ⇒ true
gradeFromName('نهم ۲')        // 9 — استنتاج پایه از نام کلاس
levelOfLC(9)                  // 'متوسطه اول'
activeStudentsOfClass(cid)    // از ایندکس، فقط دانش‌آموزان فعال
graduateStudent(s,sid,cls)    // بایگانی + status + حذف ثبت‌نام + اعلان به ولی
moveStudent(st,to,classId,r)  // انتقال با کل پرونده ⇒ {grades,attendance,discipline}
recordConflict(sid,nid,...)   // ثبت تعارض بدون تکرار
incompleteUsers(sid)          // فهرست مرتب‌شده بر اساس شدت
```

### ۴ مجموعهٔ داده
```
student_transfers   سابقهٔ انتقال‌های انجام‌شده
transfer_requests   درخواست‌ها (pending/approved/rejected)
student_archive     کارنامهٔ فارغ‌التحصیلان (معدل، درصد حضور، انضباط)
nid_conflicts       تعارض کد ملی (open/resolved/dismissed)
```

### ⚠️ نکتهٔ مهم انتقال
`moveStudent` علاوه بر `users.school_id`، فیلد `school_id` رکوردهای
`grades`/`attendance`/`discipline` را هم به‌روز می‌کند. بدون این کار پرونده در
مدرسهٔ جدید دیده نمی‌شد (چون کوئری‌ها بر اساس `school_id` مرزبندی می‌کنند).

### ⚠️ عملیات انبوه حتماً در `batchWrites` بپیچید
```js
batchWrites(()=>{ /* هزاران insert/update */ });
```
`promote-run` و `tr-ok` این کار را می‌کنند. بدون آن ارتقای یک مدرسهٔ ۵۰۰ نفره
**۳٬۲۰۰ms** طول می‌کشید؛ با آن **۱۸۷ms** (۱۷ برابر سریع‌تر).

---

## ۵.۶ افت تحصیلی، جلسات اولیا، رشد مدرسه (`32-atrisk-meetings-growth.js`) ⭐

سه صفحهٔ مستقل حول «تعامل مدرسه با خانواده».

### `atrisk` — افت تحصیلی (مدیر، سوپرادمین)
امتیاز ریسک **۰ تا ۱۰۰** از پنج نشانه؛ فقط ریسک ≥۲۰ برگردانده و نزولی مرتب می‌شود.

| نشانه | امتیاز |
|---|---|
| میانگین اخیر < ۱۰ | ۴۰ |
| میانگین اخیر < ۱۲ | ۲۵ |
| افت ≥ ۲ نمره نسبت به میانگین کل | ۲۰ |
| غیبت ≥ ۱۵٪ / ≥ ۸٪ | ۲۵ / ۱۲ |
| تأخیر ≥ ۵ | ۸ |
| انضباطی منفی ≤ −۵ | ۱۵ |
| ≥ ۳ نمره زیر ۱۰ | ۱۵ |

سطح: `high` ≥۶۰ · `medium` ≥۳۵ · `low` بقیه. بازهٔ بررسی: ۳۰/۶۰/۹۰/۱۸۰ روز.
اکشن `risk-notify` به دانش‌آموز **و** همهٔ اولیای متصل اعلان `low_grade` می‌فرستد.

> ⚡ **محاسبهٔ تنبل دلایل:** `reasons` یک **getter** است و رشته‌های فارسی فقط
> برای ردیف‌های نمایش‌داده‌شده ساخته می‌شوند. بدون این کار در مدرسهٔ ۱٬۵۲۰ نفره
> ۷۱ms طول می‌کشید؛ حالا ۱۹ms. اگر `reasons` را از حالت getter خارج کنی این
> بهینه‌سازی از بین می‌رود.

### `meetings` — جلسات اولیا (مدیر، دبیر، ولی)
نمای هر نقش متفاوت است: کارکنان نوبت می‌سازند، ولی رزرو می‌کند.

| اکشن | نقش |
|---|---|
| `mtg-new` / `mtg-save` | مدیر، دبیر — ساخت چند نوبت پشت‌سرهم |
| `mtg-book` / `mtg-book-ok` | فقط ولی |
| `mtg-cancel` | کارکنان یا ولیِ صاحب نوبت |
| `mtg-del` | فقط کارکنان (نوبت آزاد) |

**دو قاعدهٔ کسب‌وکار:** نوبت تکراری (همان دبیر/تاریخ/ساعت) ساخته نمی‌شود؛
هر ولی در هر روز فقط **یک** نوبت می‌گیرد.
`SLOT_ID` (بالای `19-actions.js`) شناسهٔ نوبت را بین مودال و ثبت نگه می‌دارد.
دبیر فقط نوبت‌های خودش را می‌بیند.

### `growth` — رشد مدرسه (مدیر، سوپرادمین)
کد معرف (`refCodeOf`)، نرخ تبدیل، سهم مدرسه از پرداخت اولیا
(`school_share_percent`، پیش‌فرض ۲۰٪)، و `invite-parents` برای دعوت گروهی
(حداکثر ۳۰۰ نفر، داخل `batchWrites`).

### API
```js
schoolStudents(sid)     // دانش‌آموزان فعال مدرسه
atRiskList(days)        // فهرست مرتب‌شده؛ reasons تنبل است
riskReasons(r)          // دلایل خوانا برای یک ردیف
parentsOfSchool(sid)    // اولیای یکتا (بدون تکرار)
refCodeOf(school)       // کد معرف؛ در نبود، از school.code ساخته می‌شود
```
مجموعهٔ داده: `meeting_slots` (status: `open`/`booked`).

---

## ۵.۷ فرم‌های رسمی و پنل پیامک (`33-forms-sms.js`) ⭐

دو تب در روت `formssms`؛ فقط مدیر و سوپرادمین.

### تب نخست: فرم‌های رسمی چاپی
سه سند آمادهٔ چاپ روی کاغذ A4 با شیوه‌نامهٔ چاپ جداگانه:

| سند | توضیح |
|---|---|
| لیست نمرات کلاس | ستون‌های «مستمر»، «پایانی» و «امضا» خالی می‌مانند تا در کلاس دستی پر شوند |
| صورت‌جلسهٔ امتحان | مشخصات جلسه، فهرست مراقبان با سمت، و فهرست حاضران با جای امضا |
| دفتر آمار مدرسه | آمار کلاس‌ها به تفکیک جنسیت، ظرفیت، میانگین و غیبت — حالت افقی |

سازندهٔ بدنهٔ هر سند از عمل چاپ **جدا** نوشته شده تا بدون باز کردن پنجره
قابل آزمون باشد: `formGradeSheet` و `formExamMinutes` و `formStatistics`
یک شیء با کلید `body` برمی‌گردانند و `printableDoc` فقط آن را چاپ می‌کند.

> ⚡ دفتر آمار با **یک** پیمایش روی نمرات و **یک** پیمایش روی حضور،
> داده را گروه‌بندی می‌کند. در نسخهٔ مرجع به‌ازای هر کلاس کل جدول پیمایش
> می‌شد که رفتار درجه‌دوم داشت.

### تب دوم: پنل پیامک
کیف پول هر مدرسه جداست و با `smsWalletOf` ساخته و خوانده می‌شود.
هر **۷۰ نویسهٔ فارسی** یک پیامک حساب می‌شود (`smsParts`).

سه قاعدهٔ کسب‌وکار که همگی آزموده شده‌اند:
۱. اگر اعتبار کمتر از نیاز باشد، ارسال انجام **نمی‌شود** و اعتبار دست‌نخورده می‌ماند.
۲. متن کوتاه‌تر از چهار نویسه رد می‌شود.
۳. گیرندگان یکتا هستند و فقط شماره‌های با الگوی درست تلفن همراه پذیرفته می‌شوند.

مخاطبان: همهٔ اولیا · همهٔ دبیران · همهٔ دانش‌آموزان · اولیای یک کلاس.
مجموعه‌های داده: `sms_wallet` و `sms_log`.

---

## ۵.۸ ویزارد ورود اکسل (`34-excel-import.js`) ⭐

چهار مرحله: انتخاب فایل ← نگاشت ستون‌ها ← بررسی خطاها ← ثبت نهایی.
فقط مدیر و سوپرادمین. وضعیت ویزارد در `S.imp` نگه داشته می‌شود.

### چرا خوانندهٔ اکسل از صفر نوشته شد؟
پروژه هیچ وابستگی بیرونی ندارد، پس آوردن کتابخانه ممکن نبود. فایل `xlsx`
در واقع یک بایگانی فشرده است؛ ساختار آن مستقیم خوانده می‌شود و محتوای
فشرده با `DecompressionStream` مرورگر باز می‌گردد. اگر مرورگر این توانایی
را نداشته باشد، پیام روشن داده می‌شود و ورود از `csv` همچنان کار می‌کند
(تابع `canReadXlsx`).

### اصل طراحی
مدرسه نباید فایلش را با قالب ما تطبیق دهد؛ سامانه باید فایل موجود مدرسه را
با هر ترتیب ستونی بفهمد. برای همین:

- **ردیف تیتر خودکار پیدا می‌شود** — پرمحتواترین ردیف میان پنج ردیف نخست،
  پس فایل‌هایی که سطر توضیح یا لوگو دارند هم درست خوانده می‌شوند.
- **نگاشت ستون‌ها هوشمند است** — هر عنوان با فهرست مترادف‌ها سنجیده می‌شود
  و امتیاز می‌گیرد؛ امتیاز ۷۰ به بالا پذیرفته می‌شود.
- **نویسه‌های عربی یکسان‌سازی می‌شوند** (`normHdr`)، پس «كدملي» با «کد ملی»
  یکی شمرده می‌شود.
- **ارقام فارسی و عربی** به لاتین تبدیل می‌شوند (`toLatinDigits`).

### اعتبارسنجی پیش از ثبت
`validateImport` هیچ داده‌ای نمی‌نویسد جز ثبت تعارض کد ملی. برای هر ردیف
خطاها و هشدارها جدا گزارش می‌شوند:

| وضعیت | نمونه |
|---|---|
| خطا (ردیف ثبت نمی‌شود) | نام کوتاه‌تر از سه نویسه · کد ملی نامعتبر · تکرار در همان فایل · کد ملی متعلق به مدرسهٔ دیگر |
| هشدار (ردیف ثبت می‌شود) | بدون کد ملی · بدون کلاس · شمارهٔ نامعتبر · تاریخ تولد خوانده نشد |
| به‌روزرسانی | کد ملی از قبل در همین مدرسه هست ⇒ رکورد موجود ویرایش می‌شود، نه ساخت تکراری |

اگر کد ملی متعلق به دانش‌آموزی در مدرسهٔ دیگر باشد، ردیف رد شده و به‌طور
خودکار در `nid_conflicts` ثبت می‌شود تا مدیر از تب «انتقالی‌ها» درخواست
انتقال بدهد. این پل میان ویزارد ورود و چرخهٔ تحصیلی است.

### ثبت نهایی
`commitImport` همهٔ نوشتن‌ها را در یک `batchWrites` انجام می‌دهد و علاوه بر
دانش‌آموز، **کلاس‌های تازه** و **حساب ولی** را هم می‌سازد و پیوند ولی به
دانش‌آموز را برقرار می‌کند. نام کاربری یکتا تضمین می‌شود.

سنجش: ثبت **۱۰۰۰ دانش‌آموز در ۲۹۷ میلی‌ثانیه**.

---

## ۵.۹ پنل سوپرادمین (`35-superadmin.js`) ⭐

سه ابزار مدیریت کل سامانه. **فقط سوپرادمین** به دو روت `finance` و `health`
دسترسی دارد؛ بازنشانی رمز برای مدیر هم باز است.

### مالی سراسری — روت `finance`
تابع `financeSummary()` با **یک پیمایش روی هر جدول** این‌ها را می‌سازد:

| بخش | محتوا |
|---|---|
| درآمد اشتراک | جمع پرداخت‌های ثبت‌شده |
| شهریه | صورت‌حساب، دریافتی، معوق، شمار اقساط سررسیدگذشته |
| اشتراک‌ها | فعال · آزمایشی · منقضی · بدون اشتراک + انقضای ۳۰ روز آینده |
| روند درآمد | شش ماه اخیر از تراکنش‌ها |
| رتبهٔ مدارس | نرخ تبدیل و شهریهٔ دریافتی هر مدرسه |

> ⚡ گروه‌بندی یک‌باره انجام می‌شود، نه پیمایش جدول به‌ازای هر مدرسه.
> اگر این را به حلقهٔ تودرتو تغییر دهی، در مقیاس ملی درجه‌دوم می‌شود.

### بازنشانی رمز — اکشن `pass-reset`
`resetPassword(userId)` رمز موقت هشت‌نویسه‌ای می‌سازد، پرچم
`must_change_password` را می‌گذارد و اعلان حاوی رمز را برای خود کاربر می‌فرستد.

**مرز امنیتی در `canResetPassword` — این را سست نکن:**

| کنشگر | مجاز است رمز چه کسی را عوض کند |
|---|---|
| سوپرادمین | همه، **جز سوپرادمین دیگر** (خودش را می‌تواند) |
| مدیر | فقط دانش‌آموز، دبیر و ولیِ **مدرسهٔ خودش** |
| بقیه | هیچ‌کس |

بدون این محدودیت، مدیر می‌توانست رمز سوپرادمین را عوض کند و کنترل کل
سامانه را بگیرد.

### سلامت سامانه — روت `health`
`healthReport()` فقط ابزارهای موجود را می‌خواند و محاسبهٔ تازه‌ای ندارد:
مصرف حافظهٔ مرورگر · نرخ اصابت ایندکس · وضعیت شبکه و صف همگام‌سازی ·
محدودهٔ دادهٔ نقش‌ها · بزرگ‌ترین جدول‌ها.

سه ابزار نگهداری: دریافت نسخهٔ پشتیبان · بازسازی ایندکس‌ها ·
آماده‌سازی عملیات ناموفق برای ارسال دوباره.

---

## ۵.۱۰ سابقهٔ تغییرات و آمار فعالیت (`36-audit-activity.js`) ⭐

دو روت ویژهٔ سوپرادمین: `audit` و `activity`.

### سابقهٔ تغییرات — روت `audit`
**تغییر مهم در `03-persistence.js`:** دفترچهٔ عملیات پیش از این فقط
`{t, c, data}` داشت و هیچ ردی از «چه کسی» نبود. حالا هنگام ثبت، دو کلید
اضافه می‌شود:

```js
op.by = S.user.id;              // چه کسی
op.at = new Date().toISOString(); // کِی
```

> ⚠️ فقط شناسه و مُهر زمان ذخیره می‌شود، **نه کل رکورد کاربر**. دفترچه در
> حافظهٔ مرورگر می‌ماند و هر بایت در مقیاس ملی ضرب می‌شود.
> سنجیده شد: هر عملیات ۱۸۰ بایت ⇒ ظرفیت ۵ مگابایتی ≈ ۲۹٬۱۲۷ عملیات
> ≈ **۹۷۱ روز** کار برون‌خط. اگر رکورد کامل ذخیره می‌شد این عدد
> به چند هفته می‌رسید.

`COLL_FA` نام فارسی ۳۹ مجموعه را دارد و `SENSITIVE` هفت مجموعهٔ حساس
(کاربر، مدرسه، نمره، تراکنش، اشتراک، کیف پیامک، بایگانی) را برجسته می‌کند.
چهار فیلتر: جستجو · نوع داده · نوع عمل · فقط حساس.

### بازدید و بار سامانه — روت `activity`
پاسخ به پرسش سوپرادمین: «چند نفر بازدید کرده‌اند، چند نفر برخط‌اند، و
چقدر فشار روی برنامه و سرور است؟»

| بخش | چگونه سنجیده می‌شود |
|---|---|
| کاربر برخط | فعالیت در **پنج دقیقهٔ اخیر** (`onlineUsers`) |
| بازدید امروز و روند | شمارش روزانه در `sms_visits_v1`، فقط **۹۰ روز** نگه داشته می‌شود |
| بار برنامه | زمان واقعی رندر با صدک ۵۰، ۹۰ و ۹۹ + کندترین صفحه |
| بار سرور | صف ارسال، عملیات یک ساعت اخیر، و برآورد مقیاس ملی |

**سنجش زمان رندر:** `renderRoute` به دو تابع شکسته شد؛ پوستهٔ بیرونی زمان
می‌گیرد و `_renderRouteInner` کار اصلی را می‌کند. سربارش سنجیده شد:
**۰٫۶۳ میلی‌ثانیه** — ناچیز. سقف ۲۰۰ نمونه نگه داشته می‌شود تا حافظه رشد نکند.

**برآورد بار سرور** بر پایهٔ سرانهٔ سنجیده‌شدهٔ ۰٫۰۰۰۹۵ درخواست بر ثانیه:
۵ میلیون کاربر همزمان ⇒ ۴٬۷۵۰ درخواست بر ثانیه ⇒ ۱۰ نمونهٔ سرویس و
۱۶ هستهٔ پایگاه داده.

---

## ۵.۱۱ سازگاری با فایل واقعی مدرسه ⭐

کاربر تصویر یک فایل اکسل واقعی مدرسه را فرستاد. مقایسه نشان داد ویزارد
ورود فقط **۶ ستون از ۳۵** را می‌شناخت. پس از بازنویسی: **۳۴ از ۳۵**
(تنها ستون «ردیف» عمداً نادیده گرفته می‌شود چون داده نیست).

### ستون‌های واقعی فایل مدرسه
```
ردیف · نام · نام خانوادگی · رشته · تاریخ تولد · شماره ملی
سری شناسنامه · سریال شناسنامه
نام پدر · کد ملی پدر · تحصیلات پدر · وضعیت حیات پدر · شغل پدر · شماره پدر
نام مادر · کد ملی مادر · تحصیلات مادر · وضعیت حیات مادر · شغل مادر · شماره مادر
سرپرست دانش آموز · تعداد خواهر · تعداد برادر
معدل سال گذشته · تعداد درس افتاده · استعداد یابی
تحت پوشش · نوع ارگان · درصد
موبایل دانش آموز · شماره ثابت · محل سکونت · نام روستا · وضعیت اقامت · آدرس
```

### چهار ناهماهنگی که یافت و رفع شد

| # | مسئله | راه‌حل |
|---|---|---|
| ۱ | فایل واقعی «نام» و «نام خانوادگی» را **جدا** دارد، سامانه فقط نام کامل داشت | فیلدهای `first_name` و `last_name` افزوده شد؛ در نبود نام کامل از این دو ساخته می‌شود و هر دو هم جدا ذخیره می‌شوند |
| ۲ | معدل به شکل **«۱۲/۸۸»** نوشته می‌شود — اسلش نقش ممیز دارد | تابع `toNumFa` که اسلش و ممیز فارسی را ممیز اعشاری می‌فهمد |
| ۳ | شمارهٔ دانش‌آموز و پدر روی هم بازنویسی می‌شدند | سه شمارهٔ جدا: `phone` · `father_phone` · `mother_phone` |
| ۴ | ستون «ردیف» به‌اشتباه به یک فیلد نگاشت می‌شد | فهرست `IMP_IGNORE` |

### تفسیر خودکار مقادیر فارسی
```
تحت پوشش:   «بلی/بله/دارد» → ۱   ·   بقیه → ۰
وضعیت حیات: هر عبارتی شامل «فوت/مرحوم/متوفی» → 'فوت'  ·  بقیه → 'در قید حیات'
اعداد:      ارقام فارسی و عربی خودکار به لاتین تبدیل می‌شوند
معدل:       بیرون از بازهٔ ۰ تا ۲۰ هشدار می‌گیرد
```

### تب «شناسنامه» در پروندهٔ دانش‌آموز
این ۳۸ فیلد باید **دیده** هم بشوند، وگرنه بی‌فایده‌اند. تب تازه‌ای در
`17-student-record.js` افزوده شد با هفت بخش: هویت · تحصیلی · پدر · مادر ·
خانواده · وضعیت حمایتی · تماس و نشانی.

**نکتهٔ طراحی:** `infoRow` و `infoBlock` فیلدهای خالی را حذف می‌کنند، پس
کارت هرگز شلوغ یا پر از «نامشخص» نمی‌شود.

### 🔴 اشکالی که ضمن این کار یافت شد
مسیریاب `viewRecord(S.user.id)` را صدا می‌زد، یعنی **همیشه پروندهٔ خود
کاربر**. مدیر اصلاً نمی‌توانست پروندهٔ دانش‌آموزی را باز کند.

تابع `recordTargetId()` نوشته شد که `S.child` را با مرز دسترسی می‌سنجد:

| نقش | چه پرونده‌ای |
|---|---|
| دانش‌آموز | فقط خودش — `S.child` نادیده گرفته می‌شود |
| ولی | فقط فرزندان متصل به خودش |
| مدیر و دبیر | فقط دانش‌آموزان مدرسهٔ خودشان |
| سوپرادمین | همه |

---

## ۵.۱۲ ابزارهای تکمیلی سوپرادمین (`37-admin-tools.js`) ⭐

### وضعیت و فعالیت مدارس
`schoolsOverview()` برای هر مدرسه: شمار دانش‌آموز و دبیر و ولی، اشتراک فعال،
نرخ تبدیل، و **آخرین فعالیت**. ستون تازه‌ای در فهرست مدارس نشان می‌دهد
مدرسه چند روز است بی‌استفاده مانده:

| وضعیت | معنی |
|---|---|
| سبز | فعالیت در هفتهٔ اخیر |
| نارنجی | بین هفت تا سی روز |
| قرمز | بیش از سی روز |
| خاکستری | هرگز فعالیتی نداشته |

آخرین فعالیت از دفترچهٔ عملیات استخراج می‌شود (کلید `at` که در دور دهم
افزوده شد) — **یک پیمایش**، نه پیمایش دفترچه به‌ازای هر مدرسه.

### خروجی اکسل
چهار صفحه خروجی می‌دهند: مدارس · کاربران · مالی · سابقهٔ تغییرات.
پیش‌فرض هم فهرست کامل دانش‌آموزان با پروندهٔ تکمیلی است.

> ⚠️ **دو جزئیات ریز اما حیاتی:**
> ۱. فایل با پیشوند BOM ساخته می‌شود، وگرنه اکسل فارسی را به‌هم‌ریخته
>    نشان می‌دهد.
> ۲. `csvCell` سلولی را که با `=` یا `+` یا `-` یا `@` شروع شود با
>    نقل‌قول خنثی می‌کند. بدون این، یک نام مخرب می‌تواند در اکسل
>    قربانی به فرمول تبدیل شود (تزریق فرمول).

### اطلاعیهٔ سراسری
`broadcastAnnouncement(title, body, scope)` با دو حالت:

| حالت | رفتار |
|---|---|
| `all` | یک اطلاعیه با شناسهٔ مدرسهٔ خالی ⇒ همه می‌بینند |
| `each` | برای هر مدرسه نسخهٔ جدا ⇒ مدیرش می‌تواند ویرایشش کند |

فقط سوپرادمین. عنوان کمتر از سه نویسه و متن کمتر از پنج نویسه رد می‌شود.

---

## ۵.۱۳ پلان فروش و پشتیبان‌گیری (`38-plans-backup.js`) ⭐

### مدیریت پلان فروش — روت `plans`
سه پلان (ماهانه، فصلی، سالانه) با آمار فروش، درآمد و **قیمت معادل ماهانه**
تا صرفهٔ هر پلان روشن باشد. سوپرادمین می‌تواند از رابط تغییرشان دهد.

`validatePlanSettings` جلوی مقدار بی‌معنا را می‌گیرد:

| قاعده | چرا |
|---|---|
| قیمت منفی یا بیش از ۱۰۰ میلیون رد می‌شود | خطای تایپی |
| دورهٔ آزمایشی بین ۰ تا ۳۶۵ روز | مقدار غیرمنطقی |
| سهم مدرسه بین ۰ تا ۱۰۰ درصد | درصد بی‌معنا |
| **سالانه نباید از ماهانه گران‌تر باشد** | مشتری انگیزه‌ای برای خریدش ندارد |

قاعدهٔ آخر منطق کسب‌وکار است نه فنی؛ اگر برداشته شود، پلان بلندمدت
بی‌معنا می‌شود.

### پشتیبان‌گیری و بازیابی
> ⚠️ پیش از این فقط دکمهٔ «دریافت پشتیبان» بود و **هیچ راهی برای
> برگرداندنش نبود** — یعنی عملاً بی‌فایده. حالا هر دو سو کامل است.

**چرا فقط دفترچهٔ عملیات ذخیره می‌شود، نه کل پایگاه داده؟**
دادهٔ پایه با مولد قطعی (`SEED`) بازساخته می‌شود؛ فقط تغییرات کاربر
یکتا هستند. این انتخاب حجم پشتیبان را ده‌ها برابر کوچک‌تر می‌کند.

**روند بازیابی در `restoreBackup`:**
```
۱. اعتبارسنجی کامل فایل (validateBackup)
۲. بازسازی دادهٔ پایه با همان مولد قطعی
۳. جایگزینی دفترچه با عملیات فایل
۴. اجرای دوبارهٔ همهٔ عملیات روی دادهٔ پایه
۵. بازسازی ایندکس‌ها و ذخیره
```

`validateBackup` شش بررسی دارد: قالب فایل · آرایه بودن عملیات · شمارهٔ
نسخه · نوع هر عمل · وجود مجموعهٔ مقصد در این نسخه. **اگر فایل خراب باشد
هیچ تغییری اعمال نمی‌شود** — با آزمون تأیید شد که داده دست‌نخورده می‌ماند.

---

## ۵.۱۴ قاعدهٔ اشتراک: یک پرداخت برای هر دانش‌آموز ⭐⭐

**خواستهٔ صریح کاربر (نکتهٔ بازاریابی محصول):**
> «اشتراک یک‌بار و برای یک سال تحصیلی است. کافی است پدر **یا** مادر
> یکی‌شان بپردازد؛ هر دو دسترسی پیدا می‌کنند. لازم نیست برای یک
> دانش‌آموز هم پدر و هم مادر جداگانه پرداخت کنند.»

### اصل طراحی
**اشتراک به دانش‌آموز تعلق دارد، نه به ولی.** پس معیار قفل بودن پنل،
اشتراکِ خودِ ولی نیست؛ این است که «آیا برای دست‌کم یکی از فرزندان او
کسی اشتراک فعال دارد؟»

```js
studentSubscription(studentId)   // اشتراک فعال یک دانش‌آموز از میان همهٔ اولیایش
effectiveParentAccess(parentId)  // دسترسی مؤثر: خودش یا از راه هم‌ولی
parentLocked()                   // حالا از effectiveParentAccess استفاده می‌کند
```

خروجی `effectiveParentAccess`:
| کلید | معنی |
|---|---|
| `active` | آیا دسترسی دارد |
| `own` | آیا خودش پرداخت کرده |
| `via` | اگر از راه هم‌ولی است: پرداخت‌کننده و نسبتش |
| `student` | کدام فرزند این دسترسی را آورده |

### ⚠️ نکتهٔ ظریفی که هنگام پیاده‌سازی کشف شد
`subOf(id, true)` در نخستین فراخوانی **خودش دورهٔ آزمایشی می‌سازد**.
اگر در `effectiveParentAccess` اول اشتراک خودِ ولی بررسی شود، ولیِ
دومی که هم‌ولی‌اش پرداخت کرده **بی‌جهت یک دورهٔ آزمایشی مصرف می‌کند**
و در گزارش‌ها به‌اشتباه «صاحب اشتراک» شمرده می‌شود.

**پس ترتیب بررسی مهم است:** اول هم‌ولی، بعد خودش. اگر این ترتیب را
جابه‌جا کنی، آمار فروش خراب می‌شود. همچنین `studentSubscription`
عمداً `subOf(id, false)` صدا می‌زند تا بررسی وضعیت عوارض جانبی نداشته باشد.

### نمایش به کاربر
اگر دسترسی از راه هم‌ولی باشد، در صفحهٔ اشتراک کارتی نشان داده می‌شود:
نام پرداخت‌کننده، نام فرزند، و توضیح اینکه نیازی به پرداخت دوباره نیست.
بدون این کارت، ولی نمی‌فهمد چرا پنلش باز است.

---

## ۵.۱۵ 🔴 اشکال ایندکس در دادهٔ نمونه (رفع‌شده)

هنگام کار روی اشتراک مشترک کشف شد: `byId('users', 1130)` مقدار
برنمی‌گرداند درحالی‌که کاربر در `db.users` بود.

**علت:** تابع `add()` در `02-demo-data.js` مستقیم `push` می‌کرد و
ایندکس را باطل نمی‌کرد. ایندکس ۱٬۱۲۹ رکورد داشت و آرایه ۱٬۱۳۴ —
**پنج کاربر نامرئی بودند.**

**رفع:** `add()` حالا `idxInvalidate(c)` صدا می‌زند. پس از رفع، هر ۳۹
مجموعه همگام شدند.

> درس: هر تابعی که آرایهٔ `db` را تغییر می‌دهد باید ایندکس را باطل کند،
> حتی اگر «فقط دادهٔ نمونه» باشد.

---

## ۵.۱۶ چرخهٔ سال تحصیلی و چیدمان کلاس (`39-school-year.js`) ⭐⭐

### 🔴 خلأ بزرگی که این ماژول پر کرد
ارتقای پایه فقط عدد `grade_level` را زیاد می‌کرد و **دانش‌آموز در کلاس
پارسالش می‌ماند**. با آزمون تأیید شد: دانش‌آموزی که به پایهٔ ۱۱ رفت،
هنوز در کلاس «دهم علوم تجربی» بود.

### مراحل چرخه

```
۱. بستن سال (تیر یا مرداد)  →  مدیر تصمیم می‌گیرد
۲. ارتقای پایه              →  خودکار هنگام بستن سال
۳. چیدمان کلاس              →  دستی یا خودکار
۴. ثبت‌نام و تأیید شهریه     →  شهریور
```

### دو منطق متفاوت بر پایهٔ مقطع
| مقطع | پایه | رفتار |
|---|---|---|
| ابتدایی و متوسطه اول | ۱ تا ۹ | **کلاس‌محور** — چیدمان هر سال از نو |
| متوسطه دوم | ۱۰ تا ۱۲ | **رشته‌محور** — رشته تا پایان دوازدهم ثابت می‌ماند |

`isFieldBased(grade)` این تفاوت را تعیین می‌کند. در متوسطه دوم،
`applyPlacement` رشتهٔ دانش‌آموز را با رشتهٔ کلاس هماهنگ نگه می‌دارد.

### چیدمان خودکار: روش مارپیچ
`autoPlacement` دانش‌آموزان را بر پایهٔ `placementScore` (معدل + انضباط
با وزن ۰٫۱) مرتب می‌کند، سپس **مارپیچی** توزیع می‌کند: قوی‌ترین به کلاس
اول، دومی به کلاس دوم… و در بازگشت برعکس.

**چرا مارپیچ؟** اگر ساده تقسیم می‌کردیم، یک کلاس همهٔ قوی‌ها را می‌گرفت.
با این روش میانگین کلاس‌ها به هم نزدیک می‌ماند — با آزمون سنجیده شد:
۸۰ دانش‌آموز در دو کلاس ⇒ میانگین ۱۵٫۲۵ و ۱۵٫۲۶، اختلاف ۰٫۰۱.

ظرفیت هم رعایت می‌شود؛ اگر کلاسی پر شود، به بعدی می‌رود.

### کلاس موازی
وقتی تعداد یک رشته از ظرفیت بیشتر شود:
`createParallelClass(sid, grade, field, suffix)` ⇒ «یازدهم علوم تجربی الف»
و «یازدهم علوم تجربی ب».

### یادآوری پایان سال
`yearEndReminder()` در چرخهٔ بوت صدا زده می‌شود و **فقط** وقتی یادآوری
می‌فرستد که: نقش مدیر باشد · سال بسته نشده باشد · ماه تیر یا مرداد باشد ·
دست‌کم هفت روز از یادآوری قبلی گذشته باشد.

### تأیید شهریه برای ثبت‌نام
`tuitionCleared(studentId)` — ملاک: دست‌کم یک قسط پرداخت‌شده.
دکمهٔ «چیدمان خودکار پرداخت‌کرده‌ها» فقط آن‌ها را می‌چیند.

### API
```js
yearState(sid) · saveYearState(sid, patch)   // وضعیت سال
isYearEndSeason() · isEnrollSeason()          // فصل جاری
needPlacement(sid)                            // ناسازگاری پایه/کلاس
targetClasses(sid, grade, field)              // کلاس‌های مقصد
placementScore(id) · autoPlacement(list, cls) // چیدمان
applyPlacement(pairs)                         // اعمال + هماهنگی پایه و رشته
createParallelClass(...) · tuitionCleared(id)
```
مجموعهٔ داده: `school_years`.

---

## ۵.۱۷ اعتبارسنجی دیداری فرم‌ها (`40-form-validation.js`) ⭐

**خواستهٔ کاربر:** «وقتی یک قسمت الزامی خالی گذاشته می‌شود و خطا می‌دهد،
دور آن قسمت قرمز شود تا کاربر سردرگم نشود.»

### چرا لایهٔ مرکزی؟
۶۲ نقطهٔ اعتبارسنجی در برنامه بود که همه فقط `toast` می‌دادند. به‌جای
ویرایش تک‌تک، یک لایهٔ مرکزی ساخته شد که هم پیام می‌دهد، هم فیلد را
قرمز می‌کند، هم فوکوس می‌گذارد.

```js
if(need('c_name','نام کلاس الزامی است')) return;              // یک فیلد
if(needAll([['m_name','نام'],['m_code','کد']])) return;        // چند فیلد
if(invalid('u_nid', !validNid(V('u_nid')), 'کد ملی معتبر نیست')) return;
```

**رفتار:** کادر قرمز + لرزش کوتاه + پیام زیر فیلد + فوکوس روی اولین
فیلد خطادار. نشانهٔ خطا به‌محض تایپ یا تغییر کاربر **خودکار پاک می‌شود**.

> ⚠️ برای فیلدهای تازه از همین توابع استفاده کن، نه `toast` خام.
> در غیر این صورت کاربر نمی‌فهمد کدام فیلد مشکل دارد.

سبک‌ها در `src/styles/base.css` زیر «نشانهٔ خطای فیلد فرم».

**۱۹ نقطه** به این لایه وصل شد. ضمن کار کشف شد که **کد ملی و موبایل
کاربر هیچ اعتبارسنجی نداشتند** و هر مقداری ذخیره می‌شد؛ رفع شد.

---

## ۵.۱۸ نوع چیدمان کلاس: کلاس‌محور یا رشته‌محور ⭐

**خواستهٔ کاربر:** «گزینهٔ کلاس‌محور یا رشته‌محور را در جای مرتبط بگذار
تا مدیر برای تعریف کلاس‌ها سردرگم نشود و دستش باز باشد.»

در فرم تعریف کلاس (`classModal`) گزینه‌ای در بالای فرم افزوده شد:

| گزینه | رفتار فرم | ذخیره |
|---|---|---|
| کلاس‌محور | بخش رشته **پنهان** می‌شود | `class_mode:'class'` · `field:null` |
| رشته‌محور | رشته **الزامی** است | `class_mode:'field'` · رشته از فهرست |
| خودکار | از روی پایه حدس می‌زند | پایهٔ ۱۰ به بالا ⇒ رشته‌محور |

راهنمای زیر گزینه با تغییر انتخاب **پویا عوض می‌شود** و توضیح می‌دهد
هر حالت چه معنایی دارد. فهرست رشته‌ها از `BRANCHES` و `fieldsOfBranch`
ساخته می‌شود (۳۱ گزینه)، پس مدیر نیازی به تایپ دستی ندارد.

`grade_level` هم خودکار از نام پایه استنتاج و ذخیره می‌شود — این برای
چرخهٔ سال تحصیلی (بخش ۵.۱۶) لازم است.

---

## ۵.۱۹ جای نوار پیمایش ⭐

**خواستهٔ کاربر:** «نوارها (آنچه صفحه و منو را بالا و پایین می‌برد) در
سمت راست باشند — راست منو و راست صفحه، برای همهٔ برنامه و پنل‌ها.»

### مسئله
صفحه راست‌به‌چپ است و مرورگر در این حالت نوار پیمایش را **خودکار سمت
چپ** می‌گذارد. تنظیم مستقیمی برای جابه‌جایی آن وجود ندارد.

### راه‌حل
جهت **ظرف** را چپ‌به‌راست می‌کنیم تا نوارش به راست برود، سپس جهت
**محتوای درون آن** را دوباره راست‌به‌چپ می‌کنیم تا متن فارسی درست بماند.

⚠️ **نکتهٔ کلیدی که در تلاش نخست از قلم افتاد:**
گذاشتن `direction:ltr` روی `html` برای نوار **صفحهٔ اصلی کافی نیست**،
چون `body` هم ارتفاع ۱۰۰٪ دارد و هم راست‌به‌چپ است؛ پس اسکرول روی
خودِ `body` می‌افتد و نوارش به چپ می‌رود.

راه درست: اسکرول عمودی از `body` گرفته و به ناحیهٔ محتوا داده شود:

```css
html{ direction:ltr }
body{ direction:rtl; overflow:hidden }   /* اسکرول از body گرفته شد */

.app { height:100dvh; overflow:hidden }
.main{ direction:ltr; overflow-y:auto; height:100dvh }  /* نوار اینجا، سمت راست */
.main > *{ direction:rtl }                              /* محتوا راست‌به‌چپ */

.sidebar{ direction:ltr }      /* نوار منو → راست */
.sidebar > *{ direction:rtl }
```

`100dvh` به‌جای `100vh` استفاده شد تا در موبایل با نوار آدرس مرورگر
جابه‌جا نشود (`100vh` به‌عنوان پشتیبان پیش از آن می‌آید).

همین الگو برای `.modal`، `.table-wrap` و `.tabs` هم اعمال شد.

> ⚠️ اگر ظرف اسکرول‌دار تازه‌ای ساختی، همین دو خط را برایش بنویس،
> وگرنه نوارش سمت چپ می‌ماند و با بقیهٔ برنامه ناهماهنگ می‌شود.

ظاهر نوار هم یکدست شد: در منوی تیره، نوار تیره؛ در بقیه، خاکستری روشن.

---

## ۵.۲۰ مدیریت حجم پروژه ⭐

**خواستهٔ کاربر:** «حجم همیشه در کمترین حالت ممکن باشد.»

### پاک‌سازی انجام‌شده
| مورد | پیش | پس |
|---|---|---|
| پوشهٔ `.git` | ۱۳ مگابایت | **۹۱۹ کیلوبایت** |
| کل پروژه (بدون `node_modules`) | ~۱۹ مگابایت | **۷.۳ مگابایت** |
| فایل‌های مخزن | ۷۱ | ۷۰ (۱.۸ مگابایت) |

### چه چیزی حذف شد و چرا
۱. **`New folder.rar` (۳.۷ مگابایت)** — بایگانی نسخهٔ مرجع. پیش از حذف
   تأیید شد که **هر ۳۶ تابع `view*` آن پیاده شده‌اند**، پس دیگر کاربردی
   نداشت. پشتیبانش در `/home/user/backup/`.
۲. **`reference/` (۵۳۲ کیلوبایت)** — فایل مرجع استخراج‌شده؛ کارش تمام شد.
۳. **`dist/` (۷۴۸ کیلوبایت)** — خروجی موقت `build.js` که با `index.html`
   یکسان است. در `.gitignore` بود ولی روی دیسک می‌ماند.

### ⚠️ درس مهم: حذف از پوشه کافی نیست
فایل rar از پوشه حذف شده بود ولی **در تاریخچهٔ گیت مانده بود** و
`.git` را ۱۳ مگابایت نگه داشته بود. راه درست:

```bash
git rm --cached "فایل"
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch "فایل"' --prune-empty -- --all
git reflog expire --expire=now --all
git gc --prune=now --aggressive
```
سپس ارسال با `--force` لازم است چون تاریخچه بازنویسی شده.

### ترکیب حجم خروجی (۶۷۰ کیلوبایت)
| بخش | حجم | آیا قابل کم‌کردن است؟ |
|---|---|---|
| جاوااسکریپت | ۵۱۴ KB | کامنت‌ها فقط ۷٪‌اند و طبق اصل دوم باید بمانند |
| فونت‌ها | ۱۷۹ KB | **نه** — برای کار برون‌خط لازم‌اند |

الگوهای نادیده‌گرفتنی افزوده شد: `*.rar` · `*.zip` · `*.7z`

### پاک‌سازی ورک‌اسپیس (نه فقط مخزن)
| پوشه | وضعیت |
|---|---|
| `/home/user/payesh` | ۶.۷ مگابایت — کد و مستندات |
| `/home/user/uploads` | ۲.۵ مگابایت — **فایل‌های کاربر، دست نزن** |
| `/home/user/backup` | ۵۲۴ کیلوبایت — فقط نسخهٔ مرجع html |

حذف‌شده از ورک‌اسپیس: بایگانی rar (محتوایش در html هست) ·
`dist/` (خروجی موقت) · `*.tmp.js` · کلون‌های `/tmp`.

### فهرست بررسی پس از هر کار
```bash
rm -f *.tmp.js          # فایل‌های موقت آزمون
rm -rf dist             # خروجی ساخته‌شده
rm -rf /tmp/gh* /tmp/p2 # کلون‌های موقت
```

---

## ۵.۲۱ وضوح دیداری کادرها (حاشیه‌ها)

کاربر گفت کادرها به‌سختی دیده می‌شوند و حاشیه‌هایشان باید پررنگ‌تر شود.

### ریشهٔ مشکل
رنگ حاشیه `#e4eaf6` بود و پس‌زمینهٔ برنامه `#f2f6fd`. اختلاف روشنایی
این دو زیر ۴٪ بود؛ یعنی خطوط عملاً در پس‌زمینه گم می‌شدند. روی نمایشگر
کم‌کیفیت یا زیر نور روز اصلاً دیده نمی‌شدند.

### تغییرها
| مورد | پیش | پس |
|---|---|---|
| متغیر `--border` | `#e4eaf6` | `#c2cee4` |
| متغیر `--border-strong` (جدید) | — | `#a9b8d4` |
| ضخامت کارت/فیلد/دکمه | ۱ پیکسل | ۱.۵ پیکسل |
| سر جدول | `#f6f9ff` | `#eaf0fb` + حاشیهٔ پررنگ |
| خط زیر ردیف جدول | `#f1f4f9` | `#dde4f0` |
| ردیف هنگام اشاره | `#fafbfe` | `#eef4ff` |
| پشت پنجرهٔ گفتگو | ‎.5 شفافیت | ‎.62 شفافیت |
| نشان‌ها (`.badge`) | بدون حاشیه | `1px solid currentColor` |

### نکته‌های پیاده‌سازی
- `--border-strong` برای حالت اشاره و پنجره‌هاست، نه حالت عادی؛ اگر همه
  جا استفاده شود صفحه شلوغ می‌شود.
- سر کارت پس‌زمینهٔ `--surface-2` گرفت. شعاع گوشه‌اش باید
  `calc(var(--radius) - 1.5px)` باشد وگرنه رنگ از زیر حاشیه بیرون می‌زند.
- نشان‌ها از `currentColor` استفاده می‌کنند تا حاشیهٔ هر نشان خودکار
  هم‌رنگ متنش شود؛ نیازی به تعریف جدا برای هر رنگ نیست.
- ⚠️ ضخامت ۱.۵ پیکسل عمدی است. ۲ پیکسل صفحه را سنگین و کارتونی می‌کند.

### دام
اگر بعداً حاشیه‌ای با رنگ ثابت (مثل `#eee`) در کد نوشته شود، از این
سامانه بیرون می‌ماند و دوباره محو دیده می‌شود. همیشه از
`var(--border)` استفاده شود. بررسی:
```bash
grep -oE "border[a-z-]*: *[0-9.]+px solid #[a-f0-9]{3,6}" src/styles/*.css
```


## ۵.۲۲ شاخه و رشتهٔ مدرسهٔ متوسطه دوم

کاربر گفت هنگام ثبت مدرسهٔ متوسطه دوم باید شاخه (نظری، فنی و حرفه‌ای،
کاردانش) به‌صورت گزینه و دسته‌بندی انتخاب شود.

### وضعیت پیشین
`BRANCHES` در `26-curriculum.js` از قبل تعریف شده بود و در تعریف
**کلاس** و **درس** استفاده می‌شد، اما در فرم **ثبت مدرسه** اصلاً
نبود؛ مدرسه فقط مقطع داشت.

### تصمیم کلیدی: چندانتخابی، نه تک‌انتخابی
هنرستان‌های واقعی معمولاً هم‌زمان چند شاخه دارند (مثلاً هم فنی و
حرفه‌ای هم کاردانش). اگر تک‌انتخابی می‌بود، این مدارس قابل ثبت
نبودند. پس دو میدان به رکورد مدرسه افزوده شد:

| میدان | نوع | معنا |
|---|---|---|
| `branches` | آرایهٔ رشته | شاخه‌های ارائه‌شده |
| `fields` | آرایهٔ رشته | رشته‌های فعال، زیرمجموعهٔ `branches` |

در مقاطع غیر از متوسطه دوم هر دو آرایهٔ خالی می‌مانند.

### ساختار سه‌سطحی
```
مقطع (متوسطه دوم) → شاخه (۳ گزینه) → رشته (۴ تا ۶ گزینه در هر شاخه)
```
`branchPicker(s)` در `18-modals.js` این را می‌سازد: هر شاخه یک
`.branch-card` با تیک، و رشته‌هایش `.field-chip` که فقط با تیک‌خوردن
شاخه باز می‌شوند.

### قواعد یکپارچگی داده
1. برداشتن تیک شاخه ⇒ رشته‌های زیرش هم پاک می‌شوند.
2. تغییر مقطع به غیرمتوسطه‌دوم ⇒ همهٔ تیک‌ها پاک و بخش پنهان می‌شود.
   (وگرنه دبستانی با رشتهٔ حسابداری ذخیره می‌شد.)
3. هنگام ذخیره، رشته‌ها دوباره در برابر شاخه‌های تیک‌خورده پالایش
   می‌شوند — تکیه بر پاک‌سازی رابط کاربری کافی نیست.
4. ثبت متوسطه دوم بدون شاخه رد می‌شود و صفحه به همان بخش می‌پرد.

### دام‌ها
- ⚠️ `$$` **وجود نداشت**؛ فقط `$` در `01-helpers.js` بود. افزوده شد و
  عمداً `Array.prototype.slice` می‌زند تا `map`/`filter` کار کند
  (`NodeList` این‌ها را کامل ندارد).
- ⚠️ تیک‌ها شناسه ندارند و با **کلاس** تشخیص داده می‌شوند، پس در
  شنوندهٔ `change` باید **پیش از** بررسی‌های مبتنی بر `id` بیایند.
- ⚠️ `fieldsOfBranch` با `function` تعریف شده (نه `const`)، پس بالابری
  می‌شود و فراخوانی‌اش در `02-demo-data.js` — که زودتر بار می‌شود —
  کار می‌کند. اگر روزی به `const` تغییر کند، دادهٔ نمونه می‌شکند.
- ⚠️ آزمون `viewSchools` باید نقش را سوپرادمین کند؛ نقش‌های دیگر فقط
  مدرسهٔ خودشان را می‌بینند و صافی معنا ندارد. (یک آزمون به همین
  دلیل شکست.)

### جای دیدن در فهرست مدارس
شاخه‌ها زیر ستون مقطع نمایش داده می‌شوند و صافی `sbranch` افزوده شد.


## ۵.۲۳ سرایت شاخهٔ مدرسه به فرم‌های پایین‌دستی

پس از افزودن شاخه به مدرسه (بخش ۵.۲۲)، فرم‌های پایین‌دستی هنوز
**همهٔ ۱۴ رشتهٔ کشور** را نشان می‌دادند. یعنی در «دبیرستان شهید
بهشتی» که فقط شاخهٔ نظری دارد، می‌شد کلاس «مکانیک خودرو» ساخت.
اعلام شاخه در فرم مدرسه بی‌اثر بود.

### دو تابع مرجع در `26-curriculum.js`
| تابع | خروجی |
|---|---|
| `schoolBranches(schoolId)` | شاخه‌های اعلام‌شدهٔ مدرسه |
| `schoolFields(schoolId)` | رشته‌های فعال مدرسه |

**قاعدهٔ بازگشت امن:** اگر مدرسه شاخه‌ای ثبت نکرده باشد (دادهٔ قدیمی
پیش از این قابلیت)، همهٔ رشته‌ها برگردانده می‌شود. اگر خالی
برمی‌گرداندیم، فهرست رشته در مدارس قدیمی خالی و کار کاربر بن‌بست
می‌شد.

### نقاط وصل‌شده
| محل | فایل |
|---|---|
| فرم کلاس (فهرست رشته) | `18-modals.js` → `classModal` |
| فرم درس (فهرست شاخه) | `18-modals.js` → `subjectModal` |
| صافی درس‌ها | `11-classes-subjects.js` |
| کتاب‌های استاندارد | `19-actions.js` |
| رویداد `s_branch` و `im_branch` | `19-actions.js` → `fieldsHere()` |
| ذخیرهٔ کلاس و درس | `19-actions.js` |

`fieldsHere(branch, schoolId)` تقاطع رشته‌های یک شاخه با رشته‌های
مدرسه است؛ اگر تقاطع خالی شد، همهٔ رشته‌های شاخه را می‌دهد.

### اعتبارسنجی دولایه
محدودکردن فهرست کافی نیست — داده می‌تواند از راه دیگری بیاید
(ورود از اکسل، همگام‌سازی). پس هنگام **ذخیره** هم بررسی می‌شود که
رشته جزو شاخه‌های مدرسه باشد.

**استثنای عمدی:** رشتهٔ فعلی خود رکورد مجاز است حتی اگر دیگر جزو
شاخه‌ها نباشد. اگر مدیر شاخه‌ای را از مدرسه بردارد، کلاس‌های قدیمی
آن رشته باید همچنان قابل ویرایش بمانند، وگرنه قفل می‌شوند.

### اشکالی که سر راه پیدا شد
در `subject-save` اعتبارسنجی رشته با شناسهٔ `c_field` نوشته شده بود،
اما فیلد فرم درس `s_field` نام دارد. کادر قرمز روی عنصری می‌رفت که
در آن فرم **وجود ندارد**، پس کاربر پیام می‌دید ولی نمی‌فهمید کدام
فیلد ایراد دارد. اصلاح شد.

⚠️ درس آموخته: پس از کپی‌کردن یک بلوک اعتبارسنجی بین فرم‌ها، شناسهٔ
فیلدها باید بازبینی شود — پیشوند `c_` و `s_` به‌راحتی جابه‌جا می‌شوند.

### جایی که عمداً محدود نشد
صافی شاخه در **فهرست مدارس** همهٔ شاخه‌ها را نشان می‌دهد، چون
سوپرادمین می‌خواهد بین مدارس مختلف بگردد.


## ۵.۲۴ کلاس‌بندی خودکار هنگام ورود از اکسل

کاربر گفت: مدیر اکسل را می‌دهد و باید **همه‌چیز خودکار** انجام شود —
دانش‌آموز و ولی ثبت شوند، و از متنی مثل «رضا احمدی دهم الکترونیک»
فهمیده شود که این دانش‌آموز به کلاس دهم الکتروتکنیک می‌رود.

### ماژول `41-class-placement.js`
جدا از `34-excel-import.js` ساخته شد چون منطق تشخیص مستقل از خواندن
فایل است و باید جداگانه آزمودنی باشد.

| تابع | کار |
|---|---|
| `normField(s)` | یکسان‌سازی: عربی→فارسی، نیم‌فاصله، فاصلهٔ اضافه |
| `detectField(text, schoolId)` | رشته + بررسی ارائه‌شدن در آن مدرسه |
| `detectBranch(text)` | شاخه وقتی رشته نوشته نشده («دهم فنی») |
| `detectSection(text, grade)` | شاخهٔ کلاس: «۲»، «الف» |
| `parsePlacement(text, schoolId)` | تحلیل کامل → پایه، رشته، نام، حالت |
| `findClassFor(pl, schoolId)` | یافتن کلاس موجود متناظر |
| `planPlacement(rows, schoolId)` | برنامهٔ کلاس‌بندی یک فایل کامل |

### جدول نام‌های عامیانه (`FIELD_ALIASES`)
مدرسه‌ها نام رسمی نمی‌نویسند. نمونه:

| در فایل | نام رسمی |
|---|---|
| الکترونیک · برق | الکتروتکنیک |
| تجربی | علوم تجربی |
| ریاضی | ریاضی فیزیک |
| انسانی · ادبیات | ادبیات و علوم انسانی |
| رایانه | کامپیوتر |
| خیاطی · دوخت | طراحی دوخت |
| مکانیک · خودرو | مکانیک خودرو |

⚠️ کلیدها به ترتیب **بلندی** بررسی می‌شوند تا «ریاضی فیزیک» پیش از
«ریاضی» بیفتد.

### تطبیق سه‌مرحله‌ای در `findClassFor`
۱. نام دقیق · ۲. پایه + رشته · ۳. پایه + شاخهٔ کلاس

مرحلهٔ ۲ کلیدی است: مدرسه ممکن است کلاس را «دهم الکتروتکنیک» ثبت
کرده باشد و فایل «۱۰ الکترونیک» بنویسد.

### دام‌های ثبت‌شده
- ⚠️ **`gradeFromName` ارقام فارسی را نمی‌خواند.** الگوی عددی‌اش
  لاتین است، پس «۱۱ ریاضی» پایه را از دست می‌داد و به پیش‌فرض دهم
  می‌افتاد. راه‌حل: `faDigits()` پیش از فراخوانی.
- ⚠️ **`GRADE_WORDS_REV` وجود ندارد.** فرض کردم هست؛ `gradeWordOf()`
  محلی ساخته شد.
- ⚠️ **بریدن نام کلاس با فاصله غلط است.** کد پیشین
  `cn.split(' ').slice(1)` می‌زد که روی «دهم ادبیات و علوم انسانی»
  رشته را خراب می‌کرد. حالا از خروجی موتور تشخیص استفاده می‌شود.
- ⚠️ **نام مادر نادیده گرفته می‌شد.** در `commitImport` جفت مادر
  `[d.mother_nid, null, 'مادر']` بود و همیشه «مادر فلانی» ساخته
  می‌شد حتی وقتی نامش در فایل بود.

### ستون «پایه» به نگاشت افزوده شد
فایل‌هایی که کلاس ندارند ولی پایه و رشته را جدا دارند، حالا با ترکیب
این دو کلاس‌بندی می‌شوند.


## ۵.۲۵ شبیه‌سازی ظرفیت و رفع سه گلوگاه

سند کامل: `docs/CAPACITY_SIM.md`

### پاسخ کوتاه
| پرسش | پاسخ |
|---|---|
| سقف دانش‌آموز در یک مرورگر | **~۲٬۰۰۰** بدون افت |
| بزرگ‌ترین مدرسهٔ ایران (۱٬۵۰۰) | ✅ هشت صفحه در **۴۷ms** |
| ورود ۲٬۰۰۰ ردیف اکسل | **۲.۷ ثانیه** |

چهار برابر شدن جمعیت (۵۰۰→۲٬۰۰۰) زمان را فقط **۹٪** بالا برد.

### سه گلوگاه که سنجش پیدا کرد
۱. **باطل‌سازی ایندکس در هر `insert`** ⇒ `idxAppend()` افزایشی.
۲. **`nidOwner` خطی** ⇒ ایندکس `idxUserByNid` (اعتبارسنجی ۱۲× سریع‌تر).
۳. **چهار جستجوی خطی در `commitImport`** ⇒ نقشه‌های درهم‌سازی.

نتیجه: ۲۰۰۰ ردیف از **۱۴٬۲۵۷ms به ۲٬۷۱۹ms** (۵.۲×).

### درس مهم
`batchWrites` نوشتن روی حافظه را دسته‌ای کرده بود اما **ایندکس** را
نه. دسته‌ای‌کردن یک لایه، لایهٔ دیگر را خودکار درست نمی‌کند.

⚠️ `idxAppend` فقط برای **درج** امن است. در ویرایش یا حذف، کلید ممکن
است عوض شده باشد و رکورد قدیمی باید از سطل قبلی برداشته شود — آنجا
`idxInvalidate` درست است.

### دام حیاتی: ترتیب تعریف نقشه‌ها
نقشه‌های درهم‌سازی ابتدا **پس از** حلقهٔ ساخت کلاس تعریف شده بودند.
`var` بالابری می‌شود ولی مقداردهی نه، پس نقشه `undefined` بود و دو
آزمون شکست. باید پیش از نخستین استفاده تعریف شوند.

### تفکیک «کاربر همزمان»
سه معنای جدا دارد: روی یک دستگاه (همیشه ۱)، در یک مدرسه (بدون سقف،
چون داده محلی است و رقابتی بر منبع مشترک نیست)، و روی سرور (گلوگاه
واقعی — برآورد ~۲ درخواست در دقیقه برای هر کاربر فعال).


## ۵.۲۶ تصحیح: الکتروتکنیک ≠ الکترونیک

**اشتباه من بود.** در جدول نام‌های عامیانه «الکترونیک» را مترادف
«الکتروتکنیک» گرفته بودم. کاربر تصحیح کرد: این دو **دو رشتهٔ مجزا**
در شاخهٔ فنی و حرفه‌ای هستند.

| رشته | محتوا |
|---|---|
| الکتروتکنیک | برق ساختمان و صنعتی، تابلوبرق، موتورهای الکتریکی |
| الکترونیک | مدارهای الکترونیکی، مخابرات، ابزار دقیق |

نگاشت درست:
| در فایل | رشته |
|---|---|
| الکتروتکنیک · برق · برق صنعتی · برق ساختمان | الکتروتکنیک |
| الکترونیک · مخابرات | الکترونیک |

⚠️ **درس:** دربارهٔ اصطلاح تخصصی که مطمئن نیستم، حدس نزنم. نام‌های
رشته را از منبع رسمی بگیرم یا از کاربر بپرسم.

## ۵.۲۷ رفع پرش منو هنگام ناوبری

کاربر گفت با کلیک روی گزینه‌های **پایین** منو (مثل «سلامت سامانه»)
منو به بالا برمی‌گردد.

**علت:** `render()` با `innerHTML` کل پوسته از جمله نوار کناری را از
نو می‌سازد، پس `scrollTop` صفر می‌شود.

**راه‌حل:** موقعیت اسکرول پیش از بازسازی برداشته و بلافاصله پس از آن
بازگردانده می‌شود. چون هر دو در یک چرخهٔ همگام رخ می‌دهند، چشم کاربر
پرشی نمی‌بیند.

افزون بر آن:
- گزینهٔ فعال اگر بیرون از دید بیفتد، با `scrollIntoView` به دید
  می‌آید (برای ناوبری با صفحه‌کلید).
- محتوای صفحهٔ تازه از بالا شروع می‌شود (`S.__routeChanged`) ولی
  اسکرول **منو** دست‌نخورده می‌ماند. این دو نباید قاطی شوند.

## ۵.۲۸ دیاگ سامانه — عیب‌یاب و تعمیرکار خودکار

کاربر گفت: «سامانه‌ای بساز که مثل مکانیک باشد، خودش تشخیص دهد و رفع
کند، عین دیاگ ماشین — تا نیازی به پشتیبان برنامه‌نویس نداشته باشم.»

سپس تصحیح کرد که دیاگ باید **پیکربندی و زیرساخت و موتور برنامه** را
بپوشاند، و در ادامه افزود که **کیفیت دادهٔ کاربر هم بماند**. پس دو
خانوادهٔ مستقل داریم.

### دو خانوادهٔ آزمون (`DIAG_CATS`)

| خانواده | معنا | مسئول رفع |
|---|---|---|
| `engine` ⚙️ | پیکربندی، زیرساخت و موتور برنامه | برنامه‌نویس / خودِ دیاگ |
| `data` 📋 | کیفیت دادهٔ واردشدهٔ مدارس | مدیر مدرسه |

این تفکیک **معنایی** است نه تزئینی: عیب موتور یعنی برنامه ایراد
دارد؛ عیب داده یعنی ورودی ایراد دارد. سوپرادمین باید بلافاصله
بفهمد کدام است.

`runDiagnostics(cat)` می‌تواند فقط یک خانواده را بدواند.

### خانوادهٔ موتور — ۱۸ آزمون
| آزمون | چه چیزی را می‌گیرد |
|---|---|
| `route-coverage` | گزینهٔ منویی که نمایش خطا می‌دهد یا خالی است |
| `title-coverage` | مسیر بدون عنوان در `TITLES` |
| `authz-coverage` | گزینهٔ منو که گارد دسترسی اجازه‌اش نمی‌دهد |
| `collection-registry` | مجموعه‌ای که در آن درج شده ولی در `db` نیست |
| `id-sequence` | شمارندهٔ عقب‌مانده ⇒ خطر شناسهٔ تکراری |
| `duplicate-ids` | دو رکورد با یک شناسه در یک مجموعه |
| `localstorage-health` | نوشتن/خواندن حافظهٔ مرورگر |
| `oplog-integrity` | عملیات خراب در دفترچه |
| `sync-backlog` | انباشت متعارض/ناموفق در صف همگام‌سازی |
| `render-performance` | صفحهٔ کندتر از ۲۵۰ میلی‌ثانیه |
| `missing-functions` | ۱۸ تابع حیاتی بارگذاری نشده |
| `module-order` | جدول پایه‌ای که ماژول دیگر پیش از تعریفش می‌خواهد |
| `bell-config` | زمان‌بندی زنگ با گسست یا خطا |
| `school-config` | متوسطه دوم بدون شاخه، مدرسهٔ بدون مقطع |
| `storage-pressure` | نزدیکی دفترچه به سقف ۵ مگابایت |
| `index-health` | نرخ اصابت ایندکس زیر ۹۰٪ |
| `index-staleness` | ناسازگاری ایندکس با دادهٔ واقعی |
| `sync-queue-stuck` | صف گیرکرده |

### خانوادهٔ داده — ۱۴ آزمون
ثبت‌نام بی‌صاحب · ثبت‌نام تکراری · پیوند ولی بی‌صاحب · کاربر بدون
مدرسه · کد ملی تکراری · نام کاربری تکراری · کد ملی نامعتبر · کلاس
پر از ظرفیت · رشتهٔ خارج از شاخه · مدرسهٔ بدون مدیر · دانش‌آموز بدون
کلاس · رمز پیش‌فرض · مبلغ منفی · رکورد مالی بی‌صاحب.

### چهار اصل طراحی
۱. **تشخیص و درمان دو تابع جدا هستند** (`check` و `fix`).
۲. **تعمیر خودکار فقط وقتی امن است.** پرچم `safe` تعیین می‌کند.
   چیزی که قضاوت انسانی می‌خواهد (کد ملی تکراری) **هرگز** خودکار
   تعمیر نمی‌شود.
۳. **هر تعمیر پشتیبان می‌گیرد** (`diagSnapshot`) و اگر شکست خورد
   خودکار بازمی‌گرداند (`diagRestore`).
۴. **دیاگ نباید خودش برنامه را بشکند.** هر آزمون در `try` است و
   خطایش با `selfError` علامت می‌خورد — آزمون شده است.

### نمرهٔ سلامت
`۱۰۰ − (بحرانی×۱۵ + هشدار×۵ + اطلاع×۱)`
وزن بحرانی سه برابر هشدار است چون یک عیب بحرانی از چند هشدار
مهم‌تر است.

### پایش خودکار پس‌زمینه
`diagAutoStart()` هر ۵ دقیقه بررسی می‌کند، عیب‌های **امن** را خودش
تعمیر می‌کند و به سوپرادمین اعلان می‌دهد. پیش‌فرض خاموش است.

### سنجش
| مورد | عدد |
|---|---|
| کل آزمون | ۳۲ (۱۸ موتور + ۱۴ داده) |
| زمان اجرای کامل | ~۲۳۵ میلی‌ثانیه |
| زمان فقط داده | ~۱۲ میلی‌ثانیه |
| تزریق عیب عمدی | ۹۰ → ۶۰ → تعمیر → ۹۰ |

`route-coverage` و `render-performance` گران‌ترند چون واقعاً هر صفحه
را رندر می‌کنند — همین گرانی است که ارزششان را می‌سازد.

### دام‌های ثبت‌شده
- ⚠️ **صف همگام‌سازی `SYNC.queue` است، نه `syncQueue`.** آزمون اولم
  نام غلط را فرض کرد و بی‌صدا رد می‌شد.
- ⚠️ **آزمون دیاگ نباید فرض کند داده تمیز است.** آزمون‌های پیشین
  عمداً رکورد می‌سازند؛ باید اول پاک‌سازی کند یا نسبی بسنجد.
- ⚠️ `route-coverage` هنگام رندر `S.route` را عوض می‌کند؛ حتماً در
  `finally` به حالت اول برگردد وگرنه کاربر به صفحهٔ دیگری پرت می‌شود.

## ۵.۲۹ شیفت مدرسه و زمان‌بندی زنگ‌ها

کاربر گفت بعضی مدارس شیفت بعدازظهرند، و زمان‌بندی زنگ و تفریح در هر
منطقه فرق دارد پس **تعریفش با مدیر است**، نه سخت‌کدشده.

### شیفت
میدان `shift` به مدرسه افزوده شد: صبح · بعدازظهر · هر دو.

### ماژول `43-bell-schedule.js` (نسخهٔ ۲ — دور ۶۳)
هر مدرسه یک رکورد در `db.bell_schedules` دارد با **ساعت کاملاً
مستقل برای هر روز هفته**:

```
{ school_id, days: [
    { start:'07:30', slots:[{kind:'lesson',min:45},{kind:'break',min:10},…] },  // شنبه
    … چهار روز دیگر
] }
```

هر زنگ با **ساعت شروع و پایان مشخص** تعریف می‌شود (زنگ اول از X
تا Y، زنگ دوم از Y تا Z — زنجیرهٔ پیوسته؛ تفریح هم یک بازه از همین
زنجیره است). **تعداد زنگ سقف ندارد.** فرم تازه هر روز را جدا
نشان می‌دهد: ساعت شروع روز + ساعت پایان هر بازه (ساعت شروع هر
بازه محاسبه‌شده است) + دکمهٔ «کپی از روز قبل» برای روزهای دوم به
بعد.

⚠️ **رکورد نسخهٔ ۱ (میراثی)** `{ school_id, start, slots }` بدون
`days` هنوز خوانده می‌شود: `bellDayData` آن را برای همهٔ روزها
برمی‌گرداند؛ با اولین ذخیرهٔ تازه رکورد به نسخهٔ ۲ ارتقا می‌یابد.
اگر رکورد تازه بنویسید، `days` را کامل (پنج روز) بدهید.

| تابع | کار |
|---|---|
| `timeToMin` / `minToTime` | تبدیل «۰۷:۳۰» ↔ ۴۵۰ (ارقام فارسی هم) |
| `bellOf(schoolId, day)` | زمان‌بندی یک روز، یا الگوی پیش‌فرض متناسب |
| `bellTimeline(schoolId, day)` | ساعت واقعی هر زنگ و تفریح یک روز (روز پیش‌فرض: شنبه) |
| `bellDayTimeline({start, slots})` | خط زمانی از دادهٔ خام یک روز |
| `bellCurrentDays(schoolId)` | پنج روز کامل (با جایگزینی الگو) |
| `bellSaveDays(schoolId, days)` | ذخیرهٔ پنج روز با اعتبارسنجی |
| `bellSave(schoolId, start, slots)` | سازگاری نسخهٔ ۱: همان برنامه برای همهٔ روزها |
| `bellApplyPreset(schoolId, key, dayIdx)` | الگوی آماده برای یک روز یا همه |

🔴 **دام «الان کدام روز است»:** `bellTimeline` بدون پارامتر روز
**شنبه** برمی‌گرداند نه «امروز». برای «الان» همیشه از
`currentSlot` (ماژول زنگ جاری) استفاده کنید که خودش روز درست را
می‌فرستد؛ یا `todayIndex(now)` را حساب کنید و بفرستید.

### سه الگوی آماده
صبح (۰۷:۳۰، زنگ ۴۵ دقیقه) · بعدازظهر (۱۳:۰۰) · ابتدایی (۰۷:۴۵، زنگ
۴۰ دقیقه با تفریح بلندتر).

⚠️ الگوها **نقطهٔ شروع‌اند، نه اجبار.** مدیر می‌تواند هر بازه را
تغییر دهد، حذف کند یا بازهٔ تازه بیفزاید.

### قاعدهٔ بازگشت امن
اگر مدرسه زمان‌بندی تعریف نکرده باشد، الگوی متناسب با **شیفت و
مقطعش** برگردانده می‌شود و در صفحه هشدار می‌گیرد که هنوز اختصاصی
نشده. برنامه هرگز بدون ساعت نمی‌ماند.

### اعتبارسنجی
ساعت نامعتبر · بدون بازه · مدت صفر · گذر از نیمه‌شب — همه رد می‌شوند.
اعتبارسنجی در `bellSave` است نه در نما، چون داده می‌تواند از
همگام‌سازی هم بیاید.


---

## ۵.۳۰ لایهٔ داده و آماده‌سازی برای سرور

### چرا این لایه ساخته شد
پروژه امروز دمو است ولی فردا به سرور وصل می‌شود. اگر صدها نقطه از
برنامه مستقیم با `localStorage` حرف بزنند، روز اتصال یعنی بازنویسی
کل برنامه. پس **یک دروازهٔ واحد** ساخته شد: `src/js/00-data-layer.js`.

سنجش پیش از کار: **۲۴ فراخوانی مستقیم `localStorage` در ۸ فایل**.
پس از کار: **صفر** — بیرون از خود لایه.

### سه بخش لایه

| بخش | مسئولیت | امروز | فردا |
|---|---|---|---|
| `Store` | انبار کلید-مقدار | حافظهٔ مرورگر | همان، برای صف آفلاین |
| `Api` | گفتگو با سرور | خاموش، رد می‌کند | `fetch` واقعی |
| `Data` | چهار عمل داده | روی `db` در حافظه | همان امضا، پشتش سرور |

```js
Store.get/set/remove/getJSON/setJSON/bytes/available
Api.request/get/post/put/del · Api.authHeader()
Data.all/find/where/create/update/delete/batch
DATA_MODE = 'local' | 'server'  ·  isServerMode()
```

### تصمیم طراحی: چرا `Data` همگام ماند
وسوسه‌انگیز بود که همین حالا همه‌چیز `Promise` شود تا بعداً آماده
باشد. این کار **نشد** و دلیلش مهم است:

معماری این برنامه «اول آفلاین» است. نوشتن‌ها به صف `27-sync.js`
می‌روند و در پس‌زمینه ارسال می‌شوند. دبیری که وسط حضور و غیاب
اینترنتش قطع می‌شود نباید منتظر سرور بماند — نوشتن باید فوری برگردد.
پس امضای `Data.create` با اتصال به سرور هم عوض نمی‌شود و ناهمگام
کردنش فقط ۴۳ فایل را بی‌دلیل پیچیده می‌کرد.

فقط **خواندن‌های اولیه** (بارگذاری داده در آغاز) ناهمگام خواهند شد،
که همین حالا هم در `initSync()` ناهمگام است.

### `Store` چرا استثنا پرتاب نمی‌کند
مرورگر در حالت ناشناس یا با حافظهٔ پر، `setItem` را با استثنا رد
می‌کند. اگر لایه استثنا را بالا بفرستد، هر نقطهٔ برنامه باید
`try/catch` داشته باشد. پس `Store.set` مقدار بولی برمی‌گرداند و
`Store.getJSON` روی داده خراب `fallback` می‌دهد — یک بار اینجا
درست انجام شود، همه‌جا امن است.

### دام‌ها
⚠️ `00-data-layer.js` باید **اول** فهرست `_order.json` باشد، پیش از
`01-helpers.js` — چون `03-persistence.js` از همان ابتدا `Store` را
صدا می‌زند.

⚠️ آزمون `localstorage-health` در دیاگ حالا از `Store.available()`
استفاده می‌کند نه `localStorage` مستقیم. اگر مستقیم بسنجد، مسیری را
می‌آزماید که برنامه واقعاً استفاده نمی‌کند.

⚠️ آزمون خودکاری در `tests/smoke.js` هر فایل `src/js` را می‌خواند و
اگر جز لایه کسی `localStorage` داشته باشد شکست می‌دهد. این نگهبان
است تا قاعده به‌مرور فرسوده نشود.

---

## ۵.۳۱ امن‌سازی خروجی در برابر XSS

### دو تابع، دو خطر متفاوت

| تابع | کجا | چه می‌بندد |
|---|---|---|
| `esc()` | متن داخل تگ | `& < > " '` |
| `escAttr()` | مقدار صفت | همان‌ها + <code>`</code> و `=` |

چرا `escAttr` جداست؟ چون در صفت، بدون هیچ `<` می‌شود از کوتیشن فرار
کرد و صفت تازه چسباند. مقدار `x" data-act="logout" x="` در یک صفت
بدون امن‌سازی، دکمهٔ خروج می‌سازد. بستن `=` و بک‌تیک جلوی صفت‌های
بدون کوتیشن را هم می‌گیرد.

### سنجش
| | پیش | پس |
|---|---|---|
| صفت پویای امن | ۶ | **۱۲۱** |
| صفت پویای ناامن | ۱۱۵ | **۰** |

متن‌های داخل تگ از قبل تقریباً کامل بودند؛ نقطهٔ ضعف واقعی
**صفت‌ها** بود — دقیقاً جایی که کمتر به آن مشکوک می‌شویم.

### آنچه عمداً امن‌سازی نشد
صفت‌های `class` و `style` که مقدارشان از جدول ثابت می‌آید
(`ROLE_BADGE[u.role]`، `ATT_BADGE[r.status]`) دست‌نخورده ماندند.
اینجا کلید از دادهٔ کاربر می‌آید ولی **مقدار** از جدول برنامه‌نویس؛
اگر کلید نامعتبر باشد نتیجه `undefined` است نه کد مهاجم.

⚠️ اگر روزی چنین جدولی از دادهٔ کاربر پر شود، این استدلال باطل
می‌شود و باید `escAttr` اضافه شود.

### دام آزمون
اولین آزمون XSS من سبز شد بدون اینکه چیزی بسنجد: کاربر آزمایشی را
ساختم ولی فهرست کاربران **صفحه‌بندی** است و ۱٬۱۳۵ کاربر دارد، پس
رکورد تازه در صفحهٔ آخر بود و اصلاً رندر نمی‌شد. حالا آزمون اول
بررسی می‌کند که رکورد واقعاً در خروجی هست، بعد تزریق را می‌سنجد.

---

## ۵.۳۲ حریم خصوصی دادهٔ نمونه

نسخهٔ دمو نباید هیچ دادهٔ واقعی داشته باشد. مشکل ظریف این بود که
داده نمونه **تصادفی** ساخته می‌شد — و عدد تصادفی می‌تواند اتفاقاً
شمارهٔ یک شهروند واقعی دربیاید.

| داده | پیش | پس | چرا امن است |
|---|---|---|---|
| تلفن | `'0912'+تصادفی` | `demoPhone()` → `0999…` | ۰۹۹۹ به هیچ اپراتوری تخصیص نیافته |
| کد ملی | ۱۰ رقم تصادفی | `makeNid()` → `999…` | ثبت احوال این بازه را صادر نمی‌کند |

⚠️ کد ملی همچنان **رقم کنترل معتبر** دارد، وگرنه `validNid` ردش
می‌کرد و مسیرهای اعتبارسنجی برنامه هرگز آزموده نمی‌شدند. ساختگی
بودن از پیشوند می‌آید نه از نامعتبر بودن.

سه آزمون خودکار این را نگه می‌دارند: تلفن در کد، تلفن در داده
تولیدشده، و کد ملی در داده تولیدشده.

📄 سند کارهای باقی‌مانده: `TODO_BEFORE_PRODUCTION.md`

---

## ۵.۳۳ جهت نوار اسکرول در چیدمان راست‌به‌چپ

### مسئله‌ای که کاربر گزارش کرد
«نوار افقی‌ها همیشه از اول سمت راست باشند. الان نوار سمت چپ بود و
مجبور شدم بکشم سمت راست تا اطلاعات را کامل نشان دهد.»

### ریشهٔ اشتباه
پیش‌تر برای بردن نوار **عمودی** به لبهٔ راست، حیلهٔ شناخته‌شدهٔ
`direction:ltr` روی ظرف و `rtl` روی فرزندان به کار رفته بود. این
برای اسکرول عمودی درست است.

اشتباه اینجا بود که همان قاعده روی ظرف‌های **افقی** هم اعمال شد:

```css
.table-wrap{ direction:ltr }   /* ❌ */
.tabs{ direction:ltr }         /* ❌ */
```

روی ظرف افقی، `ltr` نقطهٔ شروع اسکرول را هم به چپ می‌برد. جدول
راست‌به‌چپ ستون اولش (ردیف و نام) سمت راست است، پس کاربر صفحه را
باز می‌کرد و **ستون آخر** را می‌دید.

### قاعدهٔ درست — دو نوع، دو رفتار برعکس

| اسکرول | جهت ظرف | نتیجه |
|---|---|---|
| عمودی | `ltr` + فرزندان `rtl` | نوار سمت راست |
| افقی | `rtl` | شروع از راست |

### آنچه عوض شد
| گزینشگر | پیش | پس |
|---|---|---|
| `.table-wrap` | `ltr` | **`rtl`** |
| `.tabs` | `ltr` | **`rtl`** |
| `.diag-items` | — | `ltr` (عمودی بود، نوارش چپ می‌افتاد) |
| `.vscroll` (تازه) | — | `ltr` — کلاس کمکی |

دو ظرف درون‌خطی هم که `max-height` + `overflow:auto` داشتند و نوار
عمودیشان سمت چپ می‌افتاد، کلاس `vscroll` گرفتند: فهرست اعلان‌ها در
`20-communication-finance.js` و جدول صف در `27-sync.js`.

### سنجش
جهت محاسبه‌شدهٔ واقعی در سه صفحهٔ جدول‌دار بررسی شد:
`users`، `grades`، `attendance` ⇒ هر سه `rtl` ✅ ·
`main`، `sidebar`، `vscroll` ⇒ هر سه `ltr` ✅

### دام‌ها
⚠️ `.table-wrap > *{ direction:rtl }` حذف شد. وقتی خود ظرف `rtl`
است، این قاعده زائد بود و فقط سردرگمی می‌ساخت.

⚠️ هر ظرف تازه با `max-height` + `overflow:auto` باید کلاس `vscroll`
بگیرد. آزمونی در `tests/smoke.js` فایل‌های `src/js` را می‌خواند و
اگر یادتان برود شکست می‌دهد — چون این اشتباه بی‌صدا است و فقط
کاربر متوجهش می‌شود.

⚠️ ظرفی که هر دو جهت اسکرول دارد: اولویت با شروع از راست است.
ستون اول جدول بیشتر لازم می‌شود تا موقعیت نوار عمودی.

## ۵.۳۴ مشاور مدرسه و عوامل اجرایی (دور ۶۳)

نقش هفتم «مشاور مدرسه» اضافه شد. مدیر مدرسه او را **مثل سایر
کادرها** تعریف می‌کند (فرم کاربر، نقش «مشاور»). وظیفهٔ او دیدن
صف دانش‌آموزانی است که **دستوری** توسط مدیر ارجاع شده‌اند — بر
بازهٔ شمارندهٔ الگوی تکرار (طرح ۴.۷
`docs/PLAN_ATTENDANCE_STATES.md`).

### ماژول `47-counselor.js`

| کار | کجا |
|---|---|
| نقش‌های کادر اجرایی | `EXEC_ROLES` — مشاور `active:true`؛ معاونت‌های آموزشی/اداری/فنی/پرورشی `reserved:true` |
| آستانه‌های الگو | `patternRules(schoolId)` — پیش‌فرض `PATTERN_RULES` (تأخیر ۴/ماه، غیبت ۳/ماه، خروج ۳/هفته)؛ اگر `schools.discipline_rules` باشد از آنجا |
| شمارندهٔ یک دانش‌آموز | `patternCheck(studentId, days)` — تأخیر/غیبت از `db.attendance`؛ خروج هنوز ۰ (صبر می‌کند تا `class_exits` ساخته شود) |
| آستانه‌های ردشده | `patternBreaches(studentId, schoolId, days)` |
| همهٔ دانش‌آموزان الگودار | `patternFlagged(schoolId, days)` — **یک پیمایش** روی جدول حضور (نه فیلتر کامل برای هر دانش‌آموز) |
| صف ارجاع | `counselorQueue(schoolId, onlyOpen)` از `db.counselor_refs` |
| ارجاع (دستوری) | `counselorRef(sid, studentId, breach, referrerId)` — ارجاع بازِ هم‌دلیل تکرار نمی‌شود |
| رسیدگی | `counselorHandle(refId, userId, note)` — رکورد پاک نمی‌شود؛ `status` به `handled` می‌رود |
| نماها | `viewCounselorQueue` (روت `cqueue`، خانهٔ مشاور) · `viewFollowup` (`followup`، مدیر) · `viewStaff` (`staff`، مدیر) |

### تصمیمات سیاستی که بشکند دردش با همه است

۱. **ارجاع خودکار نیست.** دکمهٔ «ارجاع به مشاور» فقط در نمای
   پیگیری مدیر است (`counselor-ref` در ACTION_ROLES = manager).
۲. **اعلان به ولی خودکار نیست** (بند ۴ دور ۶۳). پیگیری و
   ارتباط با خانواده با مشاور است.
۳. **رکورد ارجاع پاک نمی‌شود.** «رسیدگی شد» فقط `status` را
   عوض می‌کند — الگوی موجه‌شده‌ها (طرح ۴.۵): تاریخچه می‌ماند.

### 🔴 محدوده مشاور — مرز بین‌مدرسه‌ای

مشاور فقط `counselor_refs`ِ مدرسهٔ خودش را می‌بیند
(`scopeDescriptor`: `queue_only:true`). نمره، برنامه و پروندهٔ
کامل دانش‌آموز در برش اوست (EXTRA_ROUTES.counselor = خالی؛
صفحه‌های record/attendance/users/grades/d.schedule/discipline
بسته‌اند). آزمون «نشت مدرسهٔ دیگر» در tests/smoke.js با یک
ارجاع واقعی در مدرسهٔ ۲ نگهبانی می‌دهد — اگر ارجاع‌های دمو را
حذف کنید، این آزمون بی‌معنا می‌شود و هشدار نمی‌دهد.

### دام‌ها

* `patternCheck` بدون فیلتر مدرسه می‌شمارد؛ مرز را
  `patternFlagged`/`counselorQueue` نگه می‌دارند — هرگز
  شمارنده را بدون فیلتر مدرسه در نما استفاده نکنید.
* دادهٔ نمونه (فاز ۱۲، `generateP12`) چند دانش‌آموز را عمداً
  «الگودار» می‌کند (۷ تأخیر · ۴ غیبت · ترکیبی) و ۴ ارجاع نمونه
  می‌سازد. با `add()` نوشته می‌شود نه `insert()` — وگرنه صف
  همگام‌سازی دمو پر می‌شود.
* `homeRoute('counselor')` باید `cqueue` بماند؛ ورود
  (`login` در 19-actions) از `homeRoute` می‌خواند.
* `counselor_refs` در ابتدای `db` (02-demo-data) تعریف
  شده؛ اگر جدول تازه بسازید، همین‌جا هم باید باشد (آزمون
  collection-registry).

---

## ۶. لایه Offline-First (ماژول `27-sync.js`) ⭐

**سناریوی هدف:** دبیر در کلاس، بدون اینترنت، حضور و غیاب می‌زند.

```
ثبت حضور  →  applyOp()  →  ┬→ db (فوری در UI دیده می‌شود)
                            ├→ log → localStorage
                            └→ enqueueOp() → صف ارسال → localStorage
                                     │
                          ┌──────────┴──────────┐
                     آفلاین؟                 آنلاین؟
                          │                     │
                  در صف می‌ماند          ارسال + حذف از صف
                          │                     │
              رویداد 'online' ────────→ ارسال خودکار
                                                │
                                     خطا؟ → backoff نمایی
                                            ۲s→۴→۸→…→۵min
```

**وضعیت‌های عملیات:** `pending` → `sending` → `synced` (حذف) / `failed` (تلاش مجدد) / `conflict`

**نشانگر نوار بالا:**

| حالت | نمایش |
|---|---|
| 🟢 | همگام |
| 🟡 | در صف ‹تعداد› |
| ⚫ | آفلاین ‹تعداد› |
| 🔵 | همگام‌سازی… |
| 🔴 | تعارض ‹تعداد› |

**اتصال به سرور واقعی — فقط دو خط:**
```js
SYNC.demoMode  = false;
SYNC.serverUrl = '/api/sync';
```

**قرارداد API مورد انتظار:**
```jsonc
// درخواست
POST /api/sync
{ "ops": [ {"uid":"op_...", "t":"ins", "c":"attendance", "data":{...}} ] }

// پاسخ
{ "results": [
    {"uid":"op_...", "ok": true},
    {"uid":"op_...", "conflict": true, "server": {...}, "message":"..."},
    {"uid":"op_...", "ok": false, "message": "خطا"}
] }
```

---

## ۷. امنیت

### ۷.۱ وضعیت — راستی‌آزمایی‌شده با حمله واقعی، نه بازبینی چشمی

| مورد | روش سنجش | نتیجه |
|---|---|---|
| XSS | ۴ payload (تگ، شکستن attribute، نقل‌قول، `javascript:`) تزریق در ۱۶ مجموعه × ۵۰ فیلد، سپس **بررسی DOM واقعی** در ۱۷۴ ترکیب نقش×روت | ✅ صفر عنصر تزریقی ساخته شد |
| مرز چندمستأجری | مدیر مدرسه A دنبال نام دانش‌آموز مدرسه B در ۲۹ روت | ✅ نشتی ندارد |
| اداره | جستجوی نام دانش‌آموز در همه روت‌ها | ✅ فقط داده تجمیعی |
| paywall | باز کردن ۱۲ روت قفل‌شده با تغییر مستقیم `S.route` | ✅ همه مسدود |
| ارتقای امتیاز | تغییر `S.route` به روت‌های مدیریتی با نقش پایین | ✅ مسدود (پس از رفع باگ زیر) |
| جانشینی | سوپرادمین → مدیر، سپس تلاش برای روت سوپرادمین | ✅ درست |

> ⚠️ نکتهٔ روش‌شناختی: تشخیص XSS با جستجوی متنی در رشتهٔ خروجی **غلط** است —
> فرم escape‌شدهٔ payload (`&lt;img...`) را مثبت کاذب می‌شمارد. سنجش درست:
> `innerHTML` را ست کن و با `querySelectorAll` ببین آیا عنصر واقعاً ساخته شد.

### ۷.۲ 🔴 باگ بحرانی که یافت و رفع شد (ممیزی دور ۵)

**مسئله:** `renderRoute()` هیچ بررسی مجوزی نداشت و فقط روی `S.route` سوییچ می‌کرد.
`NAV` تعیین می‌کرد چه چیزی در **منو** دیده شود، اما هیچ‌چیز جلوی تغییر مستقیم
`S.route` (یا hash آدرس) را نمی‌گرفت.

**شدت: بحرانی.** فقط نشت اطلاعات نبود — **تغییر داده** هم ممکن بود:

```
دانش‌آموز → S.route='attendance' → کل لیست کلاس + دکمه‌های فعال «ثبت وضعیت»
         → کلیک att-set روی هم‌کلاسی
         → غیبت جعلی ثبت شد و وارد صف ارسال به سرور شد ❌
دانش‌آموز → S.route='adminsubs' → ۵۱ کیلوبایت پنل مدیریت اشتراک سوپرادمین ❌
```

**رفع:** ماژول `30-authz.js` با دو گارد:

```js
// در renderRoute() — پیش از switch
if(typeof canRoute==='function' && !canRoute(S.route)) return viewForbidden();

// در دیسپچر کلیک 19-actions.js — پیش از اجرای اکشن
if(S.user && typeof canAction==='function' && !canAction(a)){
  toast('شما اجازهٔ انجام این عملیات را ندارید','err'); return;
}
```

**اصل طراحی:** مجوز روت از خودِ `NAV` استخراج می‌شود (تک‌منبع حقیقت) به‌علاوهٔ
`EXTRA_ROUTES` برای زیرصفحه‌هایی که در منو نیستند. پس افزودن آیتم منو خودکار
مجوز می‌دهد و امکان ناهماهنگی بین منو و مجوز از بین می‌رود.

**API ماژول `30-authz.js`:**
```js
navRoutesOf(role)      // روت‌های موجود در منوی نقش
allowedRoutes(role)    // مجموعهٔ کامل مجاز (NAV + EXTRA + COMMON) با کش
canRoute(route, role)  // آیا این روت مجاز است؟
canAction(act, role)   // آیا این اکشن مجاز است؟
homeRoute(role)        // روت خانه
viewForbidden()        // صفحهٔ «دسترسی مجاز نیست»
EXTRA_ROUTES           // روت‌های خارج از منو، به تفکیک نقش
COMMON_ROUTES          // باز برای همهٔ واردشده‌ها
ACTION_ROLES           // نگاشت اکشن → نقش‌های مجاز (۳۰ اکشن حساس)
```

**راستی‌آزمایی:** ۱۰ دسترسی غیرمجاز مسدود شد، ۱۹ دسترسی مجاز دست‌نخورده ماند
(صفر رگرسیون)، جانشینی و پرسونای دوم درست کار کردند. ۱۰ تست دائمی افزوده شد.

### ۷.۳ مهم‌ترین تصمیم امنیتی: محدودسازی داده

اگر سرور کل دیتابیس را به مرورگر بفرستد، یک دانش‌آموز با کنسول می‌تواند نمرات و
کد ملی **همهٔ** کاربران را بخواند. **هیچ فیلتر سمت کلاینتی این را حل نمی‌کند.**

| نقش | آنچه سرور مجاز است بفرستد |
|---|---|
| دانش‌آموز | فقط رکوردهای خودش (~۸KB) |
| ولی | فقط فرزندان تأییدشده |
| دبیر | فقط کلاس‌های خودش، پنجرهٔ ۴۵ روزه (~۳۵۶KB) |
| مدیر | فقط مدرسهٔ خودش (~۱MB) |
| اداره | 🔒 فقط آمار تجمیعی — هیچ رکورد فردی |
| سوپرادمین | آمار ملی + فهرست صفحه‌بندی‌شده |

### ۷.۴ محدودیت‌های ذاتی نسخهٔ دمو — تصمیم آگاهانه، نه باگ

```
❌ رمز عبور متن ساده در db (همه: '123456')
❌ احراز هویت فقط سمت کلاینت — با کنسول قابل دور زدن
❌ کل داده در حافظهٔ مرورگر
❌ بدون CSRF / rate-limit / audit trail
```

**لایهٔ مجوزدهی سمت کلاینت، سد نهایی نیست** — تجربهٔ کاربری درست و دفاع لایه‌ای
است. سرور موظف است **مستقلاً** همین قواعد را اعمال کند:

```
• رمز با bcrypt/argon2 (۱۲ round)
• JWT کوتاه‌عمر + refresh چرخشی
• محدوده از توکن تعیین شود، نه از پارامتر کلاینت
• هر عملیات ورودی از صف sync مجدداً اعتبارسنجی شود
• paywall در لایهٔ API
• audit_log برای عملیات حساس
```

---

## ۸. کارایی و مقیاس ملی — اندازه‌گیری‌شده

### ۸.۱ هدف طراحی

سامانه برای **۱۰ میلیون دانش‌آموز ثبت‌شده** و **۵ میلیون کاربر همزمان** طراحی شده است.

**اصل حاکم:** بار هر مرورگر باید **ثابت و مستقل از مقیاس کشور** بماند.

کد کلاینت حالت مشترک بین کاربران ندارد؛ هر مرورگر مستقل است. پس ۵ میلیون کاربر همزمان = ۵ میلیون نسخهٔ مستقل. مقیاس افقی نامحدود است، به شرط آنکه بار هر نسخه ثابت بماند.

### ۸.۲ بار هر نقش — مستقل از اندازهٔ کشور

| نقش | رکورد در برش | حجم |
|---|---|---|
| دانش‌آموز | ۴۵ | **۸ KB** |
| دبیر | ۲٬۰۲۴ | **۳۵۶ KB** |
| مدیر | ۵٬۸۵۲ | **۱۰۲۹ KB** |

این اعداد با ۱۰ میلیون یا ۱۰۰ میلیون دانش‌آموز **تغییر نمی‌کنند**.

### ۸.۳ بنچمارک (کلاس ۴۰ نفره، سال کامل ۱۸۰ روزه)

| مدرسه | دانش‌آموز | حضور سال | dashboard | attendance | grades | classes | مجموع |
|---|---|---|---|---|---|---|---|
| معمول | ۵۲۰ | ۹۳٬۶۰۰ | ۱۷.۷ms | ۱.۷ms | ۶.۰ms | ۱.۳ms | **۳۴.۷ms** |
| بزرگ | ۱٬۰۰۰ | ۱۸۰٬۰۰۰ | ۳۰.۰ms | ۱.۳ms | ۵.۷ms | ۲.۳ms | **۴۷.۳ms** |
| حداکثر ایران | ۱٬۵۲۰ | ۲۷۳٬۶۰۰ | ۴۶.۰ms | ۱.۷ms | ۶.۷ms | ۳.۰ms | **۷۱.۳ms** |

مجموع شامل **هر نُه صفحه** است: داشبورد، حضور و غیاب، نمرات، چرخهٔ تحصیلی،
افت تحصیلی، جلسات اولیا، رشد مدرسه، فرم و پیامک، و ورود اکسل.
سنگین‌ترین همچنان داشبورد است؛ بقیه زیر ۷ میلی‌ثانیه‌اند.

پروفایل کامل ۳۸ ترکیب نقش×روت: **هیچ‌کدام بالای ۲۰ms** نیست.

**پایداری:** ۱٬۵۰۰ رندر با تعویض نقش و تغییر داده در میانه →
**۰ خطا**، رشد حافظه **۱.۳MB**، نرخ اصابت ایندکس **۱۰۰٪** (۱۵۸٬۸۹۱ hit / ۲۹ build).

### ۸.۳.۱ گلوگاه‌های یافته و رفع‌شده در ممیزی دور ۵

| محل | مشکل | رفع | اثر |
|---|---|---|---|
| `08-dashboard.js` رتبهٔ دانش‌آموز | برای هر هم‌کلاسی کل جدول نمرات پیمایش می‌شد — `O(peers×grades)` | `idxGradesByStudent()` | کلاس ۳۲۰ نفره: ۱۲.۷ms → **۲.۰ms**، رشد از درجه دوم به خطی |
| `19-actions.js` اکشن `att-all` | برای هر دانش‌آموز یک `find` روی کل جدول حضور | `idxAttByClassDate()` + `Map` روز | مسیر پرکاربرد روزانهٔ دبیر |
| `11-classes-subjects.js` شبکهٔ کلاس‌ها | شمارش ثبت‌نام با `filter` به ازای هر کارت | `idxEnrollByClass()` | خطی شد |
| `03-persistence.js` `applyOp` | ایندکس **پیش از** جستجو باطل می‌شد ⇒ بازسازی کامل در هر نوشتن | جابه‌جایی ترتیب باطل‌سازی | هر نوشتن از O(n) به O(1) |
| `27-sync.js` `saveQueue` | هر عملیات کل صف را سریالایز می‌کرد | پوشش در `batchWrites` | ارتقای ۵۰۰ نفره: ۳٬۲۰۰ms → **۱۸۷ms** (۱۷×) |
| `32-atrisk-meetings-growth.js` `atRiskList` | ساخت رشتهٔ «دلایل» برای همهٔ دانش‌آموزان | getter تنبل `reasons` | بدترین حالت ۸۹ms → **۳۳ms** |

### ۸.۴ لایه ایندکس — `28-indexes.js`

**قاعدهٔ صحت (حیاتی):** هر ایندکس با شمارندهٔ نسخهٔ مجموعه اعتبارسنجی می‌شود. `applyOp` در هر تغییر `idxInvalidate(op.c)` صدا می‌زند، پس ایندکس **هرگز دادهٔ کهنه نمی‌دهد**.

```js
idxGroup(coll, name, keyFn)   // کلید → آرایه رکورد
idxUnique(coll, name, keyFn)  // کلید → یک رکورد
idxInvalidate(coll)           // باطل‌سازی (خودکار از applyOp)
idxReset()                    // پاک‌سازی کامل
idxReport()                   // آمار hit/build برای تشخیص

// ایندکس‌های آماده:
idxById(coll) · idxEnrollByClass() · idxEnrollByStudent() · idxAttByClassDate()
idxAttByStudent() · idxGradesByStudent() · idxScheduleByTeacher() · idxClassesBySchool()

sortByNameFa(list)  // مرتب‌سازی فارسی با Intl.Collator یک‌بارساخته
```

**اثر:** `viewAttendance` در مدرسهٔ ۵٬۰۰۰ نفره از ۶٬۴۲۲ms به **۲ms** رسید (۳٬۲۱۱ برابر).

### ۸.۵ لایه محدودهٔ داده — `29-scope.js`

```js
scopeDescriptor(user, persona)  // دقیقاً چه داده‌ای مجاز است
scopeEstimate(user, persona)    // برآورد حجم برش
scopeHealth()                   // آیا از سقف امن عبور کرده؟
bootstrapRequest()              // ساخت درخواست /api/bootstrap
SCOPE_LIMITS = { maxRecords:60000, maxBytes:8MB, attendanceDays:45, gradesTerms:2, pageSize:50 }
```

**قرارداد سرور:**
```
GET /api/bootstrap → { scope, data:{...}, cursor }
```
سرور موظف است محدوده را از **توکن** تعیین کند (نه از پارامتر کلاینت)، هرگز دادهٔ خارج از محدوده نفرستد، و فهرست‌های بزرگ را صفحه‌بندی کند.

### ۸.۶ محافظت از حافظهٔ محلی

`saveLog()` دیگر خطا را بی‌صدا نمی‌بلعد: هشدار در ۸۰٪ ظرفیت، پیام صریح هنگام پرشدن، و تابع `storageFull()`.

ظرفیت آفلاین: هر عملیات ≈ ۴۲۳ بایت ⇒ سقف ۵MB ≈ ۱۲٬۳۸۶ عملیات ≈ **۴۱۲ روز** کار یک دبیر با ۳۰ دانش‌آموز.

## ۹. الگوهای کدنویسی — رعایت اجباری

### امنیت خروجی
```js
`<div>${esc(user.name)}</div>`   // ✅
`<div>${user.name}</div>`        // ❌ XSS
```

### رویدادها
```js
`<button data-act="save-user" data-id="${u.id}">ذخیره</button>`
// و در 19-actions.js مدیریت شود — نه onclick
```

### تاریخ
```js
const iso = jalaliToIso(input.value);    // ورودی → ذخیره
el.textContent = isoToJalali(rec.date);  // ذخیره → نمایش
```

### تغییر داده
```js
insert('grades', {...});   // ✅ وارد لاگ و صف sync می‌شود
db.grades.push({...});     // ❌ ذخیره نمی‌شود، به سرور نمی‌رود
```

### اعداد و رشته‌ها — ⚠️ تله‌ی رایج
```js
fa(1234)          // ✅ '۱٬۲۳۴'  — فقط برای عدد
fa('۱۴۰۵/۰۶/۱۰')  // ❌ 'ناعدد' — Number() روی رشته NaN می‌دهد
faD('12:30')      // ✅ '۱۲:۳۰'  — برای رشته از faD استفاده کن
```

### جستجو در داده — ⚠️ قاعدهٔ مقیاس
```js
// ❌ هرگز: find خطی داخل map — هزینه = n×m
studs.map(s => db.attendance.find(a => a.student_id===s.id))

// ✅ همیشه: یک ایندکس، سپس O(1)
const m = idxAttByClassDate().get(cid+'|'+date) || [];
const day = new Map(); m.forEach(a => day.set(a.student_id, a));
studs.map(s => day.get(s.id));
```
ایندکس‌ها خودکار باطل می‌شوند؛ نگرانی دادهٔ کهنه وجود ندارد.

### محدودهٔ داده
```js
// دادهٔ خارج از محدودهٔ نقش نباید اصلاً به مرورگر برسد.
// محدوده را در scopeDescriptor تعریف کن، نه با فیلتر سمت نمایش.
```

### فیلترها
```js
${filterPanel('routeKey', `<select data-f="myfilter">…</select>`)}
// و کلیدها را در FILTER_KEYS ثبت کن (فایل 25-filters.js)
```

### استایل
```css
color: var(--primary);   /* ✅ نه #1668f0 */
```

---

## ۹.۵ نوشتن دسته‌ای (`batchWrites`) ⭐

**مسئله‌ای که حل می‌کند:** `saveLog()` و `saveQueue()` هر بار **کل** لاگ/صف را
`JSON.stringify` می‌کنند. در عملیات انبوه این رفتار **درجه‌دوم** می‌شود:

| طول لاگ | ۱۰۰ insert |
|---|---|
| ۱۰۶ | ۲۰ms |
| ۱٬۱۰۰ | ۱۰۶ms |
| ۴٬۱۰۰ | ۴۶۰ms |

**قاعده:** هر عملیاتی که بیش از ~۵۰ نوشتن دارد باید در `batchWrites` بپیچد.

```js
batchWrites(() => {
  students.forEach(s => update('users', s.id, { grade_level: g + 1 }));
});
// یک saveLog + یک saveQueue در پایان، نه هزاران بار
```

**ویژگی‌ها:** تودرتو امن است (شمارندهٔ عمق)، در برابر استثنا امن است
(`finally`)، و هیچ عملیاتی گم نمی‌شود — لاگ و صف در پایان کاملاً همگام‌اند.

**بهینه‌سازی مرتبط در `applyOp`:** ترتیب باطل‌سازی ایندکس مهم است — ابتدا با
ایندکسِ معتبر جستجو کن، **سپس** باطل کن. در غیر این صورت `idxById` بلافاصله کل
مجموعه را بازمی‌سازد و هر نوشتن از درجهٔ n می‌شود.

---

## ۱۰. تست

```bash
npm test                 # هر دو مجموعه (۳۱ + ۳۲۹)
node tests/run.js        # ۳۱ تست ساختاری
node tests/smoke.js      # ۳۲۹ آزمون رفتاری (نیازمند jsdom)
```

**`tests/run.js`** — ساختار، ثبت ماژول‌ها، صحت build (بیت‌به‌بیت)، نبود ارجاع خارجی، تطابق route↔تابع، سلامت نحوی

**`tests/smoke.js`** — اجرای واقعی در jsdom: رندر همه صفحات × همه نقش‌ها، برنامه درسی، فیلترها، ۱۳ تست offline-sync، **۱۶ تست ایندکس/محدوده/حافظه**، ، **۱۰ تست مجوزدهی**، **۹ تست چرخه تحصیلی**، **۳ تست batchWrites** ، **۷ تست تعامل با خانواده**، **۹ تست فرم و پیامک** ، **۱۰ آزمون ویزارد ورود** ، **۸ آزمون پنل سوپرادمین** ، **۱۰ آزمون سابقه و فعالیت** ، **۷ آزمون سازگاری با فایل واقعی مدرسه** ، **۶ آزمون ابزارهای تکمیلی** ، **۹ آزمون پلان و پشتیبان** ، **۶ آزمون اشتراک مشترک** ، **۱۰ آزمون چرخهٔ سال تحصیلی** و **۹ آزمون اعتبارسنجی و نوع کلاس**

⚠️ **نکته‌ی مهم برای نوشتن تست:** متغیرهای `const` سطح‌بالا روی `window` نمی‌نشینند.
باید از `win.eval('عبارت')` استفاده شود، و هر `eval` را جداگانه صدا بزنید (نه یک رشته‌ی چندعبارتی).

---

## ۱۱. حساب‌های دمو

رمز همه: `123456`

| نام کاربری | نقش |
|---|---|
| `superadmin` | مدیر کل سامانه |
| `edu_kurdistan` | کارشناس اداره کل استان |
| `edu_baneh` | کارشناس اداره شهرستان |
| `manager1` | مدیر مدرسه |
| `teacher1_1` | دبیر |
| `student1` | دانش‌آموز |
| `parent_multi` | ولی با ۳ فرزند در ۳ مدرسه مختلف |

---

## ۱۲. دستورات

```bash
npm run dev          # سرور توسعه پورت ۳۰۰۰ + بازسازی خودکار
npm run build        # ساخت index.html + dist/payesh.html
npm run build:check  # تأیید یکسانی بیت‌به‌بیت
npm test             # ۳۱ + ۳۲۹ آزمون
```

---

## ۱۲.۵ دو نسخه و فایل مرجع

پروژه **دو نسخه** دارد و قاعدهٔ کاربر این است که هر قابلیت در هر دو پیاده شود.

### الف) نسخهٔ سروری — `github.com/rezaa2544/payesh`

| بخش | جزئیات |
|---|---|
| فرانت | React 19 + Vite 8 + React Router 7 — ۳۶ صفحه `.jsx` |
| بک‌اند | Express 5 + better-sqlite3/Postgres — ۳۳ فایل، ~۱۴٬۶۰۰ خط |
| امنیت | bcrypt، JWT + refresh چرخشی، helmet + CSP، zod، rate limit، `audit_log` |
| زیرساخت | Docker، nginx، GitHub Actions، PgBouncer، Redis |
| تست | ۱۶ فاز + bench + ui-smoke + mobile + standalone (۴۱۰+ سناریو) |

### ب) فایل مرجع تک‌فایلی — ✅ کارش تمام شد و حذف شد

نسخهٔ مرجع (۳۶ تابع `view*`) از مخزن استخراج و **همهٔ قابلیت‌هایش پیاده
شد**. پس از تأیید اینکه هیچ تابعی جا نمانده، فایل از پروژه حذف شد تا
حجم مخزن کم بماند. نسخهٔ پشتیبانش در `/home/user/backup/` است.

### ✅ همهٔ شش صفحهٔ غایب پیاده شدند

| صفحه | ماژول | بخش سند |
|---|---|---|
| چرخهٔ تحصیلی | `31-student-lifecycle.js` | ۵.۵ |
| افت تحصیلی · جلسات اولیا · رشد مدرسه | `32-atrisk-meetings-growth.js` | ۵.۶ |
| فرم‌های رسمی · پنل پیامک | `33-forms-sms.js` | ۵.۷ |
| ویزارد ورود اکسل | `34-excel-import.js` | ۵.۸ |

نسخهٔ تک‌فایلی اکنون **همهٔ ۳۶ صفحهٔ** نسخهٔ سروری را دارد، به‌علاوهٔ شش قابلیتی
که فقط در نسخهٔ تک‌فایلی هست (فیلترهای جمع‌شونده، کتاب‌های درسی، همگام‌سازی
برون‌خط، لایهٔ ایندکس، محدودهٔ داده، لایهٔ مجوزدهی).

### ⚠️ هشدار مهم هنگام انتقال

فایل مرجع **سه مشکل کارایی** دارد که در نسخهٔ من رفع شده‌اند:

| مشکل | مرجع | نسخهٔ من |
|---|---|---|
| جستجوی خطی در `viewAttendance` | دارد | رفع شد (ایندکس `Map`) |
| ۱۴ پیمایش کامل در داشبورد | دارد | رفع شد (یک پیمایش) |
| `saveLog` خطا را بی‌صدا می‌بلعد | دارد | رفع شد (هشدار) |

**پس کد را کپی مستقیم نکن** — با الگوی ایندکس‌شده (`idxGroup`/`idxUnique`) بازنویسی کن.

### ترتیب پیشنهادی افزودن
همه انجام شد: چرخهٔ تحصیلی ← جلسات و رشد و افت ← فرم و پیامک ← ورود اکسل.

---

## ۱۲.۶ زمان‌بندی ساخت سرور — تصمیم ثبت‌شده

**پرسش کاربر:** «هنوز باید خیلی جاها را ویرایش کنیم؛ صبر کنیم یا الان سرور لازم است؟»

**تصمیم: فعلاً سرور ساخته نمی‌شود.** دلیل، با شواهد:

| شاهد | یافته |
|---|---|
| رشد مدل داده | سه دور اخیر هر کدام مجموعهٔ دادهٔ تازه آوردند (اکنون ۳۹ مجموعه) |
| نقاط نوشتن | ۵۲ فراخوانی درج/ویرایش/حذف که منطقشان هنوز در حال تغییر است |
| هزینهٔ تأخیر | تقریباً صفر، چون قالب همگام‌سازی **عمومی** است |

**چرا هزینهٔ تأخیر صفر است؟** قالب عملیات به نام مجموعه گره نخورده:
`{t, c, data}` که `t` نوع عمل و `c` نام مجموعه است. افزودن مجموعهٔ جدید
هیچ تغییری در لایهٔ همگام‌سازی لازم ندارد.

### سه قراردادی که قفل شده‌اند و نباید عوض شوند
۱. قالب `POST /api/sync` با `{ops:[...]}` و پاسخ `{results:[...]}`
۲. قرارداد `GET /api/bootstrap` با محدوده از توکن سرور
۳. جدول نقش‌ها در `30-authz.js` به‌عنوان تک‌منبع حقیقت

### شرط‌های شروع ساخت سرور
دو دور پیاپی بدون مجموعهٔ دادهٔ جدید · تثبیت فیلدهای رکوردهای اصلی ·
پایان ویرایش‌های ظاهری · **بازخورد یک مدرسهٔ واقعی**

جزئیات کامل: `docs/SERVER_TIMING.md`

---

## ۱۲.۷ خلأهای شناخته‌شدهٔ پنل سوپرادمین

بررسی عملی در نقش سوپرادمین انجام شد. جزئیات: `docs/SUPERADMIN_GAPS.md`

**نکتهٔ مهم که نباید اشتباه فهمیده شود:** سوپرادمین در فهرست روت‌های مجازش
حضور و غیاب، نمرات و کلاس‌ها را ندارد. این **عمدی و درست است** — او با
دکمهٔ «ورود به مدرسه» (جانشینی مدیر) به همهٔ آن‌ها می‌رسد. آن را «خلأ»
نشمار و روت‌ها را مستقیم به او نده.

### خلأهای واقعی، به ترتیب اهمیت

> ✅ سه مورد نخست پیاده شدند (بخش ۵.۹). جدول زیر وضعیت به‌روز است.

| # | خلأ | شدت |
|---|---|---|
| ۱ | ~~داشبورد عدد مالی ندارد~~ → **انجام شد:** روت `finance` | ✅ |
| ۲ | ~~بازنشانی رمز ممکن نیست~~ → **انجام شد:** اکشن `pass-reset` | ✅ |
| ۳ | ~~صفحهٔ سلامت نیست~~ → **انجام شد:** روت `health` | ✅ |
| ۴ | ~~سابقهٔ تغییرات نیست~~ → **انجام شد:** روت `audit` | ✅ |
| ۵ | ~~وضعیت و فعالیت مدارس~~ → **انجام شد** | ✅ |
| ۶ | ~~اطلاعیهٔ سراسری~~ → **انجام شد:** `ann-broadcast` | ✅ |
| ۷ | ~~خروجی اکسل نیست~~ → **انجام شد:** `export-csv` | ✅ |
| ۸ | ~~مدیریت پلان فروش~~ → **انجام شد:** روت `plans` | ✅ |
| ۹ | ~~پشتیبان‌گیری و بازیابی نیست~~ → **انجام شد** | ✅ |

---

## ۱۳. نقشه راه

**انجام‌شده:** ۶ نقش · ۲۹ صفحه · تقویم شمسی · مالی · امتحانات · تقسیمات کشوری · چندنقشی · اشتراک · فیلترهای جمع‌شونده · برنامه درسی · لایه offline-first · **لایه ایندکس** · **محدودهٔ داده** · **محافظت حافظه** · **لایه مجوزدهی** · **چرخهٔ تحصیلی** · **افت تحصیلی/جلسات/رشد** · **فرم و پیامک** · **ورود اکسل**

**در انتظار:**
- [ ] بک‌اند واقعی + اتصال `SYNC.demoMode=false`
- [ ] UI حل تعارض (انتخاب نسخه محلی یا سرور)
- [ ] Service Worker — تا خود برنامه هم بدون اینترنت بالا بیاید
- [x] `lifecycle` — چرخهٔ تحصیلی (`31-student-lifecycle.js`)
- [x] `atrisk` · `meetings` · `growth` (`32-atrisk-meetings-growth.js`)
- [x] `formssms` (`33-forms-sms.js`)
- [x] `import` (`34-excel-import.js`) — **همهٔ صفحات مرجع تکمیل شد**
- [ ] پیاده‌سازی `/api/bootstrap` مطابق `scopeDescriptor`
- [x] **پنل سوپرادمین:** داشبورد مالی · بازنشانی رمز · سلامت سامانه (`35-superadmin.js`)
- [x] سوپرادمین: سابقهٔ تغییرات · بازدید و بار سامانه (`36-audit-activity.js`)
- [x] سوپرادمین: وضعیت مدارس · خروجی اکسل · اطلاعیهٔ سراسری (`37-admin-tools.js`)
- [x] سوپرادمین: مدیریت پلان فروش · پشتیبان‌گیری و بازیابی (`38-plans-backup.js`)

**همهٔ نُه خلأ پنل سوپرادمین برطرف شد.**
- [ ] ورود دومرحله‌ای (OTP) برای مدیر/سوپرادمین
- [ ] اعمال مستقل همین قواعد مجوزدهی سمت سرور
- [ ] `audit_log` برای عملیات حساس
- [ ] هش رمز عبور و احراز هویت واقعی
- [ ] IndexedDB به‌جای localStorage (سقف ۵MB → صدها مگابایت)
- [ ] گزارش‌گیری پیشرفته و خروجی Excel/PDF

---

## ۱۴. اشتباهات رایجی که نباید تکرار شوند

| ❌ اشتباه | ✅ درست |
|---|---|
| ویرایش `index.html` | ویرایش `src/` سپس `npm run build` |
| افزودن ماژول بدون ثبت در `_order.json` | همیشه ثبت شود، در جای درست ترتیب |
| `fa()` روی رشته | `faD()` برای رشته، `fa()` برای عدد |
| `db.x.push()` | `insert('x', {...})` |
| فرض «آفلاین = بدون CDN» | آفلاین = **کار بدون اینترنت + همگام‌سازی بعدی** |
| افزودن CDN/کتابخانه | صفر وابستگی — دستی بنویس |
| نام متغیر عمومی مثل `f`, `s`, `t` در ماژول جدید | تصادم با helperهای موجود — پیشوند بگذار |
| `find` خطی داخل `map` در مسیر رندر | ابتدا ایندکس بساز (`idxGroup`/`idxUnique`) |
| فرستادن کل دیتابیس به مرورگر | فقط برش نقش — `scopeDescriptor` |
| بلعیدن خطای `localStorage` | کاربر باید از پرشدن حافظه باخبر شود |
| افزودن ایندکس بدون باطل‌سازی | `applyOp` خودکار انجام می‌دهد؛ دستکاری نکن |
| افزودن روت بدون ثبت در `NAV`/`EXTRA_ROUTES` | گارد مجوز آن را مسدود می‌کند |
| عملیات انبوه بدون `batchWrites` | رفتار درجه‌دوم؛ رابط چند ثانیه قفل می‌شود |
| باطل‌کردن ایندکس پیش از جستجو در `applyOp` | باعث بازسازی کامل در هر نوشتن می‌شود |
| انتقال دانش‌آموز بدون به‌روزرسانی `school_id` رکوردهای پرونده | پرونده در مدرسهٔ جدید دیده نمی‌شود |
| نوشتن `\n` داخل رشتهٔ آزمون در `tests/smoke.js` | به خط جدید تبدیل نمی‌شود؛ از `String.fromCharCode(10)` استفاده کن |
| جستجوی کاربر با نام در آزمون | دادهٔ نمونه نام تکراری دارد؛ با کد ملی یا شناسه جستجو کن |
| نام‌گذاری متغیر محلی `f` داخل تابع نما | تابع سراسری سازندهٔ فیلد را می‌پوشاند؛ نام دیگری بگذار |
| فرض اینکه فایل مدرسه با قالب ما یکی است | فایل واقعی «نام» و «نام خانوادگی» جدا دارد و معدل را با اسلش می‌نویسد |
| ساخت ظرف اسکرول‌دار بدون تنظیم جهت | نوارش سمت چپ می‌ماند (بخش ۵.۱۹) |
| افزودن فایل حجیم به مخزن | حتی پس از حذف، در تاریخچه می‌ماند (بخش ۵.۲۰) |
| تنظیم جهت فقط روی `html` برای نوار صفحه | کافی نیست؛ اسکرول باید از `body` به `.main` منتقل شود |
| استفاده از `toast` خام برای خطای فیلد | کاربر نمی‌فهمد کدام فیلد؛ از `need`/`needAll`/`invalid` استفاده کن |
| ارتقای پایه بدون چیدمان کلاس | دانش‌آموز در کلاس پارسالش می‌ماند (بخش ۵.۱۶) |
| تقسیم ساده دانش‌آموزان میان کلاس‌ها | یک کلاس همهٔ قوی‌ها را می‌گیرد؛ از روش مارپیچ استفاده کن |
| فرض اینکه کلاس اول همیشه دانش‌آموز دارد | آزمون‌های دیگر ممکن است چیدمان را عوض کرده باشند |
| ثابت‌گرفتن «دبیر نخست مدرسهٔ نخست» در محیط آزمون | پایان‌سال کلاس‌های مدرسهٔ نخست را خالی می‌کند و گسترش ۶ زنگ نگاشت دبیر و کلاس را عوض کرد (دور ۵۱) — محیط را بسنجید و برگردانید |
| فراموش‌کردن اینکه اشتراک به **دانش‌آموز** تعلق دارد نه ولی | پدر و مادر نباید دوبار بپردازند (بخش ۵.۱۴) |
| صدا زدن `subOf(id,true)` برای بررسی وضعیت | خودش دورهٔ آزمایشی می‌سازد؛ برای بررسی از `false` استفاده کن |
| تغییر آرایهٔ `db` بدون باطل‌کردن ایندکس | رکوردها نامرئی می‌شوند (بخش ۵.۱۵) |
| ساخت قابلیت پشتیبان‌گیری بدون بازیابی | نیمی از کار بی‌فایده است؛ همیشه هر دو سو را بساز |
| ساخت csv بدون پیشوند BOM | اکسل فارسی را به‌هم‌ریخته نشان می‌دهد |
| ساخت csv بدون خنثی‌سازی `=` و `+` | تزریق فرمول در اکسل قربانی ممکن می‌شود |
| نمایش ندادن فیلدهای وارد‌شده | داده‌ای که جایی دیده نشود بی‌فایده است؛ تب یا ستون برایش بساز |
| فرض اینکه تابعی وجود دارد بدون بررسی | پیش از استفاده `grep` بزن؛ مثلاً `shortStamp` وجود نداشت و باید نوشته می‌شد |
| تشخیص XSS با جستجوی متنی در خروجی | مثبت کاذب می‌دهد؛ DOM واقعی را بررسی کن |
| اتکا به `NAV` به‌عنوان کنترل دسترسی | `NAV` فقط منو است؛ مجوز با `canRoute`/`canAction` |
| تست با یک `eval` چندعبارتی | هر عبارت جدا |

---

## ۱۵. خلاصه‌ی یک‌پاراگرافی

> «پایش» یک سامانه‌ی مدیریت مدرسه‌ی فارسی/RTL است که با Vanilla JS و صفر وابستگی
> نوشته شده و به‌صورت یک فایل HTML تک‌فایلی (~۵۰۰KB) توزیع می‌شود، اما ماژولار در
> `src/` (۳۰ ماژول) توسعه می‌یابد و با یک اسکریپت الحاق ساده build می‌شود که
> یکسانی بیت‌به‌بیت خروجی را تضمین می‌کند. شش نقش (سوپرادمین، اداره، مدیر، دبیر،
> دانش‌آموز، ولی) با **سه** لایه جدا شده‌اند: منو (`NAV`)، مجوز (`30-authz.js`) و
> فیلتر داده (`04-queries.js`). داده در ۳۰ مجموعه با الگوی event-log روی
> localStorage نگه داشته می‌شود. مهم‌ترین قابلیت محصول **offline-first** است:
> دبیر بدون اینترنت حضور و غیاب می‌زند و تغییرات با backoff نمایی به سرور می‌روند.
> سامانه برای **مقیاس ملی** طراحی شده (۱۰ میلیون دانش‌آموز، ۵ میلیون کاربر همزمان)
> بر پایهٔ این اصل که **بار هر مرورگر ثابت و مستقل از اندازهٔ کشور بماند**:
> لایهٔ ایندکس (`28-indexes.js`) جستجوی خطی را از مسیرهای داغ حذف کرده و لایهٔ
> محدوده (`29-scope.js`) تضمین می‌کند سرور فقط برش مجاز هر نقش را بفرستد.
> در ممیزی امنیتی دور ۵ یک **باگ بحرانی** یافت و رفع شد: `renderRoute()` هیچ
> بررسی مجوزی نداشت و دانش‌آموز می‌توانست با تغییر `S.route` صفحهٔ حضور و غیاب را
> باز کند و برای هم‌کلاسی‌اش غیبت جعلی ثبت کند که وارد صف سرور هم می‌شد.
> پروژه فعلاً **نسخهٔ دمو بدون بک‌اند** است؛ امنیت واقعی (هش رمز، احراز هویت،
> اعمال مستقل مجوز و محدوده سمت سرور) باید هنگام اتصال سرور پیاده شود.
> بنچمارک: بزرگ‌ترین مدرسهٔ ایران (۱٬۵۲۰ نفر، ۲۷۳ هزار رکورد حضور) = ۵۴ms کل رندر؛
> ۱٬۵۰۰ رندر پیاپی = ۰ خطا و ۱.۳MB رشد حافظه. آزمون: ۳۱ ایستا + ۳۲۹ رفتاری.


### ۰.۵.۱۲ بازسنجی ظرفیت روی کد فعلی (دور ۴۱)

`docs/CAPACITY_SIM.md` با ستون‌های «قبل / بعد» بازسنجی شد.

| محور | قبل | بعد |
|---|---|---|
| ۸ صفحه با ۲٬۰۰۰ دانش‌آموز | ۴۹ms | ۵۳ms |
| ورود ۲٬۰۰۰ ردیف اکسل | ۲٬۷۱۹ms | **۱۲۹ms** |
| ورود ۴٬۰۰۰ ردیف | — | ۲۵۹ms |
| اصابت ایندکس | ۹۹٫۷٪ | ۹۹٫۳٪ |

**سنجش محدودهٔ امنیتی:** بررسی شد که `byId` هنوز فیلتر `school_id`
ندارد (۱۲۴ نویسه، بدون اشاره به `school_id`) ⇒ اعداد همچنان
«پیش از سنجش محدوده»‌اند. هزینهٔ پایهٔ `byId` = **۰٫۷۶µs** هر
فراخوان؛ مبنای مقایسهٔ روز اتصال سرور.

**رفتار درجه‌دومی تازه: سنجیده شد، وجود ندارد.** `separateGender`
+۲٫۴٪ · `keepSiblings` −۱٪ (نوسان). نسبت‌های رشد ۳٫۷× به‌ازای ۴×
داده و ۲٫۰× به‌ازای ۲× داده ⇒ خطی.

⚠️ **دام:** بنچمارک توزیع سه بار با `auto:0` سبز شد — مسیر توزیع
اجرا نشده بود. همیشه شمارندهٔ خروجی را کنار زمان چاپ کنید.


### ۰.۵.۱۳ جمع‌بندی موارد باز (دور ۴۱)

سند تازه: **`docs/OPEN_ITEMS.md`** — تنها نقطهٔ مرور وضعیت باز.

هر ادعا با گرپ روی کد امروز وارسی شد، نه از روی حافظهٔ سند. نمونه:
ادعا شده بود bcrypt «باز» است؛ بررسی نشان داد `bcrypt` در دو فایل
هست ولی **فقط داخل کامنت** — یعنی واقعاً باز است.

خلاصه: چهار مسدودکننده باز (عمدی، فاز دمو) · شش مهم باز و دو
تصمیم‌گرفته · سه آزمون IDOR عمداً وارونه · دو مورد بی‌تصمیم (رضایت
والدین مهلتش زودتر از اتصال سرور است) · حجم داده ۴٫۶۴MB نزدیک سقف.

چهار ادعای «✅ انجام شده» بازسنجی شدند و همه سالم‌اند: صفر تخطی از
لایهٔ داده، صفر دادهٔ واقعی، `esc`/`escAttr` سرجا، ۸۲/۸۲ صفحه.


### ۰.۵.۱۴ اطلاع‌رسانی پیامکی — گام ۱: هستهٔ صف (دور ۴۲)

ماژول تازه **`src/js/44-sms-notify.js`** (۴۵ ماژول شد). طرح کامل:
`docs/SMS_NOTIFY_PLAN.md`.

توابع: `notifySettings`/`notifySaveSettings` · `notifyRequest` ·
`notifyCancelIfFresh` · `notifySend`/`notifyReject` ·
`notifyEstimate`/`notifySentToday` · `notifyPurge` · `notifyPending`
· `notifyHash` · `notifyParentsOf` · `minutesSince`.
جدول تازه `notify_queue`. تنظیمات در `schools.notify_rules`
(الگوی `place_rules`).

**دو یافتهٔ سنجش که طراحی را عوض کرد:**
۱. رکورد صف **۴۳۶ بایت** است نه ۲۵۰ که برآورد شده بود (۱٫۷×) ⇒
   فیلدهای `null` ذخیره نمی‌شوند و پاک‌سازی ۳۰ روزه الزامی شد.
۲. قالب غیبت با «حاضر نبود» ۷۴ نویسه = **۲ قطعه** بود؛ با «غایب
   بود» شد ۶۴ نویسه = **۱ قطعه**. چون غیبت پرتکرارترین پیام است،
   این تغییر کوچک نیمی از هزینهٔ پیامک مدرسه را حذف می‌کند.

**قاعدهٔ طلایی این ماژول:** تنها دروازهٔ ساخت پیامک خودکار است.
هیچ ماژول دیگری نباید مستقیم در `sms_log` بنویسد، وگرنه سقف روزانه
و کسر اعتبار دور زده می‌شود.

⚠️ **پنجرهٔ مهلت سه شرط `&&` دارد** (pending · درون بازه · همان
کاربر). با `||` مدیر می‌توانست بی‌ردپا پیام را محو کند.

⚠️ **اعتبار ناکافی پیام را حذف نمی‌کند** — `pending` می‌ماند.

📄 ۱۵ آزمون تازه؛ **۳۴۷/۳۴۷**.


### ۰.۵.۱۵ اطلاع‌رسانی پیامکی — گام ۲: صفحهٔ صف مدیر (دور ۴۲)

بخش ۹ به `44-sms-notify.js` افزوده شد: `viewNotifyQueue` ·
`notifyAutoBanner` · `notifyCostCard` · `notifyDailyCard` ·
`notifyKindTag` · `notifyClock` · `notifyPicked` · `NOTIFY_KIND_FA`.

کنش‌ها در `19-actions.js`: `notify-filter` · `notify-pick-all` ·
`notify-approve`/`-sel` · `notify-reject`/`-sel` · `notify-edit` ·
`notify-save-edit` · `notify-settings` · `notify-save-settings` ·
`notify-auto-off` · کمکی `_notifyApprove`.

مسیر تازه **`notifyqueue`**: `NAV.manager` + `TITLES` +
`_renderRouteInner`. صفحه‌ها **۸۲ → ۸۳** شد.

**سه تصمیم رابط که سنجیده شدند، نه فرض:**

۱. **نوار هشدار خودکار در `renderShell` است نه در صفحهٔ صف.**
   سنجش: در ۶/۶ صفحهٔ مدیر دیده می‌شود. دلیل: مدیری که سراغ صف
   نمی‌رود، دقیقاً همان کسی است که خودکار را روشن کرده.

۲. **`parts` پس از ویرایش مدیر دوباره حساب می‌شود.** ویرایش ممکن
   است پیام را از ۱ به ۲ قطعه ببرد؛ بدون محاسبهٔ دوباره، هزینه
   کمتر از واقع کسر می‌شد.

۳. **روشن‌کردن `autoSend` تأیید صریح `askConfirm` می‌خواهد** —
   خاموش‌کردنش نه. تصمیم پرریسک نباید با یک تیک بی‌توجه انجام شود.

⚠️ **مجوز:** ۹ کنش تازه در `ACTION_ROLES` فقط `manager`/`superadmin`.
دبیر پیام **می‌سازد** ولی حق **تأییدش** را ندارد — همان تفکیکی که
کل مکانیزم بازبینی بر آن بنا شده است.

📄 ۱۰ آزمون تازه (**۳۵۷/۳۵۷**) و **۵ جهش عمدی، هر پنج گرفته شد**.


### ۰.۵.۱۶ اطلاع‌رسانی پیامکی — گام ۳: پیش‌نویس حضور و غیاب (دور ۴۲)

🔴 **تغییر رفتار، نه افزودن قابلیت.** پیش از این `att-set` هر تیک
را بی‌درنگ `insert` می‌کرد. حالا تیک‌ها در پیش‌نویس جمع می‌شوند و
با `att-commit` یک‌جا نوشته می‌شوند.

بخش ۱۰ ماژول ۴۴: `attDraftKey`/`attDraftGet`/`attDraftSet`/
`attDraftSetAll`/`attDraftClear`/`attDraftPurge`/`attDraftAll`/
`attDraftDiff`. کنش‌های تازه: `att-review` · `att-commit` ·
`att-discard` · `att-tip-ok`.

**کلید پیش‌نویس `uid|cid|date` است — هر سه جزء لازم‌اند:**
| جزء | اگر نباشد |
|---|---|
| `uid` | دبیر جانشین پیش‌نویس نیمه‌کارهٔ دبیر اصلی را می‌بیند و ثبت می‌کند |
| `cid` | تعویض کلاس پیش‌نویس‌ها را در هم ادغام می‌کند |
| `date` | دو روز مختلف روی هم می‌افتند |

هر سه با آزمون جهش تأیید شدند.

**راهنمای تغییر رفتار (`attChangeTip`)**: دبیری که با روش قدیم کار
کرده، با دیدن صفحهٔ تازه گمان می‌کند تیک‌هایش ذخیره نشده. یک‌بار
توضیح داده می‌شود و با «متوجه شدم» (کلید `ATT_TIP_KEY` در `Store`،
به‌ازای هر کاربر) دیگر نمی‌آید.

**زنجیرهٔ ثبت:** `att-commit` → نوشتن در `attendance` →
`notifyRequest` با `source_ref` = شناسهٔ **واقعی** رکورد →
`notifyCancelIfFresh` برای اصلاح درون پنجره.
⚠️ ترتیب مهم است: پیامک **پس از** نوشتن ساخته می‌شود وگرنه
`source_ref` به رکورد ناموجود اشاره می‌کند.

⚠️ **`attDraftDiff` فقط تفاوت واقعی برمی‌گرداند.** تیکی که با
وضعیت فعلی یکی است شمرده نمی‌شود، وگرنه مرور نهایی «۳۰ تغییر»
نشان می‌داد که ۲۷تایش هیچ بود.

📄 ۱۲ آزمون تازه (**۳۷۰/۳۷۰**) · ۹ جهش عمدی.


### ۰.۵.۱۷ اطلاع‌رسانی پیامکی — گام ۵: اصلاحیهٔ خودکار (دور ۴۲)

بخش ۱۱ ماژول ۴۴: `notifyLastSent` · `notifyPendingCorrection` ·
`notifyReconcile` · `notifyReconcileMany`. در `att-commit` پس از
`notifyCancelIfFresh` صدا زده می‌شود.

**تصمیم محوری: مبنا «آخرین پیام ارسال‌شده» است، نه «پیام اصلی».**
اگر دبیر غایب→حاضر→غایب کند، خانواده دو پیام گرفته. با مبنا قرار
دادن پیام اصلی، وضعیت فعلی با آن یکی می‌شد و هیچ اصلاحیه‌ای ساخته
نمی‌شد — خانواده با آخرین اطلاعِ **غلط** («غایب نبوده») می‌ماند.

**چهار وضعیت `notifyReconcile`:**
| خروجی | چه وقت |
|---|---|
| `none` | پیامی نرفته، یا وضعیت همان است که خانواده می‌داند |
| `created` | اصلاحیهٔ تازه ساخته شد |
| `cancelled` | وضعیت برگشت ⇒ اصلاحیهٔ معلق لغو شد |
| `updated` | اصلاحیهٔ معلق با متن تازه به‌روز شد |

⚠️ **سه دام که هر سه با جهش تأیید شدند:**
۱. **حلقه** — `correction_of` نشان می‌گذارد؛ ۶ وارسی پیاپی صفر
   رکورد اضافه می‌کند.
۲. **رفت‌وبرگشت** — اصلاحیهٔ معلق لغو می‌شود، دومی ساخته نمی‌شود.
۳. **پیام معلق** — تا پیام نرفته اصلاحیه بی‌معناست؛ کار پنجرهٔ مهلت
   است.

⚠️ **گارد پیام تکراری در `att-commit`:** اگر برای رکوردی پیام رفته،
ثبت دوبارهٔ همان وضعیت **پیام نو نمی‌سازد** — وگرنه خانواده دو بار
خبر یکسان می‌گیرد. با `notifyLastSent` بررسی می‌شود.

📄 ۱۰ آزمون تازه (**۳۸۰/۳۸۰**) · ۷ جهش عمدی، هر هفت گرفته شد.

### ۰.۵.۱۸ اطلاع‌رسانی پیامکی — گام ۷: نمره و رویداد (دور ۴۲)

دو منبع تازه به دروازهٔ صف وصل شد (بخش ۱۲ و ۱۳ ماژول ۴۴):

**نمره — بخش ۱۲:** `notifyGradeSync` · `notifyGradeLastSent` ·
`notifyPendingGrade` · `notifyPendingGradeCorrection` ·
`notifyGradeCancel` · `notifyGradeCorrection`. قلاب: `grade-save`
(19-actions.js) پس از نوشتن (تا `source_ref` شناسهٔ واقعی باشد) و
`grade-del-ok` برای لغو پیام معلق.

**رویداد — بخش ۱۳:** `notifyEvent(schoolId, text)` یک رکورد صف
به‌ازای هر دانش‌آموزِ دارای ولی می‌سازد. قلاب: `cal-save`
(20-communication-finance.js) فقط برای رویدادِ **تازهٔ** تقویم
(`kind='event'`) — نه ویرایش، نه امتحان/تعطیلی.

**سه تصمیم سنجیده‌شده:**
۱. **هش نمره باگ داشت و اصلاح شد** — `notifyHash('grade')` از
   `g.exam_id` استفاده می‌کرد که در رکورد `grades` وجود ندارد؛
   فیلدهای واقعی `subject_id|term|exam_type|score` جایگزین شد.
۲. **عدد نمره هرگز در پیام نیست** ⇒ تا وقتی نمره زیر آستانه بماند،
   تغییر عدد اصلاحیه نمی‌خواهد؛ فقط عبور از زیر آستانه به بالای آن
   (پس از ارسال) اصلاحیه می‌سازد. قالب `gradeCorrection` هم عدد را
   بازگو نمی‌کند.
۳. **رویداد فقط `kind='event'` تقویم** — یک رکورد به‌ازای هر
   دانش‌آموز، نه یک رکورد مشترک، تا صف مدیر کلاس و دانش‌آموز هر
   ردیف را نشان دهد.

⚠️ **کارایی رویداد — سنجش واقعی، نه برآورد:** بدون `batchWrites` هر
`insert` جداگانه `saveLog` می‌کرد (JSON.stringify کل دفترچهٔ تغییرات)
⇒ **۱۲۷ms** برای ۱۲۷ دانش‌آموز؛ داخل یک `batchWrites` یک بار ذخیره ⇒
**۹ms** (**۱۴×**). آزمون نگهبان شمارندهٔ `Store.set` را می‌سنجد: اگر
`batchWrites` حذف شود، ده‌ها بار ذخیره و آزمون می‌افتد.

⚠️ **توابع نمره بر پایهٔ `kind` فیلتر می‌کنند** — شناسهٔ تکراری بین
مجموعه‌ها (attendance و grades شمارندهٔ جدا دارند) نباید پیام حضور و
نمره را قاطی کند.

📄 ۱۰ آزمون تازه (**۳۹۰/۳۹۰**).

🔴 **شکاف باز در گام ۶:** تنظیم `autoSend`، نوار هشدار و کارت
گزارش روزانه هست، ولی **خودِ ارسال خودکار** سیمی نشده — هیچ کدی پس
از ساخت پیام، وقتی `autoSend` روشن است، `notifySend` صدا نمی‌زند.
پیام‌ها همیشه در صف می‌مانند تا مدیر تأیید کند. باید جداگانه بسته
شود (همراه تصمیم: آیا اصلاحیه هم خودکار برود یا نه — بخش ۱۰ طرح).


## اصل پنجم — بازبینی امنیتی پیش از تولید («پرامپت امنیت»)

از دور ۴۲ این اصل به چهار اصل پیشین افزوده شد. **پس از هر تغییر**،
افزون بر سه کار همیشگی (گیت‌هاب · این سند · `CONTRIBUTING.md`)،
باید امنیت تغییر هم بازبینی شود.

### روش کار

بازبینی **مبتنی بر شواهد و ریسک** است، نه چک‌لیست عمومی. کد واقعی
بررسی می‌شود و فقط کنترل‌هایی اعمال می‌شوند که به معماری، حساسیت
داده، سطح حمله و مدل کاربری همین پروژه مربوط‌اند. پیچیدگی
سازمانی بی‌مورد ممنوع است.

### مرزهای اعتماد این پروژه

| مرز | وضعیت امروز |
|---|---|
| ناشناس → کاربر واردشده | ورود با رمز متن ساده در `19-actions.js` |
| دبیر → مدیر | فقط `ACTION_ROLES` سمت مرورگر |
| مرورگر → حافظهٔ محلی | `Store` تنها دروازه |
| مدرسهٔ الف → مدرسهٔ ب | `byId` محدوده نمی‌سنجد ⇒ IDOR |
| برنامه → سرور | هنوز وجود ندارد |

### طبقه‌بندی یافته‌ها

**🔴 P0 مسدودکننده** · **🟠 P1 مهم** · **🟡 P2 توصیه‌شده** ·
**⚪ P3 اختیاری**

هر یافته باید محل دقیق، شواهد، اثر واقعی، سناریوی سوءاستفاده،
کمینه‌ترین رفع و روش تأیید را داشته باشد.

### قاعدهٔ ضد مثبت کاذب

نبودِ یک کنترل به‌خودی‌خود آسیب‌پذیری نیست. نبود CSRF، نبود
رمزنگاری فیلدی، نبود محدودسازی نرخ در همه‌جا، و نبود پین‌کردن
گواهی — هیچ‌کدام خودکار ایراد نیستند. **ریسک واقعی بر کمال نظری
مقدم است.**

### وضعیت فعلی پروژه

**🔴 NOT SAFE FOR PRODUCTION** — و این **آگاهانه** است. کاربر در
دور ۲۸ تصریح کرد پروژه در فاز دمو است و بک‌اند و هش رمز در این
مرحله لازم نیست. چهار مسدودکنندهٔ `TODO_BEFORE_PRODUCTION.md`
(bcrypt · احراز هویت سمت سرور · پایگاه دادهٔ واقعی · بررسی نقش
سمت سرور) همگی باز و عمدی‌اند.

**جداسازی کاربران: FAIL (عمدی و مستند)** — سه آزمون IDOR در
`tests/smoke.js` عمداً *وجود* آسیب‌پذیری را تأیید می‌کنند و روز
اتصال سرور باید معکوس شوند.

📄 `docs/OPEN_ITEMS.md` · `TODO_BEFORE_PRODUCTION.md` ·
`docs/SERVER_SECURITY_CONTRACT.md`

---

### ۰.۵.۱۸ گام ۶: ارسال خودکار (دور ۴۲)

🔴 **شکاف واقعی که پیدا شد:** `autoSend` فقط رکورد را با `auto:1`
برچسب می‌زد و پیام همچنان منتظر تأیید دستی می‌ماند. یعنی وعده‌ای
که به مدیر داده شده بود («وقتی وقت سر زدن ندارم خودکار برود»)
**عمل نمی‌شد**. سند طرح خودش این را «نیمه‌کاره» علامت زده بود.

بخش ۱۳ ماژول ۴۴: `notifyAutoDue` · `notifyAutoFlush` ·
`notifyAutoTick`. قلاب در `render()` (فایل `24-edu-office.js`).

**سه تصمیم طراحی:**
۱. **پس از پنجرهٔ مهلت می‌فرستد، نه بلافاصله.** اگر فوری بفرستد،
   پنجرهٔ اصلاح دبیر بی‌معنا می‌شود.
۲. **بدون `setInterval`.** برنامه آفلاین است و ممکن است ساعت‌ها
   بسته باشد؛ به‌جای زمان‌سنج، هر رندر صف را وارسی می‌کند.
   محافظ تکرار ۳۰ ثانیه‌ای دارد.
۳. 🔴 **سقف روزانه اینجا دیوار است نه ترمز** — برخلاف مسیر دستی.
   در حالت خودکار کسی نیست که آگاهانه تصمیم بگیرد از سقف رد شود،
   پس نباید بی‌اجازه اعتبار مدرسه را تمام کند.

### ۰.۵.۱۹ گام ۹: سابقهٔ تغییرات حضور و غیاب

بخش ۱۴ ماژول ۴۴: `attHistory` · `attHistoryCard`. ستون «تغییرات»
در پروندهٔ دانش‌آموز + کنش `att-hist`.

🔴 **باگ جدی که حین ساخت کشف شد:** `applyOp` در عملیات درج،
**شیءِ زندهٔ رکورد** را در دفترچه می‌گذاشت. هر `update` بعدی روی
همان شیء می‌نوشت و گذشتهٔ دفترچه را بازنویسی می‌کرد. نتیجه: سابقه
دروغ می‌گفت — «ثبت present» در حالی که `absent` ثبت شده بود.
**رفع:** رونوشت سطحی هنگام ثبت در دفترچه (`03-persistence.js`).

⚠️ این باگ فقط ماژول تازه را خراب نمی‌کرد؛ **کل صفحهٔ «سابقهٔ
تغییرات» (`36-audit-activity.js`) از روز اول داده‌های نادرست نشان
می‌داد** و کسی متوجه نشده بود.

🔴 **هشدار حقوقی در رابط کاربری:** `op.by` ادعای سمت مرورگر است.
کارت سابقه صریحاً می‌گوید «تا اتصال به سرور، سند رسمی به شمار
نمی‌رود» تا کسی رویش حساب حقوقی باز نکند.


### ۰.۵.۲۰ بازبینی پس از گام ۹ — سه پرسش کاربر (دور ۴۲)

**۱) دامنهٔ باگ `applyOp` — سراسری بود، رفع هم سراسری است.**
رفع در **خودِ `applyOp`** انجام شد نه در ماژول حضور و غیاب، پس
همهٔ جدول‌ها را پوشش می‌دهد. سنجش روی پنج جدول حساس
(`grades`, `discipline`, `transactions`, `users`, `installments`):
هر پنج **✅ گذشته محفوظ**.

**بونوس نادیده‌گرفته‌شده:** `enqueueOp` (صف همگام‌سازی سرور) بعد
از رونوشت اجرا می‌شود، پس دادهٔ ارسالی به سرور آینده هم درست شد.
پیش از این، سرور تاریخچهٔ بازنویسی‌شده دریافت می‌کرد.

**۲) سرنوشت پیام‌های پشت سقف روزانه — سنجیده شد:**
| پرسش | پاسخ سنجیده‌شده |
|---|---|
| حذف می‌شوند؟ | ❌ خیر — `pending` می‌مانند (۵ ساخته، ۵ باقی) |
| `notifyPurge` پاکشان می‌کند؟ | ❌ خیر — فقط `sent`/`rejected`/`cancelled` |
| فردا می‌روند؟ | ✅ بله — شمارندهٔ روزانه صفر می‌شود و ادامه می‌یابد |
| مدیر باخبر می‌شد؟ | 🔴 **خیر — همین شکاف بود** |

**رفع:** `notifyCapBanner()` در صفحهٔ صف + هشدار در کارت داشبورد:
«⛔ سقف روزانه پر شد (۲ از ۲ قطعه) — ۴ پیام تا فردا منتظر
می‌مانند. پیام‌ها حذف نمی‌شوند.»

**۳) پشتیبان مستقل گیت:** `git bundle` در `/home/user/gitbackup/`
بیرون از پوشهٔ پروژه. با `git bundle verify` تأیید شد: «تاریخچهٔ
کامل» در ۱٫۱MB. بازیابی: `git clone payesh-*.bundle payesh-new`.


### ۰.۵.۲۱ سه قابلیت مرحلهٔ اول دور ۴۳

**#۲ اختیار مدیر (`byManager`).** `notifyCancelIfFresh` پارامتر
چهارم `opts` گرفت. مدیر می‌تواند پیام دبیر را **پس از پنجرهٔ
مهلت** هم لغو کند، ولی رکورد نشان `by_manager:1` می‌گیرد و در
کارت «لغوهای مدیر (۷ روز اخیر)» دیده می‌شود.

🔴 **دو گارد امنیتی که با جهش تأیید شدند:**
۱. **به پارامتر تنها اعتماد نمی‌شود** — نقش فعلی هم سنجیده
   می‌شود. بدون این، دبیر با پاس‌دادن `{byManager:true}` پیام
   دبیر دیگر را لغو می‌کرد (ارتقای دسترسی).
۲. **محدودهٔ مدرسه** — مدیر مدرسهٔ الف نمی‌تواند پیام مدرسهٔ ب را
   لغو کند.

**#۳ نمودار روند نمرات.** `gradeTrendData` · `gradeTrendDirection`
· `gradeTrendCard` در `17-student-record.js`، در برگهٔ نمرات.

⚠️ **دو دام داده‌ای که رفع شدند:**
۱. **ترتیب از `created_at` است نه ترتیب آرایه.** نمره ممکن است با
   تأخیر ثبت شود (نمرهٔ آبان در آذر). سنجش: درج ۱۸،۱۰،۱۴ ⇒ نمودار
   ۱۰،۱۴،۱۸ ✅
۲. **نرمال‌سازی به ۲۰.** «۸ از ۱۰» کنار «۸ از ۲۰» گمراه‌کننده
   است؛ اولی ۱۶ می‌شود.

جهت روند با مقایسهٔ میانگین نیمهٔ اول و دوم؛ زیر ۴ نمره حکم داده
نمی‌شود و زیر ۲ نمره اصلاً نمودار نمی‌آید.

**#۱۱ پیام گروهی اداره.** `officeManagers` · `officeBroadcast` در
`24-edu-office.js`، دکمه در داشبورد اداره.

⚠️ **چرا اعلان و نه پیامک؟** کیف اعتبار پیامک متعلق به **مدرسه**
است نه اداره؛ خرج‌کردن از آن برای پیام اداره درست نیست.

📄 ۸ آزمون تازه · ۶ جهش عمدی، هر شش گرفته شد.

### ۰.۵.۲۲ بررسی کشف‌پذیری

سند `docs/DISCOVERABILITY_R43.md`. سه علت متفاوت:

| قابلیت | علت | راه‌حل |
|---|---|---|
| نوبت ملاقات | 🔴 جای‌گذاری — گروه «مدرسه من» ۱۴ آیتم دارد | شکستن گروه |
| سوییچ فرزند | 🟡 نام‌گذاری + هم‌پوشانی `family`/`children` | برچسب پویا |
| داشبورد اداره | ✅ مشکلی نیست — نخستین آیتم منوی ۴تایی | — |

🔴 **یافتهٔ اصلی:** گروه «مدرسه من» با ۱۴ آیتم از ۲۶ آیتم مدیر،
عملاً به انبار تبدیل شده. وقتی نیمی از منو در یک گروه است،
گروه‌بندی کارش را نمی‌کند.

⚠️ **درس روش‌شناختی:** وقتی شبیه‌سازی می‌گوید «قابلیت X نیست»،
اول بپرسید **با کدام نقش** جست‌وجو شده.


### ۰.۵.۲۳ بازچینش منوی مدیر (دور ۴۳)

گروه «مدرسه من» با **۱۴ آیتم از ۲۶** به دو گروه شکسته شد.
معیار تقسیم: **بسامد مراجعه**، نه نوع داده.

| گروه تازه | آیتم‌ها | چرا اینجا |
|---|---|---|
| **کار روزانه** (۵) | داشبورد · افت تحصیلی · رشد مدرسه · تقویم آموزشی · فرم‌ها و پیامک | مدیر هر روز یا هر هفته سر می‌زند |
| **راه‌اندازی مدرسه** (۷) | چرخه سال · چرخه تحصیلی · ورود اطلاعات · کلاس‌ها · دروس · برنامه هفتگی · زمان‌بندی زنگ‌ها | اول سال یا فصلی؛ پس از تنظیم دست نمی‌خورد |

**دو آیتم جابه‌جا شدند به «ارتباطات»:** `meetings` (با نام تازهٔ
**«نوبت ملاقات اولیا»**) و `notifyqueue`. هر دو دربارهٔ تماس با
خانواده‌اند و کنار «اطلاعیه‌ها» و «گفتگو» معنا پیدا می‌کنند.

**توزیع پس از تغییر:** بزرگ‌ترین گروه از ۱۴ به ۷ رسید؛ ۲۶ آیتم در
۸ گروه. هیچ مسیری گم یا تکراری نشد (سنجش خودکار).

⚠️ **چرا «جلسات اولیا» گمراه‌کننده بود:** با «جلسهٔ اولیا و
مربیان» (رویداد جمعی مدرسه) اشتباه گرفته می‌شد. «نوبت ملاقات»
همان واژه‌ای است که کاربر در ذهن دارد.

🔴 **یافتهٔ تست جهش دربارهٔ `authz-coverage`:** این بررسی منو را
با `canRoute` می‌سنجد، ولی `canRoute` خودش از منو ساخته می‌شود —
پس تغییر منو به‌تنهایی هرگز نمی‌اندازدش. برای سنجش واقعی باید
`allowedRoutes` را دست‌کاری کرد. **و یک نکتهٔ مهم:** بیشتر مسیرها
در `EXTRA_ROUTES` هم هستند، پس حذفشان از منو دسترسی را قطع
نمی‌کند. تنها مسیرهایی که **فقط** از منو می‌آیند (مثل `bells`)
این بررسی را می‌اندازند — و انداختند (۲ ناهماهنگی دقیق).


### ۰.۵.۲۴ ابزارهای دبیر — مرحلهٔ دوم دور ۴۴

ماژول تازه **`src/js/45-teacher-tools.js`** (۴۶ ماژول). جدول تازه
`teacher_notes`.

#### #۷ سابقهٔ چندساله

`yearOfDate` · `studentYearHistory` · `teacherMaySeeHistory` ·
`yearHistoryCard`. نمایش در برگهٔ «شناسنامه» پروندهٔ دانش‌آموز.

**تصمیم‌های سیاستی کاربر — پیاده و آزموده:**
| قاعده | پیاده‌سازی |
|---|---|
| فقط نمرات و حضور | 🔴 `discipline` عمداً در خروجی نیست |
| فقط دانش‌آموز کلاس **فعلی** دبیر | `teacherMaySeeHistory` روی `teacherClasses` |
| مدیر استثناست | فقط در محدودهٔ مدرسهٔ خودش |

⚠️ **سال تحصیلی از مهر شروع می‌شود.** `yearOfDate` ماه ۱ تا ۶
شمسی را به سال قبل نسبت می‌دهد. اگر این اشتباه شود، نیمی از
رکوردها در سال غلط دسته‌بندی می‌شوند (با جهش تأیید شد).

🔴 **مشکلی که پیش از پیاده‌سازی کشف شد:** همهٔ دادهٔ نمونه در ۹۰
روز اخیر ساخته می‌شد، پس «سابقهٔ چندساله» چیزی برای نمایش نداشت.
`generateP12()` (فاز ۱۲) نمونهٔ سبک سال گذشته می‌سازد.

⚠️ **دو دام حجم/کارایی در `generateP12`:**
۱. نسخهٔ نخست از `insert()` استفاده می‌کرد و بوت را از **۳۱۲ms
   به بیش از دو دقیقه** برد — هر `insert` دفترچه را می‌نویسد.
   دادهٔ نمونه باید از **`add()`** استفاده کند.
۲. با ۴۰ دانش‌آموز × (۶+۱۲) رکورد، حجم `db` به **۵٫۱۰MB** رسید و
   از سقف رد شد. با ۱۵ × (۴+۸) به **۴٫۷۵MB** برگشت.

#### #۵ یادداشت خصوصی دبیر

`teacherNotesFor` · `mayWriteNote` · `addTeacherNote` ·
`removeTeacherNote` · `teacherNotesCard` · `NOTE_LEGAL_WARN`.

| قاعده | پیاده‌سازی |
|---|---|
| ولی و دانش‌آموز **هرگز** | دفاع دولایه: گارد نقش + نبود شاخهٔ بازگشتی |
| مدیر می‌بیند، **نمی‌نویسد** | `mayWriteNote` فقط `teacher` |
| دبیر فقط یادداشت خودش | `teacher_id === S.user.id` |
| با انتقال دبیر، در **مدرسه** می‌ماند | `school_id` هم شرط است |

🔴 **هشدار حقوقی `NOTE_LEGAL_WARN` نباید حذف یا نرم شود:** دبیری
که گمان کند یادداشتش کاملاً محرمانه است، ممکن است چیزی بنویسد که
در بازرسی علیه خودش یا مدرسه استفاده شود.

📄 ۱۲ آزمون تازه · **۹ جهش عمدی** (۸ گرفته شد؛ یکی «بی‌اثر» بود
چون دفاع دولایه است — با دو جهش عمیق‌تر تأیید شد).


## اصل ششم — دفترچهٔ تحویل کار (`HANDOFF.md`)

از دور ۴۵ به پنج اصل پیشین افزوده شد.

**در پایان هر سشن کاری**، پیش از بستن مکالمه، یک ورودی تازه
**بالای** `HANDOFF.md` اضافه شود:

```
## Handoff — [تاریخ] — از [نام] به [نفر بعدی]
### چی انجام شد
### تصمیمات مهم گرفته‌شده
### مشکلات باز / نکاتی که نفر بعدی باید بدونه
### قدم بعدی پیشنهادی
---
```

⚠️ **جدیدترین ورودی همیشه بالا** — نه پایین.
⚠️ **سقف ۲۰ ورودی**؛ قدیمی‌ترها به `docs/HANDOFF_ARCHIVE.md`.
⚠️ پیام کامیت: `docs: update HANDOFF.md with session summary [تاریخ]`

**تفاوتش با `PROJECT_NOTES.md`:** آن دفتر فنیِ «چه چیزی ساخته شد»
است؛ این یکی دفتر تحویلِ «نفر بعدی چه باید بداند» — شامل مشکلات
باز، ریسک‌ها و قدم بعدی.

**تیم از دور ۴۵:** رضا (بنیان‌گذار) و احمد (تازه‌وارد).


### ۰.۵.۲۵ خانوادهٔ چندفرزندی در دادهٔ نمونه (دور ۴۶ — احمد)

پیش از این `02-demo-data.js` به ازای **هر دانش‌آموز** یک ولیِ تازه
می‌ساخت، پس رابطه همیشه یک‌به‌یک بود و قابلیت سوییچ بین فرزندان —
که از نظر کد کار می‌کرد — در دمو نامرئی می‌ماند.

**نتیجهٔ سنجیده‌شده (نه برآورد):**

| | پیش | پس |
|---|---|---|
| توزیع فرزند/ولی | `{1: 521, 3: 1}` | **`{1: 309, 2: 103, 3: 3}`** |
| کل ولی | ۵۲۲ | ۴۱۵ |
| رکورد `users` | ۱۱۳۶ | ۱۰۲۹ |
| حجم `db` | ۴٫۷۵۵MB | **۴٫۶۹۷MB** |

⚠️ **این تغییر حجم را کم کرد، نه زیاد.** هر ادغام یک رکورد ولی
(۲۲۳ بایت) حذف می‌کند و فقط `parent_links` (۵۹ بایت) می‌ماند.

**انتخاب قطعی است نه تصادفی:** `generate()` با `SEED` ثابت اجرا
می‌شود؛ اگر انتخاب به `rng()` وابسته بود، هر بار خانوادهٔ
متفاوتی چندفرزندی می‌شد و آزمون‌ها ناپایدار می‌شدند. قاعده: هر
دومین دانش‌آموز از هر چهارتا (~۲۵٪، نزدیک به بُعد خانوار ایران).

✅ **اثر جانبی مثبت:** `keepSiblings` در توزیع خودکار کلاس
(دور ۳۸) تا امروز روی دادهٔ نمونه **هرگز فعال نمی‌شد** چون
خواهر/برادری نبود. حالا **۲۶ خانوادهٔ هم‌پدر** فقط در مدرسهٔ
نخست هست.

🔴 **دام کشف‌شده — دیوار پرداخت آزمون را دروغین می‌اندازد:**
هیچ ولیِ دوفرزندی اشتراک فعال ندارد (اشتراک‌ها در `generateP10`
پیش از این تغییر ساخته می‌شوند). بدون اشتراک، `parentLocked()`
صفحه را می‌بندد و آزمون «صفر دکمه» می‌گیرد. helper
`withSubscribedParent()` اشتراک را می‌سازد و در `finally`
برمی‌گرداند.

⚠️ **`subOf` با `find` کار می‌کند** — رکورد افزوده‌شده به انتهای
آرایه نادیده می‌ماند. باید ردیف‌های پیشین همان کاربر حذف و رکورد
تازه با `unshift` جلو گذاشته شود.

⚠️ **دو مورد چندفرزندیِ عمدی از پیش موجودند** و باید از سنجش‌ها
کنار بمانند: `parent_multi` («کاظم رستمی») با فرزندان در سه مدرسه
(فاز ۸) و یک کاربر نقش‌`teacher` که پیوند ولی دارد. نام خانوادگی
و مدرسهٔ ناهمخوانشان **درست** است.

📄 ۵ آزمون تازه · ۴ جهش عمدی، هر چهار گرفته شد.


### ۰.۵.۲۶ تفکیک نقش سوپرادمین از مدیر مدرسه (دور ۴۷ — احمد)

**۵۱ کنش مدرسه‌محور** از سوپرادمین گرفته شد. پیش از این ۸۸ از ۹۳
کنش را داشت.

**معیار حذف — سنجیده نه سلیقه‌ای:** آیا صفحهٔ آن کنش در منوی
سوپرادمین هست؟

| حذف شد (صفحه ندارد) | نگه داشته شد (صفحه دارد) |
|---|---|
| `imp-*` (import) · `att-*` (attendance) · `grade-*` · `class-*` · `sms-*` (formssms) · `notify-*` (notifyqueue) · `tr-*` · `mtg-*` | `user-*` (users) · `subject-*` (subjects) · `ann-save` (announcements) · `plan-*` (plans) · `school-*` · `office-*` · `backup-*` |

⚠️ **چرا `user-save` نگه داشته شد؟** صفحهٔ `users` واقعاً در منوی
سوپرادمین هست. حذف کورکورانه آن صفحه را می‌شکست.

✅ **خط قرمز رعایت شد — مسیر `S.boss` نمی‌شکند.** در `school-enter`
(`19-actions.js:74`) مقدار `S.user` **خودِ مدیر** می‌شود، پس
`activePersona()` هم `'manager'` برمی‌گرداند. سنجش:

```
سوپرادمینِ خودش:   imp-commit=false · att-set=false · grade-save=false
از راه S.boss:      imp-commit=true  · att-set=true  · grade-save=true
```

⚠️ **سه آزمون کهنه وارونه شدند** — انتظارشان
`['superadmin','imp-preview',true]` بود که مربوط به معماری پیشین
است. حالا `false`.

📄 ۴ آزمون تازه · ۳ جهش معنادار، هر سه گرفته شد.


### ۰.۵.۲۷ زنگ جاری — گام ۱: هستهٔ زمان (دور ۴۸ — احمد)

ماژول تازه **`src/js/46-bell-now.js`** (۴۷ ماژول).
`todayIndex` · `isSchoolDay` · `clockSanity` · `clockWarnText` ·
`currentSlot` · `teacherNowClass` · `bellAutoAllowed`.

**این گام هیچ رفتاری را عوض نمی‌کند** — فقط می‌گوید «الان چه
خبر است». گام‌های ۳ و ۴ طرح روی همین سوارند.

🔴 **نگاشت روز هفته — دام اصلی و راه‌حلش:**

```
Date.getDay()   :  یکشنبه=۰ · دوشنبه=۱ … شنبه=۶
schedule.day    :  شنبه=۰   · یکشنبه=۱ … چهارشنبه=۴
فرمول تبدیل     :  (getDay() + 1) % 7
```

همان فرمولی که در `22-jalali-calendar.js:43` برای نام روز به‌کار
رفته. اگر اشتباه شود، **برنامهٔ روز دیگری** نشان داده می‌شود و
هیچ خطایی پرتاب نمی‌شود — فقط دادهٔ غلط. سنجش هر هفت روز ✅.

**سه لایهٔ دفاع در برابر ساعت اشتباه:**
۱. سال خارج از ۲۰۲۴–۲۰۳۵ ⇒ `kind:'unknown'` و هشدار فارسی
۲. `hasSchedule` می‌گوید مدرسه واقعاً رکورد دارد یا `bellOf`
   الگوی پیش‌فرض برگردانده
۳. قلاب `SERVER_TIME_KEY` برای روز اتصال سرور (آستانهٔ ۱۰ دقیقه)
   — امروز هیچ‌جا نوشته نمی‌شود، فقط آماده است

⚠️ **مرز بازه `[from, to)` است** — شامل شروع، بدون پایان. با مرز
بسته، لحظهٔ دقیق پایان زنگ به دو بازه تعلق می‌گیرد و نتیجه به
ترتیب حلقه بستگی پیدا می‌کند.

⚠️ **`bellAutoAllowed()` گارد تصمیم سیاستی کاربر است:** پیش‌گزینش
فقط وقتی مجاز است که مدرسه **واقعاً** `bell_schedules` ثبت کرده
باشد، وگرنه با ساعت خیالی کار می‌کنیم.

📄 ۱۰ آزمون تازه · **۷ جهش، هر هفت گرفته شد** (سه‌تای نخست روی
نگاشت روز).


### ۰.۵.۲۸ وضعیت جاری طرح زنگ (پایان شیفت احمد، دور ۴۸)

طرح `docs/PLAN_BELL_AUTOCLASS.md` شش گام دارد. وضعیت امروز:

| گام | کار | وضعیت |
|---|---|---|
| ۱ | هستهٔ زمان (`46-bell-now.js`) | ✅ **تمام** — کامیت `a899143` |
| ۲ | نوار وضعیت زنگ (داشبورد دبیر) | ⏸️ منتظر تأیید |
| — | گسترش دادهٔ نمونه به ۶ زنگ | ⏸️ تأییدشده، اجرا نشده |
| ۳ | پیش‌گزینش در حضور و غیاب | ⏸️ پس از گام ۲ |
| ۴ | پیش‌گزینش در ثبت نمره | ⏸️ پس از گام ۳ |
| ۵ | آزمون و جهش | (با هر گام انجام می‌شود) |
| ۶ | اسناد و قلاب سرور | جزئی انجام شده |

🔴 **ناسازگاری شناخته‌شده که هنوز باقی است:** `02-demo-data.js:127`
فقط **۵ روز × ۴ زنگ** می‌سازد ولی الگوی پیش‌فرض **۵ زنگ درسی**
دارد. زنگ ۵ کلاسی ندارد ⇒ `teacherNowClass` برایش `null` می‌دهد.
تصمیم رضا: **دادهٔ نمونه گسترش یابد** (نه کاهش `BELL_PRESETS`).

⚠️ **گام‌های ۳ و ۴ باید از `bellAutoAllowed()` استفاده کنند** —
گارد تصمیم سیاستی که پیش‌گزینش را فقط برای مدارس دارای
`bell_schedules` مجاز می‌کند. آماده است، هنوز مصرف‌کننده ندارد.


### ۰.۵.۲۹ مدل داده چندمدرسه‌ای — پاسخ قطعی (دور ۴۹)

پرسش مهندس ارشد امنیت: آیا فرض «یک مدرسه = یک نمونهٔ مستقل» جایی
در مدل داده قفل شده که بعداً بازنویسی بزرگ بخواهد؟

🟢 **پاسخ: نه. مدل داده از همین حالا چندمدرسه‌ای است.**

| شاهد | سنجش |
|---|---|
| `school_id` روی جدول‌ها | **۳۲ از ۴۳** |
| شناسه‌ها | **سراسری یکتا** (مدرسه ۱ ⇒ کلاس ۱-۳، مدرسه ۲ ⇒ ۱۰-۱۲) |
| سلسله‌مراتب | `province_id / county_id / district_id` روی ۱۰۰٪ مدارس |
| کوئری‌ها | `visibleClasses`/`schoolStudents`/`visibleSubjects` همه فیلتر دارند |

🟢 **مهم‌ترین یافته: `nextId` سراسری است نه per-school.** اگر
per-school بود، دو مدرسه هر دو `classes.id=1` می‌ساختند و ادغام
در پایگاه دادهٔ مرکزی **برخورد شناسه** می‌داد. حالا ادغام
بی‌دردسر است.

🔴 **نقطهٔ قفل واقعی: ذخیره‌سازی، نه مدل داده.** `LOG_KEY` یک کلید
`localStorage` برای همهٔ مدارس است. روز سرور فقط `Store` به
`fetch` تبدیل می‌شود — **هیچ تغییری در ساختار جدول‌ها لازم نیست**.

🔴 **`org_id` اضافه نشود.** سلسله‌مراتب موجود همان کار را می‌کند و
افزودنش **دو منبع حقیقت** می‌سازد: اگر `org_id` با `district_id`
نخواند، کدام برنده است؟ افزون بر آن، تا روز سرور مصرف‌کننده‌ای
ندارد و کد بی‌مصرف پوسیده می‌شود.

**⇒ تصمیم: کارهای بعدی روی همین مسیر ادامه یابند. اصلاح زودهنگام
لازم نیست.**

📄 تحلیل کامل: `docs/ARCHITECTURE_DECISIONS.md` ضمیمهٔ ب


### ۰.۵.۳۰ زنگ جاری — گام ۲: نوار وضعیت (دور ۵۰)

بخش ۵ ماژول `46-bell-now.js`: `bellBarApplies` · `bellNowBar`.
وصل به `teacherDash()` در `08-dashboard.js`.

🔴 **فقط نمایش — هیچ رفتاری عوض نمی‌شود.** پیش‌گزینش واقعی کار
گام‌های ۳ و ۴ است.

**شش حالت نوار (همه سنجیده شدند):**
| حالت | نمونهٔ خروجی |
|---|---|
| زنگ درسی با کلاس | `🔔 زنگ ۱ (۰۷:۳۰ تا ۰۸:۱۵) — دهم ریاضی فیزیک · فارسی ۱` |
| زنگ درسی بدون کلاس | `🔔 زنگ ۱ … — در این زنگ کلاسی ندارید.` |
| تفریح | `☕ زنگ تفریح (۰۸:۱۵ تا ۰۸:۲۵) — زنگ بعدی ۰۸:۲۵` |
| پیش/پس از مدرسه | `🕗 هنوز مدرسه شروع نشده — نخستین زنگ ۰۷:۳۰` |
| تعطیل | `🌙 امروز روز درسی نیست.` |
| ساعت نامعتبر | `⚠️ ساعت دستگاه شما نادرست… (سال ۱۹۹۹)` |

⚠️ **`bellNowBar(now)` پارامتر زمان می‌گیرد** — همان الگوی بقیهٔ
توابع این ماژول، برای آزمون‌پذیری.

**گسترش دادهٔ نمونه به ۶ زنگ** (`02-demo-data.js:127`):
`schedule` از ۷۲۰ به **۱۰۸۰** ردیف · حجم `db` **۴٫۷۲۵MB**
(پیش ۴٫۷۰) · هر پنج زنگ الگو حالا کلاس دارند.

⚠️ **چرا ۶ نه ۵؟** الگوی پیش‌فرض ۵ زنگ درسی دارد؛ ۶ حاشیهٔ امن
می‌دهد اگر مدرسه‌ای الگوی بلندتری تعریف کند.

📄 ۷ آزمون تازه · ۵ جهش، هر پنج گرفته شد.


## اسکیل‌های تخصصی (`.claude/skills/`) — دور ۵۱

هفت اسکیل مرجع در مخزن هست. اصل مشترکشان همان چیزی است که در
پایش رعایت می‌کنیم: **«تأیید کورکورانه نکن»**.

| اسکیل | کاربرد امروز |
|---|---|
| `security-expert` | ✅ مکمل اصل پنجم — روش ۹ مرحله‌ای + چک‌لیست OWASP |
| `qa-testing` | ✅ «کد کارکننده لزوماً درست نیست» — فلسفهٔ تست جهش |
| `software-architect` | ✅ تصمیم‌های معماری (ضمیمهٔ الف و ب) |
| `scalability-performance` | ✅ «کارکردن با کاربر کم دلیل مقیاس‌پذیری نیست» |
| `database-architect` | ⏸️ PostgreSQL‌محور — **روز سرور** |
| `devops-cloud` | ⏸️ Docker/K8s — **روز سرور** |
| `network-infrastructure` | ⏸️ **روز سرور** |

### 🔴 تبصرهٔ مهم: چهار نقض عمدی

این اسکیل‌ها می‌گویند «رمز هرگز متن ساده نباشد» و «مجوز باید
سمت سرور اعمال شود». پایش **عمداً** هر دو را نقض می‌کند
(تصمیم فاز دمو، دور ۲۸).

⚠️ **هنگام به‌کارگیری `security-expert`، این چهار مورد را
دوباره گزارش نکنید** — در `TODO_BEFORE_PRODUCTION.md` ثبت‌اند و
تصمیم آگاهانه‌اند:
۱. bcrypt · ۲. احراز هویت سرور · ۳. پایگاه دادهٔ واقعی ·
۴. بررسی نقش سمت سرور

### روش ۹ مرحله‌ای بازبینی امنیتی (از `security-expert`)

افزوده به اصل پنجم:
۱. بفهم قابلیت چه می‌کند
۲. دادهٔ حساس و مرزهای اعتماد را بیاب
۳. مهاجم و سناریوی سوءاستفاده را تصور کن
۴. احراز هویت و مجوز را بررسی کن
۵. **هر** ورودی کاربر را وارسی کن
۶. نشت احتمالی داده را بیاب
۷. ارتباط با سرویس بیرونی را بررسی کن
۸. ضعف‌ها را فهرست کن
۹. بر پایهٔ شدت طبقه‌بندی کن (P0–P3)

**چک‌لیست OWASP:** Broken Access Control · Authentication
Failures · Injection · Insecure Design · Security
Misconfiguration · Vulnerable Dependencies · Data Integrity
Failures · Logging Failures · SSRF


### ۰.۵.۳۱ طرح ورود با موبایل — بازنویسی دور ۵۳

سند: `docs/PLAN_PHONE_AUTH.md` (بازنویسی کامل پس از بازبینی
کاربر). **کد نوشته نشده.**

**جریان:** شماره → کد پیامکی → ورود → نمایش نقش‌ها ·
اگر نقشی نبود ⇒ «هیچ نقشی تعریف نشده» + دکمهٔ **پیگیری**.

**مسیر پیگیری:** نقش (کشویی) → استان/شهرستان/منطقه →
[ولی و دانش‌آموز: مقطع + پایه + جنسیت] → فهرست فیلترشدهٔ مدارس
با جست‌وجو → اعلان کامل به مدیر.

**مسیریابی دو صف:**
| نقش ادعایی | گیرنده |
|---|---|
| دبیر · دانش‌آموز · ولی | مدیر مدرسه |
| مدیر · رئیس اداره | سوپرادمین |

🔴 **سناریوی شمارهٔ اشتباه (خواستهٔ صریح کاربر):** اگر کد ملی در
پایگاه داده نباشد ولی **شماره روی کاربر دیگری ثبت** باشد، یعنی
مدرسه‌ای شماره را برای شخص اشتباهی وارد کرده. باید:
۱. به کاربر بگوید «برای شخص دیگری ثبت شده» — **بدون نام**
۲. به مدیرِ آن مدرسه اعلان کامل بدهد تا اصلاح کند
۳. اگر در چند مدرسه ثبت است، به **همهٔ** مدیران

جدول تازه `phone_conflicts` برای همین است.

🔴 **تناقض حل‌نشده (بخش ۲ سند):** توصیف تازهٔ کاربر مرحلهٔ کد ملی
و شاهکار را **نیاورده**، ولی در دور ۵۲ تصریح کرده بود «عدم
تطابق = رد قطعی». و در همین توصیف خواسته «کد ملی برای پیگیری به
مدیر ارسال شود» — یعنی جایی گرفته شده. **طرح بر پایهٔ ماندن آن
مرحله نوشته شد؛ منتظر تعیین تکلیف.**

⚠️ **کمبود فنی کشف‌شده:** هیچ‌جای کد امروز با **شمارهٔ تلفن**
جست‌وجو نمی‌شود. `idxUserByPhone` لازم است وگرنه ورود در مقیاس
ملی خطی می‌شود.

🟢 **آنچه از قبل آماده است:** `panelsOf` (چندنقشی) ·
`viewChildren` (چند فرزند) · `districts.kind` (منطقه/روستا) ·
`schools.level/gender` · `SESSION_KEY` (ماندگاری نشست) ·
`notifications`.

**برآورد: ۷ تا ۹ روز** در هشت گام.


### ۰.۵.۳۲ تایمر کد تأیید و ارسال مجدد (دور ۵۴)

بخش ۴.۷ به `docs/PLAN_PHONE_AUTH.md` افزوده شد.

**دو تایمر جدا — و تمایزشان مهم است:**
| تایمر | مدت | کار |
|---|---|---|
| ارسال مجدد | ۶۰ ثانیه | تا پایانش دکمه خاموش |
| اعتبار کد | ۱۸۰ ثانیه | پس از آن کد باطل |

⚠️ **چرا اعتبار بلندتر است؟** اگر هر دو ۶۰ ثانیه باشند، کاربری
که پیامکش با تأخیر می‌رسد کدِ باطل وارد می‌کند و گیج می‌شود.

🔴 **دام معماری کشف‌شده:** `render()` کل `#root` را با
`innerHTML` بازمی‌سازد (`24-edu-office.js:760`). هر
`setInterval` که به عنصر DOM وابسته باشد با نخستین رندر مرجعش
را از دست می‌دهد.

**راه‌حل: زمان پایان در `Store` نگه داشته شود، نه در شمارنده.**
```js
Store.setJSON(OTP_KEY, {phone, codeHash, sentAt, resendAt, expiresAt, sendCount, attempts});
var left = Math.max(0, Math.ceil((otp.resendAt - Date.now())/1000));
```
`setInterval` فقط عدد روی صفحه را تازه می‌کند؛ **منبع حقیقت
`Store` است**. مزیت جانبی: بارگذاری دوباره محدودیت را دور
نمی‌زند و تایمر از جایی که بود ادامه می‌دهد.

**سه لایه محدودسازی:** فاصلهٔ ۶۰ ثانیه · سقف ۳ ارسال در ۱۰
دقیقه · سقف ۵ تلاش کد غلط سپس ابطال.

⚠️ **هزینه:** هر ارسال مجدد یک پیامک واقعی است. پیشنهاد شد کد
تأیید از کیف اعتبار **مدرسه** کم نشود — احراز هویت هزینهٔ سامانه
است نه مدرسه.

برآورد کل طرح: **۸ تا ۱۰ روز** (از ۷ تا ۹).


## اصل هفتم — گزارش کار در پایان هر پاسخ

از دور ۵۵ به شش اصل پیشین افزوده شد.

🔴 **در پایان هر پاسخ، بدون استثنا، گزارش کار نوشته شود** — حتی
اگر کار کوچک بوده یا فقط تحلیل بوده.

### گزارش باید چه چیزی داشته باشد

| بخش | محتوا |
|---|---|
| **چه کردم** | کار انجام‌شده، نه وعده |
| **عدد سنجیده‌شده** | نه «بهبود یافت» بلکه «۲۷۱۹ms → ۱۲۹ms» |
| **چه چیزی پیدا شد** | دام، باگ، یا تصمیمی که لازم شد |
| **چه چیزی باز ماند** | پرسش بی‌پاسخ یا کار نیمه‌تمام |
| **وضعیت** | آزمون‌ها · بیلد · کامیت · گیت‌هاب |

⚠️ **تفاوتش با `HANDOFF.md`:** آن دفتر تحویل بین نشست‌هاست؛ این
گزارش **همین پاسخ** است. هر دو لازم‌اند.

⚠️ **اگر کاری انجام نشد، همان را بگو** — «فقط تحلیل کردم، کد
نزدم» بهتر از گزارش مبهم است.

### هفت اصل — فهرست کامل

۱. همگامی مستندات · ۲. تفکیک اصولی کد · ۳. گیت‌هاب همیشه به‌روز
· ۴. کمترین حجم ممکن · ۵. بازبینی امنیتی · ۶. `HANDOFF.md` ·
**۷. گزارش کار در پایان هر پاسخ**


### ۰.۵.۳۳ طرح تقویم، ساعت و زمان‌بندی متغیر (دور ۵۵)

سند: `docs/PLAN_CALENDAR_TIME.md`. **کد نوشته نشده.**

چهار نیاز بررسی شد:

| # | نیاز | وضعیت سنجیده‌شده |
|---|---|---|
| ۱ | همگامی ساعت با سرور | 🟡 قلاب `SERVER_TIME_KEY` از دور ۴۸ آماده، **هیچ‌جا نوشته نمی‌شود** |
| ۲ | روزهای هفتهٔ متغیر | 🔴 `DAYS[]` در `01-helpers.js:26` **ثابت ۵ روزه** |
| ۳ | تعریف زنگ توسط مدیر | 🟢 `viewBells`+`bellSave` هست · 🔴 per-shift نیست |
| ۴ | کلاس جبرانی | 🔴 **وجود ندارد** |

🔴 **پرریسک‌ترین بخش: `DAYS[]`.** در **پنج جا** مصرف می‌شود
(`08-dashboard`, `21-exams`, `24-edu-office`×۳) و
`isSchoolDay()` در `46-bell-now.js` هم `<= 4` را قفل کرده. راه
امن: `DAYS` دست‌نخورده بماند و `DAYS_FULL` هفت‌عضوی افزوده شود.

🟢 **تقویم شمسی نیازی به همگامی ندارد** — `22-jalali-calendar.js`
محاسباتی است نه جدولی. فقط **لحظهٔ کنونی** همگامی می‌خواهد.
⚠️ ولی **تعطیلات رسمی** محاسباتی نیستند و هر سال عوض می‌شوند.

**سرآیند `Date` در هر پاسخ HTTP هست** — پس همگامی ساعت نقطهٔ
پایانی جدا نمی‌خواهد.

⚠️ **ابهام در درخواست کاربر:** «دبیر خودش تایم را بنویسد» دو
خوانش دارد: (الف) دبیر زمان‌بندی مدرسه را عوض کند — 🔴 رد شد،
هرج‌ومرج می‌شود؛ (ب) دبیر ساعت **کلاس جبرانی خودش** را بنویسد —
✅ پیشنهاد شد.

**کلاس جبرانی با `date` مدل می‌شود نه `day`+`period`** — چون
یک‌باره است نه هفتگی.

برآورد: **۶ تا ۸ روز**.


### ۰.۵.۳۴ شاهکار — تناقض حل شد (دور ۵۶)

**تصمیم قطعی کاربر:**

| نتیجهٔ شاهکار | وضعیت | پیام |
|---|---|---|
| تطابق دارد | ✅ احراز هویت **سالم** | وارد می‌شود |
| تطابق ندارد | 🔴 احراز هویت **ناقص** | «شمارهٔ شما با کد ملی مطابقت ندارد» |

مرحلهٔ کد ملی و شاهکار **سر جایش می‌ماند** — ابهام دور ۵۳ بسته
شد. جایگاهش: پس از تأیید کد پیامکی، فقط بار اول.

⚠️ **متن پیام عمداً صریح است.** کاربری که سیم‌کارت به نامش نیست
باید دقیقاً بفهمد چرا رد شد، نه پیام مبهم «خطایی رخ داد». ولی
🔴 **نام مالک واقعی گفته نمی‌شود** — نشت اطلاعات است.

**چهار چیز که پس از عدم تطابق نمی‌شود:**
۱. ورود با پرچم «تأییدنشده» — تصمیم دور ۵۲
۲. اعلان به مدیر — به او ربطی ندارد
۳. ذخیرهٔ کد ملی ردشده — دادهٔ شخصی بی‌استفاده
۴. تلاش نامحدود — سقف دارد (پایین)

🔴 **سقف تلاش کد ملی افزوده شد: ۳ بار سپس قفل ۲۴ ساعته.**
بدون آن، مهاجم با یک شمارهٔ معتبر می‌تواند کد ملی‌های مختلف را
آزمون کند تا **مالک واقعی شماره** را بیابد. این با سقف تلاش کد
پیامکی (۵ بار) فرق دارد و جداگانه لازم است.

تنظیمات تازه: `nid_max_attempt: 3` · `nid_lock_hours: 24`


### ۰.۵.۳۵ نمای زندهٔ زنگ برای سه نقش (دور ۵۷)

سند: `docs/PLAN_LIVE_SCHEDULE.md`. **کد نوشته نشده.**

سه خواستهٔ نخست شدنی‌اند و روی زیرساخت دور ۴۸/۵۰ سوارند.
خواستهٔ چهارم محدودیت بنیادی دارد.

🔴 **نوتیفیکیشن روی گوشی خاموش در معماری فعلی ناممکن است.**
سنجش کد: Service Worker ❌ · manifest ❌ · Push API ❌ · سرور ❌

سه چیز لازم است که هیچ‌کدام نیست: Service Worker (نیازمند
HTTPS، روی `file://` کار نمی‌کند) · Push Server (Firebase/APNs)
· اشتراک Push.

🔴 **و یک تضاد معماری:** Service Worker **باید فایل جدا باشد** —
نمی‌تواند درون `index.html` باشد. یعنی قاعدهٔ ثابت پروژه
(«خروجی نهایی همیشه یک `index.html`») می‌شکند.

**سه راه با هزینهٔ سنجیده‌شده:**
| راه | هزینه | محدودیت |
|---|---|---|
| الف) اعلان درون‌برنامه‌ای | ۰٫۵ روز | گوشی خاموش ⇒ نمی‌بیند |
| ب) پیامک | ۰٫۵ روز + هزینه | روی گوشی خاموش می‌رسد |
| ج) PWA + Push | ۵–۸ روز + سرور + HTTPS | معماری عوض می‌شود |

**پیشنهاد: الف + ب زنجیره‌ای** — اعلان فوری، و اگر تا ۵ دقیقه
دیده نشد، پیامک. زیرساخت پیامک از دور ۴۲ آماده است.

🔴 **نکتهٔ حیاتی در نمای ولی:** وضعیت حضور باید **سه‌حالتی**
باشد: `حاضر` · `غایب` · **`هنوز ثبت نشده`**. اگر دبیر هنوز ثبت
نکرده و به ولی «غایب» نشان داده شود، هم غلط است هم اعتماد به
سامانه را از بین می‌برد.

⚠️ **پیشنهاد انسانی:** تلنگر مدیر **دستی** بماند نه خودکار.
دبیری که سه دقیقه دیر کرده شاید مشغول آرام‌کردن کلاس است.

برآورد (بدون راه ج): **۴ تا ۵ روز**.


## اصل هشتم — نوشتار فارسی بدون قاطی‌شدن با انگلیسی

از دور ۵۸ به هفت اصل پیشین افزوده شد. کاربر تصریح کرد: «فارسی و
انگلیسی با هم قاطی می‌شود و نمی‌دانم متن را از کجا بخوانم».

### قاعده

🔴 **در متن جاری فارسی، نام انگلیسی نیاید.** جایش نام فارسی
گفته شود و اگر نام فنی لازم است، **در پرانتز و در پایان جمله**.

| ❌ نادرست | ✅ درست |
|---|---|
| «`bellNowBar` این کار را می‌کند» | «نوار وضعیت زنگ این کار را می‌کند» |
| «گام ۳ `PLAN_BELL_AUTOCLASS` معلق است» | «گام سوم طرح زنگ خودکار معلق است» |
| «`Service Worker` لازم است» | «کارگر پس‌زمینهٔ مرورگر لازم است» |
| «`idxAttByClassDate` را به‌کار ببرید» | «از ایندکس حضور روزانه استفاده کنید» |

### کجا انگلیسی مجاز است

✅ **داخل قطعه‌کد** — طبیعی و لازم
✅ **مسیر فایل** در فهرست یا جدول: `src/js/46-bell-now.js`
✅ **سلول جدول** وقتی نام فنی ستون است
✅ **پرانتز پس از معادل فارسی** — یک‌بار در نخستین کاربرد

### در گزارش کار (اصل هفتم)

🔴 **گزارش برای خواندن انسان است، نه ارجاع فنی.** اگر جمله‌ای
سه نام انگلیسی دارد، بازنویسی‌اش کنید.

⚠️ **جهت متن هم مهم است:** نام انگلیسی وسط جملهٔ فارسی، ترتیب
خواندن را در نمایشگر راست‌به‌چپ به‌هم می‌ریزد. این مشکل زیبایی
نیست، مشکل **خوانایی** است.

### هشت اصل — فهرست کامل

۱. همگامی مستندات · ۲. تفکیک اصولی کد · ۳. گیت‌هاب همیشه به‌روز
· ۴. کمترین حجم ممکن · ۵. بازبینی امنیتی · ۶. دفترچهٔ تحویل ·
۷. گزارش کار در پایان هر پاسخ · **۸. نوشتار فارسی بدون قاطی‌شدن**


### ۰.۵.۳۶ احراز هویت دروازهٔ مطلق است (دور ۵۹)

**تصحیح مهم کاربر:** ترتیب مطلق است —

```
احراز هویت  ⇒  اگر تأیید نشد، هیچ‌چیز دیگری رخ نمی‌دهد
```

نه صفحهٔ «نقشی تعریف نشده»، نه مسیر پیگیری، نه دیدن نقش‌ها.
کاربر **اصلاً وارد نمی‌شود**.

🔴 **دکمهٔ «ویرایش کد ملی» حذف شد.** نسخهٔ پیشین طرح آن را
داشت — اشتباه بود. اگر کاربر بتواند کد ملی را عوض کند و دوباره
بیازماید، دقیقاً همان حمله رخ می‌دهد که می‌خواستیم جلویش را
بگیریم: آزمون کد ملی‌های مختلف با یک شمارهٔ معتبر.

### سناریوی «کد ملی هست، شماره به نام خودش نیست»

| راه | چرا رد شد |
|---|---|
| ورود مشروط | هویت تأیید نشده — دادهٔ کودکان است |
| تأیید مدیر از راه دور | مدیر نمی‌تواند مالکیت سیم‌کارت را بسنجد |
| پرسش امنیتی جایگزین | ضعیف‌تر از شاهکار |
| **مراجعهٔ حضوری** | ✅ **تنها راه امن** |

🔴 **تصمیم قطعی: هیچ راه نرم‌افزاری وجود ندارد.** این محدودیت
**عمدی** است نه نقص.

### چرخهٔ اصلاح (بخش ۲.۵ سند)

چرخه باید کامل باشد وگرنه بن‌بست می‌شود. سه چیز لازم است:
۱. **هشدار به مدیر** هنگام تغییر شماره: «شمارهٔ جدید باید به نام
   خودِ این شخص باشد»
۲. **آزمون پیشاپیش** — پرسش از شاهکار پیش از رفتن کاربر
   (هزینه دارد ولی از رفت‌وآمد دوباره ارزان‌تر است)
۳. **باز شدن قفل** ۲۴ ساعته پس از تغییر شماره توسط مدیر

🔴 **دام: مدیر نباید کد ملی را آزادانه عوض کند.** شماره بله،
کد ملی نه — وگرنه می‌تواند کد ملی خودش را روی حساب دیگری
بگذارد. کد ملی **کلید هویت** است.


### ۰.۵.۳۷ حالت‌های زمان‌دار حضور و غیاب (دور ۶۰)

سند: `docs/PLAN_ATTENDANCE_STATES.md`. **کد نوشته نشده.**

**پرسش مدیر: ویرایش کاربران آزاد است؟** ✅ **بله** — سنجش
اجرایی: مدیر مدرسهٔ یک، ۲۴۳ کاربر خودش را می‌بیند و کاربران
مدرسهٔ دو **دیده نمی‌شوند**. محدودهٔ مدرسه رعایت می‌شود.

🔴 **ولی یک استثنا لازم است:** طبق تصمیم دور ۵۹، مدیر باید
بتواند شماره را عوض کند ولی **کد ملی نه**. امروز هر دو آزادند —
یعنی مدیر می‌تواند کد ملی خودش را روی حساب دیگری بگذارد و از
احراز هویت رد شود.

**کمبود ساختاری کشف‌شده:** رکورد حضور و غیاب **هیچ فیلد زمانی
ندارد** (فقط `date` و `status`). پس «تأخیر» فقط برچسب است —
نمی‌دانیم پنج دقیقه بوده یا پنجاه. دو فیلد لازم است:
`late_minutes` و `arrived_at`.

**خروج موقت از کلاس** جدول جدا می‌خواهد (`class_exits`) نه فیلد
روی حضور — چون یک دانش‌آموز ممکن است روزی چند بار خارج شود.

🔴 **سه حالت مرزی خروج:** برنگشتن تا پایان زنگ · فراموشی دبیر ·
کسر زمان تفریح از محاسبه.

🔴 **ثبت خودکار انضباطی رد شد.** پیشنهاد: تأخیر همیشه در رکورد
حضور ثبت شود، ولی رکورد انضباطی فقط با **آستانهٔ قابل تنظیم
مدیر** و **تأیید دبیر**. گزارش تجمیعی («این ماه ۷ بار، جمعاً ۹۵
دقیقه») از هفت رکورد جدا مفیدتر است.

🔴 **حذف پس از ارسال پیامک ممنوع بماند.** اگر خانواده پیامک
«غیبت» گرفته و بعد رکورد پاک شود، هیچ ردی نمی‌ماند که چرا آن
پیام رفت. اصلاحیه باید برود، نه پاک‌کردن تاریخ.

🟢 **چهار لایهٔ دفاعی از دور ۴۲ از قبل هست** — آنچه کم است
صفحهٔ متمرکز «گزارش‌های من» برای دبیر است.

برآورد: **۵ تا ۷ روز**.


### ۰.۵.۳۸ رفع پرش صفحه + بررسی ویرایش کاربران (دور ۶۰)

#### 🔴 پرش صفحه به بالا — رفع شد

**علت سنجیده‌شده:** عنصر محتوا با هر رندر **تازه** ساخته می‌شود
(چون ریشه بازنویسی می‌گردد)، پس اسکرولش صفر می‌شد. سنجش پیش از
رفع: ۲۵۰ → ۰.

**رفع:** مثل اسکرول منو، پیش از بازسازی خوانده و پس از آن
بازگردانده می‌شود — با تفکیک دو حالت:

| حالت | رفتار |
|---|---|
| تغییر مسیر | صفحهٔ تازه از بالا شروع می‌شود |
| همان صفحه (کلیک، فیلتر، تیک) | جای کاربر حفظ می‌شود |

⚠️ بدون این تفکیک، یا هر کلیک به بالا می‌پرد یا صفحهٔ تازه وسط
محتوا باز می‌شود.

**اسکرول پنجره هم بازگردانده می‌شود** (برای نمای موبایل که
محتوا اسکرول جدا ندارد) — درون `try/catch` چون در محیط آزمون
پیاده‌سازی نشده است.

سنجش پس از رفع: کلیک عادی ۲۵۰ → ۲۵۰ ✅ · تغییر مسیر ۲۵۰ → ۰ ✅
· منو ۸۰ → ۸۰ ✅ · کلیک واقعی روی دکمه ۱۸۰ → ۱۸۰ ✅

۳ آزمون تازه · ۲ جهش، هر دو گرفته شد.

#### بررسی: ویرایش مشخصات کاربران برای مدیر

**پرسش کاربر:** «امکان ویرایش مشخصات کاربران برای مدیر باید
آزاد باشد. بررسی کن درست است یا نه.»

**پاسخ: بله، آزاد است.** سنجش اجرایی:

| کنش | مدیر |
|---|---|
| باز کردن فرم · ذخیره · حذف · فعال/غیرفعال · بازنشانی رمز | ✅ همه |

**میدان‌های قابل ویرایش:** نام · نام کاربری · نقش · کد ملی ·
تلفن · کلاس · رمز · وضعیت فعال

**دو محدودیت درست که وجود دارد:**
۱. مدیر **نمی‌تواند مدرسهٔ کاربر را عوض کند** — فقط مدیر کل
   سامانه. درست است؛ وگرنه می‌شد کاربر را به مدرسهٔ دیگر برد.
۲. مدیر **فقط کاربران مدرسهٔ خودش را می‌بیند** — نشت
   بین‌مدرسه‌ای ندارد.

🔴 **ولی یک ریسک که در دور ۵۹ هشدار داده بودم، تأیید شد:**
**مدیر می‌تواند کد ملی را آزادانه عوض کند.**

با ورود تلفنی (طرح احراز هویت) این خطرناک می‌شود: مدیر می‌تواند
کد ملی خودش را روی حساب دیگری بگذارد و با شمارهٔ خودش وارد آن
حساب شود.

⚠️ **امروز مسئله‌ای نیست** چون ورود با نام کاربری و رمز است.
**روز پیاده‌سازی ورود تلفنی، باید حل شود:** یا ثبت در سابقهٔ
تغییرات با اعلان به مدیر کل، یا نیازمند تأیید دوگانه.


## اصل نهم — گزارش تجمیعی بر حسب درخواست

از دور ۶۱ افزوده شد. مکمل اصل هفتم است، نه جایگزینش.

**اصل هفتم:** گزارش کوتاه در پایان **هر پاسخ**.
**اصل نهم:** وقتی کاربر گفت «گزارش کلی بده»، گزارش **تجمیعی**
از یک نقطهٔ مشخص تا اکنون.

### نقطهٔ مبنای فعلی

🔴 **از گفتگوی اسکیل‌ها (کامیت `7b60285`) تا اکنون.**

هر بار که کاربر «گزارش کلی» خواست، همهٔ کارهای پس از آن نقطه
جمع‌بندی شود: چه ساخته شد، چه کدی زده شد، چه تصمیمی گرفته شد،
چه چیزی باز ماند.

### ساختار گزارش تجمیعی

۱. **جدول کامیت‌ها** — شناسه و یک‌خطی هرکدام
۲. **کد نوشته‌شده** در برابر **طرح نوشته‌شده** (تفکیک صریح)
۳. **تصمیم‌های گرفته‌شده** با دلیلشان
۴. **پرسش‌های بی‌پاسخ** — فهرست کامل
۵. **وضعیت** — آزمون‌ها، ساخت، گیت‌هاب

⚠️ **تفکیک «کد» از «طرح» مهم‌ترین بخش است.** در این دوره طرح
بسیار بیشتر از کد نوشته شده؛ اگر مخلوط گزارش شوند، کاربر گمان
می‌کند قابلیت‌ها ساخته شده‌اند.


### ۰.۵.۳۹ نظم مدرسه: تأخیر، خروج و الگوی تکرار (دور ۶۲)

سه خواستهٔ تازه به طرح حالت‌های غیبت افزوده شد
(`docs/PLAN_ATTENDANCE_STATES.md`). **کد نوشته نشده.**

#### موجه کردن پس از ثبت (بخش ۴.۵)

تأخیری که ثبت شده، هفته‌ها بعد می‌تواند با توضیح ولی موجه شود.

🔴 **رکورد پاک نمی‌شود، فقط `excused` می‌گیرد.** اگر دانش‌آموزی
ده تأخیر داشته و همه موجه شده‌اند، این خودش الگویی است که مدیر
باید ببیند.

⚠️ **دام محاسباتی:** هر جا امتیاز انضباطی جمع می‌شود باید
موجه‌شده‌ها را کنار بگذارد. اگر یک جا فراموش شود، دو عدد متفاوت
در دو صفحه دیده می‌شود.

**مسیر درخواست ولی:** جدول `excuse_requests` — ولی درخواست
می‌دهد، مدیر تصمیم می‌گیرد. بدون این، ولی باید تلفن بزند.

#### الگوی تکرار (بخش ۴.۷) — مهم‌ترین بخش

یک تأخیر ۱۰ دقیقه‌ای چیزی نیست؛ **ده تأخیر در ماه** یک الگوست.

`patternCheck(studentId, days)` سه شمارنده می‌دهد: تأخیر · خروج
از کلاس · غیبت — هرکدام با تعداد، مجموع دقیقه، و **روند**
(افزایشی/ثابت/کاهشی).

⚠️ **شمارندهٔ خروج جدا از تأخیر لازم است.** نمونه: دانش‌آموزی
که تأخیرش کم است ولی ۹ بار از کلاس خارج شده — همان «فرار از
کلاس» که کاربر گفت. بدون شمارندهٔ جدا این الگو دیده نمی‌شود.

**آستانه‌ها قابل تنظیم‌اند** (`pattern_late_month: 4` و…) چون
مدرسهٔ روستایی با سرویس، تأخیر بیشتری طبیعی دارد.

🔴 **اعلان به ولی خودکار نیست.** ممکن است دانش‌آموز مشکل
خانوادگی یا پزشکی داشته باشد که مدیر می‌داند و سامانه نه. پیام
خودکار در چنین شرایطی آسیب‌زننده است. `pattern_notify_parent`
پیش‌فرض خاموش.

**موجه‌شده‌ها در الگو شمرده می‌شوند ولی جدا:**
`۷ تأخیر (۳ موجه · ۴ غیرموجه)` — دانش‌آموزی که هفت بار دیر
آمده، حتی با عذر موجه، مشکلی دارد که باید دیده شود.

⚠️ **گام شمارندهٔ الگو پرریسک‌ترین است** — روی همهٔ
دانش‌آموزان اجرا می‌شود. بدون ایندکس، در مدرسهٔ ۵۰۰ نفره هر
رندر کند می‌شود.

برآورد طرح از ۵–۷ روز به **۸ تا ۱۱ روز** رسید.

### ۰.۵.۴۰ سند کارهای ناتمام

`docs/OPEN_WORK.md` ساخته شد — پاسخ به «اگر کار ناتمامی داریم
بگو».

🔴 **یافتهٔ مهم: شش طرح آماده است و هیچ‌کدام پیاده نشده.**
جمعاً ۲۹ تا ۳۷ روز کار. ترتیب پیشنهادی: دستهٔ الف (منو و زنگ،
۳ روز) ⇒ دستهٔ ب (نظم مدرسه، ۸–۱۱ روز) ⇒ دستهٔ ج (بزرگ‌ها).
