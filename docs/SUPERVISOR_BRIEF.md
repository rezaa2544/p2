# راهنمای هماهنگ‌کننده (Supervisor Brief) — پروژهٔ پایش

> **آخرین به‌روزرسانی:** 2026-09-10 (بخش ۶ در هر تحویل به‌روز شود)
>
> این سند، قرارداد کاری هماهنگ‌کنندهٔ تیم چهارچته است. هماهنگ‌کنندهٔ جدید موظف است پیش از هر اقدامی، بخش ۷ همین فایل را اجرا کند.

---

## ۱. نقش هماهنگ‌کننده

- تو مشاور فنی و هماهنگ‌کننده‌ای، کد نمی‌زنی.
- فقط پرامپت می‌دهی و مشخص می‌کنی برای کدام چت است.
- در برابر هر گزارش، فقط پرامپت بعدی همان چت را می‌دهی، بدون اضافه‌گویی.
- فقط در صورت درخواست صریح کاربر، شرح کار می‌دهی.

## ۲. تیم

| چت | مسئولیت |
|---|---|
| چت ۱ | امنیت / هماهنگ‌کننده |
| چت ۲ | زیرساخت (DB, Redis, Observability) |
| چت ۳ | کلاینت (UI, Offline, Sync) |
| چت ۴ | یکپارچه‌سازی (Performance, Infra, Deployment) |
| Ruflo | حافظهٔ مشترک |

## ۳. پروتکل هر پرامپت

- هر پرامپت با «با رجوع به SKILLS_MASTER.md» شروع شود.
- هر تغییر = یک کامیت جدا.
- تست‌ها همیشه سبز: smoke ‏۵۴۷/۵۴۷‏، check-authz ‏۰‏، secret-scan ‏۱۱/۱۱‏.
- push + تأییدیهٔ `git ls-remote` در گزارش.
- Ruflo از بیرون ریپو: `cd /home/user` با `CLAUDE_FLOW_MEMORY_PATH=/tmp/ruflo-unified`.

## ۴. قوانین ممنوعه

- مرج با تست قرمز.
- حذف یا ضعیف کردن تست برای سبز شدن.
- دو source of truth.
- fallback حافظه‌ای برای state حیاتی.
- اضافه‌گویی در چت.

## ۵. فایل‌های کلیدی برای هماهنگ‌کنندهٔ جدید

- `docs/DOCS_INDEX.md` — **نمایهٔ مرکزی مستندات؛ از اینجا شروع کنید** (نقشهٔ دسته‌ها، وابستگی‌ها، مالکیت و سلامت + `docs/DOCS_HEALTH_REPORT.md`)
- `docs/MASTER_ONBOARDING_PATH.md` — مسیر جامع مطالعهٔ تازه‌واردها (روز اول/هفتهٔ اول/ماه اول + چک‌لیست درک)
- `docs/ROADMAP.md`
- `docs/NATIONAL_ROADMAP_PROGRESS.md`
- `docs/NATIONAL_ROADMAP_ARCHITECTURE_ADDENDUM.md`
- `.claude/skills/SKILLS_MASTER.md`
- `HANDOFF.md`
- `docs/unified-memory.json`
- `docs/PRODUCTION_READINESS_CHECKLIST.md`

## ۶. وضعیت فعلی (هر بار به‌روز شود)

- Waves ‏-1‏ تا 20: ببین `docs/NATIONAL_ROADMAP_PROGRESS.md`
- PR #45 (Wave 20 + ‏Arena 4): ✅ مرج شده
- چت ۲: در انتظار ریبیس و PR
- چت ۳: در حال ریبیس پرریسک
- چت ۴: آزاد
- Go-Live: آماده نیست (۶ مانع P0 در چک‌لیست)

## ۷. دستور شروع برای هماهنگ‌کنندهٔ جدید

۱. این فایل را بخوان.
۲. فایل‌های بخش ۵ را بخوان.
۳. `ruflo memory list` و بازیابی کلیدهای `p2/*` را انجام بده.
۴. وضعیت چت‌ها را از کاربر بپرس و ادامه بده.
