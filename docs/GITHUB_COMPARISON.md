# مقایسه ورک‌اسپیس با مخزن گیت‌هاب

> مخزن اول: `github.com/rezaa2544/payesh` — فقط نسخهٔ سروری (`school.rar`)
> مخزن دوم: `github.com/rezaa2544/p2` — **کامل** (`New folder.rar`، شامل فایل مرجع تک‌فایلی)
> تاریخ بررسی: ۱۴۰۴/۰۶/۱۱

---

## فایل مرجع پیدا شد

در مخزن `p2` فایلی که لازم داشتم موجود بود:

```
New folder/پایش-سامانه-مدیریت-مدرسه.html   →  ۵۳۴ KB (۵۰۱٬۶۶۹ کاراکتر)
```

این همان نسخهٔ کاملی است که در `tests/standalone.mjs` نسخهٔ سروری به آن ارجاع می‌شد — نسخه‌ای که فایل مبنای من (`نسخه ۹`) از آن عقب‌تر بود.

⚠️ چند فایل جانبی (`.local`, `.git/hooks`) خراب استخراج شدند، اما **فایل مرجع سالم و کامل** خوانده شد.

---

## نتیجهٔ مقایسه — دقیق و عددی

### توابع `view*`

| | تعداد |
|---|---|
| مرجع | **۳۶** |
| نسخهٔ من | **۳۰** |
| مشترک | ۳۰ |
| **غایب در من** | **۶** |
| در من و نه در مرجع | **۰** |

**تأیید مهم:** صفر مورد اضافه یعنی من هیچ چیزی را حذف نکرده‌ام؛ فقط این ۶ صفحه هرگز در نسخهٔ ۹ نبوده‌اند.

### ۶ صفحهٔ غایب

| صفحه | خطوط تابع view | توضیح |
|---|---|---|
| `viewLifecycle` | ۱۳۶ | چرخهٔ تحصیلی: ارتقای پایه، فارغ‌التحصیلی، انتقال، تعارض کد ملی، کاربران ناقص |
| `viewImport` | ۷۰ | ویزارد ورود اکسل با خوانندهٔ بومی xlsx |
| `viewFormsSms` | ۴۶ | فرم‌های رسمی چاپی + پنل پیامک با کیف پول |
| `viewMeetings` | ۳۲ | جلسات اولیا و مربیان (نوبت‌دهی و رزرو) |
| `viewGrowth` | ۳۲ | رشد مدرسه: کد معرف، نرخ تبدیل، دعوت گروهی |
| `viewAtRisk` | ۲۶ | افت تحصیلی: امتیاز ریسک، هشدار به خانواده |

### وابستگی‌های کامل این ۶ صفحه

**۲۰ تابع غایب** (شامل ۶ تابع view بالا + ۱۴ تابع کمکی):
```
generateP16      gradeFromName    graduateStudent   moveStudent
recordConflict   incompleteUsers  atRiskList        smsWalletOf
printableDoc     parseCSV         parseXLSX         prepSheet
suggestMap       validateImport
```

**۱۵ اکشن غایب:**
```
promote-run  tr-send      school-clearf   invite-parents
mtg-new      mtg-save     mtg-book-ok     sms-new
sms-send     sms-topup    sms-topup-ok    imp-preview
imp-commit   imp-back     imp-reset
```

**۷ مجموعهٔ دادهٔ غایب:**
```
student_transfers   transfer_requests   student_archive   nid_conflicts
meeting_slots       sms_wallet          sms_log
```

**۶ روت غایب در `TITLES`/`NAV`:**
```
lifecycle   import   formssms   meetings   growth   atrisk
```

---

## آنچه من دارم و مرجع ندارد

هیچ‌یک از بهبودهای من در مرجع نیست:

| قابلیت | ماژول من | وضعیت در مرجع |
|---|---|---|
| فیلترهای جمع‌شونده (۱۴ روت) | `25-filters-ui.js` | ❌ |
| کتاب‌های درسی (۱۴ رشته) | `26-curriculum.js` | ❌ |
| لایه offline-first | `27-sync.js` | ❌ |
| لایه ایندکس | `28-indexes.js` | ❌ |
| محدودهٔ داده | `29-scope.js` | ❌ |
| محافظت از پرشدن localStorage | `03-persistence.js` | ❌ |

### مقایسهٔ کارایی — مرجع همان مشکلاتی را دارد که من رفع کردم

| مشکل | مرجع | نسخهٔ من |
|---|---|---|
| جستجوی خطی در `viewAttendance` | ❌ دارد | ✅ رفع شد (ایندکس Map) |
| ۱۴ پیمایش کامل در داشبورد | ❌ دارد | ✅ رفع شد (یک پیمایش) |
| `saveLog` خطا را بی‌صدا می‌بلعد | ❌ دارد | ✅ رفع شد (هشدار) |

یعنی اگر این ۶ صفحه را از مرجع بیاورم، باید **با الگوی ایندکس‌شدهٔ خودم** بازنویسی شوند، نه کپی مستقیم.

---

## جمع‌بندی

1. ✅ **هیچ چیزی جا نینداخته‌ام** — صفر تابع در من هست که در مرجع نباشد.
2. ⚠️ **۶ صفحه** (۲۰ تابع، ۱۵ اکشن، ۷ مجموعهٔ داده، ۶ روت) باید افزوده شود.
3. ✅ **۶ قابلیت** من دارم که مرجع ندارد.
4. ⚠️ مرجع سه مشکل کاراییِ شناخته‌شده را دارد؛ هنگام انتقال باید با الگوی جدید نوشته شوند.

### برنامهٔ پیشنهادی افزودن (به ترتیب اولویت)

| مرحله | صفحه | چرا این ترتیب |
|---|---|---|
| ۱ | `lifecycle` | بزرگ‌ترین و پایه‌ای‌ترین؛ ۴ مجموعهٔ داده می‌آورد |
| ۲ | `meetings` + `growth` + `atrisk` | کوچک و مستقل؛ سریع تمام می‌شوند |
| ۳ | `formssms` | نیازمند `sms_wallet` و `printableDoc` |
| ۴ | `import` | پیچیده‌ترین (خوانندهٔ xlsx بومی با `DecompressionStream`) |
