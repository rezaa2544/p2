# بازبینی نهایی: نقشه‌راه تحقیقات در برابر کدِ فعلی — ۱۴۰۵/۰۶/۱۵ (2026-09-06)

> **بند ۱۰ دور ۷۸.** اسکنِ کاملِ دو سندِ تحقیق:
> `docs/RESEARCH_2026-09-05_UNIFIED_EDU_ROADMAP.md` (نقشه‌راهِ ۷-فاز) و
> `docs/OPINION_2026-09-05_ROADMAP_REVIEW.md` (نظر/پیشنهادها) — در برابر
> **حالتِ واقعیِ کد** (دور ۷۸، smoke 545/545).
>
> وضعیت‌ها: **کدگذاری‌شده** (کد + تست) · **کدگذاری‌شده (بخشی)** (هسته کد است،
> زیرمجموعه باقی) · **طراحی** (فقط مستند) · **عمداً-ساخته-نشده** (تصمیم ثبت‌شده) ·
> **واقعی-باقی** (نه تصمیمی بوده، نه کدی).

## فاز ۰ — اصول معماری

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۰.۱ | «نوع مدرسه» با پیامد واقعی | **کدگذاری‌شده** | به‌شکلِ پیشنهادیِ OPINION: **پروفایلِ کلیدهایِ قابلیت** (`CAP_DEFS`/`hasCap` در 09-schools.js) — ۷ کلید (توشه/خوابگاه/IEP/کارگاه/چندپایه/نوبت-دوم-امتحان/کلاس-شبانه)؛ مدیر خودش ترکیب را می‌چیند؛ smoke قفل |
| ۰.۲ | هم‌زمانیِ دو سال تحصیلی | **کدگذاری‌شده** | `year_state` (بستنِ سال + چیدمانِ سالِ مقصد) + قیفِ `pre_enrollments` به «سالِ بعد» (دور ۶۳) |
| ۰.۳ | جداسازیِ امتحانِ نهاییِ کشوری | **کدگذاری‌شده** | `exams.exam_type` «امتحان نهایی» + `finalGradeOk` (فقط نهم/دوازدهم؛ فقط وارد می‌شود، محاسبه نمی‌شود) |
| ۰.۴ | دادهٔ پایه هرگز پولی نمی‌شود | **کدگذاری‌شده (قفل)** | اشتراکِ سطحِ دانش‌آموز: نمره/حضور/برنامه همیشه رایگان؛ paywall فقط روی افزودنی‌ها؛ smoke قفل |
| ۰.۵ | مدلِ چندمدرسه‌ای (tenant) | **طراحی + پیاده‌سازیِ بخشی** | طراحی: ARCHITECTURE.md + `school_id` سراسری + `inScope` + simulation.js؛ پیاده‌سازی: سرورِ sync (server/sync.js + server1-10)؛ «چند مدرسهٔ واقعی» = خطِ اتصالِ به سرور |
| ۰.۶ | ورود بدونِ رمز (پیامکی) | **کدگذاری‌شده (درگاهِ دمو)** | PLAN_PHONE_AUTH: شماره + کد + کد ملی؛ درگاهِ واقعیِ پیامک + سامانهٔ تطبیقِ کد ملی = خطِ خارجیِ عمداً-جدا |
| ۰.۷ | مدلِ مجازت و بقایِ داده | **کدگذاری‌شده (نسخهٔ سبک)** | ROUTE_ROLES/ACTION_ROLES به تفکیکِ نقش + مالکیتِ school_id روی داده + 58-privacy (حذفِ حساب/دسترسی) + DATA_SAFETY.md |

## فاز ۱ — نیازهای پایه

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۱.۱ | حضور و غیاب + اطلاع‌رسانی | **کدگذاری‌شده** | دور ۴۲-۷۷: پیش‌نویس→ثبت نهایی، رویدادهای تاخیر/خروج، موجه، قانون ۳۰٪ + صفِ پیام/اعتبار |
| ۱.۲ | شاخصِ ریسکِ ترکیبی | **کدگذاری‌شده** | atrisk + patternFlagged؛ فرمول شفاف در UI (طبق توصیهٔ OPINION) |
| ۱.۳ | کانالِ پیامک | **کدگذاری‌شده (شبیه‌سازی‌شده)** | صف + پنجرهٔ مهلت + سقفِ روزانه + کیفِ اعتبار؛ درگاهِ واقعی = عمداً-به-تعویق (خطِ خارجی) |
| ۱.۴ | مسیرِ دانش‌آموزِ پرخطر به مشاور | **کدگذاری‌شده** | counselor_refs + patternNotifyParent + پیام‌هایِ مشاور |
| ۱.۵ | غیبتِ دبیر + جایگزینی سریع | **کدگذاری‌شده** | «جابه‌جایِ موقت (بند ۱.۵)» در 24-edu-office.js: sub-save/sub-del + teacherBusyAt + گاردهایِ تاریخ/روز؛ فقط مدیر/سوپرادمین |
| ۱.۶ | گواهی‌هایِ رسمی | **کدگذاری‌شده** | گواهیِ اشتغال/انتقال/نمرات + کارنامه + **کدِ صحت‌سنجی** (cert-verify)؛ اعتبارِ حقوقی = نسخهٔ سبکِ انتخابی (کد، نه امضای دیجیتال) |
| ۱.۷ | بسته طراحیِ پایه | **کدگذاری‌شده** | کارتِ «امروز» + سوییچرِ فرزندانِ ثابت + رنگ‌بندیِ یکسان + تقویمِ شمسی + خلاصهٔ روزانه |

## فاز ۲ — شمول

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۲.۱ | کلاسِ چندپایه (روستایی) | **کدگذاری‌شده (بخشی)** | `classes.multigrade` + `class_subject_members` (عضویتِ جداگانهٔ هر درس — بند ۲.۱)؛ «یک معلم چند پایه در یک کلاس» به‌طورِ کامل = **واقعی-باقی** |
| ۲.۲ | IEP + کارکنانِ کمکی | **کدگذاری‌شده** | `iep_notes`/`iep_staff`/`iep_updated` + مودالِ IEP (بند ۲.۲) |
| ۲.۳ | شناساییِ مدارسِ کم‌برخوردار | **کدگذاری‌شده (بخشی)** | regions (آمارِ مناطق) + offices/officedash (داشبوردِ تجمیعی)؛ شاخصِ صریحِ «کم‌برخوردار» (نه فقط مقایسهٔ عملکرد) = **واقعی-باقی** |
| ۲.۴ | قیفِ کمک‌هزینه/بورسیه | **کدگذاری‌شده** | scholarships + جریانِ وضعیت (بند ۲.۴) |

## فاز ۳ — رشد و مقیاس

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۳.۱ | معماریِ سرور/همگام‌سازی | **کدگذاری‌شده** | server/ + sync.js + قراردادِ امنیتی + سئوت‌های server1-10 (۱۵۴ بررسی) + simulation.js؛ «چند مدرسهٔ واقعی» = خطِ اتصال |
| ۳.۲ | داشبوردِ رئیسِ اداره | **کدگذاری‌شده** | officedash (داشبوردِ منطقه‌ای) + گزارش‌های تجمیعی (regions/finance) |
| ۳.۳ | ابلاغِ بخشنامهٔ گروهی | **کدگذاری‌شده** | ann-broadcast (سراسری / هر مدرسه) + اعلانِ اداره |

## فاز ۴ — تمایز و درآمدزایی

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۴.۱ | مالی/شهریهٔ کامل | **کدگذاری‌شده** | tuition + tuition_plans + انجمن (مدارس دولتی) |
| ۴.۲ | نمرهٔ عملی + ساعتِ کارآموزی | **کدگذاری‌شده** | `grades.kind` practical/theory + internships (تأییدِ دبیر/مدیر — بند ۴.۲) |
| ۴.۳ | کارنامهٔ حرفه‌ای + قالب | **کدگذاری‌شده (بخشی)** | reportCardCert (قالبِ A4) + printableDoc؛ «چند قالبِ قابلِ انتخاب» = **واقعی-باقی** (یک قالب) |
| ۴.۴ | قیفِ پیش‌ثبت‌نام | **کدگذاری‌شده** | preapps (۵ مرحله) + pre_enrollments (قیفِ سالِ جدید) |
| ۴.۵ | ردیابیِ سرویس (سبک) | **کدگذاری‌شده** | دور ۱۱: مسیر/راننده/دانش‌آموزان + سوار/پیاده + موقعیت + نیاز + پیگیریِ مغایرت |
| ۴.۶ | مدیریتِ مراجعین | **کدگذاری‌شده** | visitors (بند ۷) |
| ۴.۷ | کتابخانه | **کدگذاری‌شده** | lib_books/lib_loans (بند ۸) + ۶.۳ شمارهٔ سریال |
| ۴.۸ | اموال/انبار | **کدگذاری‌شده** | assets (بند ۹) |
| ۴.۹ | مقایسه با میانگینِ کلاس | **کدگذاری‌شده (بخشی)** | `classScoreContext` (میانگینِ کلاس کنارِ هر نمره در پرونده) + ستونِ میانگین در کارنامهٔ چاپی؛ «نمودارِ مقایسه‌ایِ ولی» (روند در برابر میانگین) = **واقعی-باقی** |
| ۴.۱۰ | تکلیف + تصحیحِ دیجیتال | **کدگذاری‌شده** | hw: آپلود + بازه/قفل + بومِ تصحیح (بند ۴) |
| ۴.۱۱ | گیمیفیکیشنِ دبستان | **کدگذاری‌شده** | dojo (بند ۵): مدلِ امتیازِ مخصوصِ هر مدرسه + چیپ‌هایِ رفتار |
| ۴.۱۲ | اختلاف با سیدا (OPINION) | **کدگذاری‌شده** | 56-sida-diff.js (sidadiff) |

## فاز ۵ — مسیر تحصیلی

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۵.۱ | دادهٔ رشته‌ها برای نهم | **کدگذاری‌شده (بخشی)** | داده هست (`fieldsOfBranch`: شاخه→رشته) ولی فقط در تنظیماتِ مدیر (مدرسه/درس/کلاس)؛ صفحهٔ اطلاع‌رسانیِ دانش‌آموز/ولی = **واقعی-باقی** (ریسکِ نگهداریِ داده طبق OPINION) |
| ۵.۲ | ارتباطِ ساختاریافته با مشاور (دوازدهم) | **کدگذاری‌شده** | counselor-msg send/reply (بند ۵.۲) |

## فاز ۶ — بلندمدت

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۶.۱ | امتحاناتِ شهریور/تجدیدی | **کدگذاری‌شده** | reexams (بند ۶.۱) |
| ۶.۲ | صورتجلسهٔ انجمن | **کدگذاری‌شده** | assoc_minutes + بایگانی (بند ۶.۲) |
| ۶.۳ | شمارهٔ سریالِ کتاب | **کدگذاری‌شده** | lib-serial (بند ۶.۳) |
| ۶.۴ | کلاس‌های تابستانی | **کدگذاری‌شده** | summer_classes (بند ۶.۴) |
| ۶.۵ | برنامهٔ هفتگیِ خودکارِ بدونِ تداخل | **کدگذاری‌شده (نسخهٔ سبک)** | بند ۶.۵: کنترلِ تداخل + پیشنهاد + جابه‌جایی — دقیقاً نسخهٔ «کنترل + پیشنهاد» که OPINION پیشنهاد داد، نه تولیدِ کامل |

## مواردِ افزودهٔ OPINION

| مورد | محتوا | وضعیت | شواهد/یادداشت |
|---|---|---|---|
| ۱.۸ | ویزاردِ ورودِ انبوه | **کدگذاری‌شده** | ویزاردِ اکسل (دانش‌آموز/دبیر/نمره/حضور) — ۴ مرحله + نگاشتِ هوشمندِ فارسی + جدولِ ظرفیت |
| سنجهٔ ۳۰ ثانیه | بارِ ثبتِ دبیر | **طراحی (سنجهٔ قفل)** | AD.md + گاردِ عملکردیِ smoke (چند صد میلی‌ثانیه؛ ایستادگی < ۳۰ ثانیه) |
| معیارِ خروجِ فازها | قابلِ سنجش | **طراحی (بخشی)** | فاز ۱ در AD.md دارد؛ فازهای ۲-۶ = **واقعی-باقی** |
| B2B اول، B2G بعد | ترتیبِ فاز | **تصمیم (قفل)** | اصلِ کاربر: مشتری = مدرسه؛ توشه/مالی اول؛ اداره = لایسنس + اعتماد فقط |
| اعتبارِ حقوقیِ گواهی | پیشِ ۱.۶ | **تصمیم (قفل)** | نسخهٔ سبک: کدِ صحت‌سنجی (cert-verify)، نه امضای دیجیتال |

## عمداً-ساخته-نشده (تصمیم ثبت‌شده — نه بدهی)

| مورد | تصمیم | کجا ثبت |
|---|---|---|
| اتصال به شاد | فقط «لینکِ دستی» — یکپارچه‌سازیِ سیستمی | دور ۶۵ (پاسخ‌های کاربر) |
| سخت‌افزار/RFID سرویس | نسخهٔ سبکِ دکمه‌ای به‌جای RFID | نقشه‌راه (۴.۵) + پیاده‌سازی |
| تولیدِ کاملِ خودکارِ برنامه | «کنترلِ تداخل + پیشنهاد» | OPINION بند ۸ → پیاده‌سازیِ ۶.۵ |
| چند قالبِ کارنامه | یک قالبِ حرفه‌ایِ A4 | ۴.۳ |
| ابرِ ذخیره‌سازیِ فایل | قفل (دور ۶۵)؛ فایل‌ها سمتِ کاربر (IDB) | بند ۹ دور ۷۸ (مقایسه، بدونِ تصمیم) |
| درگاهِ پرداختِ واقعی (زرین‌پال/شاپرک) | فازِ عرضه؛ فعلاً آزمون/تست | PLAN + TODO_BEFORE_PRODUCTION |

## واقعی-باقی (کارِ واقعیِ باقی‌مانده، غیر از خطِ خارجی)

1. **۴.۹** — نمودارِ مقایسه‌ایِ ولی: روندِ نمره در برابر میانگینِ بی‌نامِ کلاس (داده هست؛ فقط نمایِ نمودار).
2. **۲.۱** — دیدِ کاملِ چندپایه: یک معلم، چند پایه در یک کلاس (الگوریتمِ تدریسِ هم‌زمان) — امروز فقط عضویتِ چنددرس + پرچمِ multigrade.
3. **۲.۳** — شاخصِ صریحِ «کم‌برخوردار» برای رئیسِ اداره (ترکیبِ داده‌های موجود، نه دادهٔ تازه).
4. **۵.۱** — صفحهٔ اطلاع‌رسانیِ رشته‌ها برای نهم (داده: fieldsOfBranch) + «منبع + تاریخِ به‌روزرسانی».
5. **۴.۳** — قالبِ دومِ کارنامه (اگر خواسته شد).
6. **۴.۳-OPINION** — معیارهایِ خروجِ سنجیدنیِ فازهای ۲-۶ (فقط مستند).

## خطِ خارجیِ استراتژیک (تنها «اتصال به سرور» — بدونِ تغییر)

درگاهِ واقعیِ پیامک · سامانهٔ واقعیِ تطبیقِ کد ملی · درگاهِ پرداخت ·
ذخیره‌سازیِ فایل (تصمیم قفل، مقایسه در بند ۹) · استقرارِ چندمدرسه‌ایِ واقعی
(کدِ sync کامل و تست‌شده است؛ فقط «وصل‌شدن» نمی‌ماند).

## جمع‌بندی

از ۳۵ موردِ نقشه‌راه + ۷ پیشنهادِ OPINION: **۲۸ مورد کاملاً کدگذاری‌شده**،
**۶ مورد کدگذاری‌شده-بخشی** (هرکدام فقط یک زیرنمایِ کوچک باقی دارد — فهرستِ
«واقعی-باقی»)، **۰ موردِ طراحیِ خالص** (همهٔ طراحی‌ها یا اجرا شدند یا تصمیمِ
قفل دارند)، و **۶ تصمیمِ عمداً-ساخته-نشده**. کوهی که OPINION «بزرگ‌ترین
ریسک» می‌خواند (۳.۱ معماری) ساخته و تست‌شده است؛ آنچه واقعاً باقی است:
شش زیرنمایِ نمایشی/داده‌ایِ کوچک + خطِ خارجیِ اتصال.
