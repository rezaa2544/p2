# گزارش روزانه — ۲۰۲۶-۰۹-۱۳ · چت ۶ (حاکمیت مستندات و ممیزی PRها)

**شاخه:** `arena/01a09801-p2` · **پایه:** `main` @ `23bf513` (ادغام‌شده؛ **بدون rebase شاخه‌های دیگران**)
**قفل مستندات:** `rc41` → **`rc42`** (با افزودنِ همین سند) · **دامنه:** فقط لایهٔ docs/governance — **هیچ تغییر کدی**

## خلاصهٔ اجرایی

دور پ۶ بر تثبیتِ حاکمیت مستندات، ممیزیِ PRهای باز، و اصلاحِ اعدادِ نادرستِ ابلاغ‌شده تمرکز داشت.

- قفل مستندات **rc41** تثبیت شد (۳۷۰ درخت / ۴۶۶ تست) و با افزودنِ این سند به **rc42** ارتقا یافت.
- **قالبِ PR** (`.github/pull_request_template.md`) برای جلوگیری از رانشِ آماریِ PRهای docs-only ساخته شد — پیش‌تر وجود نداشت.
- پچ‌های **شکافتِ PR #135** تولید و در کامنتِ همان PR ثبت شد؛ خودِ PR باز/بسته/مرج **نشد** (قاعدهٔ ۶).
- **حکم counselor** با ارجاعِ تصحیح‌شده ثبت شد (`2026-09-12.md` §«بند ۱.۳ فاز ۱»).

## تصحیح‌های حیاتی (جلوگیری از fake-green)

1. **شکافِ academic/finance:** عددِ «۲۵–۴۰× کندتر» **رد شد** — `REPORT_PERF_GAP_SPEC.md` §۰ صریحاً می‌گوید «هیچ‌کدام در اسناد این مخزن نیست». واقعیتِ measured: **~۲–۳× کندتر زیر بارِ هم‌زمانی** — بنچ ۵۰ درخواستِ هم‌زمان @200k حضور/60k نمره، Pool max=10: مسیرِ حافظه **p50≈۱۰۴–۱۰۸ms** در برابر DB-native **p50≈۲۲۶–۳۲۱ms و p95≈۳۶۴–۴۱۶ms**. در تک‌درخواست، EXPLAIN نشان می‌دهد ایندکس‌ها کار می‌کنند: academic **7.8ms** · finance **1.3ms + 1.3ms**. مقیاسِ ملی = **NOT-RUN**.
2. **حذفِ `OR IS NULL` (tenancy):** بهبودِ ~۱۹۰× متعلق به **ایندکسِ کرسرِ مرکبِ attendance** است (Seq 14.1ms → Index 0.074ms). بهبودِ واقعیِ tenancy **~۴۸۰× روی میانه** است (۱۰۵.۱۶۸ms → ۰.۲۱۹ms؛ `Rows Removed by Filter: 648,000`).
3. **دوزیهٔ counselor:** در `docs/daily-reports/2026-09-12.md` §«دوزیهٔ counselor (بند ۱.۳ فاز ۱)» یافت شد — جستجوی پیشینِ چت ۶ ناقص بود (خطای خودم، ثبت‌شده در C-4).

## خطاهای خودم (شفاف‌سازی)

- **تخریبِ کارِ ثبت‌نشده:** استفاده از `git checkout` برای آزمودنِ پچ، ویرایش‌های فاز ۱ و ۲ را پاک کرد ⇒ بازسازیِ کامل + جایگزینی با `git apply --check` (بدون دستکاریِ درخت).
- **تلهٔ `.gitignore` خط ۶۶ (`*.patch`):** پچ‌های ممیزی را پنهان کرد ⇒ `git add -f` (کامیت `7f9b33e`) + **ویرایشِ کامنتِ #135** با SHAی درست.
- **تعارضِ ادغام با main:** `DOCS_FREEZE_v1.0.0-rc40.md` را main بازهمگام کرده بود ⇒ محتوای main (آخرین وضعیتِ واقعی) + بنرِ تاریخیِ من؛ مانیفست دست‌نخورده ماند.

## وضعیتِ گیت‌ها (Measured — محلی، همین سندباکس)

| گیت | نتیجه |
|---|---|
| `tests/smoke.js` | **547/547** ✅ (۱ هشدارِ کنسولِ بی‌خطر: `window.scrollTo` در jsdom) |
| `tests/run.js` | **35/35** ✅ |
| `tests/secret-scan.js` | **11/11** ✅ |
| `build.js --check` | ✅ (۴ چک) |
| `tools/reza-mirror-check.js` | ✅ همهٔ آینه‌های زنده برابر با `docs/` |
| `tools/migrate-helper.js --check` | ✅ (شمارهٔ بعدی: **011**) |
| `tests/docs-freeze-marker.js` | **14/14** ✅ |
| `tools/docs-stats-sync.js --check` | ✅ |
| `tools/docs-metadata.js` | ✅ **یتیم: ۰** |
| `tests/docs-metrics.js` · `docs-index-coverage.js` | **11/11** · **75/75** ✅ |
| `tools/docs-consistency-check.sh` | **۴۹ هماهنگ / ۰ تعارض** ✅ |
| `tools/docs-refs-check.js` | ⚠️ exit 0 — «تازه = **۱۹**» = ۱۱ پیش‌موجود + **۸ آگاهانه از چت ۶** |

**NOT-RUN:** گیت‌های PG (`wave23-reports-pg` · `wave3-parity` — فقدانِ باینری/پردازه/لیسنر در سندباکس) · **CI** (انسدادِ بیلینگ — ادعای سبز نمی‌کنم) · بنچ مقیاس ملی / P0-4.

## بودجهٔ ورک‌اسپیس (§۱ WORKSPACE_HYGIENE)

| مرحله | حجم | فایل‌ها | چراغ |
|---|---|---|---|
| پیش از پاک‌سازی | 94.5 MB | ۱,۴۲۰ | 🟢 |
| پس از حذفِ `dist/` + `.build-cache.*` | 92.9 MB | ۱,۴۱۸ | 🟢 |
| پایانِ دور | 94.5 MB | ۱,۴۲۰ | 🟢 |

worktree = ۱ (≤۲) ✅ · `node_modules`/`dist`/`build`/`.cache` طبق §۱ مستثنی‌اند.

## قلم‌های باز (ارجاع به نشست بعد)

1. **ACCEPT کردن PR #129** — بازکنندهٔ ۸ ارجاعِ آگاهانه (HO-4) و دوزیه‌های Flag-1 / Cache-Isolation؛ بدون آن هر دورِ docs یک بدهیِ ارجاع دیگر می‌سازد.
2. **تعیین تکلیفِ شکافتِ PR #135** — پچ‌ها در `tools/audit-patches/` آماده‌اند (بخش ب = ارزشِ یکتا، بخش الف = مخرب)؛ تصمیم فقط با مالک.
3. **پیاده‌سازیِ حکم counselor** — مالک: **چت ۴ / چت ۳**؛ تا آن ردیف **هیچ کدی تغییر نمی‌کند**.
4. **رفعِ DIRTYِ ۹ PR باز** — نیازمند rebase توسط صفِ ادغام (چت ۵/۷)، نه چت ۶.
5. **دورِ شکافِ هم‌زمانی** (چت ۳) با عددِ اصلاح‌شدهٔ **۲–۳×** و روی شاخهٔ مستقل تا با stackِ #129 تداخل نسازد.

## مراجع

- ثبتِ رسمیِ تصمیم‌ها: [`docs/SUPERVISOR_DECISIONS_2026-09-13.md`](../SUPERVISOR_DECISIONS_2026-09-13.md)
- چک‌لیستِ مرجِ docs-only: [`docs/DOCUMENTATION_MAINTENANCE.md`](../DOCUMENTATION_MAINTENANCE.md) §۵٫۱
- قالبِ PR: `.github/pull_request_template.md`
- گزارشِ دور پ۶: `CHAT6_ROUND_P6_REPORT_2026-09-13.md` (ریشهٔ ریپو)

---

## دور ۷ — ممیزیِ بستهٔ هماهنگ‌کننده و ردِ دستورات نادرست (ثبت: 2026-09-13)

**بستهٔ ورودی ممیزی شد: ۴ دستورِ غیرقابل‌اجرا و ۳ انتسابِ نادرستِ مالکیت با شاهد رد شدند.**

| ادعا/دستور | حکم | شاهد |
|---|---|---|
| `cp` دو دوزیه به `reza/` | 🔴 رد — فایل‌ها در این شاخه نیستند | `ls docs/REPORT_CACHE_ISOLATION_DOSSIER.md docs/REPORT_PERF_GAP_SPEC.md` ⇒ No such file (فقط در شاخهٔ PR #129)؛ افزون بر این ساختِ **آینهٔ تازه** طبق قاعدهٔ ایستا ممنوع است |
| بامپِ rc41 → rc42 | 🔴 رد — تکراری | از قبل در `b7880f6` انجام شده؛ HEAD == remote |
| `git add server/` | 🔴 رد — بدون دیف | `git diff --name-only 23bf513 HEAD -- server/` ⇒ خالی |
| «ممیزیِ ۱۳۴ PR انجام شد» | 🔴 انتسابِ نادرست | مالِ **PR #151** (مرج ۰۰:۲۳Z) — `2026-09-13-pr-audit-final.md`؛ نه کارِ چت ۶ |
| «دوزیه‌های کش/شکاف تدوین شدند» | 🔴 انتسابِ نادرست | نویسنده: **PR #129**؛ کارِ چت ۶ = ثبتِ حکم + تصحیحِ اعداد |
| «تله‌متری در `server/metrics.js`» | 🔴 انتسابِ نادرست | با **PR #133/#147** وارد main شد؛ تفاضلِ چت ۶ در `server/` = صفر فایل |

**کارِ واقعیِ چت ۶ در این روز (انتسابِ صحیح):**
ثبتِ ۵ حکمِ ناظر + ۴ تصحیحِ عددی (C-1..C-4) · قالبِ PR + چک‌لیستِ §۵٫۱ ·
بامپِ rc40→rc41→rc42 · پچِ شکافتِ #135 + کامنتِ ممیزی · حکمِ counselor ·
بستنِ ۸ یتیم (۱ + ۷) · ادغامِ main@23bf513 · پرامپتِ هندآف (`98c462a`).

**تصمیمِ هماهنگ‌کننده:** بدون فایلِ `…-chat6-final.md` (همپوشانی + برخوردِ نام + بامپِ سوم)؛
ثبتِ این دور = الحاق به همین فایل. **rc43 ساخته نشد** — شمارِ اسنادِ ریشه تغییر نکرد
(تنها تغییر: ویرایشِ `DOCUMENTATION_MAINTENANCE.md` ⇒ بازتولیدِ درجای مانیفستِ rc42 با `--freeze`).

**پروتکلِ بامپِ قفل در کارِ موازی** در `docs/DOCUMENTATION_MAINTENANCE.md` §۵٫۲ کدگذاری شد.

### ⚠️ یافتهٔ تازه — برخوردِ بامپِ موازی با PR #159 (ثبتِ پیشگیرانه)

`gh pr view 159 --json files` نشان می‌دهد آن شاخه (base=main، **DIRTY/CONFLICTING**) این فایل‌ها را
تغییر می‌دهد: `docs/DOCS_FREEZE_v1.0.0-rc41.md` (**جدیدِ واگرا**) · `docs/DOCS_FREEZE_v1.0.0-rc40.md` ·
`docs/DOCS_METRICS.md` · `docs/DOCUMENTATION_MAP.md` · `tests/docs-freeze-marker.js` —
یعنی دقیقاً همان نقطه‌ای که پروتکلِ §۵٫۲ جلویش را می‌گیرد: **دو rc41 با دو مانیفستِ متفاوت**.

**اقدامِ چت ۶:** صفر تغییر روی #159 (بلاک‌شده طبق مرزهای مالکیت). **توصیه برای مرج:** rc41ِ چت ۶
(که زیرِ rc42 تاریخی شده) مرجع بماند و شاخهٔ #159 پیش از مرج روی آن بازپایه شود؛ در غیر این صورت
`tests/docs-freeze-marker.js` روی main با دو قفلِ مدعی روبه‌رو می‌شود.

### گیت‌ها (فقط مسیرِ تغییر)

| گیت | نتیجه |
|---|---|
| `node tools/docs-stats-sync.js --check` | ✅ ۰ کهنه (بازتولیدِ درجای rc42) |
| `node tests/docs-freeze-marker.js` | **14/14** ✅ |
| `node tools/docs-metadata.js` | ✅ یتیم: هیچ |
| `node tools/reza-mirror-check.js` | ✅ بدون رانش (فقط چک؛ **هیچ `cp` دستی و هیچ آینهٔ تازه**) |

`node tests/docs-metrics.js` **11/11** ✅ · `node tests/docs-index-coverage.js` **75/75** ✅ ·
`bash tools/docs-consistency-check.sh` **۴۹ هماهنگ / ۰ تعارض** ✅

`run`/`smoke` بازاجرا نشدند — **بدون تغییرِ کد** در این دور (طبق دستورِ پرهیز از تکرار).
**NOT-RUN:** گیت‌های PG (بدون باینری در سندباکس) · CI (انسدادِ بیلینگ).

**بدهیِ ارجاعی (تفکیک‌شده):** `docs-refs-check` «تازه = **۲۴**» = ۱۹ دورِ قبل + **۵ این دور**،
همگی از فایل‌های چت ۶: ۳ ارجاع به دوزیه‌هایِ **فقط‌در-#129**
(`REPORT_PERF_GAP_SPEC` ×۲ · `REPORT_CACHE_ISOLATION_DOSSIER` ×۱) که با ACCEPTِ #129 موجود می‌شوند،
و **۲ ارجاعِ پیش‌رو** در پرامپتِ هندآف به خروجی‌هایِ آتیِ چت ۳
(`docs/REPORT_CONCURRENCY_SPEC_2026-09-13.md` · `docs/daily-reports/2026-09-13-chat3-concurrency.md`)
که با ساخته‌شدنِ همان اسناد بسته می‌شوند. حذف نشدند (حذفِ ارجاع برای سبز نگه‌داشتنِ ابزار = fake-green)
و به `docs-refs-baseline.json` دست نخورده است (قلمروی چت ۸، طبق HO-1).

---

## دور ۱۰ — ممیزی همگرایی #159 + آمادگی پچ‌های #135 (ثبت: 2026-09-13)

### فاز -۱ و ۰ — رفعِ مانع و بازیابی
- مانعِ دورهای ۸/۹ در ابتدای این دور **رفع شد** (`echo probe` ⇒ موفق).
- اما **`.git` محلی باز هم به پایه (`523c1f5`) برگشته بود** ⇒ بازیابی با `fetch` + `reset --mixed c9e62fb`
  (**بدون force-push**) ⇒ HEAD `c9e62fb` · درخت تمیز.
- **main جلو رفته است:** `23bf513` → **`0a208f8`** (`compare` ⇒ `ahead_by=106, behind_by=0`؛ PR #166–#173،
  از جمله #171 «نقشهٔ تعارضِ #129/#157/#159» و #173 «بستن ۱۴ سند یتیم»).
- وضعیتِ PRها (اندازه‌گیری‌شده): #129 `OPEN/DIRTY` · #157 `OPEN/UNSTABLE` · #159 `OPEN/DIRTY` ·
  #135 `CLOSED/DIRTY` — **صفر تغییر/کامنت/rebase از سوی چت ۶**.

### فاز ۱ — جدول واگراییِ #159 (فقط‌خواندنی؛ استخراج از head `80c5910`)

| فایل | نسخهٔ #159 | نسخهٔ main (`0a208f8`) | نسخهٔ من (rc42) | واگرایی (اندازه‌گیری) |
|---|---|---|---|---|
| `DOCS_FREEZE_v1.0.0-rc41.md` | **جاری** (جانشین rc40) | وجود ندارد | **تاریخی** (زیرِ rc42) | #159↔من: ۳۶+/۳۸− |
| `DOCS_FREEZE_v1.0.0-rc40.md` | تاریخی (با rc41) | **جاری** | تاریخی (با rc41) | #159↔من ۲۱+/۱۸− · #159↔main ۶+/۴− |
| `DOCS_METRICS.md` | ۱ خط متفاوت | — | — | #159↔من ۱+/۱− · #159↔main ۱+/۱− |
| `DOCUMENTATION_MAP.md` | کل **۳۷۳** · فعال ۳۰۹ | — | **۳۷۲** · فعال ۳۰۷ | #159↔من ۶+/۶− · #159↔main ۶+/۶− |
| `tests/docs-freeze-marker.js` | `FREEZE = …rc41` | `…rc40` | `…rc42` | #159↔من ۵+/۵− · #159↔main ۴+/۴− |

**یافتهٔ فراتر از فایل‌های قفل:** #159 فایلِ `docs/daily-reports/2026-09-13.md` را **+۱۶۵/−۷۸** تغییر
می‌دهد — همان فایلی که چت ۶ در دور پ۶ بخشِ خود را به آن الحاق کرد ⇒ **تعارضِ مستقیم با شاخهٔ من**
(نه فقط تقابلِ دو rc). #159 همچنین `2026-09-13-chat6-final-delivery.md` (+۱/−۱) را لمس می‌کند.
فهرستِ کامل: ۱۳ فایل · +۶۸۸/−۹۸.

**تطبیق با نقشهٔ تعارضِ چت ۷ (اکنون روی main):** آن نقشه #159 را با headِ `e29e71fc1aff` و ۱۲ فایل
«MERGEABLE/UNSTABLE» ثبت کرده و ترتیبِ مرج #159 → #157 → #129 را پیشنهاد داده است؛ اما #159 اکنون
`80c5910` و ۱۳ فایل و `DIRTY` است ⇒ آن نقشه برای #159 **کهنه** است.

**طرح همگرایی (شماره‌دار — توصیه، نه اقدام):**
1. مرج/ادغامِ شاخهٔ چت ۶ (rc41 تاریخی + **rc42 جاری**) **پیش از** #159 — تا فقط یک مدعیِ «جاری» باشد؛
2. rebase #159 روی main جاری؛ 3. **حذفِ `rc41` واگرا** از شاخهٔ #159؛
4. بازتولیدِ درجای مانیفست با `--freeze` (سندِ جدیدِ #159 در زیرپوشه است ⇒ ریشه ثابت ⇒ **بدون rc43**)؛
5. `freeze-marker` ۱۴/۱۴ + `stats-sync --check`؛ 6. حلِ دستیِ تعارضِ `2026-09-13.md`؛
7. اسنادِ جدیدِ #159 باید ارجاعِ ورودی بگیرند وگرنه `docs-metadata` یتیم گزارش می‌کند.

### فاز ۲ — آمادگی پچ‌های #135 (بدون لمسِ PR)

| پچ | نتیجهٔ اندازه‌گیری‌شده روی main@0a208f8 | وضعیت |
|---|---|---|
| `pr135-keep-partB.patch` (قدیمی) | ❌ `patch failed: docs/daily-reports/2026-09-12.md:587` | **stale** |
| `pr135-keep-partB.main-0a208f8.patch` (**بازتولیدشده**) | ✅ **`git apply --check` = CLEAN (exit 0)** | **آماده** |
| `pr135-drop-partA.patch` | ❌ stale در هر دو جهت (`forward` و `--reverse`) | مستندِ صِرف؛ اقدامِ درست = حذفِ ۴ فایل از شاخهٔ PR توسط مالک |

**روشِ بازتولید:** محتوای بخش ب استخراج شد از سرِ #135 (`0c57bb9`) ⇒ دو فایلِ جدید (۱۳۰ و ۷۶ خط؛
هیچ‌کدام روی main نیستند ⇒ ارزشِ یکتا) + **۹ خطِ الحاقیِ خالص** به انتهای `2026-09-12.md`
(تفاوتِ پایهٔ عصرِ `523c1f5` با سرِ #135 = ۹+/۰−). پچِ تازه در worktreeای تمیز روی main ساخته و
راستی‌آزمایی شد؛ پچِ قدیمی برای مقایسه همان خطا را نشان می‌دهد.

### ENGINEERING CLAIM
```
EXPECTED BEHAVIOR: #159 دارای rc41 واگرا؛ طرح = rebase + حذف rc41 + --freeze درجا
                   #135: پچ‌های بخش ب clean روی main جاری
MEASURED BEHAVIOR: #159 ⇒ واگراییِ اندازه‌گیری‌شده در ۵ فایل + تعارضِ مستقیم روی 2026-09-13.md
                   #135 ⇒ بخش ب: بازتولید شد و CLEAN است (قدیمی stale)؛ بخش الف: stale در دو جهت
TEST ENVIRONMENT:  main @ 0a208f8 (worktree موقت، فقط‌خواندنی) · شاخهٔ من @ c9e62fb
DATASET SIZE / CONCURRENCY / P50·P95·P99 / DB·CACHE·SECURITY IMPACT: n/a
ERROR RATE: ۱ خطای اندازه‌گیری‌شده (پچ قدیمی) + ۱ staleِ نمایشی (بخش الف) — هر دو مستند و پیگیری‌شده
FAILURE TEST: #159 مرج نشود ⇒ rc41 واگرا + تعارضِ 2026-09-13.md باقی می‌ماند
              #135 بازگشایی نشود ⇒ پچِ بازتولیدشده در tools/audit-patches/ آماده می‌ماند
REGRESSION RISK: صفر تغییر کد — فقط اسناد و پچ
KNOWN UNKNOWNs: CI NOT-RUN (بیلینگ) · زمان مرج #159/#135 نامشخص · PG در این سندباکس موجود نیست
FINAL VERDICT: VERIFIED (#159: جدول واگرایی با evidence) + PASS (بخش ب بازتولیدشده: apply --check clean)
```

### گیت ۸ سؤال
۱) چه تغییر کرد؟ → بازتولیدِ پچِ بخش ب روی main جاری + ثبتِ طرح همگرایی (صفر تغییر روی PRها).
۲) چرا؟ → قطعِ تکرارِ تعارضِ rc40. ۳) invariant؟ → rc42 معتبر · ریشهٔ ۳۲۶ ثابت · #135/#159 دست‌نخورده.
۴) اثبات؟ → جدولِ ۵ فایل + `apply --check` = CLEAN روی main@0a208f8 (worktree تمیز). ۵) مقیاس‌پذیری؟ → n/a.
۶) امنیت؟ → n/a. ۷) failure؟ → `apply --check` خطا می‌دهد و ثبت می‌شود (همان که برای پچ قدیمی رخ داد).
۸) rollback؟ → صفر تغییر کد؛ revertِ کامیتِ اسناد/پچ. ⇒ **همهٔ ۸ سؤال پاسخ دارند.**

### ماشین و REZA (روی شاخهٔ من)
- `docs-stats-sync --check`: ✅ (۳۲۶ ریشه · ۳۷۲ درخت · قفل rc42) ⇒ **بدون rc43** (ریشه ثابت ماند)
- `docs-freeze-marker`: **۱۴/۱۴** ✅ · `docs-metadata`: **یتیم = هیچ** ✅
- `docs-metrics` ✅ · `docs-index-coverage` ✅ · `reza-mirror`: **۰ همگام / ۰ اختراع** ✅
- `run`/`smoke` بازاجرا نشد (بدون تغییر کد) · PG/CI = **NOT-RUN**

### بدهی ارجاعی (تفکیک‌شده)
«تازه» = **۲۶** = ۲۴ دورِ ۷ + **۲ این دور** — و جالب آن‌که این ۲ مورد **از خودِ پاراگرافِ افشا** در دور ۷
برآمده‌اند (هنگام نام‌بردنِ دو خروجیِ آتیِ چت ۳). همان‌جا شرطِ بسته‌شدن‌شان نوشته شده: با ساخته‌شدنِ
`docs/REPORT_CONCURRENCY_SPEC_2026-09-13.md` و `docs/daily-reports/2026-09-13-chat3-concurrency.md`
توسط چت ۳. حذف نشدند (حذف برای سبزکردن گیت = سبز جعلی)؛ `docs-refs-baseline.json` دست‌نخورده (قلمروی چت ۸).

---

## دور ۱۱ — بازتولید نقشهٔ تعارض #159 + طرح مرج ۷مرحله‌ای (ثبت: 2026-09-13)

### فاز ۰ — وضعیتِ تازه (اندازهٔ امروز، نه شاهدِ دور قبل)
- main باز هم جلو رفت: `0a208f8` → **`952b0ba`** · شاخهٔ من `277f5b5` · **PR #157 اکنون MERGED است**.
- #129 `OPEN/DIRTY` · #159 `OPEN` · #135 `CLOSED/DIRTY` — **صفر لمس/کامنت/rebase از سوی چت ۶**.
- **`git merge` در این کلون ممکن نیست** (کم‌عمق؛ `merge-base` ناموجود حتی پس از fetch کردنِ `base.sha`).
  ⇒ روشِ جایگزینِ **اندازه‌گیری‌شده**: three-way merge با پایهٔ صریح از طریق
  `git read-tree -m <base>^{tree} <ours>^{tree} <theirs>^{tree}` و شمارشِ `git ls-files -u`
  (بدون worktree، بدون تغییر مخزن، بدون کامیت).

### فاز ۱ — تعارضِ #159 با main (پایه = `base.sha` از API = `1a73c52`)

| نوع | شمار | فایل‌ها |
|---|---|---|
| code | **۳** | `HANDOFF.md` · `monitoring/alert-rules.yml` · `out/dast-live/dry-run-skeleton.txt` |
| doc | **۵** | `DOCS_FREEZE_v1.0.0-rc40` · `DOCS_INDEX` · `DOCS_METRICS` · `DOCUMENTATION_MAP` · `daily-reports/NEXT_ACTIONS` |

**تأییدِ یافتهٔ دور ۱۰ — با یک ظرافتِ مهم:** `docs/daily-reports/2026-09-13.md` با **main تعارض ندارد**.
تعارضِ آن **با شاخهٔ چت ۶** است (فاز ۲)، نه با main — چون #159 آن را +۱۶۵/−۷۸ بازنویسی می‌کند و من در دور پ۶
بخشِ چت ۶ را به آن **الحاق** کرده‌ام. ادعای دور ۱۰ در نتیجه درست است، اما باید دقیق خوانده شود.

### فاز ۲ — تعارضِ #159 با شاخهٔ چت ۶ (پایهٔ درست = `23bf513`)

> یک تلهٔ روش‌شناختی که اصلاح شد: پایهٔ #159 (`1a73c52`) **جدِ شاخهٔ من نیست**
> (`compare/1a73c52...277f5b5` ⇒ `diverged`, ahead=9, behind=82). استفاده از آن به‌عنوان پایه،
> ۳۸ «تعارضِ» کاذب تولید کرد (تغییراتِ main به نامِ #159 نوشته شد). پایهٔ درست
> `23bf513` است (`compare/23bf513...1a73c52` ⇒ `status=ahead, behind_by=0` ⇒ جد بودن تأیید شد).

| نوع | شمار | فایل‌ها |
|---|---|---|
| code | **۱** | `tests/docs-freeze-marker.js` (من → rc42؛ #159 → rc41) |
| doc | **۸** | `DOCS_CONSISTENCY_REPORT` · `DOCS_FREEZE_v1.0.0-rc40` · **`DOCS_FREEZE_v1.0.0-rc41`** (واگرا) · `DOCS_METRICS` · `DOCUMENTATION_MAP` · `TEST_COVERAGE_REPORT` · **`daily-reports/2026-09-13.md`** · `daily-reports/NEXT_ACTIONS` |

⇒ **#159 با شاخهٔ چت ۶ تعارضِ قطعی دارد (۹ فایل)** و قلبِ آن همان `rc41` واگرا و `2026-09-13.md` است.

### فاز ۳ — طرح مرج ۷مرحله‌ای برای #159 (توصیه؛ تصمیم با ناظر/مالک)

1. **rebase** #159 روی main جاری (`952b0ba`)؛
2. حلِ تعارضِ `docs/daily-reports/2026-09-13.md` به‌صورت **اتحادِ append-only** (بخشِ چت ۶ + بلاکِ #159)؛
3. **حذفِ فایلِ `rc41` واگرا** از شاخهٔ #159 (تک‌مدعی بودنِ «جاری»)؛
4. بازتولیدِ درجای مانیفست با `node tools/docs-stats-sync.js --freeze` (ریشه ۳۲۶ ⇒ **بدون rc43**)؛
5. `node tests/docs-freeze-marker.js` ⇒ **۱۴/۱۴**؛
6. `node tools/docs-stats-sync.js --check` ⇒ سبز؛
7. دلتای انتظارِ `docs-refs-check`: اسنادِ جدیدِ #159 باید ارجاعِ ورودی بگیرند، وگرنه `docs-metadata` یتیم می‌سنجد
   (امروز روی شاخهٔ من یتیم = هیچ است).

### ENGINEERING CLAIM
```
EXPECTED BEHAVIOR: #159 با main و با شاخهٔ چت ۶ تعارض دارد
MEASURED BEHAVIOR: #159↔main ⇒ ۳ code + ۵ doc (پایه 1a73c52، از API)
                   #159↔چت ۶ ⇒ ۱ code + ۸ doc (پایه 23bf513، ancestor بودن تأیید شد)
                   2026-09-13.md: با main تعارض ندارد · با شاخهٔ چت ۶ تعارض دارد
TEST ENVIRONMENT:  main @ 952b0ba · شاخهٔ چت ۶ @ 277f5b5 · PR #159 @ 80c5910 (base.sha=1a73c52)
                   ابزار: git read-tree -m (سه‌درختی با پایهٔ صریح) + git ls-files -u
DATASET SIZE / CONCURRENCY / P50·P95·P99 / DB·CACHE·SECURITY IMPACT: n/a
ERROR RATE: ۰ (هیچ mergeای در مخزن باقی نماند؛ اندازه‌گیری با index موقت و بدون تغییر کار برجا)
FAILURE TEST: merge در کلون کم‌عمق از اساس ناممکن بود ⇒ روش جایگزین (read-tree) به‌کار رفت و مستند شد
RECOVERY RESULT: n/a (چیزی برای بازیابی نبود — هیچ worktree/mergeای در مخزن باقی نماند)
REGRESSION RISK: صفر تغییر کد؛ فقط سند و طرح
KNOWN UNKNOWNs: CI NOT-RUN (بیلینگ) · زمان مرج #159 نامشخص · شاخهٔ من ۸۲+ کامیت از main عقب است
FINAL VERDICT: VERIFIED — تعارض‌ها با دو پایهٔ متفاوت اندازه‌گیری و تفکیک شدند
```

### گیت ۸ سؤال
۱) چه تغییر کرد؟ → بازتولیدِ نقشهٔ تعارض + طرحِ ۷مرحله‌ای (صفر تغییر روی PRها). ۲) چرا؟ → نقشهٔ چت ۷ کهنه بود.
۳) invariant؟ → rc42 معتبر · ریشهٔ ۳۲۶ ثابت. ۴) اثبات؟ → شمارشِ تعارض با `read-tree -m` روی هر دو جفت
(+ ردّ پایهٔ نادرست که ۳۸ تعارضِ کاذب می‌ساخت). ۵) مقیاس‌پذیری؟ → n/a. ۶) امنیت؟ → n/a.
۷) failure؟ → در کلونِ کم‌عمق merge ناممکن بود ⇒ تله شناسایی و با روشِ جایگزین پوشش داده شد.
۸) rollback؟ → صفر تغییر کد؛ revertِ کامیتِ اسناد. ⇒ **همهٔ ۸ سؤال پاسخ دارند.**

### باز
- تصمیم ناظر: ترتیبِ مرج (شاخهٔ چت ۶ → #159 با طرح ۷مرحله‌ای → #129)؛
- #135: پچِ بخش ب آماده است (`pr135-keep-partB.main-0a208f8.patch`، CLEAN)؛ بازگشایی فقط با مالک؛
- revoke توکن — فقط اقدامِ مالک.

---

## دور ۱۲ — بستهٔ آمادگی مرج (ثبت: 2026-09-13)

### فاز ۰ — وضعیتِ تازه (دو تحولِ بزرگ)
- **#129 مرج شد** ✅ و main باز هم جلو رفت: `952b0ba` → **`55c5e15`**.
- **۳ دوزیهٔ «فقط‌در-#129» اکنون روی main هستند:** `REPORT_PERF_GAP_SPEC` ·
  `REPORT_CACHE_ISOLATION_DOSSIER` · `FLAG1_SCOPE_SQL_DOSSIER`
  ⇒ ۹ ارجاع از بدهیِ چت ۶ **با rebase شدنِ شاخهٔ من روی main خودبه‌خود بسته می‌شوند**
  (امروز روی شاخهٔ من هنوز «ناموجود»اند، چون شاخه ۸۰+ کامیت عقب است).
- برای شاخهٔ من **هیچ PR بازی نبود** ⇒ پس از این کامیت یک PRِ فقط‌مستنداتی باز می‌شود
  (تا rc42 پیش از #159 روی main بنشیند).
- ضدتکرار: شمارش‌های تعارضِ دور ۱۱ بازاجرا نشدند (۳+۵ و ۹ = شاهدِ موجود).

### فاز ۱ — راستی‌آزماییِ مستقلِ main (اجرای تازه، نه استنتاج از payload مرج)

روی `main@55c5e15` در یک worktree کاملاً خواندنی (سپس حذف شد):

| گیت | نتیجه | رتبه |
|---|---|---|
| `node tests/docs-freeze-marker.js` | **۱۴/۱۴** ✅ | **PASS** |
| `node tools/docs-stats-sync.js --check` | ✅ exit 0 (۳۲۷ ریشه · ۳۸۰ درخت · قفلِ جاری روی main = **rc40**) | **PASS** |
| `node tools/docs-metadata.js` | ۳۸۰ سند · **یتیم = ۰** ✅ | **PASS** |
| `node tools/reza-mirror-check.js` | بدون drift ✅ | **PASS** |

⇒ ادعای دور ۱۱ که صرفاً **VERIFIED** بود، اکنون با اجرای تازه روی خودِ main به **PASS** ارتقا یافت.
**هیچ repairای روی main انجام نشد** (قلمروی مالک/ناظر).

### فاز ۲ — کیتِ همگراییِ #159 (اجرای‌مالک)

`tools/audit-patches/PR159_CONVERGENCE_RUNBOOK.md` — شامل: وضعیتِ اندازه‌گیری‌شده ·
روشِ سنجه در کلون کم‌عمق (`read-tree -m` سه‌درختی + `ls-files -u`) · **تلهٔ پایهٔ نادرست**
(`1a73c52` ⇒ ۳۸ تعارض کاذب؛ پایهٔ درست `23bf513`) · نقشهٔ تعارضِ دور ۱۱ ·
طرحِ ۷مرحله‌ای با فرمان‌ها و خروجی‌های انتظار · ترتیبِ مرج · و **بازتولیدِ پچِ #135 در لحظهٔ اجرا**.

> **روش‌شناسیِ بازتولیدِ پچ (یافتهٔ تازه):** پچِ بخش ب که در دور ۱۰ روی main@`0a208f8` **CLEAN** بود،
> در این دور روی main@`55c5e15` دوباره **stale** شد (`patch failed: 2026-09-12.md:628`) — چون main
> متحرک است. ⇒ ارزشِ پایدار، **روشِ بازتولید** است (در runbook ثبت شد)، نه فایلِ پچِ freezing‌شده.

**هشدارِ صریح در runbook:** اجرای مراحل فقط توسط مالکِ #159 یا با واگذاریِ کتبیِ ناظر؛
چت ۶ صفر تغییر/کامنت/rebase روی #159 گذاشته است.

### فاز ۳ — تثبیتِ درس‌ها در پلی‌بوک + چرخهٔ ماشین
- `docs/PUSH_RECOVERY_PLAYBOOK.md`: دو بخشِ تازه الحاق شد —
  **§۷ چک‌لیستِ ریستِ مکررِ فضای کار** (تشخیصِ origin/هویت/node22/objectِ گم‌شده + بازیابی با
  `reset --mixed` بدون force-push + هشدار دربارهٔ `git checkout <sha> -- .` که یک بار کارِ ثبت‌نشده را پاک کرد)
  و **§۸ سنجهٔ تعارض در کلون کم‌عمق** (`read-tree -m` + تلهٔ پایهٔ نادرست).
- ماشین: `freeze-marker` ابتدا **۱۳/۱۴ قرمز شد** (`DF-HASH` روی `PUSH_RECOVERY_PLAYBOOK`)
  ⇒ بازتولیدِ **درجای** rc42 با `--freeze` ⇒ **۱۴/۱۴** ✅ · `stats-sync --check` ✅ ·
  **ریشه ۳۲۶ ثابت ⇒ بدون rc43** · `reza-mirror`: پلی‌بوک آینهٔ قراردادی ندارد ⇒ **۰ همگام / ۰ اختراع**.
- `run`/`smoke` بازاجرا نشدند (بدون تغییر کد) · PG/CI = **NOT-RUN**.

### بدهیِ ارجاعی (بازسنجی‌شده)
«تازه» = **۲۶** (بدون تغییر نسبت به دور ۱۰) + «تولیدی» ۴ → **۶** (اشاره‌های متنیِ runbook تازه،
که با قاعدهٔ `.gitignore` رد می‌شوند و بدهی نیستند). تفکیک: ۹ ارجاع به دوزیه‌هایِ **اکنون‌روی-main**
(با rebaseِ شاخهٔ من بسته می‌شوند) · ۲ ارجاعِ پیش‌رو به خروجی‌های آتیِ چت ۳ · بقیه پیش‌موجود یا مربوط
به تست‌هایِ فقط‌در-#129. **حذف برای سبزکردن گیت ممنوع**؛ `docs-refs-baseline.json` دست‌نخورده.

### باز (تصمیمِ ناظر)
1. ترتیبِ مرج: **شاخهٔ چت ۶ (rc42) → #159 با کیتِ ۷مرحله‌ای → #129** (دو موردِ آخر اکنون مرج شده‌اند)؛
2. تعارضِ سه‌گانهٔ code در #159↔main (`HANDOFF.md` · `alert-rules.yml` · `out/dast-live/…`) مالکِ کد می‌خواهد؛
3. بازگشاییِ #135 با مالک (پچ باید در لحظهٔ اجرا بازتولید شود)؛
4. rotate توکن — فقط اقدامِ مالک.

---

## دور ۱۳ — بازپایه روی main + همگراییِ قفل rc43 (ثبت: 2026-09-13)

### فاز ۱ — چرا «rebase ساده» نشد و چه کردم

| تلاش | نتیجه |
|---|---|
| `git rebase origin/main` (طبق پرامپت) | ⛔ **متوقف شد**: در حال بازپخشِ **۱۱۹۳ کامیت** بود — تاریخچهٔ شاخه شامل کلِ history است، نه فقط کارِ چت ۶ ⇒ `git rebase --abort` فوری (بدون تغییر) |
| **روشِ جایگزینِ برگزیده** | ✅ **بازپخشِ فقط تغییراتِ خالصِ من**: `git diff 23bf513 a5686f9` (نقطهٔ ادغامِ main در شاخه تا سرِ شاخه) = **۲۳ فایل · ۲۸۱۶+/۱۸−** ⇒ سپس `git checkout -B arena/01a09801-p2 origin/main` و `git apply --3way` |

**تعارض‌ها: ۴ مورد** (خروجیِ واقعیِ `git apply --3way`):
- ۳ بلوکِ **ماشینی** (`DOCS_METRICS` · `DOCUMENTATION_MAP` · `TEST_COVERAGE_REPORT`) ⇒ طبق قاعده
  **هیچ‌کدام دستی حل نشد**: با `git checkout origin/main -- …` به نسخهٔ main برگردانده و سپس با ابزار بازتولید شد.
- ۱ گزارشِ انسانی (`docs/daily-reports/2026-09-13.md`) ⇒ **اتحادِ append-only**: سمتِ main (بخشِ چت ۸، ۱۵۶ خط)
  **و** سمتِ من (بخشِ چت ۶، ۶۷ خط) هر دو حفظ شدند (تأیید: ۴ منشنِ «چت ۶» + ۵ منشنِ «چت ۸»؛ صفر نشانیِ تعارض).
- `NEXT_ACTIONS.md` و `tests/docs-freeze-marker.js` بدون تعارض اعمال شدند.

### فاز ۲ — همگراییِ قفل: rc43 (قلمروی چت ۶ طبق §۵٫۲)

| قفل | وضعیت |
|---|---|
| `rc40` | **تاریخی** (بنر + مانیفستِ دست‌نخورده) |
| `rc41` | **تاریخی** |
| `rc42` | **تاریخی** (بنر؛ مانیفست دست‌نخورده) |
| **`rc43`** | ✅ **تنها «جاری»** — ۳۳۱ ریشه (مانیفست ۳۳۰) + ۵۴ زیرپوشه = **۳۸۵ درخت** · تست‌ها **۴۸۲** |

⇒ پیش از این **سه مدعی** داشتیم (main روی rc40 · چت ۶ روی rc42 · #159 با rc41 واگرا)؛ اکنون روی این شاخه **تک‌مدعی** است.
پس از مرجِ این شاخه، کارِ #159 به «فقط حذفِ rc41 واگرا + بازتولید مانیفست» ساده می‌شود.

### فاز ۳ — گیت‌ها روی درختِ پس‌از-rebase (اجرای تازه)

`freeze-marker` **۱۴/۱۴** ✅ · `stats-sync --check` ✅ (۳۳۱ ریشه · ۳۸۵ درخت · ۴۸۲ تست) ·
`docs-metadata` **۳۸۵ سند · یتیم = ۰** ✅ · `docs-metrics` ✅ · `docs-index-coverage` ✅ ·
`docs-consistency` **۴۹ هماهنگ / ۰ تعارض** ✅ · `reza-mirror` **۰ همگام / ۰ اختراع** ✅

**بدهیِ ارجاعی: ۲۶ → ۷** ✅ (۱۹ ارجاع با آمدنِ دوزیه‌ها به main و بازپایه خودبه‌خود بسته شدند).
تفکیکِ ۷ باقی‌مانده: **۶ از خودم** (۴ ارجاعِ پیش‌رو به دو خروجیِ آتیِ چت ۳ در پرامپتِ هندآف و این گزارش ·
۱ ارجاع به `2026-09-13-pr-audit-list.md` که با اعمالِ بخش بِ #135 بسته می‌شود · ۱ ارجاعِ قدیمیِ
`chat6-final-delivery`) و **۳ از چت‌های دیگر** (`bounded-cache.js` · `load-test.js` ·
`reports-sql.w23norm-mutated.js`). **حذف برای سبزکردن گیت ممنوع**؛ `docs-refs-baseline.json` دست‌نخورده.

**NOT-RUN:** `run`/`smoke` — بدون تغییر کد از سوی من، و `node_modules` در این سندباکس پاک است
(jsdom نصب نیست ⇒ smoke خودش را رد می‌کند؛ «رد شدن» را «سبز» نمی‌نویسم). کدِ این شاخه با
`origin/main@622146f` یکی است ⇒ گیت‌های کدیِ main معتبرند. · گیت‌های PG · CI (بیلینگ).

### فاز ۴ — پوش با `--force-with-lease` (و یک تکرارِ مستند)

| گام | نتیجه |
|---|---|
| push روی `main@6bd4f6d` | `a5686f9` → `fe7c91d5` (lease روی a5686f9) ✅ |
| بازمحاسبهٔ #176 | ❌ باز هم `CONFLICTING/DIRTY` — **main در همان فاصله به `622146f` جلو رفته بود** |
| بازپخشِ دوم (همان روش؛ diff خالص ۳۴۷۱ خط) | ✅ روی `main@622146f` |
| push دوم (lease روی fe7c91d5) | `fe7c91d5` → **`01baba0`** ✅ |
| بازمحاسبهٔ #176 | ✅ **`mergeable=MERGEABLE` · `UNSTABLE`** (UNSTABLE فقط به‌خاطر CI = NOT-RUN/بیلینگ) |

⇒ این دقیقاً همان درسِ «پچِ فریزشده را باور نکنید» است، در سطحِ شاخه: main **هر نیم‌ساعت** جلو می‌رود
⇒ «merge-ready» فقط با اندازه‌گیریِ بلافاصله‌پس‌از-push معتبر است، و مرج باید در همان پنجره انجام شود.

### فاز ۴ — پوش با `--force-with-lease`
فقط روی شاخهٔ خودم، با lease صریح روی SHAی قبلیِ ریموت (`a5686f9`). سپس `ls-remote == HEAD`
و پذیرشِ این دور = **`mergeable=clean` از API برای #176** (اندازه‌گیری‌شده، نه حدس).

### باز
- مرجِ #176 توسط ناظر (ترتیب: **#176 → #159 با کیت**)؛
- تعارضِ سه‌گانهٔ code در #159↔main مالکِ کد می‌خواهد؛
- بازگشاییِ #135 با مالک (پچ باید در لحظهٔ اجرا بازتولید شود)؛
- rotate توکن — فقط اقدامِ مالک.
