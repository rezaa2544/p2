# اقدامات بعدی — اولویت‌بندی‌شده · به‌روز: 2026-09-12

> **P0-1..P0-6** مطابق حکم بازبین مستقل (هماهنگ‌کننده، پ۴ 2026-09-12).
> **در جریان:** PR #124 — P1-2 قطع آینه از مسیر نوشتن در PG-live + dedup با TTL (رفع ۳ باگ دور ۱ بازبین: p06 59/59 + جهش N1-N7؛ rebased روی main جدید) · PR #127 — P1-3 تجمیع گزارش عمومی سمت PG (Wave 18 §۵-۸؛ p13 9/9 + جهش M1-M4) · PR #125/#128 (گزارش‌های روزانه). مرج‌شده‌ها: #112/#115 (دور ۲ بعداً با #119: 5e3a824) · #116/#118 (گزارش‌ها) · #93→#123/#126 (Wave 23 DB-native) · **#94 (PAYESH_PG_HYDRATE_LIMIT/SKIP — c91c788؛ گیت wave18-hydration-guards روی main 18/18؛ دنبالهٔ «auth از آینه» با P1-1/#115 بسته شد؛ اعداد بنچمارکِ WAVE18 = Evidence مانورِ سندباکس، نه وضعیت تولید)** · **#102 (گزارش میانی P0-4 — ad65f9b؛ مرج پیش از رسیدنِ حکمِ «باز بماند تا بررسی مستقل»؛ وضعیتِ انجام‌شدنِ آن بررسی از این‌جا قابل راستی‌آزمایی نبود)** · P0-6 انجام و مرج شد (#112/#119) — ادامه در P1-2 (#124).
> **P1/P2** از evidence واقعی مخزن مشتق شده‌اند (گزارش Wave 18 §۵/§۸، محدودیت‌های HANDOFF، ردیف‌های ⏳ نقشهٔ ملی) — سند شماره‌گذاری‌شدهٔ بازبین در مخزن موجود نبود؛ **بازبین می‌تواند بازشماره‌گذاری کند.**

## P0 — مسیر GO/NO-GO (مسدودکنندهٔ تولید ملی)

| # | اقدام | چرا | معیار قبولی |
|---|---|---|---|
| P0-1 | **Reporting DB-native** | public-report و گزارش‌ها از آینهٔ درون‌حافظه‌ای محاسبه می‌شوند (نقض قاعدهٔ in-memory aggregation؛ در دیتاست ملی با هیدراتاسیون مقیّد = تقریبِ غلط) | کوئری‌های SQL با ایندکس + نتیجهٔ bounded؛ benchmark قبل/بعد + `EXPLAIN ANALYZE` |
| P0-2 | **Browser Cache Hardening** | کش سمت مرورگر باید صریحاً مهار شود (پیش‌نیاز درست‌بودن اعداد ظرفیت و یکپارچگی داده) | گیت رگرسیون کش + تست رفتاری |
| P0-3 | **Offline E2E** | مسیر آفلاین باید end-to-end اثبات شود (صف → reconnect → push → persistence → conflict → retry + ضدتکرار + OCC + idempotency) | ✅ **تست E2E خودکار سبز (محلی):** `tests/offline-e2e.js` ‏۲۳/۲۳ + جهش ۷/۷ (این دور)؛ «سبز در CI» = NOT-RUN (بیلینگ Actions)؛ SW-live/استیجینگ باقی |
| P0-4 | **National Dataset** | بارگذاری دیتاست کامل مقیاس ۱ (۱۰M کاربر / ۱۰۰k مدرسه / ۵۰M حضور / ۲۰M نمره) — ابزار آماده (`tools/generate-national-dataset.js` + `tools/w18-load-pg.sh`) | بارگذاری موفق + بازشماری + گیت‌های داده |
| P0-5 | **Capacity Environment** | استیجینگ چندنمونه‌ای مجزا (طبق §21): API/PG/Redis/mولد جدا؛ اعداد Wave 18 فعلی حدِ پایینِ تک‌جعبه‌اند | اجرای ۵ سناریوی Wave 18 روی آن + SLO سبز |
| P0-6 | **OOM Root Cause (اصلاح معماری)** | یافته‌های کمّی Wave 18: snapshot ‏O(collection) در sync (~۱MB/درخواستِ هم‌زمان) + رشد آینهٔ store (۱۰,۴۸۸B/نوشتن) | snapshot ‏O(batch) یا حذف در PG-live؛ سوکِ زیر بار بدون رشد خطی حافظه |

## Wave بعدی پیشنهادی (به‌روز 2026-09-12 — پس از بستن قیود Wave 3/6/10/11)

1. **P0-4: National Dataset** — بارگذاری مقیاس ۱ (۱۰M کاربر) با ابزار موجود؛ تنها مسیر اثبات ادعاهای مقیاس
2. **P0-5: Capacity Environment** — استیجینگ چندنمونه‌ای (Load Generator مستقل از API/DB)
3. **P0-6: OOM Root Cause** — profiling واقعی (heap/RSS/GC/event-loop lag) طبق یافته‌های کمّی Wave 18
4. **P0-1: Reporting DB-native** — ادامهٔ PR #93 (attendance DB-native انجام شده؛ بقیهٔ گزارش‌ها باقی)

## صفِ باگ‌هانت و ادغام (چت ۵ — به‌روز 2026-09-12)

| # | اقدام | چرا | معیار قبولی | مالک |
|---|---|---|---|---|
| BH-1 | تعیین تکلیفِ **#76** | حکمِ راستی‌آزمایی‌شده: **superseded با سه قلمِ یکتا** (۲۰۲۶-۰۹-۱۲، شاهد در بلوکِ زیر) | باز/`dirty` است؛ بستن فقط با تأیید مالک | چت ۵ → مالک |
| BH-2 | **تصمیمِ توکن** `setup-deps.ps1` (حذف/ابطال یا نگه‌داشتن) | قاعدهٔ ۴: فقط مالک. تا آن زمان، دو فایلِ بیرون‌دامنهٔ #74 دست‌نخورده | دستورِ صریحِ مالک | مالک |
| BH-3 | مرجِ **#106** (شکافتِ #74) | ✅ **مرج‌شده** @ ۲۰۲۶-۰۹-۱۲T15:09:23Z (merge `5b96897b64`)؛ گیت‌ها روی main سبز: 9/9 + 7/7 + 7/7 + 16/16 + 3/3 | برآورده شد — بسته | چت ۵ |
| BH-4 | **#74** پس از BH-2 | بازنویسیِ secret-scan نیازمندِ رفعِ نهفتهٔ `else if (e.isFile())` (خطاهای پیمایش) | تصمیمِ BH-2 + پچِ مذکور | مالک |
| BH-5 | اسنپ‌شاتِ `reza/index.html` · `reza/payesh.html` · `reza/USER_GUIDE.html` (۲۰۲۶-۰۹-۰۸) | ✅ **بسته به‌صورتِ «مستندشده»** (۲۰۲۶-۰۹-۱۲): بازتولیدِ HTML تصمیمِ محصول است و انجام نشد؛ وضعیت در `reza/README.md` جدول شد | ثبتِ وضعیت در `reza/README.md` + `tools/reza-mirror-check.js` | چت ۵ |
| BH-6 | بازفعال‌سازیِ ایندکس **۰۱۲** `(school_id, created_at DESC, id)` | فعلاً «NOT-ADD»: هیچ Seq Scan روی مسیر داغ اثبات نشده | اثباتِ `EXPLAIN (ANALYZE, BUFFERS)` روی بارِ واقعی + بهبودِ `p95` بیش از نویز + شاهدِ قبل/بعد | مهندسی |

### راستی‌آزماییِ صفِ ادغام — دورِ پس از بهداشت (چت ۵، ۲۰۲۶-۰۹-۱۲ · نمونه‌های زندهٔ API و `ls-remote`)

| قلم | وضعیتِ راستی‌آزمایی‌شده | شاهد |
|---|---|---|
| **#106** (شکافتِ #74) | **مرج‌شده — بسته** (تغییر نسبت به گزارشِ پیشین) | `merged=true · merged_at=2026-09-12T15:09:23Z · merge_commit=5b96897b64`؛ `bf833d87` **جدِ** `origin/main` است (`git merge-base --is-ancestor` ✅) |
| گیت‌های محتوای #106 روی `main` جاری | **سبز** | `route-url-guard 9/9` · `sync-del-mirror 7/7` · `idb-migration-queue 7/7` · **`idb-persistence 16/16`** (این یکی پیش‌تر روی main اجرا نشده بود ⇒ اجرا و سبز شد) · `pr74-mutations 3/3` (هر ۳ جهش کشته) |
| **#76** | **باز (open) و `mergeable=false`/`dirty`** — بسته **نشد** (قاعدهٔ ۶: فقط مالک) | `head=57661d87` · ۴ کامیت · ۷ فایل · +۸۹۲/−۲ |
| حکمِ ساختاریِ #76 | **superseded** با **سه قلمِ یکتا** — ثبت‌شده، بدونِ اقدام | (۱) `tools/wal-drill/setup-pg.sh` — روی `main` نیست (بوت‌استرپِ جایگزین با ۳ قابلیتِ غایب) · (۲) قیدِ صداقتیِ §۳-۱: شاهدِ `PANIC` از سمتِ **کلاینت** بود، نه لاگِ سرور · (۳) یافتهٔ §۴-۲: پرشدنِ مقصدِ `archive_command` **به‌تنهایی** دیتابیس را متوقف نمی‌کند |
| **BH-5** (اسنپ‌شات‌های `reza/`) | **بسته‌شده به‌صورتِ «مستندشده»** (بازسازی نشد — تصمیمِ محصول) | `reza/README.md` §«وضعیت آینه‌ها و اسنپ‌شات‌ها» + `node tools/reza-mirror-check.js` |
| **BH-6** (ایندکس ۰۱۲) | بدون تغییر: «NOT-ADD» تا اثباتِ `EXPLAIN (ANALYZE, BUFFERS)` | همین سند، ردیف BH-6 |
| **توکن‌ها** | **نگه‌داشته‌شده، بیرونِ ریپو** — هیچ تغییری ندادیم | `node tests/secret-scan.js` ⇒ ۱۱/۱۱ · هیچ توکنی در فایل/کامیت/گزارش نوشته نشد |


## P1 — کیفیت/پایداری (پس از P0)

| # | اقدام | منبع evidence |
|---|---|---|
| P1-1 | ایندکس `users(phone)` + auth از PG (حذف اسکن خطیِ آینه در ۱۰M کاربر) — ✅ پیاده‌سازی PR #115 (unit 17/17 + live 11/11 + بنچ ۱M: 132.5ms→0.36ms) در بازبینی | Wave 18 §۵-۴ |
| P1-1′ (بریف ناظر این دور) | **PDF E2E مسیر واقعی UI** — reports→rptPrint→printableDoc→Chromium→%PDF+نشانگر tenant | ✅ این دور: `tests/reports-pdf-e2e.js` ‏۸/۸ (مدیر مجاز + student ردشده پیش از تولید)؛ reports-export دست‌نخورده |
| P1-2 | قطع آینهٔ درون‌حافظه‌ای از مسیر نوشتن در حالت PG-live (dedup با سقف/TTL) | Wave 18 §۵-۳ |
| P1-3 | public-report → aggregation سمت PG (هم‌راستا با P0-1) | Wave 18 §۵-۸ |
| P1-4 | tracing در محیط بار: sampling ۱۰٪ + collector زنده (حذف always_on بدون collector) | Wave 18 §۵-۵ |
| P1-5 | سوک ۲۴ ساعت واقعی روی استیجینگ (اسکریپت آماده) + ثبت drift حافظه/تأخیر | Wave 18 §۶ |
| P1-6 | تعیین‌تکلیف ردیف ۲۲ (UNMAPPED) و ردیف‌های شروع‌نشدهٔ P0 نقشه (۶/۷/۸/۱۰/۱۳) | ROADMAP.md این پوشه |
| P1-7 | رفع انسداد billing گیت‌هاب CI (۶ job با steps=0/0) یا مهاجرت گیت‌ها به runner خودمختار | HANDOFF ویو ۲۳ + PR #94 |

## تحویل‌های این دور (چت ۳ — 2026-09-12، شاخهٔ `feat/migration-009-check-constraints`)

- ✅ مهاجرت ۰۰۹: CHECK ‏kind/format/status/generated_by روی `report_logs` (enum از کد، NOT VALID+VALIDATE) + ۳ ایندکس مرکب `(school_id, updated_at, id)` با اثبات EXPLAIN ANALYZE (grades دلتای tenant: ‏47.2ms→0.101ms) — گیت `tests/migration-009-live.js` ‏۱۳/۱۳ + جهش ۵/۵
- 📋 دوزیهٔ tenancy ‏`school_id IS NULL` (تصمیم ناظر — گزینهٔ الف/ب با اعداد در `2026-09-12.md` بخش چت ۳)؛ **بدون تغییر کد**
- 📋 دوزیهٔ counselor ‏(model.json/ REPORT_ROLES در برابر منوی کلاینت) — تصمیم ناظر؛ **بدون تغییر مجوز**

## جدول رزرو شمارهٔ مهاجرت (مرکزی — قاعدهٔ ۹ ناظر)

| شماره | رزرو برای | وضعیت |
|---|---|---|
| ۰۰۹ | CHECK ‏report_logs + ایندکس‌های مرکب tenant/delta | ✅ مرج (PR #101) |
| ۰۱۰/۰۱۱ | renumbering شاخهٔ PR #82 | 🔒 رزرو — شاخه دست‌نخورده |
| ۰۱۲ | ایندکس زمانی `(school_id, created_at DESC, id)` در صورت اثبات | آزاد — دور P0-4 با اعداد NOT-ADD شد (معیار بازفعال‌سازی: messages ‏>10ms یا Seq Scan در scale≥0.1) |

## وضعیت P0-4 / P1-3 (به‌روز این دور)

- **P0-4:** مولد با ۱۰ رابطهٔ جدید + گیت integrity ‏۱۲/۱۲ + جهش ۷/۷ + بارگذاری scale=0.01 روی PG زنده (۱.۴GB، ‏COPY ‏~۳۵s، ‏۸ کوئری داغ همه Index Scan)؛ **مقیاس 10M = NOT-RUN (سندباکس ۲core/2GB)** — برچسب: اندازه‌گیری‌شده در scale=0.01
- **P1-3:** نامزد updated_at از قبل در ۰۰۹؛ نامزد created_at با بنچمارک NOT-ADD

## موارد باز (پابرجا)

- tenancy `school_id IS NULL` = تصمیم ناظر (دوزیه در `2026-09-12.md` بخش چت ۳)
- CI بخش B = بیلینگ GitHub Actions (NOT-RUN)
- `CONCURRENTLY` برای ایندکس‌های تولیدی = تصمیم deployment

## P2 — بهبود / بدهی فنی

- P2-1 ‏pgbouncer برای استقرار چندنمونه‌ای (تک‌نمونه لازم نبود — Wave 18 §۵-۷)
- P2-2 ‏waiver موقت سبزها NOT-RUN به موج‌های دستی مربوط (سوک ۲۴h، مانور DR استیجینگ، compose رصدپذیری)
- P2-3 واگرایی ابعاد مولد دیتاست با §۲.۱ طرح بار (قلم ۲ ‏DOCS_CONSISTENCY_REPORT)
- P2-4 ایندکس‌های مسیرهای فیلترِ O(n) باقی‌مانده از Wave 9
- P2-5 پاک‌سازی فایل‌های توکن محلی پس از پایان هر session (قاعدهٔ ۱۱) و rotate توکن‌های افشاشده در متن چت

## مالک‌ها

- P0-1..P0-3: Arenaهای مربوط (پس از تعیین توسط هماهنگ‌کننده)
- P0-4..P0-6 + ‏P1-1..P1-5: دنبالهٔ طبیعی Wave 18 (همان Arena)
- P1-6/P1-7/P2ها: هماهنگ‌کننده/بازبین مستقل
