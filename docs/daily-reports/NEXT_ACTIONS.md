# اقدامات بعدی — اولویت‌بندی‌شده · به‌روز: 2026-09-12

> **P0-1..P0-6** مطابق حکم بازبین مستقل (هماهنگ‌کننده، پ۴ 2026-09-12).
> **در جریان (به‌روز 2026-09-13 پس از دور ۳ چت ۳):** صفِ ۱۳ PRِ بلاک‌شده با ACCEPT صریح ناظر تسویه شد — مرج‌های دور ۳: ‏#114 ‏(tenant-live ‏10/10+جهش 7/7) · #128 (گزارش‌ها) · #82 ‏(**مهاجرت‌های 011/012 renumber از 008/009** + کرسر v3 + پارتیشن‌بندی 62/62 زنده) · #91 (ضمیمهٔ ب استیجینگ W18) · #149 ‏(BH-mut دسته ۴ ‏mutant-kit) · #74 ‏(**حذف توکن از setup-deps.ps1 — ⚠️ revoke روی گیت‌هاب = اقدام مالک، هنوز باز**) · #162 (پورت سه قلم #76؛ #76 بسته) · #163/#164 (سازگاری هارنس‌های PG با مهاجرت 012: متاکامند psql) · #165 (stats/mirror) · #166 (گزارش دور ۳) · #158 ‏(T7a ‏allowlist سنجه — wave14 ‏95/95) · #160/#161. **PR باز — 🟠 بلاک صریح ناظر (دور ۴، 2026-09-13):** ‏#129 ‏@ `2b7e102` (چت ۳ — هیچ تغییری تا رفع بلاک/دستور صریح) · #157 (چت ۸) · #159 ‏(BH) — کار روی هر سه متوقف؛ گزارش دور ۴: `2026-09-13-chat3-round4-sync.md`. مهاجرت بعدی = **013** ‏(`migrate-helper --next`).
> **P1/P2** از evidence واقعی مخزن مشتق شده‌اند (گزارش Wave 18 §۵/§۸، محدودیت‌های HANDOFF، ردیف‌های ⏳ نقشهٔ ملی) — سند شماره‌گذاری‌شدهٔ بازبین در مخزن موجود نبود؛ **بازبین می‌تواند بازشماره‌گذاری کند.**

## P0 — مسیر GO/NO-GO (مسدودکنندهٔ تولید ملی)

| # | اقدام | چرا | معیار قبولی |
|---|---|---|---|
| P0-1 | **Reporting DB-native** | public-report و گزارش‌ها از آینهٔ درون‌حافظه‌ای محاسبه می‌شوند (نقض قاعدهٔ in-memory aggregation؛ در دیتاست ملی با هیدراتاسیون مقیّد = تقریبِ غلط) | کوئری‌های SQL با ایندکس + نتیجهٔ bounded؛ benchmark قبل/بعد + `EXPLAIN ANALYZE` |
| P0-2 | **Browser Cache Hardening** | کش سمت مرورگر باید صریحاً مهار شود (پیش‌نیاز درست‌بودن اعداد ظرفیت و یکپارچگی داده) | ✅ **انجام شد (پ۳ 2026-09-12):** کش کراندار گزارش‌ها — دفاع دولایه (کران ردیف/بایت سرور در pull + کران مستقل کلاینت + TTL ۲۴h + نشانگر «دادهٔ جزئی»)؛ گیت `tests/reports-bounded-cache.js` ‏۱۵/۱۵ + جهش ۱۰/۱۰ (BM1/BM10 چندویرایشی) |
| P0-3 | **Offline E2E** | مسیر آفلاین باید end-to-end اثبات شود (صف → reconnect → push → persistence → conflict → retry + ضدتکرار + OCC + idempotency) | ✅ **تست E2E خودکار سبز (محلی):** `tests/offline-e2e.js` ‏۲۳/۲۳ + جهش ۷/۷ (این دور)؛ «سبز در CI» = NOT-RUN (بیلینگ Actions)؛ SW-live/استیجینگ باقی |
| P0-4 | **National Dataset** | بارگذاری دیتاست کامل مقیاس ۱ (۱۰M کاربر / ۱۰۰k مدرسه / ۵۰M حضور / ۲۰M نمره) — ابزار آماده (`tools/generate-national-dataset.js` + `tools/w18-load-pg.sh`) | بارگذاری موفق + بازشماری + گیت‌های داده | **+ راستی‌آزمایی مستقل #102 انجام شد (چت ۸، PG 17.11 زنده در سندباکس): G1–G7 همه سبز در scale 0.002/0.01 (هر دو حالت لودر)؛ قطعیت مولد اثبات؛ برون‌یابی خطی scale=1 ≈ 28.5GB ⇒ تأیید برآورد 28GB گزارش #102 و **ردّ عدد 140GB** (سند دور ۳ چت ۸ — تصحیح در HANDOFF ثبت شد). یافتهٔ ران‌بوک: «001–004» کافی نیست؛ `server/schema.sql` هم لازم است (server_outbox). مقیاس ۱ همچنان نیازمند استیجینگ P0-5 |
| P2-WAL | **wal-disk-full W1–W5** | ۵ سناریوی live (WAL 80%/100%/RTO/replica/RPO) — `mount -t tmpfs` لازم دارد | در سندباکس ناممکن: root نیست و `unshare -Urmp` هم مسدود است («Cannot fork» — اندازه‌گیری چت ۸) ⇒ فقط استیجینگ P0-5 |
| P0-5 | **Capacity Environment** | استیجینگ چندنمونه‌ای مجزا (طبق §21): API/PG/Redis/mولد جدا؛ اعداد Wave 18 فعلی حدِ پایینِ تک‌جعبه‌اند | اجرای ۵ سناریوی Wave 18 روی آن + SLO سبز |
| P0-6 | **OOM Root Cause (اصلاح معماری)** | یافته‌های کمّی Wave 18: snapshot ‏O(collection) در sync (~۱MB/درخواستِ هم‌زمان) + رشد آینهٔ store (۱۰,۴۸۸B/نوشتن) | snapshot ‏O(batch) یا حذف در PG-live؛ سوکِ زیر بار بدون رشد خطی حافظه |

## Wave بعدی پیشنهادی (به‌روز 2026-09-12 — پس از بستن قیود Wave 3/6/10/11)

1. **P0-4: National Dataset** — بارگذاری مقیاس ۱ (۱۰M کاربر) با ابزار موجود؛ تنها مسیر اثبات ادعاهای مقیاس
2. **P0-5: Capacity Environment** — استیجینگ چندنمونه‌ای (Load Generator مستقل از API/DB)
3. **P0-6: OOM Root Cause** — profiling واقعی (heap/RSS/GC/event-loop lag) طبق یافته‌های کمّی Wave 18
4. **P0-1: Reporting DB-native** — ادامهٔ PR #93 (attendance DB-native انجام شده؛ بقیهٔ گزارش‌ها باقی)

## صفِ چت ۶ — پوشش لبه‌ها و handoff بین‌چتی (به‌روز 2026-09-13)

| # | اقدام | چرا | معیار قبولی | مالک |
|---|---|---|---|---|
| W23-NORM | **بستن شکافِ پوششِ جهشِ لبه‌های `norm` تحصیلی** | ✅ **بسته** (۲۰۲۶-۰۹-۱۳): ‏`tests/wave23-norm-edge-mutations.js` چهار ردیفِ فیکسچرِ لبه (`max_score` صفر/منفی/اعشاری · `score=NULL`) + اوراکلِ عددیِ دقیق؛ **۴/۴ جهش کشته** با شاهدِ چاپ‌شده. گاردِ تقسیم‌صفرِ تازه **اضافه نشد** چون red-first نشان داد شرطِ `mx > 0` خودش گارد است و `NULLIF` اضافی می‌شد (§۹.۶) | ۴/۴ کشته · `sha256` سورس پیش/پس یکسان · صفر کپیِ جهش‌یافتهٔ باقی‌مانده | چت ۶ |
| HO-1 | **handoff به چت ۸:** رفعِ سه ارجاعِ کهنه در `docs/daily-reports/2026-09-13.md` | ‏`tests/docs-refs-check.js` روی `main` قرمزِ ۲۷/۲ است؛ هر سه ارجاع (`tests/bounded-delta-resume.js` · `-mutations.js` · `tests/reports-bounded-cache-mutations.js`) از فایلِ روزانهٔ چت ۸ می‌آیند. چت ۶ به آن فایل و به `tools/docs-refs-baseline.json` **دست نزد** (قاعدهٔ ضدتداخل) | سبز شدنِ `docs-refs-check` بدونِ دست‌کاریِ baseline | چت ۸ (یا تصمیم مالک برای بازپایه‌سازی) |
| HO-2 | **✅ بسته توسط چت ۸ (PR جدید):** اجرای تأییدی هر دو سوئیت جهش wave23 روی PG 17.11 زنده | `wave23-reports-mutations` **11/11 کشته** · `wave23-norm-edge-mutations` **4/4 کشته** + فیکسچر live — نکته: مسیر live سوئیت دوم رگرسیون پنهانِ پس از #82 داشت (متاکامند 012 از client.query عبور نمی‌کند) که در همان PR با الگوی #163 رفع شد | هر دو «همه کشته» | چت ۸ — بسته |

| HO-3 | **handoff به مالک/زیرساخت:** گیت‌های جهش به هیچ ورک‌فلویِ CI وصل نیستند | بررسیِ `.github/workflows/` نشان می‌دهد **هیچ** ورک‌فلویی هیچ گیتِ جهشی را صدا نمی‌زند؛ CI فقط `npm test` (‏`tests/run.js` + `smoke.js`) را اجرا می‌کند و سوئیت‌های جهش در `run.js` نیستند. وصل‌کردنِ آن‌ها به `run.js` درست نیست چون آن گیتِ سریعِ **آفلاین** است و شمارِ ۳۵ تستش در اسنادِ زیادی ارجاع شده | یک step در ورک‌فلو که هر سه گیتِ جهش (`wave23-reports-mutations.js` · `wave23-norm-edge-mutations.js` · `tools/mutate-reports-check.sh`) را با `DATABASE_URL` اجرا کند؛ تا آن زمان این گیت‌ها فقط دستی‌اند | مالک/زیرساخت |
## صفِ باگ‌هانت و ادغام (چت ۵ — به‌روز 2026-09-12)

| # | اقدام | چرا | معیار قبولی | مالک |
|---|---|---|---|---|
| BH-1 | تعیین تکلیفِ **#76** | حکمِ راستی‌آزمایی‌شده: **superseded با سه قلمِ یکتا** (۲۰۲۶-۰۹-۱۲، شاهد در بلوکِ زیر) | باز/`dirty` است؛ بستن فقط با تأیید مالک | چت ۵ → مالک |
| BH-2 | **تصمیمِ توکن** `setup-deps.ps1` (حذف/ابطال یا نگه‌داشتن) | قاعدهٔ ۴: فقط مالک. تا آن زمان، دو فایلِ بیرون‌دامنهٔ #74 دست‌نخورده | دستورِ صریحِ مالک | مالک |
| BH-mut | **مهاجرت فایل‌های جهش به الگوی امن** (کپی جدا، کیت `tests/helpers/mutant-kit.js`) | 🟢 **دستهٔ ۴ بسته (دور ۵)** — فاز ۲ ادامه: الگوی A (۱۴ فایل) بدونِ مالک: انباریِ واقعی = **۱۲۳** فایلِ جهش (عددِ ۱۱۴ِ برنامه کهنه است — سوئیت ۹ فایل رشد کرده) · ۵۹ مهاجرت‌یافته: ۱۲ (دستهٔ ۱ — main از طریق #131/#139) + ۴۷ (دسته‌های ۲/۳/۴ — **مرج‌شده به main در 2026-09-13 از طریق #152**؛ قلمِ رهاشدگی بسته شد) · **یافتهٔ server15** (پایهٔ قرمز 37/40؛ re-resolve 404 به‌جای 409 already_resolved) در **#143** (`079d847`) رفع شد — داوری: باگِ `resolve⇒del` (#124) قراردادِ Gap-3 (#59) و 409 را می‌شکست ⇒ keep + هرسِ سقف‌دارِ resolvedها (پیش‌فرض ۵۰۰) — مهاجرتِ جهش‌هایِ server15-mutations (با لنگرهایِ تازه) باقی‌است · باقی‌مانده: **۵۵** = ۱۴ الگوی A + **۴۱ الگوی B** (ادعای «۳۲ فایل» دستهٔ ۴ با ۱۱۴ فایلِ کهنه بود؛ با ۱۲۳ واقعی = ۴۱ — فهرست منبعِ حقیقت است) · **دستهٔ ۴ بسته شد (دور ۵، 2026-09-13)**: ۲۹ هارنس روی main (از طریق #152) + ۱ (server15 — #147 مرج) + ۳ (PR #149 باز: national-dataset 7/7 · preapp2 8/8 · user-guide 5/5، همه روی `23bf513` measured) + ۴ بی‌خطرِ بدونِ تغییر (db-engineering · idb-persistence · dropout · migrate-pg-constraints — سبزِ measured) + ۴ NOT-RUN (PG17/Redis در ساندباکس نیست: migration-009-live · wave6-11-redis-live · wave3-parity · wal-disk-full W1–W5 [خود 9/14 سبز، exit 2]) | **مالکیتِ دستهٔ ۴ به بعد (خانوادهٔ الگوی B) = چت ۸ (دور ۵، ۲۰۲۶-۰۹-۱۳)** — اگر چت ۲ برگشت، مطابق همین ردیف؛ بقیهٔ الگوی A (۱۴ فایل) هنوز بدون مالک | چت ۸ |
| BH-3 | مرجِ **#106** (شکافتِ #74) | ✅ **مرج‌شده** @ ۲۰۲۶-۰۹-۱۲T15:09:23Z (merge `5b96897b64`)؛ گیت‌ها روی main سبز: 9/9 + 7/7 + 7/7 + 16/16 + 3/3 | برآورده شد — بسته | چت ۵ |
| BH-4 | **#74** پس از BH-2 | بازنویسیِ secret-scan نیازمندِ رفعِ نهفتهٔ `else if (e.isFile())` (خطاهای پیمایش) | تصمیمِ BH-2 + پچِ مذکور | مالک |
| BH-5 | اسنپ‌شاتِ `reza/index.html` · `reza/payesh.html` · `reza/USER_GUIDE.html` (۲۰۲۶-۰۹-۰۸) | ✅ **بسته به‌صورتِ «مستندشده»** (۲۰۲۶-۰۹-۱۲): بازتولیدِ HTML تصمیمِ محصول است و انجام نشد؛ وضعیت در `reza/README.md` جدول شد | ثبتِ وضعیت در `reza/README.md` + `tools/reza-mirror-check.js` | چت ۵ |
| BH-6 | بازفعال‌سازیِ ایندکس **۰۱۲** `(school_id, created_at DESC, id)` | فعلاً «NOT-ADD»: هیچ Seq Scan روی مسیر داغ اثبات نشده | اثباتِ `EXPLAIN (ANALYZE, BUFFERS)` روی بارِ واقعی + بهبودِ `p95` بیش از نویز + شاهدِ قبل/بعد | مهندسی |

### راستی‌آزماییِ صفِ ادغام — دورِ پس از بهداشت (چت ۵، ۲۰۲۶-۰۹-۱۲ · نمونه‌های زندهٔ API و `ls-remote`)

| قلم | وضعیتِ راستی‌آزمایی‌شده | شاهد |
|---|---|---|
| **#106** (شکافتِ #74) | **مرج‌شده — بسته** (تغییر نسبت به گزارشِ پیشین) | `merged=true · merged_at=2026-09-12T15:09:23Z · merge_commit=5b96897b64`؛ `bf833d87` **جدِ** `origin/main` است (`git merge-base --is-ancestor` ✅) |
| گیت‌های محتوای #106 روی `main` جاری | **سبز** | `route-url-guard 9/9` · `sync-del-mirror 7/7` · `idb-migration-queue 7/7` · **`idb-persistence 16/16`** (این یکی پیش‌تر روی main اجرا نشده بود ⇒ اجرا و سبز شد) · `pr74-mutations 3/3` (هر ۳ جهش کشته) |
| **#76** | **باز (open) و `mergeable=false`/`dirty`** — بسته **نشد** (قاعدهٔ ۶: فقط مالک) | `head=57661d87` · ۴ کامیت · ۷ فایل · +۸۹۲/−۲ |
| حکمِ ساختاریِ #76 | **superseded** با **سه قلمِ یکتا** — ثبت‌شده، بدونِ اقدام | (۱) `tools/wal-drill/setup-pg.sh` — روی `main` نیست (بوت‌استرپِ جایگزین با ۳ قابلیتِ غایب) · (۲) قیدِ صداقتیِ §۳-۱: شاهدِ `PANIC` از سمتِ **کلاینت** بود، نه لاگِ سرور · (۳) یافتهٔ §۴-۲: پرشدنِ مقصدِ `archive_command` **به‌تنهایی** دیتابیس را متوقف نمی‌کند |
| **BH-5** (اسنپ‌شات‌های `reza/`) | **بسته‌شده به‌صورتِ «مستندشده»** (بازسازی نشد — تصمیمِ محصول) | `reza/README.md` §«وضعیت آینه‌ها و اسنپ‌شات‌ها» + `node tools/reza-mirror-check.js` |
| **BH-6** (ایندکس ۰۱۲) | بدون تغییر: «NOT-ADD» تا اثباتِ `EXPLAIN (ANALYZE, BUFFERS)` | همین سند، ردیف BH-6 |
| **توکن‌ها** | **نگه‌داشته‌شده، بیرونِ ریپو** — هیچ تغییری ندادیم | `node tests/secret-scan.js` ⇒ ۱۱/۱۱ · هیچ توکنی در فایل/کامیت/گزارش نوشته نشد |


## P1 — کیفیت/پایداری (پس از P0)

| # | اقدام | منبع evidence |
|---|---|---|
| P1-1 | ایندکس `users(phone)` + auth از PG (حذف اسکن خطیِ آینه در ۱۰M کاربر) — ✅ پیاده‌سازی PR #115 (unit 17/17 + live 11/11 + بنچ ۱M: 132.5ms→0.36ms) در بازبینی | Wave 18 §۵-۴ |
| P1-1′ (بریف ناظر این دور) | **PDF E2E مسیر واقعی UI** — reports→rptPrint→printableDoc→Chromium→%PDF+نشانگر tenant | ✅ این دور: `tests/reports-pdf-e2e.js` ‏۸/۸ (مدیر مجاز + student ردشده پیش از تولید)؛ reports-export دست‌نخورده |
| P1-2 | قطع آینهٔ درون‌حافظه‌ای از مسیر نوشتن در حالت PG-live (dedup با سقف/TTL) | Wave 18 §۵-۳ |
| P1-3 | public-report → aggregation سمت PG (هم‌راستا با P0-1) | Wave 18 §۵-۸ |
| P1-4 | tracing در محیط بار: sampling ۱۰٪ + collector زنده (حذف always_on بدون collector) | Wave 18 §۵-۵ |
| P1-5 | سوک ۲۴ ساعت واقعی روی استیجینگ (اسکریپت آماده) + ثبت drift حافظه/تأخیر | Wave 18 §۶ |
| P1-6 | تعیین‌تکلیف ردیف ۲۲ (UNMAPPED) و ردیف‌های شروع‌نشدهٔ P0 نقشه (۶/۷/۸/۱۰/۱۳) | ROADMAP.md این پوشه |
| P1-7 | رفع انسداد billing گیت‌هاب CI (۶ job با steps=0/0) یا مهاجرت گیت‌ها به runner خودمختار | HANDOFF ویو ۲۳ + PR #94 |

## تحویل‌های این دور (چت ۳ — 2026-09-12، شاخهٔ `feat/migration-009-check-constraints`)

- ✅ مهاجرت ۰۰۹: CHECK ‏kind/format/status/generated_by روی `report_logs` (enum از کد، NOT VALID+VALIDATE) + ۳ ایندکس مرکب `(school_id, updated_at, id)` با اثبات EXPLAIN ANALYZE (grades دلتای tenant: ‏47.2ms→0.101ms) — گیت `tests/migration-009-live.js` ‏۱۳/۱۳ + جهش ۵/۵
- 📋 دوزیهٔ tenancy ‏`school_id IS NULL` (تصمیم ناظر — گزینهٔ الف/ب با اعداد در `2026-09-12.md` بخش چت ۳)؛ **بدون تغییر کد**
- 📋 دوزیهٔ counselor ‏(model.json/ REPORT_ROLES در برابر منوی کلاینت) — تصمیم ناظر؛ **بدون تغییر مجوز**

## جدول رزرو شمارهٔ مهاجرت (مرکزی — قاعدهٔ ۹ ناظر)

| شماره | رزرو برای | وضعیت |
|---|---|---|
| ۰۰۹ | CHECK ‏report_logs + ایندکس‌های مرکب tenant/delta | ✅ مرج (PR #101) |
| ۰۱۰/۰۱۱ | renumbering شاخهٔ PR #82 | 🔒 رزرو — شاخه دست‌نخورده |
| ۰۱۲ | ایندکس زمانی `(school_id, created_at DESC, id)` در صورت اثبات | آزاد — دور P0-4 با اعداد NOT-ADD شد (معیار بازفعال‌سازی: messages ‏>10ms یا Seq Scan در scale≥0.1) |

## وضعیت P0-4 / P1-3 (به‌روز این دور)

- **P0-4:** مولد با ۱۰ رابطهٔ جدید + گیت integrity ‏۱۲/۱۲ + جهش ۷/۷ + بارگذاری scale=0.01 روی PG زنده (۱.۴GB، ‏COPY ‏~۳۵s، ‏۸ کوئری داغ همه Index Scan)؛ **مقیاس 10M = NOT-RUN (سندباکس ۲core/2GB)** — برچسب: اندازه‌گیری‌شده در scale=0.01
- **P1-3:** نامزد updated_at از قبل در ۰۰۹؛ نامزد created_at با بنچمارک NOT-ADD

## موارد باز (پابرجا)

- tenancy `school_id IS NULL` = تصمیم ناظر (دوزیه در `2026-09-12.md` بخش چت ۳)
- CI بخش B = بیلینگ GitHub Actions (NOT-RUN)
- `CONCURRENTLY` برای ایندکس‌های تولیدی = تصمیم deployment

## P2 — بهبود / بدهی فنی

- P2-1 ‏pgbouncer برای استقرار چندنمونه‌ای (تک‌نمونه لازم نبود — Wave 18 §۵-۷)
- P2-2 ‏waiver موقت سبزها NOT-RUN به موج‌های دستی مربوط (سوک ۲۴h، مانور DR استیجینگ، compose رصدپذیری)
- P2-3 واگرایی ابعاد مولد دیتاست با §۲.۱ طرح بار (قلم ۲ ‏DOCS_CONSISTENCY_REPORT)
- P2-4 ایندکس‌های مسیرهای فیلترِ O(n) باقی‌مانده از Wave 9
- P2-5 ◐ انجامِ بخشِ حذف (پ۰ دور ۲، 2026-09-12): سه فایل توکن‌دارِ محلی حذف شدند (`git-auth.sh`/`replay-push.sh`/`w18-push-rebuild.sh`؛ شاهد: `docs/BUNDLE_REGISTRY.md` §۸) — **باقی مانده: rotate توکن‌های افشاشده** (توکنِ درون `git-auth.sh` + توکن‌های ارسالی در متن چت‌ها) نیازمند اقدامِ مالک ریپو

## مالک‌ها

- P0-1..P0-3: Arenaهای مربوط (پس از تعیین توسط هماهنگ‌کننده)
- P0-4..P0-6 + ‏P1-1..P1-5: دنبالهٔ طبیعی Wave 18 (همان Arena)
- P1-6/P1-7/P2ها: هماهنگ‌کننده/بازبین مستقل

## قلم‌های باز تازه (پ۳ — 2026-09-12)

- **flag-1 (scopedSchools → SQL):** دوزیهٔ کامل برای تصمیم ناظر در
  `docs/FLAG1_SCOPE_SQL_DOSSIER.md` (EXPLAIN دو گزینه + هزینهٔ مقیاس ملی +
  پیشنهاد فازبندی‌شده). **تغییر مجوزی — بدون ACCEPT ناظر هیچ کدی عوض نمی‌شود.**
- **قرمزهای محیطی reports-basic/tenant:** رفع شد (pin حالت حافظه) —
  `WAVE23_DB_NATIVE_REPORTS.md` §۹.۲ج.
- ~~**دلتای بریده (P0-2، خودبازبینی):** قلم باز `full_snapshot_required`/resume~~
  **بسته شد (پ۳-ادامه، 2026-09-12):** قراردادِ resume پیاده شد — سرور
  `full_snapshot_required_collections` اعلام می‌کند، کلاینت خودکار snapshot
  کاملِ کران‌دارِ همان مجموعه‌ها را می‌گیرد (حلقه‌شکن دولایه)، متا با union،
  UI با `data-rpt-resuming` + بنر shell در نماهای عملیاتی. قرارداد کامل:
  `WAVE23_DB_NATIVE_REPORTS.md` §۹.۲د. گیت‌ها: `bounded-delta-resume` ۱۴/۱۴ +
  ۱۰/۱۰ جهش کشته. **قلم باز جدید (معماری، تصمیم ناظر):** — دوزیهٔ تصمیم
  آماده شد: `docs/REPORT_CACHE_ISOLATION_DOSSIER.md` **پیوست (2026-09-13):** مشخصهٔ فنی شکاف کارایی academic/finance —
  `docs/REPORT_PERF_GAP_SPEC.md` (پیش‌نیاز گزینهٔ پ؛ توصیه: بنچ مقیاس هدف
  اول، MV فقط با نقض بودجهٔ p95؛ ادعای «۲۵–۴۰×» بریف ورودی با اسناد رد شد). **بستهٔ اندازه‌پذیری (2026-09-13، شاخهٔ
  `feat/p03-truncation-telemetry`):** ۳ کانتر بریدگی/resume در `/metrics`
  (روش سنجش معیار #۱ دوزیهٔ کش) + هارنس `tools/bench-reports-scale.js`
  (اجرای گزینهٔ «ت» مشخصه در استیجینگ؛ خود-skip بدون PG) — گیت ۹/۹ + جهش ۹/۹.
  ‏(۳ گزینه + توصیه +
  معیار بازفعال‌سازی؛ بدون تغییر کد) — جداسازی کامل کشِ
  گزارشی از دادهٔ عملیاتی (کامنت ۳ بازبین #129 — بنر shell رفعِ کوتاه‌مدت است).

## چت ۸ — بستهٔ اندازه‌پذیری (2026-09-13، دورِ راستی‌آزمایی)

- تحویلاتِ مأموریت از قبل به‌صورت **PR #133** (`feat/p03-truncation-telemetry`، stacked روی **#129**) موجود بود — قاعدهٔ ضدتکرار: راستی‌آزمایی شد، **بازسازی نشد**.
- راستی‌آزمایی (2026-09-13، node v22.14.0، درختِ دقیق #133 @ `3361154`): گیت ۹/۹ · جهش ۹/۹ کشته (TM1–9) · run 35/35 · smoke 547/547؛ + #129 روی همان درخت: 14/14 + 15/15 + جهش 10/10 + 10/10 (شواهد کامل: `2026-09-13.md`).
- **تصمیم ناظر:** ① ترتیبِ مرجِ stack **#129 → #133** (فنی‌اً merge-ready است) ② دامنهٔ بنچ (۶ کوئری academic/finance طبق مشخصه vs عبارتِ «چهار گزارش» مأموریت) ③ مشخصه‌های تعارضِ پیش‌موجود در `USER_GUIDE.html` روی main ④ اجرای استیجینگِ گزینهٔ «ت» (محیط P0-5) برای دادهٔ تصمیم MV.
- **به‌روزرسانی (دور ۳، 2026-09-13):** stack **تک‌یک شد** — #133 درون شاخهٔ #129 مرج شد (`aa936e4`)؛ head واحد `09c5578` باز-راستی‌آزمایی و سبز شد (۹/۹ + 35/35 + 547/547 + 14/14 + 15/15). رفعِ USER_GUIDE مرج شد (`58c7ece` = PR #138). PRهای #137/#138 در **#139** (`docs/daily-reports-2026-09-13`) تجمیع شدند؛ #137 به‌عنوانِ «superseded» بسته شد. **باقی‌مانده:** ① یک ACCEPTِ **#129 @ `09c5578`** (= کل stack) ② ACCEPT #139 ③ تأییدِ دامنهٔ بنچ (توصیه: حفظ academic/finance + PR پیگیریِ اختیاری) ④ زمانِ استیجینگ (P0-5 — سناریو در `2026-09-13.md`).

### قلم‌های بازِ ناظر — دور ۵ (2026-09-13، ثبتِ چت ۸)

1. **مجوزِ رفعِ تعارضِ #129** (فقط-مکانیکی/مستنداتی: merge main درون شاخه + بازتولید آمار با ابزار + باز-راستی‌آزمایی ⇒ head جدید) — بدون مجوز، استک بلوکه می‌ماند (مالک شاخه: چت ۳).
2. **ACCEPT #129** (استک #129+#133) — پس از قلم ۱. (#141 قبلاً مرج شده است.)
3. **دوزیهٔ کش** (گزینه‌های الف/ب/پ) + **دامنهٔ بنچ** + **استیجینگ P0-5** — بر اساس دوزیه‌ها.
4. **چرخاندن توکن** `setup-deps.ps1`.
5. ~~تصمیمِ شاخهٔ `bh-mut/phase2`~~ — **بسته شد**: #152 (2026-09-13) به main مرج شد؛ ۵۹ فایل روی main است.

### چت ۸ — نتیجهٔ نهاییِ دور ۵ (2026-09-13، بستهٔ BH-mut دستهٔ ۴)

- **دستهٔ ۴ (۴۱ فایلِ الگوی B) کامل شد:** 29 هارنس روی main از طریق #152 (spot: red-team 4/4 · server11 6/6 · offline-e2e 7/7 سبز) + 3 هارنس در **PR #149** (باز، در انتظار ACCEPT — روی `23bf513` measured: 7/7 + 8/8 + 5/5) + 4 بی‌خطر (بدونِ تغییر، سبز) + 4 NOT-RUN (اندودۀ‌شده: باینریِ PG17/Redis در ساندباکس نیست).
- **یافتهٔ بسته‌شده:** R5-W4C (قرمزیِ پیش‌موجودِ wave1-writes روی `ca648cf`) با #144 رفع شد — روی `23bf513`: 15/15 سبز ✅.
- **رویدادِ میانیِ دور:** ناظر #144/#145/#147/#151/#152/#153 را مرج کرد؛ PR #149 به‌دلیلِ ضدتکرار به ۳ فایلِ واقعاًِ باقی‌مانده تقلیل یافت (29 هم‌فایل با #152 — بازسازی نشدند).
- **درس‌های ثبت‌شده در هارنس‌ها:** سایهٔ هم‌جوار باید بعد از هر جهش unlink شود + sweepStrays پیش از خطِ پایه (readdir-scanning suites) · /tmp ~1GB tmpfs — قبل/بعد df + جاروی payesh-* (ENOSPC دو spot را کرش کرد).
- **موردِ بعدی (بدونِ مالک):** الگوی A (۱۴ فایل) + هماهنگ‌سازیِ جزئیاتِ envFail-class بینِ نسخه‌هایِ main/من.

---

## چت ۷ — دو ردیفِ سیاستیِ پیش‌مرج (2026-09-13، دور ۵ — الحاقِ یگانه در انتهای فایل؛ ردیف‌های چت ۳/۸ دست‌نخورده)

| # | قاعده | چرا (شاهد) | معیار قبولی | مالک |
|---|---|---|---|---|
| C7-R1 | **رزرو `rc42`** — هر بامپِ قفلِ بعدی با **چت ۶** و در قالب PR مستقل؛ `rc41` در یدِ **#159** است و چت ۷ هرگز فایل `rc41`/`rc42` نمی‌سازد | `--freeze` روی `main` مانیفست **جاری** (`rc40`) را بازنویسی می‌کند بدون بامپ — در تضاد با §۱ بندهای ۱ و ۳ سند قفل؛ تصمیمِ ناظر = **گزینهٔ ②** | `docs-freeze-marker` 14/14 + `stats-sync --check` exit 0 در همان PRی که قفل را می‌بندد | چت ۶ (سازنده) / چت ۷ (رزرو) |
| C7-R2 | **قاعدهٔ «آخرین مرج‌کننده = آمارساز»** — هر PRِ سندی پیش از مرج، `node tools/docs-stats-sync.js` را **درجا** اجرا و همان خروجی را در یک کامیت `chore(docs)` می‌گذارد؛ `--freeze` فقط با تأیید بامپ | کانفلیکت هر سه PRِ بلاک‌شده در دور ۵: **`code=0` / `doc=4..5`، تماماً در آمارِ مولّد** (اجرای ایزوله در `/tmp/mergegate5`)؛ در دور ۱ هم فقط همین بود | پس از مرج: `docs-stats-sync --check` exit 0 روی `main` | همهٔ چت‌ها |
