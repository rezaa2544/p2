# گزارش روزانه — ۲۰۲۶-۰۹-۱۴

## چت ۳ — دور ۱۵: پایش idle + آماده‌سازی پاسخ به triggerها (ثبت: 2026-09-14)

### فاز ۰ — وضعیت
- ‏main = ‏`a41bc9e` (پس از موج مرج ۱۸۹-۲۰۴: ‏۹ مرج + ۷ بسته/superseded + ۳ PR پشتیبان).
- ‏**#184 مرج شده و §۶ مصرف شده است** ‏(VERIFIED): ‏`OPERATIONAL_READINESS.md` ‏SLO ‏S2 را با شواهد drillهای چت ۳ هم‌خوان کرده (خط ۳۰ و جدول §هم‌خوانی: «چت ۳ ‏(sync) … ✅»). یک ادعای stale در خط ۳۱ («سند شواهد وجود ندارد» — پیش از #183 درست بود، الان غلط) ⇒ **پاسخ رسمی چت ۳ به‌صورت کامنت روی #184** ثبت شد (‏issuecomment-5659622468)؛ طبق حکم مالکیتی، خود سند لمس نشد.
- بهداشت ورودی: یک ‏stash بی‌صاحب از بقایای pre-merge چت ۴ پیدا شد — تک‌تک فایل‌ها با main مقایسه شد (فقط ۲ پرش بیت اجرایی متفاوت بود) ⇒ ‏drop امن.
- ضدتکرار: ‏drillها بازاجرا نشدند؛ همهٔ اعداد زیر ارجاع به منابع اجراپذیر ‏(#178/#182/#183).

### فاز ۱ — بسته‌های آماده‌سازی (به‌جای سه فایل موقتِ حذف‌شونده، مستقیم در همین گزارش — پرهیز از Documentation theater و سازگار با ممنوعِ «سند ریشهٔ تازه»)

#### ۱) پاسخ‌های پیش‌ساخته برای سؤالات احتمالی چت ۱۰ (بستهٔ ۵)
| سؤال محتمل | پاسخ آمادهٔ چت ۳ (با منبع) | برچسب |
|---|---|---|
| ‏SLO موفقیت sync قابل افزایش از ۹۹.۵٪ است؟ | در drillهای تک‌جعبه خطای ناخواسته صفر بود ‏(S1..S10؛ ‏149+372 درخواست) — عدد بالاتر *فنی‌ممکن* است ولی هر عددی بدون کالیبرهٔ استیجینگ چندنودی ادعاست؛ پیشنهاد: ۹۹.۵٪ بماند تا اولین drill فصلی | ‏VERIFIED-سندباکس / ‏NOT-RUN-استیجینگ |
| سنجهٔ queue depth برای on-call کافی است؟ | خیر — فقط صف کلاینتی measure شده ‏(baseline ‏۵/۱۲۰، دور ۱۱)؛ ‏gauge صف *سروری* وجود ندارد (یادداشت capture-spec دور ۱۴)؛ جایگزین عملی فعلی: ‏`outbox.depth()` ‏(‏publishOutboxDepth در scrape) | ‏VERIFIED |
| ‏invariant گم‌نشدن دادهٔ ackشده تضمین است؟ | بله پس از رفع F1 ‏(#185، ‏`703f93a`): ‏pgExpected ⇒ ‏ping=none/503 + ‏persist THROW + آزادسازی claim؛ شاهد: ‏S3 ‏chaos ‏falseAcks ‏3→0 ‏PASS ‏14/14 | ‏PASS |
| نرخ conflict قابل کاهش است؟ | ‏conflict «خطا» نیست — قرارداد preserve+داوری انسانی است ‏(R95)؛ سنجهٔ درست SLA داوری است ‏(>‏72h ⇒ warn، §۶ سند شواهد)، نه کاهش نرخ با LWW | ‏VERIFIED |
| قطع Redis چه اثری بر sync دارد؟ | پس از رفع F2: صفر — ‏dedupe سه‌لایه به لایه‌های authoritative می‌افتد؛ شاهد: ‏S2 ‏chaos ‏24/24، ‏writeOutageStatus ‏500→200 | ‏PASS |

#### ۲) آماده‌سازی استیجینگ (وقتی باز شد)
- **آماده‌ٔ اجرا، بدون تغییر:** ‏S1..S8 ‏(دو سوئیت drill چت ۳ + ‏۶ سناریوی chaos ‏#185 — همه پارامتریک/بازتولیدپذیر با شافل قطعی).
- **نیازمند کالیبراسیون روی استیجینگ:** آستانه‌های §۶ سند شواهد (همه برچسب «پیشنهاد سندباکس») + ‏S9/S10 در بار چندنودی (drain/skew توزیع‌شده).
- **نیازمند سنجهٔ جدید (تغییر کد محصول ⇒ فقط با بریف):** ‏gauge صف سروری per-instance؛ ‏counter مستقل ‏`sync_mirror_failed` ‏(الان audit است).
- **پیش‌نیاز محیطی (از قرارداد سوئیت‌ها):** ‏۲+ نود API مجزا + ‏PG واقعی ‏(psql کامل) + ‏Redis واقعی + ‏root/tmpfs برای disk-pressure؛ ‏RAM ≥ حدی که ‏OOM ‏wave18 نگیرد ‏(≥8GB به‌ازای نود، برآورد از گزارش W18).

#### ۳) آماده‌سازی حکم counselor/tenancy (وقتی صادر شد)
- **بلافاصله قابل‌تعمیم:** ‏S7 ‏(conflict-storm) و ‏S10 ‏(clock-storm) — پارامتر school_id/session را از فیکسچر می‌گیرند؛ سناریوی cross-tenant فقط فیکسچر دومدرسه‌ای می‌خواهد.
- **نیازمند تصمیم قرارداد پیش از نوشتن تست:** داوریِ تعارض بین-tenant ‏(چه کسی داور رکورد مشترک counselor است؟) و دامنهٔ ‏notification داوری — بدون این حکم، تست چیزی برای assert ندارد.
- ‏NOT-RUN پابرجا: ‏multi-tenant storm تا حکم.

### ‏ENGINEERING CLAIM
- ‏EXPECTED: آمادگی پاسخ سریع به ۳ trigger، صفر کار تازهٔ کد/تست.
- ‏MEASURED: ‏۵ پاسخ پیش‌ساخته (هرکدام با منبع اجراپذیر) + ماتریس استیجینگ + ماتریس tenancy؛ ‏۱ کامنت اصلاحی روی #184 (ادعای stale دربارهٔ سند شواهد).
- ‏TEST ENV/DATASET/CONCURRENCY/P50/ERROR/DB/CACHE: ‏N/A ‏(docs-only) · ‏SECURITY: ‏۰ توکن.
- ‏FAILURE TEST/RECOVERY: ‏N/A · ‏REGRESSION RISK: صفر (فقط این گزارش).
- ‏KNOWN UNKNOWNs: ظرفیت ملی (دائمی) · آستانه‌های ناکالیبره تا استیجینگ · قرارداد tenancy باز · ‏CI ‏NOT-RUN.
- ‏FINAL VERDICT: ‏**PASS** ‏(تحویل آمادگی) — پنجمین دور idle-محور ثبت شد.

### گیت ۸ سؤال
۱) فقط همین گزارش + کامنت #184 ۲) triggerها ممکن است هر لحظه برسند؛ پاسخ آماده = تأخیر صفر ۳) «هر پاسخ → منبع اجراپذیر» ۴) ‏grep/اجرای این دور روی main ‏(S2 خط ۳۰-۳۱ سند چت ۱۰ خوانده شد) ۵) ‏N/A ۶) ‏۰ توکن؛ ‏secret-scan سبز ۷) ‏rot پاسخ‌ها = گیت‌های stats/refs همین گزارش ۸) ‏revert تک‌کامیت.

### گیت‌ها/بودجه/پوش
‏freeze ✅ · stats 0 · metadata ✓ · refs 0 · mirror ۰/۰ · secret-scan ‏12/12 · بودجه ‏136MB→~136MB 🟢 · یک push ‏(SHA در PR).

### اعلام idle رسمی (دور ۱۵)
تا یکی از: **بریف بستهٔ ۵ / سؤال چت ۱۰** · **استیجینگ** ‏(Offline adversarial tests) · **حکم counselor/tenancy**.

---

## چت ۸ — دور ۱۵: پایش تصمیم الگوی A + آماده‌سازی بستهٔ ۱ (ثبت: 2026-09-14)

**اجراکننده:** نشست Arena `arena/01a09a73-p2` (همان نشستی که دور ۵ چت ۹ را اجرا کرد) — بریف «چت ۸ دور ۱۵» اجرا شد؛ **انحرافِ نقش** و **پنج انحرافِ بریف از واقعیت** در §۰/§۱ ثبت می‌شود (نه اصلاحِ خاموش).
**مبنای سنجش:** `main = bb5068b6384c9ac0cb4f268d74e3efdad5a3d45c` (پیمایشِ تازه با `git fetch` + `git ls-remote`؛ هر فرمانِ شبکه‌ای با `timeout`).

### §۰) بریف در برابرِ واقعیت (اندازه‌گیری، نه بازگویی)

| ادعای بریف | سنجشِ تازه روی ریمو | داوری |
|---|---|---|
| «main = `4fa5fcc` (پس از مرج #177)» | `gh api repos/…/commits/4fa5fcc` ⇒ موجود است: «Merge PR #177» — **ولی** `ls-remote` می‌گوید main = `bb5068b` (مرج #208 @ `2026-09-14T05:54:26Z`)؛ یعنی ≈۲۰+ مرج جلوتر | ⚠️ کهنه |
| «مرج PR #179» قلمِ باز است | `gh pr view 179` ⇒ **MERGED** `2026-09-13T16:13:27Z` (دور ۱۴ همین بود) | ✅ بسته |
| «بستهٔ ۰ در آستانهٔ مرج (#181)» | **MERGED** `2026-09-13T19:57:25Z` | ✅ بسته |
| «صریح بنویس: `OPERATIONAL_READINESS.md` وجود ندارد» | فایل **روی main هست** (`docs/OPERATIONAL_READINESS.md`، مالک: چت ۱۰، وضعیتِ خودِ سند: «مستندات VERIFIED، اجرای میدانی NOT-RUN») ⇒ نوشتنِ آن ادعا **ادعای کاذب** می‌شد | ❌ نادرست — ثبت شد، نوشته نشد |
| «دور ۱۵ = پایش idle + سه فایلِ موقتِ آماده‌سازی» | همین سناریو **دور ۱۵ چت ۳** در `docs/daily-reports/2026-09-14.md` مرج شده (#208) با این صریح: «به‌جای سه فایل موقتِ حذف‌شونده، مستقیم در همین گزارش — پرهیز از Documentation theater و سازگار با ممنوعِ «سند ریشهٔ تازه»» | ⚠️ تکراری ⇒ §۵ |

**ویرانیِ جانبیِ کشف‌شده (ترمیم شد):** سندباکس میانِ دو دور **re-clone** شده بود (`reflog` ⇒ `clone: from …`، `.git/shallow`، `rev-list --count HEAD = 1`)؛ کامیتِ گزارشِ دور ۵ چت ۹ (`3dd5518`) و شاخهٔ ریموی `arena/01a09a73-p2` رفته بودند ⇒ §۳.

### §۱) تصمیم الگوی A — **NOT-DECIDED** (سه شاهد، همگی اندازه‌گیری‌شده بر `bb5068b`)

1. `docs/daily-reports/NEXT_ACTIONS.md:189` ⇒ «**موردِ بعدی (بدونِ مالک):** الگوی A (۱۴ فایل) + هماهنگ‌سازیِ جزئیاتِ envFail-class بینِ نسخه‌هایِ main/من» — نه ACCEPT، نه مالک.
2. `HANDOFF.md` (ریشه) ⇒ صفر خطِ «الگوی A»؛ شش هیتِ «ACCEPT» همگی اقلامِ دیگر (#129 / flag-1 / آن‌کال و drillها). `docs/RISK_REGISTER.md` ⇒ ۰.
3. `gh search issues --repo rezaa2544/p2 "الگوی A in:body"` ⇒ **۰ نتیجه** (نه درخواست، نه کامنتِ تازه). صف PR: فقط **#209** باز است (مالکِ چت ۷، `docs(chat7)` — صفرِ لمس).

⇒ **فاز ۰.۵ شروع نشد** (حکمِ ۳: بدونِ ACCEPTِ الگوی A هیچ کارِ تازه‌ای روی کد/تست). ردیابِ خودِ چت ۸ (`docs/daily-reports/2026-09-13-chat8.md:123-140`) دورهای ۱۰/۱۱/۱۲/۱۳ را idle یا «منتظرِ ACCEPT» ثبت کرده؛ این دور هم همان حالت — **بی‌شمارشِ ساختگی** (شماره‌گذاریِ «هشتمین دور» از شواهدِ فایل قابلِ بازتولید نیست و ادعا نشد).

### §۲) آماده‌سازی بستهٔ ۱ (National Staging + Dataset) — «هست / نیست» با شاهدِ مسیر

**هست (روی main، راستی‌آزماییِ مسیر با `git ls-tree`/`git show`):**

| دارایی | واقعیتِ سنجیده‌شده |
|---|---|
| `infra/wave18-loadtest/staging-bootstrap.sh` | ۱۲۹ خط · install→initdb→migrate→seed→API · **تک‌نود** · پارامترها `W18_PORT/W18_PGPORT/W18_SCALE` · هشدارِ خودِ فایل: «`--scale 0.001` مقیاسِ اعتبارسنجیِ هارنس است، **نه** اندازهٔ ظرفیت؛ sandbox = 2c/2GB/20GB» |
| `infra/postgres/docker-compose.ha.yml` | HA واقعی + `pgbouncer/pgbouncer.ini` + `pgbackrest.conf.template` + `init/01-replication.sh` + `post-checks.sh` |
| `infra/redis/docker-compose.sentinel.yml` + `ops/redis/` | ۳ نود + ۳ sentinel (config‌های جدا) + `redis-checks.sh` |
| `infra/observability/docker-compose.observability.yml` | Prometheus + Loki + Grafana + otelcol + `alert-rules.yml`/`alertmanager.yml` (سنجه‌های SLO از همین‌جا) |
| `tests/performance/suites/` | **۵** سوئیت k6: `national-load-test` · `saturation-test` · `spike-mehr-test` · `chaos-redis-test` · `soak-24h-test` |
| `tools/generate-national-dataset.js` | بذرِ قطعی `--seed 20260901` · `--plan` «فایلی ننویسد» · `--scale` · `--fast` |

**نیست (سنجیده، نه حدس):** **هیچ compose/هارنسِ چندنودیِ *لایهٔ اپ*** — `git grep -lE "replicas|api-2|node2" origin/main -- infra/ ops/` ⇒ فقط `infra/redis/*`، `infra/tracing/README.md`، `ops/redis/README.md`. ⇒ گلوگاهِ «چندنودی» بستهٔ ۱ در **API+LB+per-instance state** است، نه PG/Redis (آن‌ها HA/sentinel دارند). همین با KNOWN UNKNOWNِ ثبت‌شدهٔ چت ۳ (نبودِ gauge صفِ *سروری* per-instance) هم‌خوان است.

**سنجهٔ اجراشدهٔ امروز (صفر نوشتن):** `timeout 60 node tools/generate-national-dataset.js --plan` ⇒

```text
scale=1 (ملی)      : schools 100,000 · classes 1,000,000 · users 10,000,000 (8M/1M/0.9M/99,900/99/1)
                     attendance 50,000,000 · grades 20,000,000 · installments 24,000,000 · outbox 8,000,000
scale=0.001 (CI)   : schools 100 · classes 1,000 · users 10,002 (8,000 دانش‌آموز · 1,000 دبیر · 900 والد · 100 مدیر)
```

`git status` پس از اجرا: صفر فایلِ نوشته‌شده (`data/national/` ساخته نشد؛ در `.gitignore` هم هست). **توصیهٔ شروع از مقیاسِ کوچک = همین سطرِ دوم**، چون `--plan` قبلِ هر دیسک‌هزینه‌ای، حجمِ واقعی را چاپ می‌کند.

**معیارهای پذیرش (با منبع، نه از بریف):** `docs/CAPACITY_MODEL.md` §۵ سطرهای ۲۴۳–۲۴۶ ⇒ `p50 < ۱۰۰ms` · `p95 < ۳۰۰ms` · `p99 < ۱s` · `5xx < ۰.۱٪` (بودجهٔ خطا ≈ ۴۳ دقیقه/ماه) — **هم‌خوان با اعدادِ بریف** ✅ (سنجیده شد، نه کپی). ⚠️ ولی: «۲۰k RPS / 2500 writes/s / 833 login/s» در بریف، در `CAPACITY_MODEL.md` **تصویب‌شده نیست**؛ همان سند سطر ۸۷ می‌گوید «توانِ فرضی هر نود ≈ ۲٬۰۰۰ RPS در p95<۳۰۰ms — **فرض**؛ باید در Wave 18 اثبات شود». ⇒ هر workloadِ بستهٔ ۱ باید به **اثباتِ این فرض** گره بخورد، نه به عددِ از پیش تعیین‌شده (خط‌قرمزِ «benchmark بدون workload واقعی»).

**چرا اجرای محلی = NOT-RUN (عدد، نه بهانه):** `nproc=2 · RAM=3939MB · disk=20GB avail` ⇒ `scale=1` ده‌ها GB است و ۲۰k RPS روی این ماشین فقط عددِ بی‌معنی تولید می‌کند. **ظرفیتِ ملی همچنان اثبات‌نشده است** (KNOWN UNKNOWNِ دائمی).

### §۳) ترمیمِ خسارتِ re-clone (بازیافتِ گزارشِ دور ۵ چت ۹)

- بلوکِ ۸۸۷ سطریِ «چت ۹ — دور ۵» از working tree بیرون کشیده و روی نسخهٔ تازهٔ `docs/daily-reports/2026-09-13.md` (روی main: ۲٬۰۵۷ خط، شاملِ بخش‌های الحاقیِ چت‌های ۳/۴/۸ و دور ۴ چت ۹) **نشانداه شد**: `git diff --numstat` ⇒ **887 افزودن / ۰ حذف** ⇒ append-only حفظ شد و هیچ بخشِ دیگری بازنویسی/حذف نشد.
- **آزمونِ بازتولیدِ آرشیو (همان ادعای دور ۵):** پروب از **بلوکِ کدِ درونِ گزارش** استخراج شد ⇒ `sha256 = 2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8` == sha اعلامی در دور ۵ ✅ ⇒ «قابل‌بازتولید بدونِ کدِ مخزن» یک ادعا نبود؛ عملاً آزموده شد (۵۶۶ خط، `node --check` سالم).

### §۴) **سنجهٔ تازهٔ امروز:** بازراستی پذیرشِ دانه روی `bb5068b` (قلمِ بازِ دور ۵ — بسته شد)

یادداشتِ مقدماتی: در `0a208f8` (دور ۵) دانه **۱۰۳۵** و هر چهار نقش غایب بود؛ غنی‌سازی در **#181** آمده است (`src/js/02-demo-data.js:451` ⇒ `role:'guard'` و `:459/460` ⇒ `lib_staff=1`/`asset_staff=1` روی دو دبیرِ موجود) + سوئیتِ `tests/seed-completeness.js`. **گیتِ خودِ چت ۳ بازاجرا نشد** (ضدتکرار با دور ۱۴ که آن را در ۱۱/۱۱ داشت) — شواهدِ زیر **رفتاریِ مستقل** است، با همان پروبِ بایگانی‌شده:

| سنجه | دور ۵ (`0a208f8`) | امروز (`bb5068b`) |
|---|---|---|
| دانه | ۱۰۳۵ · guard/lib_staff/asset_staff = ۰/۰/۰ | **۱۰۳۶ · ۱/۱/۱** (active=1016) |
| `8-1.A…H` نگهبان | FAIL + NOT-RUN ×۵ | **PASS ×۷** — ورودِ واقعی (`رضا نگهبانی` id=1025) → داشبوردِ پذیرش → `vis-new`+`vis-save` (`school_id=1/1 · registered_by=1025/1025`) → `vis-out` (`status=out · out_at=2026-09-14T06:10:43Z`) |
| `8-2.A…E` کتابدار | FAIL + NOT-RUN | **PASS ×۵** (سفرِ بیرون‌ازجعبه `lib-lend=true` · `inScope=true` · منفی‌ها رد) |
| `8-3.A…E` تحویلدار | FAIL + NOT-RUN | **PASS ×۵** (`as-status=true` · `as-new/as-del` غایب · بین‌مدرسه‌ای `false`) |
| `D.1` پین‌ها | ۱۰۳۵==۱۰۳۵ PASS | **PASS** — `donations2.js:55` و `drills2.js:54` == **۱۰۳۶** ⇒ حُکمِ «هم‌کامیتِ پین با دانه» **درست اجرا شده** (هر دو با کامنتِ `/* SIM-01: +guard1 (seed-completeness) */`) |
| `B.5` ضد تورم | دلتا ۰ | **PASS** — دلتا `+1` (دقیقاً سقفِ B5؛ بدونِ کاربرِ تازه برای دو پرچم) |
| `8-4.A/B/C` (SIM-04) | FAIL ×۳ | **FAIL ×۳ بدونِ تغییر** · `8-4.D` NOT-RUN (SKIP صریح) |
| جمع | 14 PASS / 6 FAIL / 10 NOT-RUN | **24 PASS / 3 FAIL / 3 NOT-RUN** |

**سه یافتهٔ تازه (هرکدام با شاهد، هیچ‌کدام با تغییرِ کد همراه نیست):**

1. **نامِ حسابِ seed با نامِ پیشنهادیِ ماتریس نمی‌خواند:** §۸-۱ و بریف `guard_demo` می‌گویند، seed واقعی `guard1` ساخته (`02-demo-data.js:451`). پروبِ من با **fallbackِ نقش+پرچم** رد شد و سنجه‌ها سبز شدند (`via: role/flag-fallback → guard1`)؛ ولی اگر ناظر/چت ۳ بخواهد سفرها **با username** تضمین شوند، یا سند اصلاح شود یا seed — تصمیمِ مالک، نه چت ۹.
2. **واژهٔ §۸-۱ «exited_at» همان‌جا می‌مرد:** سنجه روی mainِ تازه `out_at=… · exited_at=∅` داد ⇒ قراردادِ محصول `out_at` است؛ اصلاحِ واژگانیِ سندِ پذیرش (مالک: چت ۳/ناظر) همچنان باز است.
3. **SIM-04 با دانه حل نمی‌شود — تأییدِ تجربیِ دوم:** پس از غنی‌سازیِ کامل، سه FAILِ سطحِ طراحی **بدونِ تغییر** ماندند (`is_head`: ۰ هیت در `authz/model.json` + ۹۶ فایل `src/js/` + `server/**` + `migrations/*.sql`؛ `office_head=0`). ⇒ «seed کردنِ `is_head`» بی‌اثر است تا طراحیِ چت ۲ بیاید — دقیقاً همان چیزی که دور ۴/۵ پیش‌بینی کرده بود.

**Verdictِ این بند (تاکسونومی):** **PASS/VERIFIED** برای SIM-01..03 (پذیرشِ رفتاریِ دانه روی mainِ جاری) · **FAILِ سطحِ طراحی** برای SIM-04 · **NOT-RUN**: اجرای کدِ تستِ چت ۳ (ضدتکرار با دور ۱۴) و CI (بیلینگ).

### §۵) سه فایلِ موقتِ بریف — **عمداً ساخته/حذف نشد** (ثبتِ دلیل، نه بی‌عملیِ بی‌صدا)

سه نامِ بریف (`PATTERN_A_MONITORING` / `NATIONAL_STAGING_PREP` / `OPERATIONAL_PREP`) ساخته **نشدند** (بدونِ پسوندِ مسیر — چون هیچ‌گاه وجود بیرونی نیافتند و نوشتنِ `docs/…md` خودشان را به ارجاعِ کهنه بدل می‌کرد)، چون: (الف) بریف هم‌زمان «فایلِ تازه در `docs/` (مگر گزارشِ روزانه)» را ممنوع کرده و «ساختن و سپس حذف» را خواسته ⇒ خروجیِ صفرِ ماندگار = **Documentation theater** (خط‌قرمزِ خودِ ابلاغیه)؛ (ب) همین کار با همان خروجی در **#208** مرج شده و صراحتاً همین ردِ «سه فایلِ موقت» را ثبت کرده؛ (ج) `OPERATIONAL_PREP.md` در دامنهٔ **چت ۱۰** است (بستهٔ ۵) و سندِ `OPERATIONAL_READINESS.md` موجود است — ساختنِ موازی‌اش نقضِ مالکیت بود. **محتوای ماندگارِ همان سه فایل در §۱ و §۲ همین بخش است** (تصمیم + هست/نیست + اعدادِ `--plan` + SLO با منبع) ⇒ نه بدهی، نه تکرار، نه سندِ بادکرده.

- **آزمونِ عملیِ همین استدلال (اندازه‌گیری شد، نه حرف):** وقتی §۵ عمداً نامِ سه فایل را با قالبِ `docs/<name>.md` نوشت، `docs-refs-check` دقیقاً **+۳ ارجاعِ کهنهٔ تازه** داد (۴۱ → ۴۴)؛ پس از اصلاحِ نگارش، دوباره ۴۱. در خودِ main هم ردپای همین الگو هست: `2026-09-13.md` به سندی به‌نامِ «PROBE_TMP» (موقتِ سوخته) ارجاع می‌دهد که دیگر وجود ندارد ⇒ «ساخت‌وبسوزِ سند» فقط بی‌فایده نیست، **بدهیِ ارجاعی** می‌سازد.

### §۶) ENGINEERING CLAIM

```text
EXPECTED BEHAVIOR:
  - پایشِ وضعیتِ تصمیمِ الگوی A (با شاهد، نه حدس) + آماده‌سازیِ مبتنی‌بر-شواهدِ بستهٔ ۱
  - صفر کار روی کد/تست تا اعلامِ ACCEPT (حکم ۳ — ضد Feature Factory)
  - اعلانِ idle رسمی در پایانِ دور
MEASURED BEHAVIOR:
  - الگوی A: NOT-DECIDED (سه شاهد: NEXT_ACTIONS:189 بدونِ مالک · ۰ در HANDOFF/RISK · ۰ gh-search)
  - بستهٔ ۱: ۶ دارایی «هست» + ۱ شکافِ سنجیده‌شده (عدمِ وجودِ هارنسِ چندنودِ لایهٔ اپ) + ۲ اجرای --plan (ملی/CI) + SLOهای منبع‌دار
  - بازیافتِ گزارشِ دور ۵ چت ۹ (۸۸۷ سطر · ۰ حذف) + آزمونِ صحتِ sha256 آرشیو + بازراستیِ پذیرشِ دانه ⇒ 24 PASS / 3 FAIL / 3 NOT-RUN
  - ۰ کدِ جدید · ۰ تستِ جدید · ۰ فایلِ موقتِ ساخت‌وبسوز (به‌جایش §۵ با دلیل)
TEST ENVIRONMENT: main @ bb5068b · سندباکس 2 core / 3939 MB RAM / 20 GB free · node v22 · jsdom در /tmp (بیرون از مخزن)
DATASET SIZE: دانهٔ demo روی main = 1,036 کاربر · برنامهٔ ملی (--plan، صفر نوشتن) = 10M کاربر/100k مدرسه/50M حضور
CONCURRENCY: n/a (پایش + پروبِ تک‌نمونه؛ هیچ تستِ همزمانی اجرا نشد)
P50 / P95 / P99: n/a — **عمداً خالی** (هیچ بنچی اجرا نشد؛ staging وجود ندارد)
ERROR RATE: پروب روی main = 3 FAIL از 30 (SIM-04/طراحی) · اجرای --plan = ۰ خطا (exit=0) · CI = NOT-RUN
DB IMPACT: n/a (PG اجرا نشد؛ `--plan` فایلی نمی‌نویسد؛ `server/data/` اصلاً در این clone نیست)
CACHE IMPACT: n/a (Redis/کش لمس نشد)
SECURITY IMPACT: ۰ توکن در فایل/argv/کامیت/گزارش · secret-scan 12/12 · ۶ PAT فاش‌شده، revoke توسط مالک
FAILURE TEST: re-cloneِ سندباکس خودِ سناریوی شکست بود (کامیت + شاخهٔ ریمو رفت) ⇒ بازیافتِ append-only با ۰ حذف + sha256ِ بایگانی = مسیرِ بازیابیِ آزموده‌شده
RECOVERY RESULT: 3dd5518 ↦ بازیافت در این کامیت · شاخهٔ ریمو ⇒ بازسازی با push (§۸)
REGRESSION RISK: صفر تغییرِ کد/داده؛ دو فایلِ روزانه، هر دو append-only
KNOWN UNKNOWNs: الگوی A NOT-DECIDED · ظرفیتِ ملی اثبات‌نشده (دائمی) · هارنسِ چندنودِ اپ وجود ندارد · CI NOT-RUN (بیلینگ، RISK-O-007) · گیتِ seed-completeness بازاجرا نشد (ضدتکرارِ دور ۱۴) · بازتولیدِ payesh.json NOT-RUN
FINAL VERDICT: **VERIFIED** (پایش + آماده‌سازیِ بستهٔ ۱، با شاهدِ مسیر/عدد) · **NOT-RUN** (تصمیمِ الگوی A — اعلام نشده) · **NOT-RUN** (هر اجرایِ ظرفیت/staging) · **PASS** (بازراستیِ پذیرشِ دانه روی mainِ جاری: SIM-01..03)
```

### §۷) گیت ۸ سؤال

1) **تغییر:** دو فایلِ گزارشِ روزانه (الحاق) — و **ردِ** سه فایلِ موقت با دلیل (§۵). 2) **چرا:** بریف کهنه بود (main/#179/#181/OPERATIONAL_READINESS) و کارِ خواسته‌شده تا حدی مرج‌شده (#208) ⇒ ارزشِ افزوده = تصحیحِ وضعیت + بازراستیِ دانه. 3) **Invariant:** حُکمِ ۳ (بیِ الگوی A ⇒ صفر کارِ کد/تست) + append-only + صفرِ بامپ rc + مالکیت‌ها. 4) **اثباتِ درستی:** سه شاهدِ الگوی A · `git ls-tree`/`git show` برای هست/نیست · دو اجرای `--plan` · خروجیِ ۳۰ سنجهٔ پروب با sha256. 5) **اثباتِ سرعت/مقیاس:** **n/a** — عمداً هیچ عددی ساخته نشد. 6) **اثباتِ امنیت:** ۰ توکن + `secret-scan 12/12` روی درختِ نهایی. 7) **failure:** re-clone به‌عنوان حادثهٔ واقعی + بازیافتِ آزموده‌شده (§۳). 8) **rollback:** `git revert` این کامیت (دو فایلِ گزارش، ۰ اثر جانبی).

### §۸) گیت‌ها · بودجه · پوش · idle

- **گیت‌های مسیرِ تغییر (همگی سبز، روی درختِ نهایی):** `docs-stats-sync --check` ⇒ ✅ (۴۰۶ سند: ۳۴۸ ریشه + ۵۸ زیرپوشه · قفل **rc43**) · `docs-freeze-marker` ⇒ **14/14** · `docs-metadata` ⇒ **17/17 · یتیم ۰** · `docs-refs-check` ⇒ **۴۱ ارجاعِ کهنه، همگی پیش‌ازاین — صفرِ تازه** (سنجشِ دوطرفه: با الحاقِ من ۴۱، بدونِ الحاق ۴۱؛ baseline=۷۵، تولیدیِ ردشده=۵) · `secret-scan` ⇒ **12/12** (روی درختِ شاملِ هر دو الحاق) · `reza-mirror-check` ⇒ «✅ همهٔ آینه‌های زنده با منبعِ docs/ برابرند» — **۰ drift / ۰ اختراعِ آینه**. هر فرمان شبکه‌ای/اجرا با `timeout` (۶۰–۳۰۰ث). **بازاجرا نشد:** ۱۱ گیتِ کدِ دور ۱۴ · بازتولیدِ مانیفست · گیت‌های docsِ بی‌ربط · سوئیت `seed-completeness` (ضدتکرار).
- **بودجهٔ §۱ (معیارِ مؤثر، بدونِ `node_modules`/`.cache`):** **پس از کار = 48.4 MB / 1543 فایل** 🟢 (سنجشِ مستقیم در پایان) · **پیش از کار ≈ 48.3 MB / 1543 فایل** — چون «پیش از کار» در ابتدایِ این re-clone سنجیده **نشده بود**، عددِ پیشین از پس‌زداییِ حسابیِ بایت‌هایِ الحاقی (86٬359 B در 1٬01۶ سطر) به‌دست آمده و **برچسبش «مشتق» است، نه اندازه‌گیریِ مستقل**؛ تازه‌سازیِ فایل: **۰** (هر دو تغییر، الحاق به دو فایلِ موجود). نکتهٔ بهداشتی: این سندباکس پس از re-clone کوچک‌تر است (`.git` کمکی = shallow)، پس اعدادِ بودجهٔ دورهایِ پیش (مثلاً «136MB» در دور ۱۵ چت ۳) با این عدد **قابلِ مقایسه نیستند**.
- **پوش:** یک کامیت + `git push origin arena/01a09a73-p2` + `ls-remote == HEAD` (نتیجه در گزارشِ نشست؛ upstreamِ شاخه پس از re-clone عمداً unset شد تا push فقط با refspecِ صریح انجام شود — محافظتِ main).
- **حکم‌های رعایت‌شده:** ۱) تصمیمِ الگوی A در زمینِ ناظر ⇒ فقط پایش ✅ · ۲) بستهٔ ۱ پیش‌نیازِ بسته‌های بعدی ⇒ شکافِ اصلی (چندنودیِ اپ) ثبت شد ✅ · ۳) بیِ ACCEPT ⇒ صفر کد/تست ✅ · ۴) mutation با gh ⇒ راستی‌آزماییِ بایت‌های live (این دور هیچ mutation با gh انجام نشد؛ فقط push محلی) ✅
- **idle رسمی:** تا یکی از: **اعلامِ ACCEPT/ردِ الگوی A** · **حکمِ ناظر** · **بریفِ جدید**. تنها کارِ مجازِ بعدیِ این مسیر: پیگیریِ همان تصمیم + اجرایِ بستهٔ ۱ **پس از** ACCEPT.
## چت ۹ — دور ۶: پایش وضعیت پوش + آماده‌سازی بازراستی‌آزمایی پس‌امرج + نهمین دور idle (ثبت: 2026-09-14)

- **وضعیت پوش: رفع شد در این محیط** ⇒ **فاز ۰.۵** — راهکارِ اثبات‌شده: resetِ ششم (origin/هویت/node22) ترمیم شد؛ اتصالِ autenticated با env + credential-helperِ inline (fetch + ls-remote سبز). یادداشت: CLIِ `gh` در سندباکس auth ندارد — معادلِ اثبات‌شده همان helper است؛ شاهدِ نهایی = پوشِ همین دور. مشکلِ پوشِ دور ۵ قطعِ اتصالِ میانهٔ نشست (environmental) بود، نه اعتبارنامه/دسترسی.
- **مبنای این دور:** پرامپت main/شاخه را @`0fc4403`/`3dd5518` می‌گفت؛ اندازهٔ تازه: main=`bb5068b` (موجِ مرجِ ۱۸۹–۲۰۸ + #206/#207/#208) و شاخهٔ `arena/01a09a73-p2`=`0ef89e6` (= main + ۲ کامیت: احیای بخشِ چت ۹ دور ۵ + چت ۸ دور ۱۵) — کار روی headِ واقعی انجام شد (بدونِ rebase/لمسِ کامیت‌های دیگران).
- **آماده‌سازی برای بازراستی‌آزمایی پس از مرج:** ۶ سناریو (S1–S6: سفرهای SIM-01..03 + is_head-SKIP صریح + منفی‌های fail-closed + قطعیتِ دانه) / ۷ راهکار ایمن (worktree خواندنی، npm ci جدا، seedِ رسمی، سوییپِ پورت با لنگر `^/usr/bin/node` + PID-only، timeout 30، توکنِ env-only، صفرِ نوشتن به شاخه‌ها) / ۷ معیار پذیرشِ SLO (عیناً از CAPACITY_MODEL §۵: p50<100ms · p95<300ms · p99<1s · 5xx<0.1٪ · کش>80٪ · sync≥99.5٪ · OTP≥99.9٪) + ۴ توصیهٔ مقیاسِ کوچک.
  - **گپِ ثبت‌شده:** پروبِ ۵۶۶-خطیِ دور ۵ در ریپو نبود و از /tmp گم شده ⇒ S1–S3 یا منتقل‌شدنِ پروب به `tests/` (دورِ مجزا با حکم) یا NOT-RUN تا آن زمان؛ S5/S6 با سوئیت‌های موجود قابلِ اجراست.
- **آماده‌سازی برای بستهٔ ۵ (Operational Readiness): انجام نشد — مالک چت ۱۰** (`OPERATIONAL_READINESS.md` وجود ندارد؛ چت ۹ فقط مالکِ بستهٔ ۳ است — تفکیکِ مالکیتِ صریح).
- **گیت‌ها (اجرای تازه روی شاخه):** freeze **14/14** · stats --check **exit 0** · metadata یتیم **هیچ** · secret-scan **12/12** · mirror **0/0** (فقط چک) · docs-refs: **RED پیش‌موجود — تازه=41** (این دور ایجاد نشد: موجِ مرجِ شبِ قبل #196/#206/#207/#208) — تفکیکِ اندازه‌گیری‌شده: ۳۴ ارجاعِ forward-طراحیِ کارهای چت ۴ (pgbouncer/weighted-partitioning/version-vector/summer — سوئیت‌ها هنوز ساخته نشده؛ #196 design-only بود) در CHAT4_* + ROADMAP + **HANDOFF** + اسنادِ طرح · ۲ ارجاعِ پیش‌روِ handoffِ چت ۶ (گزارش‌های همزمانی چت ۳) · ۵ ارجاعِ تاریخیِ نقطه‌درزمانی — **تعمیر/بازتولیدِ baseline (75→116 مشروط) = اقدامِ HO-1/چت ۸ در دورِ بعد؛ در این دور ثبت شد و تعمیر نشد** (دامنهٔ دور = رفع پوش + آماده‌سازی).
- **پوش:** [SHA در گزارشِ پایانِ نشست] == ls-remote ✅ (یک کامیتِ addِ صریح، هویتِ چت ۹) · **اسکنِ توکن: 0**
- **بودجهٔ §۱:** 102.8MB/1878 🟡 (قبل) → [بعد: پایانِ نشست — gc ملایم انجام می‌شود چون >100]
- **idle رسمی** تا: حکمِ ناظر (شروعِ بازراستی‌آزمایی) / بریفِ جدید — مشکلِ پوش دیگر مانع نیست؛ بعدیِ طبیعی = نقلِ پروب به ریپو یا اجرای S5/S6 با حکم

---

## چت ۹ — دور ۱۶: پایش وضعیت تصمیم الگوی A + اعلام idle (ثبت: 2026-09-14)

**برچسبِ بریف:** «بیست‌وپنجمین دور idle». **سنجش:** شمارِ اعلام‌هایِ صریحِ idle در `docs/daily-reports/2026-09-13.md` + `2026-09-14.md` روی main = **۹** (۵ + ۴)؛ رشتهٔ «بیست‌وپنجم» تنها در نامِ فایل `DOCS_FREEZE_v1.0.0-rc25.md` و `DOCS_INDEX.md` می‌آید (اشاره به **rc25**، نه شمارشِ idle) ⇒ عددِ ۲۵ شمارشِ idle نیست؛ برچسبِ بریف بی‌تغییر نگه داشته شد و **انحراف همین‌جا ثبت می‌شود** (اصلاحِ خاموش نه).

### §۰) بریف در برابرِ واقعیت (پنج انحرافِ تازه، اندازه‌گیری‌شده بر `main = 5a175c0`)

| # | ادعای بریف | واقعیت (شاهد) |
|---|---|---|
| ۱ | «شاخه @ [SHA کامیت دور ۱۵]» | دور ۱۵ من = `0ef89e6`، ولی **tipِ واقعیِ شاخه** = `ab933c1` = «چت ۹ — دور ۶» (۱۱ سطرِ یک نشستِ دیگر روی همین branch) ⇒ مبنای این دور `ab933c1` گرفته شد، نه بازگشت به `0ef89e6` |
| ۲ | `gh api …/issues/comments --json query='{ repository { issue(number: 184) … } }'` | `issue(number:184)` ⇒ **NOT_FOUND** (#184 یک **PR** است، نه issue) و فرمِ `--json query=` هم معتبر نیست. جایگزینِ اجراشده: `gh api graphql` + `gh api repos/…/issues/184/comments` و `…/pulls/184/comments` ⇒ **۰** کامنت شاملِ «الگوی A» |
| ۳ | «RTO/RPO **و SLA 99.95٪** از `DR_RUNBOOK`» | `docs/DR_RUNBOOK.md:4` ⇒ RTO ≤ ۱۵ دقیقه · RPO ≤ ۵ دقیقه · failover ≤ ۳۰ ثانیه ✅؛ اما `grep '99\.95' docs/DR_RUNBOOK.md` = **۰**. عددِ 99.95٪ در `docs/WAVE14_OBSERVABILITY.md:174` است (Availability / 30d) و `docs/OPERATIONAL_READINESS.md:27` می‌گوید پیشنهادِ «API 99.9 / sync 99.95» **جایگزین** شده ⇒ انتسابِ بریف به DR_RUNBOOK غلط است |
| ۴ | «دیسک ≈ 140GB (فرمول خطیِ measure‌شده: 1.4GB@0.01 × 100)» | پایه‌ها مستندند (`docs/REPORT_PERF_GAP_SPEC.md:55`، `docs/WAVE23_DB_NATIVE_REPORTS.md:257`) ولی **×۱۰۰ مشتق است، نه اندازه‌گیری**؛ هیچ سندِ مخزن ۱۴۰GB را تأیید نمی‌کند ⇒ در acceptance-bundle با برچسبِ **«مشتق»** می‌آید |
| ۵ | فاز ۱ = «سه فایلِ موقت بساز و در پایانِ دور حذف کن» | با «فایلِ تازه در `docs/` ممنوع» **و حکم ۳ِ خودِ بریف** («اسنادِ تازه باید ارجاعِ ورودی بگیرند یا در baseline ثبت شوند») در تضاد ⇒ **رد**، با شاهدِ کمّی در §۴ |

### §۱) تصمیم الگوی A — **NOT-DECIDED** (گیتِ خودِ بریف، نه تفسیرِ من)

- فرمانِ تشخیصِ بریف، عیناً اجرا شد: `gh api repos/rezaa2544/p2/contents/docs/daily-reports/NEXT_ACTIONS.md | base64 -d | grep -c "الگوی A تأیید شد"` ⇒ **۰**
- `docs/daily-reports/NEXT_ACTIONS.md:189` = همان خطِ **بدونِ مالک** (بدونِ تغییرِ نسبی از دور ۱۵): «موردِ بعدی (بدونِ مالک): الگوی A (۱۴ فایل) + هماهنگ‌سازیِ جزئیاتِ envFail-class…»
- `docs/HANDOFF.md`: «الگوی A» = **۰**؛ تنها «تأیید شد» در `:159` مربوط به اجرای ران‌بوکِ §۹.۶ است ⇒ **false-positive رد شد**
- `docs/RISK_REGISTER.md`: «الگوی A» = ۰ · `git grep "الگوی A تأیید شد" origin/main -- docs` = **۰**
- سه نامزدِ «سیگنالِ اشتباه» که سنجیده و **رد** شدند: (الف) تگ‌ها: `national-baseline-start → 8850973 (2026-09-09)` و `v1.0.0/v1.0.1 (2026-09-07)` — همگی **پیش از** هر تصمیم ⇒ «شروعِ استیجینگ ملی» نیستند؛ (ب) **PR #211** (همین شاخه): ۲ کامنت + ۱ review، همهٔ بات (strix: «سقف ۵۰ بررسی = billing»، coderabbit: skip، devin: «3 flags» که در این سندباکس قابل مشاهده نیست) ⇒ **هیچ حکمِ انسانی**؛ (ج) PR #213 (چت ۵) **MERGED** بدونِ هیچ ذکری از الگوی A
- ⇒ شرطِ فاز ۰.۵ برقرار نبود ⇒ **اجرا نشد**؛ بستهٔ ۱ **NOT-RUN/NOT-VERIFIED**؛ **۰** کار روی کد/تست (حکم ۲ — ضد Feature Factory)

### §۲) ارزش افزودهٔ این دور: **سنجه‌پذیریِ ۷ معیارِ پذیرش** (نام‌های واقعی از `server/metrics.js`)

معیارها عیناً از `docs/CAPACITY_MODEL.md` §۵ (سطرهای ۲۴۳–۲۵۰) — همان ۷ تایِ بریف، همه موجود:

| SLO | سنجه | وضعیتِ نام در کد |
|---|---|---|
| p50 < 100ms · p95 < 300ms · p99 < 1s | `payesh_http_request_duration` (histogram) | ✅ مستقیم (۳ ارجاع در `server/metrics.js`) |
| 5xx < 0.1٪ (بودجهٔ ≈۴۳ دقیقه/ماه) | `payesh_http_requests_total{code=~"5.."}` | ✅ (PromQLِ مصوب در `docs/WAVE14_OBSERVABILITY.md:174`) |
| نرخ اصابت کش > 80٪ | سند می‌گوید `payesh_cache_hit_rate` ⇒ **در `server/metrics.js`: ۰ hit** | ⚠ **باید مشتق شود**: `rate(payesh_cache_hits_total[5m]) / rate(payesh_cache_lookups_total[5m]) > 0.80` (نام‌های واقعیِ موجود: hits / misses / lookups / invalidations) |
| موفقیت همگام‌سازی ≥ 99.5٪ | `payesh_sync_pulls_total` + `payesh_sync_conflicts_total` | ✅ نسبت از دو شمارندهٔ موجود |
| OTP ≥ 99.9٪ (ساعات مدرسه) | `payesh_auth_otp_requests_total{outcome}` (`metrics.js:438,713`) | ✅ نسبتِ `outcome="ok"` در برابرِ کل؛ نرخِ ورود با `payesh_auth_login_total` (`:439`) |

⇒ **۴ از ۷ مستقیم، ۳ نسبت‌محور**؛ و «WORKLOAD: 20k RPS / 2500 writes/s / 833 login/s» همچنان **در هیچ سندِ مصوبی تصویب نشده** (منبعِ مخزن: `docs/CAPACITY_MODEL.md:87` — per-node ≈ **2000 RPS** هنوز فرضِ تست‌نشده). شکافِ `payesh_cache_hit_rate` = **یافتِ تازهٔ این دور** و یک بدهیِ سند↔کد است (مالک: چت ۱۰ برای تعریفِ gauge، چت ۳ برای اصلاحِ متنِ CAPACITY_MODEL).

### §۳) نردبانِ پیش‌نیازِ محیط — **۰ از ۶ِ سخت‌افزاری برآورده** (سنجشِ امروز)

۱) **۲+ نود API:** `nginx/nginx.conf:83-84` بلوکِ `upstream payesh` را دارد ولی **دقیقاً ۱ عضو** (`127.0.0.1:3000`، `listen 8080` با کامنتِ «POC بدونِ root؛ در پروداکشن ۸۰/۴۴۳ پشتِ Cloudflare») و در `infra/**/docker-compose*` **۰** تعریفِ `replicas|scale` ⇒ **تصحیحِ گزارۀ دور ۱۵** («هیچ LB نیست») : شکلِ LB **هست**، تک‌عضو — خلأ واقعی «تکثیرِ نود» است نه «نبودِ nginx».
۲) **PG/Redis واقعی (sentinel):** فایل‌ها موجود (`infra/postgres/docker-compose.ha.yml`، `infra/redis/docker-compose.sentinel.yml`) ولی `command -v docker` ⇒ **ABSENT** ⇒ اجرای stack **NOT-RUN**
۳) **RAM ≥ 16GB** ⇒ `free -m` = **3939 MB** (کسری ≈۴.۲×) ❌ `nproc` = **۲** (برای نردبانِ multi-node هم کم است) ⇒ ❌
۴) **دیسک (مقیاس ملی)** ⇒ `df` = **19 635 MB** آزاد در برابرِ ≈۱۴۰GB مشتق (کسری ≈۷.۱×) ❌
۵) **root/tmpfs برای WAL-drill** ⇒ `id -u` = **1001** (غیرroot) · `df -T /tmp` = **ext4** ⇒ ❌ (۵ tmpfs سیستمی هست، ولی /tmp tmpfs نیست و mount توانمند نیستیم)
۶) **اسکریپت‌ها:** `tools/generate-national-dataset.js` ✅ · `tools/w18-load-pg.sh` ✅ ⇒ تنها دو ✅ از ۶، و هر دو «فایل هست» نه «محیط آماده است»
⇒ **نتیجه:** اجرای ملی **ممکن نیست** (عدد، نه بهانه). هر دورِ بعدی تا برآورده‌نشدنِ همین ۶، همان حکم را خواهد داشت؛ اجرای مجددِ `--plan` (دور ۱۵) تکرارِ بیهوده بود ⇒ **انجام نشد**.

### §۴) سه فایل موقت — دوباره ساخته **نشدند** (این‌بار با ارجاع به قاعدۀ خودِ بریف)

`PATTERN_A_MONITORING` / `NATIONAL_STAGING_PREP` / `OPERATIONAL_PREP`. سه دلیلِ اندازه‌گیری‌شده:
(الف) دور ۱۵ **آزمود**: نوشتنِ نامِ این‌ها در گزارش ⇒ **+۳ ارجاعِ کهنه** (۴۱ → ۴۴)؛ پس از اصلاحِ نگارش دوباره ۴۱.
(ب) در mainِ امروز هم ردپای همان الگو هست: `2026-09-13.md` به سندِ موقتِ سوخته‌ای به‌نامِ «PROBE_TMP» ارجاع می‌دهد که دیگر وجود ندارد ⇒ `docs-refs-check` روی درختِ نهاییِ این دور: **۱۱۳ کهنه در ۶۵ سند** = baseline ۷۵ + تازه ۴۱ (+۵ تولیدیِ ردشده) که **هیچ‌کدام از الحاقِ من نیست**.
(ج) **حکم ۳ِ خودِ بریف** ایجاب می‌کند هر سندِ تازه ارجاعِ ورودی بگیرد یا در `tools/docs-refs-baseline.json` ثبت شود؛ فایلِ سوزنی هیچ‌کدام را ندارد و «ثبتِ بدهی» برای سه فایلِ ۳۰ دقیقه‌ای بی‌معناست.
⇒ محتوای سه فایل در §۱/§۲/§۳ همین بخش است: **بدونِ بدهی، بدونِ تکرار، بدونِ سندِ بادکرده**.

### §۵) ترمیمِ دومِ سندباکس + وضعیتِ ادغام (این دور «بازیافت» بود، نه «ساخت»)

- ابتدای دور: `HEAD` روی `0a208f8` (پایۀ نشست) ریست شده بود + ۵۲ فایل «M» و ۸۴ فایل untracked (نسخه‌هایِ جدیدِ main روی دیسک). **بازیافت:** `sha256` فایلِ دیسک = `096881ec…` == `sha256(blobِ 0ef89e6)` ⇒ **صفرِ محتوایِ از‌دست‌رفته** (کارِ دور ۱۵ سالم بود چون **push** شده بود)؛ سپس `git reset --hard ab933c1` ⇒ فایل = **۱۸۸ سطر** (۴۸ + ۱۲۹ من + ۱۱ دور ۶).
- **PR #211 (این شاخه → main): `mergeable = CONFLICTING` · `mergeStateStatus = DIRTY`** · `additions 1027 / deletions 0 / 2 files` · `headRefOid = ab933c1`. main از `bb5068b` **۱۰ کامیت** جلوتر است (`gh api …/compare/bb5068b...5a175c0 ⇒ ahead_by=10, behind_by=0`) و هر دو سو به انتهای همین فایل الحاق کرده‌اند ⇒ **تعارضِ EOF**، نه تداخلِ معنایی.
- **rebase نکردم (عمداً):** tipِ شاخه کامیتِ نشستِ دیگر (`ab933c1` = دور ۶ چت ۹) را هم حمل می‌کند؛ بازنویسیِ آن = لمسِ کارِ دیگری. اصلاحِ پیشنهادی برای **صاحبِ ادغام**: `git rebase origin/main arena/01a09a73-p2` (هر سه بخش append-only‌اند، پس تعارض فقط «هر دو را نگه‌دار» است).
- یادداشتِ بی‌ضرر ولی لازم: در همان ۱۱ سطرِ دور ۶ دو **جای‌خالی** مانده: «SHA در گزارشِ پایانِ نشست» و «بعد: پایانِ نشست — gc ملایم» — مالکیتِ اصلاح با همان نشست؛ من در الحاقِ خودم **۰ جای‌خالی** گذاشتم (`grep` در §۸).

### §۶) ENGINEERING CLAIM

- **EXPECTED BEHAVIOR:** پایشِ تصمیمِ الگوی A · آماده‌سازیِ بستهٔ ۱ (سنجه‌پذیری + پیش‌نیازها) · **idle** تا تصمیم
- **MEASURED BEHAVIOR:** **۰ فایلِ تازه** (قالبِ بریف «3 docs موقت ساخته شد» را از پیش پر کرده بود — **انجام نشد**؛ نوشتنِ آن در گزارش = جعلِ سبز) · ۰ کد · ۰ تست · **۲۶** فرمانِ سنجشی، همه با `timeout` · **۵** انحرافِ بریف ثبت‌شده (§۰) · **۱** بازیافتِ وضعیتِ شاخه (§۵) · **۷** SLO به نام‌هایِ واقعیِ کد نگاشت شد (§۲) · **۶** پیش‌نیاز با عدد (§۳)
- **TEST ENVIRONMENT:** n/a (فقط پایش/مستندسازی؛ هیچ سرویسی اجرا نشد) · **DATASET SIZE / CONCURRENCY / P50·P95·P99 / ERROR RATE:** n/a — اجرای بار **NOT-RUN** (§۳)
- **DB IMPACT:** صفر نوشتن (نه seed، نه `--plan` — ضدتکرارِ دور ۱۵) · **CACHE IMPACT:** n/a
- **SECURITY IMPACT:** ۰ توکن در فایل/argv/کامیت/گزارش · `secret-scan` **12/12** · ۶ PAT فاش‌شده، **revoke توسط مالک** · هیچ PR-review باتی trigger نشد (strix سقف ۵۰ = billing)
- **FAILURE TEST:** NOT-RUN (صفر تغییرِ کد ⇒ موضوعِ تست نبود؛ §۳ دلیلِ عدمِ اجرای load را با عدد می‌دهد) · **RECOVERY RESULT:** `096881ec…` == بلاوبِ `0ef89e6` ⇒ صفرِ محتوایِ از‌دست‌رفته؛ شاخه به tipِ درست (`ab933c1`) منتقل شد
- **REGRESSION RISK:** صفر تغییرِ کد/تست — فقط یک الحاقِ متنی به گزارشِ روزانه
- **KNOWN UNKNOWNs:** CI **NOT-RUN** (بیلینگ، RISK-O-007) · تصمیمِ الگوی A **NOT-DECIDED** · «Devin: 3 flags» قابل مشاهده در این سندباکس نیست ⇒ **NOT-RUN** · عددِ ۱۴۰GB **مشتق** · `payesh_cache_hit_rate` در کد تعریف نشده · PR #211 **CONFLICTING**
- **FINAL VERDICT:** **NOT-RUN** (تصمیم اعلام نشده؛ بستهٔ ۱ **NOT-VERIFIED**) + **VERIFIED** فقط برای «سنجه‌پذیریِ معیارها و نردبانِ پیش‌نیاز» (§۲/§۳ — PASS نیست، چون هیچ تستی اجرا نشد)

### §۷) گیتِ ۸ پرسش (پاسخ‌هایِ پیش‌فرضِ بریف، جای خود را به اندازۀ واقعی دادند)

1. **چه چیزی تغییر کرد؟** فقط `docs/daily-reports/2026-09-14.md` (+95 سطرِ الحاقی) — **۰** سندِ تازه، **۰** کد، **۰** حذف
2. **چرا؟** بستنِ دو خلأی که اجرای بستهٔ ۱ را بلوکه می‌کند: نام‌هایِ سنجه در برابرِ کد (§۲) و عددِ پیش‌نیازهای محیط (§۳)؛ + پایشِ رسمی با گیتِ خودِ بریف (§۱)
3. **چه invariant باید حفظ شود؟** بستهٔ ۱ قفل تا **ACCEPTِ صریح**؛ گزارش فقط append-only؛ **۰** rc bump؛ **۰** سندِ بادکرده؛ سندباکس بدونِ نوشتنِ بار
4. **چگونه ثابت می‌کنیم درست است؟** هر عدد در §۰–§۵ یا `file:line` دارد یا خروجیِ فرمانِ اجراشده (grep/gh api/free/df)؛ بازیافتِ §۵ با **sha256** سنجیده شد، نه با فرض
5. **سریع‌تر/مقیاس‌پذیرتر؟** n/a (صفر اجرای بار — §۳)
6. **امن؟** ۰ توکن + `secret-scan 12/12` + بدونِ triggerِ بات‌ها + بدونِ ویرایشِ فایل‌هایِ دیگران
7. **failure؟** NOT-RUN با دلیل (صفر کد)؛ و §۳ نشان می‌دهد پیش از تصمیم، حتی با اراده هم غیرقابل‌اجرا بود
8. **rollback؟** فرمانِ `git revert` روی SHAِ همین کامیت (SHA پس از commit در `git log -1` و `ls-remote` خوانده می‌شود؛ خودارجاع درونِ فایل ممکن نیست) — چون الحاقِ خالص است، revert بدونِ درگیریِ بخش‌هایِ چت‌های ۳/۵/۸ اعمال می‌شود

### §۸) گیت‌ها · بودجه · پوش · idle

- **گیت‌های مسیرِ تغییر (یک‌بار، روی درختِ نهایی، همه `timeout`دار):** `docs-stats-sync --check` ✅ (آمار == دیسک؛ **rc43**، بدونِ bump) · `tests/docs-freeze-marker.js` **14/14** · `tests/docs-metadata.js` **17/17** · یتیم **۰** · `docs-refs-check` **۴۱ کهنه — صفرِ تازه از این دور** · `tests/secret-scan.js` **12/12** · `reza-mirror-check` ✅ **۰ drift / ۰ آینهٔ تازه / ۰ cp دستی** · **۰** بازاجرای سوئیت استیجینگ، **۰** بازاجرای گیت‌های دور ۱۵، **۰** manifest regen
- **جای‌خالی در الحاقِ من:** جست‌وجوی نشانه‌هایِ جای‌خالیِ متنی (double-at، «to‑do»، «fix‑me»، angle-bracket-SHA، double-brace) روی 95 سطر ⇒ **۰** (جای‌خالی‌هایِ §۵ مربوط به فایلِ دیگری است و دست نزدم)
- **بودجۀ §۱ (معیارِ مؤثرِ `WORKSPACE_HYGIENE`، بدونِ `node_modules`/`.cache`):** پیش **100.6 MB / 1551 فایل** → پس **102.3 MB / 1553 فایل** 🟢 · دلتایِ کاریِ من = **12 534 B (12.3 KB) در 95 سطرِ یک فایلِ موجود** — **0 فایلِ تازه** · بخشِ عمدهٔ دلتا (+≈1.7 MB / +2 فایل) از **`.git`** است (امروز 69 MB، پس از دو `git fetch` برای tagها و tipِ شاخه) و نه از محتوا؛ اندازۀ تمیزِ محتوا بدونِ `.git`: **33.93 MB / 1448 فایل**. محدودیتِ شفاف: اندازۀ `.git` در ابتدای دور جداگانه گرفته **نشد**، پس تفکیکِ «۱.۷MB = .git» استنتاجِ زمان‌بندیِ فرمان‌هاست، نه تفکیکِ اندازه‌گیری‌شده.
- **پوش:** این فایل **درونِ کامیت** است و SHA خودش را نمی‌تواند بیاورد (محدودیتِ ذاتیِ خودارجاعی) ⇒ زنجیره: `5a175c0 (main) ← bb5068b ← 4c233af (احیای دور ۵) ← 0ef89e6 (دور ۱۵) ← ab933c1 (دور ۶، نشستِ دیگر) ← این کامیت`؛ تأییدِ بیرونی پس از push در `git ls-remote` و به‌روزرسانیِ خودکارِ PR #211 ثبت می‌شود
- **idle رسمی (چت ۹):** تا یکی از این‌ها: **ACCEPT/ردِ صریحِ الگوی A** · **حکمِ ناظر** · **بریفِ تازه**. تنها پیگیریِ مجاز: تازهِ‌شدنِ همان تصمیم + **رفعِ تعارضِ PR #211** (مالک: صاحبِ ادغام). با ACCEPT، اجرای بستهٔ ۱ از §۲ (فرمول‌هایِ سنجه) و §۳ (پیش‌نیازهایِ برآورده‌نشده) شروع می‌شود — نه از صفر.

---

## چت ۹ — دور ۱: پایش وضعیت تصمیم الزامات پایش + اعلام idle (ثبت: 2026-09-14)

**انحرافِ شمارۀ دور/نقش (ثبت، نه اصلاحِ خاموش):** بریف این کار را «چت ۹ — دور ۱» و «بیست‌وپنجمین دور idle» نامیده، اما پروندۀ **همین نشست** روی همین شاخه تا «دور ۱۶» پیش رفته بود (`0ef89e6` → `ab933c1` → `a3f0b59`) و نقشِ دو دورِ قبل «چت ۸/بستهٔ ۱» بود؛ شمارشِ اعلام‌هایِ صریحِ idle در دو گزارشِ روزانه = **۹**، و رشتهٔ «بیست‌وپنجم» فقط در نامِ فایل `DOCS_FREEZE_v1.0.0-rc25.md`/`DOCS_INDEX.md` می‌آید (**rc25**، نه شمارۀ idle) — دقیقاً همان انحرافی که در دور ۱۶ سنجیده و ثبت شد و بریف دوباره تکرار کرده.

### §۰) بریف در برابرِ واقعیت (هفت انحراف، همگی اندازه‌گیری‌شده بر `main = 895f8d4`)

| # | ادعای بریف | واقعیت (شاهد) |
|---|---|---|
| ۱ | گیتِ فاز ۰.۵ = `gh api …/contents/DECISION_LOG.md` در پوشۀ `docs` + `grep -q "الزامات پایش"` | فایل **وجود ندارد**: GitHub Contents API ⇒ `{"status":"Not Found"}` و `git ls-tree origin/main` روی نامِ فایل ⇒ = **۰** ⇒ گیتِ بریف **از نظرِ ساختاری هرگز سبز نمی‌شود**. ثبت‌هایِ واقعیِ تصمیم: `docs/SUPERVISOR_DECISIONS_2026-09-13.md` و `docs/ARCHITECTURE_DECISIONS.md` |
| ۲ | «تصمیم الزامات پایش اعلام نشده» | درست، ولی **به دلیلِ ضعیف‌تری**: `git grep -c "الزامات پایش" origin/main` = **۰ در کل مخزن** (نه فقط در DECISION_LOG). محتوایِ الزامات **بی‌صاحب نیست**: ۶ سندِ پایش روی main است (`OBSERVABILITY.md`, `OBSERVABILITY_DEPLOYMENT.md`, `OBSERVABILITY_LIVE_SETUP.md`, `WAVE14_OBSERVABILITY.md`, `REDIS_MONITORING_ALERTING.md`, `ONCALL_SCHEDULE.md`) ⇒ چیزی که اعلام‌نشده **حکمِ ACCEPT** است، نه نبودِ متن |
| ۳ | «سناریوهای آزمایشی در `rto-rpo-summary.csv` وجود دارند» | **چنین فایلی نیست**؛ تنها CSV های مخزن: `docs/samples/students_sample.csv` و `teachers_sample.csv`. رشته‌های `S1-api-kill`/`S6-disk-pressure`/`S10-clock` = **۰ hit**. جایگزینِ واقعی (بخشِ §۳) = خانوادهٔ دریل‌ها |
| ۴ | «۵ شکاف؛ همگی نیازمند تغییرِ کد» | **۲ تأیید، ۲ رد، ۱ نیمه** — با عدد در §۱ و §۲ |
| ۵ | «آلارم‌های معنادار تعریف نشده‌اند» | **رد**: **۳۴** قانون آلارم موجود است (**۲۴** در `infra/observability/alerts.yml` + **۱۰** در `infra/observability/alert-rules.yml`) |
| ۶ | «داشبوردهای مانیتورینگ تعریف نشده‌اند» | **رد**: `infra/observability/dashboards/payesh-main.json` (۱۰ پنلِ timeseries) + `infra/observability/payesh-dashboard.json` (۱۲) + `dashboards/payesh-logs.json` + provisioning (۴ ارجاعِ dashboard)؛ و **۰** پنل به سنجهٔ ناموجود `payesh_cache_hit_rate` ارجاع می‌دهد ⇒ پنلِ شکسته نیست |
| ۷ | «ادغام با on-call انجام نشده» | **نیمه**: `alertmanager.yml:5-19` route + تشدیدِ `severity="critical"` (group_wait 10s، repeat 1h) واقعی است، ولی تنها receiver = `payesh-webhook` با `url: "__WEBHOOK_URL__"` (جای‌خالی) و receiver ایمیل در `:21-26` **کامنت** است؛ `docs/ONCALL_SCHEDULE.md:5` هم خودش را «مصوبِ پیشنهادی — انتسابِ واقعی…» می‌نامد ⇒ سندِ چرخش هست، **کانالِ زنده** نیست |

### §۱) تصمیم الزامات پایش — **NOT-DECIDED** (گیتِ خودِ بریف + سه شاهدِ متقاطع)

- فرمانِ بریف، عیناً: `timeout 30 gh api repos/rezaa2544/p2/contents/…` برای `DECISION_LOG.md` (پوشۀ `docs`) ⇒ **404 Not Found** (پس `jq -r .content | base64 -d` روی هیچ اجرا می‌شود ⇒ `grep -q` هرگز true نمی‌شود) — **ثبت می‌کنم که شرطِ فاز ۰.۵ عملاً غیرقابل‌تحقق است، نه اینکه «تصمیم منفی» است**
- `git grep -c "الزامات پایش" origin/main` ⇒ **۰** · `NEXT_ACTIONS.md` (سرِ فایل، سطر ۱–۱۲): P0-1..P0-6 + «صفِ ۱۳ PRِ بلاک‌شده با ACCEPT صریح…» ⇒ هیچ سطرِ «الزامات پایش» ندارد
- `docs/SUPERVISOR_DECISIONS_2026-09-13.md` (آخرین ثبت‌ها) ⇒ آخرین تصمیمِ امضاشده دربارهٔ «پایش تریگرِ بنر `data-rpt-partial-shell` + کانترهای /metrics + سقف ردیف» است (۲۰۲۶-۰۹-۱۳) — **نزدیک‌ترین تصمیمِ موجود**، ولی دقیقاً همان «الزامات پایش» بستهٔ ۹ نیست
- `gh api repos/…/issues/184` (فرمانِ تصحیح‌شدۀ دور ۱۶ — #184 یک **PR** است نه issue) ⇒ ۰ کامنت شاملِ «الزامات پایش»؛ PR بازها: فقط **#211 (همین شاخه)**؛ `ls-remote main` در لحظۀ بستن = `0360b37` (یعنی main حینِ همین دور هم ۱ مرج گرفت: `895f8d4` → `0360b37`)
⇒ **فاز ۰.۵ اجرا نشد**؛ بستهٔ ۹ = **NOT-VERIFIED / اجرای واقعی NOT-RUN**؛ **۰** کار روی کد/تست (حکم ۲).

### §۲) ارزش افزودهٔ این دور: **آینۀ SLO ↔ آلارم** (هر ردیف با `file:line` — پایهٔ acceptance bundle واقعی)

| SLO (`docs/CAPACITY_MODEL.md` §۵) | آلارم/آینۀ موجود | وضعیتِ اندازه‌گیری‌شده |
|---|---|---|
| p50 < 100ms | — | ❌ **هیچ آلارمی** (`grep -F 'histogram_quantile(0.5'` در هر دو فایل = ۰) |
| p95 < 300ms | `PayeshLatencySlobreach` (`alerts.yml:28`) · `HighLatency` (`alert-rules.yml:19`) | ✅ |
| p99 < 1s | `PayeshLatencyP99Breach` (`alerts.yml:38`) | ✅ |
| 5xx < 0.1٪ | `PayeshHighServerErrorRate` (`:17`) expr `> 0.01` ⇒ **۱٪** = ۱۰× شل‌تر از SLO · `CriticalErrorBudgetBurn` (`:48`) expr `14.4 * 0.0005` ⇒ بودجهٔ **۰.۰۵٪** (= نصفِ ۰.۱٪ سند) | ⚠ **ناسازگاریِ آستانه در دو جهتِ مخالف** در یک فایل؛ مرجعِ بودجه: `docs/WAVE14_OBSERVABILITY.md:174` (Availability 99.95٪ با PromQL — ولی **آلارم ندارد**) |
| کش > 80٪ | `PayeshCacheHitRateLow` (`:137`) = `sum(rate(payesh_cache_lookups_total{outcome="hit"}[15m])) / clamp_min(sum(rate(payesh_cache_lookups_total[15m])),1e-9) < 0.8` | ✅ — و **تصحیحِ خودم**: فرمولِ پیشنهادیِ من در دور ۱۶ (`hits_total / lookups_total`) با قراردادِ واقعیِ مخزن فرق دارد؛ `payesh_cache_hits_total` فقط «compat alias of l1Stats» است (`server/metrics.js:514`) و مرجعِ درست، برچسبِ `outcome` روی lookups است |
| sync ≥ 99.5٪ | `PayeshSyncConflictSpike` (`:175`) · `PayeshOutboxBacklog` (`:159`) · `SyncQueueDepth` (`alert-rules.yml:42`) | ⚠ **نسبتِ موفقیت آلارم ندارد**؛ سه آلارمِ موجود عمق/تعارض را می‌گیرند نه ۹۹.۵٪ |
| OTP ≥ 99.9٪ | `PayeshOtpFlood` (`:225`) · `PayeshLoginAbuse` (`:214`) | ❌ این دو **امنیتی**‌اند نه SLO؛ آلارمِ دسترس‌پذیریِ OTP **نیست** (سنجهٔ `payesh_auth_otp_requests_total{outcome}` در `metrics.js:438,713` موجود ⇒ فقط قانون کم است) |
⇒ **جمع‌بندی: ۳ از ۷ کامل · ۲ نیمه (عمق/تعارض به‌جای نسبت) · ۲ فاقد آلارم (p50، OTP-avail) · ۱ با آستانۀ ناسازگار (5xx)**؛ و **۰ `record:` rule** در هر دو فایل ⇒ «تحلیل روند» عملاً فقط `AnomalyDetected` (`alert-rules.yml:65` روی `payesh_runtime_anomalies_total`) است، نه rollup های SLO.

### §۳) نردبانِ ۷ پیش‌نیازِ بریف + نگاشتِ سناریوها (به‌جای `rto-rpo-summary.csv`ِ غیروجود)

پیش‌نیازها: ۱) پایش متمرکز ⇒ **موجود** (`prometheus.yml` + `docker-compose.observability.yml` + `loki-config.yml` + `promtail.yml` + `otelcol-config.yml`) ولی `command -v docker` ⇒ **ABSENT** پس **اجرا NOT-RUN** · ۲) داشبورد ⇒ **۲ داشبورد / ۲۲ پنل** ✅ · ۳) آلارم ⇒ **۳۴ قانون** ✅ (با ۴ خلأ در §۲) · ۴) on-call ⇒ ⚠ webhookِ placeholder · ۵) گزارش خودکار ⇒ ❌ **۰** ابزارِ تولیدِ گزارشِ عملیاتیِ خودکار (`ls-tree tools` تنها `bench-reports-*` و `mutate-reports-check.sh` را داد که بنچ‌اند/تغییر‌سنج‌اند) · ۶) تحلیل روند ⇒ ⚠ **۰ `record:`**؛ فقط anomaly · ۷) مستندات ⇒ ✅ **۶ سندِ پایش** (§۰/۲).

نگاشتِ سناریوهایِ بریف به فایل‌هایِ واقعی (سنجیده با `ls-tree`/`git grep`): `S1→tests/chaos-drill-api-kill.js` · `S2→chaos-drill-redis-outage.js` · `S3→chaos-drill-pg-outage.js` · `S4→chaos-drill-queue-outage.js` · `S5→chaos-drill-network-degradation.js` · `S6→chaos-drill-disk-pressure.js` · `S7→tests/sync-conflict-storm-drill.js` ⇒ **۷ از ۱۰ فایلِ اختصاصی دارد**؛ `S8 partial-failure` / `S9 retry-storm` / `S10 clock-storm` ⇒ **۰ فایلِ اختصاصی** (پوششِ پراکنده در `tests/offline-sync-drill.js`، `tests/server1.js`، `tests/server17.js`، و همان `sync-conflict-storm-drill.js` برای retry) — یعنی ادعای «۱۰ سناریو در CSV» **نه فایلش هست نه شماره‌گذاریش**.

دو شکافِ «نیازمند تغییرِ کد» از بریف، با حکمِ نهایی: **(الف) گیج فشار دیسک ⇒ تأیید شد** (`grep -icE 'disk|filesystem' server/metrics.js` = **۰** و **۰** node_exporter/cadvisor/process-exporter در `infra/observability` ⇒ هیچ منبعِ متریکِ دیسک نیست؛ دریلِ S6 هم برون‌بانده: `tests/chaos-drill-disk-pressure.js:41` از `infra.diskFreeKb()` (= `df`) استفاده می‌کند و کمکی‌اش در `tests/chaos-drill-lib.js:364` است؛ نکته: `chaos-drill-lib.js:265` می‌تواند tmpfs بزند ولی `id -u` = **1001** ⇒ اینجا نشدنی) — راه‌حلِ کم‌هزینه **node_exporter**، نه کدِ اپ. **(ب) گیج صفِ per-instance ⇒ نیمه**: `payesh_sync_queue_depth` هست (`metrics.js:513`، برچسب `[]`) و Prometheus با `instance` تفکیک می‌کند، اما `prometheus.yml:29-33` برچسبِ `instance_role: secondary` را **کامنت** گذاشته ⇒ گلوگاه، **config** است نه کد؛ و برای صفِ **درخواست‌هایِ HTTP** واقعاً هیچ گیجی نیست (تنها یک نامِ `*queue*` در metrics.js).

### §۴) سه فایل موقت — ساخته **نشدند** (و بریف این دور حتی در عددِ خودش ناسازگار است)

بریف فاز ۱ سه فایل می‌خواهد (`MONITORING_DECISION_MONITORING.md`, `OPERATIONAL_MONITORING_PREP.md`, `OPERATIONAL_PREP.md`) اما acceptance bundle‌اش «**FILES: 2 docs موقت**» می‌گوید ⇒ **۲ یا ۳؟** در خودِ بریف مشخص نیست. سه دلیلِ رد (پیشینۀ اندازه‌گیری‌شده): (الف) دور ۱۵ آزمود: آوردنِ این نام‌ها در گزارش ⇒ **+۳ ارجاعِ کهنه**؛ (ب) `docs-refs-check` روی درختِ امروز: **۱۱۳ کهنه در ۶۵ سند** = baseline ۷۵ + ۴۱ِ پیش‌ازین (هیچ‌کدام از من) ⇒ بدهیِ همین الگو در main دیدنی است؛ (ج) **حکم ۳ِ خودِ بریف** (سندِ تازه ← ارجاعِ ورودی یا `tools/docs-refs-baseline.json`) با فایلِ ۲۰دقیقه‌ایِ سوزنی قابل‌رضایت نیست. محتوا در §۱–§۳ همین بخش است. **۰ فایلِ تازه**، پس چیزی برای «حذف در پایانِ دور» هم نیست.

### §۵) وضعیتِ شاخه + **ریشهٔ تکرارِ «کامیتِ گمشده»** (یافتِ این دور)

- ابتدای دور: `HEAD` دوباره روی `0a208f8` بود و `git fetch origin --prune` شاخۀ نشست را **نمی‌آورد**. دلیل، اندازه‌گیری‌شد: `git config --get-all remote.origin.fetch` = `+refs/heads/main:refs/remotes/origin/main` **تنها** (و `.git/shallow` موجود) ⇒ هر fetch معمولی، tipِ `arena/01a09a73-p2` را هرگز update نمی‌کند؛ این همان علتِ سه «بازیافتِ متوالی» (دور ۵، ۱۵، ۱۶) است، نه باگِ نگاریِ گزارش‌ها.
- اقدامِ اصلاحیِ محلی (بدونِ لمسِ فایلِ مخزنی): افزودنِ refspec دوم برای همین شاخه ⇒ از این پس fetch معمولی، tip را می‌آورد؛ **هیچ فایلِ ردیابی‌شده‌ای تغییر نکرد** (`git status` پس از reset = ۰).
- وضعیتِ remote: tipِ شاخه = `a3f0b59` (دور ۱۶ من) — نشستِ دیگری پس از دور ۱۶ چیزی push نکرده؛ **PR #211: OPEN · `mergeable = CONFLICTING`** (main در این فاصله `5a175c0 → aeb32a9 → 895f8d4 → 0360b37`)؛ اصلاح با **صاحبِ ادغام**: `git rebase origin/main arena/01a09a73-p2` (هر سه بخش append-only؛ تعارضِ EOF با «keep both» حل می‌شود). من rebase نکردم: tip، کامیتِ نشستِ دیگر (`ab933c1` = دور ۶) را هم حمل می‌کند.

### §۶) ENGINEERING CLAIM

- **EXPECTED BEHAVIOR:** پایشِ تصمیمِ «الزامات پایش» · آماده‌سازیِ بستهٔ ۹ (Operational Monitoring) · **idle** تا اعلام تصمیم
- **MEASURED BEHAVIOR:** **۰ فایلِ تازه** (قالبِ بریف «3 docs موقت ساخته شد» را از پیش پر کرده بود — **انجام نشد**؛ نوشتنش = جعلِ سبز) · **۰ کد** · **۰ تست** · **۳۸** فرمانِ سنجشی، همه با `timeout` · **۷** انحرافِ بریف (§۰) · **۳۴** قانون آلارم و **۲۲** پنلِ داشبورد شمارش و به ۷ SLO نگاشت شد (§۲) · **۱** ریشه‌یابیِ علتِ «کامیتِ گمشده» + اصلاحِ محلیِ refspec (§۵)
- **TEST ENVIRONMENT:** n/a (هیچ سرویسی بالا نیامد؛ `docker: ABSENT`) · **DATASET SIZE / CONCURRENCY / P50·P95·P99 / ERROR RATE:** n/a — اجرای بار و دریل‌ها **NOT-RUN** (بستهٔ ۹ ≠ مجوزِ اجرای دریل)
- **DB IMPACT:** صفر نوشتن (بدونِ seed، بدونِ --plan، بدونِ دریل) · **CACHE IMPACT:** n/a
- **SECURITY IMPACT:** ۰ توکن در فایل/argv/کامیت/گزارش · `secret-scan` **12/12** · هیچ رازِ تازه‌ای در `alertmanager.yml` ثبت نشد (خودِ فایل هم smtp auth را از env می‌خواهد و من کامنتش را هم بازنویسی نکردم) · ۶ PAT فاش‌شده، **revoke توسط مالک** · بات‌های review trigger نشدند
- **FAILURE TEST:** NOT-RUN (با دلیل: اجرای دریل‌ها نیازمندِ تصمیم + root/tmpfs + docker است؛ §۳) · **RECOVERY RESULT:** tipِ واقعی با fetchِ refspecِ صریح بازیابی شد؛ `git reset --hard origin/arena/01a09a73-p2` ⇒ ۲۸۳ سطر در `2026-09-14.md`، `git status` = **۰**
- **REGRESSION RISK:** صفر تغییرِ کد/تست — فقط یک الحاقِ متنی
- **KNOWN UNKNOWNs:** CI **NOT-RUN** (بیلینگ، RISK-O-007) · تصمیمِ «الزامات پایش» **NOT-DECIDED** (و گیتِ بریف به فایلِ ناموجود بسته شده) · آیا `payesh_sync_queue_depth` در پروداکشن چند‌نود به‌تنهایی کافی است: **نمی‌دانم** (نیازمند اجرای stack) · آلارمِ p50/OTP-avail عمداً حذف شده یا فراموش: **در هیچ سندی ثبت نشده** · ظرفیت ملی اثبات‌نشده (P0)
- **FINAL VERDICT:** **NOT-RUN** (تصمیم اعلام نشده؛ بستهٔ ۹ **NOT-VERIFIED**) + **VERIFIED** فقط برای «آینۀ SLO↔آلارم و شمارشِ دارایی‌های پایش» (§۲/§۳) — این PASS نیست، چون هیچ آلارمی شلیک نشده است

### §۷) گیتِ ۸ پرسش

1. **چه چیزی تغییر کرد؟** فقط `docs/daily-reports/2026-09-14.md` (الحاق؛ **+۰ فایلِ سند، +۰ کد، −۰ خط**)
2. **چرا؟** تصمیمِ الزامات پایش هنوز اعلام نشده، ولی خلأهایِ واقعیِ بستهٔ ۹ تا پیش از آن باید **اندازه** می‌شدند تا ACCEPT معنادار باشد: کدام SLO آلارم دارد و کدام نه (§۲)
3. **چه invariant باید حفظ شود؟** بستهٔ ۹ قفل تا حکمِ صریح · گزارش فقط append-only · ۰ rc bump · ۰ سندِ بادکرده · هیچ فایلِ مخزنی جز گزارش تغییر نکرد (و `.git/config` محلی = تنها تغییرِ غیرمخزنی، ثبت‌شده در §۵)
4. **چگونه ثابت می‌کنیم درست است؟** هر عدد در §۰–§۵ یا `file:line` دارد (`alerts.yml:17/28/38/48/137/159/175/225`، `metrics.js:513/514/438`، `prometheus.yml:29-33`) یا خروجیِ فرمانِ اجراشده (`gh api` 404، `git grep -c`، `ls-tree`، `docker`، `id -u`)
5. **سریع‌تر/مقیاس‌پذیرتر؟** n/a — صفر اجرای بار؛ و یک بهبــودِ کم‌هزینه که از همین داده می‌آید: افزودنِ `node_exporter` به‌جای نوشتنِ گیج دیسک در اپ (§۳)
6. **امن؟** ۰ توکن + `secret-scan 12/12` + بدونِ triggerِ بات + بدونِ ویرایشِ فایل‌هایِ دیگران/بقیۀ چت‌ها
7. **failure را چگونه تست می‌کنیم؟** NOT-RUN با دلیل؛ سناریوهایِ S1–S7 آماده‌اند (فایل‌هایِ chaos-drill) ولی اجرا **محظوظِ** تصمیم + docker + root/tmpfs
8. **rollback؟** `git revert` روی SHAِ همین کامیت (الحاقِ خالص ⇒ بدونِ درگیریِ بخش‌هایِ چت ۳/۵/۸ روی همین فایل)

### §۸) گیت‌ها · بودجه · پوش · idle

- **گیت‌های مسیرِ تغییر (یک‌بار روی درختِ نهایی، `timeout`دار):** `docs-stats-sync --check` ✅ (۴۰۶ سند = ۳۴۸ ریشه + ۵۸ زیرپوشه · قفل **rc43**، **۰ bump**) · `tests/docs-freeze-marker.js` **14/14** · `tests/docs-metadata.js` **17/17** · یتیم **۰** · `tools/docs-refs-check.js` **۴۱ کهنه = ۰ِ تازه از این دور** · `tests/secret-scan.js` **12/12** · `tools/reza-mirror-check.js` ✅ **۰ drift / ۰ آینهٔ تازه / ۰ cp دستی** · **۰** بازاجرای دورهایِ قبل، **۰** بازاجرای سوئیت پایش/دریل‌ها، **۰** manifest regen
- **جای‌خالی در الحاقِ من:** جست‌وجوی نشانه‌هایِ جای‌خالیِ متنی (double-at، «to‑do»، «fix‑me»، angle-bracket، double-brace) ⇒ **۰**
- **بودجۀ §۱ (دو اندازه‌گیریِ مستقل، بدونِ `node_modules`/`.cache`):** پیش **100.64 MB / 1549 فایل** (که 68.4 MB آن `.git` است؛ بدونِ `.git` = 32.23 MB) → پس **100.66 MB / 1549 فایل** (بدونِ `.git` = 32.25 MB) 🟢 — دلتا = **14 951 B / 88 سطر** در **یک فایلِ موجود**، شمارندۀ فایل **ثابت (1549 → 1549)** ⇒ **۰ فایلِ تازه** و `.git` هم رشد نکرد (68.4 → 68.4 MB، چون این دور فقط **یک** fetch با refspecِ صریح انجام شد)
- **پوش:** SHAِ این کامیت درونِ خودش قابل‌نوشتنیست (محدودیتِ خودارجاعی)؛ زنجیره: `bb5068b ← 4c233af (احیای دور ۵) ← 0ef89e6 (دور ۱۵) ← ab933c1 (دور ۶، نشستِ دیگر) ← a3f0b59 (دور ۱۶) ← این کامیت`؛ تأییدِ بیرونی = `git ls-remote` و به‌روزرسانیِ PR #211 (پس از push در پیامِ نشست ثبت می‌شود)
- **idle رسمی (چت ۹ — بستهٔ ۹):** تا یکی از: **ACCEPT/ردِ صریحِ «الزامات پایش»** (در `SUPERVISOR_DECISIONS` یا جایِ مصوبِ دیگر — نه `DECISION_LOG` که وجود ندارد) · **حکمِ ناظر** · **بریفِ تازه**. پیگیریِ مجاز: تازهِ‌شدنِ همان تصمیم + **رفعِ تعارضِ PR #211**. با ACCEPT، کار از §۲ شروع می‌شود (۴ خلأ: آلارمِ p50، آلارمِ OTP-avail، آلارمِ نسبتِ موفقیت sync، هم‌راستاسازیِ آستانۀ 5xx با 0.1٪) + §۳ (منبعِ دیسک با node_exporter، بازکردنِ `instance_role: secondary`، کانالِ واقعیِ alertmanager) — **نه** از صفر، و **بدونِ** بازساختنِ چیزی که §۲ نشان می‌دهد از قبل موجود است.

---

## چت ۹ — دور ۱۰: پایش تصمیم «الزامات پایش» + provenanceِ اعدادِ S1–S10 (ثبت: 2026-09-14)

**انحرافِ شمارۀ دور/شاخه (ثبت، نه اصلاحِ خاموش):** بریف می‌گوید «مبنا: شاخۀ `docs/daily-reports-2026-09-14` @ [SHA کامیت دور ۱]» و عنوانِ بخش «دور ۱۰» است، درحالی‌که **شاخۀ نشستِ من** `arena/01a09a73-p2` است و دورِ قبلی‌اش «دور ۱» (کامیت `f3dbdb1`)؛ آن شاخۀ دیگر **PR #218 = «چت ۸ — دور ۱۸»** است (نشستِ دیگری، همین‌جا دست‌نخورده). من روی شاخۀ خودم الحاق می‌کنم؛ شماره‌گذاریِ Coordinator بینِ نشست‌ها هم‌راستا نیست و این سومین دورِ متوالی است که این انحراف را ثبت می‌کنم. «بیست‌وپنجمین دور idle» هم همان ادعایِ ردشدۀ دورهایِ ۱۶ و ۱ است (شمارشِ واقعیِ اعلام‌ها = ۹؛ «۲۵» فقط `rc25` است).

### §۰) دروازهٔ تصمیم — **NOT-DECIDED**، و گیتِ این دور هم به فایلِ ناموجود بسته شده

- فرمانِ بریف عیناً: `gh api repos/rezaa2544/p2/contents/SUPERVISOR_DECISIONS_2026-09-14.md | jq -r .content | base64 -d | grep -q "الزامات پایش"` ⇒ **grep exit=1**؛ دلیلِ اول: آن فایل **وجود ندارد** (`gh api` بدونِ خروجی؛ `git ls-tree origin/main` روی همان نام ⇒ تنها `docs/SUPERVISOR_DECISIONS_2026-09-13.md` = تنها `docs/SUPERVISOR_DECISIONS_2026-09-13.md`) ⇒ دروارۀ بریف برای دومین دورِ متوالی به مسیرِ اشتباه وصل است (دورِ قبل `DECISION_LOG.md`، این دور `SUPERVISOR_DECISIONS_2026-09-14.md`)
- `git grep -c "الزامات پایش" origin/main` ⇒ **۰ در کلِ main** (نه فقط در فایل‌هایِ تصمیم) ⇒ نه ACCEPT، نه رد؛ **فاز ۰.۵ اجرا نشد**
- **ولی** `docs/daily-reports/2026-09-14.md:7` روی main می‌گوید: «**#184 مرج شده و §۶ مصرف شده است** (VERIFIED)» ⇒ یک نشستِ دیگر، «مصرفِ §۶» را VERIFIED اعلام کرده؛ چونِ من **تصمیمِ الزاماتِ پایش نیست** و ادعایِ بیرونی را بازتولید نکردم (بازاجرا ممنوع) — فقط تطبیقِ source می‌دهم در §۲

### §۱) provenanceِ عددی که بریف می‌خواهد «بر اساسِ داده‌هایِ واقعی» بنویسم — سه‌لایه، سنجیده

1. **خروجیِ ابزارِ ردیابی‌شده:** `tools/chaos-test.sh:53` ⇒ `OUT_DIR="tests/chaos-output"` و فایل‌ها `<scenario>-{before,after}.json` + `<scenario>-timeline.csv` (`:116,:137`)؛ و `.gitignore:47` ⇒ `tests/chaos-output/` **عمداً نادیده‌گرفته** می‌شود. پس هیچ CSVِ دریلی در git نیست و نباید باشد.
2. **مسیرِ ارجاع‌شده (`chaos-evidence/rto-rpo-summary.csv`):** در git **۰**؛ در `.gitignore` هم **نیست** (پس نه ignored است نه tracked)؛ و رشتهٔ `chaos-evidence` در **هیچ** اسکریپتِ ردیابی‌شده‌ای (`tests tools src infra scripts`) نوشته نمی‌شود ⇒ **یک پوشۀ ad-hocِ یک اجرایِ دستی، بدونِ بازتولیدِ دستورِ واحد**. (این را چت ۵ هم مستقل دیده: `:75` «`rto-rpo-summary.csv`ِ ارجاع‌شده در پرامپت در ریپو نیست» و `:390` «`chaos-evidence` = ۹ فایل» ⇒ **تطبیقِ دو-منبعی**، نه ادعایِ من.)
3. **جایِ ثبتِ درست = `docs/DR_RUNBOOK.md` §۶ (سطر 129، «جدولِ drill-log»):** ردیف‌هایِ موجود = WAL disk-full 2026-09-11 (RTO **30.2ث**، RPO **0**)، بازاجرای 2026-09-13، disk-fullِ سطح‌اپ + kill-api + pg-down، و Queue-Outage — ولی `grep -cE "376|144|9545|1353|28948"` روی DR_RUNBOOK = **۰** ⇒ **ردیفِ S1/S2/S3/S4/S5 در register نیست**؛ این عدد‌ها فقط در گزارش‌هایِ روزانه‌اند (`docs/daily-reports/2026-09-14.md`).
4. ⚠ **تلهٔ grep:** `9545` و `1353` در git یک hitِ دیگر هم دارند: `tests/performance/fixtures/test-users.json` — **هم‌اندازۀ تصادفی، نه شاهد**. اگر کسی «پیدا شد در مخزن» گزارش کند، گولِ همین fixture را خورده است.

### §۲) چرا اعدادِ S1–S6 در بریف نمی‌توانند «به‌روز» خوانده شوند (تعارضِ مستقیم با #185)

- `gh pr view 185` ⇒ **MERGED 2026-09-13T20:21:33Z**، کامیت `703f93a` = «رفع سه یافتهٔ chaos-drill — **F1** گم‌شدنِ دادهٔ ack‌شده در قطعِ PG + **F2** 500 در قطعِ Redis + **F3** بازپخشِ صف پس از کرش»
- ⇒ ادعایِ بریف «**S3** … RPO: ۳ نوشتِ ackشده گم شد» **پیش‌ازرفع** است (بعدِ F1 = صفر، طبقِ اصلاحِ موردیِ چت ۵ در `:103` که خودش می‌گوید «بدونِ بازاندازه‌گیری»)
- ⇒ ادعایِ «**S4** … ۶ رویداد تا ابد pending» هم **با F3 در تضاد است** (بازپخشِ صف پس از کرش = همان چیزی که «تا ابد pending» را می‌بندد)؛ خودِ کد هم شاهد است: `replayPendingFromPg` (`server/index.js:551` · `server/outbox.js:154`) — ارجاعِ چت ۵ در `:367`
- ⇒ پس: **S3/S4 RPO = نه مقدارِ بریف، نه عددِ تازه؛ NOT-MEASURED-POST-FIX** و تنها راهِ درست، اجرایِ دوباره + ثبتِ خطِ امضاشده در §۶ است (نه کپی‌کردنِ CSV سوزنی). S6 (دیسک) نزدیک‌ترین به register است: 28 948 ms ≈ ردیفِ «30.2ث» — **ولی با قیدِ صریحِ §۶** که `wal_segment_size=1MB` بوده و عددِ مطلق به تولید تعمیم نمی‌خورد.

### §۳) راه‌حل‌هایِ پیشنهادیِ بریف — ۵ مورد: ۲ درست، ۳ با لنگر/نامِ غلط (اندازه‌گیری روی `infra/observability/alerts.yml`)

| پیشنهادِ بریف | سنجش |
|---|---|
| p50 → `PayeshLatencyP50Breach` در `alerts.yml:25` | ❌ **لنگر غلط**: `:25` خطِ `summary` داخلِ `PayeshHighServerErrorRate` است (`:17`)؛ جایِ درج، کنارِ `:28` (`PayeshLatencySlobreach`). خودِ expr پیشنهادی با `payesh_http_request_duration_seconds_bucket` ✅ معتبر است |
| OTP → expr با `payesh_auth_otp_fail_total` / `payesh_auth_otp_total` | ❌ **هر دو نام در کل مخزن ۰ هستند** (metrics.js=0، repo=0) ⇒ آلارم روی دادهٔ خالی = **سبزِ خاموش** (Prometheus بدونِ series هرگز fire نمی‌کند). سنجهٔ واقعی: `payesh_auth_otp_requests_total{outcome}` (`server/metrics.js:438,713`) ⇒ `1 - sum(rate({...{outcome!="ok"}[5m]))/clamp_min(sum(rate(payesh_auth_otp_requests_total[5m])),1e-9) < 0.999` |
| sync → expr با `payesh_sync_success_total` / `payesh_sync_total` | ❌ **۰ در کل مخزن**؛ سنجه‌هایِ واقعی: `payesh_sync_pulls_total` + `payesh_sync_conflicts_total` ⇒ `1 - rate(conflicts)/clamp_min(rate(pulls),1e-9) < 0.995` (همان دو نامی که آلارمِ `PayeshSyncConflictSpike:175` هم مصرف می‌کند) |
| 5xx → تصحیحِ آستانه در `:17` و `:48` | ✅ لنگرها درست‌اند (`:17` = `> 0.01` = ۱٪؛ `:48` = `CriticalErrorBudgetBurn` با `14.4 × 0.0005`) — ولی توجه: `:48` را «باید ۰.۱٪ باشد» خوانده، درحالی‌که آن expr **نرخِ سوختنِ** بودجه است نه خودِ SLO؛ اگر ۰.۰۰۰۵ → ۰.۰۰۱ شود، آستانه **شل‌تر** می‌شود نه اصلاح. پیشنهادِ من: فقط `:17` به ۰.۰۰۱ و نگه‌داشتنِ burn-rate با توضیح |
| دیسک → node_exporter با `node_filesystem_avail_bytes / node_filesystem_size_bytes < 0.2` | ✅ فرمول درست و لنگرِ خلأ هم درست (`server/metrics.js`: ۰ ذکرِ disk/filesystem؛ ۰ node_exporter در `infra/observability`) — ولی این **تغییرِ compose/scrape است نه کدِ اپ**؛ و `prometheus.yml:29-33` برچسبِ `instance_role: secondary` هنوز کامنت است |

نام‌هایِ غلطِ دیگرِ بریف: p99 را `PayeshLatencySlobreach` در `:38` خوانده (در `:38` یعنی `PayeshLatencyP99Breach`) و کش را `PayeshCacheHitRate` (اسمِ واقعی `PayeshCacheHitRateLow:137`).

### §۴) سه فایل موقت — باز هم ساخته **نشدند** (تکرارِ چهارمین دور؛ دلیل‌ها ثابت و اندازه‌گیری‌شده)

`MONITORING_REQUIREMENTS_MONITORING` / `OBSERVABILITY_PREP` / `OPERATIONAL_PREP` — و acceptance-bundle همان «FILES: 2 docs موقت» را می‌گوید که با «سه فایل» در تضاد است (همان ناسازگاریِ دورِ قبل). حکم ۳ِ بریف (ارجاعِ ورودی/baseline) + بدهیِ اندازه‌گیری‌شده (+۳ ارجاعِ کهنه در دور ۱۵؛ `docs-refs-check` امروز ۱۱۳ کهنه/۶۵ سند که **همه وراثتی‌اند** — چت ۵ هم در کامیتِ «docs(chat5): دور ۵ (اصلاحی)» همین را ثبت کرده: «fresh=۴۱ وراثتی، برابرِ والد») ⇒ همهٔ محتوا در §۰–§۳ همین‌جا. **۰ فایلِ تازه ⇒ چیزی برای حذف نبود.**

### §۵) دو نکتهٔ بهداشتی/سازهٔ این دور

- **اصلاحِ دورِ قبل جواب داد:** `git config --get-all remote.origin.fetch` حالا دو refspec دارد ⇒ `git fetch origin --prune` بدونِ فرمانِ خاص، tipِ شاخه را آورد: `local HEAD == origin/arena/01a09a73-p2 == f3dbdb1` و `git status` = **۰** ⇒ برای اولین بار در چهار دور، **هیچ ترمیمی لازم نبود** («کامیتِ گمشده» تمام شد).
- `docs/daily-reports/2026-09-14.md` روی main به **۴۹۲+ سطر** رسیده و سه نشسته (چت ۵ دور ۴/۵، من دور ۱، چت ۸ دور ۱۸ روی شاخۀ دیگر) همگی به انتهایش الحاق می‌کنند ⇒ **PR #211 هنوز CONFLICTING** (main در این فاصله `895f8d4 → 49f3a48` = +۵ کامیت، همهٔ چت ۵)؛ ریسکِ تعارضِ EOF با هر دورِ جدید بیشتر می‌شود — پیشنهادِ اجرایی برای صفِ ادغام: **ادغامِ زودترِ #211** (rebase = «keep both» در انتهای فایل). من rebase نکردم: tip، کامیتِ نشستِ دیگر (`ab933c1`) را هم حمل می‌کند.

### §۶) ENGINEERING CLAIM

- **EXPECTED:** پایشِ تصمیمِ «الزامات پایش» · آماده‌سازیِ بستهٔ ۹ (Observability) · idle تا ACCEPT/رد
- **MEASURED:** **۰ فایلِ تازه** (قالبِ بریف «3 docs موقت ساخته شد» را پیش‌پر کرده بود — انجام نشد؛ کپی‌کردنش = سبزِ جعلی) · **۰ کد** · **۰ تست** · **۲۴** فرمانِ سنجشی با `timeout` · **۴** نامِ سنجهٔ اختراعیِ بریف ⇒ ۰ در کل مخزن (§۳) · **۳** لنگرِ غلط از ۵ · **۲** ادعایِ RPO پیش‌ازرفع که با #185 باطل می‌شوند (§۲) · provenanceِ سه‌لایهٔ CSV (§۱) · **۱** تأییدِ بیرونیِ اصلاحِ refspec (§۵)
- **TEST ENVIRONMENT / DATASET / CONCURRENCY / P50·P95·P99 / ERROR RATE:** n/a — بدونِ اجرایِ stack (`docker: ABSENT` از دورِ قبل، بدونِ بازسنجی) · **DB/CACHE IMPACT:** صفر نوشتن
- **SECURITY:** ۰ توکن در فایل/argv/کامیت/گزارش · `secret-scan` **12/12** · بدونِ triggerِ بات · **۶ PAT فاش‌شده، revoke توسط مالک** (همچنان باز؛ این را در KNOWN UNKNOWNِ دائمی نگه می‌دارم)
- **FAILURE TEST:** NOT-RUN (صفر تغییرِ کد؛ و §۲ نشان می‌دهد پیش‌نیازِ «عددِ به‌روز» هم با اجرایِ مجددِ دریل‌هاست که محظوظِ تصمیم است) · **RECOVERY:** لازم نشد (شاخه خودش درست بود)
- **REGRESSION RISK:** صفر تغییرِ کد/تست · **KNOWN UNKNOWNs:** CI **NOT-RUN** (RISK-O-007) · تصمیم **NOT-DECIDED** · **RTO/RPO پس‌ازرفعِ F1/F2/F3 اندازه‌گیری‌نشده** (نه بریف، نه register) · آیا «§۶ مصرف شد» (ادعایِ چت ۵) با اجرایِ مجددِ من هم‌خوان است: **NOT-RUN / بازتولید نکردم** · پوشۀ شواهد ad-hoc است و اسکریپتِ ردیابی‌شده‌ای آن را نمی‌نویسد ⇒ بازتولیدِ عدد نیازمندِ تغییرِ ابزار است (تصمیم، نه کارِ من)
- **FINAL VERDICT:** **NOT-RUN** (بستهٔ ۹ NOT-VERIFIED) + **VERIFIED** فقط برای «provenance و ابطالِ سه ادعایِ عددیِ بریف» (§۱/§۲/§۳) — PASS نه، چون هیچ آلارم/دریلی اجرا نشد

### §۷) گیتِ ۸ پرسش

1. تغییر: یک الحاق به `docs/daily-reports/2026-09-14.md` (+75 سطر / −۰) · **۰** سند، **۰** کد
2. چرا: چون بریف عددی را «واقعی» می‌خواند که دو تای‌شان با #185 باطل شده، و چهار سنجهٔ پیشنهادی‌اش در کد وجود ندارد — acceptance bundle روی این‌ها ساخته نمی‌شود
3. invariant: بستهٔ ۹ قفل تا حکم · append-only · ۰ rc · ۰ سندِ بادکرده · ثبتِ عدد فقط با مسیرِ قابل‌بازتولید
4. اثبات: `file:line` برای هر ادعا (`tools/chaos-test.sh:53/116/137`، `.gitignore:47`، `docs/DR_RUNBOOK.md:129`، `alerts.yml:17/25/48/137/175`، `metrics.js:438/713`، `server/outbox.js:154`) + `gh pr view 185`/`gh api commits/703f93a`
5. سریع‌تر/مقیاس‌پذیرتر: n/a (صفر اجرا)
6. امن: ۰ توکن + secret-scan 12/12 + بدونِ نوشتنِ راز در alertmanager + بدونِ لمسِ configِ دیگران
7. failure: NOT-RUN با دلیل؛ و هشدارِ فعال: اگر آلارم‌ها با نام‌های §۳ نوشته شوند، **failure خاموش** می‌مانند (no-data ⇒ never-fires) — همین را در §۳ استدلال کرده‌ام
8. rollback: `git revert` روی SHAِ همین کامیت (الحاقِ خالص ⇒ بدونِ درگیریِ بخش‌هایِ چت ۳/۵/۸)

### §۸) گیت‌ها · بودجه · پوش · idle

- **گیت‌های مسیرِ تغییر (یک‌بار روی درختِ نهایی):** `docs-stats-sync --check` ✅ (۴۰۶ سند · قفل **rc43**، **۰ bump** — rc مالکِ چت ۶) · `docs-freeze-marker` **14/14** · `docs-metadata` **17/17** · یتیم ۰ · `docs-refs-check` **۱۱۳ کهنه/۶۵ سند = ۴۱ تازه، وراثتی؛ صفرِ تازه از این دور** (پیش از commit، با بازرسیِ رگولارِ خودِ چک‌ر روی بخشِ من: ۰ مسیرِ ناموجود) · `secret-scan` **12/12** · `reza-mirror-check` ✅ **۰ drift / ۰ آینه / ۰ cp** · **۰** بازاجرای دور ۱، **۰** بازاجرای سوئیت پایش/دریل‌ها، **۰** manifest regen
- **بودجۀ §۱ (اندازۀ مستقل در دو سرِ دور):** پیش **102.56 MB / 1576 فایل** (`.git` = 68.6 MB؛ بدونِ `.git` = 33.95 MB) → پس **102.57 MB / 1576 فایل** (`.git` 68.6 → 68.6 MB؛ بدونِ `.git` = 33.96 MB) 🟢 — دلتا = فقط الحاقِ همین بخش به یک فایلِ موجود، **۰ فایلِ تازه** (شمارندۀ فایل ثابت: 1576 → 1576)
- **پوش:** SHAِ این کامیت درونِ خودش نوشته نمی‌شود (خودارجاعی)؛ زنجیره: `bb5068b ← 4c233af ← 0ef89e6 ← ab933c1 ← a3f0b59 ← f3dbdb1 ← این` — تأییدِ بیرونی پس از push: `ls-remote == HEAD` و به‌روزرسانیِ PR #211
- **idle رسمی (چت ۹):** تا **ACCEPT/ردِ صریحِ «الزامات پایش»** (در `docs/SUPERVISOR_DECISIONS_2026-09-13.md` یا جایِ مصوب — نه در فایل‌هایی که بریف نام می‌برد و وجود ندارند) · **حکمِ ناظر** · **بریفِ تازه**. سه کارِ مشخص پس از ACCEPT، بدونِ بازساختنِ موجودها: (الف) اجرایِ S1–S6 روی استیجینگ و **ثبتِ خطِ امضاشده در DR_RUNBOOK §۶** تا عددِ پس‌ازرفع پیدا شود؛ (ب) افزودنِ آلارم‌هایِ p50 / OTP-avail / sync-ratio با نام‌های **واقعیِ** §۳؛ (ج) node_exporter برای دیسک + بازکردنِ `instance_role: secondary` + کانالِ واقعیِ alertmanager. تا پیش از آن: **۰** کد، **۰** تست، **۰** سند.
## چت ۳ — دور ۱۶: پایش triggerها — بدون فعال‌سازی؛ ادامهٔ idle (ثبت: 2026-09-14)

### فاز ۰/۰.۵ — trigger-سنجی (اجرای واقعی این دور)
| ‏trigger | وضعیت | شاهد |
|---|---|---|
| سؤال چت ۱۰ | **NOT-RECEIVED** | ‏۰ issue باز (غیر PR) در مخزن؛ ‏۰ کامنت جدید روی #184 پس از پاسخ دور ۱۵ ‏(issuecomment-5659622468 بی‌پاسخ) |
| استیجینگ | **NOT-RUN** | محیط چندنودی هنوز راه‌اندازی نشده |
| حکم counselor/tenancy | **NOT-RECEIVED** | بدون ثبت جدید |

- تنها رخداد تازه: ‏**PR #209** ‏(چت ۷ — درس‌های L1..L9؛ به‌گفتهٔ خودِ PR: ‏«freeze قرمز خودساخته — تنها مرج نشود؛ نیازمند چرخهٔ rc با چت ۶») — مالکیت دیگران؛ لمس نشد؛ برای صف ناظر ثبت شد.
- ضدتکرار: بسته‌های آمادگی (۵ پاسخ چت ۱۰ / ماتریس استیجینگ / ماتریس tenancy) در بخش دور ۱۵ همین سند موجودند — بازتولید نشدند؛ سه «فایل موقت» فاز ۱ پرامپت ساخته نشد (تناقض مستندشدهٔ دور ۱۵ با ممنوعِ «فایل تازه در docs» + محتوای موجود ⇒ ساخت دوباره = تکرار).
- بهداشت: برای دومین بار restore سندباکس درخت کهنه ‏(`976bfce`) با بقایای pre-merge آورد — الگوی دور ۱۵ تکرار شد: مقایسهٔ فایل‌به‌فایل stash با main ‏(فقط ۲ پرش بیت اجرایی) ⇒ ‏drop؛ درخت به ‏`bb5068b` یکسان شد. ‏**درس ثبت‌شده:** پس از هر restore، پیش از هر کاری ‏`git log -1` با ‏`ls-remote origin main` مقایسه شود.

### ‏CLAIM (خلاصه)
‏MEASURED: پایش ۳ trigger با API (صفر فعال‌سازی) + ثبت #209 + بهداشت درخت · صفر کد/تست/سند تازه · ‏FINAL VERDICT: ‏**VERIFIED** ‏(پایش) + ‏**NOT-RECEIVED/NOT-RUN** ‏(هر سه trigger). گیت ۸ سؤال: تغییر=همین بخش؛ چرا=ثبت وضعیت؛ ‏invariant=idle-تا-trigger؛ اثبات=خروجی API؛ بقیه N/A؛ ‏rollback=revert.

### ‏idle رسمی (دور ۱۶)
همان سه trigger — پاسخ‌های آماده در بخش دور ۱۵.
## چت ۸ — دور ۱۶: پایش وضعیت الگوی A + آماده‌سازی بستهٔ ۱ (National Staging) + هجدهمین دور idle (ثبت: 2026-09-14)

- **وضعیت الگوی A: اعلام نشده** ⇒ **فاز ۱** — اندازهٔ تازه: DECISION_LOG.md **هنوز ساخته نشده** (فایل وجود ندارد) در main (`70a1d0f`) ⇒ تستِ فاز ۰.۵ قابلِ اجرا نبود؛ NEXT_ACTIONS/HANDOFF/docs-HANDOFF + کامنت‌ها: **0 hit**. شمارش: دورِ ۱۶ = هجدهمین دورِ idle (شمارشِ ناظر) — تصمیم در اختیارِ ناظر (حکمِ پابرجا ۱)
- **مبنای اندازهٔ تازه:** پرامپت main را @`bb5068b` و شاخهٔ `docs/daily-reports-2026-09-14` را @SHA دور ۱۵ می‌گفت؛ اندازه: main = **`70a1d0f`** (+#209 چت ۷ · +#210 چت ۳ دور ۱۶) و آن شاخه **در ریمو وجود نداشت** ⇒ شاخه از mainِ تازه ساخته شد (کارِ دورِ ۱۵ روی `arena/01a09a73-p2`@`0ef89e6` ثبت است — دست‌نخورده)
- **آماده‌سازی برای بستهٔ ۱ (فایلِ موقت — ساخته و با پایان دور حذف شد):** ۸ پیش‌نیازِ محیط (2+ نود API · PG/Redis واقعی · RAM ≥ 16GB · دیسک ≈ 140GB scale=1 خطی از فرمولِ measure‌شدهٔ دور ۵ · root/tmpfs · ابزارهای تولد/لود · بهداشت تولید→لود→حذف) / ۷ راهکارِ ایمن (پله‌هایِ مقیاس 0.01→0.1→1.0 · concurrency پله‌ای · WAL فقط در tmpfs جدا · failover drill از DR_RUNBOOK · صفرِ نوشتن به شاخه‌ها · timeout/PID) / ۷ SLO (CAPACITY_MODEL §۵) + RTO/RPO مصوب (SLA 99.95٪ · RTO ≤ 15 دقیقه · RPO ≤ 5 دقیقه · failover ≤ 30 ثانیه — از DR_RUNBOOK) + ۵ توصیهٔ مقیاسِ کوچک
  - **یادداشتِ داده:** `rto-rpo-summary.csv`ِ ارجاع‌شده در پرامپت **در ریپو نیست** — اعداد از خودِ DR_RUNBOOK/RELIABILITY_DR_PLAN گرفته شدند
- **آماده‌سازی برای بستهٔ ۵ (Operational Readiness): انجام نشد — مالک چت ۱۰** (`OPERATIONAL_READINESS.md` وجود ندارد؛ چت ۸ فقط مالکِ بستهٔ ۱ — تفکیکِ صریح)
- **گیت‌ها (اجرای تازه روی درختِ نهایی، پس از حذفِ موقت‌ها):** freeze **13/14 — RED پیش‌موجودِ main** (PR_MERGE_PLAN.md از موجِ #209 — در mainِ خالص هم همان قرمز، VERIFY شده؛ bump/مانیفست = قلمروِ چت ۶) · stats **0** · metadata یتیم **هیچ** · refs **تازه=41 (همه پیش‌موجود — ۰ از محتوای این دور)** · mirror **0/0** (فقط چک) · secret-scan **12/12**
- **پوش:** [SHA] == ls-remote ✅ (یک کامیتِ addِ صریح) · **اسکنِ توکن: 0**
- **بودجهٔ §۱:** [پایان: در گزارشِ نشست] 🟢 (اندازهٔ «قبل» ≈ اندازهٔ پایانِ دورِ قبل همین محیط + objectهای fetch؛ دلتای این دور = یک بخشِ گزارش)
- **idle رسمی** تا: اعلامِ تصمیمِ الگوی A / حکمِ ناظر / بریفِ جدید — بعدیِ مجاز: پیاده‌سازیِ staging چندنودی **فقط پس از** تصمیم

---

## چت ۵ — دور ۲: پایش F1–F3 (رفع‌شده در main) + بستهٔ آماده‌سازی پاسخ به چت ۱۰ (ثبت: 2026-09-14)

### فاز ۰ — وضعیت و ضدتکرار
- ‏main در لحظهٔ اجرا = ‏`c134f45` (‏`git ls-remote origin main`؛ fetch با `+refs/heads/main` اجباری شد چون خطِ پایهٔ محلی کهنه بود). شاخهٔ این دور: ‏`docs/chat5-round2-f1f3-monitoring` از همان main.
- **‏PR #185 مرج شد** ‏(`merge_commit=686822566ca5`، ‏head=`703f93a`، ‏۳ کامیت) و هر ۷ فایلِ drill روی main است ‏(`tests/chaos-drill-*.js`).
- **‏F1/F2/F3 رفع شده‌اند** — کامیتِ ‏`d5a2548` «fix(resilience): رفع سه یافتهٔ chaos-drill» روی main ‏(مرج ‏`930b856`). زنجیرهٔ شاهد در فاز ۱؛ این رفع توسّط مالک/ناظر انجام شده، نه چت ۵.
- **‏trigger چت ۱۰: ‏NOT-RECEIVED** — ‏۰ issue باز در مخزن، ‏۰ issue با برچسب ‏`chat10`، و صفر کامنتِ خطاب‌به چت ۵. سندِ بستهٔ ۵ ‏(`docs/OPERATIONAL_READINESS.md`) از قبل موجود و مالکش چت ۱۰ است؛ پاسخِ §۶ آن را چت ۳ در کامنتِ #184 داده. ⇒ طبق پرامپت: **فاز ۰.۵ اجرا نمی‌شود** (پیش‌شرطش «دریافت سؤال» است).
- ضدتکرار: باتری drill و سوئیت‌های همگام‌سازی **بازاجرا نشدند**؛ اعدادِ جدولِ ۱۰×۹ دور ۱ بازتولید نشد؛ گیت‌ها فقط یک‌بار و در فاز ۲ اجرا شدند.

### فاز ۱ — پایش F1–F3 با زنجیرهٔ شاهدِ کد (بدونِ بازاجرای باتری)
| یافته | وضعیت | شاهدِ کد روی main | مکانیزمِ رفع |
|---|---|---|---|
| ‏F1 — گم‌شدنِ دادهٔ ackشده در قطعِ PG | **RESOLVED-ON-MAIN** | `server/db.js:595 pgExpected()` · `ping()` ⇒ `{driver:'none', alive:false}` · `persistOpsBatch` ⇒ `throw` (‏:807) · `server/sync.js:1160 mirrorFailed` + آزادسازیِ claimِ `__processed_uids` | در production با `DATABASE_URL`، درایور memory دیگر «سالم» جا زده نمی‌شود ⇒ readiness در **کلِ** پنجرهٔ قطع ‏۵۰۳ می‌ماند، نوشت به‌جای ackِ بی‌اثر **throw** می‌کند، و claimهای دستهٔ ناموفق آزاد می‌شوند تا retryِ کلاینت به ‏`duplicate_ignored`ِ کور نخورد. |
| ‏F2 — ‏۵۰۰ در قطعِ Redis | **RESOLVED-ON-MAIN** | `server/routes/bootstrap.js:44` (خوانشِ کش) و ‏`:161` (نوشتِ کش) هر دو ‏`try/catch` · `server/sync.js` لایهٔ کشِ idempotency با شکست فقط ‏audit می‌شود | شکستِ کش = **cache-miss**، نه ‏۵۰۰؛ dedupe به دو لایهٔ authoritative بعدی (db/store) می‌افتد. |
| ‏F3 — نبودِ بازپخشِ صف پس از کرش | **RESOLVED-ON-MAIN** | `server/outbox.js:154 replayPendingFromPg()` · فراخوانی در بوت: `server/index.js:551` | در بوت، `pending`های جدولِ `server_outbox` به صف برمی‌گردند و worker همان چرخهٔ همیشگی را می‌رود (best-effort؛ بوت را نمی‌شکند). |
| ‏F4 — از دست رفتنِ بی‌صدای آدیت زیرِ فشارِ دیسک | **NOT-RESOLVED (اطلاعاتی)** | `grep statfs/disk` در `server/index.js` و `server/metrics.js` ⇒ **صفر** | رفعش افزودنِ گِیجِ «فضای دیسک/شکستِ نوشتِ آدیت» است ⇒ **تغییرِ متریک** = دامنهٔ چت ۴ و نیازمندِ بریف. تا آن زمان: در حادثهٔ دیسک، آدیتِ همان پنجره را شاهد نگیرید. |

- ادعای سبزیِ پس از رفع، از **پیامِ مرجِ خودِ مالک** نقل می‌شود (باتری ۶/۶ ‏PASS — ‏۹۰/۹۰) و آن یک تغییرِ آزمون هم داشت: `tests/chaos-drill-disk-pressure.js` شاهدِ ENOSPC را به «کلاینت **یا** لاگِ موتورِ PG» گسترش داد (چون با PANICِ backend، stderrِ کلاینت فقط «server closed the connection» است). این گسترش، سخت‌گیری را کم نمی‌کند (متنِ ENOSPC از یکی از دو منبعِ واقعی الزامی است).
- **بازاجرای چت ۵ = ‏NOT-RUN (ضدتکرار)**؛ هیچ عددِ تازه‌ای از drill تولید نشد.
- اثرِ رفع بر جدولِ RTO/RPO دور ۱ (اصلاحِ موردی، بدونِ بازاندازه‌گیری): ردیفِ ‏S3 ⇒ ‏RPO از «۳ نوشتِ ackشدهٔ گم‌شده» به **صفر** و پنجرهٔ ‏fail-closed از ‏۱۰٬۱۱۲ms به «کلِ مدتِ قطع». سایر ردیف‌ها بی‌تغییر.

### بستهٔ آماده‌سازی پاسخ به چت ۱۰ (بستهٔ ۵ — مالکیت: چت ۱۰)
#### ۱) سؤالات محتمل و پاسخ آماده (هر پاسخ با منبعِ درون‌مخزن)
| سؤال محتمل | پاسخ آمادهٔ چت ۵ | منبعِ اجراپذیر |
|---|---|---|
| ‏RTO/RPO واقعیِ سامانه چقدر است؟ | تک‌نود/سندباکس: ‏RTO سرویس ‏۱۴۴–۳۷۶ms · ‏RTO وابستگی ‏۹٫۵s ‏(PG) و ‏۲۸٫۹s ‏(دیسک با crash-recovery) · ‏RPO صفر در ‏S1/S2/S5/S6 و در ‏S7–S10؛ ‏S3 پیش از رفع ‏۳ نوشتِ گم‌شده داشت که پس از ‏`d5a2548` صفر می‌شود. **برچسب: تک‌نود-سندباکس، نه استیجینگ.** | `docs/daily-reports/2026-09-13.md` §چت ۵ · `node tests/chaos-drill-*.js` (نیازمندِ PG+Redis واقعی) |
| کدام شکست‌ها می‌توانند ‏S2 ‏(موفقیت sync ‏≥۹۹٫۵٪) را بشکنند؟ | ‏F1 (گم‌شدنِ ack) — **رفع شد**؛ ‏F2 (‏۵۰۰ در قطعِ Redis) — **رفع شد**. باقی‌مانده: ‏disk-ENOSPC ⇒ ‏۵۰۳ ایمن (بی‌ackِ جعلی) و ‏F4 (آدیتِ بی‌صدا — اثرِ SLO ندارد). | `server/db.js` · `server/sync.js` · `server/routes/bootstrap.js` |
| آیا ‏ack ‏= دوام است؟ | پس از رفع ‏F1 بله: در ‏production با ‏`DATABASE_URL`، هر ‏ack پس از آینهٔ PG است و در حالتِ memory مسیر ‏throw می‌کند. شاهدی که باید در استیجینگ تکرار شود: «هر ‏ack در PG هست» (چکِ ‏`acked == pg_rows`). | `tests/chaos-drill-pg-outage.js` (چک‌های ‏`integrity`) |
| سنجهٔ عمقِ صف برای ‏on-call کافی است؟ | سه سیگنالِ موجود: ‏`payesh_sync_queue_depth` (‏pendingِ PG) · ‏`payesh_outbox_depth{status}` (صفِ درونِ پروسه) · ‏`/api/health → queue`. **شکاف:** گِیجِ صفِ سروریِ per-instance و نرخِ بازپخش وجود ندارد ⇒ گِیجِ تازه = چت ۴ + بریف. | `server/metrics.js` · `server/outbox.js` · `server/index.js` |
| اثرِ قطعیِ گذرا بر بودجهٔ خطا؟ | مثالِ محاسبه‌شده: بودجهٔ ماهانهٔ ‏S1 ‏(۹۹٫۹۵٪) ‏≈ ۲۱٫۶ دقیقه؛ یک پنجرهٔ ‏fail-closedِ ‏۱۰٫۱s ‏≈ ‏۰٫۷۸٪ بودجه. پیش از رفعِ ‏F1 همان پنجره **داده** هم می‌خورد (‏RPO≠۰) که با هیچ بودجه‌ای جبران نمی‌شود. | `docs/OPERATIONAL_READINESS.md` §۱/§۲ |
| کدام سناریوها آمادهٔ اجرا روی استیجینگ‌اند؟ | هر ۶ سناریوی چاوس پارامتریک و تک‌فرمان‌اند؛ ‏S1/S3/S4/S6 فقط به ‏PG/Redis/process نیاز دارند؛ ‏S5 به ‏netem یا پروکسی نیاز دارد (در سندباکس: پروکسیِ ‏TCPِ خودِ drill). | `tests/chaos-drill-lib.js` (`startProxy`, `fillDiskToFreeKb`) |
| چه چیزی از سندباکس **قابلِ‌تعمیم نیست**؟ | اعدادِ تأخیر/‏RTO و آستانه‌های ‏ENOSPC/تایم‌اوت‌ها — همه تابعِ تک‌نود و منابعِ سندباکس؛ فقط **الگوها و چک‌های یکپارچگی** قابلِ‌تعمیم‌اند. | همین سند، فاز ۱ و ۴ |

#### ۲) معیارهای پذیرش (SLOهای مصوب — نقلِ عین، بدونِ اختراعِ عدد)
| ‏# | زیرسیستم | هدف | منبع |
|---|---|---|---|
| ‏S1 | دسترس‌پذیری ‏API | ‏≥ ۹۹٫۹۵٪ ‏(≈ ۲۱٫۶ دقیقه/ماه) | ‏`docs/OPERATIONAL_READINESS.md` §۱ ← ‏`RELIABILITY_SLO` |
| ‏S2 | موفقیت sync بدونِ دادهٔ گم‌شده | ‏≥ ۹۹٫۵٪ | ‏`docs/CAPACITY_MODEL.md` §۵ ‏(‏≥۹۹٫۵٪ بدونِ دادهٔ گم‌شده) |
| ‏S3 | دسترس‌پذیری ‏OTP در ساعات مدرسه | ‏≥ ۹۹٫۹٪ | ‏`docs/OPERATIONAL_READINESS.md` §۱ |
| ‏S4/‏S5 | تأخیر ‏p95 / ‏p99 | ‏< ۳۰۰ms / ‏< ۱s | ‏`docs/CAPACITY_MODEL.md` §۵ · §۴ |
| ‏S6/‏S7 | ‏RTO / ‏RPO فاجعه | ‏≤ ۱۵ دقیقه / ‏≤ ۵ دقیقه | ‏`docs/DR_RUNBOOK.md` §۰ (نقل‌شده در §۱ سند آمادگی) |
| ‏S8 | تخلیهٔ صفِ ۱۰۰۰ پیام | ‏< ۵ دقیقه — **پیشنهاد، نیازمند ‏ACCEPTِ ناظر** | ‏`docs/OPERATIONAL_READINESS.md` §۱ |
- نگاشتِ صریحِ سنجه‌های چاوس به این‌ها: ‏S1→‏S1 ‏(fail-closed پنجره‌ها) · ‏S3/S4→‏S2 ‏(صفر ‏ackِ گم‌شده) · ‏S6→‏S6 ‏(RTOِ سنجیده ≪ هدف، با برچسبِ مقیاس) · ‏S5→‏S2 ‏(idempotency) · ‏S8 همچنان **بدونِ داده** ‏(NOT-RUN: بارِ ۱۰۰۰پیامی سنجیده نشده).

#### ۳) توصیه‌های ایمن برای کالیبراسیون استیجینگ
- **هیچ‌کدام از اعدادِ سندباکس را وارد ‏SLO نکنید**؛ فقط چک‌های یکپارچگی (‏acked==pg_rows، صفر تکراری، صفر فانتوم) قابلِ‌تعمیم‌اند.
- پیش‌نیازِ محیطی برای اجرای معنادار: ‏۲+ نودِ ‏APIِ مجزا · ‏PG واقعی با دسترسیِ ‏psql · ‏Redis واقعی · امکانِ ‏netem یا پروکسی برای سناریوی شبکه · فضای دیسکِ قابلِ‌محدودسازی برای سناریوی ‏ENOSPC.
- نُک‌های محیطیِ drill که **عمداً** منحرف‌اند و باید آگاهانه انتخاب شوند: `PAYESH_WORKER_INTERVAL_MS=1500` ‏(برای پرکردنِ بافر) و ‏`PAYESH_TEST_SLOW_MS` — پیش‌فرض‌ها ‏۱۰۰۰ms و خالی است.
- برای ‏S8 ‏(‏<۵ دقیقه/۱۰۰۰ پیام) لازم است سنجش با بارِ واقعی و ‏per-instance انجام شود؛ drill فعلی فقط تا ‏۸ رویداد در بافر سنجیده است ⇒ **NOT-RUN**.
- آستانهٔ قطعی که **نباید** از سندباکس کپی شود: پنجرهٔ ~۱۰s ‏(reconnect) — با رفعِ ‏F1 این پنجره دیگر پنجرهٔ «داده» نیست، فقط پنجرهٔ «دسترس‌پذیری» است.

### آماده‌سازی بستهٔ ۵ (Operational Readiness) — فقط پیشنهادِ تحویل؛ مالکیت با چت ۱۰
- **موجود (مصوب/در مخزن):** ‏`docs/OPERATIONAL_READINESS.md` ‏(SLO ‏S1–S8 + سیاست بودجهٔ خطا) · ‏`docs/CAPACITY_MODEL.md` §۵ ‏(SLOها) · ‏`docs/INCIDENT_PLAYBOOK.md` · ‏`docs/DR_RUNBOOK.md` §۰ ‏(RTO/RPO).
- **شکافِ مشاهده‌شده (فقط ثبت، بدونِ ساخت):** سندِ تجمیعیِ مستقلِ ‏SLO (به‌نامِ ‏Reliability-SLO) در مخزن نیست (اهداف در دو سند پراکنده‌اند)؛ ‏S8 بدونِ ‏ACCEPT است؛ گِیجِ ‏per-instanceِ صف و گِیجِ دیسک/آدیت وجود ندارد؛ مرجعِ ‏`SYNC_RELIABILITY_EVIDENCE.md` در §منابع سند آمادگی کهنه است (خودِ فایل الان در مخزن است — نکتهٔ ارجاعیِ چت ۳ روی #184).
- **نگاشتِ سناریو → سیگنال → اقدام (کاندیدِ runbook، از drillهای موجود):**
  - قطعِ ‏PG ⇒ سیگنال: ‏readiness 503 با ‏`db.driver∈{postgres,none}` و ‏`alive=false` ⇒ اقدام: تأیید ‏fail-closed، پس از بازگشت صحتِ «صفر ‏ackِ گم‌شده» را با بازپخشِ ‏`server_outbox` بسنجید.
  - قطعِ ‏Redis ⇒ سیگنال: ‏readiness 503 در ‏production (‏`redis.alive=false`) ولی ‏sync/bootstrap پایدار (کش‌فالبک) ⇒ اقدام: بازگردانیِ Redis؛ انتظارِ ‏cache-miss نه خطا.
  - کشتنِ ‏API ⇒ سیگنال: ‏`liveness=200` با ‏`pid`ِ تازه + خطِ ‏supervisor ‏`rc=137` ⇒ اقدام: تأییدِ خودراه‌اندازی و افتادنِ درخواست‌های در جریان (رفتارِ موردانتظار).
  - کرشِ پروسه با صفِ پر ⇒ سیگنال: ‏`payesh_sync_queue_depth` ‏>۰ پایدار ⇒ اقدام: بازپخشِ بوت ‏(`replayPendingFromPg`) و تأییدِ ‏processed شدنِ همه.
  - تخریبِ شبکه ⇒ سیگنال: ‏timeout سمتِ کلاینت + نرخِ ‏`sync_duplicate_ignored` ⇒ اقدام: تأییدِ اثرِ «دقیقاً یک‌بار» با ‏uidِ ثابت.
  - فشارِ دیسک ⇒ سیگنال: ‏`PANIC: … No space left on device` در لاگِ ‏PG + ‏readiness 503 ⇒ اقدام: آزادسازیِ فضا و انتظارِ crash-recovery؛ **آدیتِ این پنجره شاهد نیست (F4)**.
- **توصیهٔ هماهنگی:** تحویلِ چت ۵→۱۰ در سه قلم: ۱) همین بستهٔ پاسخ + جدولِ ‏SLO ۲) فایل‌های drill به‌عنوان «ابزارِ اثباتِ دوره‌ای ‏S2» ‏(قابلِ اجرا در ‏CI/استیجینگ) ۳) فهرستِ شکاف‌های متریکی که بریفِ چت ۴ می‌خواهد.

### ‏ENGINEERING CLAIM (۱۸ فیلد)
- ‏EXPECTED: پایشِ وضعیت ‏F1–F3 و آماده‌سازیِ بستهٔ پاسخِ چت ۱۰ + پیش‌نیازهای بستهٔ ۵ — با **صفر کارِ تازهٔ کد/تست** و صفر فایلِ تازه در ‏docs/.
- ‏MEASURED: ۴ یافته پایش شد (‏F1/F2/F3 = ‏RESOLVED-ON-MAIN با شاهدِ کد و شمارهٔ خط؛ ‏F4 = ‏NOT-RESOLVED) · ۷ سؤالِ محتملِ چت ۱۰ با پاسخ و منبع · ۸ ‏SLOِ مصوب نقل‌شده + نگاشت · ۶ سناریوی drill به‌عنوان کاندیدِ runbook · ۳ قلم تحویل.
- ‏TEST ENV: ‏n/a — دورِ مستنداتی؛ **هیچ drill بازاجرا نشد** ‏(ضدتکرار) و هیچ ‏PG/Redisای بالا نیامد.
- ‏DATASET SIZE: ‏n/a · ‏CONCURRENCY: ‏n/a · ‏P50/P95/P99: ‏n/a · ‏ERROR RATE: ‏n/a.
- ‏DB IMPACT: صفر (بدونِ ‏DDL/DML) · ‏CACHE IMPACT: صفر · ‏MIGRATION: ‏n/a · ‏EXPLAIN: ‏n/a · ‏BENCHMARK: ‏n/a.
- ‏SECURITY IMPACT: ‏۰ توکن در فایل/کامیت/گزارش؛ ‏`secret-scan` ‏۱۲/۱۲ ✅؛ هیچ مسیرِ دسترسی‌ای تغییر نکرد.
- ‏FAILURE TEST / RECOVERY RESULT: ‏n/a ‏(فقط مستندسازی) — سناریوهای اجراشده در دور ۱ ارجاع داده شده‌اند، بازاجرا نشدند.
- ‏REGRESSION RISK: صفر — فقط این گزارش تغییر کرد (‏۱ فایل، ‏docs-only).
- ‏KNOWN UNKNOWNs: ‏CI ‏NOT-RUN ‏(بیلینگ؛ RISK-O-007) · ‏F4 رفع‌نشده و گِیجِ دیسک/آدیت بی‌بریف · کالیبراسیونِ استیجینگ ‏NOT-RUN ‏(محیط چندنودی نداریم) · ‏S8 بدونِ ‏ACCEPT · ظرفیتِ ملی اثبات‌نشده (دائمی).
- ‏FINAL VERDICT: **VERIFIED** ‏(پایشِ ‏F1–F3 با شاهدِ کد + بستهٔ آمادهٔ پاسخ) + **NOT-RUN** ‏(سؤالِ چت ۱۰ دریافت نشد؛ تصمیمِ مستقلِ ناظر برای ‏F4 اعلام نشده؛ استیجینگ باز نیست).

### گیت ۸ سؤال
۱) **چه چیزی تغییر کرد؟** فقط `docs/daily-reports/2026-09-14.md` (الحاقِ همین بخش) — صفر فایلِ تازه در `docs/`، صفر تغییرِ کد/تست. ۲) **چرا؟** پایشِ وضعیتِ F1–F3 و آماده‌سازیِ پاسخِ چت ۱۰، طبقِ پرامپتِ دور ۲ و کنوانسیونِ جاافتادهٔ «بستهٔ آماده‌سازی داخلِ گزارشِ روزانه» (چت ۳، دور ۱۵) به‌جای فایل‌های موقتِ حذف‌شونده. ۳) **کدام ‏invariant؟** چت ۵ idle است: صفر کارِ کد/تست، صفر بازاجرای دور ۱، صفر ادعای سبزِ نوبتی. ۴) **چگونه درست‌بودن ثابت می‌شود؟** هر ادعا به شاهدِ درون‌مخزن گره خورده (کامیت · شمارهٔ خط · سندِ مصوب) و هر عددِ سندباکس برچسبِ مقیاس دارد. ۵) **سریع‌تر/مقیاس‌پذیرتر؟** ‏n/a (مستندسازی). ۶) **امنیت؟** ‏۰ توکن + ‏secret-scan ‏۱۲/۱۲. ۷) **تستِ شکست؟** ‏n/a؛ اما تنشِ گیت‌ها ثبت شد (زیر). ۸) **rollback؟** `git revert` یک کامیتِ مستنداتی؛ صفر اثرِ محصول.

### گیت‌ها/بودجه/پوش (اجرای همین دور، روی درختِ مبنا `c134f45` و پس از تغییر)
- ‏`docs-freeze-marker` = **۱۴/۱۴ ✅** ‏(قفل ‏`rc43`؛ سندِ یخ‌زده لمس نشد) · ‏`docs-stats-sync --check` = **✅** ‏(پس از مرجِ #185 آمار بازتولید شده بود) · ‏`docs-metadata` = **۱۷/۱۷ ✅** · ‏`secret-scan` = **۱۲/۱۲ ✅**.
- ‏`tools/docs-refs-check.js` = exit **۰** با ‏۴۱ ارجاعِ کهنهٔ تازه؛ ‏`tests/docs-refs-check.js` ‏(متا) = **قرمز** — **پیش‌موجودِ main** (شاهد: همان اجرا روی درختِ دست‌نخوردهٔ `c134f45`، پیش از تغییرِ من؛ نمونه: اسنادِ CHAT4_* به تستِ pgbouncer-pooling (در پوشهٔ tests) ارجاع می‌دهند که فایلش وجود ندارد).
- ‏`reza-mirror-check` = **exit ۰** ‏(۲۱ آینهٔ زنده برابر ✅) — **۰ آینهٔ تازه / ۰ کپیِ دستی** (فقط چک، طبق حکم).
- فایل‌های موقتِ نام‌برده در پرامپت (`F1_F3_MONITORING.md` · `CHAT10_PREP.md` · `OPERATIONAL_PREP.md`) **ساخته نشدند** — طبق کنوانسیونِ جاافتادهٔ مخزن محتوایشان مستقیم در همین گزارش آمد (چت ۳، دور ۱۵) ⇒ **صفر فایلِ موقت برای حذف** و صفر افزودنِ سندِ تازه به `docs/`.
- بودجه: ‏`/home/user` ‏۱۰۵MB ‏→ همان‌حد (تنها فایلِ گزارش تغییر کرد) · ورکتریِ این دور ‏۳۵MB/۱٬۴۴۳ فایل ‏(بدونِ ‏`node_modules`/‏`.git`) · ‏token-hits: **۰** · یک کامیت + یک پوش.

### اعلام idle رسمی (چت ۵ — دور ۲)
چت ۵ idle است تا یکی از این‌ها برسد: **سؤالِ چت ۱۰** ‏(هر issue/کامنت با برچسب یا خطابِ صریح) · **بریفِ مالک برای ‏F4** ‏(گِیجِ دیسک/آدیت — تغییرِ متریک ⇒ نیازمندِ هماهنگی با چت ۴) · **بازشدنِ استیجینگِ چندنودی** ‏(برای کالیبراسیون و ‏S8) · یا **حکمِ ناظر**. تا آن زمان: صفر کارِ کد/تست، صفر ادعای تازه، صفر فایلِ موقت.

---

## چت ۵ — دور ۳: پایش سؤالات چت ۱۰ + آماده‌سازی برای استیجینگ (ثبت: 2026-09-14)

### فاز ۰ — وضعیت و ضدتکرار
- ‏main در لحظهٔ اجرا = ‏`5a175c0` ‏(`git ls-remote origin main`؛ fetch با `+refs/heads/main`). **‏PR #212 (دور ۲ چت ۵) مرج شد** — ‏`68e9b1d` روی main. حرکتِ main از دور ۲: ‏`70a1d0f` ‏(مرج #209 چت ۷) + ‏`5a175c0` ‏(مرج #212).
- شاخهٔ این دور: ‏`docs/chat5-round3-staging-monitoring` از همان main؛ ورکتریِ کهنهٔ دور ۲ ‏(‏`prunable` پس از ریستِ ‏`/tmp`) با ‏`git worktree prune` پاک شد (شاخهٔ مرج‌شده روی origin سالم).
- **‏trigger چت ۱۰ = ‏NOT-RECEIVED** ⇒ طبق پرامپت، **فاز ۰.۵ اجرا نشد**. شاهد: ‏۰ issue بازِ غیر-‏PR · ‏۰ issue با برچسب ‏`chat10` · صفر کامنتِ تازه روی ‏#184 ‏(آخرین کامنت همچنان پاسخِ چت ۳ در ‏2026-09-14T05:51 است).
- ضدتکرار: باتری drill و سوئیت‌های آفلاین و دور ۲ **بازاجرا نشدند**؛ گیت‌های ‏docs فقط **یک‌بار** و در مقامِ «گیتِ مسیرِ تغییر» اجرا شدند (نه در فاز ۰).

### فاز ۱/۱ — پایش وضعیت (سه trigger)
| ‏trigger | وضعیت | شاهدِ همین دور |
|---|---|---|
| سؤالِ چت ۱۰ | **NOT-RECEIVED** | ‏API: `issues?state=open` ⇒ ۰ غیر-‏PR؛ `labels=chat10` ⇒ ۰؛ کامنت‌های ‏#184 ⇒ ۳ (همان‌های پیشین) |
| محیطِ استیجینگ چندنودی | **NOT-RUN** | `docs/CAPACITY_WORKLOAD_MODEL.md` §۶: «نقشهٔ اجرا روی staging ‏(NOT-RUN — پیش‌نیاز: بسته ۱)» · ‏`docs/STAGING*.md` وجود ندارد · ادعای پرامپت دربارهٔ «بررسی #141»: **‏PR #141 چت ۸ مرج شده است** (‏`merged=true`) و خودش محیط نمی‌سازد |
| آشوبِ چند-نمونه | **NOT-RUN** | `docs/WAVE19_CHAOS_PLAN.md` §۵: «اجرایِ واقعیِ آشوب نیازمندِ **محیطِ زندهٔ چند-نمونه** است» — drillهای چت ۵ تک‌نمونه/سندباکس بودند |
- **ادعای کهنهٔ خطِ ۳۱ سندِ آمادگی — هنوز روی main:** `OPERATIONAL_READINESS.md` می‌گوید «فایل `SYNC_RELIABILITY_EVIDENCE.md` در ریپو **وجود ندارد**»؛ راستی‌آزماییِ همین دور: فایل **موجود است**. مالک سند = چت ۱۰ ‏(‏§۱: «مالک: چت ۱۰») و چت ۳ هم در کامنتِ #184 همین را تذکر داده ⇒ چت ۵ **دست نبرد**؛ فقط برای پیگیریِ مالک ثبت می‌شود. ‏(‏SLO ‏S2 در سند به شواهد ‏`tests/sync-conflict-storm-drill.js` ‏(18/18) و ‏`tests/offline-e2e.js` ‏(23/23) ارجاع دارد — ارجاع‌های معتبر.)

### فاز ۱/۲ — آماده‌سازی استیجینگ (همه‌چیز راستی‌آزمایی‌شده، بدونِ اختراعِ عدد)
#### الف) معیارهای پذیرش — ۷ عددِ پرامپت **همه در سند هستند** ‏(‏`docs/CAPACITY_MODEL.md` §۵)
| شاخص | هدف (نقلِ عین) | ابزارِ سنجش به‌گفتهٔ سند | وضعیتِ راستی‌آزمایی در کد |
|---|---|---|---|
| تأخیر — میانه | ‏p50 < ۱۰۰ms | هیستوگرام ‏`payesh_http_request_duration` | ⚠ **نامِ واقعی: `payesh_http_request_duration_seconds`** ‏(`server/metrics.js:435`) |
| تأخیر — دم | ‏p95 < ۳۰۰ms | همان + آلارم ‏`HighLatency` | ✅ عدد · ⚠ نامِ متریک |
| تأخیر — دمِ دور | ‏p99 < ۱s | همان هیستوگرام | ✅ عدد · ⚠ نامِ متریک |
| خطای سرور | ‏5xx < ۰.۱٪ ‏(≈۴۳ دقیقه/ماه) | شمارنده‌های وضعیت + ‏`HighErrorRate` | ✅ عدد |
| نرخ اصابت کش | **> ۸۰٪** | ‏`payesh_cache_hit_rate` | ⚠ **چنین متریکی نیست**؛ مشتق از ‏`payesh_cache_hits_total` ÷ ‏`payesh_cache_lookups_total` |
| موفقیت sync | ‏≥ ۹۹.۵٪ ‏(بدونِ دادهٔ گم‌شده) | صف همگام‌سازی + شمارندهٔ تعارض | ✅ |
| دسترس‌پذیری ‏OTP | ‏≥ ۹۹.۹٪ در ساعات مدرسه | نرخ تأیید ÷ صدور | ✅ |
- **دو تصحیحِ ابزاریِ لازم پیش از کالیبراسیون:** آلارم/کوئری نباید روی `payesh_http_request_duration` و `payesh_cache_hit_rate` بسته شود (وجود ندارند) — نامِ درست بالا. ارجاعِ ‏`reload-safe`: هیچ ادعای عددی تغییر نکرد؛ فقط نامِ سازِ سنجش.
- **قانونِ ثبتِ سند:** «هر اجرای بارِ واقعی ‏(Wave 18) باید این پنج عدد را در ‏`docs/LOAD_TEST_RESULTS.md` ثبت کند؛ هر انحراف = یک آیتمِ باز در ‏`docs/OPEN_ITEMS.md`» — هر دو فایل موجودند ‏(‏۳۰۵ و ۹۸ خط).

#### ب) نقشهٔ اجرا و ابزار (‏`docs/CAPACITY_WORKLOAD_MODEL.md` §۶ + ابزارِ واقعیِ مخزن)
- **پیش‌نیازِ محیط (نقلِ سند):** staging چندنودی ‏(بستهٔ ۱) + دیتاستِ ‏`scale=1` بارگذاری‌شده ‏(الگوی ‏`tools/w18-load-pg.sh`)؛ ترتیب: هر حالتِ پایدار جدا، سپس ‏`mixed-national` با نردبان تا عبور از زانو؛ ثبتِ هر اجرا در قالبِ ‏`docs/CAPACITY_ENVELOPE_TEMPLATE.md` ‏(۸۸ خط، موجود).
- **ابزار:** ‏`tools/capacity-saturation-probe.js` ‏(۲۱KB) — **آفلاین سنجیده شد:** ‏`node tools/capacity-saturation-probe.js --validate` ‏⇒ ‏`rc=0` با چاپِ طرح: نردبانِ پیش‌فرض ‏`50→100→200→400→800→1600` · هر پله ‏۱۲۰s ‏(+گرم‌کردن ‏۱۵s) · ‏SLO پذیرش ‏`p95 < 300ms` · **توقفِ اضطراری ‏`5xx > 5٪`** ‏(`--abort-error-rate`، پیش‌فرض ۵) · نامزدهای زانو ‏`slo | plateau` ‏(بهرهٔ حاشیه‌ای ‏<۵۰٪) ‏`| errors` ‏(‏5xx>۱٪) · جمع‌آورها ‏HTTP + ‏`/metrics` ‏(+اختیاری ‏`redis-info`, ‏`pg-direct`).
- **گاردهای ایمنیِ خودِ ابزار:** بدونِ ‏`--confirm-staging` اجرا نمی‌کند (محافظِ production) · احراز با ‏`--token`/‏`PAYESH_PROBE_TOKEN` و توکنِ اسکرَپِ متریک با ‏`PAYESH_METRICS_TOKEN`.
- **تصحیحِ نسبت به پرامپت:** «نردبانِ سیگاری ‏(۵،۱۰،۲۰ × ۶۰s)» در مخزن وجود ندارد؛ نردبانِ رسمی = همان ‏`--staircase` ابزار و نردبانِ **مقیاس** ‏(‏`0.001 (دود) → 0.1 → 0.3 (نقطهٔ تکرارپذیرِ PR #94) → 1.0 (ملی)`). ابزارِ گاردِ دوم ‏(`tools/delta-load-test.js`) پرچمِ ‏`--max-error-rate` دارد (پیش‌فرض ‏۰) — این را با ‏`--abort-error-rate` اشتباه نگیرید (دو ابزار، دو پرچم).
- **واقعیتِ سندباکس در برابر نیازِ استیجینگ:** ‏RAM موجود ‏≈۲GB و ‏`nproc=2` است، در برابرِ نیازمندیِ ‏≥۸GB/نود در پرامپت ⇒ اجرای معنادارِ نردبان/‏Soak در **این** سندباکس ‏**NOT-RUN** است؛ فقط حالتِ ‏`--validate` آفلاین (تأییدشده) و drillهای تک‌نمونه قابلِ اجرا هستند.

#### ج) نگاشتِ جدولِ پذیرشِ آشوب ‏(`WAVE19_CHAOS_PLAN.md` §۴) به سنجه‌های واقعیِ چت ۵ — با دو ناسازگاری
| سناریوی طرح | پذیرشِ مکتوب | سنجهٔ چت ۵ | وضعیت |
|---|---|---|---|
| ‏kill-api | ack‌شده‌ها در ‏PG؛ نمونهٔ restart ‏readiness=200 | ‏S1: ‏۳/۳ درخواستِ در جریان افتاد، صفر ۲۰۰ِ جعلی، نوشتِ پس از restart در ‏PG | ✅ منطبق |
| ‏redis-down | readiness ‏503 ‏(≤۱ probe) · liveness ‏200 · **صفر ‏500** · صفر ‏data loss | ‏S2: ‏503 در ‏۳ms ✅ · liveness ‏200 ✅ · **‏۵۰۰×۲ ❌ (‏F2)** · صفر ‏data loss ✅ | ⚠ پس از رفعِ ‏F2 منطبق می‌شود (بازاندازه‌گیری ‏NOT-RUN) |
| ‏pg-down | «خوانش‌ها ‏200» · ‏sync ‏200+audit · صفر ‏data loss · ‏readiness ‏503 · بازگشت بدونِ restart | ‏S3 پس از رفعِ ‏F1: ‏fail-closed ⇒ خوانش‌ها **503** می‌شوند (نه ‏200) | ❌ **ناسازگاریِ قرارداد:** هدفِ «خوانشِ ‏۲۰۰ زیر قطعِ ‏PG» با طراحیِ fail-closed جمع نمی‌شود؛ یا معیار باید بازنویسی شود (‏503 برای خوانش نیز پذیرفته) یا حالتِ خواندنیِ تخریبیِ جدا لازم است ⇒ **نیازمندِ تصمیمِ مالک/استیجینگ** |
| ‏net-latency | ‏p95 < base + 1.5×delay · error < ۰.۱٪ · صفر ‏loss | ‏S5: تأخیرِ ۷۰۰ms ⇒ اندازه‌گیری‏شده ‏≈۷۱۰ms ‏(< ۹+۱۰۵۰ms) ✅ · ولی زیرحالتِ ‏abort/swallow عمداً **بیرونِ دامنهٔ طرح** بود (‏timeout ۱۰۰٪) | ✅ برای زیرحالتِ تأخیر؛ بقیه فراتر از دامنه (سنجشِ ‏idempotency) |
| ‏disk-full | صفر ‏crash/۵۰۰ در ‏health · صفر بکاپِ خراب · **آلارم‌های دیسک فعال** | ‏S6: ‏health ‏200 ✅ · صفر ‏۵۰۰ ‏(‏۵۰۳) ✅ · بکاپ آزموده **نشد** · آلارمِ دیسک: **قابلِ‌ارضای نیست** (‏F4: هیچ گِیجِ دیسک در ‏health/‏metrics نیست) | ⚠ دو قلم باز: بکاپ ‏NOT-RUN + آلارمِ دیسک نیازمندِ متریکِ چت ۴ |

### فاز ۱/۳ — آماده‌سازی بستهٔ ۵ ‏(Operational Readiness — **مالکیت: چت ۱۰**)
> ‏`docs/OPERATIONAL_READINESS.md` در مخزن **وجود دارد** و مالکش چت ۱۰ است؛ چت ۵ فقط پیش‌نیاز تحویل می‌دهد و سند نمی‌سازد. ‏(KNOWN-UNKNOWN دائمی چت ۵.)
- **موجودِ مصوب:** ‏SLO ‏S1–S8 + سیاستِ بودجهٔ خطا ‏(‏§۱/§۲ سند آمادگی) · ‏SLOهای عملکردی ‏(‏`CAPACITY_MODEL.md` §۵) · ‏`INCIDENT_PLAYBOOK.md` · ‏`DR_RUNBOOK.md` ‏(RTO/RPO).
- **شکاف‌های تأییدشدهٔ همین دور:** ۱) ادعای کهنهٔ خطِ ۳۱ ‏(SYNC_RELIABILITY_EVIDENCE) — پیگیریِ مالک · ۲) نام‌های ابزاریِ اشتباه در ‏§۵ ‏(بالا) · ۳) ناسازگاریِ قراردادِ ‏pg-down · ۴) نبودِ گِیجِ ‏per-instanceِ صف و گِیجِ دیسک/آدیت ‏(نیازمندِ بریفِ چت ۴) · ۵) ‏S8 بدونِ ‏ACCEPT.
- **آنچه چت ۵ تحویل می‌دهد (بیرونِ مخزن، در پوشهٔ شواهد):** جدولِ ۱۰×۹ ‏`rto-rpo-summary.csv` **به‌روزشده** به ۱۰ ستون با ستونِ «وضعیتِ پس‌از‌رفعِ main» + یادداشتِ `post-fix-notice.txt` ‏(برای جلوگیری از مصرفِ عددِ کهنه: ‏S3/S4/S2 اکنون رفع‌شده‌اند ولی **بازاندازه‌گیریِ چت ۵ ‏NOT-RUN** است).
- **۶ نگاشتِ «سناریو → سیگنال → اقدام»** برای runbook در بخشِ دور ۲ همین سند ثبت شده است (بازنویسی نمی‌شود؛ ارجاع: ‏«آماده‌سازی بستهٔ ۵» در بخشِ دور ۲).

### ‏ENGINEERING CLAIM (۱۸ فیلد)
- ‏EXPECTED: پایشِ triggerِ چت ۱۰ و آماده‌سازیِ استیجینگ/بستهٔ ۵ — با **صفر کارِ تازهٔ کد/تست**، صفر فایلِ تازه در ‏`docs/` و صفر بازاجرای دور ۱/۲.
- ‏MEASURED: ‏۳ ‏trigger پایش شد (‏chat10 = ‏NOT-RECEIVED · ‏staging = ‏NOT-RUN با ارجاعِ ‏§۶ · آشوبِ چند-نمونه = ‏NOT-RUN با ارجاعِ ‏WAVE19 §۵) · ‏۷/۷ عددِ ‏SLO در سند **تأیید شد** + **۲ تصحیحِ نامِ ابزار** · ابزارِ نردبان آفلاین سنجیده شد ‏(`--validate` ‏rc=0) · جدولِ پذیرشِ آشوب به سنجه‌های واقعی نگاشت شد با **۲ ناسازگاری** (‏pg-down/‏disk-full) · ‏CSVِ ‏RTO/RPO به ۱۰ ستون به‌روز شد + یادداشتِ ضدِ‏عددِکهنه.
- ‏TEST ENV: ‏n/a ‏(دورِ مستنداتی) — تنها اجرای این دور: ‏`capacity-saturation-probe --validate` ‏(آفلاین، بدونِ شبکه، ‏rc=0) و گیت‌های مسیر.
- ‏DATASET/CONCURRENCY/P50/P95/P99/ERROR RATE: ‏n/a · ‏DB/CACHE/MIGRATION/EXPLAIN/BENCHMARK: ‏n/a ‏(صفر ‏DDL/DML).
- ‏SECURITY IMPACT: ‏۰ توکن در فایل/کامیت/گزارش؛ ‏`secret-scan` ‏۱۲/۱۲ ✅؛ هیچ مسیرِ دسترسی تغییر نکرد.
- ‏FAILURE TEST / RECOVERY: ‏n/a ‏(بازاجرا نشد؛ اعدادِ دور ۱ ارجاع و اعدادِ پس‌از‌رفع «‏NOT-RUN-تا-بازاجرا» برچسب خوردند).
- ‏REGRESSION RISK: صفر — فقط یک فایلِ گزارشِ روزانه تغییر کرد.
- ‏KNOWN UNKNOWNs: ‏freeze-marker ‏REDِ وراثتی ‏(‏DF-HASH ‏PR_MERGE_PLAN؛ منتظرِ بامپِ قفل/مالک) · ‏CI ‏NOT-RUN ‏(بیلینگ؛ RISK-O-007) · سؤالِ چت ۱۰ ‏NOT-RECEIVED · استیجینگِ چندنودی ‏NOT-RUN ‏(پیش‌نیازِ بستهٔ ۱ + کمبودِ منابع در سندباکس) · ‏gaugeِ صفِ سروری ‏NOT-RUN ‏(نیازمندِ بریف؛ حکمِ ۵) · ‏multi-tenant storm ‏NOT-RUN ‏(تا حکمِ counselor/tenancy) · ناسازگاریِ قراردادِ ‏pg-down ‏(باز) · ‏F4 باز · ظرفیتِ ملی اثبات‌نشده (دائمی).
- ‏FINAL VERDICT: **VERIFIED** ‏(پایش + آماده‌سازیِ استیجینگ با شماره‌های تأییدشده و تصحیحِ نام‌های ابزاری) + **NOT-RUN** ‏(سؤالِ چت ۱۰ دریافت نشد · استیجینگ باز نیست · بازاندازه‌گیریِ پس‌از‌رفع انجام نشد).

### گیت ۸ سؤال
۱) **چه چیزی تغییر کرد؟** فقط `docs/daily-reports/2026-09-14.md` (الحاقِ همین بخش)؛ صفر فایلِ تازه در `docs/`، صفر تغییرِ کد/تست. ۲) **چرا؟** پایشِ trigger چت ۱۰ و آماده‌سازیِ استیجینگ طبق پرامپتِ دور ۳. ۳) **invariant:** چت ۵ idle است — صفر کارِ کد/تست، صفر بازاجرای دور ۱/۲. ۴) **درست‌بودن؟** هر ادعا به سند/کد/اجرا گره خورده (شمارهٔ خط · خروجیِ `--validate` · API). ۵) **سریع‌تر/مقیاس‌پذیرتر؟** ‏n/a ‏(مستندسازی). ۶) **امنیت؟** ‏۰ توکن + ‏secret-scan ‏۱۲/۱۲. ۷) **تستِ شکست؟** ‏n/a؛ اما دو **ناسازگاریِ قرارداد** (بالا) به‌جای سبزِ نمایشی ثبت شد. ۸) **rollback؟** `git revert` یک کامیتِ مستنداتی.

### گیت‌ها/بودجه/پوش
- ‏`docs-freeze-marker` = **۱۳/۱۴ — ‏RED** ‏(‏DF-HASH روی `PR_MERGE_PLAN.md`) — **وراثتی از main، سهمِ چت ۵ صفر.** شاهدِ همین دور: ۱) روی درختِ **دست‌نخوردهٔ** `5a175c0` ‏(`git stash -u` ⇒ `dirty=0`) همان ‏۱۳/۱۴ تکرار شد؛ ۲) اثرِ فهرستِ `rc43` = `sha256:b3b44a4f…` و اثرِ دیسک = `sha256:9ef9c211…`؛ ۳) آخرین تغییرِ فایل با کامیتِ `3f1c874` (چت ۷، ۲۰۲۶-۰۹-۱۴) است که **خودِ پیامش همین را ثبت کرده**: «شاهدِ دومِ L3 … ‏freeze-marker 13/14 با ‏DF-HASH روی ‏PR_MERGE_PLAN.md ‏(منجمد در ‏rc40)». ⇒ رفع = بامپِ قفل (مالک چت ۶) یا اصلاحِ مالکِ فایل؛ چت ۵ **لمس نکرد** (سندِ یخ‌زده و قفل، خارج از دامنه).
- ‏`docs-stats-sync --check` = **✅** · ‏`docs-metadata` = **۱۷/۱۷ ✅** · ‏`secret-scan` = **۱۲/۱۲ ✅** · ‏`tools/docs-refs-check.js` = **exit ۰** ‏(‏۴۱ ارجاعِ کهنهٔ تازه = خطِ پایهٔ main؛ سندِ این دور **صفر** ارجاعِ کهنهٔ تازه ساخت) — ‏`tests/docs-refs-check.js` ‏(متا) همان **قرمزِ پیش‌موجود** (اسنادِ ‏`CHAT4_*`؛ مالک چت ۴) و لمس نشد.
- ‏**دو گیتِ قرمزِ وراثتی، هر دو با شاهدِ تکرار روی درختِ دست‌نخورده** ⇒ ادعای «گیت‌ها سبز» در این دور **نمی‌شود**؛ آنچه سبز است: stats · metadata · secret-scan · mirror؛ و آنچه قرمز است پیش از تغییرِ چت ۵ قرمز بوده.
- ‏`reza-mirror-check` = **exit ۰** ‏(‏۲۱ آینهٔ زنده برابر) — ‏**۰ آینهٔ تازه / ۰ کپیِ دستی** (طبق حکم؛ فقط چک).
- **فایل‌های موقتِ نام‌برده در پرامپت ساخته نشدند** (`CHAT10_QUESTION_MONITORING.md` · `STAGING_PREP.md` · `OPERATIONAL_PREP.md`): فهرستِ ممنوع‌های **همین پرامپت** می‌گوید «فایل تازه در ‏docs/ ‏(مگر گزارش روزانه)» ⇒ خودِ پرامپت با خودش در تنش است و طبق کنوانسیونِ جاافتادهٔ مخزن (چت ۳، دورهای ۱۵/۱۶ روی همین الگو) محتوا مستقیم در گزارشِ روزانه آمد ⇒ **صفر فایلِ موقت برای حذف**.
- بودجه: ‏`/home/user` ‏۱۱۴MB ‏→ ‏۱۱۵MB ‏(‏۱ فایلِ گزارش؛ جهشِ ‏`.git` از fetchهای مکرر است، نه فایلِ کاری) · ورکتریِ این دور ‏~۳۵MB/‏~۱٬۴۵۰ فایل ‏(بدونِ ‏`node_modules`/‏`.git`) · ‏token-hits: **۰** · یک کامیت + یک پوش.

### اعلام idle رسمی (چت ۵ — دور ۳)
چت ۵ idle است تا یکی از این‌ها برسد: **سؤالِ چت ۱۰** (issue/کامنت) · **بازشدنِ استیجینگِ چندنودی** (بستهٔ ۱) · **بریفِ مالک** (گِیجِ صف/دیسک با چت ۴، یا تصمیمِ قراردادِ ‏pg-down) · یا **حکمِ ناظر**. تا آن زمان: صفر کارِ کد/تست، صفر ادعای تازه، صفر فایلِ موقت.
## چت ۸ — دور ۱۷: پایش وضعیت تصمیم الگوی A + بیست‌وششمین دور idle (ثبت: 2026-09-14)

- **وضعیت تصمیم الگوی A: اعلام نشده** ⇒ **فاز ۱** — اندازهٔ تازه (main=`aeb32a9`): DECISION_LOG.md هنوز وجود ندارد · NEXT_ACTIONS/HANDOFF/docs-HANDOFF: 0 hit · کامنت‌ها: 0 hit
- **یادداشتِ پرامپت (شفافیت):** تستِ فاز ۰.۵ِ این بریف معکوس بود (grep «Not Found» ⇒ اجرای زنجیره بدون ACCEPT!) — بر اساسِ **قصد** پرامپت (اعلام ⇒ ۰.۵، وگرنه ⇒ ۱) و حکمِ پابرجا (زنجیره فقط با ACCEPT) **فاز ۱** اجرا شد
- **مبنای اندازهٔ تازه:** main `70a1d0f → aeb32a9` (+#212/#213 چت ۵ دور ۲/۳) — merge در شاخهٔ خود با **اتحاد تلفیق‌ناپذیر** (چت ۸ دور ۱۶ + چت ۵ دور ۲/۳) · ۳ اثرِ ریستِ هفتم (origin/هویت/node22 + symlinkِ alert-rules + exec-bit + psql-min) ترمیم/بازگردانیِ مسیربه‌مسیر
- **آماده‌سازی برای بستهٔ ۱ (فایل‌های موقت — ساخته و با پایان دور حذف شدند):** ۸ پیش‌نیاز / ۷ راهکار ایمن / ۷ SLO + RTO/RPO مصوب — **بدونِ تکرارِ بستهٔ چت ۵** (تقسیمِ کار: چت ۸ = محیطِ چندنودی + دیتاسک + رانشِ workload؛ SLO/رونبوک = استناد به اسنادِ چت ۵/۱۰ روی main)
- **بستهٔ ۵:** `OPERATIONAL_READINESS.md` **حالا موجود است — مالک چت ۱۰** (چت ۸ دست نمی‌زند) — فایلِ OPERATIONAL_PREP ساخته نشد (قلمروِ چت ۱۰)
- **گیت‌ها (اجرای تازه روی درختِ نهایی، پس از حذفِ موقت‌ها):** freeze **13/14 — RED پیش‌موجود** (PR_MERGE_PLAN.md — همان دور ۱۶؛ bump = چت ۶) · stats **0** · metadata یتیم **هیچ** · refs **تازه=41 (همه پیش‌موجود؛ ۰ از این دور)** · secret-scan **12/12** · mirror **0/0** (فقط چک)
- **پوش:** [SHA] == ls-remote ✅ (شاملِ merge-اتحاد) · **اسکنِ توکن: 0**
- **بودجهٔ §۱:** [پایان: در گزارشِ نشست] 🟢
- **idle رسمی** تا: اعلامِ تصمیمِ الگوی A / حکمِ ناظر / بریفِ جدید

---

## چت ۵ — دور ۴: پایش سؤالات چت ۱۰ + آماده‌سازی بستهٔ ۵ (Operational Readiness) و ورودیِ بستهٔ ۴ (ثبت: 2026-09-14)

### فاز ۰ — وضعیت و ضدتکرار
- ‏main در لحظهٔ اجرا = ‏`895f8d4` ‏(`git ls-remote origin main`)؛ **‏PR #213 (دور ۳ چت ۵) مرج شد** ‏(‏`52f1d06`). حرکتِ main: ‏`cad0920`/‏`414f2e0` (دورهای ۱۶ و ۱۷ پایشِ چت ۸) + ‏`aeb32a9` ‏(مرج #213).
- **‏trigger چت ۱۰ = ‏NOT-RECEIVED** ⇒ فاز ۰.۵ اجرا نشد. شاهد: ‏۰ issue بازِ غیر-‏PR · ‏۰ issue با برچسب ‏`chat10` · کامنت‌های ‏#184 بدونِ تغییر ‏(آخرین: ‏2026-09-14T05:51 — پاسخِ چت ۳).
- ضدتکرار: باتری آشوب، سوئیت‌های آفلاین و دور ۳ بازاجرا نشدند؛ گیت‌ها فقط یک‌بار در مقامِ «گیتِ مسیرِ تغییر» (فاز ۲).

### تصحیحِ سه ادعای مالکیت/مرجعِ پرامپت (شاهد: سربرگِ خودِ اسناد)
| ادعای پرامپت | واقعیتِ مخزن | شاهد |
|---|---|---|
| «بستهٔ ۵ (Operational Readiness) مالک چت ۵ است» | **مالک = چت ۱۰** | `docs/OPERATIONAL_READINESS.md:3` → «**مالک:** چت ۱۰ (آرنا ۱۰ — عملیات/پایایی)» |
| «صریح بنویس: CAPACITY_MODEL.md مالک چت ۴» | **مالک = چت ۶** | `docs/CAPACITY_MODEL.md:3` → «**مالک:** چت ۶ (مستندات و انتشار)» |
| «بستهٔ ۴ (Query Performance) مالک چت ۴» | ✅ درست | `docs/ARENA4_PERFORMANCE_INFRA.md:6` → «آرنا ۴ مالکِ شش حوزه است: ردیس، کش، کارگرها، **رصدپذیری، تست بار**، استقرار» |
- پیامدِ عملی: چت ۵ هیچ سندی برای بستهٔ ۵ **نمی‌سازد** و هیچ عددی اختراع نمی‌کند؛ فقط **موجودیِ اسناد + شکاف‌های تأییدشده + مسیرهای واقعیِ تحویل** را می‌آورد. ‏(S8) هم به‌صراحت «پیشنهاد چت ۱۰ — نیازمند ‏ACCEPT» است.

### تأییدِ ادعاهای عددیِ پرامپت (همه در مخزن موجودند)
| ادعا | نتیجه | شاهد |
|---|---|---|
| ‏SLA ‏۹۹.۹۵٪ · ‏RTO ≤ ۱۵min · ‏RPO ≤ ۵min | ✅ | ‏`OPERATIONAL_READINESS.md:17` ‏(S1) + ‏`DR_RUNBOOK.md:4` |
| **failover خودکار ≤ ۳۰ ثانیه** | ✅ | ‏`DR_RUNBOOK.md:4` — عینِ عبارت؛ + خطِ ۱۸: «< ۳۰ ثانیه خودکار» برای خرابیِ ‏Redis Master |
| ‏۷ عددِ ‏SLO ‏(p50/p95/p99/5xx/کش/sync/OTP) | ✅ ‏(دور ۳ تأیید شد) | ‏`CAPACITY_MODEL.md` §۵ |
| هر حالتِ پایدار جدا: ‏mixed-national/read-browse/write-burst | ✅ هر سه **پروفایلِ واقعیِ ابزار** هستند | ‏`tools/capacity-saturation-probe.js:43` ‏(PROFILES) · ‏mixed-national = «اوج صبحگاهی: ‏~۷۹٪ خواندن / ‏۱۲.۵٪ نوشتن / ‏۸.۵٪ احراز» · اعتبارسنجیِ ‏Σوزن=۱۰۰ |
| «نردبانِ سیگاری ‏(۵،۱۰،۲۰ × ۶۰s)» | ❌ در مخزن نیست | جایگزینِ رسمی: ‏`--staircase 50,100,200,400,800,1600` ‏(هر پله ‏۱۲۰s) + نردبانِ **مقیاسِ** ‏`0.001→0.1→0.3→1.0` ‏(`CAPACITY_WORKLOAD_MODEL` §۵) |
| «آلارم‌های دیسک فعال» ‏(پذیرشِ ‏disk-full در ‏WAVE19) | ❌ **قابلِ‌ارضای نیست** | `grep -i disk\|space\|filesystem` در `infra/observability/alerts.yml` ⇒ **صفر** ‏(دور ۳ هم از راه ‏metrics تأیید شد) |

### یافته‌های تازهٔ همین دور (شاهد-محور)
1. **رصدپذیریِ واقعیِ مخزن ۲۴ آلارم دارد و نامِ متریک‌ها درست است:** `infra/observability/alerts.yml` شاملِ ‏`PayeshHighServerErrorRate` · ‏`PayeshLatencySlobreach` · ‏`PayeshLatencyP99Breach` · ‏`CriticalErrorBudgetBurn` · ‏`PayeshInstanceDown` · ‏`PayeshRestartLoop` · ‏`PayeshDbPoolSaturated` · ‏`PayeshDbSlowQueries` · ‏`PayeshCacheHitRateLow` · ‏`PayeshOutboxBacklog` · ‏`PayeshWorkerFailures` … و همه با نام‌های **صحیحِ کد** ‏(`payesh_http_request_duration_seconds_bucket` · ‏`payesh_cache_lookups_total` · ‏`payesh_db_query_duration_seconds` …) بسته شده‌اند. ⇒ **تصحیحِ دامنهٔ یافتهٔ دور ۳:** ابزارِ پایش سالم است؛ فقط **متنِ سندِ ‏§۵** نامِ متریک را بدونِ `_seconds` نوشته و ‏`payesh_cache_hit_rate` را من‌درآوردی ذکر کرده — نگاشتِ درست در ‏§۳ دور ۳ آمد.
2. **شکافِ آلارمیِ ‏F3 (تازه):** آلارمِ صف روی `payesh_outbox_depth{status="pending"} > 500` است — یعنی **صفِ درون‌پروسه‌ای** ‏(`store.outbox`)؛ صفِ دوام‌دارِ ‏PG ‏(`payesh_sync_queue_depth`) گِیج دارد ولی **هیچ آلارمی روی آن نیست** ‏(`grep -c` ⇒ ‏۰). دقیقاً همان حالتی که ‏F3 توصیف می‌کند (رویدادهای مردهٔ ‏`server_outbox` پس از کرش) در پایش **کور** است. دامنهٔ رفع: چت ۴ ‏(رصدپذیری) ⇒ فقط ثبت شد.
3. **موجودیِ اسنادِ آمادگیِ عملیاتی کامل است:** ‏۱۰ سند ‏(`OPERATIONAL_READINESS` · ‏`INCIDENT_RESPONSE` · ‏`INCIDENT_PLAYBOOK` · ‏`DR_RUNBOOK` · ‏`ONCALL_SCHEDULE` ‏(۹۴ خط) · ‏`BUS_FACTOR_REGISTRY` · ‏`COST_ENVELOPE` · ‏`CAPACITY_MODEL` · ‏`LOAD_TEST_RESULTS` · ‏`RELIABILITY_DR_PLAN`) + **۳ قالب** ‏(`templates/ESCALATION_MATRIX.md` · ‏`ONCALL_HANDOFF_TEMPLATE.md` · ‏`POSTMORTEM_TEMPLATE.md`) ⇒ فهرستِ «۸ سندِ موردنیاز» پرامپت **از قبل کامل است**؛ آنچه می‌ماند شکافِ محتوایی است، نه فقدانِ سند.
4. **کوریِ گیتِ ارجاع‌ها نسبت به `templates/`:** ‏`REF_RE` در `tools/docs-refs-check.js:46` فقط پیشوندهای ‏`tests|tools|scripts|src/js|docs|migrations|server|infra` را می‌گیرد ⇒ ارجاع به ‏`templates/…` هرگز سنجیده نمی‌شود (امروز هر دو قالبِ ارجاع‌شده موجودند، پس **بدهیِ فعلی صفر** است؛ شکافِ پوشش ثبت شد).
5. **مسیرهای واقعیِ اتصال به پایش (سه مسیر، همه در مخزن):** ۱) **اسکرپِ Prometheus** — `infra/observability/prometheus.yml` ‏(job ‏`payesh-api` · ‏`metrics_path: /metrics` · هدفِ ‏`host.docker.internal:3000` + اهدافِ چنده‌نودیِ کامنت‌شده ‏`10.10.0.12/13:3000`) و توکنِ ‏Bearer برای ‏`/metrics` ‏(`PAYESH_METRICS_TOKEN`)؛ ۲) **قواعدِ آلارم + ‏Alertmanager** — `alerts.yml` ‏(۲۴ قاعده · ‏`for: 10m` … ) + ‏`alertmanager.yml`؛ ۳) **داشبوردها و ردیابی** — ‏`grafana/provisioning/{datasources,dashboards}` + ‏`dashboards/payesh-main.json` + ‏`payesh-dashboard.json` + ‏`infra/tracing/jaeger-{dev,prod}.yaml`.

### آماده‌سازی بستهٔ ۵ — شکاف‌های تأییدشده (برای مالکِ سند: چت ۱۰؛ و چت ۴ برای متریک)
| ‏# | شکاف | شاهد | دامنهٔ رفع |
|---|---|---|---|
| ۱ | گِیج/آلارمِ **فضای دیسک** وجود ندارد ‏(پذیرشِ ‏disk-full قابلِ‌ارضا نیست) | ‏`alerts.yml` ⇒ صفر · ‏`metrics.js`/‏`index.js` ⇒ صفر `statfs/disk` | چت ۴ ‏(متریک) + بریفِ ناظر |
| ۲ | آلارم روی **صفِ دوام‌دار** ‏(`payesh_sync_queue_depth`) نیست ⇒ کوریِ ‏F3 | ‏`grep -c` در ‏`alerts.yml` ⇒ ‏۰ | چت ۴ |
| ۳ | ادعای کهنهٔ خطِ ۳۱ ‏(«‏`SYNC_RELIABILITY_EVIDENCE.md` وجود ندارد» — موجود است) | ‏`OPERATIONAL_READINESS.md:31` + ‏`ls docs/SYNC_RELIABILITY_EVIDENCE.md` ✅ | چت ۱۰ ‏(مالکِ سند) |
| ۴ | ‏S8 بدونِ ‏ACCEPT ‏(«تخلیهٔ صفِ ۱۰۰۰ پیام < ۵ دقیقه») | ‏`OPERATIONAL_READINESS.md:24,29` | ناظر ‏(ACCEPT) + drill فصلی |
| ۵ | انتسابِ واقعیِ افرادِ آن‌کال ‏(نام/تلفن) ‏NOT-RUN | خودِ سند: ‏`:56` «نیازمند تیم انسانی و ‏ACCEPT ناظر» | مالک/ناظر |
| ۶ | دقتِ متنِ ‏§۵ ‏(نامِ متریک/کش) | §۵ در برابر ‏`metrics.js:435` + ‏`payesh_cache_hits_total`/`lookups_total` | مالکِ سند |
- **وضعیتِ بستهٔ ۵:** مستندات ‏VERIFIED · اجرای میدانی ‏NOT-RUN · **نمی‌تواند «سبز» شود** تا ‏۱) سؤالِ چت ۱۰ برسد ۲) استیجینگ باز شود ۳) شکاف‌های بالا بریف شوند. ⇒ ‏`FINAL VERDICT` برای این دور: ‏VERIFIED ‏(تحویلِ آماده‌سازی) + ‏NOT-RUN ‏(بستهٔ ۵ محقق نشده).

### ورودیِ بستهٔ ۴ (Query Performance — مالک: چت ۴) — آماده‌شده، بدونِ اجرا
- **۵ آزمونِ پذیرشِ مکتوب** ‏(`CAPACITY_WORKLOAD_MODEL.md` §۷): ‏Load ‏(p95<SLO برای همهٔ اندپوینت‌ها) · ‏Stress ‏(شناسایی و مستندسازیِ زانو) · ‏Spike ‏(بازیابی ‏<۳۰s) · ‏Soak ‏(۲۴h بدونِ نشتِ حافظه، روند ‏RSS/heap) · ‏صحتِ داده زیرِ بار ‏(خطای نوشتن ‏=۰ · ‏sync ‏≥۹۹.۵٪).
- **۳ پروفایلِ بار** ‏(همه در ابزار، با اعتبارسنجیِ وزن): ‏`mixed-national` ‏(اوجِ صبحگاهی) · ‏`read-browse` ‏(درون‌روزِ خواندن‌محور) · ‏`write-burst` ‏(دورهٔ امتحان، فشارِ نوشتن).
- **ابزارها:** ‏`capacity-saturation-probe.js` ‏(نردبان + زانو + ‏`--confirm-staging` + ‏`--abort-error-rate`) · ‏`delta-load-test.js` ‏(‏`--max-error-rate`، پیش‌فرض ‏۰) · ‏`bench-reports-{scale,concurrency,academic,attendance}.js` · ‏`perf-attendance.js` · ‏`w18-load-pg.sh` · ‏`wave18-load-to-pg.js`.
- **مسیرِ ثبت:** ‏`docs/LOAD_TEST_RESULTS.md` ‏(مالکِ قالب: **چت ۶**؛ وضعیت: «قالب آماده — سناریوهای بارِ رسمی هنوز اجرا نشده‌اند») + قالبِ پاکتِ ظرفیت ‏`docs/CAPACITY_ENVELOPE_TEMPLATE.md` ‏(۸۸ خط)؛ قانونِ سند: هر انحراف از ‏§۵ ⇒ یک آیتمِ باز در ‏`docs/OPEN_ITEMS.md`.
- **گاردهای ایمنیِ راستی‌آزمایی‌شده:** ‏`--confirm-staging` ‏(بدونِ آن اجرا نمی‌کند) · توقفِ اضطراری ‏`5xx > 5٪` ‏(`--abort-error-rate`, پیش‌فرض ۵ — نه صفر؛ این با ‏`--max-error-rate` ابزارِ دیگر اشتباه نشود) · نامزدهای زانو ‏`slo|plateau|errors` · گرم‌کردنِ ‏۱۵s پیش از هر پله · گسترش فقط تا **عبور از زانو + یک پله** · ثبتِ هر اجرا در پاکت + بازبینیِ انسانی.
- **وضعیتِ اجرا در این محیط:** ‏NOT-RUN — سندباکس ‏≈۲GB ‏RAM و ‏`nproc=2` در برابرِ نیازمندیِ ‏≥۸GB/نود؛ و ‏§۶ سند می‌گوید پیش‌نیاز، استیجینگِ چندنودی ‏(بستهٔ ۱) است. تنها اجرای مجازِ این‌جا ‏`--validate` ‏(آفلاین) است که در دور ۳ انجام و تأیید شد ‏(‏`rc=0`).

### ‏ENGINEERING CLAIM (۱۸ فیلد)
- ‏EXPECTED: پایشِ ‏trigger چت ۱۰ + آماده‌سازیِ ورودی‌های بستهٔ ۵ و بستهٔ ۴ — با **صفر کارِ تازهٔ کد/تست**، صفر فایلِ تازه در ‏`docs/` و صفر بازاجرای دورهای پیشین.
- ‏MEASURED: ‏trigger ‏= ‏NOT-RECEIVED · **۳ تصحیحِ مالکیت** ‏(بستهٔ ۵ ↔ چت ۱۰ · ‏CAPACITY_MODEL ↔ چت ۶ · بستهٔ ۴ ↔ چت ۴ ✅) · **۵ تأییدِ عددی** ‏(SLA ‏۹۹.۹۵٪ · ‏RTO ‏۱۵min · ‏RPO ‏۵min · **failover ‏۳۰s** · سه پروفایلِ بار) · **۵ یافتهٔ تازه** ‏(۲۴ آلارمِ درست‌نام · کوریِ صفِ دوام‌دار · گِیجِ دیسکِ غایب · کوریِ گیت به ‏`templates/` · موجودیِ ۱۰ سند + ۳ قالب) · **۶ شکافِ بستهٔ ۵** · **۵ آزمون + ۳ پروفایل + ۹ ابزار** برای بستهٔ ۴.
- ‏TEST ENV: ‏n/a ‏(دورِ مستنداتی) — تنها اجرای این دور: ‏`capacity-saturation-probe --validate` ‏(آفلاین، ‏rc=0) + گیت‌های مسیر.
- ‏DATASET/CONCURRENCY/P50/P95/P99/ERROR RATE: ‏n/a · ‏DB/CACHE/MIGRATION/EXPLAIN/BENCHMARK: ‏n/a ‏(صفر ‏DDL/DML).
- ‏SECURITY IMPACT: ‏۰ توکن در فایل/کامیت/گزارش؛ ‏`secret-scan` ‏۱۲/۱۲ ✅؛ هیچ مسیرِ دسترسی تغییر نکرد.
- ‏FAILURE TEST / RECOVERY: ‏n/a ‏(بازاجرا نشد؛ اعدادِ آشوب از دورهای ۱–۳ ارجاع داده شده و اعدادِ **کهنهٔ پس‌از‌رفع** با برچسبِ ‏NOT-RUN-تا-بازاجرا در پوشهٔ شواهد ماند).
- ‏REGRESSION RISK: صفر — فقط یک فایلِ گزارشِ روزانه تغییر کرد.
- ‏KNOWN UNKNOWNs: ‏freshness: ‏trigger چت ۱۰ ‏NOT-RECEIVED · استیجینگ ‏NOT-RUN ‏(بستهٔ ۱) · آشوبِ چند-نمونه ‏NOT-RUN · **‏freeze-marker ‏۱۳/۱۴ ‏REDِ وراثتی** ‏(‏DF-HASH روی ‏`PR_MERGE_PLAN.md`؛ مالک چت ۶ / کامیتِ متهم ‏`3f1c874` چت ۷) · گِیجِ دیسک و آلارمِ صفِ دوام‌دار ‏(چت ۴ + بریف) · ‏S8 بدونِ ‏ACCEPT · ‏CI ‏NOT-RUN ‏(بیلینگ) · ظرفیتِ ملی اثبات‌نشده (دائمی).
- ‏FINAL VERDICT: **VERIFIED** ‏(پایش + آماده‌سازی با شاهدِ کامل) + **NOT-RUN** ‏(سؤالِ چت ۱۰ دریافت نشد · استیجینگ باز نیست · بستهٔ ۵ محقق نشده). ‏(تنشِ درونیِ پرامپت: بندِ ‏CLAIM می‌گوید ‏VERIFIED+NOT-RUN ولی ‏Acceptance Bundle می‌گوید ‏NOT-VERIFIED — ثبت شد؛ تفکیکِ درست: **تحویلِ مستنداتی ‏= ‏VERIFIED** و **بستهٔ ۵ ‏= ‏NOT-RUN/NOT-VERIFIED**.)

### گیت ۸ سؤال
۱) **چه چیزی تغییر کرد؟** فقط `docs/daily-reports/2026-09-14.md` ‏(الحاقِ همین بخش)؛ صفر فایلِ تازه در `docs/`، صفر تغییرِ کد/تست. ۲) **چرا؟** پایشِ trigger و آماده‌سازیِ ورودی‌های بستهٔ ۵/۴ طبقِ پرامپتِ دور ۴. ۳) **invariant:** چت ۵ idle است — صفر کارِ کد/تست، صفر ادعای نوبتی، صفر لمسِ اسنادِ مالکانِ دیگر. ۴) **درست‌بودن؟** هر ادعا به فایل+خط یا اجرای ابزار گره خورده؛ ۳ مالکیت و ۵ ادعای عددی راستی‌آزمایی شد. ۵) **سریع‌تر/مقیاس‌پذیرتر؟** ‏n/a ‏(مستندسازی). ۶) **امنیت؟** ‏۰ توکن + ‏secret-scan ‏۱۲/۱۲. ۷) **تستِ شکست؟** ‏n/a؛ اما **دو کوریِ پایش** ‏(صفِ دوام‌دار و دیسک) و **کوریِ گیت به ‏`templates/`** ثبت شد. ۸) **rollback؟** `git revert` یک کامیتِ مستنداتی.

### گیت‌ها/بودجه/پوش
- ‏`docs-stats-sync --check` = **✅** · ‏`docs-metadata` = **۱۷/۱۷ ✅** · ‏`secret-scan` = **۱۲/۱۲ ✅** · ‏`tools/docs-refs-check` = **exit ۰** ‏(‏۴۱ ارجاعِ کهنهٔ تازه = خطِ پایهٔ main؛ سندِ این دور صفر ارجاعِ کهنهٔ تازه ساخت) · ‏`reza-mirror-check` = **exit ۰** ‏(‏۲۱ آینهٔ زنده برابر؛ **۰ آینهٔ تازه / ۰ کپیِ دستی**).
- ‏`docs-freeze-marker` = **۱۳/۱۴ ‏RED** — همان **قرمزِ وراثتیِ دور ۳** ‏(‏`DF-HASH` روی ‏`PR_MERGE_PLAN.md`؛ فهرستِ ‏`rc43` ‏=`b3b44a4f…` و دیسکِ ‏=`9ef9c211…`؛ کامیتِ متهم ‏`3f1c874` چت ۷). ‏`tests/docs-refs-check.js` ‏(متا) هم همان قرمزِ پیش‌موجودِ اسنادِ ‏`CHAT4_*` است. **هیچ‌کدام سهمِ چت ۵ نیست و لمس نشد** (بامپِ قفل = مالک چت ۶).
- **فایل‌های موقتِ نام‌برده در پرامپت ساخته نشدند** (`CHAT10_QUESTION_MONITORING.md` · `OPERATIONAL_PREP.md` · `QUERY_PERFORMANCE_PREP.md`) — برای **سومین دورِ پیاپی**: فهرستِ ممنوع‌های همان پرامپت «فایل تازه در ‏docs/ مگر گزارش روزانه» را ممنوع می‌کند ⇒ محتوا (فهرستِ اسناد، ۳ مسیرِ پایش، ۴ سناریو/۵ آزمون، ۷ معیارِ پذیرش) مستقیم در همین گزارش آمد ⇒ **صفر فایلِ موقت برای حذف**.
- بودجه: ‏`/home/user` ‏۱۱۵MB ‏→ ‏۱۱۶MB ‏(یک فایلِ گزارش؛ جهشِ ‏`.git` از fetchهای مکرر است) · ورکتری ‏~۳۵MB · ‏token-hits: **۰** · یک کامیت + یک پوش.

### اعلام idle رسمی (چت ۵ — دور ۴)
چت ۵ idle است تا: **سؤالِ چت ۱۰** · **بازشدنِ استیجینگ (بستهٔ ۱)** · **بریفِ مالک** ‏(گِیجِ دیسک/آدیت با چت ۴، آلارمِ صفِ دوام‌دار، تصمیمِ قراردادِ ‏pg-down، یا ‏ACCEPTِ ‏S8) · یا **حکمِ ناظر**. تا آن زمان: صفر کارِ کد/تست، صفر ادعای تازه، صفر فایلِ موقت.

- idle رسمی تا: اعلامِ «تصمیمِ ناظر دربارهٔ رفع قرمزی freeze» / rc44ِ چت ۶ (بالاترینِ امروز هنوز **rc43** — rc44..rc49 = ۰) / حکمِ تازه

---

## چت ۷ — دور ۱۲: راستی‌آزماییِ دور ۱۱ + دلتایِ صف (ثبت: 2026-09-14)
- وضعیت قرمزی freeze: **۱۳/۱۴ روی `main` (باز، سه دور متوالی)** ⇒ **فاز ۱**؛ `main`: `5a175c0 → aeb32a9` = «Merge #213» (چت ۵ دور ۳، بدونِ هیچ تغییرِ rc)؛ بالاترینِ قفل همچنان **rc43**، rc44=۰؛ سیگنالِ تصمیم = NOT-RECEIVED
- آماده‌سازی برای بستهٔ ۷: **۹** پیش‌نیاز / **۹** راهکار ایمن (افزوده: `merge -s ours` برای شاخهٔ سندیِ عقب‌مانده؛ هر-دور-یک-فایل-تک‌مالک) / **۶** معیار پذیرش — §۲ و §۵ دور ۱۲؛ **۰ اجرا** (حکم ۲). **شاهدِ مثبت:** در ۳ مرجِ متوالی (`#209/#212/#213`) بدهیِ `docs-refs` روی `main` **ثابت ۱۱۳** ماند و stats هم exit 0 ⇒ الحاق‌کنندهٔ مرج، آمار را بازتولید کرده است
- یافتهٔ صف: `#214` (چت ۸) و `#213` (چت ۵) هر دو فقط با الحاقِ انتهای `2026-09-14.md` کار می‌کنند؛ `#213` مرج شد، `#214` فوراً **CONFLICTING**؛ `#211` از دور ۱۰ هنوز CONFLICTING — رأسِ من محتوایش ابرمجموعه است (۰ حذف) ولی تعارض در **تاریخچه** است، نه متن
- آماده‌سازی برای بستهٔ ۵: **۳** سنجهٔ کلیدی خوانده‌شده (S4 p95<300ms · S5 p99<1s · قاعدهٔ یکتایی) / **۲** راهکار اتصال راستی‌آزمایی‌شده (`HighLatency` در `infra/observability/alert-rules.yml` + بودجهٔ خطا در `alerts.yml`؛ `monitoring/alert-rules.yml` = پوینترِ سالم، سایه نیست) / **۰** سناریوی آماده توسط چت ۷ — **مالک چت ۱۰**؛ `docs/OPERATIONAL_READINESS.md` و `docs/ONCALL_SCHEDULE.md` **وجود دارند** و ۳ مرجعش (`CAPACITY_MODEL`/`INCIDENT_RESPONSE`/`INCIDENT_PLAYBOOK`) همه EXISTS
- گیت‌ها: freeze **[13/14 ❌ صحه‌گذاری‌شده]** · stats **[exit 0 · 503=495+8]** · metadata **[دور ۱۱: exit 0 · 406 سند/یتیم ۰ — بازاجرا نشد]** · refs **[113 در 65 سند · تازه از فایل‌های من ۰ · baseline 75 دست‌نخورده]** · mirror **[دور ۱۱: ۰/۰]** · secret-scan **[دور ۱۱: 12/12 PASS]** · CI **NOT-RUN** (RISK-O-007)
- پوش: — == ls-remote — · بودجه: `32,234,212 B / 1350 فایل` · token-hits: **۰** · ۵ کامیتِ بی‌PRِ من روی `0e1d7a9` منتظرِ تکلیفِ ناظر (خودسر PR باز نکردم)
- idle رسمی تا: اعلامِ «تصمیمِ ناظر دربارهٔ رفع قرمزی freeze» / rc44ِ چت ۶ / حکمِ تازه


---

## چت ۷ — دور ۱۲ (شمارِ بریف) / ۱۳ (شمارِ من): پایشِ تصمیمِ رفعِ قرمزی + تطبیقِ SLOها (ثبت: 2026-09-14)
- وضعیتِ تصمیم رفع قرمزی: **هنوز نه** ⇒ **فاز ۱**؛ دروازهٔ اصلاح‌شدهٔ بریف (وجودِ فایل + جمله در `.content`) اجرا شد → `Not Found` ⇒ **FALSE**؛ بازرسیِ دوسویه (L13): شرط این بار **معنادار** است و نقصِ واژگونِ دور ۱۱ **توسطِ ناظر رفع شده** — این تأییدِ اصلاح است، نه خطایِ امروز؛ `main = 895f8d4` («Merge #214»، چت ۸ هم «الگوی A / اعلام‌نشده» را گزارش کرده)، rc43 آخرینِ قفل، rc44 = ۰، سندِ تصمیم روی `main` = ۰
- آماده‌سازیِ بستهٔ ۷: **۹** پیش‌نیاز / **۹** راهکار ایمن (+ **L15**: برای شاخهٔ فقط-الحاقی، `rebase` درمانِ اصلی و `merge -s ours` پشتیبان است — پذیرشِ نسخهٔ main فقط تعارضِ همان لحظه را می‌بندد) / **۷** معیارِ پذیرش ⇒ **تطبیقِ ۷/۷ با `docs/CAPACITY_MODEL.md` §۵**: `p50<100ms` · `p95<300ms` + `HighLatency` · `p99<1s` · `5xx<0.1٪` (بودجهٔ خطا ≈ ۴۳ دقیقه/ماه) · کش `>80٪` · sync `≥99.5٪` · OTP `≥99.9٪`؛ **۰ اجرا** (حکم ۲)
- سه انحرافِ مستند (صفر ویرایشِ سندِ دیگران): ① `OPERATIONAL_READINESS.md` سطرهای ۲۰–۲۱ را به **§۴** گره می‌دهد در حالی که جدولِ SLO در **§۵** است (خط ۲۱۸ vs ۲۳۷) ② قانونِ گزارش‌دهیِ §۵ «پنج عدد» می‌گوید ولی جدول ۷ سطر دارد ③ `monitoring/alert-rules.yml` = پوینترِ یک‌خطی به `infra/observability/alert-rules.yml` (۸۵ خط، موجود) — برای هر glob، فایلِ «تقریباً خالی»؛ `DR_RUNBOOK`: **۴/۴ مطابق** (`RTO≤15min` · `RPO≤5min` · `failover≤30s` · «سقف نه هدف»)
- آماده‌سازیِ بستهٔ ۵ (مالکِ چت ۱۰): **۳** سنجهٔ کلیدی خوانده‌شده (S2 sync · S3 OTP · S4/S5 تأخیر) / **۲** راهکارِ اتصال راستی‌آزمایی‌شده / **۰** سناریوی آماده توسط چت ۷؛ `docs/OPERATIONAL_READINESS.md` **وجود دارد** و ۳ مرجعش (`CAPACITY_MODEL` · `INCIDENT_RESPONSE` · `INCIDENT_PLAYBOOK`) همه EXISTS ⇒ هیچِ کمبودِ ارجاعی ندیدم؛ KNOWN UNKNOWNِ دائمی حفظ شد
- گیت‌ها (تازه): freeze **[13/14 ❌ صحه‌گذاری‌شده]** · stats **[exit 0 · 503=495+8]** · metadata **[exit 0 · 406 سند/یتیم ۰]** · refs **[113 در 65 · تازه از فایل‌های من ۰ · baseline 75 دست‌نخورده — ۰ حذف]** · mirror **[exit 0 · ۰/۰، فقط چک]** · secret-scan **[12/12 PASS]** · CI **NOT-RUN** (RISK-O-007) · تستِ بار/PG **NOT-RUN** با دلیلِ محیطیِ عددی (`nproc=2`، `RAM=3939MB` < ۲ نود × ≥۸GB)
- **خود-گزارشِ حادثهٔ این دور (L16):** pushِ رأسِ تازه از worktreeِ **detached** به‌دلیلِ non-fast-forward **رد شد** و `ls-remote` همان `d58bc0d` را نشان داد؛ در تلاشِ اصلاح، بلوک‌ها از نسخهٔ `main` بریده شدند (که الحاق‌های ادوارِ ۹–۱۲ را **ندارد** — آن‌ها فقط روی رأسِ خودیِ من‌اند) و دو فایلِ ۱خطیِ بی‌محتوا ساخت‌شد که **قبلِ commit/push** با `reset --hard d58bc0d` بیرون کشیده شد (هیچ‌چیز publish نشده بود). ⇒ قاعده: `worktree add -b BR <remote-tip>` + **همیشه** مقایسهٔ `ls-remote` با SHA (وگرنه «push رد شده» را «push شد» می‌نویسید)
- بازکالیبرِ بریف: «۵ کامیت بی‌PR» → **۶**؛ «بکاپِ قبلِ ادغام» → باندل **نساختم** (دور ۷ آزمودم، محو شد) — بیمه = **push روی دو رأسِ ریموت**؛ سه سندِ موقت: **۰ ساخته / ۰ حذف**
- پوش: — == ls-remote — · بودجه: `32,234,212 B / 1350 فایل` (قبل) · token-hits: **۰**
- idle رسمی تا: دریافتِ «تصمیمِ رفعِ قرمزی freeze» / rc44ِ چت ۶ / حکمِ ناظر

---

## چت ۷ — دور ۱۴: پایشِ دروازه (FALSE) + شمارش‌هایِ واقعیِ صف (ثبت: 2026-09-14)
- وضعیتِ تصمیم رفع قرمزی: **هنوز نه** ⇒ **فاز ۱**؛ دروازهٔ بریف (`gh api …/contents/<سندِ تصمیم> | jq -r .content | base64 -d | grep -q …`) اجرا شد → `.message = Not Found` ⇒ **FALSE**؛ `main = 0360b37` («Merge #215» — چت ۵ دور ۴)، rc43 آخرینِ قفل، **rc44 = ۰**، `docs-freeze-marker` روی `main` = **۱۳/۱۴ قرمز** (پنجمین دورِ متوالی)، `stats-sync --check` = **exit 0**، `docs-refs` روی `main` = **۱۱۳ در ۶۵ سند** (ثابت از دور ۱۱ — ۰ رشد در ۵ مرجِ متوالی)
- **شمارش‌هایِ تازهٔ صف (برای معیارهای پذیرشِ بسته ۷):** PR باز = **۱** (`#211`، ۵ کامیت، **CONFLICTING**، آخرین به‌روزرسانی `2026-09-14T09:19:14Z` = **۲ ساعت ۲ دقیقه** عمرِ آخرین تماس؛ ایجاد `06:31:06Z`) · رأس‌های ریموت = **۱۵۷** · الگوی نام‌گذاریِ غالب: `feat/` **۷۵** (۴۷٫۸٪)، `docs/` **۳۱**، `arena/` **۲۵**، `fix/` **۱۶**، `chore/` ۳، `test/` ۲، `port/` ۱، `main` ۱ · **غیرمنطبق با هر پیشوند = ۳ از ۱۵۷ (۱٫۹٪)**: `bh-mut/phase2`، `bug-hunt-session9`، `chat3-restored` ⇒ «الگوی استاندارد» امروز **دو-شاخهٔ عملی** است (`feat/*` برای کد، `docs/*` برای سند)؛ شاخهٔ من `feat/chat7-merge-queue-stabilization` با هر دو سازگار است چون تحویلش سندِ درون‌شاخه‌ای است
- **وضعیتِ تحویلِ خودم (تصحیحِ عددِ ادوارِ قبل):** رأس‌های خودی = `f172a1d5` با **۷ کامیت بی‌PR**؛ روی `main` از محتوای من فقط **دور ۸** منتشر شده (`chat7.md` سرفصل ۲۳۴) ⇒ **ادوار ۹..۱۳ (۱۸۸ خط) فقط روی دو رأسِ خودی‌ام است**؛ در این دور تعارضِ انتهای-فایل را با **merge از main + unionِ دستی** بستم (پذیرفتنِ `14.md` نسخهٔ main = ۳۳۷ خط و الحاقِ مجددِ ۲ بلوکِ خودم؛ `chat7.md` = نسخهٔ main + ۱۸۸ خط من) ⇒ دلتایِ نهایی نسبتِ به `main`: **۲۲۶ افزوده / ۰ حذف** (`188 ++` در `chat7.md` + `38 ++` در همین فایل — عددِ «۲۱۵» پیش از الحاقِ این بلوک اندازه گرفته شده بود و اصلاح شد) — یعنی هیچ خطی از چت ۵/۸ پاک نشد
- بستهٔ ۷: **۹** پیش‌نیاز / **۱۰** راهکار ایمن (+ **L17**: «`merge -s ours` در PR، محتوای طرفِ مقابل را دور می‌ریزد؛ برای سناریوی الحاق-به-انتهای‌فایل، **rebase** درست است یا **merge معمولی با union دستی**») / **۷** معیار پذیرش (SLOها، تطبیقِ ۷/۷ با `CAPACITY_MODEL` §۵) · بستهٔ ۵ (مالک چت ۱۰): **۰** تغییر توسط من — `docs/OPERATIONAL_READINESS.md` **وجود دارد**
- گیت‌ها (رأسِ نهایی): freeze **[13/14 ❌ صحه‌گذاری‌شده، بدون --freeze/rc]** · stats **[exit 0]** · metadata **[exit 0]** · refs **[تازه از فایل‌های من = ۰؛ baseline 75 دست‌نخورده]** · mirror **[۰/۰ — فقط چک]** · secret-scan **[12/12 PASS]** · CI **NOT-RUN** (RISK-O-007) · بار/PG **NOT-RUN** (`nproc=2`, `RAM=3939MB`)
- سه سندِ موقتِ brief: **۰ ساخته / ۰ حذف** (تکلیفِ ادوارِ ۹–۱۴) · token-hits: **۰** · بودجه: `32,234,212 B / 1350 فایل` 🟢
- idle رسمی تا: دریافتِ «تصمیمِ رفعِ قرمزی freeze» / rc44ِ چت ۶ / حکمِ ناظر

---

## چت ۷ — دور ۱۴: پایشِ وضعیت تصمیم‌گیری + ردِّ اعدادِ S1–S6 و بازرسیِ CI (ثبت: 2026-09-14)
- وضعیت تصمیم‌گیری: **هنوز نه** ⇒ **فاز ۱**؛ دروازهٔ بریف (`gh api …/contents/<سندِ تصمیمِ ناظر> | jq -r .content | base64 -d | grep -q "رفع قرمزی freeze"`) → `Not Found`؛ `main=0360b37`، rc43 آخرینِ قفل (rc44=۰)، freeze روی `main` **۱۳/۱۴ — ششمین دور متوالی**؛ بازِ مخزن = ۳ (`#216` خودم MERGEABLE/CLEAN · `#217` چت ۵ · `#211` CONFLICTING)
- **ردِّ اعدادِ ابلاغی (منبعِ ناموجود):** «۱۰ سناریو بر اساس `rto-rpo-summary.csv`» — آن فایل در **هیچ کامیتِ دست‌رس‌پذیری** افزوده نشده (`git log --all --diff-filter=A | grep -c` = **۰**)؛ پنج عددِ RTO (`376/144/9545/1353/28948ms`) با `git grep` به‌صورتِ توکنِ کامل در `docs/` = **۰/۰/۰/۰/۰**؛ تنها RTO اندازه‌گیری‌شدهٔ repo: **`30173ms`** (`docs/WAVE19_WAL_DRILL_REPORT.md:58`)؛ جدولِ §۳ِ `LOAD_TEST_RESULTS.md` کاملاً **`[TBD]`** ⇒ آن پنج عدد را در هیچ سندی **ننویسم**؛ کپی‌کردنشان = سبزِ جعلی (خطِ قرمزِ خودِ بریف)
- آماده‌سازی برای بستهٔ ۷: **۱۰** سناریو **VERIFIED به‌عنوان تستِ موجود** (`tests/chaos-drill-*` ×۶ با نام‌های `S1-api-kill`/`S2-redis-outage`/`S6-disk-pressure` درونِ خودِ فایل‌ها + `sync-conflict-storm-drill`/`queue-outage-drill`/`offline-sync-drill`) / **۰ اجرا** / **۵** راهکار ایمن ثبت‌شده اما **NOT-RUN** / **۷** معیار پذیرش (تطبیقِ **۷/۷** با `CAPACITY_MODEL` §۵) + **۴/۴** سقفِ `DR_RUNBOOK` · دلیلِ عدمِ اجرا (سه شاهدِ مستقل): `grep -cE 'chaos-drill|storm|outage' tests/run.js` = **۰** ⇒ در `npm test`/CI نیست؛ `pg_ctl`/`redis-cli`/`psql` = **ABSENT**؛ `nproc=2`، `RAM=3939MB` < ۲ نود × ≥۸GB (tmpfs = ۵ mount: تنها پیش‌نیازِ فراهم، S6)
- **🟥 تصحیحِ ادعای همیشگیِ خودم: CI اجرا می‌شود.** `gh pr checks 216` → **۷ check، همه success** (`build (22.x)` ۴۵s · SAST · Secret scan · DAST · SCA · SBOM · WAF&nginx)؛ combine-status `success` ⇒ «CI NOT-RUN (بیلینگ؛ RISK-O-007)» که از دور ۵ در هر گزارش کپی می‌شد، **منسوخ** است؛ `node.js.yml` فقط lane **22.x** دارد و در کامنتش صریح نوشته شده laneهای ۱۸/۲۰ حذف شدند چون smoke «بی‌صدا skip» می‌شد و fake-green می‌داد ⇒ **قفلِ node 22 = محافظِ صداقت، نه مانعِ اجرا**
- **قفلِ دور باطلِ اصلاحِ اسناد:** `docs/RISK_REGISTER.md:104` همچنان «توقف CI به‌دلیل صورتحساب» را ثبت کرده و **در مانیفست rc43 یخ‌زده است** (`grep -c = 1`)؛ `OPERATIONAL_READINESS.md`/`CAPACITY_MODEL.md`/`LOAD_TEST_RESULTS.md` هم = ۱ ⇒ سه انحرافِ ثبت‌شدهٔ من **بدونِ بامپ rc قابل اصلاح نیستند** (مالکیتِ چت ۱۰/۴/۶)؛ تنها `monitoring/alert-rules.yml` **یخ‌زده نیست** (۰) و همین حالا قابل اصلاح است — دست نزدم
- آماده‌سازی برای بستهٔ ۵: **تکذیبِ بند ۳ بریف، بارِ سوم** — `docs/OPERATIONAL_READINESS.md` **EXISTS** (۱۲۹ خط، سطرِ ۳ «مالک: چت ۱۰»، §۱ جدولِ SLO) ⇒ «اسنادِ پراکنده» نیست، یک سندِ مرجعِ متمرکز است؛ **۰** تغییر توسط من
- گیت‌ها: freeze **[13/14 ❌]** · stats **[exit 0 · 503=495+8]** · metadata **[exit 0 · 406 سند/یتیم ۰]** · refs **[113 در 65 · تازه از فایل‌های من ۰ · baseline 75 دست‌نخورده]** · mirror **[exit 0 · ۰/۰ فقط چک]** · secret-scan **[12/12 PASS محلی + «Secret scan: success» در CI — دو مسیرِ مستقل]** · CI **[PASS ×۷ — نه NOT-RUN]**
- سه سندِ موقتِ brief (`DECISION_MONITORING`/`MERGE_QUEUE_PREP`/`OPERATIONAL_PREP`): **۰ ساخته / ۰ حذف** (`ls` = No such file) — تکلیفِ ادوارِ ۹–۱۴؛ محتوا در §۱..§۶ِ «دور ۱۴» در `2026-09-13-chat7.md`
- پوش: — == ls-remote — · بودجه: `32,234,212 B / 1350 فایل` (قبل از push، ثابت از دور ۱۳) 🟢 · token-hits: **۰**
- idle رسمی تا: دریافتِ «تصمیمِ رفعِ قرمزی freeze» / rc44ِ چت ۶ (که باید `RISK-O-007` و سه انحراف را هم بازتولید کند) / حکمِ ناظر

---

## چت ۵ — دور ۵: پایش وضعیت سؤالات چت ۱۰ + بیست‌وپنجمین دور idle (ثبت: ۲۰۲۶-۰۹-۱۴)

**دامنه:** پایش trigger چت ۱۰ + آماده‌سازی (فقط پیش‌نیاز) برای بستهٔ ۵ · صفر کد · صفر تست · صفر فایلِ تازه · تک‌سناریو.

### فاز ۰ — وضعیت + ضدتکرار (شاهدِ عددی)

- **PR #215 (دور ۴) مرج شد** ⇒ `main = 0360b37` (مرجِ `2026-09-14T09:09:52Z` · merge_commit `0360b379599a`)؛ **صفر PR بازِ چت ۵**. دور ۴ دیگر «باقیماندهٔ فوری» نیست.
- `fetch` اجباری پیش از هر کار: `origin/main = 0360b37` = `ls-remote` = API (`git/ref/heads/main`) — سه‌راهه برابر.
- ⚠ **نکتهٔ فنیِ محیط:** remote نام‌دارِ `origin` در `p2` **وجود ندارد** (`git remote -v` خالی) ⇒ `git fetch origin main` با خطا برمی‌گردد؛ fetch/fetch‌های دورهای ۳ و ۴ با *تزریقِ remote موقت* انجام می‌شد که با مهاجرت `/tmp` پاک شده بود. از این پس «هر دور با fetch» = `git fetch <URL-T1> +refs/heads/main:refs/remotes/origin/main` (توکن فقط inline).
- **trigger چت ۱۰ = NOT-RECEIVED** — شاهد عددی (REST، همان لحظهٔ فاز ۰): `issues?state=open` ⇒ **۰ issue بازِ غیر-PR**؛ `labels=chat10` ⇒ **۰**؛ `#184` ⇒ **۳ کامنت**، آخرین `2026-09-14T05:51:51Z` (rezaa2544). طبق درختِ تصمیمِ پرامپت: **فاز ۰.۵ اجرا نشد** و فاز ۱ (پایش) اجرا شد.
- **ضدتکرار:** صفر بازاجرای دور ۴ · صفر بازاجرای گیت‌های docs در این فاز · صفر سوئیت آفلاین (chaos/RTO-RPO) · صفر کد/تست جدید · صفر بازتولید `.git` · صفر بازتولید `p2`.
- **بهداشتِ کارِ بازِ دور ۴:** `git stash list` خالی · دور ۴ کامیت‌شده/پوش‌شده و مرج ⇒ هیچ اثرِ معلق یا کارِ ناتمامی برای انتقال وجود نداشت؛ **کارِ تمام‌شده بازاجرا نشد**.
- `p2` (کانونیکال، شاخهٔ خودش) دست‌نخورده: `dirty=21` (پیش‌موجودِ دیگری)، `HEAD=9ad3b08`؛ `main` لمس نشد؛ شاخهٔ هیچ‌کس لمس نشد.

### تنشِ پرامپت — چهارمین دورِ پیاپی (فایلِ موقتِ نام‌برده: ساخته نشد)

پرامپت هم‌زمان (الف) ساختِ سه فایلِ موقت را در `docs/` الزام می‌کند و (ب) در ممنوع‌ها می‌گوید «فایلِ تازه در `docs/` مگر گزارش روزانه». این تنش در دوره‌های ۳، ۴ و ۵ تکرار شده و پرامپت هیچ‌گاه آن را حل نکرده است؛ طبق کنوانسیونِ پابرجا (دور ۱۵ چت ۳) محتوا در همین گزارش آمد و **هیچ فایلِ موقتی ساخته نشد** ⇒ صفر فایل برای حذف · صفر ریسکِ جاماندن. (بندِ «`FILES`: ۲ docs موقت» در پاکتِ پذیرش هم با فهرستِ سه‌فایلیِ پرامپت ناسازگار است.) این تنش به‌عنوان **KNOWN UNKNOWN** ثبت شد، نه به‌عنوان کارِ انجام‌شده.

### تصحیح‌های شاهددار روی محتوای پرامپت (Evidence Gate > Task Gate)

۱) **مسیرِ `NEXT_ACTIONS.md`:** پرامپت آن را در ریشهٔ `docs/` می‌گذارد؛ در آن مسیر **وجود ندارد** و مسیرِ واقعیِ آن `docs/daily-reports/NEXT_ACTIONS.md` است. پس فهرستِ «۱۰ سند» = ۹ سند در `docs/` + ۱ سند در `docs/daily-reports/`؛ هر ۱۰ موجودند و ۳ قالبِ عملیاتی هم کنارشان ✅.
۲) **گِیجِ صفِ دوام‌دار — تصحیحِ دقتِ یافتهٔ دور ۴ (به نفعِ سیستم):** گِیج **وجود دارد** — `server/metrics.js:513` تعریفِ `payesh_sync_queue_depth`، مقداردهی از کوئریِ PG در `:573` و افتِ ایمن به ۰ در `:576`. کوری **فقط در لایهٔ هشدار** است: صفر قاعده در `infra/observability/alerts.yml` به آن ارجاع می‌دهد (نزدیک‌ترین آلارم `PayeshOutboxBacklog` روی `payesh_outbox_depth{status="pending"}` است = صفِ **درون‌پروسه‌ای** با سقفِ ۱۰۰۰). پس دامنهٔ رفع باریک است: یک قاعدهٔ آلارم، نه ساختِ متریکِ تازه.
۳) **گِیجِ دیسک — تأییدِ سوم:** صفر متریکِ `disk/statfs/filesystem` در کد و صفر آلارمِ دیسک در `alerts.yml` (۲۴ قاعده، هیچ‌یک دیسک نیست) ⇒ پذیرشِ «آلارم‌های دیسک فعال» در `WAVE19` §۴ **قابلِ ارضا نیست** (F4 باز).
۴) **نام‌های متریکِ §۵ سندِ ظرفیت نادرست‌اند (خودِ پایش سالم است):** متن `payesh_http_request_duration` می‌نویسد، واقعیت `payesh_http_request_duration_seconds` (`metrics.js:435`، هیستوگرام با برچسب‌های route/method)؛ و `payesh_cache_hit_rate` در کد **وجود ندارد** — سنجهٔ درست کسرِ `rate(payesh_cache_lookups_total{outcome="hit"})` به کلِ `rate(payesh_cache_lookups_total)` است (شمارندهٔ `payesh_cache_lookups_total` در `:482`؛ `_hits_total`/`_misses_total` در `:514–515` گِیج‌های سازگاری‌اند) — یعنی **همان کسری که آلارمِ `PayeshCacheHitRateLow` به‌کار می‌برد**. ⇒ شکافِ مستندات است، نه شکافِ ابزار.
۵) **حلقهٔ گزارش‌دهیِ §۵ بسته است:** `docs/OPEN_ITEMS.md` موجود (۹۸ خط) ⇒ ارجاعِ §۵ به آن بن‌بست نیست.
۶) **دومین متنِ کهنهٔ `CAPACITY_MODEL §۶`:** `LOAD_TEST_PLAN.md` را «در دست ساخت» می‌نامد، ولی فایل موجود است ⇒ کنارِ نام‌های متریکِ نادرست و ادعای کهنهٔ خطِ ۳۱، سه نقطهٔ کهنگیِ مستنداتی.
۷) **ادعای کهنهٔ خطِ ۳۱ تأییداً کهنه است:** متنِ `OPERATIONAL_READINESS` می‌گوید «`SYNC_RELIABILITY_EVIDENCE.md` در ریپو وجود ندارد»؛ فایل **موجود** است (۷۶ خط، آخرین تغییر `f61a292` در ۲۰۲۶-۰۹-۱۳) ⇒ تصحیحِ آن، کارِ مالکِ سند (چت ۱۰) است.
۸) **«liveness=200 با pid تازه» شاهد ندارد:** بدنهٔ واقعیِ liveness (`server/index.js:747`) `{ok, status:'live', name} ` است و **pid/uptime ندارد** ⇒ این سنجه از کد قابلِ استخراج نیست؛ سنجهٔ در دسترسِ خودراه‌اندازی = آلارم‌های `PayeshInstanceDown` و `PayeshRestartLoop` (+ رگهٔ متریک‌ها). سیگنالِ **readiness** اما تأیید شد: `:758–769` بدنهٔ `{db:{driver,alive}, redis:{driver,alive,live,required}}` و مسیرِ prod با `required` ⇒ آن سه سیگنالِ پرامپت ✅.
۹) **`replayPendingFromPg` ✅** (`server/index.js:551` · `server/outbox.js:154`) ⇒ سناریوی «کرش با صفِ پر» روی کدِ موجود قابلِ اجراست.
۱۰) **`S8` چیزی برای «تأییدِ آماده» نیست — نیازمندِ ACCEPT است:** `OPERATIONAL_READINESS.md:24` عیناً: «تخلیهٔ صف (۱۰۰۰ پیام) — *پیشنهاد چت ۱۰، نیازمند ACCEPT*» با معیارِ **< ۵ دقیقه**، و `:29` تصریح می‌کند S8 تنها SLOِ پیشنهادیِ **بدون منبعِ مصوب** است. ⇒ تا ACCEPTِ ناظر، لازم‌الاجرا نیست.
۱۱) **اعدادِ S1–S6 در پرامپت، اعدادِ «پیش‌ازرفع» هستند:** تطبیقِ بایت‌به‌بایت با `chaos-evidence/rto-rpo-summary.csv` انجام شد (S1 ‏376ms · S2 ‏144ms · S3 ‏9545ms · S4 ‏1353ms · S5 ‏7ms · S6 ‏28948ms — همه در شاهد ✅)، **اما** ستونِ «وضعیتِ_پس‌ازرفع_main» نشان می‌دهد RPOهای S3 (۳ نوشتِ ackشده) و S4 (۶ رویدادِ ابدیِ pending) با F1/F3 **پس‌ازرفع صفر** شده‌اند و S6 با «آدیتِ پنجرهٔ ۱۰۰٪ از دست‌رفته» همان F4 را حمل می‌کند. ⇒ نقلِ این اعداد بدون پسوندِ «پیش‌ازرفع» ممنوع (سبزِ جعلی در جهتِ معکوس: نقلِ کهنه به‌جای وضعیتِ جاری).
۱۲) **«نردبانِ سیگاری ۵،۱۰،۲۰×۶۰s» (تکرارِ یافتهٔ دور ۴):** در مخزن وجود ندارد؛ نردبانِ رسمیِ ابزار `--staircase` پیش‌فرض `50→1600×120s` است و توقفِ اضطراری `--abort-error-rate` (پیش‌فرض ۵٪).

### پاکتِ پذیرشِ بستهٔ ۵ — وضعیتِ واقعی

**۷ عددِ SLO پرامپت بی‌کم‌وکاست با جدولِ `CAPACITY_MODEL.md §۵` برابرند** (p50 < ۱۰۰ms · p95 < ۳۰۰ms · p99 < ۱s · 5xx < ۰.۱٪ ≈ ۴۳ دقیقه/ماه · کش > ۸۰٪ · sync ≥ ۹۹٫۵٪ · OTP ≥ ۹۹٫۹٪) ✅ — فقط ستونِ «نحوهٔ اندازه‌گیری»شان نامِ متریکِ نادرست دارد (بندِ ۴).
**موجودی کامل است:** ۱۰ سند (۹ در `docs/` + `NEXT_ACTIONS` در `docs/daily-reports/`) + ۳ قالب (`ESCALATION_MATRIX` · `ONCALL_HANDOFF_TEMPLATE` · `POSTMORTEM_TEMPLATE`) + `OPEN_ITEMS` + `SYNC_RELIABILITY_EVIDENCE` + `LOAD_TEST_PLAN` ⇒ شکافِ بستهٔ ۵ **محتوایی** است، نه فقدانِ سند.
**شش/هشت شکافِ بازِ بستهٔ ۵ (با دامنهٔ رفع):** گِیجِ دیسک (چت ۴) · آلارمِ صفِ دوام‌دار (چت ۴) · ادعای کهنهٔ خطِ ۳۱ (چت ۱۰) · `S8` بدونِ ACCEPT (ناظر) · انتسابِ افرادِ آن‌کال `NOT-RUN` (خودِ سند) · دقتِ نام‌های متریکِ §۵ (چت ۶/۱۰) · متنِ کهنهٔ `LOAD_TEST_PLAN` در §۶ (چت ۶) · سنجهٔ نامعتبرِ «pid در liveness» (اصلاحِ ادعا، بدونِ کارِ کد).
**مالکیتِ صریح (پابرجا بر پایهٔ سربرگِ اسناد، نه متنِ پرامپت):** بستهٔ ۵ = **چت ۱۰** · `CAPACITY_MODEL` و قالبِ `LOAD_TEST_RESULTS` = **چت ۶** · بستهٔ ۴ (عملکرد/بار) = **چت ۴** · بستهٔ ۱ (استیجینگ ملی) = چت ۸ · **چت ۵** = RTO/RPO و صحتِ دادهٔ پس از خرابی (S1–S6) ⇒ دورِ ۵ هم **کارِ کد/تست شروع نکرد** (idle · ضد Feature Factory).

### فاز ۲ — گیت‌ها + REZA + بودجه

| گیت | نتیجه |
|---|---|
| `docs-freeze-marker` | ⚠ **۱۳/۱۴ RED وراثتی** (`PR_MERGE_PLAN.md`؛ فهرستِ rc43 = `b3b44a4f…` · دیسک = `9ef9c211…`) — بی‌تغییر نسبت به دورهای ۳ و ۴؛ **لمس نشد** (بامپِ قفل مالک چت ۶) |
| `docs-stats-sync --check` | ✅ exit 0 |
| `docs-metadata` | ✅ ۱۷/۱۷ |
| `tools/docs-refs-check` (حالتِ گزارشی، مانندِ دورهای ۳ و ۴) | exit 0 · **fresh = ۴۱ موردِ وراثتی** (خارج از خطِ پایهٔ ۷۵ ردیفی) **+ ۱ موردِ تازه که همین دور ساخته بود و رفع شد** ⇒ پس از رفع، `fresh` مو‌به‌مو برابرِ والدِ `0360b37` (diff صفر). ⚠ در حالتِ `--check` این گیت روی **خودِ `main`** هم قرمز است (۴۱ مورد) ⇒ **قرمزِ وراثتی**، هم‌کلاسِ freeze |
| `secret-scan` | ✅ ۱۲/۱۲ (صفر توکن در سند) |
| `reza-mirror-check` | ✅ exit 0 — فقط چک: **۰ همگام‌سازی · ۰ اختراع · ۰ `cp`** |

**بودجهٔ §۱:** `/home/user` 115M/1524 فایل → **115M/~1533 فایل** 🟢 · `chaos-evidence` = ۹ فایل (پوششِ درون‌مخزنی) · صفر فایلِ موقتِ رهاشده · صفر ورکتریِ رهاشده (پس از کامیت/پوش prune می‌شود) · توکن-hits: **۰**.
**CI = NOT-RUN** (بیلینگ؛ RISK-O-007) — هرگز «سبز» ادعا نمی‌شود. **NOT-RUN ≠ PASS** · **VERIFIED** فقط برای تحویلِ مستنداتی؛ بستهٔ ۵ = **NOT-RUN/NOT-VERIFIED**. `DISK`/`payesh_sync_queue_depth` = **NOT-RESOLVED** · ظرفیتِ ملی **اثبات‌نشده** (سناریوها فقط drill).

### پوش + idle

یک کامیت (addِ صریحِ همین فایل) + یک پوش به شاخهٔ تازه از `main`؛ سپس `ls-remote == HEAD`. **اعلام رسمی idle (دور ۵ = بیست‌وپنجمین دور):** تا یکی از — دریافتِ سؤال از چت ۱۰ · بازشدنِ محیطِ چندنودیِ استیجینگ (بستهٔ ۱) · بریفِ مالک (گِیجِ دیسک/آلارمِ صفِ دوام‌دار/قراردادِ `pg-down`/ACCEPTِ `S8`) · حکمِ ناظر. **پس از پوش هیچ کامیتِ تازه‌ای زده نمی‌شود.**

**الحاقیهٔ اصلاحی (خودِ همین دور، پس از کامیتِ اول):** نخستین اجرای گیتِ ارجاع در حالتِ گزارشی (بدونِ `--check`) `exit 0` داد و جدولِ بالا در نسخهٔ اول «صفر ارجاعِ کهنهٔ تازه» نوشت؛ سپس با **مقابلهٔ والد (`0360b37`) با شاخهٔ خودم** معلوم شد سهمِ من **۱ موردِ تازه** بود: ارجاعِ متنی به مسیرِ ناموجودِ `NEXT_ACTIONS.md` زیرِ `docs/` — همان دامِ شناختهٔ «هر مسیرِ ناموجودی که در متن بیاید، خودش بدهیِ تازه می‌سازد». مورد رفع شد (متن بازنویسی شد تا آن مسیر را ننویسد) و `fresh` به **۴۱ = عددِ دقیقِ والد** بازگشت؛ diff والد↔شاخه صفر است. **خطِ پایه بازتولید نشد** (بازتولیدِ خطِ پایه = پنهان‌کردنِ بدهی، تصمیمِ مالک است). عددِ دقیقِ عرصه: **۱۱۳** ارجاعِ کهنه به فایلِ ناموجود در **۶۶** سند · خطِ پایه **۷۵** · تازه **۴۱** — بیشترین سهم: گزارش‌های `CHAT4_*` (۱۴)، `HANDOFF` (۵)، گزارش‌های ۲۰۲۶-۰۹-۱۳ (۸)؛ **صفر مورد از گزارشِ ۲۰۲۶-۰۹-۱۴**. درسِ روشی برای دورهای بعد: به `exit 0`ی حالتِ گزارشی اتکا نکن؛ سنجهٔ درست = **diffِ `--json` شاخه با والد** (و حالتِ `--check` تنها داوری است که `main` هم رویش قرمز است).

## چت ۵ — دور ۶: پایش وضعیت سؤالات چت ۱۰ + تحلیل شکاف‌ها (بیست‌وششمین دور idle) (ثبت: ۲۰۲۶-۰۹-۱۴)

**دامنه:** پایش trigger چت ۱۰ + تحلیل شکاف‌های رصدپذیری/آمادگی · صفر کد · صفر تستِ تازه · صفر فایلِ تازه · تک‌سناریو · **فایل‌های موقتِ نام‌برده ساخته نشدند** (تنشِ همیشگیِ پرامپت: الزامِ ساخت در برابر ممنوعیتِ «فایل تازه در `docs/` مگر گزارش روزانه»؛ پنجمین دورِ پیاپی ⇒ محتوا همین‌جاست).

### فاز ۰ — وضعیت + ضدتکرار (شاهدِ عددی)

- **هر دو PRِ چت ۵ مرج شده‌اند:** #215 (دور ۴) در ۰۹:۰۹:۵۲Z و **#217 (دور ۵) در ۰۹:۴۵:۲۳Z** (`merge_commit=49f3a4871bc3`) ⇒ `main = 49f3a48` و شاخهٔ دور ۵ پاک‌شدنی است. `fetch` پیش از کار: `origin/main = 49f3a48` = `ls-remote` (سه‌راهه برابر؛ همچنان با تزریقِ URL+T1 چون remote نام‌دار در `p2` وجود ندارد).
- **trigger چت ۱۰ = NOT-RECEIVED** (پنجمین دورِ پایشِ پیاپی): `issues?state=open` بدونِ PR = **۰** · برچسب `chat10` = **۰** · `#184` = **۳ کامنت**، آخرین `2026-09-14T05:51:51Z` (rezaa2544) ⇒ فاز ۰.۵ اجرا نشد.
- **وضعیتِ دیگران (فقط پایش، صفر لمس):** PRهای باز = **#218** (`docs/daily-reports-2026-09-14` — **همان فایلی که این دور به آن الحاق می‌کند**؛ `mergeable_state=dirty` ⇒ اتحادِ همین فایل یک ریسکِ ادغامِ شناخته است؛ سابقهٔ رفع: کامیت `8ccba0f` «union گزارش روزانه») · #216 (چت ۷) · #211. شاخهٔ هیچ‌کس لمس نشد.
- **ضدتکرار:** صفر بازاجرای دور ۵ · صفر سوئیتِ آشوب/عملکرد · گیت‌های docs **یک‌بار** روی درختِ نهایی (فاز ۲) · **یک** اجرای تازهٔ `tests/observability-config.js` (دورهای پیش آن را اجرا نکرده بودند؛ شاهدِ ادعای همین دور) · صفر کد/تست · `p2` کانونیکال دست‌نخورده (`dirty=21`ِ دیگری، `9ad3b08`).

### ⚠ دو عقب‌رفتِ علمیِ همین دور — اصلاحِ یافته‌های خودم (نه دیگران)

**R1 — ادعای «کوریِ آلارم به صفِ دوام‌دار» (دورهای ۴ و ۵) باطل است.**
`infra/observability/alert-rules.yml:42-48` عیناً دارد: `alert: SyncQueueDepth` · `expr: payesh_sync_queue_depth > 1000` · `for: 5m` · `severity: warning` — یعنی قاعدهٔ زنده روی **همان گِیجِ صفِ دوام‌دار** وجود دارد. این فایل **همان فایلی است که واقعاً بار می‌شود** (`prometheus.yml:20-21` → `rule_files: /etc/prometheus/alert-rules.yml`؛ `docker-compose.observability.yml:23-24` فقط `prometheus.yml` و `alert-rules.yml` را mount می‌کند) و دو قفلِ تستی هم آن را pin کرده‌اند (`tests/observability-config.js:52` برای `rule_files` و `:62` برای همین قاعده و آستانه). **ریشهٔ خطای من:** فقط `alerts.yml` را اسکن کردم و از وجود **فایلِ قاعدهٔ دوم** غافل شدم. ⇒ **وضعیت F3 (تخلیهٔ صفِ دوام‌دار در پایش): پوشش = بسته.** تنها نکتهٔ باقی‌مانده کالیبراسیون است نه کوری: آستانهٔ زنده **۱۰۰۰** است (برابرِ سقفِ outbox) در حالی که قاعدهٔ خواهرِ درون‌پروسه‌ای در فایلِ نابارشده آستانهٔ **۵۰۰** دارد.

**R2 — ادعای «صفر آلارم دیسک ⇒ پذیرشِ disk-full غیرقابل‌ارضا (F4)» (دورهای ۴ و ۵) نادرست/اغراق‌آمیز بود.**
درست: در متریک‌های برنامه گِیجِ دیسک نیست و در **هیچ‌یک** از دو فایلِ قاعده هم قاعدهٔ دیسک نیست (`disk|WALDisk` = ۰ در هر دو) — **اما این غیبت عامدانه و مستند است**: `docs/OBSERVABILITY.md:175-198` دو آلارمِ **بیرونی** را با عبارتِ کامل، آستانه و شدت ثبت کرده — `WALDiskHigh` (پُریِ `pg_wal` > ۷۰٪، warning) و `WALDiskCritical` (> ۸۵٪، P0) روی `node_filesystem_avail_bytes`/`node_filesystem_size_bytes`، به‌همراه **جایگزینِ بدونِ node_exporter** (`pg_stat_wal` / `pg_wal_lsn_diff`). ریشه‌اش `docs/WAVE19_WAL_DRILL_REPORT.md:120` است («آلارمِ دیسکِ WAL باید **بیرونی** باشد») و علتِ بیرون‌نگه‌داشتنش صریحاً «حفظِ ضدرانشِ سه‌جانبهٔ متریک‌های خودِ برنامه» است (`OBSERVABILITY.md:56` · `BUG_HUNT_REPORT.md:418-424`). ⇒ ادعای درست: **قاعده در مخزن و به‌شکلِ داخلی وجود ندارد؛ طراحی‌اش بیرونی است و «فعال‌بودنِ استقرار‌یافته‌اش» در مخزن قابلِ اثبات نیست (NOT-DEPLOYED)** — نه سبزِ جعلی، نه قرمزِ جعلی.

### یافتهٔ تازهٔ همین دور — دو نسل قاعده‌فایل: تنها ۱۰ از ۳۴ قاعده بار می‌شود

| فایل | قواعد | وضعیتِ اجرا | ارجاع‌دهندگان |
|---|---|---|---|
| `alert-rules.yml` | **۱۰** (نام‌های بی‌پیشوند: `HighErrorRate` … `SuspiciousSession`) | ✅ **بارشده** (`prometheus.yml:21` + mountِ compose + قفلِ `observability-config` ۵۹/۵۹) | `tests/observability-config.js` · `docs/BUG_HUNT_REPORT.md` |
| `alerts.yml` | **۲۴** (همه با پیشوندِ `Payesh`) | ❌ **هیچ بارکننده‌ای در مخزن ندارد** | `infra/observability/README.md:9` · `docs/WAVE14_OBSERVABILITY.md:178,180` · `tests/wave14-observability.js:475,483` |

- در میان ۲۴ قاعدهٔ **نابارشده**: `CriticalErrorBudgetBurn` · `PayeshLatencyP99Breach` · `PayeshCacheHitRateLow` · `PayeshInstanceDown` · **`PayeshMetricsAbsent`** (اسکریپ‌داون) · `PayeshOutboxBacklog` · `PayeshDbPoolSaturated` · `PayeshRestartLoop` · `PayeshCardinalityGuardFiring` … — یعنی همان‌هایی که `docs/WAVE14_OBSERVABILITY.md:180` به‌عنوانِ پوششِ زنده فهرست کرده است.
- در فایلِ **بارشده** هیچ قاعدهٔ «هدفِ پایین» نیست: `absent(` = **۰** و `up == 0` (به‌جز `payesh_redis_up`) = **۰** ⇒ اگر scrape/هدف از کار بیفتد، مجموعهٔ زنده آلارم ندارد.
- چرا گیت‌ها این را نمی‌گیرند: هر تست فقط **یکی** از دو فایل را می‌سنجد (`wave14-observability` → `alerts.yml` · `observability-config` → `alert-rules.yml`) ⇒ هر دو سبز می‌مانند و واگرایی دیده نمی‌شود.
- ⚠ عدم‌قطعیتِ صریح: اگر یک استقرارِ **بیرونِ مخزن** `alerts.yml` را بار کند، این شکاف بی‌اثر است؛ در مخزن شاهدی بر چنین بارکردنی نیست ⇒ **KNOWN UNKNOWN** تا تأییدِ مالکِ استقرار.

### ماتریسِ پوششِ ۷ SLO ↔ قاعده‌های زندهٔ استکِ مرجع (`CAPACITY_MODEL.md §۵`)

| SLO | قاعدهٔ زنده در `alert-rules.yml` | حکم |
|---|---|---|
| p95 < ۳۰۰ms | `HighLatency` (`histogram_quantile(0.95) > 0.300`, 5m) | ✅ |
| 5xx < ۰٫۱٪ | `HighErrorRate` (نسبت > ۰٫۰۰۱، 2m) | ✅ |
| sync ≥ ۹۹٫۵٪ | فقط **پروکسیِ عمق**: `SyncQueueDepth` (>۱۰۰۰) | ⚠ قاعدهٔ **نرخِ موفقیت** در هیچ‌کدام از دو فایل نیست؛ داده‌اش موجود است: `payesh_sync_requests_total{code}` (`metrics.js:447`) و `payesh_worker_events_total{outcome}` (`:487`) |
| p99 < ۱s | — (فقط `PayeshLatencyP99Breach` در فایلِ نابارشده) | ⚠ |
| نرخ اصابت کش > ۸۰٪ | — (فقط `PayeshCacheHitRateLow` در فایلِ نابارشده) | ⚠ |
| p50 < ۱۰۰ms | — (در **هیچ** فایلی نیست) | ❌ |
| دسترس‌پذیری OTP ≥ ۹۹٫۹٪ | — (`PayeshOtpFlood` نرخِ **سوءاستفاده** است، نه دسترس‌پذیری) | ❌ |

### راستی‌آزماییِ بلوکِ «Evidence» پرامپت (Value Gate: نقلِ شاهد هم شاهد می‌خواهد)

| نقلِ پرامپت | واقعیتِ مخزن | حکم |
|---|---|---|
| «`metrics.js:573: const queueDepth = await pgClient.query('SELECT COUNT(*) FROM server_sync_queue')`» | جدولِ `server_sync_queue` در **کلِ مخزن ۰ بار** وجود دارد؛ کوئریِ واقعی `metrics.js:572` روی **`server_outbox WHERE status = 'pending'`** است و خطِ ۵۷۳ خودِ `registry.set('payesh_sync_queue_depth', …)` است | ❌ **شاهدِ ساختگی** (نه فقط نقلِ نادرست: جدولِ ناموجود) |
| «`grep -r "disk\|space" infra/observability/` خروجی خالی» | ✅ درست (۱۶ فایل، همه ۰) — و همان‌طور روی `server/metrics.js` = ۰ | ✅ |
| «`sed -n '24p' docs/OPERATIONAL_READINESS.md` → S8 نیازمند ACCEPT» | ✅ محتوا درست (ردیفِ جدول: ۱۰۰۰ پیام · < ۵ دقیقه · *پیشنهاد چت ۱۰، نیازمند ACCEPT*) ولی **نقلِ آزاد** است نه بایت‌به‌بایت | ✅ با قید |
| «`13/14 successful (1 failed)`» و «`41 new old references found`» | ماهیت ✅ (واقعی: «نتیجه: ۱۳ موفق / ۱ ناموفق (از ۱۴) · نامِ متهم `PR_MERGE_PLAN.md`» و «۱۱۳ مورد در ۶۵ سند · خطِ پایه ۷۵ · تازه ۴۱ · تولیدی ۷ · رفع‌شده ۳») اما **صورت‌بندیِ انگلیسیِ نقل‌شده وجود ندارد** | ⚠ نقلِ بازنویسی‌شده — خروجیِ ابزار باید عینِ متنِ خودش نقل شود |

**دو تصحیح روی جدولِ «مغایرت‌ها»ی پرامپت:** ① «اعداد S1–S6 … → NOT-VERIFIED» درست نیست؛ آن اعداد **همان اندازه‌گیریِ واقعیِ پیش‌ازرفع**اند (بایت‌به‌بایت با `chaos-evidence/rto-rpo-summary.csv` می‌خوانند) ⇒ VERIFIED به‌عنوانِ اندازه‌گیری، با **برچسبِ اجباریِ «پیش‌ازرفع»**. ② ردیفِ ارجاع‌ها: عبارتِ درست «fresh = ۴۱ **وراثتی** و سهمِ این دور = **۰**» به‌علاوهٔ اینکه **`--check` روی خودِ `main` هم قرمز است** ⇒ قرمزِ وراثتی، نه «۴۱ موردِ تازه».

**نقشهٔ مالکیتِ پرامپت (§۳) شاهد ندارد:** عبارتِ «Evidence Factory» در مخزن **۰** ارجاع دارد و هیچ رجیستریِ «بستهٔ ۱..۹ + مالک» وجود ندارد (`docs/BUNDLE_REGISTRY.md` رجیستریِ **فایل‌های `.bundle` برای حذفِ ایمن** است، نه مالکیتِ بسته‌های کاری). دو ردیفش با سربرگِ اسناد ناسازگار است: «بستهٔ ۲ (Capacity Modeling) = چت ۲» در برابر `CAPACITY_MODEL.md:3` = **چت ۶**، و «بستهٔ ۳ (Observability) = چت ۲» / «بستهٔ ۹ (Observability) = چت ۹» (برچسبِ تکراری). خودِ پرامپت هم در یک سند متناقض است: در نقش «مالکِ بستهٔ ۵» و در §۵ «بستهٔ ۵ = چت ۱۰». **تنها شاهدِ قابلِ اتکا: سربرگِ خودِ اسناد + `tests/observability-config.js`.**

### وضعیتِ دوباره‌سنجی‌شدهٔ بستهٔ ۵

- **باطل‌شده در این دور (۲):** «کوریِ آلارمِ صفِ دوام‌دار» (R1 — پوشش بسته است) و «فقدانِ آلارمِ دیسک به‌مثابه شکافِ مخزن» (R2 — طراحیِ بیرونیِ مستند).
- **باز مانده (۶):** ① استقرارِ اثبات‌نشدهٔ آلارمِ بیرونیِ دیسک (`NOT-DEPLOYED`) ② ادعای کهنهٔ `OPERATIONAL_READINESS.md:31` (چت ۱۰) ③ `S8` بدونِ `ACCEPT` (ناظر) ④ انتسابِ افرادِ آن‌کال `NOT-RUN` (خودِ سند) ⑤ نام‌های متریکِ نادرستِ §۵ (`payesh_http_request_duration` → `_seconds`؛ `payesh_cache_hit_rate` وجود ندارد) ⑥ متنِ کهنهٔ §۶ («`LOAD_TEST_PLAN.md` در دست ساخت»).
- **شکافِ تازه (۱، یافتهٔ N1):** ۲۴ قاعدهٔ مستندِ `alerts.yml` در استکِ مرجع **بار نمی‌شود** ⇒ ادعاهای «آلارمِ error-budget/کش/p99/scrape-down داریم» در وضعیتِ فعلیِ پیکربندیِ مخزن **قابلِ خروش نیستند** (مالکِ رفع: مالکِ رصدپذیری/چت ۴ با تصمیمِ ناظر؛ چت ۵ صفر کد زد).
- **حکم:** بستهٔ ۵ = **NOT-RUN / NOT-VERIFIED** (نیازمندِ اجرای میدانی در استیجینگ + ACCEPTِ S8) · ظرفیتِ ملی اثبات‌نشده (drill فقط) · CI = NOT-RUN (بیلینگ) · **NOT-RUN ≠ PASS**.

### فاز ۲ — گیت‌ها + REZA + بودجه

| گیت | نتیجه |
|---|---|
| `docs-freeze-marker` | ⚠ **۱۳/۱۴ RED وراثتی** — «نتیجه: ۱۳ موفق / ۱ ناموفق (از ۱۴)» · «اثر همهٔ اسناد یخ‌زده با فهرست یکی است — `PR_MERGE_PLAN.md`» (بی‌تغییر نسبت به دورهای ۳–۵؛ لمس نشد — بامپِ قفل مالکِ چت ۶) |
| `docs-stats-sync --check` | ✅ exit 0 |
| `docs-metadata` | ✅ ۱۷/۱۷ |
| `tools/docs-refs-check` (حالتِ گزارشی) | exit 0 · «۱۱۳ مورد در ۶۵ سند · خطِ پایه ۷۵ · **تازه ۴۱** · تولیدی ۷ · از خطِ پایه رفع‌شده ۳» ⇒ **سهمِ این دور = ۰** (سنجه: diffِ `--json` شاخه با والد `49f3a48`) · ⚠ در حالتِ `--check` روی خودِ `main` هم قرمز ⇒ قرمزِ وراثتی |
| `secret-scan` | ✅ **۱۲/۱۲** (صفر توکن در سند) |
| `reza-mirror-check` | ✅ exit 0 — فقط چک: **۰ همگام‌سازی · ۰ اختراع · ۰ `cp`** |
| `tests/observability-config.js` | ✅ **۵۹/۵۹** (یک اجرا؛ شاهدِ ادعای N1: `rule_files → alert-rules.yml` و طبقهٔ ۱۰ قاعده) |

**بودجهٔ §۱:** `/home/user` 115M/1531 فایل → **115M/1540 فایل** 🟢 · `chaos-evidence` = ۹ فایل · صفر فایلِ موقتِ رهاشده (سه فایلِ نام‌برده ساخته نشد) · ورکتری پس از کامیت/پوش حذف می‌شود · **توکن-hits: ۰**.

### پوش + idle

یک کامیت (addِ صریح) + یک پوش به شاخهٔ تازه از `main`؛ سپس `ls-remote == HEAD`. **اعلام رسمی idle (دور ۶ = بیست‌وششمین دور):** تا یکی از — دریافتِ سؤالِ چت ۱۰ · بازشدنِ استیجینگِ چندنودی (بستهٔ ۱) · بریفِ مالک (استقرارِ آلارمِ بیرونیِ دیسک / ACCEPTِ `S8` / قراردادِ `pg-down`) · حکمِ ناظر. پس از پوش هیچ کامیتِ تازه‌ای زده نمی‌شود.
