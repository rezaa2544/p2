# Chat6 — Mission Report — C6-01 Release baseline

**Mission ID:** `C6-01` (Board `docs/daily-mission-boards/2026-09-14/Chat6.md`, exactly 20 rows C6-01…C6-20)
**Board line (verbatim):** "Release baseline | Inventory current release/merge evidence."
**Arena / Chat:** Chat6 — Release / database / merge control
**Session branch:** `arena/01a0a658-p2`
**Date:** 2026-09-15 (~19:20–19:50 UTC)
**Status:** **EXECUTED — REPORTED — COMMITTED — PUSHED — PR OPEN (merge per §8 decision below)**
**National GO/NO-GO:** NOT DECLARED (not an authority of this Arena)
**P0/P1:** none created, renamed, closed, or re-numbered (P0-1 read-only, §1.4)

---

## 0. Base verification & drift (mandatory first step)

| # | Check (exact command) | Result |
|---|---|---|
| 1 | Packet BASE | `8feb9e3d168e27a03c9055e5887ed1a7ae504e78` |
| 2 | `git ls-remote origin refs/heads/main` | **`5e2e0e22e5184249ab4e948941186c3232817903`** |
| 3 | **DRIFT: packet base is 3 commits behind current main** | `675a9468` (Merge #263, Chat10 — adds `docs/daily-reports/Chat10/2026-09-15.md`), `f5b95d16` (Merge #261, Chat3 — modifies `docs/daily-reports/Chat3/2026-09-14.md`), `5e2e0e22` (Chat10 R10 receipts, #264 — modifies `docs/daily-reports/Chat10/2026-09-15.md`). **All three docs-only (nested report files); no code, freeze, gate, or manifest changes.** File lists verified via GitHub API per commit. |
| 4 | Session-branch integration | Branch was forked at `e3893f3b` (11 commits behind at session start, per prior-session record); before measurement `origin/main` (`5e2e0e22`) was merged into the session branch → merge commit `1553df5` (clean, no conflicts; Chat7 content already on main via #259 auto-resolved). All measurements in §1–§2 are on this merged tree = current main content + one unmerged Chat6 file (cf. F2). |
| 5 | Shallow clone | `.git/shallow` present ⇒ **full commit-ancestry checks NOT-RUN (environment)**; remote history via `gh api`. |

Canonical paths per packet: `docs/*.md` directly; `docs/control-plane/` does not exist and
`CHAT1_CONTROL_PLANE.md` exists under no path (re-verified `find` in prior session) — **neither
was created**, per instruction.

## 1. Release baseline — inventory of current release/merge evidence

### 1.1 Freeze lock (rc44)

- Current freeze: `docs/DOCS_FREEZE_v1.0.0-rc44.md`; guard reads `const FREEZE` from
  `tests/docs-freeze-marker.js` (NOT `tools/` — the `tools/` path does not exist; using it
  yields MODULE_NOT_FOUND which a piped `tail` misreads as exit 0 — the exact class of error
  Chat7 retracted in #254. Tool path recorded with every exit below).
- rc44 landed on main via PR **#241** (commit `025c2f7e`, 2026-09-15 10:37Z) — "bump lock
  rc43 → rc44; version guard rc-agnostic, anchored on the title line". Per packet: this
  lifted Chat7's C7-12 blocker — **recorded only; C7-12 is Chat7's mission, not this Arena's.**
- Resync history: rc44 manifest resynced via `tools/docs-stats-sync.js --freeze` in #243
  (`915afd47`) and #244 (`dfa863e4`) — both Chat6 deliveries, MERGED.
- **No bump / no `--freeze` performed in this Mission** (forbidden scope).

### 1.2 Merge queue (open PRs, measured via `gh pr list` / `gh pr view`)

| PR | Branch | Owner (per title) | State |
|---|---|---|---|
| #265 | `arena/01a0a659-p2` | Chat4 (C4-01 Redis baseline report) | OPEN, MERGEABLE, UNSTABLE |
| #262 | `arena/01a0a658-p2` | **Chat6 — this session** (state-recovery record; head moves with this C6-01 delivery) | OPEN, MERGEABLE, UNSTABLE |
| #260 | `arena/01a0a656-p2` | Chat9 (state-recovery hand-back) | OPEN, MERGEABLE, UNSTABLE |
| #258 | `arena/01a0a651-p2` | audit escalation (session 01a0a651) | OPEN, MERGEABLE, UNSTABLE |
| #257 | `arena/01a0a64f-p2` | Chat1 (shift state-recovery) | OPEN, MERGEABLE, UNSTABLE |
| #245–#253 | `alert-autofix-*` | **foreign** — GitHub CodeQL auto-fix DRAFT PRs (9) | DRAFT, untouched |

- `UNSTABLE` on all five Arena PRs = failing CI checks; consistent with the known main-line
  CodeQL SARIF rejection + billing constraint (RISK-O-007 per `docs/daily-reports/2026-09-14.md`;
  CodeQL red re-traced by Chat7 in `4a21074`). **NOT re-adjudicated here** (merge-hygiene
  ownership: Chat7/Chat1).
- Merged today (context): #255 (Chat3), #256 (Chat3 R10/R11 → `8feb9e3d` = packet base),
  #259 (**this session branch's** Chat7 recovery commit `4a21074` → squash `ec659dd0` — see F4),
  #261 (Chat3 R9.1), #263 (Chat10), #264 (Chat10 receipts → `5e2e0e22`).
- **No PR of another Arena was opened, modified, or closed by this Mission.**

### 1.3 Release gates (measured this Mission, merged tree = current main + unmerged #262 file; raw exits, no pipes)

| Gate (exact path) | Raw exit | Result |
|---|---|---|
| `node tests/docs-freeze-marker.js --check` | **0** | 14/14 ✅ "قفل مستندات معتبر است" — matches Chat1's measurement |
| `node tools/docs-stats-sync.js --check` | **1** | ❌ this branch tree: 420→**422**; **main alone: 420→421** (independently re-measured from a detached `5e2e0e22` worktree, tool run from inside the worktree). Chat1's packet states "exit 0 · 420 · green" — **does not reproduce; see F2.** |
| `node tools/docs-refs-check.js` | **1** | ❌ **73 fresh stale refs** — matches Chat1. Top offenders: 4 docs → `docs/control-plane/CHAT1_CONTROL_PLANE.md` (stale path, F5), 3 docs → `tests/summer-mutations.js`. **Not fixed; `tools/docs-refs-baseline.json` untouched** (ownership: Chat7/owner decision, per packet). |
| `node tests/secret-scan.js` | **0** | 12/12 ✅ — matches Chat1 |
| `node tools/config-audit.js` | **0** | 155 code vars / 176 doc vars; **0 fatal**; 25 advisory ("in doc, not in code" — infra-only: `ELASTIC_*`, `GRAFANA_*`, `MINIO_*`, `PGHA_*`, `POSTGRES_*`, etc.); "هیچ متغیر کدی بی‌سند نیست" |
| `node tests/config-audit.js` | **0** | 7/7 ✅ (tool test: exit-0 on repo + negative case + counts) |
| `node tests/config-reference-coverage.js` | **0** | 79/79 ✅ (reference contract: sections + documented vars + validation/failure rules) |

### 1.4 National / P0 context (read-only)

- National status per canonical control docs: **NO-GO** (stated as fact, not re-declared).
- `docs/P0_BLOCKER_TRACKER.md` → **پ0-1** (PostgreSQL sole source of truth, Wave-1 final
  merge): status line as-is = "🔴 بلاک‌شده — منتظر تصمیم ناظر + ادغام" (blocked — awaiting
  supervisor decision + merge; owner: Chat1 + Chat5 quality gate; resolution path 1/5).
  **No status changed.**

## 2. Mandatory part — disposition of #243/#244 against board items C6-02/03/04

Context: #243 (`915afd47`, merged 11:24Z) = "Chat6 bundle2a — refs-silence fail-fast +
P1 config-audit sh-local filter + P3 DOCS_INDEX orphan ref; rc44 manifest resynced";
#244 (`dfa863e4`, merged 13:16Z) = "CONFIGURATION_REFERENCE §2.7 auto-audit table: 48 env
vars extracted from code (Chat6); resolves all config-audit fatal items; rc44 manifest
resynced via --freeze". Chat1 rates the three items **CLAIMED-PARTIAL**; per-item verdicts
below, evidence-only (no new implementation).

### C6-02 — "Config audit diagnosis | Reproduce config-audit and classify false positives vs real gaps." → **MET**

| Evidence | Detail |
|---|---|
| Reproduction | #244 body records the BASE reproduction (temp worktree, BASE `915afd47`): `config-audit` **EXIT 1, 48 fatal + 25 advisory** pre-patch → **EXIT 0, 0 fatal + 25 advisory** post-patch — raw exits recorded in the PR, not chat claim |
| Classification | the 48 fatal = code vars undocumented (real gaps — documented by #244 §2.7); the 25 advisory = doc-only infra vars ("فقط-زیرساخت بودن را تأیید کنید" — classify-confirm class); shell-locals in `*.sh` = false positives (classified and filtered by #243 P1) |
| Live re-verification (this Mission) | `node tools/config-audit.js` on merged tree → **EXIT 0**, 0 fatal, exactly 25 advisory (list in §1.3); `node tests/config-audit.js` → 7/7 EXIT 0 |

### C6-03 — "Config scanner hardening | Fix shell-variable false positives with focused tests." → **PARTIAL**

| Part | Verdict | Evidence |
|---|---|---|
| Fix shell-variable false positives | ✅ done, shipped | #243 `tools/config-audit.js` (+11/−1): `shLocalsFile` set built from non-`export` vars in `*.sh` (lines 21/43) and excluded from `inCodeNotDoc` (line 64) — **verified present on current main**; live `config-audit` EXIT 0 with filter in place |
| **with focused tests** | ❌ missing | #243's file list contains **no test file** (4 files: 2 tools, 2 docs). `tests/config-audit.js` predates the fix (only commit: `3e5fae22`, 2026-09-11) and tests tool exit/negative-case/counts — it does **not** pin the sh-local exclusion (grep of `tests/` for `sh-local`/`shLocal`: 0 matches). No other test exercises the filter |

**Verdict: PARTIAL** — the fix is implemented and effective (live green), but the board's
acceptance clause "with focused tests" is not evidenced. Closing the gap = one focused test
(no implementation change to the tool itself) — candidate next mission, §6.

### C6-04 — "Config reference completion | Document verified missing runtime configuration variables." → **MET**

| Evidence | Detail |
|---|---|
| Documentation | #244 `docs/CONFIGURATION_REFERENCE.md` (+56): new section at line 200 "### ۲.۷ متغیرهای استخراج‌شده از کد — ممیزیِ خودکار (چت ۶)"; **exactly 48 rows verified on current main** (counted this Mission, no duplicate names), each row with consumption site (e.g. `` `server/db.js` ``); matches #244 body (PAYESH_* ×33, PGDRILL_* ×6, PG_* ×3, WAVE23_* ×5, ALLOW_MEMORY_FALLBACK) |
| rc44 manifest | `docs/DOCS_FREEZE_v1.0.0-rc44.md` (+1/−1) resynced via `--freeze` in #244 (frozen doc touched ⇒ resync mandatory, done) |
| Live verification | `node tools/config-audit.js` EXIT 0 (all code vars documented — the 48 are exactly the former fatal set); `node tests/config-reference-coverage.js` 79/79 EXIT 0 |

## 3. Findings

- **F1 (P3, doc-consistency, candidate C6-17):** `docs/CONFIGURATION_REFERENCE.md` has a
  **duplicated `### ۲.۷` heading** — line 106 ("رصدپذیری و تریسینگ") and line 200
  ("متغیرهای استخراج‌شده از کد", added by #244) — plus 2.12 precedes 2.11 (lines 156/178).
  Cosmetic numbering drift, no content loss. Not fixed under C6-01 (evidence-only scope).
- **F2 (P2, gate-discrepancy, for Chat1 re-measurement):** Chat1's packet records
  `tools/docs-stats-sync.js --check` = "exit 0 · 420 · green" on main. Direct measurements:
  **main alone (`5e2e0e22`, detached worktree, tool run from inside it) = EXIT 1, 420→421**
  (drift since #255, which added `docs/daily-audits/2026-09-15-session-01a0a653-state-recovery.md`
  — a counted top-level subfolder doc); this session's tree = 420→422 (main + this
  session's unmerged `#262` record file, also a counted daily-audits doc). The written
  rc44 manifest (420) is now stale by +1 on main. **Consequence for Chat1:** if PR #262
  merges before the rc-resync Mission, main becomes 422. Fix = rc bump + `--freeze`
  resync (Chat6 domain; forbidden here; requires a Mission). This session does not
  adjudicate which measurement is "the" gate state — raw exits are recorded for both.
- **F3 (P2, evidence gap — recorded, not backfilled):** `docs/daily-reports/Chat6/` did not
  exist on main before this report. The 2026-09-14 Chat6 M1–M4 queue (ACTIVE.md) was never
  delivered as a report (prior-session B5), and the prior session's escalation commit
  `1147370` (on `arena/01a0a647-p2`, which also carries two Chat5 commits, no PR) remains
  **undelivered to main** — landing decision = Chat1. This report creates the directory per
  the Mission's explicit instruction; **no 2026-09-14 content was backfilled.**
- **F4 (informational, closure of prior-session C1):** prior-session record
  (`docs/daily-audits/2026-09-15-session-01a0a658-state-recovery.md`, on this branch via
  `cfbe23d`) escalated an identity contradiction (packet CHAT 6 vs branch tip `4a21074`
  CHAT 7). That Chat7 commit is **merged on main via PR #259** (squash `ec659dd0`, 18:42Z)
  and the current packet explicitly assigns **CHAT 6** to this session ⇒ **C1 adjudicated by
  the Coordinator; recorded here for traceability.** Chat7's work is referenced as evidence
  only and was not modified.
- **F5 (informational):** the top refs-check offender (4 docs) is the stale
  `docs/control-plane/CHAT1_CONTROL_PLANE.md` path that the assignment packet itself still
  cites in the wild (B3 in prior records). Per packet: **not fixed, baseline untouched**
  (Chat7/owner ownership). Packet's path correction ("canonical = `docs/*.md` directly; do
  not create them") is accepted and followed.

## 4. NOT-RUN (exact reasons)

| Item | Reason |
|---|---|
| refs-check fix + `tools/docs-refs-baseline.json` | Forbidden by Mission (ownership Chat7/owner) |
| Freeze rc bump / `--freeze` resync | Forbidden by Mission (F2 documents the need; requires a Chat1-issued Mission) |
| Any P0/P1 status change / Roadmap change | Forbidden (governance) |
| National GO/NO-GO | Not this Arena's authority |
| Product / release-database test suites | No code delta (evidence-only Mission); the 7 gate/test runs in §1.3 are the applicable verification |
| CI | No code change in this Mission; CI availability separately constrained (billing, RISK-O-007); PR `UNSTABLE` state reported as-is (§1.2) |
| Full commit-ancestry verification | Shallow clone (depth 2) — environment limitation; remote evidence via `gh api` |
| Board items C6-05…C6-20 | Not assigned; no self-selection (protocol) |

## 5. Scope / files / Git discipline of THIS Mission

- **Files changed:** `docs/daily-reports/Chat6/2026-09-15.md` (this file, new; the only file
  of this Mission). No code. No frozen doc. No other Arena's file/branch/PR touched.
- **`git add -A`:** not used — single explicit path; `git diff --stat` / `--name-status`
  reviewed pre-commit. **Force-push:** none. **History rewrite:** none (plain commit on the
  session branch after a clean integration merge of current main).
- **Merge decision (ARENA_EXECUTION_MODEL.md §8):** self-merge **not** exercised — the
  delivery contains a docs-stats drift (F2) whose proper fix is a separate Chat6 Mission,
  and merge disposition of the stacked session-recovery records (#262 head) is Chat1's
  call. ⇒ **PR OPEN, NOT-MERGED, by deliberate decision, with reason recorded.**

## 6. Suggested next action (Chat1 decides)

1. **C6-03 closure:** one focused test pinning the config-audit sh-local false-positive
   filter (completes PARTIAL → MET; tool itself unchanged).
2. **F2 rc-resync Mission** (Chat6 domain): rc bump + `--freeze` resync of
   `DOCS_METRICS.md`/`DOCUMENTATION_MAP.md`/rc44 manifest — needed before main's
   docs-stats gate goes green; Chat1's re-measurement of F2 first.
3. Alternatively **C6-05** (migration audit) per board order.
4. Dispositions carried: F1 (dup 2.7 heading → C6-17 candidate), F3 (queue re-issue vs
   archive; `arena/01a0a647-p2` landing), B4 PAT rotation (owner).

**Board items NOT started beyond C6-01** — next Mission only on Chat1 assignment.

---

# Mission Report — C6-03 (STEP B: the test half) + STEP C read-only decision package — 2026-09-15 (append)

**Mission ID:** `C6-03` — "Config scanner hardening | Fix shell-variable false positives with focused tests." — executing the remaining half per Chat1 ruling #2 (filter shipped and effective in `915afd47`; missing piece = focused test). C6-01 = **VERIFIED ON MAIN** per ruling #1 (PR #262 merged, merge commit `0f30a1e7`, report at `d41dcbb` present on main — re-verified this session via `git show origin/main:docs/daily-reports/Chat6/2026-09-15.md` = 190 lines before this append).

**BASE (re-verified):** `git ls-remote origin refs/heads/main` = **`b5360fe744b61a51967e9c0f9c668dcc1222aa48`**. Packet's reference `328bdedf` is ~8 commits behind (drift commits: `678d7984` Chat1 R4, `7017033f` Chat4 C4-02 #269, `0f963c85` Merge #268, `9819e27d`/`6a67b834` CodeQL #270, 9 CodeQL autofix merges `370dfe43`…`bc0107b0`, `33afce8b` Chat9 C9-17, `b5360fe7` Merge #271 — all docs/reports + .github workflow files + code-scan fixes). All numbers below carry BASE `b5360fe7` unless another SHA is stated.

## C6-03 / 0. Environment incident (F6, recorded — no delivered content affected)

The sandbox was re-provisioned mid-shift with a fresh shallow clone (reflog: only `clone` + `checkout`; fetch refspec restricted to `main` only; double shallow grafts at `b5360fe7` + `e3893f3b`). Consequences handled, all verified:
- Local branch pointer was at `e3893f3b` while the **remote** branch held `d41dcbb` (verified by `ls-remote` before any local op); all prior-session work is intact on the remote.
- Worktree residue from the graft-blind checkout was **byte-verified** against remote content (e.g. Chat7 report residue == `4a21074` version, hash-compared) ⇒ **no unique local data**; nothing discarded that wasn't on the remote.
- Recovery was **forward-only**: fetch branch ref → `reset --hard` to remote tip `d41dcbb` → fast-forward merge of `origin/main` (`d41dcbb` is an ancestor of `b5360fe7` via `0f30a1e7`; 49 commits) → `git fetch --unshallow` (full history, 1929 commits) — no force-push, no rewrite of pushed history.
- Lesson (echoes ruling #1): checkout state can silently diverge from remote state; `ls-remote` before and after, hash-compare residue, before acting.

## C6-03 / 1. STEP B — focused test for the sh-local filter (acceptance results)

**File changed (only code file):** `tests/config-audit.js` — **extended in place (7 → 15 checks); no new test file created** (per Chat1's strong preference — the official test count 504 = 496 root + 8 API is untouched; verified: `tests/` root file count unchanged). **`tools/config-audit.js` logic UNCHANGED** (sha256 proof in §2 below).

New checks (section "■ فیلتر sh-local (پینِ مثبت‌های کاذب — C6-03)"):
1. **Non-vacuous pin (repo level):** the test independently recomputes S = {non-`export` `.sh` locals that are read via the `${V:-` default pattern, are undocumented, and are not in the tool's test-local exception set}. At BASE `b5360fe7`, **S = {`DUPNUMS`}** (read-only measurement) — the pin is non-vacuous; if the filter is absent, `DUPNUMS` becomes fatal and the suite fails.
2. **S-members-not-fatal:** every S member must be absent from the repo tool's fatal output (the filter excludes it).
3. **Hermetic fixtures (tmpdir, byte-identical copy of the real tool, hash-verified at run time):**
   - **A (positive):** `.sh` with `A_LOCAL_TESTVAR=1` (no export) + `${A_LOCAL_TESTVAR:-d}` → tool EXIT 0, var absent from output (filter works).
   - **B (no over-exclusion):** `export B_EXPORT_TESTVAR=1` + `${B_EXPORT_TESTVAR:-d}` → tool EXIT 1, var IS fatal (the filter must exclude only non-export locals).
   - **C (bite, self-contained):** a second tool copy with exactly the clause ` && !shLocalsFile.has(v)` removed (repo tool untouched) → fixture A becomes fatal, EXIT 1 (the test detects the filter's absence).
   - tmpdir removed in `finally`.

### Acceptance evidence (raw outputs, BASE `b5360fe7`)

| # | Acceptance | Result |
|---|---|---|
| 1 | `node tests/config-audit.js` with new assertions | **EXIT 0 — 15/15** (7 original + 8 new; raw output preserved in session log) |
| 2 | `node tools/config-audit.js` unchanged behavior | **EXIT 0** — 155 code vars / 176 doc vars, **0 fatal**, 25 advisory (infra-only list) |
| 3 | "The test bites": temporary filter removal ⇒ suite FAILS | **Proven, repo-level protocol:** sha256 before = `2af421ea923108d4a3bc061fd0bb3d2f43297f8e460a070bb6c1579e09b0d971`; clause removed from `tools/config-audit.js`; `node tests/config-audit.js` → **EXIT 1, 9/15 (6 failures** — repo-exit, no-fatal-message, S-members, fixture A×2, and the removal-guard — all attributable to the removal; raw output in session log); restored via `git checkout -- tools/config-audit.js`; sha256 after = identical (`HASH_IDENTICAL`); `git status` → only `tests/config-audit.js` modified ⇒ **tree clean of the tool, byte-identical** |

## C6-03 / 2. Reference gates at BASE `b5360fe7` (measured this session, detached worktree at BASE + final tree; exact paths, raw exits, no pipes)

| Gate (exact path) | Raw exit @BASE | Result @BASE |
|---|---|---|
| `node tests/docs-freeze-marker.js --check` | 0 | 14/14 ✅ (matches Chat1 reference) |
| `node tools/docs-stats-sync.js --check` | **1** | ❌ 420 ↔ **424** (manifest 420 vs 424 on disk; 61→65 subfolder docs; active 353→357) — matches Chat1's F2 confirmation at `328bded`; count unchanged at BASE |
| `node tools/docs-refs-check.js` | **1** | ❌ **113** fresh stale refs at BASE `b5360fe7` (Chat1's 114 was at `328bdedf`; the −1 delta over the drift range is an observation, not adjudicated here — F7) |
| `node tests/secret-scan.js` | 0 | 12/12 ✅ |
| `node tools/config-audit.js` | 0 | 0 fatal / 25 advisory ✅ |
| `node tests/config-audit.js` | 0 | 15/15 ✅ (extended) |
| `node tests/config-reference-coverage.js` | 0 | 79/79 ✅ |

**Neutrality of this delivery (measured pre/post append):** `docs-refs-check` = 113 before this append → 113 after (Δ 0 — this append cites no missing path; per the refs trap rule, the full missing path of the control-plane offender previously noted as F5 is deliberately **not** re-written here); `docs-stats-sync --check` = 420↔424 before → after (Δ 0 — the append lives in a nested `daily-reports/Chat6/` file, which the frozen count does not tally; the test-file change is `.js`, not a doc).

## C6-03 / 3. STEP C — stats-sync decision package (READ-ONLY, for ChatGPT; no writes performed)

### 3.1 The +4 documents that moved the frozen count 420 → 424 (all top-level `docs/daily-audits/`; introduced since the rc44 manifest resync at `dfa863e4` / #244)

| # | File (top-level in `docs/daily-audits/`) | Introducing commit | PR (merge commit, merged at) |
|---|---|---|---|
| 1 | `2026-09-15-session-01a0a653-state-recovery.md` | `03b31894` (the merge commit itself) | **#255** (Chat3 recovery, 2026-09-15 18:35Z) |
| 2 | `2026-09-15-session-01a0a651-escalation.md` | `38fc63eb` (18:37Z) | **#258** (merge `f445abb1`, 19:13:16Z) |
| 3 | `2026-09-15-session-01a0a656-state-recovery.md` | `9f6cb7af` (18:39Z) | **#260** (merge `ecaa84c9`, 19:13:31Z) |
| 4 | `2026-09-15-session-01a0a658-state-recovery.md` | `cfbe23de` (18:42Z, this Arena's prior-session record) | **#262** (merge `0f30a1e7`, 19:13:53Z) |

Pattern: **every top-level session-recovery/escalation record in `daily-audits/` is +1 to the frozen count** — a structural churn source (4 in one day). Nested report dirs (`daily-reports/<Chat>/`) do not tally (verified: Chat4/Chat8/Chat9/Chat10 reports in the drift range left the count at 424).

### 3.2 Exactly which rows change on regeneration (proposal, NOT applied — from `--check` بود/شد at BASE `b5360fe7`)

- `docs/DOCS_METRICS.md` [docs-total-row]: `… + ۶۱ سند در زیرپوشه‌ها … = جمع درخت ۴۲ … درخت بدونِ گزارش‌های روزانه ۳۸۲` → `… + ۶۵ … = جمع درخت ۴۲۴ … ۳۸۶`
- `docs/DOCUMENTATION_MAP.md` [map-total]: `| کل اسناد (لحظهٔ قفل rc44) | **۴۲۰** |` → `**۴۲۴**`
- `docs/DOCUMENTATION_MAP.md` [map-sub]: `اسناد زیرپوشه‌ها (…) | ۶۱` → `۶۵`
- `docs/DOCUMENTATION_MAP.md` [map-status-active]: `فعال (در حال استفاده) | ۳۵` → `۳۵۷`
- `docs/DOCUMENTATION_MAP.md` [map-sum]: `**جمع** | **۴۲۰**` → `**۴۲**`

These five rows are the full owner-set of the drift (the `--check` output enumerates exactly these). If regenerated **under rc45** (option الف), two further shifts apply: the `rc44` literal inside docs-total-row and map-total becomes `rc45`, and the root figures shift +1 (new freeze doc): `۳۵ ریشه (پیش از قفل؛ با آن ۳۵)` → `۳۵۹ (با آن ۳۶۰)`, tree `۴۲۴` → `۴۲۵`, active `۳۵۷` → `۳۵۸`.

### 3.3 rc45 consequences (what must move together; gates that shift)

1. **New** freeze document `DOCS_FREEZE_v1.0.0-rc45.md` (to be created in the `docs/` root — successor of the rc44 freeze doc) — title line must anchor `v1.0.0-rc45` (guard is title-anchored, rc-agnostic per #241); required sections per the guard: `قاعدهٔ قفل`, `امضا`, `فهرست اسناد ریشه با هش`, date line `**تاریخ قفل:** 2026-09-DD`, bump rule, live-doc exception, GPG-honesty clause; manifest = all root docs (now incl. the rc44 freeze doc, listed + sha256'd).
2. `tests/docs-freeze-marker.js`: `const RC = '44'` → `'45'` and `const FREEZE = …rc44.md` → `…rc45.md`.
3. `node tools/docs-stats-sync.js --freeze` — **F13: this rewrites the two frozen stat docs AND regenerates the manifest** (write mode — forbidden outside an authorized bundle).
4. Atomicity: (1)+(2)+(3) must land in **one commit/PR** or `docs-freeze-marker` is red in between (missing new freeze doc vs updated constant, or stale hashes).
5. Gates shifting: `docs-freeze-marker` (constant + title anchor + manifest rows/hash), `docs-stats-sync --check` (green only after resync), and the three drift-sensitive consumers named in the stats tool's header (`docs-metrics`, `documentation-map-coverage`, `test-coverage-report-coverage`). `docs-refs-check` is count-independent (not shifted by the bump itself).
6. Context: `rc44` is referenced in 15 docs at BASE — 2 structural freeze docs (rc43/rc44), 2 stat docs, the marker test, and 10 historical reports (untouched by a bump; historical by definition).

### 3.4 Three policy options (with risks — the vote belongs to ChatGPT/owner, not to this Arena or Chat1)

- **(الف) Regenerate + rc45** (bundle: new freeze doc + test constant + `--freeze` resync, one commit). *Pros:* preserves lock-moment semantics; clean audit trail (rc44 doc stays frozen, listed & hashed by rc45); all gates green. *Risks:* touches the entire frozen surface in one bundle; the F13 write commands must run only inside the authorized bundle; **the drift recurs** with the next top-level `daily-audits/` doc (structural — 4 in one day); transient guard redness if not atomic.
- **(ب) Tool-side exclusion** (exclude the volatile top-level daily docs — `daily-audits/` + `daily-reports/` — from the frozen tally, documented definition change). *Pros:* stops the structural churn at the root; the frozen count becomes stable across sessions. *Risks:* changes the *meaning* of the frozen statistics — the definition row in the frozen docs must be re-stated once (still a write, F13); the stats tool + its three consumer tests + all human readers must agree on the new definition; the historical rc44 lock value (420) keeps the old definition ⇒ an explicit definitional note is required in the freeze rule text; the tool is shared infrastructure (Chat6-domain per its origin, but a policy change is owner/ChatGPT's).
- **(پ) Re-baseline without an rc bump** (rewrite the five rows to the current 424 while staying rc44). *Risks:* rewrites a **lock-moment historical value** (`کل اسناد (لحظهٔ قفل rc44) | ۴۲`) inside a frozen doc — destroys the lock-time audit trail; and Chat7 already proved the naive variant unsafe (commit `4a21074`: resync without rc bump makes `docs-freeze-marker` **13/14** red via the frozen hashes of the two stat docs) ⇒ as stated, NOT viable; the only safe form collapses into (الف).

### 3.5 F13 compliance (this session)

Only `--check` / `--json`-equivalent read-only invocations were run: `docs-stats-sync --check`, `docs-refs-check` (plain, read-only at plain run — it only writes under `--baseline`, which was **not** used), `docs-freeze-marker --check`, `config-audit` (verified read-only from source before first run: no `fs.writeFileSync` in the file). **Never run this session:** `docs-stats-sync` write mode, `docs-stats-sync --freeze`, `docs-refs-check --baseline`.

## C6-03 / 4. NOT-RUN & forbidden-scope compliance

| Item | Reason |
|---|---|
| refs-check fix + baseline edit | Forbidden (ownership Chat7, C7-03 per ruling #5) |
| rc bump / `--freeze` / any write-mode stats run | Forbidden (F13 + STEP C is read-only; fix requires a ChatGPT/owner vote) |
| Any P0/P1 status change / Roadmap change / National GO-NO-GO | Forbidden (governance) |
| F1 (duplicate 2.7 heading / 2.12-2.11 order) | QUEUED under C6-17 per ruling #4 — deliberately not touched |
| Product / release-DB test suites, PG/Redis live paths | No product code change (test-only delivery); no live PG/Redis in sandbox ⇒ NOT-RUN |
| CI | NOT-RUN as an action (no .github/workflows edit; CI availability separately constrained, RISK-O-007) |
| C6-05…C6-20 (incl. C6-17) | Not assigned; no self-selection |

## C6-03 / 5. Findings

- **F6 (P3, environment):** re-clone/graft incident + forward-only recovery (recorded in §0) — no delivered content affected; remote was source of truth throughout.
- **F7 (informational):** refs count 114 (`328bdedf`, Chat1) → 113 (`b5360fe7`, measured) — a −1 delta over the drift range (plausibly one CodeQL autofix merge repaired a ref); observation only, refs ownership stays Chat7 (C7-03).
- **C6-03 verdict movement:** PARTIAL → **MET** (this delivery): filter shipped (`915afd47`) + now pinned by 8 focused assertions (repo-level non-vacuous pin on `DUPNUMS` + hermetic positive/export-control/bite fixtures), bite proven repo-level with hash-verified restore.

## C6-03 / 6. Suggested next action (Chat1 decides)

1. Route the §3 decision package to **ChatGPT/owner** for the F2 policy vote (options الف/ب/پ).
2. **C6-17** (F1 queued: duplicate `2.7` heading + 2.12/2.11 order in the config reference) — small doc-consistency fix.
3. **C6-05** (migration audit) per board order.

**Board items NOT started beyond C6-03** — next Mission only on Chat1 assignment.
