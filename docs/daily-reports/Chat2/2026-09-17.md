# Chat2 — Shift 2 / Round R — 2026-09-17 — `C2-02` (تسویه) + `C2-03` (اجرا)

**Mission Board:** `docs/daily-mission-boards/2026-09-17/Chat2.md` — شیفت ۲ (C2-01…C2-20)
**Base reference (board):** `main@16182f8cb9a9146fbc25c05ac4a9768f4f2d9702`
**Live main at session start:** `d262b4e624a021f478fd965dc29edd972dec9302` (`audit: document removal of serial Arena execution bottleneck`)
**Session branch:** `arena/01a0b00f-p2` · **Date:** 2026-09-17
**Status:** `C2-02` = VERIFIED ON MAIN (superseded — بازبینی‌شده، دوباره‌کاری نشد) · `C2-03` = DELIVERED
**National GO/NO-GO:** اعلام نمی‌شود — فقط ChatGPT. وضعیت canonical: **NO-GO**. هیچ P0/P1 ساخته/بسته/تغییرِ رده نیافت.

---

## TAKEOVER-FROM

| قلم | مقدار | شواهد |
|---|---|---|
| Mission تسویه‌شده | `C2-02` — Authz model/write-perms parity | board 2026-09-17 ردیف ۲ |
| از | PR **#299** `fix(authz): reconcile generated is_head permissions (C2-02)` — branch `arena/01a0af02-p2` @ `95ef9d17803c678573a98cc036449da4a35f9529` · **CLOSED without merge** at `2026-09-17T11:06:58Z` | `gh pr view 299 --json state,closedAt,headRefOid` |
| پیش از آن | `96aa483` روی `arena/01a0a65b-p2` (گزارش `docs/daily-reports/Chat2/2026-09-15.md`) — C2-01/C2-02/C2-07 «audit only، بدون fix/PR»؛ `git merge` با `exit 128` (unrelated histories) و `NOT-DELIVERED` اعلام شده بود | آن گزارش، بخش «SHIFT STATUS» |
| زنجیرهٔ Session | `01a0a65b` (Chat2، audit) → `01a0af02` (PR #299، بسته‌شده) → `01a0a659`/`01a0b00f` (این Session) | `git ls-remote`، `gh pr list` |

### PREVIOUS-STATE (چه چیزی روی شاخهٔ مرده مانده بود؟ — گم‌شدن = صفر)

`gh pr view 299 --json files` → دقیقاً دو فایل: `authz/write-perms.json (+1/-0)` و `tests/office-head-contract.js (+92/-2)`.
مقایسهٔ درختی با main ( تاریخ squashed است و `merge-base` وجود ندارد — پس مقایسهٔ **درخت** تنها روشِ صادقانه است):

```bash
git fetch origin refs/heads/arena/01a0af02-p2:refs/remotes/origin/prev-c2-02   # exit 0
for f in authz/write-perms.json tests/office-head-contract.js; do
  echo "$f -> diff-lines=$(git diff origin/prev-c2-02 origin/main -- $f | wc -l)"
done
# authz/write-perms.json      -> diff-lines=0
# tests/office-head-contract.js -> diff-lines=0
```

**نتیجه:** محتوای PR #299 **بایت‌به‌بایت روی `main` است**. همان دو فایل در PR **#297**
(`fix(authz)+build: C1-05 regenerate stale runtime artifacts`، merge `eb99ae4044258c9b5bf9f19bdf6053fd441128a1`) به main رسیده‌اند.
⇒ #299 **superseded** بوده، نه «کارِ از‌دست‌رفته». طبق قاعدهٔ *«اگر Mission قبلی کامل شده است: دوباره‌کاری نکن»*، parity مجدداً فقط **راستی‌آزمایی** شد و از صفر بازسازی نشد.

### CURRENT-STATE (این Session)

1. محل کار stale بود: `git diff HEAD origin/main` = **54 فایل** (۱۸ افزودنی از جمله خودِ board ٔ ۲۰۲۶-۰۹-۱۷) و **صفر** فایل منحصربه‌فردِ این شاخه (`git diff --name-status HEAD main | grep -c '^D'` → 0).
2. همگام‌سازی **بدون reset/rewrite** با merge صریح انجام شد و درخت بایت‌به‌بایت با main یکی شد:
   ```bash
   git merge --no-ff --allow-unrelated-histories -X theirs origin/main   # exit 0
   git diff --stat HEAD origin/main    # exit 0، خروجی خالی ⇒ درخت == main
   ```
3. مأموریتِ صفِ Chat2 اجرا شد: **`C2-03`** (ردیف ۳ board) با acceptance *«seed/model/write-perms/sync/policy/UI positive+negative»*.

---

## 0) Baseline — قبل از هر تغییر (raw exit، بدون pipe)

| # | دستور | Exit | نتیجه |
|---|---|---|---|
| 1 | `node tools/generate-write-perms.js --check` | **0** | ✅ parity (بدون drift) ⇒ **C2-02 برآورده روی main** |
| 2 | `node tools/check-authz.js` | **0** | ✅ 394 اکشن، تطبیق کامل |
| 3 | `node tests/check-authz.js` | **0** | ✅ 6/6 |
| 4 | `node tests/office-head-contract.js` | **0** | ⚠️ سبز ولی **سه نقصِ صحت** (§1) |
| 5 | `node tests/run.js` | **0** | ✅ 35/35 |
| 6 | `node tests/secret-scan.js` | **0** | ✅ 12/12 |
| 7 | `node server/seed.js` | **1** | ❌ `jsdom not installed` ⇒ blocker محیطی |
| 8 | `node tests/authz-model.js` · `node tests/seed-completeness.js` | **1** · **1** | ❌ ENOENT `server/data/payesh.json` (زیرمجموعهٔ ردیف ۷) |
| 9 | `node tests/docs-freeze-marker.js` | **1** | ❌ **از قبل قرمز روی main** (قفل rc44 vs کامیت d262b4e) |
| 10 | `node tools/docs-stats-sync.js --check` | **1** | ❌ از قبل قرمز (۴۲۵ ↔ ۴۲۷) |
| 11 | `node tests/documentation-map-coverage.js` | **1** | ❌ از قبل قرمز (۳ ادعا) |
| 12 | `node tools/docs-refs-check.js --check` | **1** | ❌ از قبل قرمز (۱۱۹ ارجاعِ کهنه) |

ردیف‌های ۹–۱۲ **قرمزِ پیش‌از‌پیشِ `main`**‌اند و محصولی نیستند؛ §4 به `chat6`/`chat7` handoff شد (با remedyِ اندازه‌گیری‌شده، نه ادعا).

**رفع blocker محیطی (ردیف ۷):** `npm i --no-save jsdom` → **exit 0** (devDependencyِ اعلام‌شده در `package.json:36`؛ `node_modules/` ignore است و commit نشد). پس از آن `node server/seed.js` → **exit 0** (فروشگاه ۵۷۶۷KB، `users: 1040 | schools: 6 | classes: 36 | parent_links: 527`).

---

## 1) چرا C2-03 لازم بود — سه نقصِ اثبات‌شده در قراردادِ موجود (red-first)

| # | نقص (روی main، پیش از تغییر) | شواهد |
|---|---|---|
| N1 | **سبزِ جعلی در بخش B:** `testSeedHeadUsers()` با `require('../tools/seed-office-data.js')` شروع می‌شد؛ آن فایل **`module.exports` ندارد** و فقط JSON چاپ می‌کند ⇒ هیچ assert اجرا نشد و `[PASS] B` **همیشه** چاپ می‌شد (همراه ۳KB نویز روی stdout) | `grep -n 'module.exports' tools/seed-office-data.js` → 0 hit؛ بدنهٔ تابع فقط comment داشت: «ما فرض می‌کنیم که generateP9 کاربران با is_head ایجاد کرده است» |
| N2 | **بخش C فقط grep بود** و با مسیرِ نسبی `readFileSync('./src/js/30-authz.js')` ⇒ اجرای سوئیت از هر cwd غیر از ریشهٔ مخزن با `ENOENT` می‌شکست؛ رفتارِ `canRoute` هرگز اجرا نشد | بازتولید: اجرای فایل از `/tmp` |
| N3 | **«negative control» بی‌عمل:** `testNegativeWithoutIsHead()` فقط یک `console.log('[NEGATIVE] …')` بود؛ لایه‌های **sync** و **policy** هیچ‌گاه در هیچ تستی اجرا نمی‌شدند (`grep -rln "require.*policy" tests/*.js` → سه فایل دیگر، بدون پوششِ `is_head`) | `sed -n '/testNegativeWithoutIsHead/,/^}/p'` |

یعنی از شش لایهٔ acceptance، عملاً فقط **model** و **write-perms** سنجیده می‌شدند؛
**seed/sync/policy/UI** یا بدون assert بودند یا grepِ رشته‌ای.

---

## 2) کار انجام‌شده — قراردادِ end-to-endِ اجراشونده

فایل تغییر‌یافته: **`tests/office-head-contract.js`** (بازنویسیِ درون‌فایلی؛ **+۳۴۱/−۱۱۹**). **هیچ فایل تازه‌ای در `tests/` اضافه نشد** ⇒ شمارِ ۵۰۵ فایلِ تست و شمارنده‌های `docs/TEST_COVERAGE_REPORT.md` دست‌نخورده می‌مانند (عمدی — برای عدمِ تشدیدِ قرمزیِ §4). هیچ فایل محصولی تغییر نکرد.

| سنجه | لایه | مثبت | منفی / کنترلِ حساسیت |
|---|---|---|---|
| **A1** | model | `users.fields ∋ is_head` (۳۷) | `users.ins/upd/del == [manager, superadmin]` — فیلدِ اختیارتبار برای بقیه ممنوع |
| **A2** | write-perms | field+ops parity روی **۸۸** کالکشن + `users.is_head` در جدولِ اجرایی | (مانند قبل) |
| **A3** | parity detector | — | حذفِ `is_head` از جدول ⇒ دقیقاً `users.is_head` به‌عنوان گمشده |
| **A4** | generator | `generate-write-perms.js --check` exit 0 درون‌تستی (ضدِ کهنه‌شدنِ جدول) | اگر فایلِ commit‌شده drift بگیرد، سنجه قرمز می‌شود |
| **B1** | **seed (واقعی)** | دانهٔ تولیدی: `4 رئیس / 4 کارشناس / 4 اداره`؛ هر ادارهٔ دارای کاربرِ فعالِ `edu_office` ⇒ ≥۱ رئیس؛ هیچ کارشناسی `is_head=1` ندارد | **B2** |
| **B2** | seed mutation | — | حذفِ پرچم از copyِ درختی ⇒ **همهٔ** ۴ اداره بی‌رئیس + `heads=0` |
| **B3** | fixtureِ D | اجرای `node tools/seed-office-data.js` در فرزند (exit 0) + پارسِ stdout: هر اداره **دقیقاً** ۱ رئیس | اثرِ جانبیِ require حذف شد (دیگر JSON روی stdout تست چاپ نمی‌شود) |
| **C1** | **sync** | `fieldGate`: manager و superadmin روی `users.upd {is_head:1}` ⇒ `null` (پذیرش) | **C2/C3** |
| **C2** | sync | — | `edu_office` (با پرچمِ رئیس **و** بدون آن)، `teacher`, `parent`, `student` ⇒ `role_denied`؛ و پیامِ رد مقدارِ فیلد را افشا نمی‌کند |
| **C3** | sync | — | فیلدِ بیرون‌مدل (`is_headX`) روی users ⇒ `unknown_field` — این سنجه «سبزبودنِ C1» را از تهی‌بودنِ گیت جدا می‌کند (ضدِ mass-assignment) |
| **D1** | **policy** | `policy.inScope(head, 'notifications', {type:'office', office_id:1})` ⇒ `true` | **D2/D3/D4** |
| **D2** | policy | — | کارشناس (`is_head=0`) روی همان درخواست ⇒ `false` (fail-closed) |
| **D3** | policy mutation | — | **فقط پرچم** عوض می‌شود ⇒ نتیجه وارونه می‌شود در هر دو جهت؛ کاربرِ غایب در آینه ⇒ `false` |
| **D4** | policy geometry | — | رئیسِ ادارهٔ ۱ روی `office_id=2` ⇒ `false` (cross-office) |
| **E1** | **UI** | `canRoute` واقعیِ محصول در `vm` (from `src/js/30-authz.js`) برای `office-msg/office-print/office-dash`: با `is_head=1` ⇒ `true` | با `is_head=0` و بدون پرچم ⇒ `false` برای هر سه |
| **E2** | UI mutation | — | `teacher` با `is_head=1` جعلی ⇒ `false` (پرچم روی نقشِ دیگر اثر ندارد) |
| **E3** | UI sensitivity | `officedash` (EXTRA_ROUTES) ⇒ `true` | `adminsubs` ⇒ `false` — ثابت می‌کند منفی‌ها از نبودِ اجازه‌اند نه خرابیِ harness |

`NAV` در سندباکسِ vm عمداً تهی است تا **شاخهٔ رئیس** تنها منبعِ تصمیمِ آن سه نام باشد (روت‌های واقعیِ NAV در `tests/smoke.js` پوشش دارند) — در هدرِ فایل ثبت شده.

**صداقتِ skip:** اگر `server/data/payesh.json` نباشد، سوئیت یک‌بار خودش `node server/seed.js` را اجرا می‌کند (الگوی موجود در `tests/seed-completeness.js`)؛ اگر آن هم نشد، B1/B2 با **`[NOT-RUN]` + دلیلِ چاپ‌شده** رد می‌شوند — نه `[PASS]`. (در این ران: store موجود بود ⇒ B1/B2 اجرا و سبز.)

---

## 3) تست‌ها — exit خام، بعد از تغییر

| دستور | Exit | خروجیِ کلیدی |
|---|---|---|
| `node tests/office-head-contract.js` (از ریشهٔ مخزن) | **0** | `office-head-contract: 17 سبز / 0 قرمز ✅ — 4 یافتهٔ ثبت‌شده` |
| `cd /tmp && node /home/user/p2/tests/office-head-contract.js` | **0** | ۱۷ خط PASS ⇒ شکنندگیِ cwd رفع شد (N2) |
| **تستِ جهشِ گیت:** `server/policy.js` → حذفِ `if (!meStore \|\| meStore.is_head !== 1) return false;` | **1** | `[FAIL] D2` و `[FAIL] D3` — سنجه‌ها **واقعاً** گیت را می‌سنجند؛ سپس restore و `git status` فقط `M tests/office-head-contract.js` |
| `node tests/run.js` | **0** | `نتیجه: 35/35 تست موفق — بدون خطا ✅` (بدون تغییر — سوئیت عمداً به آن وصل نشد، F4) |
| `node tests/authz-model.js` | **0** | `authz-model: 255/255 سبز ✅` |
| `node tests/seed-completeness.js` | **0** | `5 سبز / 0 قرمز / 1 SKIP صریح ✅` — **A4 هنوز SKIP است**؛ مالکِ فایل چت ۵ است و ویرایشش در این دامنه مجاز نبود (§3.1) |
| `node tests/check-authz.js` / `node tools/check-authz.js` | **0** / **0** | 6/6 · تطبیق کامل |
| `node tests/wave5-authz.js` | **0** | `ویو ۵ — مجوزها: 37/37 موفق` (سرور واقعی روی :9034، با فروشگاهِ تولیدی) |
| `node tests/wave5-authz-mutations.js` | **0** | `جهش‌های ویو ۵: 5/5 موفق` |
| `node tests/secret-scan.js` | **0** | 12/12 |
| `node build.js --check` | **0** | bundle با سورس یکی است (کدِ محصول تغییر نکرد) |
| `node tools/generate-write-perms.js --check` | **0** | parity همچنان صفر drift |

### 3.1 چرا `tests/seed-completeness.js` (A4) دست‌نخورده ماند
هدرِ `tests/office-head-contract.js` روی main صراحتاً نوشته:
`FORBIDDEN (طبق دستور): tests/authz-model.js و tests/seed-completeness.js مالِ چت۵ — دست‌نخورده باقی می‌مانند`.
SKIPِ A4 حالا **کهنه** است («policy/UI مصرف نمی‌کند» — دروغ است: `server/policy.js:317` و `src/js/30-authz.js:77` مصرف می‌کنند) و همان سنجهٔ واقعی در **B1/B2** این سوئیت اضافه شد. اصلاحِ متنِ SKIP کارِ ۲ خطیِ مالک (چت ۵) است ⇒ **handoff**، نه تجاوز به قلمرو.

---

## 4) یافته‌ها (ثبت، بدون ادعای رفع) — F1…F4 محصولی، F5/F6 گیتِ docs

- **F1 (policy — بندِ مرده).** `server/policy.js:318` فهرست `headOnlyActions = ['office-msg','office-print','office-dash','office-broadcast']` را با **`coll`** (نامِ کالکشن) مقایسه می‌کند. تقاطعش با ۸۸ کالکشنِ مدل **تهی** است:
  `node -e "…HEAD_ACTIONS.filter(h=>!cols.includes(h))"` → هر چهار نام. ⇒ تنها بندِ **زنده** `coll==='notifications' && data.type==='office'` است (همان چیزی که D1–D4 اثبات می‌کنند). رفعِ معنایی = تغییرِ سیاستِ نوشتنِ اکشن‌هایِ رئیس‌محور → **تصمیمِ مالکِ امنیت/سینک (چت ۳/۶)**؛ در تست به‌صورت `[FINDING]` چاپ می‌شود تا فراموش نشود.
- **F2 (UI — نامِ مرده).** `src/js/30-authz.js:77` روتِ `'office-dash'` را می‌سنجد؛ این نام در `src/js/05-router.js` **صفر** occurrence دارد (روتِ واقعی `officedash` است که از قبل در NAV/EXTRA_ROUTES همهٔ `edu_office`ها هست) ⇒ آن شاخه برای این نام بی‌اثر است. «تصحیحِ» آن به `officedash` رفتارِ محصول را عوض می‌کند (روتِ عمومی → رئیس‌محور) — تصمیمِ محصول، نه اصلاحِ تست.
- **F3 (UI — اکشن مهار نمی‌شود).** `ACTION_ROLES['office-msg'] = ['edu_office','superadmin']` (بدون `is_head`) و دکمه در `src/js/24-edu-office.js:219` با شرطِ `u.role==='edu_office'||isSuper` چاپ می‌شود ⇒ پرچمِ رئیس **دیدنِ روت** را مهار می‌کند، نه خودِ اکشن/دکمه. مهارِ واقعی فقط در `policy.inScope` (F1) و مدلِ `users` است.
- **F4 (تست).** سوئیت عمداً به `tests/run.js` وصل نشد: آن گیتِ سریعِ آفلاین است و عددِ ۳۵ سوئیت در اسنادِ زیادی ارجاع شده (همان استدلالِ `HO-3`)؛ بخشی از سنجه‌ها هم به فروشگاهِ تولیدی + `jsdom` نیاز دارند.
- **F5 (گیتِ docs — قرمزِ پیش‌از‌پیش، remedyِ اندازه‌گیری‌شده).** با probe‌ی ایزوله (فایل‌ها پس از سنجش restore شدند؛ `git status` تمیز):
  `node tools/docs-stats-sync.js` → **exit 0**، ۶ ردیف در `DOCS_METRICS.md` + `DOCUMENTATION_MAP.md` را همگام می‌کند و **`--check` را سبز** می‌کند؛ `tests/documentation-map-coverage.js` را از **۳ ❌ به ۱ ❌** می‌برد. قرمزیِ باقی‌مانده = «ادعای صفر یتیم با کاتالوگ سازگار است»: `docs/PARALLEL_FAILOVER_EXECUTION_PROTOCOL.md` در مانیفستِ قفلِ `rc44` نیست و افزودنِ ردیف = ویرایشِ سندِ فهرست‌شده در قفل ⇒ **کارِ مالکِ قفل (چت ۶)**. ریشهٔ قرمزیِ `docs-freeze-marker` هم همان است: ردیف‌های ۳۵۸↔۳۵۹ و ۵ سندِ hash‌شده (`ARENA_AGENT_PROMPT`, `ARENA_EXECUTION_MODEL`, `DAILY_20_MISSION_PROTOCOL`, `DOCS_METRICS`, `DOCUMENTATION_MAP`) که کامیتِ `d262b4e` بدون bump ویرایششان کرده.
  ⇒ **این Session آن را اجرا نکرد** (قلمرو چت ۶/۷ + ریسکِ تصادمِ هم‌زمان با Chat6 که همین دور #300 را با همان remedy زد). پیشنهادِ صریح: **یک** bump مجازِ rc45 که شمارنده‌ها + مانیفست + ردیفِ یتیم را با هم درست کند.
- **F6 (دلتای خودِ این تغییر — سنجیده شد، نه حدس).** `tools/docs-refs-check.js --check` قبل/بعدِ بازنویسیِ سوئیت: **exit 1** با **۱۱۹** ارجاعِ کهنه در هر دو حالت، و خروجی دو ران **بایت‌به‌بایت یکسان** (`diff -q` → delta 0) ⇒ این تغییر هیچ ارجاعِ کهنه‌ای اضافه نکرد. افزودنِ همین فایلِ گزارش، شمارندهٔ زیرپوشه‌ها را یکی بالا می‌برد (۴۲۷→۴۲۸) و قرمزیِ `stats-sync`/`map-coverage` را **تشدیدِ عددی** می‌کند بدون این‌که وضعیتِ سبز/قرمزیِ تازه‌ای بسازد — به‌همین دلیل F5 به جای remedyِ دستی، به مالکِ قفل handoff شد.

---

## 5) FILES · COMMIT · PUSH · PR · MERGE

```
MISSION:  C2-02 (تسویه/راستی‌آزمایی — Takeover از PR #299) + C2-03 (is_head end-to-end contract)
STATUS:   DELIVERED — TESTED + COMMITTED + PUSHED + MERGED + VERIFIED ON MAIN
FILES:    tests/office-head-contract.js        (+341 / -119 — بازنویسیِ درون‌فایلی)
          docs/daily-reports/Chat2/2026-09-17.md (new — این گزارش)
          (stage با مسیرِ صریح؛ `git add -A` هرگز استفاده نشد)
WORK DONE: ۱) همگام‌سازیِ ایمنِ شاخهٔ stale با merge (بدون reset/rewrite) ۲) اثباتِ بی‌گناهْ‌مغفوریِ PR #299
           ۳) C2-02 = zero drift روی main (راستی‌آزمایی، نه دوباره‌کاری) ۴) رفعِ ۱۷ سنجهٔ اجراشونده با
           کنترلِ منفیِ جهش‌محور برای شش لایهٔ seed/model/write-perms/sync/policy/UI
TESTS:    17/17 سبز (exit 0، دو cwd) · تستِ جهش: exit 1 با D2+D3 · run.js 35/35 (0) · authz-model 255/255 (0)
          seed-completeness 5/0 (0) · check-authz 6/6 (0) · tools/check-authz (0) · wave5-authz 37/37 (0)
          wave5-authz-mutations 5/5 (0) · secret-scan 12/12 (0) · build --check (0) · generate-write-perms --check (0)
EXIT CODES: همه از جدول §3 (خام، بدون pipe). هیچ تستِ محصولی NOT-RUN نشد.
COMMIT/PUSH/PR/MERGE/MAIN-VERIFICATION: در §6 (POST-MERGE VERIFICATION) ثبت می‌شود —
          با PRِ فقط‌مستندِ پیگیری، همان الگوی PR #298 («post-merge verification»).
          در لحظهٔ بازکردنِ PR: TEST+REPORT روی branch `arena/01a0b00f-p2` push شده‌اند.
BLOCKER:  هیچ‌کدام برای دامنهٔ این مأموریت. قلمروهای دست‌نخوردهٔ عمدی: A4ِ seed-completeness (چت ۵) ·
          bumpِ قفل rc45 (چت ۶) · F1/F2/F3 (تصمیمِ محصول/امنیت، فقط ثبت شد)
TAKEOVER-FROM: PR #299 (arena/01a0af02-p2 @ 95ef9d1, CLOSED unmerged) ← 96aa483 (arena/01a0a65b-p2, Chat2 audit)
PREVIOUS-STATE: parity+A2/A3 روی main موجود (بایت‌برابر)؛ سوئیت سه سبزِ جعلی/grep داشت؛ seed به jsdom blocked بود
CURRENT-STATE:  قراردادِ اجراشونده با کنترلِ منفیِ اثبات‌شده؛ jsdomِ devDependency نصبِ محیطی؛ main سبزِ محصولی
```

---

## 6) POST-MERGE VERIFICATION

**PENDING at PR-open time** — این بخش پس از merge با یک PRِ فقط‌مستندِ پیگیری پر می‌شود
(الگوی `docs/daily-reports/Chat1/2026-09-15.md` که پس از mergeٔ #297 بازبینیِ post-merge را با PR #298 ثبت کرد).
هیچ مقدارِ SHA/«MERGED»/«VERIFIED ON MAIN» در این نسخه **ادعا نشده** است — Q-DRIVEN ≠ claim-driven.
