# Mission Report — Chat5 — 2026-09-14

## Mission

- **Mission ID:** M-2026-09-14-C5-001
- **Owner:** Chat5 — QA / chaos / quality gate (`docs/ARENA_REGISTRY.md`)
- **Source:** `docs/daily-missions/Chat5/ACTIVE.md` (read from `origin/main` before execution)
- **Base SHA (mission):** `b272e09136f13488845d957ac6ce588dbcb8c416`
- **P0_REF:** `docs/P0_BLOCKER_TRACKER.md#پ۰-۱: ادغام نهایی موج ۱ — پستگرس تنها منبع حقیقت`
- **Current main SHA at execution:** `f852f29` ("audit: initialize Git-driven daily execution control")

**Base-SHA validation (master prompt §4):** `b272e09` **is an ancestor** of current `main` (`git merge-base --is-ancestor b272e09 origin/main` → 0). Drift = 11 commits, and the delta is **documentation-only**:

```
$ git diff --name-only b272e09..origin/main
docs/ARENA_REGISTRY.md
docs/daily-audits/2026-09-14.md
docs/daily-missions/Chat{1,2,3,4,5,6,7,8,10}/ACTIVE.md      (9 files)
$ git diff --stat b272e09..origin/main | tail -1
 11 files changed, 407 insertions(+)
```

Zero code/test changes in the drift ⇒ the mission remains executable on current `main`; both SHAs are recorded above. No `BLOCKED` condition was triggered.

## Result

- **Status:** **REPORT_SUBMITTED** (self-assessment: all five acceptance criteria met; acceptance/closure belongs to Chat1 reconciliation + ChatGPT audit)
- **Scope completed:**
  - [x] Current main SHA recorded (`f852f29`) + base drift assessed.
  - [x] Production PostgreSQL enforcement inspected on `main` (code, not documents).
  - [x] `tools/wave1-gate.js` and the multi-instance/production suites executed or marked `NOT-RUN`.
  - [x] Gap between "production refuses fallback" and the tracker's closure wording explicitly assessed.
  - [x] This report committed to `docs/daily-reports/Chat5/2026-09-14.md`.
- **Scope not completed:** none of the mission scope. Two evidence findings were raised (F1, F2 below); both are **reported, not fixed** (no code changes allowed).
- **Out of scope (not done, deliberately):** P0-1 closure/renumbering, tracker/roadmap edits, merge, any code or test change, National GO.

## Changes

- **Files changed:** `docs/daily-reports/Chat5/2026-09-14.md` (new; report only)
- **Commit SHA:** `see §Git — recorded after push`
- **Branch:** `mission/c5-p0-1-quality-gate`
- **Push:** recorded below
- **PR:** recorded below
- No code, test, tracker, roadmap or migration file was touched. No `force-push`. No `git add -A` (single explicit path staged).

## What was inspected (evidence)

### 1. Production PostgreSQL enforcement — on current main

| Location | Behaviour |
|---|---|
| `server/db.js:72–114` | P0-1 policy block: `isProductionEnv()` = `NODE_ENV==='production' \|\| PAYESH_ENV==='production'`; `memoryFallbackAllowed()` returns **false** in production; `backingStorePolicy()` exposes `allow_memory_fallback` + `reason` |
| `server/db.js:146–157` | `init()` with no `DATABASE_URL` (or no `pg` driver): in production returns `{ok:false, driver:'none', error:'DATABASE_URL is required when NODE_ENV=production …'}` — the JSON/in-memory store is **not** used |
| `server/db.js:249–254` | Unreachable PostgreSQL in production ⇒ `{ok:false}` startup failure, "refusing the JSON/in-memory fallback" |
| `server/db.js:836–837` | `isUidProcessed()` idempotency check fails closed in production (no silent per-process answer) |
| `server/index.js:1194–1204` | **Synchronous boot gate** evaluated **before** `listen()`: production + missing `DATABASE_URL` ⇒ `[FATAL]` + `process.exit(1)` |
| `server/index.js:146–157` | Async `db.init` `ok:false` handler ⇒ production exits 1, never serves from JSON |
| `ALLOW_MEMORY_FALLBACK=1` | Comment at `server/db.js:84–85` ("a flag must not be able to re-open a production data-loss path") is **enforced**, see probe 2 below |

### 2. Tests actually executed in this sandbox (current main `f852f29`)

| Command | Result | Evidence state |
|---|---|---|
| `node tools/wave1-gate.js` | **35/35 — GATE GREEN**, exit 0 (21 static + 14 suites; `smoke.js` 47.6s; wall 98.8s) | TESTED LOCALLY |
| `node tests/wave1-multi-instance.js` | **33/33**, exit 0 (two instances, one PG via pg-mem) | TESTED LOCALLY |
| `node tests/pg-prod-boot-no-db.js` | **14/14**, exit 0 (production without a reachable PG fails closed; dev unaffected) | TESTED LOCALLY |
| `node tests/pg-prod-no-json-writes.js` | **17/17**, exit 0 (no production write path lands in JSON; flag powerless) | TESTED LOCALLY |
| `pg-prod-boot-with-db` | **11/11**, exit 0 | TESTED LOCALLY (live PG 18.4) |
| `pg-prod-two-instance` | **12/12**, exit 0 | TESTED LOCALLY (live PG 18.4) |
| `pg-prod-uid-idempotency` | **10/10**, exit 0 | TESTED LOCALLY (live PG 18.4) |
| `pg-prod-persistence-failure` | **11/11**, exit 0 | TESTED LOCALLY (live PG 18.4) |
| `pg-prod-client-error` | **10/10**, exit 0 | TESTED LOCALLY (live PG 18.4) |
| `tests/pg-prod-suite-policy.js` — clean checkout | **7/9 — FAILED** (checks `1e`, `1f`) ⇒ **finding F1** | TESTED LOCALLY (RED) |
| `tests/pg-prod-suite-policy.js` — after `node server/seed.js` | **9/9**, exit 0 | TESTED LOCALLY |
| `tests/multi-instance.js` — clean checkout | **exit 0 while doing nothing**: `⏭️ store موجود نیست — اول: node server/seed.js` ⇒ **finding F1** | TESTED LOCALLY |
| `tests/multi-instance.js` — store seeded, no `DATABASE_URL` | **exit 1**: `MI-1 ❌ … [FATAL] production: DATABASE_URL required — JSON/in-memory fallback is dev-only` ⇒ the suite is stale vs. the gate (**finding F2**) | TESTED LOCALLY |

**Live PostgreSQL harness (how the live-PG suites were made runnable, no repo pollution):**

```
mkdir -p /tmp/pgws && cd /tmp/pgws && npm i embedded-postgres@18.4.0-beta.17 @embedded-postgres/linux-x64   # 61 MB, OUTSIDE the repo
NODE_PATH=/tmp/pgws/node_modules node tests/helpers/boot-pg.js        # → BOOTED url=postgres://chat1:chat1@127.0.0.1:55433/payesh_chat1  (VERSION PostgreSQL 18.4)
PG_LIVE_PG='postgres://chat1:chat1@127.0.0.1:55433/payesh_chat1' node tests/pg-prod-<suite>.js
```

`tests/helpers/boot-pg.js` is the **committed** harness; the npm payload was installed outside the workspace so nothing entered the repository or the workspace budget. Without this harness the four live-PG suites exit `2` = explicit `NOT-RUN — PostgreSQL not reachable` (observed before booting: `ECONNREFUSED 127.0.0.1:55433`).

### 3. Independent QA probes (mine, not the package's own assertions)

```
[1] NODE_ENV=production, no DATABASE_URL, TLS gate satisfied (PAYESH_BEHIND_PROXY=1)
    → exit 1 · "[FATAL] production: DATABASE_URL required — JSON/in-memory fallback is dev-only"
    → "Error: production requires PostgreSQL — set DATABASE_URL (the JSON/in-memory store is dev-only and forks per instance)"
[2] same + ALLOW_MEMORY_FALLBACK=1
    → exit 1 · identical [FATAL]   (the flag is powerless in production — confirmed first-hand)
[3] production + live PG URL   → db.init() = driver=postgres ok=true
    → full boot then stops at the PRE-EXISTING Redis gate:
      "[FATAL] Cache readiness failed: REDIS_URL is required when NODE_ENV=production (in-memory fallback is dev-only)"
[4] json store SHA before/after the refused boots → byte-identical (cmp) ⇒ no JSON writes occurred
[5] backingStorePolicy() across envs:
      NODE_ENV=development                       → allow_memory_fallback=true
      NODE_ENV=production                        → false
      PAYESH_ENV=production                      → false
      NODE_ENV=production + ALLOW_MEMORY_FALLBACK=1 → false
```

## Findings / Blockers

### F1 — `pg-prod-suite-policy` is RED from a clean checkout (false-green skip path) — NOT-RESOLVED
`tests/multi-instance.js` prints `⏭️ store موجود نیست` and **exits 0** when the gitignored artifact `server/data/payesh.json` is absent (`.gitignore:29 → server/data/`). The policy gate spawns exactly that suite and asserts `status !== 0` + the `production: DATABASE_URL required` message, so on a clean checkout it reports:

```
pg-prod-suite-policy: 7/9  FAILED: 1e without DATABASE_URL it does NOT exit 0 (no false green) | 1f the refusal is the production DATABASE_URL gate
```

After `node server/seed.js` the same gate is **9/9**. ⇒ The gate that exists to prevent "a production boot that exits 0" is itself **not reproducible from a clean checkout**, and the skip-with-0 path it guards against is still present. This is the same class of defect the repo already fixed once for the live-PG suites ("the harness used to be created ad hoc in the sandbox and evaporated on every reset" — `tests/helpers/boot-pg.js` header). Owner: P0-1 package owner (Chat 6/1) — not Chat5 (no code changes in this mission).

### F2 — `tests/multi-instance.js` is stale relative to the P0-1 gate — NOT-RESOLVED
Its header still describes "PG بدونِ دسترسی … Wave 1/3" and it boots two **production** instances without `DATABASE_URL`; the new gate correctly refuses that boot, so `MI-1` fails and the suite exits 1. Either the suite must be made DB-backed (the treatment `arena5-recovery.js` received in round 2, per `pg-prod-suite-policy` check `1c`) or its expectation must be re-stated. Reported, not fixed.

### F3 — Evidence gap: "production refuses fallback" vs. tracker wording (the mission's core question)
Tracker card `docs/P0_BLOCKER_TRACKER.md:44–57` states, verbatim, that on the `main` it references (`1c07a6f`) the fallback still exists, status `🔴 بلاکشده — منتظر تصمیم ناظر + ادغام`, fix path `(۱/۵)` with steps ۳–۵ open. Repository evidence now shows:

| Tracker step | Evidence on current main |
|---|---|
| ۱. implementation on branch ✅ | consistent |
| ۲. adjudication of the two implementations (supervisor) | **no repository artifact** records it. Both competitor branches are merged in history: **PR #48** (head `arena/01a08a2e-p2`, merge `ac590be523`, 2026-09-10) and **PR #47** (head `arena/01a08545-p2` = the "writes" line, merge `4926041f36`, 2026-09-10, now 835 commits behind). `server/writes.js` exists in neither. ⇒ **UNKNOWN / MISSING EVIDENCE** — I do not infer that adjudication happened, nor that it did not. |
| ۳. final merge into `main` | **appears satisfied**: the enforcement package is on `main` via **PR #181** "بستهٔ ۰ — PostgreSQL production enforcement (P0-1)" (head `arena/01a09a7d-p2`, merge `275caf8f01`, merged **2026-09-13T19:57:25Z**, +4133/−11, 21 files), incl. commits `1467164` (2026-09-13 12:11Z) and `1613120` (2026-09-13 14:59Z). |
| ۴. run `tools/wave1-gate.js` on merged `main` | **satisfied locally**: 35/35 GREEN on `f852f29` (my run). Evidence state = **TESTED LOCALLY**, *not* CI (billing-blocked) and *not* staging. |
| ۵. remove the JSON/in-memory code from the production path | **behaviourally satisfied, literally not done**: production is proven to refuse the fallback (probes 1–5 + 82 green checks in the pg-prod family), but the fallback **code remains in the tree** and is still the dev/test backing store. The tracker wording is ambiguous between "unreachable in production" (proven) and "deleted" (not done). Re-wording or deleting are owner/supervisor decisions. |

Additional wording gaps found: the tracker's referenced SHA `1c07a6f` (2026-09-10, merge of #50) is **not an ancestor** of current `main` (1670+ commits of divergence in its line) ⇒ the card describes a historical `main`, not today's; the control layer itself agrees the item is open (`docs/EXECUTION_CONTROL_PROTOCOL.md` §9: "P0-1 PostgreSQL source-of-truth: **OPEN / UNDER ADJUDICATION**").

### F4 — The P0-1 evidence is not wired into automation (informational)
`grep -rn "pg-prod\|wave1" .github/workflows/*.yml` → **0 matches**; `npm test` = `node tests/run.js && node --expose-gc --max-old-space-size=2048 tests/smoke.js`, and `tools/wave1-gate.js` is referenced by **no** workflow or runner. So "gate green on main" exists only as hand-recorded local evidence — exactly the distinction the audit demands. CI is billing-blocked (`NOT-RUN`), so this cannot be closed by Chat5.

## Environment limitations (recorded, not hidden)

- Sandbox: `nproc=2`, ~1984 MB RAM; **no** `postgres`/`psql`/`redis-server` binaries pre-installed (checked). Live PG 18.4 was provisioned **outside the repo** (see §2) so four suites moved from `NOT-RUN` to real green.
- **Full production boot with both backends: `NOT-RUN — ENVIRONMENT LIMITATION`** — with `DATABASE_URL` live the boot proceeds past the DATABASE gate and is then refused by the pre-existing Redis gate (`REDIS_URL is required when NODE_ENV=production`); no Redis is available here (binary download blocked in this sandbox). Only `db.init()` returning `driver=postgres ok=true` is proven.
- **CI: `NOT-RUN`** (GitHub billing; RISK-O-007). No CI/staging/production claim is made anywhere in this report.
- Nothing was installed inside `/home/user`; workspace budget unchanged (115 MB) apart from this report file.

## Tests (mission-format summary)

- **Test:** P0-1 wave-1 gate + production-enforcement family + multi-instance
- **Result:** 35/35 gate GREEN · 33/33 · 14/14 · 17/17 · 11/11 · 12/12 · 10/10 · 11/11 · 10/10 · **policy 7/9 clean-checkout RED / 9/9 seeded**
- **Evidence:** exact commands + outputs in §2/§3 above (all run on `f852f29`, sandbox)
- **Status:** TESTED LOCALLY (not CI, not staging)

## Evidence State

- **IMPLEMENTED:** yes on `main` (package landed via PR #181)
- **TESTED LOCALLY:** yes — see §2/§3 (this session, `f852f29`)
- **COMMITTED / PUSHED / PR OPEN:** this report only (recorded in §Git)
- **MERGED:** no
- **VERIFIED ON MAIN:** for the *report* — pending Chat1 reconciliation; for P0-1 **not claimed**
- **VERIFIED IN STAGING:** no — no staging exists (`NOT-RUN`)
- **PRODUCTION PROVEN:** no

## Out of Scope

- P0-1 status, wording, numbering or closure; `ROADMAP.md` / `P0_BLOCKER_TRACKER.md` / `NATIONAL_ROADMAP_PROGRESS.md` edits.
- Any code or test change (including F1/F2 remediation).
- Merge of any PR; adjudication of the two wave-1 implementations.
- National GO/NO-GO (auditor-only) · reuse of the RTO/RPO figures S1–S6 (`UNSOURCED` per the control layer).

## Next Action Requested

1. Coordinator to reconcile this report against repository evidence (branch, commit, diff, test outputs).
2. Route F1 and F2 to the P0-1 package owner as remediation (both are one-file test-hygiene fixes; **not** executed here).
3. Route F3 to the P0-1 owner/supervisor: decide whether step ۵ is "unreachable in production" (proven — then re-word the card) or "delete the fallback code" (then issue a code mission), and record the adjudication outcome of step ۲ as a repository artifact (today it is MISSING EVIDENCE).
4. Wire `tools/wave1-gate.js` + `tests/pg-prod-*.js` into automation if a CI budget is restored (currently `NOT-RUN`).

---

### Git (this block is completed by an immediate follow-up commit — the report cannot contain its own SHA)

- **Branch:** `mission/c5-p0-1-quality-gate` (pushed to `origin`)
- **Commit (report):** `81425510c10681a3025380741bc1719c9763c603` — "report(chat5): M-2026-09-14-C5-001 — independent P0-1 quality gate (wave1 gate 35/35 on main + evidence findings F1-F4)"
- **Push:** `git push <url> HEAD:refs/heads/mission/c5-p0-1-quality-gate` → `git ls-remote` == `81425510c106` ✅
- **PR:** **#220** → https://github.com/rezaa2544/p2/pull/220 (base `main`, state `open`, **not merged by Chat5** — merge is out of scope)
- **Merge:** none requested by this report; the mission's forbidden scope excludes merge.
- **Second commit:** adds this block only (one line-level edit); no other content changed.

---

# M1-CHAT5-20260914 (v2) — P0-1 independent verification · fresh measurement on BASE `9888cb1`

**Mission ID:** `M1-CHAT5-20260914` (v2 — re-issued by Chat1 after the Chat5/Chat3 paste-error)
**Owner:** Chat5 — QA / chaos / quality gate (`docs/ARENA_REGISTRY.md`)
**Issued by:** Chat1 (Coordinator) · **Date:** 2026-09-14
**Branch:** `arena/01a0a199-p2`
**BASE (verbatim):** `git ls-remote origin main` → `9888cb12bc5bb5444a31e86e27ede1566ffd41df` (matches R3 expectation `9888cb1`)
**P0_REF:** `docs/P0_BLOCKER_TRACKER.md` — card «پ0-۱: ادغام نهایی موج ۱ — پستگرس تنها منبع حقیقت»; header at **L43**, table **L45–L57** on BASE. «مسیر رفع» row: ۱ ✅ → ۲ ⬜ → ۳ ⬜ → ۴ ⬜ → ۵ ⬜ — **(۱/۵)**, status «🔴 بلاک‌شده — منتظر تصمیم ناظر + ادغام». (Card is Persian with Persian numerals; ASCII `grep "P0-"` returns nothing — referenced by line/section as instructed.)
**Status:** REPORT_SUBMITTED — closure belongs to Chat1 reconciliation + ChatGPT audit.
**Supersedes:** nothing. The numbers of PR #220 (`f852f29`) are **carried over as history only, never as evidence** (R3).

## 1. Scope performed

- `server/db.js` on BASE: production gate + JSON/in-memory fallback, read **as code, not as documents**; every claim below carries `path:line`.
- Both Wave-1 gates executed **by me on BASE**, raw exit codes, no masking pipe.
- Every claim classified on the evidence ladder.
- Acceptance gaps numbered `AG-1..AG-5`.
- Explicit delta table against PR #220's numbers.

**Out of scope (not done):** no `server/**`, `tests/**`, `tools/**`, `src/**` edit; no tracker/roadmap/NRP edit; no P0/P1 create/close/renumber; no National GO. Only this file was written (append).

## 2. Bootstrap (documented deviation)

| Step | Command | Result |
|---|---|---|
| deps | `npm ci` | exit 0 · `added 126 packages` · **NOT** `npm install --no-save pg-mem` |
| seed | `node server/seed.js` | `RAW_EXIT_seed=0` · `✅ store written: /home/user/p2/server/data/payesh.json (5683 KB) · users: 1036 \| schools: 6 \| classes: 36 \| parent_links: 527` |

**Why `npm ci` instead of `npm install --no-save pg-mem`:** `pg-mem` alone is insufficient — `server/db.js` requires `pg` and `ioredis` (runtime deps) and the behavioural suites spawn `server/index.js`. `npm ci` is lock-exact, installs `pg-mem` (devDep) plus the runtime deps, and **does not modify `package.json` / `package-lock.json`** — verified: `git status --porcelain` empty after install. `pg-mem` presence independently confirmed by the gate's own first static check: `✅ pg-mem available (multi-instance stand-in)`.

## 3. Tests — raw exit codes (no pipe)

### 3.1 As-cloned / unseeded tree (the honest "clean checkout" baseline)

```
$ node tools/wave1-gate.js
  جمعِ گیت: 27 موفق، 8 ناموفق          ⛔ GATE RED — wave1_status must stay non-completed in ruflo.
RAW_EXIT_wave1_gate=1

$ node tests/wave1-multi-instance.js
  جمع: 33 موفق، 0 ناموفق از 33
RAW_EXIT_wave1_multi_instance=0
```

8 failing suites (21/21 static checks passed in both runs; 6 of 14 suites passed):

| Suite | Failure |
|---|---|
| `tests/tombstone.js` | `ENOENT … copyfile 'server/data/payesh.json'` |
| `tests/occ.js` | `ENOENT … copyfile 'server/data/payesh.json'` |
| `tests/server7.js` | `ENOENT … copyfile 'server/data/payesh.json'` |
| `tests/security2.js` | `FATAL ENOENT … copyfile 'server/data/payesh.json'` |
| `tests/server15.js` | `ENOENT … copyfile 'server/data/payesh.json'` |
| `tests/server18.js` | `server18 crashed: ENOENT … copyfile 'server/data/payesh.json'` |
| `tests/api/runner.js` | `ENOENT … copyfile 'server/data/payesh.json'` |
| `tests/session-revocation.js` | `❌ M-0 سید هست \| ❌ H-0 سید هست` (asserts a seeded store) |

### 3.2 After `node server/seed.js` (the state the mission's bootstrap prescribes)

```
$ node tools/wave1-gate.js
  ✅ tests/wave1-multi-instance.js (0.4s)   ✅ tests/sync-atomic-batch.js (0.1s)
  ✅ tests/sync-queue-caps.js (2.6s)        ✅ tests/tombstone.js (0.9s)
  ✅ tests/occ.js (0.7s)                    ✅ tests/server7.js (5.2s)
  ✅ tests/session-revocation.js (3.1s)     ✅ tests/security2.js (4.5s)
  ✅ tests/audit.js (0.0s)                  ✅ tests/server15.js (23.4s)
  ✅ tests/server18.js (6.2s)               ✅ tests/check-authz.js (0.3s)
  ✅ tests/api/runner.js (4.3s)             ✅ tests/smoke.js (48.0s)
  جمعِ گیت: 35 موفق، 0 ناموفق
  🟢 GATE GREEN — Wave 1 P0 verified.
RAW_EXIT_wave1_gate_seeded=0

$ node tests/wave1-multi-instance.js
  جمع: 33 موفق، 0 ناموفق از 33
RAW_EXIT_wave1_multi_instance_seeded=0
```

## 4. Code evidence — production gate & memory fallback on BASE

| Location (BASE `9888cb1`) | Behaviour |
|---|---|
| `server/db.js:86-88` | `isProductionEnv()` = `NODE_ENV==='production' \|\| PAYESH_ENV==='production'` |
| `server/db.js:90-93` | `memoryFallbackAllowed()` → `if (isProductionEnv()) return false;` then **`return true;` unconditionally** for non-production |
| `server/db.js:95-97` | `memoryFallbackRequested()` = `ALLOW_MEMORY_FALLBACK==='1'` — **not consulted by the allow decision** (see below) |
| `server/db.js:103-116` | `backingStorePolicy()` → `{production, allow_memory_fallback, flag_requested, reason}`; production reason = `production: DATABASE_URL required — JSON/in-memory fallback is dev-only` |
| `server/db.js:142-153` | `init()` with no `DATABASE_URL` (or no `pg`): production returns `{ok:false, driver:'none', error:'DATABASE_URL is required when NODE_ENV=production (JSON/in-memory fallback is dev-only)'}` — the JSON store is **not** used |
| `server/db.js:251-254` | Unreachable PostgreSQL in production → `{ok:false, driver:'none', error:'PostgreSQL unreachable in production: …'}`; the JSON downgrade at `:255-256` is reachable only when the fallback is allowed |
| `server/db.js:595-597` | `pgExpected()` = `!memoryFallbackAllowed() && !!DATABASE_URL` — the F1 from chaos-drill #185 |
| `server/db.js:836-840` | `isUidProcessed()` **fails closed** in production (`throw`), dev/test keeps warn-and-continue (`:840`) |
| `server/index.js:1199-1204` | **Synchronous** boot gate, evaluated **before** `startListening()`: `db.isProductionEnv() && !DATABASE_URL && !db.memoryFallbackAllowed()` → `[FATAL] …` + `process.exit(1)` |
| `server/index.js:150-155` | Async `db.init` `ok:false` handler → production `process.exit(1)`, never serves from JSON |

## 5. Findings (fresh; reported, not fixed — no code changes in this Mission)

### F5 — `tools/wave1-gate.js` is RED (27/35) on an as-cloned tree, GREEN (35/35) only after `node server/seed.js` — **NOT-RESOLVED, severity raised**
PR #220's `F1` recorded this class of defect in `tests/pg-prod-suite-policy.js` / `tests/multi-instance.js`. **On BASE the same defect now sits inside `tools/wave1-gate.js` itself** — the very gate that P0-1 step ۴ depends on. Root cause: `server/data/payesh.json` is a **gitignored** artifact (`.gitignore:29 → server/data/`, confirmed `git check-ignore -v` → `.gitignore:29:server/data/ server/data/payesh.json`), so a clean checkout has no store and 8 of 14 behavioural suites abort on `ENOENT`. **Consequence: "wave1 gate 35/35 green" is true only under an undocumented precondition.** Owner: P0-1 package owner (Chat 6/1). Not fixed by Chat5 (no `tests/**`/`tools/**` writes allowed).

### F6 — CI is green while the Wave-1 gate is red — **NEW, contradicts PR #220's F4**
PR #220 recorded `CI: NOT-RUN (GitHub billing)`. **That is no longer true on BASE.** Recent workflow runs are `completed / success`:
```
34898824835  Node.js CI                 main  push          53s
34898824876  Security Program (SAST…)   main  push          1m27s
34898049223  Node.js CI                 main  push          43s   (merge of PR #216)
```
But `.github/workflows/node.js.yml` runs `npm ci → npm run build --if-present → npm test`, and `npm test` = `node tests/run.js && node --expose-gc --max-old-space-size=2048 tests/smoke.js`. `grep -rn "wave1\|pg-prod" .github/workflows/*.yml` → **0 matches**. And `tests/smoke.js` **passed on the unseeded tree** (48.1s ✅ in §3.1). ⇒ **CI green does not imply Wave-1 gate green; CI does not seed and does not run either P0-1 gate.** PR #220's F4 must be re-worded from "CI billing-blocked" to "CI running but P0-1 gates unwired".

### F7 — `ALLOW_MEMORY_FALLBACK` is now inert in *every* environment, not only in production
On BASE `memoryFallbackAllowed()` (`:90-93`) returns `false` in production and `true` unconditionally otherwise; the flag is read only by `memoryFallbackRequested()` (`:95-97`) and surfaces **only** as the diagnostic `flag_requested` field of `backingStorePolicy()` (`:109`). It is **not** part of the allow decision in any environment. PR #220 described the flag as "powerless in production" (correct, and still true); the fresh reading shows it is now **purely informational everywhere**. This is a documentation/behaviour drift worth recording, not a defect: production refusal is unaffected.

### F8 — Tracker card `پ0-۱` is unchanged since PR #220 and still describes a historical `main`
Card «مسیر رفع» is still `(۱/۵)` with steps ۲–۵ open (`⬜`), and its «توضیح» still references `main` @ `1c07a6f`. **I could not verify that SHA**: `git fetch origin 1c07a6f` → `fatal: couldn't find remote ref 1c07a6f` and `git cat-file -t 1c07a6f` → `Not a valid object name` — the object is not reachable from `main`'s fetched history (1789 commits after `git fetch --unshallow`). Recorded as **NOT-RUN**, not as "not an ancestor". By contrast `f852f29` (PR #220's base) **is** a verified ancestor of BASE (`git merge-base --is-ancestor f852f29 9888cb1…` → 0).

### F9 — Line-number drift vs PR #220's citations
PR #220 cited `db.js:72-114 / 146-157 / 249-254 / 836-837` and `index.js:1194-1204 / 146-157`. On BASE the same constructs live at `db.js:86-116 / 142-158 / 251-254 / 836-840` and `index.js:1199-1204 / 150-155`. The **constructs and behaviour are confirmed present**; only the line numbers moved. Any future citation must be re-measured against the current BASE.

## 6. CLAIM vs EVIDENCE

| Claim | Evidence on BASE `9888cb1` | Evidence level |
|---|---|---|
| Production refuses the JSON/in-memory store when `DATABASE_URL` is absent | `server/db.js:142-153` + `server/index.js:1199-1204` (code read) | **IMPLEMENTED** (on main, via PR #181) · gate behaviour **TESTED LOCALLY** in the PR #220 session, **not re-executed here** (no live/serverless prod-boot probe was in this Mission's required-test list) |
| Production refuses the fallback when PostgreSQL is unreachable | `server/db.js:251-254` (code read) | **IMPLEMENTED** · boot-level behaviour **NOT-RUN** this session (no reachable PostgreSQL in this sandbox — see NOT-RUN) |
| `ALLOW_MEMORY_FALLBACK` cannot reopen the production path | `server/db.js:90-97`, `:103-116` (code read) | **IMPLEMENTED** (verified by reading, not by execution, this session) |
| Wave-1 gate is green on `main` | **27/35 RED before seed · 35/35 GREEN after seed** (§3) | **TESTED LOCALLY** — conditionally green |
| Two instances, one PostgreSQL | 33/33, exit 0 (§3.1 and §3.2) | **TESTED LOCALLY** (pg-mem stand-in, **not** real PostgreSQL) |
| P0-1 behaves correctly in staging / production | nothing executed | **NOT-RUN** — no claim |
| P0-1 step ۴ satisfied | gate green **only** post-seed | **TESTED LOCALLY** with precondition · **not** CI, **not** staging |

No lower level is promoted to a higher one. `NOT-RUN ≠ PASS`.

## 7. Acceptance gaps (numbered)

- **AG-1 — step ۵ («حذف کد فال‌بک حافظه‌ای/جی‌سان از مسیر تولید»):** behaviourally satisfied (production is proven to refuse it), **literally not done** — the fallback code is still in the tree and is still the dev/test backing store (`server/db.js:158` returns `{ok:true, driver:'memory'}` in non-production). The wording is ambiguous between *"unreachable in production"* and *"deleted"*. Owner/supervisor decision; not Chat5's.
- **AG-2 — step ۲ (adjudication of the two competing implementations):** **no repository artifact** records the supervisor's decision. Reported as MISSING EVIDENCE; I do not infer either way.
- **AG-3 — step ۴ («اجرای `tools/wave1-gate.js` روی main مرج‌شده»):** satisfiable only after `node server/seed.js`. The acceptance criterion must state the seed precondition, or the gate must be made seed-independent. **This is the gap that changes the meaning of "green".**
- **AG-4 — tracker provenance:** the card's referenced `main` (`1c07a6f`) is unresolvable from `main`'s history ⇒ the card describes a historical tree. Re-baselining is an owner action.
- **AG-5 — automation:** neither `tools/wave1-gate.js` nor `tests/wave1-multi-instance.js` is wired into any workflow; CI runs `tests/run.js` + `tests/smoke.js` only, and `smoke.js` is green even when the Wave-1 gate is red (F6).

## 8. Delta vs PR #220's numbers (explicit, as required)

| Item | PR #220 (`f852f29`) | This Mission (`9888cb1`) | Verdict |
|---|---|---|---|
| `node tools/wave1-gate.js` | **35/35 GREEN**, exit 0, wall 98.8s | **35/35 GREEN**, exit 0 — **after seed**; **27/35 RED**, exit 1 — as-cloned | **Reproduced, but only under a precondition PR #220 did not state** → F5 |
| `node tests/wave1-multi-instance.js` | 33/33, exit 0 | 33/33, exit 0 (both clean and seeded) | **Reproduced exactly** |
| Static checks | 21 | 21 | Identical |
| CI | `NOT-RUN` (billing) | **CI is running and green**, but does not run either P0-1 gate; `smoke.js` green even when the gate is red → F6 | **Changed — F4 of #220 is stale** |
| `ALLOW_MEMORY_FALLBACK` | "powerless in production" | inert in **all** environments (diagnostic only) → F7 | Refined |
| P0-1 tracker card | `(۱/۵)`, steps ۲–۵ open | **unchanged**, still `(۱/۵)`, still references `1c07a6f` → F8 | No movement |
| `pg-prod-*` / policy suites | 7/9 clean, 9/9 seeded | **NOT-RUN** this session — not in this Mission's required-test list | Deliberate scope limit |
| Line citations | `db.js:72-114`, `index.js:1194-1204` | `db.js:86-116`, `index.js:1199-1204` → F9 | Drift |

## 9. NOT-RUN (with exact reason)

| Item | Reason |
|---|---|
| Live PostgreSQL suites (`tests/pg-prod-*.js`, `tools/wave1-gate.js` prod-boot probes) | **NOT-RUN — ENVIRONMENT LIMITATION**: no `postgres`/`psql` binary in this sandbox; only the pg-mem stand-in is available. Not in the Mission's required-test list. |
| `tests/pg-prod-suite-policy.js` (PR #220's F1/F2) | **NOT-RUN** — not in the required-test list; its subject (`tests/multi-instance.js` staleness) is out of scope for writes. |
| Redis-backed full production boot | **NOT-RUN — ENVIRONMENT LIMITATION**: no `redis-server` binary. |
| Verification of tracker SHA `1c07a6f` | **NOT-RUN** — `git fetch origin 1c07a6f` → `couldn't find remote ref`; object not reachable from `main`'s 1789-commit history. |
| CI run **on this Mission's PR** | recorded in §10 after the run |
| Staging / production verification | **NOT-RUN** — no staging or production environment exists or is reachable. No such claim is made anywhere in this report. |

## 10. Git / delivery receipts

| Stage | Receipt (verbatim) | State |
|---|---|---|
| Branch | `arena/01a0a199-p2` | — |
| Commit (report body) | `5252bcde712b3f72dfdcd4ad469e1a3d12893df3` — *"report(chat5): M1-CHAT5-20260914 — P0-1 independent verification on BASE 9888cb1 (gate 27/35 clean vs 35/35 seeded; F5-F9; AG-1..AG-5; delta vs PR #220)"* | **COMMITTED** |
| Files in commit | `git diff --cached --name-status` → `M docs/daily-reports/Chat5/2026-09-14.md` (**single path**; no `git add -A`) | ✅ |
| Push | `git push origin arena/01a0a199-p2` → `1eb2485..5252bcd arena/01a0a199-p2 -> arena/01a0a199-p2`; `git ls-remote origin refs/heads/arena/01a0a199-p2` → `5252bcde712b3f72dfdcd4ad469e1a3d12893df3` == local HEAD | **PUSHED** ✅ |
| PR | **#225** → https://github.com/rezaa2544/p2/pull/225 (base `main`, head `arena/01a0a199-p2`) | **PR OPEN → MERGED** |
| CI on PR #225 | `build (22.x)` **pass 41s** · `Secret scan` **pass 12s** · `SCA (npm audit)` **pass 11s** · `SBOM (SPDX)` **pass 13s** · `WAF & nginx config` **pass 29s** · `CodeRabbit` **pass** — **0 failures** | **CI GREEN** |
| CI caveat | None of the above run `tools/wave1-gate.js` or `tests/wave1-multi-instance.js` (`.github/workflows/node.js.yml` → `npm ci → npm run build --if-present → npm test`) ⇒ CI green **≠** Wave-1 gate green (see **F6 / AG-5**) | recorded |
| Merge | **merge commit** `fb130f547d3fdf09d67ed64642164e43d9390c18` · `mergedAt` `2026-09-14T22:00:50Z` · state **MERGED** | **MERGED** ✅ |
| `origin/main` after merge | `git ls-remote origin main` → `fb130f547d3fdf09d67ed64642164e43d9390c18` | — |
| Verify on main | `git ls-tree origin/main docs/daily-reports/Chat5/2026-09-14.md` → blob `38b22ce5446f133811f06388bb544eeec34e3ede`; `git show origin/main:…2026-09-14.md \| grep -c "M1-CHAT5-20260914"` → `2` | **VERIFIED ON MAIN** ✅ |
| Force-push | none | ✅ |

**Why the receipts live in a second commit/PR:** a report cannot contain its own commit SHA, and a merge commit SHA does not exist until after the merge. Commit `5252bcd` carried the report body; this block is the follow-up that records the chain it produced. Both commits touch **only** `docs/daily-reports/Chat5/2026-09-14.md`.

## 11. Blockers

1. **F5 blocks a clean reading of P0-1 step ۴**: until `tools/wave1-gate.js` is seed-independent (or the criterion states the precondition), "gate green on main" is not reproducible from a clean checkout.
2. **AG-1 / AG-2 are governance decisions**, not Chat5 execution items: step ۵ wording and step ۲ adjudication need the P0-1 owner / supervisor.
3. **AG-5**: P0-1 gates are unwired in CI while CI itself is green — a false-comfort condition (F6).

## 12. Suggested next action (proposal only — Chat1 decides)

1. Route **F5** to the P0-1 package owner: either make the eight suites seed-independent or add `node server/seed.js` to the gate's own preamble, and state the precondition in the P0-1 card.
2. Route **F6 / AG-5** to the release/CI owner: wire `tools/wave1-gate.js` (+ `tests/wave1-multi-instance.js`) into a workflow now that CI is running again; until then CI green must not be read as P0-1 green.
3. Route **AG-1 / AG-2 / AG-4** to the P0-1 owner and the supervisor: decide whether step ۵ means "unreachable in production" (proven → re-word) or "delete the fallback code" (→ code mission); record the step-۲ adjudication as a repository artifact; re-baseline the card off `1c07a6f`.
4. Next QA Mission suggestion: re-run the pg-prod family on BASE **if** a live PostgreSQL can be provisioned (currently NOT-RUN).
