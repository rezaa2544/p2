{
  "startedAt": "2026-09-14T17:09:47.403384+00:00",
  "finishedAt": "2026-09-14T17:09:47.442776+00:00",
  "command": "git merge-base 8bf2c00d9149cfce92f8af863ced422ec8755e1e 049ee713aec784e53688d1b5d3088666a688d063; git rev-list --left-right --count 8bf2c00d9149cfce92f8af863ced422ec8755e1e...049ee713aec784e53688d1b5d3088666a688d063; git diff --stat 8bf2c00d9149cfce92f8af863ced422ec8755e1e...049ee713aec784e53688d1b5d3088666a688d063; git diff --name-status 8bf2c00d9149cfce92f8af863ced422ec8755e1e...049ee713aec784e53688d1b5d3088666a688d063; git diff --no-ext-diff --unified=2 8bf2c00d9149cfce92f8af863ced422ec8755e1e...049ee713aec784e53688d1b5d3088666a688d063 -- docs/daily-reports; git merge-tree --write-tree --name-only 8bf2c00d9149cfce92f8af863ced422ec8755e1e 049ee713aec784e53688d1b5d3088666a688d063",
  "exitCode": 1,
  "stdout": "bb5068b6384c9ac0cb4f268d74e3efdad5a3d45c\n52\t6\n docs/daily-reports/2026-09-13.md | 887 +++++++++++++++++++++++++++++++++++++++\n docs/daily-reports/2026-09-14.md | 398 ++++++++++++++++++\n 2 files changed, 1285 insertions(+)\nM\tdocs/daily-reports/2026-09-13.md\nM\tdocs/daily-reports/2026-09-14.md\ndiff --git a/docs/daily-reports/2026-09-13.md b/docs/daily-reports/2026-09-13.md\nindex 5ffe3ca..2367b9f 100644\n--- a/docs/daily-reports/2026-09-13.md\n+++ b/docs/daily-reports/2026-09-13.md\n@@ -2056,2 +2056,889 @@ secret-scan ‏**12/12** · docs-stats-sync ‏**--check سبز پس از --free\n - ورودی HANDOFF ریشه — NOT-DONE (تناقضِ پرامپت؛ محتوای آماده بالاتر)\n - revoke توکن‌های افشاشده در چت (مالک)\n+\n+---\n+\n+## چت ۹ — دور ۵: پروب پذیرش غنی‌سازی دانه + خط‌مبنای قرمز (ثبت: 2026-09-13)\n+\n+**نقش:** چت ۹ — شبیه‌ساز/ممیزِ رفتاری · **صفر تغییر کد** (پروب فقط در `/tmp`؛ درختِ مخزن فقط‌خوانده شد).\n+**مبنای کار:** `main` @ `0a208f8` (پس از مرج #173 — ۱۴ سند یتیم بسته شد) · **شاخهٔ نشست:** `arena/01a09a73-p2`.\n+\n+### §۰) وضعیت و ضدتکرار (اجرای واقعی، نه بازگویی)\n+\n+| بند | شاهد (خروجیِ همین نشست) |\n+|---|---|\n+| head اصلی | `git ls-remote origin refs/heads/main` → `0a208f8a2fbb82c9ed742dba1dff40f3c0d06051` ✅ == مبنای بریف |\n+| #129 (چت ۳) | OPEN · `mergeable=CONFLICTING` — بلاک، صفر لمس ✅ |\n+| #157 (چت ۸) | OPEN · `mergeable=MERGEABLE` · مرج‌نشده — صفر لمس ✅ |\n+| #159 (BH) | OPEN · `mergeable=CONFLICTING` — صفر لمس ✅ |\n+| #173 | **MERGED** در `2026-09-13T09:15:10Z` (HEAD جاریِ main همان مرج است) ✅ |\n+| فهرست PRهای باز | دقیقاً ۳ مورد: #129 / #157 / #159 — **PRِ دانهٔ چت ۳ وجود ندارد** (§۳) |\n+| ضدتکرار | کارِ دور ۴ بازسازی نشد؛ فقط assertهای §۸ (سفرهای کلیکی) که هرگز اجرا نشده بودند اجرا شد. سوئیت‌های دور ۲ (`sim_full2`/`sim_full3`/…) بازاجرا **نشدند** (ممنوعِ بریف) |\n+| اتصال GitHub | **حینِ همین دور قطع شد** (`gh` ⇒ `HTTP 401: Bad credentials`؛ `git ls-remote` ⇒ `could not read Username`). شواهدِ بالا از خواندِ نخستِ نشست است؛ پوشِ §۹ مسدود شد |\n+| امنیت | ۶ PAT فاش‌شده، revoke توسط مالک. هیچ توکنی در این سند/کامیت/پروب نیست (`grep -E \"ghp_\\|github_pat_\"` روی پروب ⇒ ۰ هیت) |\n+\n+**انحرافِ شناسهٔ نشست (ثبت می‌شود؛ خودسرانه اصلاح نمی‌شود).** بریف شاخهٔ چت ۹ را `arena/01a097f8-p2 @ 15e60d0` و زنجیرهٔ `15e60d0 → [SHA]` را انتظار دارد؛ نشستِ Arena این دور روی `arena/01a09a73-p2` و از `0a208f8` (=main) گشوده شد و **`15e60d0` در main نیست** (`git merge-base --is-ancestor 15e60d0 origin/main` ⇒ خیر) ⇒ گزارشِ دور ۴ هنوز مرج نشده است. دو پیامد:\n+\n+1. **§۸ ماتریس از همان ref خوانده شد** (`git show 15e60d0:docs/daily-reports/2026-09-13-chat9-school-simulation.md`) — در `SEED_SIM_ROLES_ACCEPTANCE.md` روی `main` فقط §۱..§۴ هست و هیچ «§۸»ی وجود ندارد. سندِ مبنا **بدونِ ویرایش** ماند (read-only، طبقِ بندِ ۵ فاز ۰).\n+2. بخشِ این دور به **نسخهٔ main از همین فایل** الحاق می‌شود ⇒ الحاقِ بعدیِ شاخهٔ دور ۴ در همین فایل تعارضِ «هر دو تهٔ فایل» می‌سازد؛ رفعِ درست **union (append-only)** است — در زمینِ ناظر/چت ۶، نه چت ۹.\n+\n+### §۱) پروب موقت (فقط `/tmp` — صفر فایلِ تازه در مخزن)\n+\n+- `​/tmp/sim-accept-probe.js` · `sha256=2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8` · 566 خط · اجرا: `PAYESH_ROOT=<ریشه> node /tmp/sim-accept-probe.js --tag … --mode baseline|simulated-seed [--json …]`\n+- پوشش: assertهای **§۸-۱** (guard: `vis-new`/`vis-out` + رندرِ داشبوردِ پذیرش) · **§۸-۲** (lib_staff: `lib-lend`/`lib-return` + `inScope`) · **§۸-۳** (asset_staff: `as-status` + `inScope`) · چکِ سطحِ طراحیِ **§۸-۴** (`is_head`: grep شمارشی در `authz/model.json` + `src/js/*` + `server/**` + `migrations/*.sql`).\n+- سفرِ بیرون‌ازجعبه با **فلوِ واقعیِ محصول** ساخته شد، نه ست‌کردنِ `S.user`: `#lpn` (شماره) → کلیکِ `[data-act=\"login-code\"]` → کدِ `SmsPanel.demoCode()` → `#lnid` (کدِ ملی) → کلیکِ `[data-act=\"login\"]` → `finishLogin`. سپس `S.route=…; render()` → **کلیکِ حقیقیِ DOM** روی `data-act` → assert روی `db`/`byId`.\n+- `inScope` از **منبعِ یکتای سیاست** خوانده شد: `require(server/policy.js).inScope(session, store, coll, recId, data)` با `store` ساخته‌شده از `db` همان باندل — نه تقلیدِ کلاینتی.\n+- پاکیزگی: هر پرچم/رکوردِ آزمونی با نشانِ `_probe`/`_sim` در پایانِ همان سنجه **بازگردانده** می‌شود؛ `git status --porcelain` پس از دو اجرای کامل **تهی** ماند (§۷).\n+\n+### §۲) خط‌مبنای قرمز روی main — حالتِ شکستِ هر assert (نه فقط «قرمز»)\n+\n+جمعِ اجرا: **۳۰ سنجه · PASS=14 · FAIL=6 · NOT-RUN=10 · `exit=1`** (VERDICT: `RED`).\n+\n+| SIM | نقش | حالت شکست | چرا (زنجیرهٔ علّی) | پیامِ بازگشتیِ پروب |\n+|---|---|---|---|---|\n+| SIM-01 | guard | **FAIL** در `8-1.A` (پیش‌نیاز) ⇒ `8-1.D…H` **NOT-RUN** | `db.users.filter(u=>u.role==='guard').length` = **۰** ⇒ هیچ حسابی برای `login('guard_demo')` نیست ⇒ سفر در گامِ ۱ قطع می‌شود؛ رندر/کلیک/assert اجرا نمی‌شوند | `guard=0` · «کاربرِ غایب ⇒ login انجام نمی‌شود» · وابستگی `8-1.A` |\n+| SIM-02 | lib_staff | **FAIL** در `8-2.A` ⇒ `8-2.E` **NOT-RUN** | `role==='teacher' && lib_staff===1` = **۰** ⇒ نه کاربری برای ورود، نه کلیکِ `lib-lend` روی DOM | `lib_staff(دبیر)=0` · «کاربرِ غایب ⇒ گامِ login قطع می‌شود؛ هیچ کلیکِ حقیقی روی DOM انجام نمی‌شود» |\n+| SIM-03 | asset_staff | **FAIL** در `8-3.A` ⇒ `8-3.E` **NOT-RUN** | `role==='teacher' && asset_staff===1` = **۰** ⇒ سفرِ تحویلدار در گامِ اول می‌شکند | `asset_staff(دبیر)=0` · «سفر در گامِ اول قطع می‌شود» |\n+| SIM-04 | is_head | **FAIL×۳ سطحِ طراحی** (`8-4.A/B/C`) + `8-4.D` **SKIP** | `is_head` در `authz/model.json` (whitelistِ ۳۶ فیلدِ `users`)، `src/js/*.js` (۹۶ فایل)، `server/**` و `migrations/*.sql` **۰ هیت** است؛ تنها ۵ هیت در `tools/seed-office-data.js` (پیش‌نویسِ چت ۱/۲، «NOT wired anywhere») ⇒ seed = دادهٔ مرده | `۰ هیت · 0 هیت در src/+server/+migrations+authz [هیچ فایلی]` · `office_head=0` |\n+\n+**چهار نکتهٔ دقت که همین اجرا بیرون داد (اصلاحِ ماتریس، نه تغییرِ کد):**\n+\n+1. **قابلیت سالم است، دانه ناقص:** assertهای **بی‌نیاز از کاربر** روی همان main سبز شدند — `canAction('vis-new'/'vis-out','guard')=true` · `canRoute('visitors','guard')=true` (`05-router.js:15` ⇒ `guard:[['پذیرش',[['visitors','🚪','مهمان‌ها']]]]`) · `authz/write-perms.json` ⇒ `vis-save`/`vis-out` = `[manager, guard]` · گیت‌های `lib_staff`/`asset_staff` در `server/policy.js` (B.3 PASS). ⇒ شکافِ SIM-01..03 از جنسِ **داده** است، نه رفتار.\n+2. **منفی‌ها روی دادهٔ واقعی سبزند (fail-closed تأیید شد):** دبیرِ بی‌پرچم ⇒ `inScope('lib_loans')=false` · `inScope('assets',{status:'repair'})=false` · بین‌مدرسه‌ای (`user.school_id=2 ⊄ asset.school_id=1`) ⇒ `false` · نمایِ دانش‌آموز فاقدِ `lib-lend`.\n+3. **واژهٔ §۸-۱ «exited_at» در محصول وجود ندارد:** قراردادِ واقعی `visitors.out_at` است (و `authz/model.json → collections.visitors.fields` همان را whitelist می‌کند). پروب **هر دو** را می‌سنجد تا ادعای ماتریس «سبزِ جعلی» نشود؛ در کنترلِ §۴: `status=out · out_at=2026-… · exited_at=∅`. ⇒ اصلاحِ واژگانیِ §۸-۱ با **چت ۳/ناظر** است، نه تغییرِ کدِ چت ۹.\n+4. **پین‌ها روی main هم‌خوان‌اند و همین شاهدِ «دانه هنوز غنی نشده» است:** `tests/donations2.js:55` و `tests/drills2.js:54` ⇒ `db.users.length === 1035` == شمارِ واقعیِ دانه (خوانده‌شده از `db` درونِ باندل؛ `server/data/payesh.json` روی دیسکِ این کارست **نیست** — `server/data/` در `.gitignore`). در کنترلِ §۴ همین دو پین **قرمز** می‌شوند ⇒ ریسکِ §۴ ماتریس عملی شد: پین‌ها باید در **همان کامیتِ دانه** ۱۰۳۵→۱۰۳۶ شوند (بررسیِ هم‌کامیتِ پین‌ها: §۳).\n+\n+### §۳) راستی‌آزماییِ مشروطِ «دانهٔ چت ۳» — **NOT-RUN با دلیل**\n+\n+| SIM | نقش | وضعیت | دلیل |\n+|---|---|---|---|\n+| SIM-01 | guard | **NOT-RUN** | `feat/chat3-seed-sim-roles` (یا مشابه) وجود ندارد: ۰ از ۱۳۴ شاخهٔ remote؛ ۳ PRِ باز که هیچ‌کدام دانه نیستند (#129/#157/#159) |\n+| SIM-02 | lib_staff | **NOT-RUN** | همان؛ `gh pr list --search \"seed\" / \"sim\"` ⇒ تنها #172 (مرج‌شده — **فقط سندِ ماتریس**، بدونِ دموساز) |\n+| SIM-03 | asset_staff | **NOT-RUN** | همان |\n+| SIM-04 | is_head | **SKIP** | پیش‌نیازِ طراحیِ چت ۲ (`office_head` در برابرِ `is_head`) — §۸-۴ سه FAIL دارد |\n+\n+جست‌وجویِ فراگیر (شاهد): پیمایشِ `git grep -lE \"guard_demo|lib_staff_demo|asset_staff_demo\"` روی هر ۱۳۴ ref ⇒ **تنها یک هیت**: `15e60d0:docs/daily-reports/2026-09-13-chat9-school-simulation.md` — یعنی سندِ *پیشنهاددهندهٔ* همان نام‌های کاربری (خودِ چت ۹، دور ۴). ⇒ **هیچ پیاده‌سازیِ دانه‌ای در هیچ ref ای نیست**؛ کارِ چت ۳ هنوز «واگذاریِ صریحِ ناظر» را نگرفته است (§۳ سندِ ماتریس). worktree ایزوله ساخته **نشد** (refِ قابلِ بیرون‌کشیدن وجود نداشت)؛ «**بازراستی پس از مرج**» به‌عنوان قلمِ باز ثبت شد (§۱۰).\n+\n+### §۴) کنترلِ تفکیک‌پذیریِ پروب (نه سبزِ جعلی، نه جایگزینِ چت ۳)\n+\n+برای اینکه «قرمز»ی §۲ معنا داشته باشد، همان پروب در `--mode simulated-seed` اجرا شد: غنی‌سازی **فقط در حافظهٔ jsdom** و **دقیقاً طبقِ طرحِ §۳-۲ ماتریس** (۱ نگهبان پس از آخرین `add` تا diff حداقلی بماند + دو پرچم روی دبیرهای *موجود*، بدونِ کاربرِ تازه). صفر نوشتن روی دیسک.\n+\n+| سنجه | خط‌مبنا | کنترل (دانهٔ ساختگی) | معنی |\n+|---|---|---|---|\n+| شمارشِ دانه | ۱۰۳۵ · guard=0 · lib_staff=0 · asset_staff=0 | **۱۰۳۶** · guard=1 · lib_staff=1 · asset_staff=1 | دلتای `+1` ⇒ سازگار با **B5** |\n+| `8-1.D…H` (سفرِ نگهبان) | NOT-RUN ×۵ | **PASS ×۵** (ورودِ واقعی → داشبوردِ پذیرش → `vis-new`+`vis-save` → `vis-out`) | سفر با غنی‌سازی سبز می‌شود ⇒ پروب واقعاً می‌سنجد |\n+| `8-2.E` (سفرِ کتابدار) | NOT-RUN | **PASS** (`login` → render → `lib-lend=true`) | همان |\n+| `8-3.E` (سفرِ تحویلدار) | NOT-RUN | **PASS** (`as-status=true`) | همان |\n+| `8-2.C` / `8-3.D` (ظرفیت + `inScope`) | PASS | PASS | ظرفیتِ قابلیت مستقل از دانه است |\n+| `D.1` پین‌ها | PASS (۱۰۳۵==۱۰۳۵) | **FAIL** (۱۰۳۶ ≠ ۱۰۳۵) | **نشانهٔ درستِ انتظار**: پین‌ها باید هم‌کامیت با دانه به‌روز شوند |\n+| `8-4.A/B/C` (SIM-04) | FAIL ×۳ | **FAIL ×۳ (بدونِ تغییر)** | غنی‌سازیِ دانه **نمی‌تواند** SIM-04 را سبز کند — فقط طراحی/کدِ چت ۲ ⇒ تشخیصِ دور ۴ («SIM-04 از جنسِ SIM-01..03 نیست») **تأییدِ تجربی** گرفت |\n+\n+جمعِ کنترل: **PASS=23 · FAIL=4 · NOT-RUN=3 · `exit=1`** — چهار قرمزِ باقی‌مانده = `D.1` (انتظارِ §۴ ماتریس) + سه قرمزِ طراحیِ SIM-04؛ **هر چهار «قرمزِ درست»اند.** این بند **راستی‌آزماییِ چت ۳ نیست** و این‌طور هم برچسب نمی‌خورد؛ ادعایش فقط یکی است: *پروب تفکیک‌کننده است و معیارهای §۸ با یک PRِ کوچکِ دانه برآورده می‌شوند.*\n+\n+### §۵) قالب ENGINEERING CLAIM\n+\n+```text\n+EXPECTED BEHAVIOR:\n+  - SIM-01 (guard): login ⇒ render visitors ⇒ vis-new/vis-out ⇒ assertها pass\n+  - SIM-02 (lib_staff): login ⇒ render library ⇒ lib-lend/lib-return ⇒ assertها pass\n+  - SIM-03 (asset_staff): login ⇒ render assets ⇒ as-status ⇒ assertها pass\n+  - SIM-04 (is_head): FAIL سطح طراحی (is_head در مدل/کد نیست؛ فقط پیش‌نویسِ چت ۱/۲)\n+\n+MEASURED BEHAVIOR:\n+  - خط‌مبنای قرمز روی main @ 0a208f8: 30 سنجه ⇒ PASS=14 / FAIL=6 / NOT-RUN=10 · exit=1\n+      FAIL = 8-1.A (guard=0) · 8-2.A (lib_staff=0) · 8-3.A (asset_staff=0) · 8-4.A/B/C (is_head: ۰ هیت در مدل/کد)\n+      NOT-RUN = سفرهای کلیکیِ وابسته (8-1.D…H · 8-2.E · 8-3.E) + B.1/B.2 + 8-4.D (SKIP صریح)\n+  - راستی‌آزماییِ شاخهٔ دانهٔ چت ۳: NOT-RUN — اصلاً وجود ندارد (۰/۱۳۴ شاخه · ۰ PRِ دانه)\n+  - کنترلِ تفکیک‌پذیری (simulated-seed، درحافظه): PASS=23 · FAIL=4 · NOT-RUN=3\n+\n+TEST ENVIRONMENT:\n+  - main @ 0a208f8 · کارستِ تمیز (`git status --porcelain` پس از دو اجرا: تهی)\n+  - پروبِ موقت /tmp/sim-accept-probe.js (sha256=2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8)\n+  - jsdom 25 (بیرون از مخزن: /tmp/sim-probe-env) + باندلِ واقعیِ index.html + دانهٔ ۱۰۳۵/۱۰۳۶\n+  - Node v22.22.3 · بدونِ node_modules در مخزن ⇒ صفر وابستگیِ تازه به درخت\n+\n+DATASET SIZE:\n+  - دانهٔ فعلی main: ۱۰۳۵ کاربر (guard=0 · lib_staff=0 · asset_staff=0 · is_head=0) · ۶ مدرسه (۵ فعال) · teacher=72 · manager=12 · edu_office=4 (همگی بدونِ کلیدِ is_head)\n+  - دانهٔ غنی‌شدهٔ چت ۳: وجود ندارد ⇒ «شمارِ واقعی» تعریف‌نشده (NOT-RUN). در کنترلِ درحافظه: ۱۰۳۶\n+\n+CONCURRENCY: n/a (single-instance probe — یک jsdom، یک اجرا در هر حالت)\n+\n+P50 / P95 / P99: n/a (assert test، نه benchmark — هیچ عددی ساخته نشد)\n+\n+ERROR RATE:\n+  - خط‌مبنای قرمز: سنجه‌هایِ قابل‌اجرای دانه ۳ FAIL از 4 (≈75٪) · سفرهای کلیکی ۰ از ۰ (NOT-RUN)\n+  - کل سنجه‌ها: 6/30 = 20٪ (خط‌مبنا) · 4/30 ≈ 13.3٪ (کنترل)\n+  - راستی‌آزماییِ نهایی: ۰٪ — NOT-RUN (دانهٔ غنی‌شده‌ای برای سنجش نیست)\n+\n+DB IMPACT: n/a (فقط read روی `db` درونِ باندل · server/data/payesh.json در این کارست نیست · نوشتن‌هایِ آزمونی در حافظه برگشت داده شدند)\n+\n+CACHE IMPACT: n/a (هیچ Redis/کشی لمس نشد)\n+\n+SECURITY IMPACT:\n+  - tenant isolation: سه سنجهٔ منفی روی دادهٔ واقعی سبز — دبیرِ بی‌پرچم رد · تحویلدارِ بی‌پرچم رد · رکوردِ بیرون‌مدرسه رد\n+  - fail-closed: گیت‌های lib_staff/asset_staff در server/policy.js حاضرند (B.3 PASS)\n+  - secret-scan: **اجرا شد روی همین درختِ پس از الحاقِ گزارش ⇒ 12/12 سبز** (شاهدِ تازهٔ امروز، نه بازگوییِ ادعایِ پس از #170) ⇒ اثباتِ اینکه هیچ توکنی در این بخش/پروب کامیت‌شده نیست\n+\n+FAILURE TEST:\n+  - خط‌مبنای قرمز = failure test طبیعی (دانهٔ ناقص ⇒ سفر قطع می‌شود) + جهشِ معکوس (دانهٔ ساختگی ⇒ سبز) ⇒ پروب نه همیشه‌قرمز است نه همیشه‌سبز\n+\n+RECOVERY RESULT: n/a (بازیابی معنا ندارد: صفر تغییرِ محصول؛ rollback = rm پروب + revertِ کامیتِ گزارش)\n+\n+REGRESSION RISK:\n+  - صفر تغییرِ کد؛ فقط پروبِ /tmp + یک الحاقِ append-only به گزارشِ روزانه (زیرپوشه ⇒ بدونِ بامپِ rc)\n+  - تنها ریسکِ باقی‌مانده: تعارضِ الحاقیِ همین فایل با شاخهٔ مرج‌نشدهٔ دور ۴ (رفع: union — در زمینِ ناظر/چت ۶)\n+\n+KNOWN UNKNOWNs:\n+  - CI NOT-RUN (انسدادِ بیلینگ Actions — RISK-O-007)\n+  - ظرفیتِ ملی اثبات‌نشده (این دور فقط completeness بود؛ هیچ ادعایِ کارایی/ظرفیت نشد)\n+  - زمان/تصمیمِ مرجِ PRِ دانهٔ چت ۳ نامشخص (واگذاریِ صریحِ ناظر معلق)\n+  - SIM-04 منتظرِ طراحیِ چت ۲ (office_head در برابرِ is_head)\n+  - اتصالِ GitHubِ این نشست پیش از پوش قطع شد ⇒ زنجیرهٔ SHA فقط محلی است\n+\n+FINAL VERDICT:\n+  - خط‌مبنای قرمز: **NOT-RUN (دانهٔ ناقص)** برای سفرهای SIM-01..03 — طبیعی و انتظار‌شده؛ خودِ «سنجشِ دقیقِ شکست» PASS ثبت می‌شود (۶ FAIL با حالتِ شکستِ کامل)\n+  - SIM-04: **FAILِ سطحِ طراحی** (تأییدِ تجربی در §۴)\n+  - راستی‌آزماییِ دانهٔ چت ۳: **NOT-RUN** (وجود ندارد) ⇒ «بازراستی پس از مرج» قلمِ باز\n+```\n+\n+**Acceptance (قالبِ جدیدِ ۱۴ بندی):** `COMMIT` = کامیتِ همین گزارش (§۹) · `FILES` = `docs/daily-reports/2026-09-13.md` تنها فایلِ تغییریافته (append-only؛ صفر فایلِ تازه) · `TEST COMMANDS` = `PAYESH_ROOT=$PWD node /tmp/sim-accept-probe.js --tag main@0a208f8 --mode baseline --json /tmp/probe-main.json` ⟩ و ⟩ `--mode simulated-seed` · `RAW OUTPUT` = پیوستِ ب (هر دو جدول، بایت‌به‌بایت) · `DATABASE` = n/a (read-only) · `MIGRATION` = none (دانه در `src/js/02-demo-data.js` ساخته می‌شود؛ مهاجرتِ بعدی ۰۱۳ دست‌نخورده) · `SECURITY` = ۶ PAT فاش‌شده، revoke توسط مالک · صفرِ توکن در سند/کامیت/پروب · `PERFORMANCE` = n/a · `EXPLAIN` = n/a · `BENCHMARK` = n/a (خط‌قرمزِ «بنچِ بی workload واقعی») · `OBSERVABILITY` = JSONِ هر-سنجه (`status`/`why`/`depends_on`) + sha256ِ پروب · `ROLLBACK` = `git revert` همین کامیت + `rm -rf /tmp/sim-accept-probe.js` · `CI RUN` = **NOT-RUN** (بیلینگ).\n+\n+### §۶) گیتِ ۸ سؤالی (هیچ‌کدام بی‌جواب نمانده)\n+\n+| # | سؤال | پاسخ (با شاهدِ همین دور) |\n+|---|---|---|\n+| ۱ | چه چیزی تغییر کرد؟ | هیچ کد/تست/دانه‌ای در مخزن. افزوده = پروبِ `/tmp` + این بخشِ گزارش |\n+| ۲ | چرا تغییر کرد؟ | اثباتِ ناقص‌بودنِ دانهٔ فعلی (سه پیش‌نیازِ A1..A3 با حالتِ شکستِ دقیق) و اثباتِ اینکه معیارهای §۸ با غنی‌سازیِ §۳-۲ برآورده‌شدنی‌اند (کنترلِ §۴) |\n+| ۳ | چه invariant ای باید حفظ شود؟ | صفر تغییرِ کد؛ فقط پروب و گزارش. درخت پس از دو اجرا `git status` تهی · اسناد ریشهٔ قفل‌شده ویرایش نشد · rc بامپ نشد (مالکیتِ چت ۶) |\n+| ۴ | چگونه ثابت می‌کنیم درست است؟ | ۳۰ assert با تاکسونومی PASS/FAIL/NOT-RUN و ورودیِ خام · ورودِ واقعی از فرمِ محصول · کلیکِ واقعی روی `data-act` · `inScope` از `server/policy.js` |\n+| ۵ | چگونه ثابت می‌کنیم سریع‌تر/مقیاس‌پذیرتر است؟ | **n/a** — این دور فقط completeness بود؛ هیچ عددِ کارایی ساخته نشد |\n+| ۶ | چگونه ثابت می‌کنیم امن است؟ | سه سنجهٔ منفیِ جداسازیِ tenant روی دادهٔ واقعی + تأییدِ fail-closed در policy + صفرِ راز در هر artifact |\n+| ۷ | چگونه failure آن را تست می‌کنیم؟ | خودِ خط‌مبنای قرمز failure test است + جهشِ معکوس (§۴) ⇒ تفکیک‌پذیریِ پروب ثابت شد |\n+| ۸ | چگونه rollback می‌کنیم؟ | `rm -rf /tmp/sim-accept-probe.js /tmp/sim-probe-env` + `git revert` همین کامیت (فقط یک فایلِ گزارش) |\n+\n+### §۷) ماشین و REZA (اجرای واقعی روی همین درخت)\n+\n+| گیت | دستور | خروجیِ واقعی | وضعیت |\n+|---|---|---|---|\n+| همگامیِ آمار | `node tools/docs-stats-sync.js --check` | «✅ آمارِ مستندات با دیسک یکی است» · ۳۷۶ سند (۳۲۴ ریشه + ۵۲ زیرپوشه) · قفل `rc40` · ۴۷۵ تست | PASS |\n+| قفلِ یخ | `node tests/docs-freeze-marker.js` | «نتیجه: ۱۴ موفق / ۰ ناموفق (از ۱۴)» | **PASS 14/14** |\n+| متادیتا/یتیم | `node tests/docs-metadata.js` | «نتیجه: ۱۷ موفق / ۰ ناموفق» · «هیچ سند یتیمی وجود ندارد» (پس از #173) | **PASS 17/17 · یتیم=۰** |\n+| آینهٔ reza | `node tools/reza-mirror-check.js` | «✅ همهٔ آینه‌های زنده با منبعِ docs/ برابرند» (سه اسنپ‌شاتِ باندل با یادداشتِ خودِ ابزار) | **PASS · ۰ drift / ۰ اختراعِ آینهٔ تازه** |\n+| رازیاب | `node tests/secret-scan.js` | «secret-scan: 12/12 سبز ✅» — **روی همین درخت، با حضورِ این بخشِ گزارش** ⇒ صفرِ توکن در آنچه کامیت می‌شود (شاهدِ تازهٔ همین دور، نه ارجاعِ #170) | PASS 12/12 |\n+\n+هیچ سندِ **تازه‌ای** به مخزن افزوده نشد (فقط یک بخش به فایلِ موجود) ⇒ بامپِ rc و ردیفِ نمایه لازم نشد و `docs-metadata` هم یتیمِ تازه نساخت.\n+\n+### §۸) بودجه (سنجهٔ §۱ `WORKSPACE_HYGIENE.md`)\n+\n+| لحظه | مؤثر | فایل |\n+|---|---|---|\n+| پیش از کار | 93.5 MB | 1395 |\n+| پس از کار (اندازه‌گیری‌شده پس از الحاق) | **93.6 MB** 🟢 | **1395** (صفر فایلِ تازه) |\n+\n+🟢 زیرِ هر دو آستانه (<۱۰۰MB · <۸۰۰۰ فایل). `node_modules` در مخزن ساخته **نشد**؛ jsdom در `/tmp` است و در شمارندهٔ §۱ نمی‌آید.\n+\n+### §۹) پوش (final-push-last-write) — **مسدودِ اعتبارنامه**\n+\n+| بند | نتیجه |\n+|---|---|\n+| کامیت روی `arena/01a09a73-p2` | ساخته شد (پایه `0a208f8`؛ زنجیرهٔ `0a208f8 → این کامیت` — انحراف از `15e60d0` در §۰ مستند شد). SHA در `git log -1` همین شاخه؛ خودارجاعِ SHA در سندِ همان کامیت ممکن نیست |\n+| `git push origin arena/01a09a73-p2` | ⛔ **NOT-RUN/BLOCKED** — اجرای واقعی: `fatal: could not read Username for 'https://github.com': terminal prompts disabled` (`exit=128`)؛ `gh` هم `HTTP 401: Bad credentials` داد. اتصالِ GitHubِ نشست **میانهٔ همین دور** از کار افتاد (خواندِ فاز ۰ پیش از قطع انجام شد). توکن‌های جایگزین در این دور مصرف **نشدند** (خطِ امنیتی بریف: هیچ توکنی در فایل/کامیت/گزارش) |\n+| `git ls-remote == HEAD` | ⛔ NOT-RUN (همان دلیل) — پس از وصل‌شدنِ مجددِ GitHub در Arena، `git push origin arena/01a09a73-p2` کافی است و این جدول در دورِ بعد سبز می‌شود |\n+\n+### §۱۰) قلم‌های باز (ارجاع به مالک — بدونِ بدهیِ بامپِ تازه)\n+\n+| قلم | مالک | توضیح |\n+|---|---|---|\n+| واگذاریِ صریحِ ماتریس ⇒ اجرای `feat/chat3-seed-sim-roles` (۱ نگهبان + ۲ پرچم + به‌روزرسانیِ پین‌ها) | **ناظر → چت ۳** | `SEED_SIM_ROLES_ACCEPTANCE.md` §۳ هنوز «منتظرِ واگذاری» است؛ ۰ شاخهٔ دانه |\n+| بازراستیِ پس از مرج (انتظار: سبزِ SIM-01..03 با **همین** پروب روی worktree ایزوله) | **چت ۹ (دورِ بعد)** | پروب و معیارها در پیوستِ الف بایگانی شده‌اند ⇒ بازتولیدِ کامل بدونِ کدِ مخزن |\n+| اصلاحِ واژگانیِ §۸-۱ (`exited_at` → `out_at`) | **چت ۳/ناظر** | §۲-۳ |\n+| طراحیِ SIM-04 (`office_head` یا `is_head` + مصرف‌کننده در policy/UI/مدل) | **چت ۲** | تا پیش از آن seed = دادهٔ مرده (§۴: سه FAIL بدونِ تغییر) |\n+| HO-4 (بامپِ rc40→rc41) + رفعِ تعارضِ الحاقیِ `2026-09-13.md` با `15e60d0` | **چت ۶** | چت ۹ عمداً `tests/docs-freeze-marker.js` و اسناد ریشه را لمس نکرد |\n+| بلاک‌های ناظر: #129 (CONFLICTING) · #157 (MERGEABLE/بلاکِ مرج) · #159 (CONFLICTING) | **ناظر** | صفر لمس در این دور |\n+| وصلِ مجددِ GitHub در Arena ⇒ پوشِ §۹ | **مالک/پلتفرم** | `gh` ⇒ HTTP 401 · `git` ⇒ prompt-disabled |\n+| revoke توکن‌ها | **مالک** | ۶ PAT فاش‌شده، revoke توسط مالک |\n+\n+---\n+\n+### پیوستِ الف — متنِ کاملِ پروب (قابلِ بازتولید بدونِ کدِ مخزن)\n+\n+ذخیره در `​/tmp/sim-accept-probe.js` (بایت‌به‌بایت؛ `sha256sum` باید `2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8` بدهد):\n+\n+```bash\n+cd <کارستِ p2> && PAYESH_ROOT=\"$PWD\" node /tmp/sim-accept-probe.js --tag main --mode baseline\n+# کنترلِ تفکیک‌پذیری:  --mode simulated-seed\n+```\n+\n+```js\n+#!/usr/bin/env node\n+/* ═══════════════════════════════════════════════════════════════════════\n+   /tmp/sim-accept-probe.js — پروب پذیرشِ غنی‌سازی دانه (SIM-01..04)\n+   چت ۹ — دور ۵ · صفر تغییر کد · فقط‌خواندنی روی درختِ هدف\n+\n+   اجرا:\n+     PAYESH_ROOT=<ریشهٔ درخت> node /tmp/sim-accept-probe.js [--tag main@sha] [--json /tmp/out.json]\n+   متغیرها:\n+     PAYESH_ROOT  پیش‌فرض: cwd        (درختی که پروب سنجیده می‌شود)\n+     SIM_JSDOM    پیش‌فرض: /tmp/sim-probe-env/node_modules/jsdom\n+\n+   منشأ معیارها: docs/daily-reports/SEED_SIM_ROLES_ACCEPTANCE.md (A1..A4/B1..B5)\n+                  + §۸ الحاقیِ دور ۴ (سفرهای کلیکی) در\n+                    docs/daily-reports/2026-09-13-chat9-school-simulation.md\n+   تاکسونومی هر سنجه: PASS · FAIL · NOT-RUN (با دلیل و وابستگی)\n+   خروج: ۰ = هیچ FAIL نیست (سبز) · ۱ = دست‌کم یک FAIL (قرمز) · ۳ = پروب بالا نیامد\n+   ═══════════════════════════════════════════════════════════════════════ */\n+'use strict';\n+const fs = require('fs');\n+const path = require('path');\n+const cp = require('child_process');\n+\n+const ROOT = path.resolve(process.env.PAYESH_ROOT || process.cwd());\n+const JSDOM_MOD = process.env.SIM_JSDOM || '/tmp/sim-probe-env/node_modules/jsdom';\n+const argv = process.argv.slice(2);\n+const TAG = argv.includes('--tag') ? argv[argv.indexOf('--tag') + 1] : path.basename(ROOT);\n+const JSON_OUT = argv.includes('--json') ? argv[argv.indexOf('--json') + 1] : null;\n+/* --mode simulated-seed : غنی‌سازی *درحافظه* طبقِ طرحِ §۳-۲ ماتریس (۱ نگهبان + دو پرچم روی دبیرهای موجود)\n+   — فقط برای اثباتِ اینکه پروب تفکیک‌کننده است (RED↔GREEN). صفر تغییرِ دیسک/مخزن؛ جانشینِ راستی‌آزماییِ چت ۳ نیست. */\n+const MODE = argv.includes('--mode') ? argv[argv.indexOf('--mode') + 1] : 'baseline';\n+\n+/* ───────────────────────── recorder ───────────────────────── */\n+const A = []; /* هر سنجه */\n+const NOTES = [];\n+let seq = 0;\n+function entry(sim, id, expectDesc) {\n+  return { n: ++seq, sim, id, expect: expectDesc };\n+}\n+function check(sim, id, expectDesc, thunk) {\n+  const e = entry(sim, id, expectDesc);\n+  try {\n+    const r = thunk();\n+    if (r && r.skip) {\n+      e.status = 'NOT-RUN';\n+      e.actual = r.actual == null ? '—' : String(r.actual);\n+      e.why = r.why || 'پیش‌نیازِ سفر فراهم نشد';\n+      e.depends_on = r.depends_on || null;\n+    } else {\n+      e.status = r.ok ? 'PASS' : 'FAIL';\n+      e.actual = String(r.actual);\n+      if (!r.ok) e.why = r.why || 'شرط برقرار نشد';\n+      if (r.note) e.note = r.note;\n+    }\n+  } catch (err) {\n+    e.status = 'FAIL';\n+    e.actual = 'استثنا';\n+    e.why = 'پیامِ پروب: ' + (err && err.message ? err.message : String(err));\n+  }\n+  A.push(e);\n+  return e;\n+}\n+function note(s) { NOTES.push(s); }\n+function readRel(p) {\n+  try { return fs.readFileSync(path.join(ROOT, p), 'utf8'); } catch (e) { return null; }\n+}\n+function walk(dir, filter, acc) {\n+  acc = acc || [];\n+  let ents;\n+  try { ents = fs.readdirSync(path.join(ROOT, dir), { withFileTypes: true }); } catch (e) { return acc; }\n+  for (const d of ents) {\n+    const rel = dir ? dir + '/' + d.name : d.name;\n+    if (d.isDirectory()) { if (d.name !== 'node_modules' && d.name !== '.git') walk(rel, filter, acc); }\n+    else if (filter.test(d.name)) acc.push(rel);\n+  }\n+  return acc;\n+}\n+function countHits(relFiles, re) {\n+  let total = 0; const files = [];\n+  for (const f of relFiles) {\n+    const t = readRel(f); if (!t) continue;\n+    const m = t.match(new RegExp(re.source, 'g'));\n+    if (m && m.length) { total += m.length; files.push(f + ':' + m.length); }\n+  }\n+  return { total, files };\n+}\n+\n+/* ═══════════════ فاز ایستا (بی‌نیاز از jsdom) — §۸-۴ سطح طراحی ═══════════ */\n+const srcJs = walk('src/js', /\\.js$/);\n+const serverJs = walk('server', /\\.js$/);\n+const migrations = walk('migrations', /\\.sql$/);\n+const modelRaw = readRel('authz/model.json') || '';\n+const writePermsRaw = readRel('authz/write-perms.json') || '';\n+\n+let MODEL = null, WP = null;\n+try { MODEL = JSON.parse(modelRaw); } catch (e) { note('authz/model.json قابل پارس نبود: ' + e.message); }\n+try { WP = JSON.parse(writePermsRaw); } catch (e) { note('authz/write-perms.json قابل پارس نبود: ' + e.message); }\n+\n+const isHeadProduct = countHits([].concat(srcJs, serverJs, migrations, ['authz/model.json', 'authz/write-perms.json']), /is_head/g);\n+const isHeadTool = countHits(['tools/seed-office-data.js'], /is_head/g);\n+const officeHeadProduct = countHits([].concat(srcJs, serverJs, ['authz/model.json', 'authz/write-perms.json']), /office_head/g);\n+const consumerCount = countHits([].concat(srcJs, serverJs), /is_head/g);\n+\n+/* ═════════════════════════ boot: jsdom + index.html ══════════════════════ */\n+let JSDOM = null, VCtor = null, jsdomErr = null;\n+try { const m = require(JSDOM_MOD); JSDOM = m.JSDOM; VCtor = m.VirtualConsole; }\n+catch (e) { jsdomErr = e.message; }\n+\n+const sleep = (ms) => new Promise((r) => setTimeout(r, ms));\n+const consoleErrors = [];\n+let win = null, W = null;\n+\n+const travel = { guard: { ok: false }, lib: { ok: false }, asset: { ok: false } };\n+let census = null, store = null;\n+\n+async function main() {\n+  if (!JSDOM) {\n+    /* همهٔ سنجه‌های سفر/داده NOT-RUN می‌مانند؛ سنجه‌های ایستا اجرا می‌شوند */\n+    note('jsdom در دسترس نیست ⇒ سفرها و سنجه‌های زندهٔ دانه NOT-RUN (' + jsdomErr + ')');\n+  } else {\n+    const html = readRel('index.html');\n+    if (!html) { console.error('FATAL: index.html در این درخت نیست: ' + path.join(ROOT, 'index.html')); process.exit(3); }\n+    const dom = new JSDOM(html, {\n+      runScripts: 'dangerously', pretendToBeVisual: true, url: 'http://localhost/',\n+      virtualConsole: new VCtor().on('jsdomError', (e) => consoleErrors.push(e.message)).on('error', (m) => consoleErrors.push(String(m))),\n+    });\n+    win = dom.window; W = (expr) => win.eval(expr);\n+    await sleep(600);\n+    const j = (expr) => JSON.parse(W('JSON.stringify(' + expr + ')'));\n+    probeApi = { j, sleep };\n+  }\n+\n+  const j = probeApi && probeApi.j;\n+  const sleepP = probeApi && probeApi.sleep;\n+\n+  /* ── ۰) سلامت بوت ── */\n+  check('BOOT', 'B.0', 'بوتِ باندل در jsdom بدونِ خطای کنسول', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست ⇒ بوت ممکن نبود (' + jsdomErr + ')' };\n+    return { ok: consoleErrors.length === 0, actual: consoleErrors.length + ' خطای کنسول' + (consoleErrors.length ? ' | ' + consoleErrors.slice(0, 2).join(' ⋅ ') : '') };\n+  });\n+\n+  /* ── ۰.۵) کنترلِ تفکیک‌پذیری: دانهٔ غنی‌شدهٔ شبیه‌سازی‌شده (فقط در حافظه) ── */\n+  if (j && MODE === 'simulated-seed') {\n+    const e = W(`(function(){\n+      try{\n+        var visSid=(db.visitors[0]&&db.visitors[0].school_id)||1;\n+        var libSid=((db.lib_books||[]).filter(function(b){return b.school_id;})[0]||{}).school_id||visSid;\n+        var asSid=((db.assets||[]).filter(function(a){return a.school_id;})[0]||{}).school_id||visSid;\n+        var g=add('users',{school_id:visSid,role:'guard',full_name:'نگهبانِ نمونهٔ پروب',username:'guard_demo',\n+          password:'123456',national_id:nid(),phone:demoPhone(),active:1,title:'نگهبان/میزِ پذیرش',created_at:'2026-09-01T06:30:00.000Z'});\n+        var lt=db.users.filter(function(x){return x.role==='teacher'&&x.school_id===libSid&&x.active!==0;})[0];\n+        var at=db.users.filter(function(x){return x.role==='teacher'&&x.school_id===asSid&&x.active!==0&&(!lt||x.id!==lt.id);})[0];\n+        if(lt) lt.lib_staff=1;\n+        if(at) at.asset_staff=1;\n+        db.users.forEach(function(u){ if(u.id===(g&&g.id)||u===lt||u===at) u._sim=1; });\n+        return 'ok:guard='+((g&&g.id)||'∅')+' · lib_staff→'+(lt?lt.id:'∅')+' · asset_staff→'+(at?at.id:'∅');\n+      }catch(err){ return 'EXCEPTION: '+err.message; }\n+    })()`);\n+    note('MODE=simulated-seed ⇒ ' + e + ' (دانهٔ غنی‌شدهٔ *درحافظه* — طرحِ §۳-۲ ماتریس؛ صفر تغییرِ دیسک؛ جانشینِ مرجِ چت ۳ نیست)');\n+  }\n+\n+  /* ── ۱) شمارشِ دانه (DATASET SIZE) ── */\n+  census = (() => {\n+    if (!j) return null;\n+    return j(`(function(){\n+      var U=db.users;\n+      function c(f){return U.filter(f).length;}\n+      var eo=U.filter(function(u){return u.role==='edu_office';}).map(function(u){return {id:u.id,username:u.username,office_id:u.office_id==null?null:u.office_id,is_head:('is_head' in u)?u.is_head:'__absent__'};});\n+      return {users:U.length, active:c(function(u){return u.active!==0;}),\n+        schools:db.schools.length, schoolsActive:db.schools.filter(function(s){return s.active===1||s.active===true;}).length,\n+        guard:c(function(u){return u.role==='guard';}),\n+        guardActive:c(function(u){return u.role==='guard'&&u.active!==0&&u.school_id;}),\n+        lib_staff:c(function(u){return u.lib_staff===1;}),\n+        lib_staff_teacher:c(function(u){return u.role==='teacher'&&u.lib_staff===1&&u.school_id;}),\n+        asset_staff:c(function(u){return u.asset_staff===1;}),\n+        asset_staff_teacher:c(function(u){return u.role==='teacher'&&u.asset_staff===1&&u.school_id;}),\n+        is_head_truthy:c(function(u){return !!u.is_head;}),\n+        is_head_key_present:c(function(u){return 'is_head' in u;}),\n+        teacher:c(function(u){return u.role==='teacher';}),\n+        manager:c(function(u){return u.role==='manager';}),\n+        edu_office:U.filter(function(u){return u.role==='edu_office';}).length,\n+        edu_office_rows:eo,\n+        lib_books:db.lib_books?db.lib_books.length:0,\n+        lib_loans:db.lib_loans?db.lib_loans.length:0,\n+        assets:db.assets?db.assets.length:0,\n+        visitors:db.visitors?db.visitors.length:0\n+      };})()`);\n+  })();\n+  if (census) {\n+    note('دانهٔ زنده (از db درون باندل): ' + JSON.stringify(census));\n+    check('DATASET', 'D.1', 'شمارِ کاربرانِ دانه با پین‌های تستی هم‌خوان است', () => {\n+      const pins = pinsOf();\n+      const mismatched = pins.filter((p) => p.pinned != null && p.pinned !== census.users);\n+      return { ok: mismatched.length === 0, actual: 'users=' + census.users + ' · پین‌ها: ' + pins.map((p) => p.file + ':' + p.line + '=' + p.pinned).join(' , ') + (mismatched.length ? ' ⇒ ناهم‌خوان' : ''),\n+        why: mismatched.length ? 'پینِ شمارِ کاربران با دانهٔ جاری نمی‌خواند (یا دانه تغییر کرده و پین به‌روز نشده، یا برعکس)' : '',\n+        note: pins.map((p) => p.file + ':' + p.line + ' → ' + p.text).join('\\n')\n+          + (MODE === 'simulated-seed' ? '\\n  ⚠️ در mode=simulated-seed قرمز شدنِ این سنجه **انتظارِ درست** است: ریسکِ ثبت‌شدهٔ §۴ ماتریس — دو پین باید در همان کامیتِ دانه ۱۰۳۵→۱۰۳۶ شوند' : '') };\n+    });\n+    check('DATASET', 'D.2', 'کاربرانِ edu_office در دانه (بازدیدِ is_head روی داده)', () => ({\n+      ok: census.edu_office > 0, actual: 'edu_office=' + census.edu_office + ' · ردیف‌ها: ' + JSON.stringify(census.edu_office_rows),\n+      why: 'هیچ کاربرِ اداره‌ای در دانه نیست' }));\n+  }\n+\n+  /* ── ۲) §۸-۱ — SIM-01: نگهبان (guard) × مراجعین E.9 ── */\n+  const GUARD_USERNAME = 'guard_demo';\n+  const canGuard = (() => {\n+    if (!j) return null;\n+    return j(`(function(){return {visNew:canAction('vis-new','guard'),visOut:canAction('vis-out','guard'),route:canRoute('visitors','guard')};})()`);\n+  })();\n+  check('SIM-01', '8-1.A', 'دانه ≥۱ کاربرِ guard فعال با school_id معتبر (معیار A1)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست ⇒ شمارشِ db ممکن نبود' };\n+    return { ok: census.guardActive >= 1, actual: census.guardActive,\n+      why: 'دانه هیچ کاربرِ guard ندارد ⇒ login(' + GUARD_USERNAME + ') حسابی پیدا نمی‌کند و سفر در گام ۱ قطع می‌شود (قابلیت در کد هست، داده نیست)' };\n+  });\n+  check('SIM-01', '8-1.B', \"canAction('vis-new'/'vis-out','guard') === true\", () => {\n+    if (!canGuard) return { skip: true, why: 'نیازمند بوتِ باندل (jsdom)' };\n+    return { ok: canGuard.visNew === true && canGuard.visOut === true, actual: 'vis-new=' + canGuard.visNew + ', vis-out=' + canGuard.visOut,\n+      why: 'ACTION_ROLES نگهبان را نمی‌پذیرد' };\n+  });\n+  check('SIM-01', '8-1.C', \"canRoute('visitors','guard') === true (منوی «پذیرش»)\", () => {\n+    if (!canGuard) return { skip: true, why: 'نیازمند بوتِ باندل (jsdom)' };\n+    return { ok: canGuard.route === true, actual: String(canGuard.route), why: 'مسیرِ مراجعین برای نگهبان باز نیست' };\n+  });\n+  check('SIM-01', '8-1.D', 'ورودِ واقعیِ نگهبان با فلوِ شماره+کد+کد ملی (login «' + GUARD_USERNAME + '»)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    if (!census.guardActive) return { skip: true, actual: 'guard=0', why: 'کاربرِ غایب ⇒ login انجام نمی‌شود', depends_on: '8-1.A' };\n+    const r = uiLogin(GUARD_USERNAME, \"x.role==='guard'\");\n+    travel.guard.ok = r.ok; travel.guard.user = r.user;\n+    return { ok: r.ok, actual: r.actual, why: r.why, note: r.via };\n+  });\n+  check('SIM-01', '8-1.E', 'رندرِ داشبورد پذیرش (کارتِ «🚪 مراجعین» در homeٔ نگهبان)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    if (!travel.guard.ok) return { skip: true, why: 'سفر در گام ورود قطع شد', depends_on: '8-1.D' };\n+    const h = W(`(function(){S.route='dashboard';render();return document.getElementById('root').innerHTML;})()`);\n+    return { ok: h.indexOf('مراجعین') > -1, actual: 'کارتِ مراجعین ' + (h.indexOf('مراجعین') > -1 ? 'حاضر' : 'غایب'), why: 'کارتِ پذیرش در داشبوردِ نگهبان رندر نشد' };\n+  });\n+  check('SIM-01', '8-1.F', 'رندرِ مسیر visitors شامل data-act=\"vis-new\" (و بدونِ «دسترسی مجاز نیست»)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    if (!travel.guard.ok) return { skip: true, why: 'سفر قطع شده', depends_on: '8-1.D' };\n+    const h = W(`(function(){S.route='visitors';S.filters={};render();return document.getElementById('root').innerHTML;})()`);\n+    const ok = h.indexOf('data-act=\"vis-new\"') > -1 && h.indexOf('دسترسی مجاز نیست') === -1;\n+    return { ok, actual: 'vis-new=' + (h.indexOf('data-act=\"vis-new\"') > -1) + ' · forbidden=' + (h.indexOf('دسترسی مجاز نیست') > -1), why: 'دکمهٔ ثبتِ مهمان برای نگهبان رندر نشد' };\n+  });\n+  check('SIM-01', '8-1.G', 'کلیکِ vis-new → پرکردنِ فرم → vis-save ⇒ رکورد با school_id و registered_byِ نگهبان', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    if (!travel.guard.ok) return { skip: true, why: 'سفر قطع شده', depends_on: '8-1.D' };\n+    const before = Number(W('db.visitors.length'));\n+    W(`(function(){\n+        document.querySelector('[data-act=\"vis-new\"]').click();\n+        document.getElementById('vis_name').value='مهمانِ پروب SIM-01';\n+        document.getElementById('vis_purpose').value='سنجشِ پذیرش';\n+        document.getElementById('vis_person').value='نگهبان';\n+        document.getElementById('vis_phone').value='09120000000';\n+        document.getElementById('vis_nid').value='0000000000';\n+        document.querySelector('[data-act=\"vis-save\"]').click();\n+      })()`);\n+    const after = Number(W('db.visitors.length'));\n+    const meta = j(`(function(){var v=db.visitors[db.visitors.length-1];return {id:v.id,name:v.name,school_id:v.school_id,registered_by:v.registered_by,guard_sid:${travel.guard.user.school_id},guard_id:${travel.guard.user.id}};})()`);\n+    travel.guard.visId = meta.id;\n+    const ok = after === before + 1 && meta.school_id === meta.guard_sid && meta.registered_by === meta.guard_id;\n+    return { ok, actual: 'len ' + before + '→' + after + ' · school_id=' + meta.school_id + '/' + meta.guard_sid + ' · registered_by=' + meta.registered_by + '/' + meta.guard_id,\n+      why: 'رکورد ساخته/مهار نشد (malformed ownership)' };\n+  });\n+  check('SIM-01', '8-1.H', 'کلیکِ vis-out ⇒ وضعیت «خارج» و زمانِ خروجِ غیرتهی (ببرِ out_at/exited_at)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    if (!travel.guard.ok) return { skip: true, why: 'سفر قطع شده', depends_on: '8-1.D' };\n+    const id = travel.guard.visId;\n+    const sel = `document.querySelector('[data-act=\"vis-out\"][data-id=\"${id}\"]')`;\n+    if (!W(`!!${sel}`)) return { ok: false, actual: 'دکمهٔ vis-out برای رکورد ' + id + ' در DOM نیست', why: 'دکمهٔ خروج رندر نشده' };\n+    W(`${sel}.click()`);\n+    const v = j(`(function(){var x=byId('visitors',${id});return {status:x.status,out_at:x.out_at||'',exited_at:x.exited_at||''};})()`);\n+    const exited = !!(v.out_at || v.exited_at);\n+    return { ok: v.status === 'out' && exited, actual: 'status=' + v.status + ' · out_at=' + (v.out_at || '∅') + ' · exited_at=' + (v.exited_at || '∅'),\n+      why: 'خروج ثبت نشد',\n+      note: 'دقتِ واژگانی: §۸-۱ «exited_at» نوشته است؛ قراردادِ واقعیِ محصول `out_at` است (authz/model.json → visitors.fields). پروب هر دو را می‌سنجد.' };\n+  });\n+\n+  /* ── ۳) §۸-۲ — SIM-02: دبیرِ کتابدار (lib_staff) × امانت ── */\n+  const LIB_USERNAME = 'lib_staff_demo';\n+  check('SIM-02', '8-2.A', 'دانه ≥۱ **دبیر** با lib_staff===1 (معیار A2)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    return { ok: census.lib_staff_teacher >= 1, actual: census.lib_staff_teacher,\n+      why: 'دانه هیچ دبیرِ کتابداری ندارد ⇒ login(' + LIB_USERNAME + ') ناممکن و سفر قطع می‌شود (گیتِ پرچم در کد هست: libStaffCan + canRoute + policy)' };\n+  });\n+  check('SIM-02', '8-2.B', 'منفی (دادهٔ واقعی): دبیرِ بی‌پرچم ⇒ policy.inScope(lib_loans) === false', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const t = j(`(function(){var b=db.lib_books[0]; if(!b) return null; var u=db.users.filter(function(x){return x.role==='teacher'&&x.school_id===b.school_id&&!(x.lib_staff===1);})[0]; return u?{id:u.id,book:b.id}:{none:true};})()`);\n+    if (!t || t.none) return { ok: false, actual: 'دبیرِ بی‌پرچم/کتابی در دانه نیست', why: 'سنجهٔ منفی قابل اجرا نبود' };\n+    const ok = policyInScope(t.id, 'lib_loans', null, { book_id: t.book, school_id: schoolOfUser(t.id), returned_at: '' }) === false;\n+    return { ok, actual: 'inScope=' + (ok ? 'false' : 'true'), why: 'دبیرِ بی‌پرچم به امانت دسترسی دارد (fail-open!)' };\n+  });\n+  const libCap = { ok: false };\n+  check('SIM-02', '8-2.C', 'ظرفیت (پرچمِ ساختگیِ درحافظه، برگشت‌پذیر): کتابدار ⇒ render(lib-lend) + امانت + بازگشت + inScope=true', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const setup = j(`(function(){\n+      var b=db.lib_books.filter(function(x){return x.school_id;})[0];\n+      if(!b) return {none:'کتابی در دانه نیست'};\n+      var u=db.users.filter(function(x){return x.role==='teacher'&&x.school_id===b.school_id&&x.active!==0;})[0];\n+      if(!u) return {none:'دبیری در مدرسهٔ کتاب نیست'};\n+      u._probeLib=(u.lib_staff!==1); if(u._probeLib) u.lib_staff=1; u._probe=1;\n+      var st=db.users.filter(function(x){return x.role==='student'&&x.school_id===b.school_id&&x.active!==0;})[0];\n+      if(!st) return {none:'دانش‌آموزی در آن مدرسه نیست'};\n+      return {uid:u.id,sid:st.id,book:b.id,school:b.school_id};\n+    })()`);\n+    if (setup.none) { cleanupStaff('lib_staff'); return { ok: false, actual: setup.none, why: 'زمینهٔ سنجهٔ ظرفیت در دانه نبود' }; }\n+    libCap.setup = setup;\n+    const r = uiLoginById(setup.uid);\n+    if (!r.ok) { cleanupStaff('lib_staff'); return { ok: false, actual: 'ورودِ کتابدارِ ساختگی ناموفق: ' + r.actual, why: r.why }; }\n+    const h0 = W(`(function(){S.route='library';S.filters={};render();return document.getElementById('root').innerHTML;})()`);\n+    const hasLend = h0.indexOf('data-act=\"lib-lend\"') > -1;\n+    W(`document.querySelector('[data-act=\"lib-lend\"][data-id=\"${setup.book}\"]').click()`);\n+    W(`(function(){var s=document.getElementById('lib_stu'); if(s){s.value='${setup.sid}';} document.querySelector('[data-act=\"lib-lend-save\"]').click();})()`);\n+    const loan = j(`(function(){var l=db.lib_loans.filter(function(x){return x.book_id===${setup.book}&&!x._probe;})[db.lib_loans.filter(function(x){return x.book_id===${setup.book}&&!x._probe;}).length-1]; if(!l) return null; l._probe=1; return {id:l.id,student_id:l.student_id,school_id:l.school_id,returned_at:l.returned_at||''};})()`);\n+    const hasReturn = loan ? W(`!!document.querySelector('[data-act=\"lib-return\"][data-id=\"${loan.id}\"]')`) === true : false;\n+    if (loan) W(`(function(){var b=document.querySelector('[data-act=\"lib-return\"][data-id=\"${loan.id}\"]'); if(b) b.click();})()`);\n+    const after = loan ? j(`byId('lib_loans',${loan.id}).returned_at||''`) : '';\n+    libCap.inScopeTrue = loan ? policyInScope(setup.uid, 'lib_loans', loan.id, { returned_at: after || 'x' }) : null;\n+    const ok = hasLend && !!loan && hasReturn && !!after && libCap.inScopeTrue === true;\n+    libCap.ok = ok;\n+    cleanupStaff('lib_staff');\n+    return { ok, actual: 'lib-lend=' + hasLend + ' · loan=' + (loan ? loan.id : '∅') + ' · lib-return=' + hasReturn + ' · returned_at=' + (after || '∅') + ' · inScope=' + libCap.inScopeTrue,\n+      why: 'زنجیرهٔ امانت/بازگشت کتابدار بسته نشد', note: 'پرچمِ lib_staff روی دبیرِ موجود **موقتاً در حافظه** ست شد و در پایانِ همان سنجه برگشت (هیچ تغییری در دانه/مخزن نوشته نشد)' };\n+  });\n+  check('SIM-02', '8-2.D', 'منفی (دادهٔ واقعی): نمای دانش‌آموز از کتابخانه فاقدِ data-act=\"lib-lend\" (فقط‌خواندنی)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const s = j(`(function(){var u=db.users.filter(function(x){return x.role==='student'&&x.active!==0&&x.phone&&x.national_id;})[0];return u?u.id:null;})()`);\n+    if (!s) return { ok: false, actual: 'دانش‌آموزِ قابلِ ورود در دانه نیست', why: 'سنجه قابل اجرا نبود' };\n+    const r = uiLoginById(s);\n+    if (!r.ok) return { ok: false, actual: 'ورودِ دانش‌آموز ناموفق: ' + r.actual, why: r.why };\n+    const h = W(`(function(){S.route='library';render();return document.getElementById('root').innerHTML;})()`);\n+    return { ok: h.indexOf('data-act=\"lib-lend\"') === -1, actual: 'lib-lend در نمایِ دانش‌آموز: ' + (h.indexOf('data-act=\"lib-lend\"') > -1 ? 'حاضر (نقص!)]' : 'غایب'), why: 'دکمهٔ امانت برای دانش‌آموز رندر شده' };\n+  });\n+  check('SIM-02', '8-2.E', 'سفرِ بیرون‌ازجعبهٔ کتابدار: login(' + LIB_USERNAME + ') → render → lib-lend/lib-return', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    if (!census.lib_staff_teacher) return { skip: true, actual: 'lib_staff(دبیر)=0', why: 'کاربرِ غایب ⇒ گامِ login قطع می‌شود؛ هیچ کلیکِ حقیقی روی DOM انجام نمی‌شود', depends_on: '8-2.A' };\n+    const r = uiLogin(LIB_USERNAME, \"x.role==='teacher'&&x.lib_staff===1\");\n+    if (!r.ok) return { ok: false, actual: r.actual, why: r.why };\n+    const h = W(`(function(){S.route='library';render();return document.getElementById('root').innerHTML;})()`);\n+    return { ok: h.indexOf('data-act=\"lib-lend\"') > -1, actual: 'lib-lend=' + (h.indexOf('data-act=\"lib-lend\"') > -1), why: 'رندرِ مسیر کتابخانه برای کتابدارِ seed شده فاقدِ دکمهٔ امانت است' };\n+  });\n+\n+  /* ── ۴) §۸-۳ — SIM-03: تحویلدار اموال (asset_staff) ── */\n+  const ASSET_USERNAME = 'asset_staff_demo';\n+  check('SIM-03', '8-3.A', 'دانه ≥۱ **دبیر** با asset_staff===1 (معیار A3)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    return { ok: census.asset_staff_teacher >= 1, actual: census.asset_staff_teacher,\n+      why: 'دانه هیچ تحویلدارِ اموالی ندارد ⇒ login(' + ASSET_USERNAME + ') ناممکن و سفر قطع می‌شود' };\n+  });\n+  check('SIM-03', '8-3.B', 'منفی (دادهٔ واقعی): دبیرِ بی‌پرچم ⇒ inScope(assets,{status:repair}) === false', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const a = j(`(function(){var x=db.assets[0]; var u=db.users.filter(function(y){return y.role==='teacher'&&y.school_id===x.school_id&&!(y.asset_staff===1);})[0]; return u&&x?{uid:u.id,aid:x.id}:{none:true};})()`);\n+    if (a.none) return { ok: false, actual: 'تجهیز/دبیرِ بی‌پرچم در دانه نیست', why: 'سنجهٔ منفی قابل اجرا نبود' };\n+    const ok = policyInScope(a.uid, 'assets', a.aid, { status: 'repair' }) === false;\n+    return { ok, actual: 'inScope=' + (ok ? 'false' : 'true'), why: 'دبیرِ بی‌پرچم می‌تواند اموال را ویرایش کند (fail-open!)' };\n+  });\n+  check('SIM-03', '8-3.C', 'منفیِ جداسازیِ مستأجر: تحویلدارِ مدرسهٔ دیگر ⇒ inScope === false', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const pair = j(`(function(){\n+      var list=db.assets.filter(function(x){return x.school_id;});\n+      if(list.length<2) return {none:'دو تجهیزِ هم‌مدرسهٔ مختلف نیست'};\n+      var seen={}, out=null;\n+      for(var i=0;i<list.length&&!out;i++){ var sid=list[i].school_id; var u=db.users.filter(function(y){return y.role==='teacher'&&y.school_id!==sid&&y.active!==0;})[0];\n+        if(u&&!seen[u.id]){seen[u.id]=1;out={uid:u.id,aid:list[i].id,asid:list[i].school_id,usid:u.school_id};} }\n+      return out||{none:'زوجِ مناسب پیدا نشد'};})()`);\n+    if (pair.none) return { ok: false, actual: pair.none, why: 'سنجه قابل اجرا نبود' };\n+    const r = policyInScope(pair.uid, 'assets', pair.aid, { status: 'repair' });\n+    return { ok: r === false, actual: 'inScope=' + r + ' (user.school_id=' + pair.usid + ' ⊄ asset.school_id=' + pair.asid + ')', why: 'رکوردِ بیرون‌مدرسه در دسترس است (نقصِ جداسازیِ tenant!)' };\n+  });\n+  const asCap = { ok: false };\n+  check('SIM-03', '8-3.D', 'ظرفیت (پرچمِ ساختگی، برگشت‌پذیر): نمای تحویلدار = as-status حاضر · as-new/as-del غایب · کلیکِ وضعیت ذخیره می‌شود · inScope=true', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const setup = j(`(function(){\n+      var a=db.assets.filter(function(x){return x.school_id&&(x.status==='available'||!x.status);})[0];\n+      if(!a) return {none:'تجهیزِ قابلِ تغییروضعیت نیست'};\n+      var u=db.users.filter(function(x){return x.role==='teacher'&&x.school_id===a.school_id&&x.active!==0;})[0];\n+      if(!u) return {none:'دبیری در مدرسهٔ تجهیز نیست'};\n+      u._probeAs=(u.asset_staff!==1); if(u._probeAs) u.asset_staff=1; u._probe=1;\n+      return {uid:u.id,aid:a.id,name:a.name,before:a.status||''};\n+    })()`);\n+    if (setup.none) { cleanupStaff('asset_staff'); return { ok: false, actual: setup.none, why: 'زمینهٔ سنجهٔ ظرفیت در دانه نبود' }; }\n+    const r = uiLoginById(setup.uid);\n+    if (!r.ok) { cleanupStaff('asset_staff'); return { ok: false, actual: 'ورودِ تحویلدارِ ساختگی ناموفق: ' + r.actual, why: r.why }; }\n+    const h = W(`(function(){S.route='assets';S.filters={};render();return document.getElementById('root').innerHTML;})()`);\n+    const hasStatus = h.indexOf('data-act=\"as-status\"') > -1;\n+    const noMgr = h.indexOf('data-act=\"as-new\"') === -1 && h.indexOf('data-act=\"as-del\"') === -1;\n+    W(`document.querySelector('[data-act=\"as-status\"][data-id=\"${setup.aid}\"]').click()`);\n+    W(`(function(){var s=document.getElementById('as_status'); if(s){s.value='repair';} document.querySelector('[data-act=\"as-status-save\"]').click();})()`);\n+    const rec2 = j(`(function(){var x=byId('assets',${setup.aid});return {status:x.status,usable:x.usable_count};})()`);\n+    asCap.inScopeTrue = policyInScope(setup.uid, 'assets', setup.aid, { status: 'repair' });\n+    const ok = hasStatus && noMgr && rec2.status === 'repair' && asCap.inScopeTrue === true;\n+    asCap.ok = ok;\n+    /* بازگشتِ دادهٔ مُتحرک + برداشتنِ پرچمِ ساختگی */\n+    W(`(function(){var x=byId('assets',${setup.aid}); if(x) x.status='${setup.before || 'available'}'; var u=byId('users',${setup.uid}); if(u&&u._probe){delete u.asset_staff;delete u._probe;}})()`);\n+    return { ok, actual: 'as-status=' + hasStatus + ' · as-new/as-del غایب=' + noMgr + ' · status→' + rec2.status + ' (usable=' + rec2.usable + ') · inScope=' + asCap.inScopeTrue,\n+      why: 'زنجیرهٔ تحویلدار بسته نشد', note: 'پرچم و وضعیتِ تجهیز در حافظه بازگشت داده شد (صفر تغییرِ دیسک)' };\n+  });\n+  check('SIM-03', '8-3.E', 'سفرِ بیرون‌ازجعبهٔ تحویلدار: login(' + ASSET_USERNAME + ') → render → as-status', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    if (!census.asset_staff_teacher) return { skip: true, actual: 'asset_staff(دبیر)=0', why: 'کاربرِ غایب ⇒ سفر در گامِ اول قطع می‌شود', depends_on: '8-3.A' };\n+    const r = uiLogin(ASSET_USERNAME, \"x.role==='teacher'&&x.asset_staff===1\");\n+    if (!r.ok) return { ok: false, actual: r.actual, why: r.why };\n+    const h = W(`(function(){S.route='assets';render();return document.getElementById('root').innerHTML;})()`);\n+    return { ok: h.indexOf('data-act=\"as-status\"') > -1, actual: 'as-status=' + (h.indexOf('data-act=\"as-status\"') > -1), why: 'دکمهٔ وضعیت برای تحویلدارِ دانه رندر نشد' };\n+  });\n+\n+  /* ── ۵) §۸-۴ — SIM-04: is_head (سطحِ طراحی — grep شمارشی) ── */\n+  check('SIM-04', '8-4.A', '`is_head` در مدلِ authz (authz/model.json → users.fields) تعریف شده', () => {\n+    const inModel = /is_head/.test(modelRaw);\n+    return { ok: inModel, actual: (inModel ? 'یافت شد' : '۰ هیت') + ' · ' + hitStr(isHeadProduct, 'src/+server/+migrations+authz'),\n+      why: 'is_head در هیچ‌یک از فایل‌های مدلِ دسترسی نیست ⇒ حتی seed شدنش دادهٔ مرده است (whitelistِ فیلدها در authz/model.json آن را رد می‌کند)',\n+      note: 'تنها ارجاع‌ها: tools/seed-office-data.js (' + isHeadTool.total + ' هیت در ' + isHeadTool.files.join(',') + ') = پیش‌نویسِ چت ۲، مصرف‌کننده ندارد' };\n+  });\n+  check('SIM-04', '8-4.B', '`is_head` در کدِ محصول (src/js/*.js + server/**) مصرف می‌شود', () => ({\n+    ok: consumerCount.total > 0, actual: consumerCount.total + ' هیت',\n+    why: 'صفرِ مصرف‌کننده ⇒ هیچ گیتِ مجوز/UI ای به is_head نگاه نمی‌کند؛ «seed کردنش بی‌اثر است»' }));\n+  check('SIM-04', '8-4.C', 'نقش/پرچمِ «رئیس اداره» (office_head یا is_head) در مدل هست', () => ({\n+    ok: officeHeadProduct.total > 0 || isHeadProduct.total > 0, actual: 'office_head=' + officeHeadProduct.total + ' · is_head(محصول)=' + isHeadProduct.total,\n+    why: 'قراردادِ طراحیِ چت ۲ (office_head در برابرِ is_head) هنوز انتخاب نشده ⇒ اقدامِ دانه‌ای بی‌موضوع' }));\n+  check('SIM-04', '8-4.D', 'دانه ≥۱ کاربرِ edu_office با is_head===1 (معیار A4 — مشروط)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const has = census.is_head_truthy;\n+    return { skip: true, actual: 'is_head(truthy)=' + has + ' · کلید is_head در ۰/۱۰۳۵ کاربر وجود دارد؟ ' + census.is_head_key_present,\n+      why: 'SKIP صریح (نه سبزِ جعلی): پیش‌نیازِ طراحیِ چت ۲ برقرار نیست (8-4.A/B/C قرمز)', depends_on: '8-4.C' };\n+  });\n+\n+  /* ── ۶) گیت‌های مکملِ ماتریس (A4/B1/B5 — سطحِ سند) ── */\n+  check('MATRIX', 'B.5', 'افزایشِ دانه حداکثر +۱ کاربر است (B5 — ضد تورم)', () => {\n+    if (!j) return { skip: true, why: 'jsdom نصب نیست' };\n+    const base = 1035;\n+    return { ok: census.users <= base + 1, actual: census.users + ' کاربر (پایه ' + base + ') ⇒ دلتا ' + (census.users - base), why: 'دانه بیش از سقفِ B5 رشد کرده' };\n+  });\n+  check('MATRIX', 'B.1', 'قطعیتِ seed (دو اجرای node server/seed.js ⇒ بایت‌برابر)', () => ({\n+    skip: true, actual: '—', why: 'NOT-RUN با دلیل: اجرای seed خروجیِ server/data/payesh.json را بازنویسی می‌کند و این دور «صفر تغییر کد/داده» است؛ گیتِ بیلینگ/CI هم باز نیست. ابزار: `node server/seed.js` ×۲ + sha256sum' }));\n+  check('MATRIX', 'B.2', 'رگرسیونِ suites (run/smoke/authz-model) پس از دانهٔ جدید', () => ({\n+    skip: true, actual: '—', why: 'NOT-RUN: به دانهٔ غنی‌شدهٔ چت ۳ گره خورده (وجود ندارد)؛ همچنین بازاجرای سوئیت‌های دور ۲ در این دور ممنوع است' }));\n+  check('MATRIX', 'B.3', 'fail-closedِ سرور: policy.inScope پرچم را می‌خواهد (کدِ گیت در policy.js)', () => {\n+    const t = readRel('server/policy.js') || '';\n+    const libGate = /me\\.lib_staff\\s*!==\\s*1/.test(t);\n+    const asGate = /me\\.asset_staff\\s*!==\\s*1/.test(t);\n+    return { ok: libGate && asGate, actual: 'policy.js: lib_staff-gate=' + libGate + ' · asset_staff-gate=' + asGate, why: 'گیتِ پرچم در policy سرور نیست (fail-open)' };\n+  });\n+  check('MATRIX', 'B.4', 'اکشن‌های write-perms نگهبان/کتابدار/تحویلدار ثبت‌اند', () => {\n+    if (!WP || !WP.actions) return { ok: false, actual: 'authz/write-perms.json خوانده نشد', why: 'فایل مدلِ نوشتن در این درخت نیست' };\n+    const acts = WP.actions;\n+    const rows = { 'vis-save': ['guard'], 'vis-out': ['guard'], 'lib-lend-save': ['teacher'], 'lib-return': ['teacher'], 'as-status-save': ['teacher'] };\n+    const got = {}; const okAll = Object.keys(rows).every((k) => {\n+      const a = acts[k]; got[k] = a ? a.roles : '∅';\n+      return !!a && rows[k].every((r) => a.roles.indexOf(r) > -1);\n+    });\n+    return { ok: okAll, actual: JSON.stringify(got), why: 'مجموعهٔ نقش‌های write-perms با انتظار نمی‌خواند' };\n+  });\n+\n+  /* ── ۷) بازگشتِ اثرِ پروب روی db (بهداشت — هرچند دانه روی دیسک دست‌نخورده است) ── */\n+  if (j) {\n+    try {\n+      W(`(function(){\n+        db.visitors.filter(function(v){return v.name==='مهمانِ پروب SIM-01';}).forEach(function(v){remove('visitors',v.id);});\n+        db.lib_loans.filter(function(l){return l._probe;}).forEach(function(l){remove('lib_loans',l.id);});\n+        db.lib_books.filter(function(b){return b._probe;}).forEach(function(b){remove('lib_books',b.id);});\n+        db.users.forEach(function(u){ if(u._probeLib) delete u.lib_staff; if(u._probeAs) delete u.asset_staff; delete u._probe; });\n+        db.users.filter(function(u){return u._sim;}).forEach(function(u){ delete u.lib_staff; delete u.asset_staff; delete u._sim; if(u.username==='guard_demo') remove('users',u.id); });\n+      })()`);\n+      note('بازگشتِ اثرِ پروب: رکوردهای _probe حذف و پرچم‌های ساختگی برداشته شدند (دانهٔ دیسکی هرگز لمس نشد)');\n+    } catch (e) { note('cleanup با استثنا: ' + e.message); }\n+  }\n+\n+  finish();\n+}\n+\n+/* ───────────────────────── کمکی‌ها ───────────────────────── */\n+let probeApi = null;\n+function pinsOf() {\n+  const out = [];\n+  [['tests/donations2.js', 55], ['tests/drills2.js', 54]].forEach(([f, ln]) => {\n+    const t = readRel(f);\n+    if (!t) { out.push({ file: f, line: ln, text: '(فایل نیست)', pinned: null }); return; }\n+    const text = t.split('\\n')[ln - 1];\n+    const m = /users\\.length[^0-9]*([0-9]{3,})/.exec(text || '');\n+    out.push({ file: f, line: ln, text: (text || '').trim(), pinned: m ? Number(m[1]) : null });\n+  });\n+  return out;\n+}\n+function hitStr(o, label) { return o.total + ' هیت در ' + label + (o.files.length ? ' [' + o.files.slice(0, 8).join(' ') + ']' : ' [هیچ فایلی]'); }\n+function storeOf(colls) {\n+  /* ساختن store برای policy از db درون jsdom (فقط‌خواندنی) */\n+  return probeApi.j(`(function(){var o={};[${colls.map((c) => `'${c}'`).join(',') + ''}].forEach(function(k){o[k]=db[k]||[];});return o;})()`);\n+}\n+function schoolOfUser(uid) {\n+  const u = probeApi.j(`(function(){var x=byId('users',${uid});return x?{school_id:x.school_id==null?null:x.school_id}:{};})()`);\n+  return u.school_id == null ? -1 : u.school_id;\n+}\n+function policyInScope(uid, coll, recId, data) {\n+  const pol = require(path.join(ROOT, 'server/policy.js'));\n+  const sess = probeApi.j(`(function(){var u=byId('users',${uid});return u?{id:u.id,role:u.role,school_id:u.school_id==null?null:u.school_id,id_offices:u.office_id==null?null:u.office_id} : null;})()`);\n+  if (!sess) return false;\n+  const store2 = storeOf(['users', 'lib_loans', 'lib_books', 'assets', 'enrollments', 'classes', 'schools', 'offices', 'districts', 'counties', 'messages', 'notifications', 'parent_links']);\n+  return pol.inScope(sess, store2, coll, recId, data);\n+}\n+/* بازگرداندنِ پرچمِ ساختگی — **فقط** آنچه پروب ست کرده (پرچمِ ازپیش‌موجودِ دانه دست نمی‌خورد) */\n+function cleanupStaff(marker) {\n+  W(`(function(){db.users.forEach(function(u){if(u.${marker==='lib_staff'?'_probeLib':'_probeAs'}){delete u.${marker};delete u.${marker==='lib_staff'?'_probeLib':'_probeAs'};}});})()`);\n+}\n+function uiLogin(username, roleExpr, label) {\n+  /* ورود با فلوِ واقعیِ محصول: شماره ← کدِ پنل ← کدِ ملی ← «استعلام و ورود» */\n+  const pred = '(' + roleExpr + ')&&x.active!==0&&x.phone&&x.national_id';\n+  const cand = probeApi.j(`(function(){\n+    var byName=db.users.filter(function(x){return x.username===${JSON.stringify(username)};})[0]||null;\n+    var byRole=db.users.filter(function(x){return ${pred};})[0]||null;\n+    var u=byName||byRole;\n+    if(!u) return {none:JSON.stringify('هیچ کاربری با username=' + ${JSON.stringify(username)} + ' یا شرط ' + ${JSON.stringify(label || roleExpr)} + ' در دانه نیست · db.users=' + db.users.length)};\n+    return {id:u.id,username:u.username,phone:String(u.phone),nid:String(u.national_id),school_id:u.school_id==null?null:u.school_id,role:u.role,\n+            via:(byName?'username':'role/flag-fallback')};})()`);\n+  if (cand.none) return { ok: false, actual: String(cand.none), why: 'کاربرِ هدف در دانه نیست ⇒ سفر پیش از گامِ ورود قطع می‌شود' };\n+  return Object.assign(uiLoginById(cand.id), { via: cand.via + ' → ' + cand.username });\n+}\n+function uiLoginById(uid) {\n+  const info = probeApi.j(`(function(){var u=byId('users',${uid});return {phone:String(u.phone||''),nid:String(u.national_id||''),name:u.full_name};})()`);\n+  if (!info.phone || !info.nid) return { ok: false, actual: 'کاربر شماره/کدملیِ ورود ندارد', why: 'قراردادِ ورودِ دمو: phone+national_id' };\n+  W(`(function(){S.user=null;S.boss=null;S.persona=null;render();})()`);\n+  const pre = W(`(function(){return !!document.getElementById('lpn')&&!!document.querySelector('[data-act=\"login\"]');})()`);\n+  if (!pre) return { ok: false, actual: 'فرمِ ورود رندر نشد', why: 'renderLogin در این درک در دسترس نیست' };\n+  const r = W(`(function(){\n+    try{\n+      document.getElementById('lpn').value=${JSON.stringify(info.phone)};\n+      document.querySelector('[data-act=\"login-code\"]').click();\n+      document.getElementById('lcode').value=SmsPanel.demoCode();\n+      document.getElementById('lnid').value=${JSON.stringify(info.nid)};\n+      var box=document.getElementById('lerr'); if(box) box.textContent='';\n+      document.querySelector('[data-act=\"login\"]').click();\n+      var e=box?box.textContent.trim():'';\n+      return JSON.stringify({ok:!!(S&&S.user&&S.user.id===${uid}),err:e});\n+    }catch(err){return JSON.stringify({ok:false,err:'استثنا: '+err.message});}\n+  })()`);\n+  const parsed = JSON.parse(r);\n+  if (!parsed.ok) return { ok: false, actual: 'S.user=null · پیامِ محصول: ' + (parsed.err || '(بی‌پیام)'), why: 'ورود پذیرفته نشد' };\n+  return { ok: true, actual: 'ورود موفق: ' + info.name + ' (id=' + uid + ')', user: probeApi.j(`(function(){var u=S.user;return {id:u.id,role:u.role,school_id:u.school_id==null?null:u.school_id};})()`) };\n+}\n+\n+/* ───────────────────────── گزارش ───────────────────────── */\n+function finish() {\n+  const icon = { PASS: '✅', FAIL: '❌', 'NOT-RUN': '⏭️ ' };\n+  console.log('\\n═══ پروب پذیرش غنی‌سازی دانه (SIM-01..04) — ' + TAG + ' ═══');\n+  console.log('درخت: ' + ROOT + ' · mode=' + MODE + (MODE === 'simulated-seed' ? ' (کنترلِ تفکیک‌پذیری — دانهٔ ساختگیِ درحافظه)' : ' (خط‌مبنای واقعیِ دانه)'));\n+  if (census) console.log('دانه: ' + census.users + ' کاربر · guard=' + census.guard + ' · lib_staff=' + census.lib_staff + ' · asset_staff=' + census.asset_staff + ' · is_head(truthy)=' + census.is_head_truthy);\n+  else console.log('دانه: خوانده نشد (jsdom در دسترس نیست: ' + jsdomErr + ')');\n+  console.log('\\n| # | SIM | assert | انتظار | واقع | وضعیت | دلیل/حالتِ شکست |');\n+  console.log('|---|---|---|---|---|---|---|');\n+  for (const e of A) {\n+    console.log('| ' + e.n + ' | ' + e.sim + ' | `' + e.id + '` | ' + e.expect + ' | ' + (e.actual || '—') + ' | ' + (icon[e.status] || e.status) + ' ' + e.status +\n+      (e.depends_on ? ' ← ' + e.depends_on : '') + ' | ' + (e.why || '—') + ' |');\n+  }\n+  const n = (s) => A.filter((x) => x.status === s).length;\n+  const fails = A.filter((x) => x.status === 'FAIL');\n+  console.log('\\nجمع: ' + A.length + ' سنجه · PASS=' + n('PASS') + ' · FAIL=' + n('FAIL') + ' · NOT-RUN=' + n('NOT-RUN'));\n+  if (NOTES.length) { console.log('\\nیادداشت‌ها:'); NOTES.forEach((s) => console.log(' · ' + s)); }\n+  console.log('\\nVERDICT: ' + (fails.length === 0 ? 'GREEN (پذیرش برقرار)' : 'RED (' + fails.map((f) => f.sim + '/' + f.id).join(', ') + ')'));\n+  if (JSON_OUT) {\n+    fs.writeFileSync(JSON_OUT, JSON.stringify({ tag: TAG, root: ROOT, gitHead: gitHead(), asserts: A, census, notes: NOTES, sha256: selfSha() }, null, 2));\n+    console.log('JSON: ' + JSON_OUT);\n+  }\n+  console.log('self sha256: ' + selfSha());\n+  process.exit(fails.length === 0 ? 0 : 1);\n+}\n+function gitHead() { try { return cp.execSync('git rev-parse HEAD', { cwd: ROOT }).toString().trim(); } catch (e) { return 'n/a'; } }\n+function selfSha() { try { return require('crypto').createHash('sha256').update(fs.readFileSync(__filename)).digest('hex'); } catch (e) { return 'n/a'; } }\n+\n+main().catch((e) => { console.error('PROBE ERROR: ' + (e && e.stack || e)); process.exit(3); });\n+```\n+\n+### پیوستِ ب — خروجیِ خام\n+\n+**۱) خط‌مبنای قرمز روی `main` (`--mode baseline` · `exit=1`):**\n+\n+```text\n+═══ پروب پذیرش غنی‌سازی دانه (SIM-01..04) — main@0a208f8 ═══\n+درخت: /home/user/p2 · mode=baseline (خط‌مبنای واقعیِ دانه)\n+دانه: 1035 کاربر · guard=0 · lib_staff=0 · asset_staff=0 · is_head(truthy)=0\n+\n+| # | SIM | assert | انتظار | واقع | وضعیت | دلیل/حالتِ شکست |\n+|---|---|---|---|---|---|---|\n+| 1 | BOOT | `B.0` | بوتِ باندل در jsdom بدونِ خطای کنسول | 0 خطای کنسول | ✅ PASS | — |\n+| 2 | DATASET | `D.1` | شمارِ کاربرانِ دانه با پین‌های تستی هم‌خوان است | users=1035 · پین‌ها: tests/donations2.js:55=1035 , tests/drills2.js:54=1035 | ✅ PASS | — |\n+| 3 | DATASET | `D.2` | کاربرانِ edu_office در دانه (بازدیدِ is_head روی داده) | edu_office=4 · ردیف‌ها: [{\"id\":1026,\"username\":\"edu_kurdistan\",\"office_id\":1,\"is_head\":\"__absent__\"},{\"id\":1027,\"username\":\"edu_baneh\",\"office_id\":2,\"is_head\":\"__absent__\"},{\"id\":1028,\"username\":\"edu_sanandaj1\",\"office_id\":3,\"is_head\":\"__absent__\"},{\"id\":1029,\"username\":\"edu_tehran\",\"office_id\":4,\"is_head\":\"__absent__\"}] | ✅ PASS | — |\n+| 4 | SIM-01 | `8-1.A` | دانه ≥۱ کاربرِ guard فعال با school_id معتبر (معیار A1) | 0 | ❌ FAIL | دانه هیچ کاربرِ guard ندارد ⇒ login(guard_demo) حسابی پیدا نمی‌کند و سفر در گام ۱ قطع می‌شود (قابلیت در کد هست، داده نیست) |\n+| 5 | SIM-01 | `8-1.B` | canAction('vis-new'/'vis-out','guard') === true | vis-new=true, vis-out=true | ✅ PASS | — |\n+| 6 | SIM-01 | `8-1.C` | canRoute('visitors','guard') === true (منوی «پذیرش») | true | ✅ PASS | — |\n+| 7 | SIM-01 | `8-1.D` | ورودِ واقعیِ نگهبان با فلوِ شماره+کد+کد ملی (login «guard_demo») | guard=0 | ⏭️  NOT-RUN ← 8-1.A | کاربرِ غایب ⇒ login انجام نمی‌شود |\n+| 8 | SIM-01 | `8-1.E` | رندرِ داشبورد پذیرش (کارتِ «🚪 مراجعین» در homeٔ نگهبان) | — | ⏭️  NOT-RUN ← 8-1.D | سفر در گام ورود قطع شد |\n+| 9 | SIM-01 | `8-1.F` | رندرِ مسیر visitors شامل data-act=\"vis-new\" (و بدونِ «دسترسی مجاز نیست») | — | ⏭️  NOT-RUN ← 8-1.D | سفر قطع شده |\n+| 10 | SIM-01 | `8-1.G` | کلیکِ vis-new → پرکردنِ فرم → vis-save ⇒ رکورد با school_id و registered_byِ نگهبان | — | ⏭️  NOT-RUN ← 8-1.D | سفر قطع شده |\n+| 11 | SIM-01 | `8-1.H` | کلیکِ vis-out ⇒ وضعیت «خارج» و زمانِ خروجِ غیرتهی (ببرِ out_at/exited_at) | — | ⏭️  NOT-RUN ← 8-1.D | سفر قطع شده |\n+| 12 | SIM-02 | `8-2.A` | دانه ≥۱ **دبیر** با lib_staff===1 (معیار A2) | 0 | ❌ FAIL | دانه هیچ دبیرِ کتابداری ندارد ⇒ login(lib_staff_demo) ناممکن و سفر قطع می‌شود (گیتِ پرچم در کد هست: libStaffCan + canRoute + policy) |\n+| 13 | SIM-02 | `8-2.B` | منفی (دادهٔ واقعی): دبیرِ بی‌پرچم ⇒ policy.inScope(lib_loans) === false | inScope=false | ✅ PASS | — |\n+| 14 | SIM-02 | `8-2.C` | ظرفیت (پرچمِ ساختگیِ درحافظه، برگشت‌پذیر): کتابدار ⇒ render(lib-lend) + امانت + بازگشت + inScope=true | lib-lend=true · loan=4 · lib-return=true · returned_at=2026-09-13T14:45:50.910Z · inScope=true | ✅ PASS | — |\n+| 15 | SIM-02 | `8-2.D` | منفی (دادهٔ واقعی): نمای دانش‌آموز از کتابخانه فاقدِ data-act=\"lib-lend\" (فقط‌خواندنی) | lib-lend در نمایِ دانش‌آموز: غایب | ✅ PASS | — |\n+| 16 | SIM-02 | `8-2.E` | سفرِ بیرون‌ازجعبهٔ کتابدار: login(lib_staff_demo) → render → lib-lend/lib-return | lib_staff(دبیر)=0 | ⏭️  NOT-RUN ← 8-2.A | کاربرِ غایب ⇒ گامِ login قطع می‌شود؛ هیچ کلیکِ حقیقی روی DOM انجام نمی‌شود |\n+| 17 | SIM-03 | `8-3.A` | دانه ≥۱ **دبیر** با asset_staff===1 (معیار A3) | 0 | ❌ FAIL | دانه هیچ تحویلدارِ اموالی ندارد ⇒ login(asset_staff_demo) ناممکن و سفر قطع می‌شود |\n+| 18 | SIM-03 | `8-3.B` | منفی (دادهٔ واقعی): دبیرِ بی‌پرچم ⇒ inScope(assets,{status:repair}) === false | inScope=false | ✅ PASS | — |\n+| 19 | SIM-03 | `8-3.C` | منفیِ جداسازیِ مستأجر: تحویلدارِ مدرسهٔ دیگر ⇒ inScope === false | inScope=false (user.school_id=2 ⊄ asset.school_id=1) | ✅ PASS | — |\n+| 20 | SIM-03 | `8-3.D` | ظرفیت (پرچمِ ساختگی، برگشت‌پذیر): نمای تحویلدار = as-status حاضر · as-new/as-del غایب · کلیکِ وضعیت ذخیره می‌شود · inScope=true | as-status=true · as-new/as-del غایب=true · status→repair (usable=1) · inScope=true | ✅ PASS | — |\n+| 21 | SIM-03 | `8-3.E` | سفرِ بیرون‌ازجعبهٔ تحویلدار: login(asset_staff_demo) → render → as-status | asset_staff(دبیر)=0 | ⏭️  NOT-RUN ← 8-3.A | کاربرِ غایب ⇒ سفر در گامِ اول قطع می‌شود |\n+| 22 | SIM-04 | `8-4.A` | `is_head` در مدلِ authz (authz/model.json → users.fields) تعریف شده | ۰ هیت · 0 هیت در src/+server/+migrations+authz [هیچ فایلی] | ❌ FAIL | is_head در هیچ‌یک از فایل‌های مدلِ دسترسی نیست ⇒ حتی seed شدنش دادهٔ مرده است (whitelistِ فیلدها در authz/model.json آن را رد می‌کند) |\n+| 23 | SIM-04 | `8-4.B` | `is_head` در کدِ محصول (src/js/*.js + server/**) مصرف می‌شود | 0 هیت | ❌ FAIL | صفرِ مصرف‌کننده ⇒ هیچ گیتِ مجوز/UI ای به is_head نگاه نمی‌کند؛ «seed کردنش بی‌اثر است» |\n+| 24 | SIM-04 | `8-4.C` | نقش/پرچمِ «رئیس اداره» (office_head یا is_head) در مدل هست | office_head=0 · is_head(محصول)=0 | ❌ FAIL | قراردادِ طراحیِ چت ۲ (office_head در برابرِ is_head) هنوز انتخاب نشده ⇒ اقدامِ دانه‌ای بی‌موضوع |\n+| 25 | SIM-04 | `8-4.D` | دانه ≥۱ کاربرِ edu_office با is_head===1 (معیار A4 — مشروط) | is_head(truthy)=0 · کلید is_head در ۰/۱۰۳۵ کاربر وجود دارد؟ 0 | ⏭️  NOT-RUN ← 8-4.C | SKIP صریح (نه سبزِ جعلی): پیش‌نیازِ طراحیِ چت ۲ برقرار نیست (8-4.A/B/C قرمز) |\n+| 26 | MATRIX | `B.5` | افزایشِ دانه حداکثر +۱ کاربر است (B5 — ضد تورم) | 1035 کاربر (پایه 1035) ⇒ دلتا 0 | ✅ PASS | — |\n+| 27 | MATRIX | `B.1` | قطعیتِ seed (دو اجرای node server/seed.js ⇒ بایت‌برابر) | — | ⏭️  NOT-RUN | NOT-RUN با دلیل: اجرای seed خروجیِ server/data/payesh.json را بازنویسی می‌کند و این دور «صفر تغییر کد/داده» است؛ گیتِ بیلینگ/CI هم باز نیست. ابزار: `node server/seed.js` ×۲ + sha256sum |\n+| 28 | MATRIX | `B.2` | رگرسیونِ suites (run/smoke/authz-model) پس از دانهٔ جدید | — | ⏭️  NOT-RUN | NOT-RUN: به دانهٔ غنی‌شدهٔ چت ۳ گره خورده (وجود ندارد)؛ همچنین بازاجرای سوئیت‌های دور ۲ در این دور ممنوع است |\n+| 29 | MATRIX | `B.3` | fail-closedِ سرور: policy.inScope پرچم را می‌خواهد (کدِ گیت در policy.js) | policy.js: lib_staff-gate=true · asset_staff-gate=true | ✅ PASS | — |\n+| 30 | MATRIX | `B.4` | اکشن‌های write-perms نگهبان/کتابدار/تحویلدار ثبت‌اند | {\"vis-save\":[\"manager\",\"guard\"],\"vis-out\":[\"manager\",\"guard\"],\"lib-lend-save\":[\"manager\",\"teacher\"],\"lib-return\":[\"manager\",\"teacher\"],\"as-status-save\":[\"manager\",\"teacher\"]} | ✅ PASS | — |\n+\n+جمع: 30 سنجه · PASS=14 · FAIL=6 · NOT-RUN=10\n+\n+یادداشت‌ها:\n+ · دانهٔ زنده (از db درون باندل): {\"users\":1035,\"active\":1015,\"schools\":6,\"schoolsActive\":5,\"guard\":0,\"guardActive\":0,\"lib_staff\":0,\"lib_staff_teacher\":0,\"asset_staff\":0,\"asset_staff_teacher\":0,\"is_head_truthy\":0,\"is_head_key_present\":0,\"teacher\":72,\"manager\":12,\"edu_office\":4,\"edu_office_rows\":[{\"id\":1026,\"username\":\"edu_kurdistan\",\"office_id\":1,\"is_head\":\"__absent__\"},{\"id\":1027,\"username\":\"edu_baneh\",\"office_id\":2,\"is_head\":\"__absent__\"},{\"id\":1028,\"username\":\"edu_sanandaj1\",\"office_id\":3,\"is_head\":\"__absent__\"},{\"id\":1029,\"username\":\"edu_tehran\",\"office_id\":4,\"is_head\":\"__absent__\"}],\"lib_books\":3,\"lib_loans\":3,\"assets\":4,\"visitors\":3}\n+ · بازگشتِ اثرِ پروب: رکوردهای _probe حذف و پرچم‌های ساختگی برداشته شدند (دانهٔ دیسکی هرگز لمس نشد)\n+\n+VERDICT: RED (SIM-01/8-1.A, SIM-02/8-2.A, SIM-03/8-3.A, SIM-04/8-4.A, SIM-04/8-4.B, SIM-04/8-4.C)\n+JSON: /tmp/probe-main.json\n+self sha256: 2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8\n+```\n+\n+**۲) کنترلِ `--mode simulated-seed` (`exit=1` — چهار «قرمزِ درست»):**\n+\n+```text\n+═══ پروب پذیرش غنی‌سازی دانه (SIM-01..04) — main@0a208f8+simulated-seed ═══\n+درخت: /home/user/p2 · mode=simulated-seed (کنترلِ تفکیک‌پذیری — دانهٔ ساختگیِ درحافظه)\n+دانه: 1036 کاربر · guard=1 · lib_staff=1 · asset_staff=1 · is_head(truthy)=0\n+\n+| # | SIM | assert | انتظار | واقع | وضعیت | دلیل/حالتِ شکست |\n+|---|---|---|---|---|---|---|\n+| 1 | BOOT | `B.0` | بوتِ باندل در jsdom بدونِ خطای کنسول | 0 خطای کنسول | ✅ PASS | — |\n+| 2 | DATASET | `D.1` | شمارِ کاربرانِ دانه با پین‌های تستی هم‌خوان است | users=1036 · پین‌ها: tests/donations2.js:55=1035 , tests/drills2.js:54=1035 ⇒ ناهم‌خوان | ❌ FAIL | پینِ شمارِ کاربران با دانهٔ جاری نمی‌خواند (یا دانه تغییر کرده و پین به‌روز نشده، یا برعکس) |\n+| 3 | DATASET | `D.2` | کاربرانِ edu_office در دانه (بازدیدِ is_head روی داده) | edu_office=4 · ردیف‌ها: [{\"id\":1026,\"username\":\"edu_kurdistan\",\"office_id\":1,\"is_head\":\"__absent__\"},{\"id\":1027,\"username\":\"edu_baneh\",\"office_id\":2,\"is_head\":\"__absent__\"},{\"id\":1028,\"username\":\"edu_sanandaj1\",\"office_id\":3,\"is_head\":\"__absent__\"},{\"id\":1029,\"username\":\"edu_tehran\",\"office_id\":4,\"is_head\":\"__absent__\"}] | ✅ PASS | — |\n+| 4 | SIM-01 | `8-1.A` | دانه ≥۱ کاربرِ guard فعال با school_id معتبر (معیار A1) | 1 | ✅ PASS | — |\n+| 5 | SIM-01 | `8-1.B` | canAction('vis-new'/'vis-out','guard') === true | vis-new=true, vis-out=true | ✅ PASS | — |\n+| 6 | SIM-01 | `8-1.C` | canRoute('visitors','guard') === true (منوی «پذیرش») | true | ✅ PASS | — |\n+| 7 | SIM-01 | `8-1.D` | ورودِ واقعیِ نگهبان با فلوِ شماره+کد+کد ملی (login «guard_demo») | ورود موفق: نگهبانِ نمونهٔ پروب (id=1036) | ✅ PASS | — |\n+| 8 | SIM-01 | `8-1.E` | رندرِ داشبورد پذیرش (کارتِ «🚪 مراجعین» در homeٔ نگهبان) | کارتِ مراجعین حاضر | ✅ PASS | — |\n+| 9 | SIM-01 | `8-1.F` | رندرِ مسیر visitors شامل data-act=\"vis-new\" (و بدونِ «دسترسی مجاز نیست») | vis-new=true · forbidden=false | ✅ PASS | — |\n+| 10 | SIM-01 | `8-1.G` | کلیکِ vis-new → پرکردنِ فرم → vis-save ⇒ رکورد با school_id و registered_byِ نگهبان | len 3→4 · school_id=1/1 · registered_by=1036/1036 | ✅ PASS | — |\n+| 11 | SIM-01 | `8-1.H` | کلیکِ vis-out ⇒ وضعیت «خارج» و زمانِ خروجِ غیرتهی (ببرِ out_at/exited_at) | status=out · out_at=2026-09-13T14:45:53.030Z · exited_at=∅ | ✅ PASS | — |\n+| 12 | SIM-02 | `8-2.A` | دانه ≥۱ **دبیر** با lib_staff===1 (معیار A2) | 1 | ✅ PASS | — |\n+| 13 | SIM-02 | `8-2.B` | منفی (دادهٔ واقعی): دبیرِ بی‌پرچم ⇒ policy.inScope(lib_loans) === false | inScope=false | ✅ PASS | — |\n+| 14 | SIM-02 | `8-2.C` | ظرفیت (پرچمِ ساختگیِ درحافظه، برگشت‌پذیر): کتابدار ⇒ render(lib-lend) + امانت + بازگشت + inScope=true | lib-lend=true · loan=4 · lib-return=true · returned_at=2026-09-13T14:45:53.203Z · inScope=true | ✅ PASS | — |\n+| 15 | SIM-02 | `8-2.D` | منفی (دادهٔ واقعی): نمای دانش‌آموز از کتابخانه فاقدِ data-act=\"lib-lend\" (فقط‌خواندنی) | lib-lend در نمایِ دانش‌آموز: غایب | ✅ PASS | — |\n+| 16 | SIM-02 | `8-2.E` | سفرِ بیرون‌ازجعبهٔ کتابدار: login(lib_staff_demo) → render → lib-lend/lib-return | lib-lend=true | ✅ PASS | — |\n+| 17 | SIM-03 | `8-3.A` | دانه ≥۱ **دبیر** با asset_staff===1 (معیار A3) | 1 | ✅ PASS | — |\n+| 18 | SIM-03 | `8-3.B` | منفی (دادهٔ واقعی): دبیرِ بی‌پرچم ⇒ inScope(assets,{status:repair}) === false | inScope=false | ✅ PASS | — |\n+| 19 | SIM-03 | `8-3.C` | منفیِ جداسازیِ مستأجر: تحویلدارِ مدرسهٔ دیگر ⇒ inScope === false | inScope=false (user.school_id=2 ⊄ asset.school_id=1) | ✅ PASS | — |\n+| 20 | SIM-03 | `8-3.D` | ظرفیت (پرچمِ ساختگی، برگشت‌پذیر): نمای تحویلدار = as-status حاضر · as-new/as-del غایب · کلیکِ وضعیت ذخیره می‌شود · inScope=true | as-status=true · as-new/as-del غایب=true · status→repair (usable=1) · inScope=true | ✅ PASS | — |\n+| 21 | SIM-03 | `8-3.E` | سفرِ بیرون‌ازجعبهٔ تحویلدار: login(asset_staff_demo) → render → as-status | as-status=true | ✅ PASS | — |\n+| 22 | SIM-04 | `8-4.A` | `is_head` در مدلِ authz (authz/model.json → users.fields) تعریف شده | ۰ هیت · 0 هیت در src/+server/+migrations+authz [هیچ فایلی] | ❌ FAIL | is_head در هیچ‌یک از فایل‌های مدلِ دسترسی نیست ⇒ حتی seed شدنش دادهٔ مرده است (whitelistِ فیلدها در authz/model.json آن را رد می‌کند) |\n+| 23 | SIM-04 | `8-4.B` | `is_head` در کدِ محصول (src/js/*.js + server/**) مصرف می‌شود | 0 هیت | ❌ FAIL | صفرِ مصرف‌کننده ⇒ هیچ گیتِ مجوز/UI ای به is_head نگاه نمی‌کند؛ «seed کردنش بی‌اثر است» |\n+| 24 | SIM-04 | `8-4.C` | نقش/پرچمِ «رئیس اداره» (office_head یا is_head) در مدل هست | office_head=0 · is_head(محصول)=0 | ❌ FAIL | قراردادِ طراحیِ چت ۲ (office_head در برابرِ is_head) هنوز انتخاب نشده ⇒ اقدامِ دانه‌ای بی‌موضوع |\n+| 25 | SIM-04 | `8-4.D` | دانه ≥۱ کاربرِ edu_office با is_head===1 (معیار A4 — مشروط) | is_head(truthy)=0 · کلید is_head در ۰/۱۰۳۵ کاربر وجود دارد؟ 0 | ⏭️  NOT-RUN ← 8-4.C | SKIP صریح (نه سبزِ جعلی): پیش‌نیازِ طراحیِ چت ۲ برقرار نیست (8-4.A/B/C قرمز) |\n+| 26 | MATRIX | `B.5` | افزایشِ دانه حداکثر +۱ کاربر است (B5 — ضد تورم) | 1036 کاربر (پایه 1035) ⇒ دلتا 1 | ✅ PASS | — |\n+| 27 | MATRIX | `B.1` | قطعیتِ seed (دو اجرای node server/seed.js ⇒ بایت‌برابر) | — | ⏭️  NOT-RUN | NOT-RUN با دلیل: اجرای seed خروجیِ server/data/payesh.json را بازنویسی می‌کند و این دور «صفر تغییر کد/داده» است؛ گیتِ بیلینگ/CI هم باز نیست. ابزار: `node server/seed.js` ×۲ + sha256sum |\n+| 28 | MATRIX | `B.2` | رگرسیونِ suites (run/smoke/authz-model) پس از دانهٔ جدید | — | ⏭️  NOT-RUN | NOT-RUN: به دانهٔ غنی‌شدهٔ چت ۳ گره خورده (وجود ندارد)؛ همچنین بازاجرای سوئیت‌های دور ۲ در این دور ممنوع است |\n+| 29 | MATRIX | `B.3` | fail-closedِ سرور: policy.inScope پرچم را می‌خواهد (کدِ گیت در policy.js) | policy.js: lib_staff-gate=true · asset_staff-gate=true | ✅ PASS | — |\n+| 30 | MATRIX | `B.4` | اکشن‌های write-perms نگهبان/کتابدار/تحویلدار ثبت‌اند | {\"vis-save\":[\"manager\",\"guard\"],\"vis-out\":[\"manager\",\"guard\"],\"lib-lend-save\":[\"manager\",\"teacher\"],\"lib-return\":[\"manager\",\"teacher\"],\"as-status-save\":[\"manager\",\"teacher\"]} | ✅ PASS | — |\n+\n+جمع: 30 سنجه · PASS=23 · FAIL=4 · NOT-RUN=3\n+\n+یادداشت‌ها:\n+ · MODE=simulated-seed ⇒ ok:guard=1036 · lib_staff→4 · asset_staff→5 (دانهٔ غنی‌شدهٔ *درحافظه* — طرحِ §۳-۲ ماتریس؛ صفر تغییرِ دیسک؛ جانشینِ مرجِ چت ۳ نیست)\n+ · دانهٔ زنده (از db درون باندل): {\"users\":1036,\"active\":1016,\"schools\":6,\"schoolsActive\":5,\"guard\":1,\"guardActive\":1,\"lib_staff\":1,\"lib_staff_teacher\":1,\"asset_staff\":1,\"asset_staff_teacher\":1,\"is_head_truthy\":0,\"is_head_key_present\":0,\"teacher\":72,\"manager\":12,\"edu_office\":4,\"edu_office_rows\":[{\"id\":1026,\"username\":\"edu_kurdistan\",\"office_id\":1,\"is_head\":\"__absent__\"},{\"id\":1027,\"username\":\"edu_baneh\",\"office_id\":2,\"is_head\":\"__absent__\"},{\"id\":1028,\"username\":\"edu_sanandaj1\",\"office_id\":3,\"is_head\":\"__absent__\"},{\"id\":1029,\"username\":\"edu_tehran\",\"office_id\":4,\"is_head\":\"__absent__\"}],\"lib_books\":3,\"lib_loans\":3,\"assets\":4,\"visitors\":3}\n+ · بازگشتِ اثرِ پروب: رکوردهای _probe حذف و پرچم‌های ساختگی برداشته شدند (دانهٔ دیسکی هرگز لمس نشد)\n+\n+VERDICT: RED (DATASET/D.1, SIM-04/8-4.A, SIM-04/8-4.B, SIM-04/8-4.C)\n+JSON: /tmp/probe-sim.json\n+self sha256: 2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8\n+```\ndiff --git a/docs/daily-reports/2026-09-14.md b/docs/daily-reports/2026-09-14.md\nindex 4fc1506..6ddab8d 100644\n--- a/docs/daily-reports/2026-09-14.md\n+++ b/docs/daily-reports/2026-09-14.md\n@@ -47,2 +47,400 @@\n ### اعلام idle رسمی (دور ۱۵)\n تا یکی از: **بریف بستهٔ ۵ / سؤال چت ۱۰** · **استیجینگ** ‏(Offline adversarial tests) · **حکم counselor/tenancy**.\n+\n+---\n+\n+## چت ۸ — دور ۱۵: پایش تصمیم الگوی A + آماده‌سازی بستهٔ ۱ (ثبت: 2026-09-14)\n+\n+**اجراکننده:** نشست Arena `arena/01a09a73-p2` (همان نشستی که دور ۵ چت ۹ را اجرا کرد) — بریف «چت ۸ دور ۱۵» اجرا شد؛ **انحرافِ نقش** و **پنج انحرافِ بریف از واقعیت** در §۰/§۱ ثبت می‌شود (نه اصلاحِ خاموش).\n+**مبنای سنجش:** `main = bb5068b6384c9ac0cb4f268d74e3efdad5a3d45c` (پیمایشِ تازه با `git fetch` + `git ls-remote`؛ هر فرمانِ شبکه‌ای با `timeout`).\n+\n+### §۰) بریف در برابرِ واقعیت (اندازه‌گیری، نه بازگویی)\n+\n+| ادعای بریف | سنجشِ تازه روی ریمو | داوری |\n+|---|---|---|\n+| «main = `4fa5fcc` (پس از مرج #177)» | `gh api repos/…/commits/4fa5fcc` ⇒ موجود است: «Merge PR #177» — **ولی** `ls-remote` می‌گوید main = `bb5068b` (مرج #208 @ `2026-09-14T05:54:26Z`)؛ یعنی ≈۲۰+ مرج جلوتر | ⚠️ کهنه |\n+| «مرج PR #179» قلمِ باز است | `gh pr view 179` ⇒ **MERGED** `2026-09-13T16:13:27Z` (دور ۱۴ همین بود) | ✅ بسته |\n+| «بستهٔ ۰ در آستانهٔ مرج (#181)» | **MERGED** `2026-09-13T19:57:25Z` | ✅ بسته |\n+| «صریح بنویس: `OPERATIONAL_READINESS.md` وجود ندارد» | فایل **روی main هست** (`docs/OPERATIONAL_READINESS.md`، مالک: چت ۱۰، وضعیتِ خودِ سند: «مستندات VERIFIED، اجرای میدانی NOT-RUN») ⇒ نوشتنِ آن ادعا **ادعای کاذب** می‌شد | ❌ نادرست — ثبت شد، نوشته نشد |\n+| «دور ۱۵ = پایش idle + سه فایلِ موقتِ آماده‌سازی» | همین سناریو **دور ۱۵ چت ۳** در `docs/daily-reports/2026-09-14.md` مرج شده (#208) با این صریح: «به‌جای سه فایل موقتِ حذف‌شونده، مستقیم در همین گزارش — پرهیز از Documentation theater و سازگار با ممنوعِ «سند ریشهٔ تازه»» | ⚠️ تکراری ⇒ §۵ |\n+\n+**ویرانیِ جانبیِ کشف‌شده (ترمیم شد):** سندباکس میانِ دو دور **re-clone** شده بود (`reflog` ⇒ `clone: from …`، `.git/shallow`، `rev-list --count HEAD = 1`)؛ کامیتِ گزارشِ دور ۵ چت ۹ (`3dd5518`) و شاخهٔ ریموی `arena/01a09a73-p2` رفته بودند ⇒ §۳.\n+\n+### §۱) تصمیم الگوی A — **NOT-DECIDED** (سه شاهد، همگی اندازه‌گیری‌شده بر `bb5068b`)\n+\n+1. `docs/daily-reports/NEXT_ACTIONS.md:189` ⇒ «**موردِ بعدی (بدونِ مالک):** الگوی A (۱۴ فایل) + هماهنگ‌سازیِ جزئیاتِ envFail-class بینِ نسخه‌هایِ main/من» — نه ACCEPT، نه مالک.\n+2. `HANDOFF.md` (ریشه) ⇒ صفر خطِ «الگوی A»؛ شش هیتِ «ACCEPT» همگی اقلامِ دیگر (#129 / flag-1 / آن‌کال و drillها). `docs/RISK_REGISTER.md` ⇒ ۰.\n+3. `gh search issues --repo rezaa2544/p2 \"الگوی A in:body\"` ⇒ **۰ نتیجه** (نه درخواست، نه کامنتِ تازه). صف PR: فقط **#209** باز است (مالکِ چت ۷، `docs(chat7)` — صفرِ لمس).\n+\n+⇒ **فاز ۰.۵ شروع نشد** (حکمِ ۳: بدونِ ACCEPTِ الگوی A هیچ کارِ تازه‌ای روی کد/تست). ردیابِ خودِ چت ۸ (`docs/daily-reports/2026-09-13-chat8.md:123-140`) دورهای ۱۰/۱۱/۱۲/۱۳ را idle یا «منتظرِ ACCEPT» ثبت کرده؛ این دور هم همان حالت — **بی‌شمارشِ ساختگی** (شماره‌گذاریِ «هشتمین دور» از شواهدِ فایل قابلِ بازتولید نیست و ادعا نشد).\n+\n+### §۲) آماده‌سازی بستهٔ ۱ (National Staging + Dataset) — «هست / نیست» با شاهدِ مسیر\n+\n+**هست (روی main، راستی‌آزماییِ مسیر با `git ls-tree`/`git show`):**\n+\n+| دارایی | واقعیتِ سنجیده‌شده |\n+|---|---|\n+| `infra/wave18-loadtest/staging-bootstrap.sh` | ۱۲۹ خط · install→initdb→migrate→seed→API · **تک‌نود** · پارامترها `W18_PORT/W18_PGPORT/W18_SCALE` · هشدارِ خودِ فایل: «`--scale 0.001` مقیاسِ اعتبارسنجیِ هارنس است، **نه** اندازهٔ ظرفیت؛ sandbox = 2c/2GB/20GB» |\n+| `infra/postgres/docker-compose.ha.yml` | HA واقعی + `pgbouncer/pgbouncer.ini` + `pgbackrest.conf.template` + `init/01-replication.sh` + `post-checks.sh` |\n+| `infra/redis/docker-compose.sentinel.yml` + `ops/redis/` | ۳ نود + ۳ sentinel (config‌های جدا) + `redis-checks.sh` |\n+| `infra/observability/docker-compose.observability.yml` | Prometheus + Loki + Grafana + otelcol + `alert-rules.yml`/`alertmanager.yml` (سنجه‌های SLO از همین‌جا) |\n+| `tests/performance/suites/` | **۵** سوئیت k6: `national-load-test` · `saturation-test` · `spike-mehr-test` · `chaos-redis-test` · `soak-24h-test` |\n+| `tools/generate-national-dataset.js` | بذرِ قطعی `--seed 20260901` · `--plan` «فایلی ننویسد» · `--scale` · `--fast` |\n+\n+**نیست (سنجیده، نه حدس):** **هیچ compose/هارنسِ چندنودیِ *لایهٔ اپ*** — `git grep -lE \"replicas|api-2|node2\" origin/main -- infra/ ops/` ⇒ فقط `infra/redis/*`، `infra/tracing/README.md`، `ops/redis/README.md`. ⇒ گلوگاهِ «چندنودی» بستهٔ ۱ در **API+LB+per-instance state** است، نه PG/Redis (آن‌ها HA/sentinel دارند). همین با KNOWN UNKNOWNِ ثبت‌شدهٔ چت ۳ (نبودِ gauge صفِ *سروری* per-instance) هم‌خوان است.\n+\n+**سنجهٔ اجراشدهٔ امروز (صفر نوشتن):** `timeout 60 node tools/generate-national-dataset.js --plan` ⇒\n+\n+```text\n+scale=1 (ملی)      : schools 100,000 · classes 1,000,000 · users 10,000,000 (8M/1M/0.9M/99,900/99/1)\n+                     attendance 50,000,000 · grades 20,000,000 · installments 24,000,000 · outbox 8,000,000\n+scale=0.001 (CI)   : schools 100 · classes 1,000 · users 10,002 (8,000 دانش‌آموز · 1,000 دبیر · 900 والد · 100 مدیر)\n+```\n+\n+`git status` پس از اجرا: صفر فایلِ نوشته‌شده (`data/national/` ساخته نشد؛ در `.gitignore` هم هست). **توصیهٔ شروع از مقیاسِ کوچک = همین سطرِ دوم**، چون `--plan` قبلِ هر دیسک‌هزینه‌ای، حجمِ واقعی را چاپ می‌کند.\n+\n+**معیارهای پذیرش (با منبع، نه از بریف):** `docs/CAPACITY_MODEL.md` §۵ سطرهای ۲۴۳–۲۴۶ ⇒ `p50 < ۱۰۰ms` · `p95 < ۳۰۰ms` · `p99 < ۱s` · `5xx < ۰.۱٪` (بودجهٔ خطا ≈ ۴۳ دقیقه/ماه) — **هم‌خوان با اعدادِ بریف** ✅ (سنجیده شد، نه کپی). ⚠️ ولی: «۲۰k RPS / 2500 writes/s / 833 login/s» در بریف، در `CAPACITY_MODEL.md` **تصویب‌شده نیست**؛ همان سند سطر ۸۷ می‌گوید «توانِ فرضی هر نود ≈ ۲٬۰۰۰ RPS در p95<۳۰۰ms — **فرض**؛ باید در Wave 18 اثبات شود». ⇒ هر workloadِ بستهٔ ۱ باید به **اثباتِ این فرض** گره بخورد، نه به عددِ از پیش تعیین‌شده (خط‌قرمزِ «benchmark بدون workload واقعی»).\n+\n+**چرا اجرای محلی = NOT-RUN (عدد، نه بهانه):** `nproc=2 · RAM=3939MB · disk=20GB avail` ⇒ `scale=1` ده‌ها GB است و ۲۰k RPS روی این ماشین فقط عددِ بی‌معنی تولید می‌کند. **ظرفیتِ ملی همچنان اثبات‌نشده است** (KNOWN UNKNOWNِ دائمی).\n+\n+### §۳) ترمیمِ خسارتِ re-clone (بازیافتِ گزارشِ دور ۵ چت ۹)\n+\n+- بلوکِ ۸۸۷ سطریِ «چت ۹ — دور ۵» از working tree بیرون کشیده و روی نسخهٔ تازهٔ `docs/daily-reports/2026-09-13.md` (روی main: ۲٬۰۵۷ خط، شاملِ بخش‌های الحاقیِ چت‌های ۳/۴/۸ و دور ۴ چت ۹) **نشانداه شد**: `git diff --numstat` ⇒ **887 افزودن / ۰ حذف** ⇒ append-only حفظ شد و هیچ بخشِ دیگری بازنویسی/حذف نشد.\n+- **آزمونِ بازتولیدِ آرشیو (همان ادعای دور ۵):** پروب از **بلوکِ کدِ درونِ گزارش** استخراج شد ⇒ `sha256 = 2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8` == sha اعلامی در دور ۵ ✅ ⇒ «قابل‌بازتولید بدونِ کدِ مخزن» یک ادعا نبود؛ عملاً آزموده شد (۵۶۶ خط، `node --check` سالم).\n+\n+### §۴) **سنجهٔ تازهٔ امروز:** بازراستی پذیرشِ دانه روی `bb5068b` (قلمِ بازِ دور ۵ — بسته شد)\n+\n+یادداشتِ مقدماتی: در `0a208f8` (دور ۵) دانه **۱۰۳۵** و هر چهار نقش غایب بود؛ غنی‌سازی در **#181** آمده است (`src/js/02-demo-data.js:451` ⇒ `role:'guard'` و `:459/460` ⇒ `lib_staff=1`/`asset_staff=1` روی دو دبیرِ موجود) + سوئیتِ `tests/seed-completeness.js`. **گیتِ خودِ چت ۳ بازاجرا نشد** (ضدتکرار با دور ۱۴ که آن را در ۱۱/۱۱ داشت) — شواهدِ زیر **رفتاریِ مستقل** است، با همان پروبِ بایگانی‌شده:\n+\n+| سنجه | دور ۵ (`0a208f8`) | امروز (`bb5068b`) |\n+|---|---|---|\n+| دانه | ۱۰۳۵ · guard/lib_staff/asset_staff = ۰/۰/۰ | **۱۰۳۶ · ۱/۱/۱** (active=1016) |\n+| `8-1.A…H` نگهبان | FAIL + NOT-RUN ×۵ | **PASS ×۷** — ورودِ واقعی (`رضا نگهبانی` id=1025) → داشبوردِ پذیرش → `vis-new`+`vis-save` (`school_id=1/1 · registered_by=1025/1025`) → `vis-out` (`status=out · out_at=2026-09-14T06:10:43Z`) |\n+| `8-2.A…E` کتابدار | FAIL + NOT-RUN | **PASS ×۵** (سفرِ بیرون‌ازجعبه `lib-lend=true` · `inScope=true` · منفی‌ها رد) |\n+| `8-3.A…E` تحویلدار | FAIL + NOT-RUN | **PASS ×۵** (`as-status=true` · `as-new/as-del` غایب · بین‌مدرسه‌ای `false`) |\n+| `D.1` پین‌ها | ۱۰۳۵==۱۰۳۵ PASS | **PASS** — `donations2.js:55` و `drills2.js:54` == **۱۰۳۶** ⇒ حُکمِ «هم‌کامیتِ پین با دانه» **درست اجرا شده** (هر دو با کامنتِ `/* SIM-01: +guard1 (seed-completeness) */`) |\n+| `B.5` ضد تورم | دلتا ۰ | **PASS** — دلتا `+1` (دقیقاً سقفِ B5؛ بدونِ کاربرِ تازه برای دو پرچم) |\n+| `8-4.A/B/C` (SIM-04) | FAIL ×۳ | **FAIL ×۳ بدونِ تغییر** · `8-4.D` NOT-RUN (SKIP صریح) |\n+| جمع | 14 PASS / 6 FAIL / 10 NOT-RUN | **24 PASS / 3 FAIL / 3 NOT-RUN** |\n+\n+**سه یافتهٔ تازه (هرکدام با شاهد، هیچ‌کدام با تغییرِ کد همراه نیست):**\n+\n+1. **نامِ حسابِ seed با نامِ پیشنهادیِ ماتریس نمی‌خواند:** §۸-۱ و بریف `guard_demo` می‌گویند، seed واقعی `guard1` ساخته (`02-demo-data.js:451`). پروبِ من با **fallbackِ نقش+پرچم** رد شد و سنجه‌ها سبز شدند (`via: role/flag-fallback → guard1`)؛ ولی اگر ناظر/چت ۳ بخواهد سفرها **با username** تضمین شوند، یا سند اصلاح شود یا seed — تصمیمِ مالک، نه چت ۹.\n+2. **واژهٔ §۸-۱ «exited_at» همان‌جا می‌مرد:** سنجه روی mainِ تازه `out_at=… · exited_at=∅` داد ⇒ قراردادِ محصول `out_at` است؛ اصلاحِ واژگانیِ سندِ پذیرش (مالک: چت ۳/ناظر) همچنان باز است.\n+3. **SIM-04 با دانه حل نمی‌شود — تأییدِ تجربیِ دوم:** پس از غنی‌سازیِ کامل، سه FAILِ سطحِ طراحی **بدونِ تغییر** ماندند (`is_head`: ۰ هیت در `authz/model.json` + ۹۶ فایل `src/js/` + `server/**` + `migrations/*.sql`؛ `office_head=0`). ⇒ «seed کردنِ `is_head`» بی‌اثر است تا طراحیِ چت ۲ بیاید — دقیقاً همان چیزی که دور ۴/۵ پیش‌بینی کرده بود.\n+\n+**Verdictِ این بند (تاکسونومی):** **PASS/VERIFIED** برای SIM-01..03 (پذیرشِ رفتاریِ دانه روی mainِ جاری) · **FAILِ سطحِ طراحی** برای SIM-04 · **NOT-RUN**: اجرای کدِ تستِ چت ۳ (ضدتکرار با دور ۱۴) و CI (بیلینگ).\n+\n+### §۵) سه فایلِ موقتِ بریف — **عمداً ساخته/حذف نشد** (ثبتِ دلیل، نه بی‌عملیِ بی‌صدا)\n+\n+سه نامِ بریف (`PATTERN_A_MONITORING` / `NATIONAL_STAGING_PREP` / `OPERATIONAL_PREP`) ساخته **نشدند** (بدونِ پسوندِ مسیر — چون هیچ‌گاه وجود بیرونی نیافتند و نوشتنِ `docs/…md` خودشان را به ارجاعِ کهنه بدل می‌کرد)، چون: (الف) بریف هم‌زمان «فایلِ تازه در `docs/` (مگر گزارشِ روزانه)» را ممنوع کرده و «ساختن و سپس حذف» را خواسته ⇒ خروجیِ صفرِ ماندگار = **Documentation theater** (خط‌قرمزِ خودِ ابلاغیه)؛ (ب) همین کار با همان خروجی در **#208** مرج شده و صراحتاً همین ردِ «سه فایلِ موقت» را ثبت کرده؛ (ج) `OPERATIONAL_PREP.md` در دامنهٔ **چت ۱۰** است (بستهٔ ۵) و سندِ `OPERATIONAL_READINESS.md` موجود است — ساختنِ موازی‌اش نقضِ مالکیت بود. **محتوای ماندگارِ همان سه فایل در §۱ و §۲ همین بخش است** (تصمیم + هست/نیست + اعدادِ `--plan` + SLO با منبع) ⇒ نه بدهی، نه تکرار، نه سندِ بادکرده.\n+\n+- **آزمونِ عملیِ همین استدلال (اندازه‌گیری شد، نه حرف):** وقتی §۵ عمداً نامِ سه فایل را با قالبِ `docs/<name>.md` نوشت، `docs-refs-check` دقیقاً **+۳ ارجاعِ کهنهٔ تازه** داد (۴۱ → ۴۴)؛ پس از اصلاحِ نگارش، دوباره ۴۱. در خودِ main هم ردپای همین الگو هست: `2026-09-13.md` به سندی به‌نامِ «PROBE_TMP» (موقتِ سوخته) ارجاع می‌دهد که دیگر وجود ندارد ⇒ «ساخت‌وبسوزِ سند» فقط بی‌فایده نیست، **بدهیِ ارجاعی** می‌سازد.\n+\n+### §۶) ENGINEERING CLAIM\n+\n+```text\n+EXPECTED BEHAVIOR:\n+  - پایشِ وضعیتِ تصمیمِ الگوی A (با شاهد، نه حدس) + آماده‌سازیِ مبتنی‌بر-شواهدِ بستهٔ ۱\n+  - صفر کار روی کد/تست تا اعلامِ ACCEPT (حکم ۳ — ضد Feature Factory)\n+  - اعلانِ idle رسمی در پایانِ دور\n+MEASURED BEHAVIOR:\n+  - الگوی A: NOT-DECIDED (سه شاهد: NEXT_ACTIONS:189 بدونِ مالک · ۰ در HANDOFF/RISK · ۰ gh-search)\n+  - بستهٔ ۱: ۶ دارایی «هست» + ۱ شکافِ سنجیده‌شده (عدمِ وجودِ هارنسِ چندنودِ لایهٔ اپ) + ۲ اجرای --plan (ملی/CI) + SLOهای منبع‌دار\n+  - بازیافتِ گزارشِ دور ۵ چت ۹ (۸۸۷ سطر · ۰ حذف) + آزمونِ صحتِ sha256 آرشیو + بازراستیِ پذیرشِ دانه ⇒ 24 PASS / 3 FAIL / 3 NOT-RUN\n+  - ۰ کدِ جدید · ۰ تستِ جدید · ۰ فایلِ موقتِ ساخت‌وبسوز (به‌جایش §۵ با دلیل)\n+TEST ENVIRONMENT: main @ bb5068b · سندباکس 2 core / 3939 MB RAM / 20 GB free · node v22 · jsdom در /tmp (بیرون از مخزن)\n+DATASET SIZE: دانهٔ demo روی main = 1,036 کاربر · برنامهٔ ملی (--plan، صفر نوشتن) = 10M کاربر/100k مدرسه/50M حضور\n+CONCURRENCY: n/a (پایش + پروبِ تک‌نمونه؛ هیچ تستِ همزمانی اجرا نشد)\n+P50 / P95 / P99: n/a — **عمداً خالی** (هیچ بنچی اجرا نشد؛ staging وجود ندارد)\n+ERROR RATE: پروب روی main = 3 FAIL از 30 (SIM-04/طراحی) · اجرای --plan = ۰ خطا (exit=0) · CI = NOT-RUN\n+DB IMPACT: n/a (PG اجرا نشد؛ `--plan` فایلی نمی‌نویسد؛ `server/data/` اصلاً در این clone نیست)\n+CACHE IMPACT: n/a (Redis/کش لمس نشد)\n+SECURITY IMPACT: ۰ توکن در فایل/argv/کامیت/گزارش · secret-scan 12/12 · ۶ PAT فاش‌شده، revoke توسط مالک\n+FAILURE TEST: re-cloneِ سندباکس خودِ سناریوی شکست بود (کامیت + شاخهٔ ریمو رفت) ⇒ بازیافتِ append-only با ۰ حذف + sha256ِ بایگانی = مسیرِ بازیابیِ آزموده‌شده\n+RECOVERY RESULT: 3dd5518 ↦ بازیافت در این کامیت · شاخهٔ ریمو ⇒ بازسازی با push (§۸)\n+REGRESSION RISK: صفر تغییرِ کد/داده؛ دو فایلِ روزانه، هر دو append-only\n+KNOWN UNKNOWNs: الگوی A NOT-DECIDED · ظرفیتِ ملی اثبات‌نشده (دائمی) · هارنسِ چندنودِ اپ وجود ندارد · CI NOT-RUN (بیلینگ، RISK-O-007) · گیتِ seed-completeness بازاجرا نشد (ضدتکرارِ دور ۱۴) · بازتولیدِ payesh.json NOT-RUN\n+FINAL VERDICT: **VERIFIED** (پایش + آماده‌سازیِ بستهٔ ۱، با شاهدِ مسیر/عدد) · **NOT-RUN** (تصمیمِ الگوی A — اعلام نشده) · **NOT-RUN** (هر اجرایِ ظرفیت/staging) · **PASS** (بازراستیِ پذیرشِ دانه روی mainِ جاری: SIM-01..03)\n+```\n+\n+### §۷) گیت ۸ سؤال\n+\n+1) **تغییر:** دو فایلِ گزارشِ روزانه (الحاق) — و **ردِ** سه فایلِ موقت با دلیل (§۵). 2) **چرا:** بریف کهنه بود (main/#179/#181/OPERATIONAL_READINESS) و کارِ خواسته‌شده تا حدی مرج‌شده (#208) ⇒ ارزشِ افزوده = تصحیحِ وضعیت + بازراستیِ دانه. 3) **Invariant:** حُکمِ ۳ (بیِ الگوی A ⇒ صفر کارِ کد/تست) + append-only + صفرِ بامپ rc + مالکیت‌ها. 4) **اثباتِ درستی:** سه شاهدِ الگوی A · `git ls-tree`/`git show` برای هست/نیست · دو اجرای `--plan` · خروجیِ ۳۰ سنجهٔ پروب با sha256. 5) **اثباتِ سرعت/مقیاس:** **n/a** — عمداً هیچ عددی ساخته نشد. 6) **اثباتِ امنیت:** ۰ توکن + `secret-scan 12/12` روی درختِ نهایی. 7) **failure:** re-clone به‌عنوان حادثهٔ واقعی + بازیافتِ آزموده‌شده (§۳). 8) **rollback:** `git revert` این کامیت (دو فایلِ گزارش، ۰ اثر جانبی).\n+\n+### §۸) گیت‌ها · بودجه · پوش · idle\n+\n+- **گیت‌های مسیرِ تغییر (همگی سبز، روی درختِ نهایی):** `docs-stats-sync --check` ⇒ ✅ (۴۰۶ سند: ۳۴۸ ریشه + ۵۸ زیرپوشه · قفل **rc43**) · `docs-freeze-marker` ⇒ **14/14** · `docs-metadata` ⇒ **17/17 · یتیم ۰** · `docs-refs-check` ⇒ **۴۱ ارجاعِ کهنه، همگی پیش‌ازاین — صفرِ تازه** (سنجشِ دوطرفه: با الحاقِ من ۴۱، بدونِ الحاق ۴۱؛ baseline=۷۵، تولیدیِ ردشده=۵) · `secret-scan` ⇒ **12/12** (روی درختِ شاملِ هر دو الحاق) · `reza-mirror-check` ⇒ «✅ همهٔ آینه‌های زنده با منبعِ docs/ برابرند» — **۰ drift / ۰ اختراعِ آینه**. هر فرمان شبکه‌ای/اجرا با `timeout` (۶۰–۳۰۰ث). **بازاجرا نشد:** ۱۱ گیتِ کدِ دور ۱۴ · بازتولیدِ مانیفست · گیت‌های docsِ بی‌ربط · سوئیت `seed-completeness` (ضدتکرار).\n+- **بودجهٔ §۱ (معیارِ مؤثر، بدونِ `node_modules`/`.cache`):** **پس از کار = 48.4 MB / 1543 فایل** 🟢 (سنجشِ مستقیم در پایان) · **پیش از کار ≈ 48.3 MB / 1543 فایل** — چون «پیش از کار» در ابتدایِ این re-clone سنجیده **نشده بود**، عددِ پیشین از پس‌زداییِ حسابیِ بایت‌هایِ الحاقی (86٬359 B در 1٬01۶ سطر) به‌دست آمده و **برچسبش «مشتق» است، نه اندازه‌گیریِ مستقل**؛ تازه‌سازیِ فایل: **۰** (هر دو تغییر، الحاق به دو فایلِ موجود). نکتهٔ بهداشتی: این سندباکس پس از re-clone کوچک‌تر است (`.git` کمکی = shallow)، پس اعدادِ بودجهٔ دورهایِ پیش (مثلاً «136MB» در دور ۱۵ چت ۳) با این عدد **قابلِ مقایسه نیستند**.\n+- **پوش:** یک کامیت + `git push origin arena/01a09a73-p2` + `ls-remote == HEAD` (نتیجه در گزارشِ نشست؛ upstreamِ شاخه پس از re-clone عمداً unset شد تا push فقط با refspecِ صریح انجام شود — محافظتِ main).\n+- **حکم‌های رعایت‌شده:** ۱) تصمیمِ الگوی A در زمینِ ناظر ⇒ فقط پایش ✅ · ۲) بستهٔ ۱ پیش‌نیازِ بسته‌های بعدی ⇒ شکافِ اصلی (چندنودیِ اپ) ثبت شد ✅ · ۳) بیِ ACCEPT ⇒ صفر کد/تست ✅ · ۴) mutation با gh ⇒ راستی‌آزماییِ بایت‌های live (این دور هیچ mutation با gh انجام نشد؛ فقط push محلی) ✅\n+- **idle رسمی:** تا یکی از: **اعلامِ ACCEPT/ردِ الگوی A** · **حکمِ ناظر** · **بریفِ جدید**. تنها کارِ مجازِ بعدیِ این مسیر: پیگیریِ همان تصمیم + اجرایِ بستهٔ ۱ **پس از** ACCEPT.\n+## چت ۹ — دور ۶: پایش وضعیت پوش + آماده‌سازی بازراستی‌آزمایی پس‌امرج + نهمین دور idle (ثبت: 2026-09-14)\n+\n+- **وضعیت پوش: رفع شد در این محیط** ⇒ **فاز ۰.۵** — راهکارِ اثبات‌شده: resetِ ششم (origin/هویت/node22) ترمیم شد؛ اتصالِ autenticated با env + credential-helperِ inline (fetch + ls-remote سبز). یادداشت: CLIِ `gh` در سندباکس auth ندارد — معادلِ اثبات‌شده همان helper است؛ شاهدِ نهایی = پوشِ همین دور. مشکلِ پوشِ دور ۵ قطعِ اتصالِ میانهٔ نشست (environmental) بود، نه اعتبارنامه/دسترسی.\n+- **مبنای این دور:** پرامپت main/شاخه را @`0fc4403`/`3dd5518` می‌گفت؛ اندازهٔ تازه: main=`bb5068b` (موجِ مرجِ ۱۸۹–۲۰۸ + #206/#207/#208) و شاخهٔ `arena/01a09a73-p2`=`0ef89e6` (= main + ۲ کامیت: احیای بخشِ چت ۹ دور ۵ + چت ۸ دور ۱۵) — کار روی headِ واقعی انجام شد (بدونِ rebase/لمسِ کامیت‌های دیگران).\n+- **آماده‌سازی برای بازراستی‌آزمایی پس از مرج:** ۶ سناریو (S1–S6: سفرهای SIM-01..03 + is_head-SKIP صریح + منفی‌های fail-closed + قطعیتِ دانه) / ۷ راهکار ایمن (worktree خواندنی، npm ci جدا، seedِ رسمی، سوییپِ پورت با لنگر `^/usr/bin/node` + PID-only، timeout 30، توکنِ env-only، صفرِ نوشتن به شاخه‌ها) / ۷ معیار پذیرشِ SLO (عیناً از CAPACITY_MODEL §۵: p50<100ms · p95<300ms · p99<1s · 5xx<0.1٪ · کش>80٪ · sync≥99.5٪ · OTP≥99.9٪) + ۴ توصیهٔ مقیاسِ کوچک.\n+  - **گپِ ثبت‌شده:** پروبِ ۵۶۶-خطیِ دور ۵ در ریپو نبود و از /tmp گم شده ⇒ S1–S3 یا منتقل‌شدنِ پروب به `tests/` (دورِ مجزا با حکم) یا NOT-RUN تا آن زمان؛ S5/S6 با سوئیت‌های موجود قابلِ اجراست.\n+- **آماده‌سازی برای بستهٔ ۵ (Operational Readiness): انجام نشد — مالک چت ۱۰** (`OPERATIONAL_READINESS.md` وجود ندارد؛ چت ۹ فقط مالکِ بستهٔ ۳ است — تفکیکِ مالکیتِ صریح).\n+- **گیت‌ها (اجرای تازه روی شاخه):** freeze **14/14** · stats --check **exit 0** · metadata یتیم **هیچ** · secret-scan **12/12** · mirror **0/0** (فقط چک) · docs-refs: **RED پیش‌موجود — تازه=41** (این دور ایجاد نشد: موجِ مرجِ شبِ قبل #196/#206/#207/#208) — تفکیکِ اندازه‌گیری‌شده: ۳۴ ارجاعِ forward-طراحیِ کارهای چت ۴ (pgbouncer/weighted-partitioning/version-vector/summer — سوئیت‌ها هنوز ساخته نشده؛ #196 design-only بود) در CHAT4_* + ROADMAP + **HANDOFF** + اسنادِ طرح · ۲ ارجاعِ پیش‌روِ handoffِ چت ۶ (گزارش‌های همزمانی چت ۳) · ۵ ارجاعِ تاریخیِ نقطه‌درزمانی — **تعمیر/بازتولیدِ baseline (75→116 مشروط) = اقدامِ HO-1/چت ۸ در دورِ بعد؛ در این دور ثبت شد و تعمیر نشد** (دامنهٔ دور = رفع پوش + آماده‌سازی).\n+- **پوش:** [SHA در گزارشِ پایانِ نشست] == ls-remote ✅ (یک کامیتِ addِ صریح، هویتِ چت ۹) · **اسکنِ توکن: 0**\n+- **بودجهٔ §۱:** 102.8MB/1878 🟡 (قبل) → [بعد: پایانِ نشست — gc ملایم انجام می‌شود چون >100]\n+- **idle رسمی** تا: حکمِ ناظر (شروعِ بازراستی‌آزمایی) / بریفِ جدید — مشکلِ پوش دیگر مانع نیست؛ بعدیِ طبیعی = نقلِ پروب به ریپو یا اجرای S5/S6 با حکم\n+\n+---\n+\n+## چت ۹ — دور ۱۶: پایش وضعیت تصمیم الگوی A + اعلام idle (ثبت: 2026-09-14)\n+\n+**برچسبِ بریف:** «بیست‌وپنجمین دور idle». **سنجش:** شمارِ اعلام‌هایِ صریحِ idle در `docs/daily-reports/2026-09-13.md` + `2026-09-14.md` روی main = **۹** (۵ + ۴)؛ رشتهٔ «بیست‌وپنجم» تنها در نامِ فایل `DOCS_FREEZE_v1.0.0-rc25.md` و `DOCS_INDEX.md` می‌آید (اشاره به **rc25**، نه شمارشِ idle) ⇒ عددِ ۲۵ شمارشِ idle نیست؛ برچسبِ بریف بی‌تغییر نگه داشته شد و **انحراف همین‌جا ثبت می‌شود** (اصلاحِ خاموش نه).\n+\n+### §۰) بریف در برابرِ واقعیت (پنج انحرافِ تازه، اندازه‌گیری‌شده بر `main = 5a175c0`)\n+\n+| # | ادعای بریف | واقعیت (شاهد) |\n+|---|---|---|\n+| ۱ | «شاخه @ [SHA کامیت دور ۱۵]» | دور ۱۵ من = `0ef89e6`، ولی **tipِ واقعیِ شاخه** = `ab933c1` = «چت ۹ — دور ۶» (۱۱ سطرِ یک نشستِ دیگر روی همین branch) ⇒ مبنای این دور `ab933c1` گرفته شد، نه بازگشت به `0ef89e6` |\n+| ۲ | `gh api …/issues/comments --json query='{ repository { issue(number: 184) … } }'` | `issue(number:184)` ⇒ **NOT_FOUND** (#184 یک **PR** است، نه issue) و فرمِ `--json query=` هم معتبر نیست. جایگزینِ اجراشده: `gh api graphql` + `gh api repos/…/issues/184/comments` و `…/pulls/184/comments` ⇒ **۰** کامنت شاملِ «الگوی A» |\n+| ۳ | «RTO/RPO **و SLA 99.95٪** از `DR_RUNBOOK`» | `docs/DR_RUNBOOK.md:4` ⇒ RTO ≤ ۱۵ دقیقه · RPO ≤ ۵ دقیقه · failover ≤ ۳۰ ثانیه ✅؛ اما `grep '99\\.95' docs/DR_RUNBOOK.md` = **۰**. عددِ 99.95٪ در `docs/WAVE14_OBSERVABILITY.md:174` است (Availability / 30d) و `docs/OPERATIONAL_READINESS.md:27` می‌گوید پیشنهادِ «API 99.9 / sync 99.95» **جایگزین** شده ⇒ انتسابِ بریف به DR_RUNBOOK غلط است |\n+| ۴ | «دیسک ≈ 140GB (فرمول خطیِ measure‌شده: 1.4GB@0.01 × 100)» | پایه‌ها مستندند (`docs/REPORT_PERF_GAP_SPEC.md:55`، `docs/WAVE23_DB_NATIVE_REPORTS.md:257`) ولی **×۱۰۰ مشتق است، نه اندازه‌گیری**؛ هیچ سندِ مخزن ۱۴۰GB را تأیید نمی‌کند ⇒ در acceptance-bundle با برچسبِ **«مشتق»** می‌آید |\n+| ۵ | فاز ۱ = «سه فایلِ موقت بساز و در پایانِ دور حذف کن» | با «فایلِ تازه در `docs/` ممنوع» **و حکم ۳ِ خودِ بریف** («اسنادِ تازه باید ارجاعِ ورودی بگیرند یا در baseline ثبت شوند») در تضاد ⇒ **رد**، با شاهدِ کمّی در §۴ |\n+\n+### §۱) تصمیم الگوی A — **NOT-DECIDED** (گیتِ خودِ بریف، نه تفسیرِ من)\n+\n+- فرمانِ تشخیصِ بریف، عیناً اجرا شد: `gh api repos/rezaa2544/p2/contents/docs/daily-reports/NEXT_ACTIONS.md | base64 -d | grep -c \"الگوی A تأیید شد\"` ⇒ **۰**\n+- `docs/daily-reports/NEXT_ACTIONS.md:189` = همان خطِ **بدونِ مالک** (بدونِ تغییرِ نسبی از دور ۱۵): «موردِ بعدی (بدونِ مالک): الگوی A (۱۴ فایل) + هماهنگ‌سازیِ جزئیاتِ envFail-class…»\n+- `docs/HANDOFF.md`: «الگوی A» = **۰**؛ تنها «تأیید شد» در `:159` مربوط به اجرای ران‌بوکِ §۹.۶ است ⇒ **false-positive رد شد**\n+- `docs/RISK_REGISTER.md`: «الگوی A» = ۰ · `git grep \"الگوی A تأیید شد\" origin/main -- docs` = **۰**\n+- سه نامزدِ «سیگنالِ اشتباه» که سنجیده و **رد** شدند: (الف) تگ‌ها: `national-baseline-start → 8850973 (2026-09-09)` و `v1.0.0/v1.0.1 (2026-09-07)` — همگی **پیش از** هر تصمیم ⇒ «شروعِ استیجینگ ملی» نیستند؛ (ب) **PR #211** (همین شاخه): ۲ کامنت + ۱ review، همهٔ بات (strix: «سقف ۵۰ بررسی = billing»، coderabbit: skip، devin: «3 flags» که در این سندباکس قابل مشاهده نیست) ⇒ **هیچ حکمِ انسانی**؛ (ج) PR #213 (چت ۵) **MERGED** بدونِ هیچ ذکری از الگوی A\n+- ⇒ شرطِ فاز ۰.۵ برقرار نبود ⇒ **اجرا نشد**؛ بستهٔ ۱ **NOT-RUN/NOT-VERIFIED**؛ **۰** کار روی کد/تست (حکم ۲ — ضد Feature Factory)\n+\n+### §۲) ارزش افزودهٔ این دور: **سنجه‌پذیریِ ۷ معیارِ پذیرش** (نام‌های واقعی از `server/metrics.js`)\n+\n+معیارها عیناً از `docs/CAPACITY_MODEL.md` §۵ (سطرهای ۲۴۳–۲۵۰) — همان ۷ تایِ بریف، همه موجود:\n+\n+| SLO | سنجه | وضعیتِ نام در کد |\n+|---|---|---|\n+| p50 < 100ms · p95 < 300ms · p99 < 1s | `payesh_http_request_duration` (histogram) | ✅ مستقیم (۳ ارجاع در `server/metrics.js`) |\n+| 5xx < 0.1٪ (بودجهٔ ≈۴۳ دقیقه/ماه) | `payesh_http_requests_total{code=~\"5..\"}` | ✅ (PromQLِ مصوب در `docs/WAVE14_OBSERVABILITY.md:174`) |\n+| نرخ اصابت کش > 80٪ | سند می‌گوید `payesh_cache_hit_rate` ⇒ **در `server/metrics.js`: ۰ hit** | ⚠ **باید مشتق شود**: `rate(payesh_cache_hits_total[5m]) / rate(payesh_cache_lookups_total[5m]) > 0.80` (نام‌های واقعیِ موجود: hits / misses / lookups / invalidations) |\n+| موفقیت همگام‌سازی ≥ 99.5٪ | `payesh_sync_pulls_total` + `payesh_sync_conflicts_total` | ✅ نسبت از دو شمارندهٔ موجود |\n+| OTP ≥ 99.9٪ (ساعات مدرسه) | `payesh_auth_otp_requests_total{outcome}` (`metrics.js:438,713`) | ✅ نسبتِ `outcome=\"ok\"` در برابرِ کل؛ نرخِ ورود با `payesh_auth_login_total` (`:439`) |\n+\n+⇒ **۴ از ۷ مستقیم، ۳ نسبت‌محور**؛ و «WORKLOAD: 20k RPS / 2500 writes/s / 833 login/s» همچنان **در هیچ سندِ مصوبی تصویب نشده** (منبعِ مخزن: `docs/CAPACITY_MODEL.md:87` — per-node ≈ **2000 RPS** هنوز فرضِ تست‌نشده). شکافِ `payesh_cache_hit_rate` = **یافتِ تازهٔ این دور** و یک بدهیِ سند↔کد است (مالک: چت ۱۰ برای تعریفِ gauge، چت ۳ برای اصلاحِ متنِ CAPACITY_MODEL).\n+\n+### §۳) نردبانِ پیش‌نیازِ محیط — **۰ از ۶ِ سخت‌افزاری برآورده** (سنجشِ امروز)\n+\n+۱) **۲+ نود API:** `nginx/nginx.conf:83-84` بلوکِ `upstream payesh` را دارد ولی **دقیقاً ۱ عضو** (`127.0.0.1:3000`، `listen 8080` با کامنتِ «POC بدونِ root؛ در پروداکشن ۸۰/۴۴۳ پشتِ Cloudflare») و در `infra/**/docker-compose*` **۰** تعریفِ `replicas|scale` ⇒ **تصحیحِ گزارۀ دور ۱۵** («هیچ LB نیست») : شکلِ LB **هست**، تک‌عضو — خلأ واقعی «تکثیرِ نود» است نه «نبودِ nginx».\n+۲) **PG/Redis واقعی (sentinel):** فایل‌ها موجود (`infra/postgres/docker-compose.ha.yml`، `infra/redis/docker-compose.sentinel.yml`) ولی `command -v docker` ⇒ **ABSENT** ⇒ اجرای stack **NOT-RUN**\n+۳) **RAM ≥ 16GB** ⇒ `free -m` = **3939 MB** (کسری ≈۴.۲×) ❌ `nproc` = **۲** (برای نردبانِ multi-node هم کم است) ⇒ ❌\n+۴) **دیسک (مقیاس ملی)** ⇒ `df` = **19 635 MB** آزاد در برابرِ ≈۱۴۰GB مشتق (کسری ≈۷.۱×) ❌\n+۵) **root/tmpfs برای WAL-drill** ⇒ `id -u` = **1001** (غیرroot) · `df -T /tmp` = **ext4** ⇒ ❌ (۵ tmpfs سیستمی هست، ولی /tmp tmpfs نیست و mount توانمند نیستیم)\n+۶) **اسکریپت‌ها:** `tools/generate-national-dataset.js` ✅ · `tools/w18-load-pg.sh` ✅ ⇒ تنها دو ✅ از ۶، و هر دو «فایل هست» نه «محیط آماده است»\n+⇒ **نتیجه:** اجرای ملی **ممکن نیست** (عدد، نه بهانه). هر دورِ بعدی تا برآورده‌نشدنِ همین ۶، همان حکم را خواهد داشت؛ اجرای مجددِ `--plan` (دور ۱۵) تکرارِ بیهوده بود ⇒ **انجام نشد**.\n+\n+### §۴) سه فایل موقت — دوباره ساخته **نشدند** (این‌بار با ارجاع به قاعدۀ خودِ بریف)\n+\n+`PATTERN_A_MONITORING` / `NATIONAL_STAGING_PREP` / `OPERATIONAL_PREP`. سه دلیلِ اندازه‌گیری‌شده:\n+(الف) دور ۱۵ **آزمود**: نوشتنِ نامِ این‌ها در گزارش ⇒ **+۳ ارجاعِ کهنه** (۴۱ → ۴۴)؛ پس از اصلاحِ نگارش دوباره ۴۱.\n+(ب) در mainِ امروز هم ردپای همان الگو هست: `2026-09-13.md` به سندِ موقتِ سوخته‌ای به‌نامِ «PROBE_TMP» ارجاع می‌دهد که دیگر وجود ندارد ⇒ `docs-refs-check` روی درختِ نهاییِ این دور: **۱۱۳ کهنه در ۶۵ سند** = baseline ۷۵ + تازه ۴۱ (+۵ تولیدیِ ردشده) که **هیچ‌کدام از الحاقِ من نیست**.\n+(ج) **حکم ۳ِ خودِ بریف** ایجاب می‌کند هر سندِ تازه ارجاعِ ورودی بگیرد یا در `tools/docs-refs-baseline.json` ثبت شود؛ فایلِ سوزنی هیچ‌کدام را ندارد و «ثبتِ بدهی» برای سه فایلِ ۳۰ دقیقه‌ای بی‌معناست.\n+⇒ محتوای سه فایل در §۱/§۲/§۳ همین بخش است: **بدونِ بدهی، بدونِ تکرار، بدونِ سندِ بادکرده**.\n+\n+### §۵) ترمیمِ دومِ سندباکس + وضعیتِ ادغام (این دور «بازیافت» بود، نه «ساخت»)\n+\n+- ابتدای دور: `HEAD` روی `0a208f8` (پایۀ نشست) ریست شده بود + ۵۲ فایل «M» و ۸۴ فایل untracked (نسخه‌هایِ جدیدِ main روی دیسک). **بازیافت:** `sha256` فایلِ دیسک = `096881ec…` == `sha256(blobِ 0ef89e6)` ⇒ **صفرِ محتوایِ از‌دست‌رفته** (کارِ دور ۱۵ سالم بود چون **push** شده بود)؛ سپس `git reset --hard ab933c1` ⇒ فایل = **۱۸۸ سطر** (۴۸ + ۱۲۹ من + ۱۱ دور ۶).\n+- **PR #211 (این شاخه → main): `mergeable = CONFLICTING` · `mergeStateStatus = DIRTY`** · `additions 1027 / deletions 0 / 2 files` · `headRefOid = ab933c1`. main از `bb5068b` **۱۰ کامیت** جلوتر است (`gh api …/compare/bb5068b...5a175c0 ⇒ ahead_by=10, behind_by=0`) و هر دو سو به انتهای همین فایل الحاق کرده‌اند ⇒ **تعارضِ EOF**، نه تداخلِ معنایی.\n+- **rebase نکردم (عمداً):** tipِ شاخه کامیتِ نشستِ دیگر (`ab933c1` = دور ۶ چت ۹) را هم حمل می‌کند؛ بازنویسیِ آن = لمسِ کارِ دیگری. اصلاحِ پیشنهادی برای **صاحبِ ادغام**: `git rebase origin/main arena/01a09a73-p2` (هر سه بخش append-only‌اند، پس تعارض فقط «هر دو را نگه‌دار» است).\n+- یادداشتِ بی‌ضرر ولی لازم: در همان ۱۱ سطرِ دور ۶ دو **جای‌خالی** مانده: «SHA در گزارشِ پایانِ نشست» و «بعد: پایانِ نشست — gc ملایم» — مالکیتِ اصلاح با همان نشست؛ من در الحاقِ خودم **۰ جای‌خالی** گذاشتم (`grep` در §۸).\n+\n+### §۶) ENGINEERING CLAIM\n+\n+- **EXPECTED BEHAVIOR:** پایشِ تصمیمِ الگوی A · آماده‌سازیِ بستهٔ ۱ (سنجه‌پذیری + پیش‌نیازها) · **idle** تا تصمیم\n+- **MEASURED BEHAVIOR:** **۰ فایلِ تازه** (قالبِ بریف «3 docs موقت ساخته شد» را از پیش پر کرده بود — **انجام نشد**؛ نوشتنِ آن در گزارش = جعلِ سبز) · ۰ کد · ۰ تست · **۲۶** فرمانِ سنجشی، همه با `timeout` · **۵** انحرافِ بریف ثبت‌شده (§۰) · **۱** بازیافتِ وضعیتِ شاخه (§۵) · **۷** SLO به نام‌هایِ واقعیِ کد نگاشت شد (§۲) · **۶** پیش‌نیاز با عدد (§۳)\n+- **TEST ENVIRONMENT:** n/a (فقط پایش/مستندسازی؛ هیچ سرویسی اجرا نشد) · **DATASET SIZE / CONCURRENCY / P50·P95·P99 / ERROR RATE:** n/a — اجرای بار **NOT-RUN** (§۳)\n+- **DB IMPACT:** صفر نوشتن (نه seed، نه `--plan` — ضدتکرارِ دور ۱۵) · **CACHE IMPACT:** n/a\n+- **SECURITY IMPACT:** ۰ توکن در فایل/argv/کامیت/گزارش · `secret-scan` **12/12** · ۶ PAT فاش‌شده، **revoke توسط مالک** · هیچ PR-review باتی trigger نشد (strix سقف ۵۰ = billing)\n+- **FAILURE TEST:** NOT-RUN (صفر تغییرِ کد ⇒ موضوعِ تست نبود؛ §۳ دلیلِ عدمِ اجرای load را با عدد می‌دهد) · **RECOVERY RESULT:** `096881ec…` == بلاوبِ `0ef89e6` ⇒ صفرِ محتوایِ از‌دست‌رفته؛ شاخه به tipِ درست (`ab933c1`) منتقل شد\n+- **REGRESSION RISK:** صفر تغییرِ کد/تست — فقط یک الحاقِ متنی به گزارشِ روزانه\n+- **KNOWN UNKNOWNs:** CI **NOT-RUN** (بیلینگ، RISK-O-007) · تصمیمِ الگوی A **NOT-DECIDED** · «Devin: 3 flags» قابل مشاهده در این سندباکس نیست ⇒ **NOT-RUN** · عددِ ۱۴۰GB **مشتق** · `payesh_cache_hit_rate` در کد تعریف نشده · PR #211 **CONFLICTING**\n+- **FINAL VERDICT:** **NOT-RUN** (تصمیم اعلام نشده؛ بستهٔ ۱ **NOT-VERIFIED**) + **VERIFIED** فقط برای «سنجه‌پذیریِ معیارها و نردبانِ پیش‌نیاز» (§۲/§۳ — PASS نیست، چون هیچ تستی اجرا نشد)\n+\n+### §۷) گیتِ ۸ پرسش (پاسخ‌هایِ پیش‌فرضِ بریف، جای خود را به اندازۀ واقعی دادند)\n+\n+1. **چه چیزی تغییر کرد؟** فقط `docs/daily-reports/2026-09-14.md` (+95 سطرِ الحاقی) — **۰** سندِ تازه، **۰** کد، **۰** حذف\n+2. **چرا؟** بستنِ دو خلأی که اجرای بستهٔ ۱ را بلوکه می‌کند: نام‌هایِ سنجه در برابرِ کد (§۲) و عددِ پیش‌نیازهای محیط (§۳)؛ + پایشِ رسمی با گیتِ خودِ بریف (§۱)\n+3. **چه invariant باید حفظ شود؟** بستهٔ ۱ قفل تا **ACCEPTِ صریح**؛ گزارش فقط append-only؛ **۰** rc bump؛ **۰** سندِ بادکرده؛ سندباکس بدونِ نوشتنِ بار\n+4. **چگونه ثابت می‌کنیم درست است؟** هر عدد در §۰–§۵ یا `file:line` دارد یا خروجیِ فرمانِ اجراشده (grep/gh api/free/df)؛ بازیافتِ §۵ با **sha256** سنجیده شد، نه با فرض\n+5. **سریع‌تر/مقیاس‌پذیرتر؟** n/a (صفر اجرای بار — §۳)\n+6. **امن؟** ۰ توکن + `secret-scan 12/12` + بدونِ triggerِ بات‌ها + بدونِ ویرایشِ فایل‌هایِ دیگران\n+7. **failure؟** NOT-RUN با دلیل (صفر کد)؛ و §۳ نشان می‌دهد پیش از تصمیم، حتی با اراده هم غیرقابل‌اجرا بود\n+8. **rollback؟** فرمانِ `git revert` روی SHAِ همین کامیت (SHA پس از commit در `git log -1` و `ls-remote` خوانده می‌شود؛ خودارجاع درونِ فایل ممکن نیست) — چون الحاقِ خالص است، revert بدونِ درگیریِ بخش‌هایِ چت‌های ۳/۵/۸ اعمال می‌شود\n+\n+### §۸) گیت‌ها · بودجه · پوش · idle\n+\n+- **گیت‌های مسیرِ تغییر (یک‌بار، روی درختِ نهایی، همه `timeout`دار):** `docs-stats-sync --check` ✅ (آمار == دیسک؛ **rc43**، بدونِ bump) · `tests/docs-freeze-marker.js` **14/14** · `tests/docs-metadata.js` **17/17** · یتیم **۰** · `docs-refs-check` **۴۱ کهنه — صفرِ تازه از این دور** · `tests/secret-scan.js` **12/12** · `reza-mirror-check` ✅ **۰ drift / ۰ آینهٔ تازه / ۰ cp دستی** · **۰** بازاجرای سوئیت استیجینگ، **۰** بازاجرای گیت‌های دور ۱۵، **۰** manifest regen\n+- **جای‌خالی در الحاقِ من:** جست‌وجوی نشانه‌هایِ جای‌خالیِ متنی (double-at، «to‑do»، «fix‑me»، angle-bracket-SHA، double-brace) روی 95 سطر ⇒ **۰** (جای‌خالی‌هایِ §۵ مربوط به فایلِ دیگری است و دست نزدم)\n+- **بودجۀ §۱ (معیارِ مؤثرِ `WORKSPACE_HYGIENE`، بدونِ `node_modules`/`.cache`):** پیش **100.6 MB / 1551 فایل** → پس **102.3 MB / 1553 فایل** 🟢 · دلتایِ کاریِ من = **12 534 B (12.3 KB) در 95 سطرِ یک فایلِ موجود** — **0 فایلِ تازه** · بخشِ عمدهٔ دلتا (+≈1.7 MB / +2 فایل) از **`.git`** است (امروز 69 MB، پس از دو `git fetch` برای tagها و tipِ شاخه) و نه از محتوا؛ اندازۀ تمیزِ محتوا بدونِ `.git`: **33.93 MB / 1448 فایل**. محدودیتِ شفاف: اندازۀ `.git` در ابتدای دور جداگانه گرفته **نشد**، پس تفکیکِ «۱.۷MB = .git» استنتاجِ زمان‌بندیِ فرمان‌هاست، نه تفکیکِ اندازه‌گیری‌شده.\n+- **پوش:** این فایل **درونِ کامیت** است و SHA خودش را نمی‌تواند بیاورد (محدودیتِ ذاتیِ خودارجاعی) ⇒ زنجیره: `5a175c0 (main) ← bb5068b ← 4c233af (احیای دور ۵) ← 0ef89e6 (دور ۱۵) ← ab933c1 (دور ۶، نشستِ دیگر) ← این کامیت`؛ تأییدِ بیرونی پس از push در `git ls-remote` و به‌روزرسانیِ خودکارِ PR #211 ثبت می‌شود\n+- **idle رسمی (چت ۹):** تا یکی از این‌ها: **ACCEPT/ردِ صریحِ الگوی A** · **حکمِ ناظر** · **بریفِ تازه**. تنها پیگیریِ مجاز: تازهِ‌شدنِ همان تصمیم + **رفعِ تعارضِ PR #211** (مالک: صاحبِ ادغام). با ACCEPT، اجرای بستهٔ ۱ از §۲ (فرمول‌هایِ سنجه) و §۳ (پیش‌نیازهایِ برآورده‌نشده) شروع می‌شود — نه از صفر.\n+\n+---\n+\n+## چت ۹ — دور ۱: پایش وضعیت تصمیم الزامات پایش + اعلام idle (ثبت: 2026-09-14)\n+\n+**انحرافِ شمارۀ دور/نقش (ثبت، نه اصلاحِ خاموش):** بریف این کار را «چت ۹ — دور ۱» و «بیست‌وپنجمین دور idle» نامیده، اما پروندۀ **همین نشست** روی همین شاخه تا «دور ۱۶» پیش رفته بود (`0ef89e6` → `ab933c1` → `a3f0b59`) و نقشِ دو دورِ قبل «چت ۸/بستهٔ ۱» بود؛ شمارشِ اعلام‌هایِ صریحِ idle در دو گزارشِ روزانه = **۹**، و رشتهٔ «بیست‌وپنجم» فقط در نامِ فایل `DOCS_FREEZE_v1.0.0-rc25.md`/`DOCS_INDEX.md` می‌آید (**rc25**، نه شمارۀ idle) — دقیقاً همان انحرافی که در دور ۱۶ سنجیده و ثبت شد و بریف دوباره تکرار کرده.\n+\n+### §۰) بریف در برابرِ واقعیت (هفت انحراف، همگی اندازه‌گیری‌شده بر `main = 895f8d4`)\n+\n+| # | ادعای بریف | واقعیت (شاهد) |\n+|---|---|---|\n+| ۱ | گیتِ فاز ۰.۵ = `gh api …/contents/DECISION_LOG.md` در پوشۀ `docs` + `grep -q \"الزامات پایش\"` | فایل **وجود ندارد**: GitHub Contents API ⇒ `{\"status\":\"Not Found\"}` و `git ls-tree origin/main` روی نامِ فایل ⇒ = **۰** ⇒ گیتِ بریف **از نظرِ ساختاری هرگز سبز نمی‌شود**. ثبت‌هایِ واقعیِ تصمیم: `docs/SUPERVISOR_DECISIONS_2026-09-13.md` و `docs/ARCHITECTURE_DECISIONS.md` |\n+| ۲ | «تصمیم الزامات پایش اعلام نشده» | درست، ولی **به دلیلِ ضعیف‌تری**: `git grep -c \"الزامات پایش\" origin/main` = **۰ در کل مخزن** (نه فقط در DECISION_LOG). محتوایِ الزامات **بی‌صاحب نیست**: ۶ سندِ پایش روی main است (`OBSERVABILITY.md`, `OBSERVABILITY_DEPLOYMENT.md`, `OBSERVABILITY_LIVE_SETUP.md`, `WAVE14_OBSERVABILITY.md`, `REDIS_MONITORING_ALERTING.md`, `ONCALL_SCHEDULE.md`) ⇒ چیزی که اعلام‌نشده **حکمِ ACCEPT** است، نه نبودِ متن |\n+| ۳ | «سناریوهای آزمایشی در `rto-rpo-summary.csv` وجود دارند» | **چنین فایلی نیست**؛ تنها CSV های مخزن: `docs/samples/students_sample.csv` و `teachers_sample.csv`. رشته‌های `S1-api-kill`/`S6-disk-pressure`/`S10-clock` = **۰ hit**. جایگزینِ واقعی (بخشِ §۳) = خانوادهٔ دریل‌ها |\n+| ۴ | «۵ شکاف؛ همگی نیازمند تغییرِ کد» | **۲ تأیید، ۲ رد، ۱ نیمه** — با عدد در §۱ و §۲ |\n+| ۵ | «آلارم‌های معنادار تعریف نشده‌اند» | **رد**: **۳۴** قانون آلارم موجود است (**۲۴** در `infra/observability/alerts.yml` + **۱۰** در `infra/observability/alert-rules.yml`) |\n+| ۶ | «داشبوردهای مانیتورینگ تعریف نشده‌اند» | **رد**: `infra/observability/dashboards/payesh-main.json` (۱۰ پنلِ timeseries) + `infra/observability/payesh-dashboard.json` (۱۲) + `dashboards/payesh-logs.json` + provisioning (۴ ارجاعِ dashboard)؛ و **۰** پنل به سنجهٔ ناموجود `payesh_cache_hit_rate` ارجاع می‌دهد ⇒ پنلِ شکسته نیست |\n+| ۷ | «ادغام با on-call انجام نشده» | **نیمه**: `alertmanager.yml:5-19` route + تشدیدِ `severity=\"critical\"` (group_wait 10s، repeat 1h) واقعی است، ولی تنها receiver = `payesh-webhook` با `url: \"__WEBHOOK_URL__\"` (جای‌خالی) و receiver ایمیل در `:21-26` **کامنت** است؛ `docs/ONCALL_SCHEDULE.md:5` هم خودش را «مصوبِ پیشنهادی — انتسابِ واقعی…» می‌نامد ⇒ سندِ چرخش هست، **کانالِ زنده** نیست |\n+\n+### §۱) تصمیم الزامات پایش — **NOT-DECIDED** (گیتِ خودِ بریف + سه شاهدِ متقاطع)\n+\n+- فرمانِ بریف، عیناً: `timeout 30 gh api repos/rezaa2544/p2/contents/…` برای `DECISION_LOG.md` (پوشۀ `docs`) ⇒ **404 Not Found** (پس `jq -r .content | base64 -d` روی هیچ اجرا می‌شود ⇒ `grep -q` هرگز true نمی‌شود) — **ثبت می‌کنم که شرطِ فاز ۰.۵ عملاً غیرقابل‌تحقق است، نه اینکه «تصمیم منفی» است**\n+- `git grep -c \"الزامات پایش\" origin/main` ⇒ **۰** · `NEXT_ACTIONS.md` (سرِ فایل، سطر ۱–۱۲): P0-1..P0-6 + «صفِ ۱۳ PRِ بلاک‌شده با ACCEPT صریح…» ⇒ هیچ سطرِ «الزامات پایش» ندارد\n+- `docs/SUPERVISOR_DECISIONS_2026-09-13.md` (آخرین ثبت‌ها) ⇒ آخرین تصمیمِ امضاشده دربارهٔ «پایش تریگرِ بنر `data-rpt-partial-shell` + کانترهای /metrics + سقف ردیف» است (۲۰۲۶-۰۹-۱۳) — **نزدیک‌ترین تصمیمِ موجود**، ولی دقیقاً همان «الزامات پایش» بستهٔ ۹ نیست\n+- `gh api repos/…/issues/184` (فرمانِ تصحیح‌شدۀ دور ۱۶ — #184 یک **PR** است نه issue) ⇒ ۰ کامنت شاملِ «الزامات پایش»؛ PR بازها: فقط **#211 (همین شاخه)**؛ `ls-remote main` در لحظۀ بستن = `0360b37` (یعنی main حینِ همین دور هم ۱ مرج گرفت: `895f8d4` → `0360b37`)\n+⇒ **فاز ۰.۵ اجرا نشد**؛ بستهٔ ۹ = **NOT-VERIFIED / اجرای واقعی NOT-RUN**؛ **۰** کار روی کد/تست (حکم ۲).\n+\n+### §۲) ارزش افزودهٔ این دور: **آینۀ SLO ↔ آلارم** (هر ردیف با `file:line` — پایهٔ acceptance bundle واقعی)\n+\n+| SLO (`docs/CAPACITY_MODEL.md` §۵) | آلارم/آینۀ موجود | وضعیتِ اندازه‌گیری‌شده |\n+|---|---|---|\n+| p50 < 100ms | — | ❌ **هیچ آلارمی** (`grep -F 'histogram_quantile(0.5'` در هر دو فایل = ۰) |\n+| p95 < 300ms | `PayeshLatencySlobreach` (`alerts.yml:28`) · `HighLatency` (`alert-rules.yml:19`) | ✅ |\n+| p99 < 1s | `PayeshLatencyP99Breach` (`alerts.yml:38`) | ✅ |\n+| 5xx < 0.1٪ | `PayeshHighServerErrorRate` (`:17`) expr `> 0.01` ⇒ **۱٪** = ۱۰× شل‌تر از SLO · `CriticalErrorBudgetBurn` (`:48`) expr `14.4 * 0.0005` ⇒ بودجهٔ **۰.۰۵٪** (= نصفِ ۰.۱٪ سند) | ⚠ **ناسازگاریِ آستانه در دو جهتِ مخالف** در یک فایل؛ مرجعِ بودجه: `docs/WAVE14_OBSERVABILITY.md:174` (Availability 99.95٪ با PromQL — ولی **آلارم ندارد**) |\n+| کش > 80٪ | `PayeshCacheHitRateLow` (`:137`) = `sum(rate(payesh_cache_lookups_total{outcome=\"hit\"}[15m])) / clamp_min(sum(rate(payesh_cache_lookups_total[15m])),1e-9) < 0.8` | ✅ — و **تصحیحِ خودم**: فرمولِ پیشنهادیِ من در دور ۱۶ (`hits_total / lookups_total`) با قراردادِ واقعیِ مخزن فرق دارد؛ `payesh_cache_hits_total` فقط «compat alias of l1Stats» است (`server/metrics.js:514`) و مرجعِ درست، برچسبِ `outcome` روی lookups است |\n+| sync ≥ 99.5٪ | `PayeshSyncConflictSpike` (`:175`) · `PayeshOutboxBacklog` (`:159`) · `SyncQueueDepth` (`alert-rules.yml:42`) | ⚠ **نسبتِ موفقیت آلارم ندارد**؛ سه آلارمِ موجود عمق/تعارض را می‌گیرند نه ۹۹.۵٪ |\n+| OTP ≥ 99.9٪ | `PayeshOtpFlood` (`:225`) · `PayeshLoginAbuse` (`:214`) | ❌ این دو **امنیتی**‌اند نه SLO؛ آلارمِ دسترس‌پذیریِ OTP **نیست** (سنجهٔ `payesh_auth_otp_requests_total{outcome}` در `metrics.js:438,713` موجود ⇒ فقط قانون کم است) |\n+⇒ **جمع‌بندی: ۳ از ۷ کامل · ۲ نیمه (عمق/تعارض به‌جای نسبت) · ۲ فاقد آلارم (p50، OTP-avail) · ۱ با آستانۀ ناسازگار (5xx)**؛ و **۰ `record:` rule** در هر دو فایل ⇒ «تحلیل روند» عملاً فقط `AnomalyDetected` (`alert-rules.yml:65` روی `payesh_runtime_anomalies_total`) است، نه rollup های SLO.\n+\n+### §۳) نردبانِ ۷ پیش‌نیازِ بریف + نگاشتِ سناریوها (به‌جای `rto-rpo-summary.csv`ِ غیروجود)\n+\n+پیش‌نیازها: ۱) پایش متمرکز ⇒ **موجود** (`prometheus.yml` + `docker-compose.observability.yml` + `loki-config.yml` + `promtail.yml` + `otelcol-config.yml`) ولی `command -v docker` ⇒ **ABSENT** پس **اجرا NOT-RUN** · ۲) داشبورد ⇒ **۲ داشبورد / ۲۲ پنل** ✅ · ۳) آلارم ⇒ **۳۴ قانون** ✅ (با ۴ خلأ در §۲) · ۴) on-call ⇒ ⚠ webhookِ placeholder · ۵) گزارش خودکار ⇒ ❌ **۰** ابزارِ تولیدِ گزارشِ عملیاتیِ خودکار (`ls-tree tools` تنها `bench-reports-*` و `mutate-reports-check.sh` را داد که بنچ‌اند/تغییر‌سنج‌اند) · ۶) تحلیل روند ⇒ ⚠ **۰ `record:`**؛ فقط anomaly · ۷) مستندات ⇒ ✅ **۶ سندِ پایش** (§۰/۲).\n+\n+نگاشتِ سناریوهایِ بریف به فایل‌هایِ واقعی (سنجیده با `ls-tree`/`git grep`): `S1→tests/chaos-drill-api-kill.js` · `S2→chaos-drill-redis-outage.js` · `S3→chaos-drill-pg-outage.js` · `S4→chaos-drill-queue-outage.js` · `S5→chaos-drill-network-degradation.js` · `S6→chaos-drill-disk-pressure.js` · `S7→tests/sync-conflict-storm-drill.js` ⇒ **۷ از ۱۰ فایلِ اختصاصی دارد**؛ `S8 partial-failure` / `S9 retry-storm` / `S10 clock-storm` ⇒ **۰ فایلِ اختصاصی** (پوششِ پراکنده در `tests/offline-sync-drill.js`، `tests/server1.js`، `tests/server17.js`، و همان `sync-conflict-storm-drill.js` برای retry) — یعنی ادعای «۱۰ سناریو در CSV» **نه فایلش هست نه شماره‌گذاریش**.\n+\n+دو شکافِ «نیازمند تغییرِ کد» از بریف، با حکمِ نهایی: **(الف) گیج فشار دیسک ⇒ تأیید شد** (`grep -icE 'disk|filesystem' server/metrics.js` = **۰** و **۰** node_exporter/cadvisor/process-exporter در `infra/observability` ⇒ هیچ منبعِ متریکِ دیسک نیست؛ دریلِ S6 هم برون‌بانده: `tests/chaos-drill-disk-pressure.js:41` از `infra.diskFreeKb()` (= `df`) استفاده می‌کند و کمکی‌اش در `tests/chaos-drill-lib.js:364` است؛ نکته: `chaos-drill-lib.js:265` می‌تواند tmpfs بزند ولی `id -u` = **1001** ⇒ اینجا نشدنی) — راه‌حلِ کم‌هزینه **node_exporter**، نه کدِ اپ. **(ب) گیج صفِ per-instance ⇒ نیمه**: `payesh_sync_queue_depth` هست (`metrics.js:513`، برچسب `[]`) و Prometheus با `instance` تفکیک می‌کند، اما `prometheus.yml:29-33` برچسبِ `instance_role: secondary` را **کامنت** گذاشته ⇒ گلوگاه، **config** است نه کد؛ و برای صفِ **درخواست‌هایِ HTTP** واقعاً هیچ گیجی نیست (تنها یک نامِ `*queue*` در metrics.js).\n+\n+### §۴) سه فایل موقت — ساخته **نشدند** (و بریف این دور حتی در عددِ خودش ناسازگار است)\n+\n+بریف فاز ۱ سه فایل می‌خواهد (`MONITORING_DECISION_MONITORING.md`, `OPERATIONAL_MONITORING_PREP.md`, `OPERATIONAL_PREP.md`) اما acceptance bundle‌اش «**FILES: 2 docs موقت**» می‌گوید ⇒ **۲ یا ۳؟** در خودِ بریف مشخص نیست. سه دلیلِ رد (پیشینۀ اندازه‌گیری‌شده): (الف) دور ۱۵ آزمود: آوردنِ این نام‌ها در گزارش ⇒ **+۳ ارجاعِ کهنه**؛ (ب) `docs-refs-check` روی درختِ امروز: **۱۱۳ کهنه در ۶۵ سند** = baseline ۷۵ + ۴۱ِ پیش‌ازین (هیچ‌کدام از من) ⇒ بدهیِ همین الگو در main دیدنی است؛ (ج) **حکم ۳ِ خودِ بریف** (سندِ تازه ← ارجاعِ ورودی یا `tools/docs-refs-baseline.json`) با فایلِ ۲۰دقیقه‌ایِ سوزنی قابل‌رضایت نیست. محتوا در §۱–§۳ همین بخش است. **۰ فایلِ تازه**، پس چیزی برای «حذف در پایانِ دور» هم نیست.\n+\n+### §۵) وضعیتِ شاخه + **ریشهٔ تکرارِ «کامیتِ گمشده»** (یافتِ این دور)\n+\n+- ابتدای دور: `HEAD` دوباره روی `0a208f8` بود و `git fetch origin --prune` شاخۀ نشست را **نمی‌آورد**. دلیل، اندازه‌گیری‌شد: `git config --get-all remote.origin.fetch` = `+refs/heads/main:refs/remotes/origin/main` **تنها** (و `.git/shallow` موجود) ⇒ هر fetch معمولی، tipِ `arena/01a09a73-p2` را هرگز update نمی‌کند؛ این همان علتِ سه «بازیافتِ متوالی» (دور ۵، ۱۵، ۱۶) است، نه باگِ نگاریِ گزارش‌ها.\n+- اقدامِ اصلاحیِ محلی (بدونِ لمسِ فایلِ مخزنی): افزودنِ refspec دوم برای همین شاخه ⇒ از این پس fetch معمولی، tip را می‌آورد؛ **هیچ فایلِ ردیابی‌شده‌ای تغییر نکرد** (`git status` پس از reset = ۰).\n+- وضعیتِ remote: tipِ شاخه = `a3f0b59` (دور ۱۶ من) — نشستِ دیگری پس از دور ۱۶ چیزی push نکرده؛ **PR #211: OPEN · `mergeable = CONFLICTING`** (main در این فاصله `5a175c0 → aeb32a9 → 895f8d4 → 0360b37`)؛ اصلاح با **صاحبِ ادغام**: `git rebase origin/main arena/01a09a73-p2` (هر سه بخش append-only؛ تعارضِ EOF با «keep both» حل می‌شود). من rebase نکردم: tip، کامیتِ نشستِ دیگر (`ab933c1` = دور ۶) را هم حمل می‌کند.\n+\n+### §۶) ENGINEERING CLAIM\n+\n+- **EXPECTED BEHAVIOR:** پایشِ تصمیمِ «الزامات پایش» · آماده‌سازیِ بستهٔ ۹ (Operational Monitoring) · **idle** تا اعلام تصمیم\n+- **MEASURED BEHAVIOR:** **۰ فایلِ تازه** (قالبِ بریف «3 docs موقت ساخته شد» را از پیش پر کرده بود — **انجام نشد**؛ نوشتنش = جعلِ سبز) · **۰ کد** · **۰ تست** · **۳۸** فرمانِ سنجشی، همه با `timeout` · **۷** انحرافِ بریف (§۰) · **۳۴** قانون آلارم و **۲۲** پنلِ داشبورد شمارش و به ۷ SLO نگاشت شد (§۲) · **۱** ریشه‌یابیِ علتِ «کامیتِ گمشده» + اصلاحِ محلیِ refspec (§۵)\n+- **TEST ENVIRONMENT:** n/a (هیچ سرویسی بالا نیامد؛ `docker: ABSENT`) · **DATASET SIZE / CONCURRENCY / P50·P95·P99 / ERROR RATE:** n/a — اجرای بار و دریل‌ها **NOT-RUN** (بستهٔ ۹ ≠ مجوزِ اجرای دریل)\n+- **DB IMPACT:** صفر نوشتن (بدونِ seed، بدونِ --plan، بدونِ دریل) · **CACHE IMPACT:** n/a\n+- **SECURITY IMPACT:** ۰ توکن در فایل/argv/کامیت/گزارش · `secret-scan` **12/12** · هیچ رازِ تازه‌ای در `alertmanager.yml` ثبت نشد (خودِ فایل هم smtp auth را از env می‌خواهد و من کامنتش را هم بازنویسی نکردم) · ۶ PAT فاش‌شده، **revoke توسط مالک** · بات‌های review trigger نشدند\n+- **FAILURE TEST:** NOT-RUN (با دلیل: اجرای دریل‌ها نیازمندِ تصمیم + root/tmpfs + docker است؛ §۳) · **RECOVERY RESULT:** tipِ واقعی با fetchِ refspecِ صریح بازیابی شد؛ `git reset --hard origin/arena/01a09a73-p2` ⇒ ۲۸۳ سطر در `2026-09-14.md`، `git status` = **۰**\n+- **REGRESSION RISK:** صفر تغییرِ کد/تست — فقط یک الحاقِ متنی\n+- **KNOWN UNKNOWNs:** CI **NOT-RUN** (بیلینگ، RISK-O-007) · تصمیمِ «الزامات پایش» **NOT-DECIDED** (و گیتِ بریف به فایلِ ناموجود بسته شده) · آیا `payesh_sync_queue_depth` در پروداکشن چند‌نود به‌تنهایی کافی است: **نمی‌دانم** (نیازمند اجرای stack) · آلارمِ p50/OTP-avail عمداً حذف شده یا فراموش: **در هیچ سندی ثبت نشده** · ظرفیت ملی اثبات‌نشده (P0)\n+- **FINAL VERDICT:** **NOT-RUN** (تصمیم اعلام نشده؛ بستهٔ ۹ **NOT-VERIFIED**) + **VERIFIED** فقط برای «آینۀ SLO↔آلارم و شمارشِ دارایی‌های پایش» (§۲/§۳) — این PASS نیست، چون هیچ آلارمی شلیک نشده است\n+\n+### §۷) گیتِ ۸ پرسش\n+\n+1. **چه چیزی تغییر کرد؟** فقط `docs/daily-reports/2026-09-14.md` (الحاق؛ **+۰ فایلِ سند، +۰ کد، −۰ خط**)\n+2. **چرا؟** تصمیمِ الزامات پایش هنوز اعلام نشده، ولی خلأهایِ واقعیِ بستهٔ ۹ تا پیش از آن باید **اندازه** می‌شدند تا ACCEPT معنادار باشد: کدام SLO آلارم دارد و کدام نه (§۲)\n+3. **چه invariant باید حفظ شود؟** بستهٔ ۹ قفل تا حکمِ صریح · گزارش فقط append-only · ۰ rc bump · ۰ سندِ بادکرده · هیچ فایلِ مخزنی جز گزارش تغییر نکرد (و `.git/config` محلی = تنها تغییرِ غیرمخزنی، ثبت‌شده در §۵)\n+4. **چگونه ثابت می‌کنیم درست است؟** هر عدد در §۰–§۵ یا `file:line` دارد (`alerts.yml:17/28/38/48/137/159/175/225`، `metrics.js:513/514/438`، `prometheus.yml:29-33`) یا خروجیِ فرمانِ اجراشده (`gh api` 404، `git grep -c`، `ls-tree`، `docker`، `id -u`)\n+5. **سریع‌تر/مقیاس‌پذیرتر؟** n/a — صفر اجرای بار؛ و یک بهبــودِ کم‌هزینه که از همین داده می‌آید: افزودنِ `node_exporter` به‌جای نوشتنِ گیج دیسک در اپ (§۳)\n+6. **امن؟** ۰ توکن + `secret-scan 12/12` + بدونِ triggerِ بات + بدونِ ویرایشِ فایل‌هایِ دیگران/بقیۀ چت‌ها\n+7. **failure را چگونه تست می‌کنیم؟** NOT-RUN با دلیل؛ سناریوهایِ S1–S7 آماده‌اند (فایل‌هایِ chaos-drill) ولی اجرا **محظوظِ** تصمیم + docker + root/tmpfs\n+8. **rollback؟** `git revert` روی SHAِ همین کامیت (الحاقِ خالص ⇒ بدونِ درگیریِ بخش‌هایِ چت ۳/۵/۸ روی همین فایل)\n+\n+### §۸) گیت‌ها · بودجه · پوش · idle\n+\n+- **گیت‌های مسیرِ تغییر (یک‌بار روی درختِ نهایی، `timeout`دار):** `docs-stats-sync --check` ✅ (۴۰۶ سند = ۳۴۸ ریشه + ۵۸ زیرپوشه · قفل **rc43**، **۰ bump**) · `tests/docs-freeze-marker.js` **14/14** · `tests/docs-metadata.js` **17/17** · یتیم **۰** · `tools/docs-refs-check.js` **۴۱ کهنه = ۰ِ تازه از این دور** · `tests/secret-scan.js` **12/12** · `tools/reza-mirror-check.js` ✅ **۰ drift / ۰ آینهٔ تازه / ۰ cp دستی** · **۰** بازاجرای دورهایِ قبل، **۰** بازاجرای سوئیت پایش/دریل‌ها، **۰** manifest regen\n+- **جای‌خالی در الحاقِ من:** جست‌وجوی نشانه‌هایِ جای‌خالیِ متنی (double-at، «to‑do»، «fix‑me»، angle-bracket، double-brace) ⇒ **۰**\n+- **بودجۀ §۱ (دو اندازه‌گیریِ مستقل، بدونِ `node_modules`/`.cache`):** پیش **100.64 MB / 1549 فایل** (که 68.4 MB آن `.git` است؛ بدونِ `.git` = 32.23 MB) → پس **100.66 MB / 1549 فایل** (بدونِ `.git` = 32.25 MB) 🟢 — دلتا = **14 951 B / 88 سطر** در **یک فایلِ موجود**، شمارندۀ فایل **ثابت (1549 → 1549)** ⇒ **۰ فایلِ تازه** و `.git` هم رشد نکرد (68.4 → 68.4 MB، چون این دور فقط **یک** fetch با refspecِ صریح انجام شد)\n+- **پوش:** SHAِ این کامیت درونِ خودش قابل‌نوشتنیست (محدودیتِ خودارجاعی)؛ زنجیره: `bb5068b ← 4c233af (احیای دور ۵) ← 0ef89e6 (دور ۱۵) ← ab933c1 (دور ۶، نشستِ دیگر) ← a3f0b59 (دور ۱۶) ← این کامیت`؛ تأییدِ بیرونی = `git ls-remote` و به‌روزرسانیِ PR #211 (پس از push در پیامِ نشست ثبت می‌شود)\n+- **idle رسمی (چت ۹ — بستهٔ ۹):** تا یکی از: **ACCEPT/ردِ صریحِ «الزامات پایش»** (در `SUPERVISOR_DECISIONS` یا جایِ مصوبِ دیگر — نه `DECISION_LOG` که وجود ندارد) · **حکمِ ناظر** · **بریفِ تازه**. پیگیریِ مجاز: تازهِ‌شدنِ همان تصمیم + **رفعِ تعارضِ PR #211**. با ACCEPT، کار از §۲ شروع می‌شود (۴ خلأ: آلارمِ p50، آلارمِ OTP-avail، آلارمِ نسبتِ موفقیت sync، هم‌راستاسازیِ آستانۀ 5xx با 0.1٪) + §۳ (منبعِ دیسک با node_exporter، بازکردنِ `instance_role: secondary`، کانالِ واقعیِ alertmanager) — **نه** از صفر، و **بدونِ** بازساختنِ چیزی که §۲ نشان می‌دهد از قبل موجود است.\n+\n+---\n+\n+## چت ۹ — دور ۱۰: پایش تصمیم «الزامات پایش» + provenanceِ اعدادِ S1–S10 (ثبت: 2026-09-14)\n+\n+**انحرافِ شمارۀ دور/شاخه (ثبت، نه اصلاحِ خاموش):** بریف می‌گوید «مبنا: شاخۀ `docs/daily-reports-2026-09-14` @ [SHA کامیت دور ۱]» و عنوانِ بخش «دور ۱۰» است، درحالی‌که **شاخۀ نشستِ من** `arena/01a09a73-p2` است و دورِ قبلی‌اش «دور ۱» (کامیت `f3dbdb1`)؛ آن شاخۀ دیگر **PR #218 = «چت ۸ — دور ۱۸»** است (نشستِ دیگری، همین‌جا دست‌نخورده). من روی شاخۀ خودم الحاق می‌کنم؛ شماره‌گذاریِ Coordinator بینِ نشست‌ها هم‌راستا نیست و این سومین دورِ متوالی است که این انحراف را ثبت می‌کنم. «بیست‌وپنجمین دور idle» هم همان ادعایِ ردشدۀ دورهایِ ۱۶ و ۱ است (شمارشِ واقعیِ اعلام‌ها = ۹؛ «۲۵» فقط `rc25` است).\n+\n+### §۰) دروازهٔ تصمیم — **NOT-DECIDED**، و گیتِ این دور هم به فایلِ ناموجود بسته شده\n+\n+- فرمانِ بریف عیناً: `gh api repos/rezaa2544/p2/contents/SUPERVISOR_DECISIONS_2026-09-14.md | jq -r .content | base64 -d | grep -q \"الزامات پایش\"` ⇒ **grep exit=1**؛ دلیلِ اول: آن فایل **وجود ندارد** (`gh api` بدونِ خروجی؛ `git ls-tree origin/main` روی همان نام ⇒ تنها `docs/SUPERVISOR_DECISIONS_2026-09-13.md` = تنها `docs/SUPERVISOR_DECISIONS_2026-09-13.md`) ⇒ دروارۀ بریف برای دومین دورِ متوالی به مسیرِ اشتباه وصل است (دورِ قبل `DECISION_LOG.md`، این دور `SUPERVISOR_DECISIONS_2026-09-14.md`)\n+- `git grep -c \"الزامات پایش\" origin/main` ⇒ **۰ در کلِ main** (نه فقط در فایل‌هایِ تصمیم) ⇒ نه ACCEPT، نه رد؛ **فاز ۰.۵ اجرا نشد**\n+- **ولی** `docs/daily-reports/2026-09-14.md:7` روی main می‌گوید: «**#184 مرج شده و §۶ مصرف شده است** (VERIFIED)» ⇒ یک نشستِ دیگر، «مصرفِ §۶» را VERIFIED اعلام کرده؛ چونِ من **تصمیمِ الزاماتِ پایش نیست** و ادعایِ بیرونی را بازتولید نکردم (بازاجرا ممنوع) — فقط تطبیقِ source می‌دهم در §۲\n+\n+### §۱) provenanceِ عددی که بریف می‌خواهد «بر اساسِ داده‌هایِ واقعی» بنویسم — سه‌لایه، سنجیده\n+\n+1. **خروجیِ ابزارِ ردیابی‌شده:** `tools/chaos-test.sh:53` ⇒ `OUT_DIR=\"tests/chaos-output\"` و فایل‌ها `<scenario>-{before,after}.json` + `<scenario>-timeline.csv` (`:116,:137`)؛ و `.gitignore:47` ⇒ `tests/chaos-output/` **عمداً نادیده‌گرفته** می‌شود. پس هیچ CSVِ دریلی در git نیست و نباید باشد.\n+2. **مسیرِ ارجاع‌شده (`chaos-evidence/rto-rpo-summary.csv`):** در git **۰**؛ در `.gitignore` هم **نیست** (پس نه ignored است نه tracked)؛ و رشتهٔ `chaos-evidence` در **هیچ** اسکریپتِ ردیابی‌شده‌ای (`tests tools src infra scripts`) نوشته نمی‌شود ⇒ **یک پوشۀ ad-hocِ یک اجرایِ دستی، بدونِ بازتولیدِ دستورِ واحد**. (این را چت ۵ هم مستقل دیده: `:75` «`rto-rpo-summary.csv`ِ ارجاع‌شده در پرامپت در ریپو نیست» و `:390` «`chaos-evidence` = ۹ فایل» ⇒ **تطبیقِ دو-منبعی**، نه ادعایِ من.)\n+3. **جایِ ثبتِ درست = `docs/DR_RUNBOOK.md` §۶ (سطر 129، «جدولِ drill-log»):** ردیف‌هایِ موجود = WAL disk-full 2026-09-11 (RTO **30.2ث**، RPO **0**)، بازاجرای 2026-09-13، disk-fullِ سطح‌اپ + kill-api + pg-down، و Queue-Outage — ولی `grep -cE \"376|144|9545|1353|28948\"` روی DR_RUNBOOK = **۰** ⇒ **ردیفِ S1/S2/S3/S4/S5 در register نیست**؛ این عدد‌ها فقط در گزارش‌هایِ روزانه‌اند (`docs/daily-reports/2026-09-14.md`).\n+4. ⚠ **تلهٔ grep:** `9545` و `1353` در git یک hitِ دیگر هم دارند: `tests/performance/fixtures/test-users.json` — **هم‌اندازۀ تصادفی، نه شاهد**. اگر کسی «پیدا شد در مخزن» گزارش کند، گولِ همین fixture را خورده است.\n+\n+### §۲) چرا اعدادِ S1–S6 در بریف نمی‌توانند «به‌روز» خوانده شوند (تعارضِ مستقیم با #185)\n+\n+- `gh pr view 185` ⇒ **MERGED 2026-09-13T20:21:33Z**، کامیت `703f93a` = «رفع سه یافتهٔ chaos-drill — **F1** گم‌شدنِ دادهٔ ack‌شده در قطعِ PG + **F2** 500 در قطعِ Redis + **F3** بازپخشِ صف پس از کرش»\n+- ⇒ ادعایِ بریف «**S3** … RPO: ۳ نوشتِ ackشده گم شد» **پیش‌ازرفع** است (بعدِ F1 = صفر، طبقِ اصلاحِ موردیِ چت ۵ در `:103` که خودش می‌گوید «بدونِ بازاندازه‌گیری»)\n+- ⇒ ادعایِ «**S4** … ۶ رویداد تا ابد pending» هم **با F3 در تضاد است** (بازپخشِ صف پس از کرش = همان چیزی که «تا ابد pending» را می‌بندد)؛ خودِ کد هم شاهد است: `replayPendingFromPg` (`server/index.js:551` · `server/outbox.js:154`) — ارجاعِ چت ۵ در `:367`\n+- ⇒ پس: **S3/S4 RPO = نه مقدارِ بریف، نه عددِ تازه؛ NOT-MEASURED-POST-FIX** و تنها راهِ درست، اجرایِ دوباره + ثبتِ خطِ امضاشده در §۶ است (نه کپی‌کردنِ CSV سوزنی). S6 (دیسک) نزدیک‌ترین به register است: 28 948 ms ≈ ردیفِ «30.2ث» — **ولی با قیدِ صریحِ §۶** که `wal_segment_size=1MB` بوده و عددِ مطلق به تولید تعمیم نمی‌خورد.\n+\n+### §۳) راه‌حل‌هایِ پیشنهادیِ بریف — ۵ مورد: ۲ درست، ۳ با لنگر/نامِ غلط (اندازه‌گیری روی `infra/observability/alerts.yml`)\n+\n+| پیشنهادِ بریف | سنجش |\n+|---|---|\n+| p50 → `PayeshLatencyP50Breach` در `alerts.yml:25` | ❌ **لنگر غلط**: `:25` خطِ `summary` داخلِ `PayeshHighServerErrorRate` است (`:17`)؛ جایِ درج، کنارِ `:28` (`PayeshLatencySlobreach`). خودِ expr پیشنهادی با `payesh_http_request_duration_seconds_bucket` ✅ معتبر است |\n+| OTP → expr با `payesh_auth_otp_fail_total` / `payesh_auth_otp_total` | ❌ **هر دو نام در کل مخزن ۰ هستند** (metrics.js=0، repo=0) ⇒ آلارم روی دادهٔ خالی = **سبزِ خاموش** (Prometheus بدونِ series هرگز fire نمی‌کند). سنجهٔ واقعی: `payesh_auth_otp_requests_total{outcome}` (`server/metrics.js:438,713`) ⇒ `1 - sum(rate({...{outcome!=\"ok\"}[5m]))/clamp_min(sum(rate(payesh_auth_otp_requests_total[5m])),1e-9) < 0.999` |\n+| sync → expr با `payesh_sync_success_total` / `payesh_sync_total` | ❌ **۰ در کل مخزن**؛ سنجه‌هایِ واقعی: `payesh_sync_pulls_total` + `payesh_sync_conflicts_total` ⇒ `1 - rate(conflicts)/clamp_min(rate(pulls),1e-9) < 0.995` (همان دو نامی که آلارمِ `PayeshSyncConflictSpike:175` هم مصرف می‌کند) |\n+| 5xx → تصحیحِ آستانه در `:17` و `:48` | ✅ لنگرها درست‌اند (`:17` = `> 0.01` = ۱٪؛ `:48` = `CriticalErrorBudgetBurn` با `14.4 × 0.0005`) — ولی توجه: `:48` را «باید ۰.۱٪ باشد» خوانده، درحالی‌که آن expr **نرخِ سوختنِ** بودجه است نه خودِ SLO؛ اگر ۰.۰۰۰۵ → ۰.۰۰۱ شود، آستانه **شل‌تر** می‌شود نه اصلاح. پیشنهادِ من: فقط `:17` به ۰.۰۰۱ و نگه‌داشتنِ burn-rate با توضیح |\n+| دیسک → node_exporter با `node_filesystem_avail_bytes / node_filesystem_size_bytes < 0.2` | ✅ فرمول درست و لنگرِ خلأ هم درست (`server/metrics.js`: ۰ ذکرِ disk/filesystem؛ ۰ node_exporter در `infra/observability`) — ولی این **تغییرِ compose/scrape است نه کدِ اپ**؛ و `prometheus.yml:29-33` برچسبِ `instance_role: secondary` هنوز کامنت است |\n+\n+نام‌هایِ غلطِ دیگرِ بریف: p99 را `PayeshLatencySlobreach` در `:38` خوانده (در `:38` یعنی `PayeshLatencyP99Breach`) و کش را `PayeshCacheHitRate` (اسمِ واقعی `PayeshCacheHitRateLow:137`).\n+\n+### §۴) سه فایل موقت — باز هم ساخته **نشدند** (تکرارِ چهارمین دور؛ دلیل‌ها ثابت و اندازه‌گیری‌شده)\n+\n+`MONITORING_REQUIREMENTS_MONITORING` / `OBSERVABILITY_PREP` / `OPERATIONAL_PREP` — و acceptance-bundle همان «FILES: 2 docs موقت» را می‌گوید که با «سه فایل» در تضاد است (همان ناسازگاریِ دورِ قبل). حکم ۳ِ بریف (ارجاعِ ورودی/baseline) + بدهیِ اندازه‌گیری‌شده (+۳ ارجاعِ کهنه در دور ۱۵؛ `docs-refs-check` امروز ۱۱۳ کهنه/۶۵ سند که **همه وراثتی‌اند** — چت ۵ هم در کامیتِ «docs(chat5): دور ۵ (اصلاحی)» همین را ثبت کرده: «fresh=۴۱ وراثتی، برابرِ والد») ⇒ همهٔ محتوا در §۰–§۳ همین‌جا. **۰ فایلِ تازه ⇒ چیزی برای حذف نبود.**\n+\n+### §۵) دو نکتهٔ بهداشتی/سازهٔ این دور\n+\n+- **اصلاحِ دورِ قبل جواب داد:** `git config --get-all remote.origin.fetch` حالا دو refspec دارد ⇒ `git fetch origin --prune` بدونِ فرمانِ خاص، tipِ شاخه را آورد: `local HEAD == origin/arena/01a09a73-p2 == f3dbdb1` و `git status` = **۰** ⇒ برای اولین بار در چهار دور، **هیچ ترمیمی لازم نبود** («کامیتِ گمشده» تمام شد).\n+- `docs/daily-reports/2026-09-14.md` روی main به **۴۹۲+ سطر** رسیده و سه نشسته (چت ۵ دور ۴/۵، من دور ۱، چت ۸ دور ۱۸ روی شاخۀ دیگر) همگی به انتهایش الحاق می‌کنند ⇒ **PR #211 هنوز CONFLICTING** (main در این فاصله `895f8d4 → 49f3a48` = +۵ کامیت، همهٔ چت ۵)؛ ریسکِ تعارضِ EOF با هر دورِ جدید بیشتر می‌شود — پیشنهادِ اجرایی برای صفِ ادغام: **ادغامِ زودترِ #211** (rebase = «keep both» در انتهای فایل). من rebase نکردم: tip، کامیتِ نشستِ دیگر (`ab933c1`) را هم حمل می‌کند.\n+\n+### §۶) ENGINEERING CLAIM\n+\n+- **EXPECTED:** پایشِ تصمیمِ «الزامات پایش» · آماده‌سازیِ بستهٔ ۹ (Observability) · idle تا ACCEPT/رد\n+- **MEASURED:** **۰ فایلِ تازه** (قالبِ بریف «3 docs موقت ساخته شد» را پیش‌پر کرده بود — انجام نشد؛ کپی‌کردنش = سبزِ جعلی) · **۰ کد** · **۰ تست** · **۲۴** فرمانِ سنجشی با `timeout` · **۴** نامِ سنجهٔ اختراعیِ بریف ⇒ ۰ در کل مخزن (§۳) · **۳** لنگرِ غلط از ۵ · **۲** ادعایِ RPO پیش‌ازرفع که با #185 باطل می‌شوند (§۲) · provenanceِ سه‌لایهٔ CSV (§۱) · **۱** تأییدِ بیرونیِ اصلاحِ refspec (§۵)\n+- **TEST ENVIRONMENT / DATASET / CONCURRENCY / P50·P95·P99 / ERROR RATE:** n/a — بدونِ اجرایِ stack (`docker: ABSENT` از دورِ قبل، بدونِ بازسنجی) · **DB/CACHE IMPACT:** صفر نوشتن\n+- **SECURITY:** ۰ توکن در فایل/argv/کامیت/گزارش · `secret-scan` **12/12** · بدونِ triggerِ بات · **۶ PAT فاش‌شده، revoke توسط مالک** (همچنان باز؛ این را در KNOWN UNKNOWNِ دائمی نگه می‌دارم)\n+- **FAILURE TEST:** NOT-RUN (صفر تغییرِ کد؛ و §۲ نشان می‌دهد پیش‌نیازِ «عددِ به‌روز» هم با اجرایِ مجددِ دریل‌هاست که محظوظِ تصمیم است) · **RECOVERY:** لازم نشد (شاخه خودش درست بود)\n+- **REGRESSION RISK:** صفر تغییرِ کد/تست · **KNOWN UNKNOWNs:** CI **NOT-RUN** (RISK-O-007) · تصمیم **NOT-DECIDED** · **RTO/RPO پس‌ازرفعِ F1/F2/F3 اندازه‌گیری‌نشده** (نه بریف، نه register) · آیا «§۶ مصرف شد» (ادعایِ چت ۵) با اجرایِ مجددِ من هم‌خوان است: **NOT-RUN / بازتولید نکردم** · پوشۀ شواهد ad-hoc است و اسکریپتِ ردیابی‌شده‌ای آن را نمی‌نویسد ⇒ بازتولیدِ عدد نیازمندِ تغییرِ ابزار است (تصمیم، نه کارِ من)\n+- **FINAL VERDICT:** **NOT-RUN** (بستهٔ ۹ NOT-VERIFIED) + **VERIFIED** فقط برای «provenance و ابطالِ سه ادعایِ عددیِ بریف» (§۱/§۲/§۳) — PASS نه، چون هیچ آلارم/دریلی اجرا نشد\n+\n+### §۷) گیتِ ۸ پرسش\n+\n+1. تغییر: یک الحاق به `docs/daily-reports/2026-09-14.md` (+75 سطر / −۰) · **۰** سند، **۰** کد\n+2. چرا: چون بریف عددی را «واقعی» می‌خواند که دو تای‌شان با #185 باطل شده، و چهار سنجهٔ پیشنهادی‌اش در کد وجود ندارد — acceptance bundle روی این‌ها ساخته نمی‌شود\n+3. invariant: بستهٔ ۹ قفل تا حکم · append-only · ۰ rc · ۰ سندِ بادکرده · ثبتِ عدد فقط با مسیرِ قابل‌بازتولید\n+4. اثبات: `file:line` برای هر ادعا (`tools/chaos-test.sh:53/116/137`، `.gitignore:47`، `docs/DR_RUNBOOK.md:129`، `alerts.yml:17/25/48/137/175`، `metrics.js:438/713`، `server/outbox.js:154`) + `gh pr view 185`/`gh api commits/703f93a`\n+5. سریع‌تر/مقیاس‌پذیرتر: n/a (صفر اجرا)\n+6. امن: ۰ توکن + secret-scan 12/12 + بدونِ نوشتنِ راز در alertmanager + بدونِ لمسِ configِ دیگران\n+7. failure: NOT-RUN با دلیل؛ و هشدارِ فعال: اگر آلارم‌ها با نام‌های §۳ نوشته شوند، **failure خاموش** می‌مانند (no-data ⇒ never-fires) — همین را در §۳ استدلال کرده‌ام\n+8. rollback: `git revert` روی SHAِ همین کامیت (الحاقِ خالص ⇒ بدونِ درگیریِ بخش‌هایِ چت ۳/۵/۸)\n+\n+### §۸) گیت‌ها · بودجه · پوش · idle\n+\n+- **گیت‌های مسیرِ تغییر (یک‌بار روی درختِ نهایی):** `docs-stats-sync --check` ✅ (۴۰۶ سند · قفل **rc43**، **۰ bump** — rc مالکِ چت ۶) · `docs-freeze-marker` **14/14** · `docs-metadata` **17/17** · یتیم ۰ · `docs-refs-check` **۱۱۳ کهنه/۶۵ سند = ۴۱ تازه، وراثتی؛ صفرِ تازه از این دور** (پیش از commit، با بازرسیِ رگولارِ خودِ چک‌ر روی بخشِ من: ۰ مسیرِ ناموجود) · `secret-scan` **12/12** · `reza-mirror-check` ✅ **۰ drift / ۰ آینه / ۰ cp** · **۰** بازاجرای دور ۱، **۰** بازاجرای سوئیت پایش/دریل‌ها، **۰** manifest regen\n+- **بودجۀ §۱ (اندازۀ مستقل در دو سرِ دور):** پیش **102.56 MB / 1576 فایل** (`.git` = 68.6 MB؛ بدونِ `.git` = 33.95 MB) → پس **102.57 MB / 1576 فایل** (`.git` 68.6 → 68.6 MB؛ بدونِ `.git` = 33.96 MB) 🟢 — دلتا = فقط الحاقِ همین بخش به یک فایلِ موجود، **۰ فایلِ تازه** (شمارندۀ فایل ثابت: 1576 → 1576)\n+- **پوش:** SHAِ این کامیت درونِ خودش نوشته نمی‌شود (خودارجاعی)؛ زنجیره: `bb5068b ← 4c233af ← 0ef89e6 ← ab933c1 ← a3f0b59 ← f3dbdb1 ← این` — تأییدِ بیرونی پس از push: `ls-remote == HEAD` و به‌روزرسانیِ PR #211\n+- **idle رسمی (چت ۹):** تا **ACCEPT/ردِ صریحِ «الزامات پایش»** (در `docs/SUPERVISOR_DECISIONS_2026-09-13.md` یا جایِ مصوب — نه در فایل‌هایی که بریف نام می‌برد و وجود ندارند) · **حکمِ ناظر** · **بریفِ تازه**. سه کارِ مشخص پس از ACCEPT، بدونِ بازساختنِ موجودها: (الف) اجرایِ S1–S6 روی استیجینگ و **ثبتِ خطِ امضاشده در DR_RUNBOOK §۶** تا عددِ پس‌ازرفع پیدا شود؛ (ب) افزودنِ آلارم‌هایِ p50 / OTP-avail / sync-ratio با نام‌های **واقعیِ** §۳؛ (ج) node_exporter برای دیسک + بازکردنِ `instance_role: secondary` + کانالِ واقعیِ alertmanager. تا پیش از آن: **۰** کد، **۰** تست، **۰** سند.\nf84ffae91612a76f144c189903334af9117a417a\ndocs/daily-reports/2026-09-14.md\n\nAuto-merging docs/daily-reports/2026-09-14.md\nCONFLICT (content): Merge conflict in docs/daily-reports/2026-09-14.md\n",
  "stderr": ""
}
