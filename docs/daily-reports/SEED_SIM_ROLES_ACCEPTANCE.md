# ماتریس پذیرش — غنی‌سازی دانهٔ دمو با نقش‌های شبیه‌سازی (SIM-01..04)

> **وضعیت:** پیش‌نویس آمادهٔ اجرا — چت ۳، دور ۵ ‏(2026-09-13).
> **مقدمهٔ اجرا:** فقط پس از واگذاری صریح ناظر (توصیهٔ §۳ بریف دور ۵: «این کار را به چت ۳ بسپارید»). طبق سناریو C ‏(بلاک #129) این سند فقط مستندات است — **هیچ کدی در این دور تغییر نکرده**.

## ۱) یافته‌های ممیزی وضع موجود (شواهد از کد، این دور اجرا شد)

دانهٔ فعلی (`server/data/payesh.json` — ‏۱۰۳۵ کاربر، seed قطعی ‏20260901):

| # | نقش/پرچم | در کد محصول | در دانهٔ دمو | شکاف |
|---|---|---|---|---|
| SIM-01 | `role='guard'` (نگهبان/پذیرش — E.9) | ✅ ‏`53-visitors.js` (viewOk/writeOk خط 43/155) · ‏`30-authz.js:175-176` ‏(vis-new/vis-save) · فرم نقش `18-modals.js:211` | **۰ کاربر** | نگهبان دمو وجود ندارد ⇒ میز پذیرش E.9 در دمو فقط با manager قابل مشاهده است |
| SIM-02 | `lib_staff=1` (کتابدار — پرچم تفویضی روی **دبیر**) | ✅ ‏`54-library.js:68` + ‏`server/policy.js:273` ‏(fail-closed) | **۰ کاربر** | مسیر «دبیرِ کتابدار» در دمو تست‌پذیر نیست |
| SIM-03 | `asset_staff=1` (تحویلدار اموال — پرچم تفویضی روی **دبیر**) | ✅ ‏`55-assets.js:45` + ‏`server/policy.js:281` | **۰ کاربر** | مسیر «تحویلدار» در دمو تست‌پذیر نیست |
| SIM-04 | `is_head=1` (رئیس اداره — روی `role='edu_office'`) | ⚠️ فقط در `tools/seed-office-data.js` (پیش‌نویس چت ۲ — «فرض‌های قابل تغییر») — **در policy/UI محصول مصرف نمی‌شود** | ۰ کاربر | **پیش‌نیاز دارد:** تا وقتی کدِ مصرف‌کنندهٔ `is_head` مرج نشده، افزودن پرچم به دانه = دادهٔ مرده؛ باید بعد از تثبیت قرارداد چت ۲ انجام شود |

**نکتهٔ معماری مهم:** دانهٔ سرور از دموساز کلاینت مشتق می‌شود ‏(`server/seed.js` ⇒ اجرای `index.html` در jsdom ⇒ همان `db`). پس محل درست تغییر **`src/js/02-demo-data.js`** (یا ماژول دامنهٔ مربوطه به الگوی `47-counselor.js:577`) است، **نه** ‏`server/seed.js` — و پس از تغییر، ‏`node build.js` و `node server/seed.js` باید بازتولید شوند.

## ۲) ماتریس پذیرش (red-first — تست پیش از پیاده‌سازی)

سوئیت جدید: `tests/seed-completeness.js` ‏(هنوز ساخته نشده — قدم اول اجرا).

| ID | معیار پذیرش | سنجهٔ تست |
|---|---|---|
| A1 | حداقل ۱ کاربر `role='guard'` فعال با `school_id` معتبر در دانه | ‏query روی payesh.json: ‏`users.filter(u=>u.role==='guard'&&u.active===1&&u.school_id)` ‏≥1 |
| A2 | حداقل ۱ **دبیر** با `lib_staff===1` (نه نقش دیگر — قرارداد `54-library.js:68`) | ‏`role==='teacher'&&lib_staff===1&&school_id` ‏≥1 |
| A3 | حداقل ۱ **دبیر** با `asset_staff===1` (قرارداد `55-assets.js:45`) | مشابه A2 |
| A4 | ‏(مشروط — پس از تثبیت قرارداد چت ۲) کاربر `edu_office` با `is_head===1` | ‏`role==='edu_office'&&is_head===1` ‏≥1؛ تا آن زمان: ‏SKIP صریح با پیام (نه سبز جعلی) |
| B1 | قطعیت seed حفظ شود: دو بار `node server/seed.js` ⇒ خروجی بایت‌برابر | ‏sha256 دو اجرا برابر |
| B2 | رگرسیون صفر: ‏`run` ‏35/35 · ‏`smoke` ‏547/547 · ‏`authz-model` ‏255/255 پس از دانهٔ جدید | اجرای کامل |
| B3 | ‏policy سرور fail-closed بماند: دبیر *بدون* پرچم ⇒ ‏`lib-loan-save`/`asset-assign` رد شود (جهش: حذف شرط پرچم از policy ⇒ تست قرمز) | جهش‌آزمایی |
| B4 | کاربر جدید الگوی امنیتی دانه را نگه دارد (password دمو `123456`، ‏national_id/phone از مولدهای موجود `nid()/demoPhone()` — نه مقدار دستی) | بازرسی کد + secret-scan سبز |
| B5 | شمار کاربران افزوده حداقلی باشد (۱ نگهبان + ۲ پرچم روی دبیرهای *موجود*؛ کاربر تازه فقط guard) — دانه متورم نشود | ‏diff شمار users ≤ +1 |

## ۳) طرح اجرا (پس از واگذاری)

1. ‏RED: ‏`tests/seed-completeness.js` با A1–A3+B1 ⇒ روی دانهٔ فعلی قرمز.
2. ‏`src/js/02-demo-data.js`: افزودن ۱ نگهبان (الگوی خط 87 — ‏`username:'guard1'`) + ست `lib_staff:1` و `asset_staff:1` روی دو دبیر موجودِ مدرسهٔ ۱ (بدون کاربر جدید).
3. ‏`node build.js` ⇒ ‏`node server/seed.js` ⇒ سبز شدن A1–A3.
4. جهش B3 + رگرسیون B2 + قطعیت B1.
5. ‏A4: بلاک روی تصمیم چت ۲/ناظر دربارهٔ قرارداد `is_head` — در PR جدا.

**برآورد:** یک PR کوچک (~۳ فایل: دموساز + تست + دانهٔ بازتولیدشده) — بدون تداخل با #129 ‏(شاخهٔ بلاک هیچ‌کدام از این فایل‌ها را لمس نمی‌کند؛ راستی‌آزمایی شد).

## ۴) ریسک‌ها

- بازتولید دانه، ‏diff بزرگ باینری‌گونه در `server/data/payesh.json` می‌سازد (id/phone/nid همهٔ رکوردهای بعدی جابه‌جا می‌شوند اگر کاربر وسط توالی add شود) ⇒ **نگهبان باید بعد از آخرین add فعلی users در seq دموساز اضافه شود** تا diff حداقلی بماند؛ در غیر این صورت B1 بین نسخه‌ها معنایش را از دست می‌دهد.
- سوئیت‌هایی که به شمار دقیق کاربران پین شده‌اند قرمز می‌شوند — **ممیزی این دور (اجرا شد):** ‏`tests/donations2.js:55` و `tests/drills2.js:54` هر دو `db.users.length === 1035` را assert می‌کنند (سنجهٔ «کاربر تازه ساخته نشد») و با +۱ نگهبان ⇒ ‏1036 باید به‌روز شوند؛ ‏`tests/seed-integrity.js:66` فقط کامنت است (بی‌اثر). این دو تغییر عددی باید در همان PR و در همان کامیت دانه باشند (red-first: اول تست seed-completeness قرمز، بعد دانه + به‌روزرسانی پین‌ها با هم سبز).
- **راستی‌آزمایی عدم تداخل با #129 (اجرا شد):** ‏diff شاخهٔ بلاک‌شده هیچ‌یک از `02-demo-data.js`/`server/seed.js`/`payesh.json` را لمس نمی‌کند ⇒ ‏0 فایل مشترک.
