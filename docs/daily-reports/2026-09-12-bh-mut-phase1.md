# گزارش روزانه — BH-mut فاز ۱: مهاجرت فایل‌های جهش به الگوی امن

**تاریخ:** ۲۰۲۶-۰۹-۱۲ · **نویسنده:** Arena (چت پ₀→BH-mut) · **PR feat:** #131 (`feat/bh-mut-phase1`، head نهایی 414aafe) · **PR docs:** #134 (نسخهٔ نخست #132 در حین کار توسط مالک مرج شد؛ #134 ردیف تکراری BH-mut را نیز رفع می‌کند)

> **رویداد حین کار:** در میانهٔ این دور، مالک #124/#125/#127/#130 را مرج کرد (main ‏b53a7a7 → 011251c). شاخهٔ feat روی main جدید بازسازی شد (cherry-pick سه کامیت کد + بازسازی قفل)؛ هیچ هدف جهشی پایلوت‌ها در آن مرج‌ها تغییر نکرد و **هر ۹ فایل + خودآزم + smoke + قفل روی head نهایی دوباره اجرا و سبز شدند** (۵۴/۵۴، ۱۷/۱۷، ۵۴۷/۵۴۷، 14/14).

## مأموریت (تعریف از ردیف BH-mut در NEXT_ACTIONS — آن زمان روی #125، اکنون روی main پس از مرج #125)

> **مهاجرت فایل‌های جهش به الگوی امن** (کپی جدا + unlink در exit، الگوی p06/p11) — چون در اجرای suite-بستر (۲۰۲۶-۰۹-۱۲)، جهش‌های برخی فایل‌ها مستقیم سورس تولید را آلوده کردند و پاک‌سازی‌شان با timeout ناقص ماند → قرمزی کاذب sync-queue-caps. **معیار پذیرش:** پس از اجرا `grep -rl MUTATION` چیزی نیابد؛ md5 سورس‌ها قبل/بعد تغییرناپذیر.

## انباری (Evidence-based)

- `tests/*-mutations.js` روی main: **۱۲۳ فایل**. طبقه‌بندی اسکریپتی (نوشتن روی مسیرهای درخت vs کپی tmp):
  - **۱۱۹ فایل ناامن** (جهش درجا روی سورس + بازگردانی دستی در پایان — الگوی مستعد حادثه)
  - ۲ فایل safe از ابتدا (dropout، wal-disk-full — mkdtemp)
  - ۲ فایل سایر (db-engineering، idb-persistence — بازبینی فاز ۲)
- الگوی مرجع p06/p11 از سورس: جهش در کپی موقتِ هم‌جوار (الگوی پیشین: فایل ثابتِ جهش‌یافته — با فاز ۱ حذف شد؛ اکنون از طریق کیتِ `tests/helpers/mutant-kit.js`) + `process.on('exit', unlink)` + require همان کپی — سورس اصلی هرگز نوشته نمی‌شود.

## زیرساخت ساخته‌شده (تعمیم الگوی p06/p11 بدون دست‌کاری سوئیت‌ها)

1. **`tests/helpers/mutant-preload.js`** — پیش‌بارگذارِ بازمپ مسیر با `MUTANT_PATH_MAP` (JSON مسیر اصلی→کپی): هوک `Module._resolveFilename` (require) + fs سنکرون/آسنکرون/promises/استریم (read/write/copy/rename — دو-مسیره‌ها هر دو آرگومان). مسیرهای نسبی نسبت به cwd حل می‌شوند. بدون map کاملاً بی‌اثر. با `NODE_OPTIONS=--require=…` به همهٔ نوه‌ها به‌ارث می‌رسد.
2. **`tests/helpers/mutant-kit.js`** — سمت هارنس: `session()` ⇒ کپی جهش‌یافتهٔ **هم‌جوارِ اصلی** (requireهای نسبی سالم می‌مانند — درس ۱۰۲)، `passthrough`/`remapBuildOutputs` (index.html/USER_GUIDE.html/.build-cache.* به سایهٔ tmpdir — خروجی‌های build هرگز آلوده نمی‌شوند)، `env()` (NODE_OPTIONS + MAP)، جاروی آغاز (`sweepStrays` — بقایای SIGKILL اجرای قبلی)، unlink در exit.
3. **`tests/mutant-kit-test.js`** — خودآزم **۱۷/۱۷**: بازمپ require/read/write، مسیر نسبی، دو-مسیره‌ها، بی‌اثری بدون map، به‌ارث‌رسانی به نوه، و **بازسازی سناریوی حادثهٔ P1-2**: فرزندِ درگیرِ جهش با SIGKILL کشته می‌شود ⇒ md5 سورس تغییرناپذیر، هیچ فایل tracked تغییر نمی‌کند، stray هم‌جوار untracked با جاروی اجرای بعدی پاک می‌شود.

## مهاجرت فاز ۱ — ۹ فایل پایلوت (۵۴/۵۴ جهش کشته، همه با Evidence اجرای مستقیم)

| فایل | نتیجه | کلاس | نکته |
|---|---|---|---|
| session-revocation-mutations | **3/3 کشته** + سبز نهایی | سرور | جهش auth.js (فایل حادثه) + revocation.js |
| rate-limit-mutations | **4/4 کشته** + سبز نهایی | سرور | M3 با بوت FB |
| otp-ratelimit-mutations | **11/11 کشته** + خط پایه سبز | سرور | اجرای کامل ۷.۵ دقیقه |
| wave23-reports-mutations | **11/11 کشته** | PG زنده | initdb اختصاصی 55470 + DATABASE_URL |
| att2-mutations | **5/5 کشته** + سبز پایانی | مرورگر | build به سایه؛ src و index.html بکر |
| sync-queue-caps-mutations | **5/5 کشته** + baseline-green | مرورگر | **قربانیِ قرمزی کاذب P1-2** |
| pubrep-mutations | **7/7** (۶ جهش + خط پایه) | مرورگر | |
| pr74-mutations | **3/3 کشته** | مرورگر | assert بایت‌به‌بایت سالم‌ماندن (جایگزین restore+sha) |
| wave10-pg-live-mutations | **5/5 کشته** | live-spawn | PG_LIVE_BIN سیستم |

پس از هر اجرا: `md5sum -c` سورس‌های هدف ✓ · `find … -name '*-mutated*'` = صفر stray ✓ · `git status` فقط فایل‌های تست ✓.

## یافتهٔ جانبی — تستِ شکستهٔ RL-g روی main (اصلاح شد)

`tests/rate-limit-distributed.js` RL-g («خرابی = fail-open») روی main **قرمز بود**: استاب روی `redis.incr` می‌زد ولی پیاده‌سازی از P0-TTL به `redis.incrByWithTtl` مهاجرت کرده بود ⇒ استاب بی‌اثر، incr واقعی موفق، remaining=4 (نه 5) ⇒ چک همیشه قرمز. شواهد: اجرای مستقیم روی درخت پاک main = «RL-g ناموفق»؛ پس از اصلاح استاب: **7/7**. توجه: گیتِ جهش rate-limit روی main هم به همین دلیل exit 1 می‌داد (سبز نهایی قرمز) — با این اصلاح 4/4 + سبز نهایی ✅.

## قفل مستندات — rc39 (جانشین طبیعی rc38 پس از مرج #130)

افزودن `tests/mutant-kit-test.js` ⇒ شمار تست ۴۶۳→**۴۶۴** روی main جدید؛ helpers کیت بیرون شمارش رسمی. گارد preflight خودِ run-all این بامپ را اجباری کرد (DOCS STATS STALE). نسخهٔ اولیهٔ این قفل روی main قدیمی (b53a7a7) با پرشِ عمدی از rc37/rc38 ساخته شد؛ پس از مرج #130، روی main جدید بازسازی شد: **جانشین rc38**، مانیفست ۳۲۱ ردیف، marker بامپ rc38→rc39 (**14/14**).

## درس‌ها

- **(۱۰۲) کپی جهش باید هم‌جوار اصلی باشد، نه در tmpdir**: نسخهٔ اول کیت کپی را در tmpdir می‌گذاشت — requireهای نسبی خود فایل (`require('./redis')`) می‌شکست. الگوی p06/p11 (هم‌جوار) دقیقاً برای همین است.
- **(۱۰۳) پیش‌پردازش گارد را جدی بگیر:** `node tools/docs-stats-sync.js --help` در واقع sync اجرا کرد (فلگ ناشناخته = اجرای عادی) — درخت را قبل از تصمیمِ بامپ dirty کرد.
- **(۱۰۴) قرمزیِ «سبز نهایی» را ریشه‌یابی کن، نه دور بزن:** سبز نهایی قرمزِ rate-limit اولین‌بار به‌نظر رگرسیون مهاجرت آمد؛ اجرای سوئیتِ پاک روی درختِ بدون‌تغییرات نشان داد تست روی main شکسته است (پیش‌existing) — مهاجرت فقط آشکارش کرد.

## یافته‌هایی که خودم رد کردم

1. **مهاجرت تمام ۱۱۹ فایل در یک PR** — رد شد: هر مهاجرت باید با اجرای واقعی و کشته‌شدن جهش‌ها اثبات شود؛ ۱۱۹ اجرای کارخانه‌ای در یک دور، ریسک سبزِ جعلی دارد. فاز ۱ = ۹ پایلوتِ پرریسک (فایل‌های حادثه + هر ۳ کلاس)؛ ۱۱۰ باقی در صف فاز ۲.
2. **بازنویسی سوئیت‌ها برای پذیرش مسیر کپی (سبک p06/p11 خام)** — رد شد: نیازمند دست‌کاری ۱۰۰+ سوئیت؛ پیش‌بارگذارِ بازمپ همان گارانتی را بدون تغییر سوئیت می‌دهد.
3. **باقی‌گذاشتن کپی در tmpdir با کپی‌کردن کل `server/`** — رد شد: requireهای بالادری (خارج از دایرکتوری) هنوز می‌شکنند و کپی کامل در هر جهش سنگین است؛ هم‌جوار بودن مشکل را ریشه‌ای حل می‌کند.

## وضعیت گیت‌ها (روی head نهایی شاخهٔ feat 414aafe — پس از بازسازی روی main جدید، همه با Evidence)

- خودآزم کیت **17/17** · docs-freeze-marker **14/14** · stats-sync --freeze --check **سبز** · smoke **547/547** (exit=0، لاگ کامل)
- **run-all کامل (۲ یال موازی‌زمانی، هرکدام ~۴۵ دقیقه، DATABASE_URL به PG trust اختصاصی):**

| یال | سبز | قرمز | خروجی |
|---|---|---|---|
| شاخهٔ feat/bh-mut-phase1 (head 308c2be) | **۳۶۴** | **۹۱** | EXIT=1 |
| baseline: main خالص b53a7a7 | ۳۵۸ | **۹۶** | EXIT=1 |

تفاضل مجموعهٔ قرمزها (شاهد صادقانه — لاگ کامل هر دو یال):
- **۸۹ قرمز مشترک = پیش‌existing روی main** (سوئیت‌های سرور/پورت/منابع در این سندباکس؛ از جمله otp-ratelimit و otp-ratelimit-mutations که سوئیتِ پایه‌اش هم در هر دو یال قرمز است — اجرای مستقیمِ همان فایل روی درخت کامل: **11/11 کشته + خط پایه سبز**).
- **۷ قرمزِ main که شاخه شفاهت کرد**: rate-limit-distributed + rate-limit-mutations (هر دو روی main قرمز بودند به‌خاطر RL-g شکسته — رفع این PR) · pubrep-mutations (هارنس درجا در یالِ main قرمز؛ مهاجرت سبزش کرد) · multi-instance، pnote-mutations، tomorrow-mutations، wave12-network-mutations (flake یال — دست‌نخورده در این PR).
- **۲ قرمزِ فقط در یال شاخه — هر دو خارج از تغییرات من و flake اثبات‌شده با اجرای مستقیم سبز**: delta-phase4-mutations (**20/20 کشته، exit=0**) · waf-ddos (**exit=0**).
- درخت پس از هر اجرای جهش: `git status` پاک (حتی پس از اجرای مستقیمِ هارنس قدیمی delta-phase4 درجا — بازگردانی خودش کار کرد).

**نتیجهٔ ارزیابی یال: هیچ رگرسیونی از این PR در یال دیده نشد؛ خالص‌تر از main (۹۱ در برابر ۹۶ قرمز).** این مقایسهٔ دویالی پیش از بازسازی روی main جدید انجام شد (پایهٔ b53a7a7)؛ پس از rebase، هر ۹ فایل جهش + خودآزم + smoke + قفل روی head نهایی مستقیماً دوباره سبز شدند. یال روی main در این محیط به‌طور سیستمیک قرمز است (CI مسدود بیلینگ است — گیت‌ها محلی)؛ این وضعیت پیش‌existing جدا از این PR ثبت شد.

## کشف جانبی ۲ — کهنگی آمار مستندات main

`node tools/docs-stats-sync.js --check` روی **main خالص b53a7a7 قرمز است** (مرجِ #126 گزارشِ روزانهٔ ۱۱م را بدون stats-sync اضافه کرد؛ daily-reports ۱۰→۱۱). یعنی run-all روی main خالص بلافاصله با EXIT=5 (DOCS STATS STALE) می‌ایستد. بامپ rc39 این شاخه همین کهنگی را هم ترمیم می‌کند (شمارش ۳۴۹/۳۵۰ درست).

## باقی‌مانده / صف فاز ۲

- **۱۱۰ فایل جهش باقی‌مانده** (شامل server-mutations با ۲۵ جهش و منطق R89/R92/R97 — سنگین‌ترین، جداگانه) + ۲ فایل «سایر».
- ثبت §۲.۱۳ نمایه برای گزارش‌های pending (#125×۳، #128×۲، همین گزارش) در قفل بعدی.
- صف کلی پس از این: مرج PRهای باز #124/#125/#127/#128/#130 (بازبین) · P0-4/P0-5 · rotate توکن‌ها (مالک) · staging (ناظر).
