# گزارش چت ۹ — ممیزی و راستی‌آزمایی کل مخزن «پایش» (repo: rezaa2544/p2)

**نقش:** چت ۹ — مهندس ممیزی و راستی‌آزمایی (مالک هیچ فیچری نیست).
**تاریخ:** 2026-09-13 (۲۳ شهریور ۱۴۰۵) · **شاخه:** `arena/01a097f8-p2` (بر پایهٔ `main@523c1f5`).
**دامنه:** اسکن کامل مخزن + تست کارکرد همهٔ اجزا. **صفر تغییر کد.** این گزارش «نقشهٔ وضعیت» است، نه رفع (طبق توصیهٔ هماهنگ‌کننده).

> **خلاصهٔ یک‌خطی:** هسته و خانوادهٔ سرور/سینک/گزارش و همهٔ گیت‌های PG-زنده **سبزند**؛ قرمزِ شناخته‌شدهٔ `server15` (37/40) **در #143 رفع شده** (اکنون 40/40). دو مسدودکنندهٔ رگرسیونِ کامل (`run-all-tests.sh`) و چهار رانشِ ایستا (openapi/config/docs) یافت شد — همه پیش‌موجود و قابل ارجاع به مالک/صف.

---

## ۱) نقشهٔ وضعیت (فاز ۰ و ۱ — خروجی اول، بدون هیچ رفع)

| محور | وضعیت | جزئیات |
|---|---|---|
| کلون/شاخه | 🟢 | `/home/user/p2` موجود؛ HEAD = `origin/main` = `523c1f5` (`Merge PR #147`). درخت تمیز. |
| اتصال گیت | 🟢 | `gh auth` فعال (bot). توکن‌ها فقط در env؛ هیچ‌جا در فایل/کامیت/لاگ ننوشتم. |
| وابستگی‌ها | 🟢 | `npm ci` = ۱۲۶ پکیج، ۰ آسیب‌پذیری. jsdom@25 موجود (بدون نیاز به `npm i --no-save`). |
| دانه | 🟢 | `node server/seed.js` → `server/data/payesh.json` (۵۶۸۳KB؛ ۱۰۳۵ کاربر/۶ مدرسه). |
| بودجهٔ ورک‌اسپیس (قبل) | 🟢 | ۳۹٫۶MB / ۱۳۴۶ فایل (سنجهٔ §۱ `WORKSPACE_HYGIENE.md`). |
| بودجهٔ ورک‌اسپیس (بعد) | 🟢 | ۴۹٫۴MB / ۱۳۵۱ فایل (رشد = دانه + اجراها؛ همچنان زیر آستانهٔ 🟢 ۱۰۰MB/۸۰۰۰). |

**نتیجهٔ نقشه:** مخزن سالم و قابل‌ممیزی است. دو قرمزِ «مسدودکنندهٔ رگرسیون» و چهار «رانشِ ایستا» از پیش موجودند؛ هیچ‌کدام حاصل کار این نشست نیست (A/B با درخت تمیز = خودِ `main` چون هیچ تغییری اعمال نکردم).

---

## ۲) جدول Evidence (خروجی واقعی، نه ادعا)

### ۲-الف) گیت‌های ایستا (فاز ۱)

| گیت | دستور | نتیجهٔ واقعی | وضعیت |
|---|---|---|---|
| secret-scan | `node tests/secret-scan.js` | `11/11 سبز` | 🟢 |
| check-authz | `node tools/check-authz.js` | `اکشن 394 (۱۹۹ نویسنده) — تطبیق کامل` | 🟢 |
| build --check | `node build.js --check` | `بیت‌به‌بیت یکسان + راهنما همگام + write-perms` | 🟢 |
| docs-freeze-marker | `node tests/docs-freeze-marker.js` | `14/14 — قفل rc40 معتبر` | 🟢 |
| docs-consistency | `node tests/docs-consistency.js` | `22/22` | 🟢 |
| docs-index-coverage | `node tests/docs-index-coverage.js` | `75/75` | 🟢 |
| docs-health | `node tests/docs-health.js` | `9/9 — صفر لینک شکسته` | 🟢⚠️ |
| docs-metadata | `node tests/docs-metadata.js` | `16/17 — ۱ سند یتیم` | 🔴 (P3) |
| docs-stats-sync | `node tools/docs-stats-sync.js --check` | `آمار با دیسک یکی است (۳۲۳ ریشه + ۳۸ = ۳۶۱)` | 🟢 |
| docs-refs-check | `node tools/docs-refs-check.js --check` | `۱۱ ارجاع تازه به فایل ناموجود` (exit 1) | 🔴 (P1) |
| openapi-drift | `node tests/openapi-drift.js` | `۴ مسیر کد در اسپک نیست` (exit 1) | 🔴 (P2) |
| config-audit | `node tests/config-audit.js` | `۲۹ متغیر کد بی‌سند + ۲۵ فقط-سند` (exit 1) | 🔴 (P2) |
| reza-mirror-check | `node tools/reza-mirror-check.js` | `۲۱ آینهٔ زنده برابر` | 🟢 |

### ۲-ب) تست‌های پویا — هسته و خانوادهٔ سرور/سینک/گزارش (فاز ۲)

| سوئیت | نتیجهٔ واقعی | وضعیت |
|---|---|---|
| `tests/run.js` | `35/35` | 🟢 |
| `tests/smoke.js` | `547/547` (۱ هشدار بی‌خطر `window.scrollTo`) | 🟢 |
| server1..server18 | 31/31 · ✅ · ✅ · 16 · 14 · 9 · 15 · 9 · 10 · 7 · 9 · 43/43 · 9/9 · 13 · **40/40** · 39 · 70 · 55 | 🟢 |
| sync-queue-caps | `36/36` | 🟢 |
| sync-del-mirror | `7/7` | 🟢 |
| delta-phase4 | `23/23` | 🟢 |
| offline-e2e | `23/23 (+2 NOT-RUN ثبت‌شده)` | 🟢 |
| bgsync | `14/14` (نخستین اجرا 12/14 — flake؛ پایین را ببینید) | 🟢 |
| reports-basic | `9/9` | 🟢 |
| reports-tenant-isolation | `11/11` | 🟢 |

### ۲-ج) گیت‌های PG-زنده (PostgreSQL 18 embedded، پورت ۵۵۴۳۳، دیتادیر روی دیسک واقعی `/home/user/.local/pg-payesh`)

| سوئیت | env | نتیجهٔ واقعی | وضعیت |
|---|---|---|---|
| wave10-pg-live | `PG_LIVE_BIN` | `19/19` (replica + پارتیشن‌بندی روی PG واقعی) | 🟢 |
| wave3-parity | `DATABASE_URL` | `20/20` (JS↔SQL فیلدبه‌فیلد؛ ۴۰۰۰۰ حضور/۵۰۰۰ نمره seed) | 🟢 |
| wave3-query3 (بخش B) | `DATABASE_URL` | `25/25` (شامل B7 بدون Seq Scan) | 🟢 |
| migration-009-live | `PG_LIVE_BIN` | `12/13` — M1 stale assertion | 🔴 (P2) |
| migration-sequence | — | `19/19` | 🟢 |
| p11-phone-auth-live | `P11_LIVE_PG` | `14/14` | 🟢 |
| wave23-reports-pg | `DATABASE_URL` | `76/76` | 🟢 |
| p13-public-report-pg | `DATABASE_URL` | `9/9` | 🟢 |

### ۲-د) جهش (فقط الگوی امن mutant-kit / mutant-preload)

| سوئیت | نتیجهٔ واقعی | وضعیت |
|---|---|---|
| wave23-norm-edge-mutations | `4/4 کشته` — sha256 سورس پیش/پس یکسان | 🟢 |
| clsctx-mutations | `3/3 کشته` | 🟢 |
| deadletter-mutations | `2/2 کشته` | 🟢 |
| health-index-mutations | `5/5 کشته` | 🟢 |
| otp-ratelimit-mutations | M1–M6 کشته مشاهده شد؛ کامل NOT-RUN (>۵ دقیقه، قطع شد) | ⚠️ |

---

## ۳) یافته‌های طبقه‌بندی‌شده

> روش: همهٔ قرمزها روی `main@523c1f5` (درخت تمیز، صفر تغییرِ من) بازتولید شدند ⇒ **همه پیش‌موجود**، نه «تازهٔ این نشست».

### 🔴 P1 — مسدودکنندهٔ رگرسیون

**P1-1 · `run-all-tests.sh` با `DATABASE_URL` خالی crash می‌کند (رانر v3).**
- بازتولید حداقلی: `bash scripts/run-all-tests.sh` → `line 78: DATABASE_URL: unbound variable` (exit 1). علت: `set -u` + گاردِ `[ -n "$DATABASE_URL" ]` بدون `:-`.
- اثر: رگرسیونِ کامل بدون export دستیِ `DATABASE_URL=""` اصلاً شروع نمی‌شود.
- پیشنهاد red-first: تستی که رانر را در env بدون `DATABASE_URL` اجرا کند و خروج ≠ 0 را قرمز بداند؛ رفع = `[ -n "${DATABASE_URL:-}" ]`.
- **ارجاع:** صف NEXT_ACTIONS.

**P1-2 · `docs-refs-check` قرمز = رگرسیون کامل در pre-flight `exit 6` متوقف می‌شود.**
- بازتولید: `node tools/docs-refs-check.js --check` → `۱۱ ارجاع تازه به فایل ناموجود` در ۵ سند.
- ریشه (API تک‌PR): `#133` «مرج‌شده» است ولی merge-commit آن (`aa936e4`) **درون شاخهٔ `#129` است، نه main** (HANDOFF چت ۸ هم «ناظر تک‌یک کرد — #133 درون #129» را ثبت کرده). در نتیجه `tests/truncation-telemetry*.js` و `tools/bench-reports-scale.js` و (از #129 باز) `tests/reports-bounded-cache*.js`/`bounded-delta-resume*.js` روی main نیستند ولی ۵ سندِ مرج‌شده به آن‌ها ارجاع می‌دهند.
- وضعیت: **پیش‌موجود و شناخته‌شده** — `NEXT_ACTIONS.md` ردیف HO-1 صریحاً به چت ۸ واگذار کرده بود. با مرجِ #129 به‌طور طبیعی بسته می‌شود؛ تا آن زمان رگرسیونِ کامل قفل است.
- **ارجاع:** صف NEXT_ACTIONS (HO-1) + گزارشِ مالک.

### 🟠 P2 — رانشِ مستندات/اسپک (پیش‌موجود)

**P2-1 · `openapi-drift` — ۴ endpoint گزارش در کد ولی نه در `docs/openapi.yaml`.**
- `GET /api/v1/reports/{academic,attendance,finance,teachers}` (از Wave 23 DB-native، PR #120/#127) در اسپک نیست.
- پیشنهاد red-first: گیت موجود `tests/openapi-drift.js` خودش قرمز است؛ رفع = افزودن ۴ مسیر به اسپک.

**P2-2 · `config-audit` — ۲۹ متغیر کد بی‌سند + ۲۵ متغیر فقط-زیرساخت در سند.**
- نمونه‌های کد بی‌سند: `PAYESH_CURSOR_SECRET`, `PAYESH_DR_RPO_S`, `PAYESH_METRICS_TOKEN`, `WAVE23_*`, `PAYESH_REGION`, …
- نمونه‌های فقط-سند: `POSTGRES_PASSWORD`, `GRAFANA_*`, `MINIO_*`, `PGBACKREST_*`, `PGHA_*`, `STANDBY_BOOTSTRAP`.

**P2-3 · `migration-009-live` M1 — ادعای کهنه (آخرین مهاجرت = 009).**
- زنجیرهٔ ۰۰۱..۰۱۰ روی DB خالی **بدون خطا** اعمال می‌شود (خودم ۱۰/۱۰ را روی PG تازه زدم)؛ فقط assert `files[last].startsWith('009')` کهنه است چون `010_users_phone_auth.sql` (PR #115) اضافه شد. بله، M2–M10 سبزند ⇒ باگِ کارکردی نیست، drift تست است.
- پیشنهاد red-first: assert دینامیک شود (یا آخرین شماره از دیسک مشتق شود، یا 010 صریح).

### 🟡 P3 — جزئی

**P3-1 · `docs-metadata` — ۱ سند یتیم:** `docs/daily-reports/2026-09-13-chat6-final-delivery.md` هیچ ارجاع ورودی ندارد. *(پس‌نوشت: در همین نشست به‌طور غیرعمد رفع شد — خودِ این گزارش و `RISK_REGISTER.md` به آن ارجاع دادند؛ گیت اکنون ۱۷/۱۷ است.)*

**P3-2 · `docs/DOCS_HEALTH_REPORT.md` کهنه (۳۲۱→۳۲۳ سند).** اجرای `docs-health` فایل را بازتولید کرد (۳۲۳) ولی نسخهٔ commitشده ۳۲۱ است. من فایل را `git checkout --` کردم تا درخت تمیز بماند؛ بازتولید+کامیت مالِ مالکِ آن سند است.

### ✅ مثبت — قرمزِ شناخته‌شدهٔ رفع‌شده

**`server15` اکنون 40/40 سبز است** (نه 37/40). رگرسیونِ مظنونِ P1-2 (سناریوهای resolve-conflict) در **PR #143** (`fix(conflicts): resolve ⇒ keep + هرس سقف‌دار`) رفع شده و روی `main@523c1f5` است. «یافتهٔ server15» از NEXT_ACTIONS باید بسته علامت بخورد.

---

## ۴) یافته‌هایی که خودم رد کردم (ضدِ ثبتِ نادرست)

- **bgsync 12/14 (B1/B2 قرمز):** نخستین اجرا قرمز بود، ولی ۲ اجرای مجدد **هر دو 14/14 سبز** ⇒ همان **flake تایمینگیِ مستندشده** است (`docs/daily-reports/2026-09-12-review-round2-fixes.md`: «یک قرمز در اجرای فشرده؛ در هر دو اجرای مجدد سبز — flake، رگرسیون نیست»). به‌عنوان رگرسیون ثبت نشد.
- **DOCS_HEALTH_REPORT 321→323:** نه «رگرسیون من» و نه دست‌کاری — خروجیِ جانبیِ خودِ گیت `docs-health` است؛ به‌عنوان P3 مستند شد و فایل restore شد.

---

## ۵) NOT-RUN (صادقانه، با دلیل — سبزِ جعلی ممنوع)

| قلم | دلیل |
|---|---|
| **رگرسیون کامل `bash scripts/run-all-tests.sh`** | مسدود با P1-1 (unbound `DATABASE_URL`) و سپس P1-2 (docs-refs exit 6). سوئیت‌های منفرد مستقیماً اجرا و ثبت شدند. |
| `reports-bounded-cache` / `bounded-delta-resume` / `truncation-telemetry` | فایل‌ها روی main **موجود نیستند** (در شاخهٔ #129). |
| `wave18w19-multinode-live --live` | نیازمند Redis واقعی (`LIVE_REDIS_URL/BIN_DIR/DATA_DIR`)؛ `redis-server` در سندباکس نیست. **DRY_RUN اجرا شد ✅** (طرح + اسکلت). |
| a11y / مرورگری (`a11y-*.js`) | نیازمند `playwright-core` + chromium؛ نصب نیست ⇒ NOT-RUN صریح. |
| CI گیت‌هاب | انسداد بیلینگ (قید صداقت — هرگز «CI سبز» ادعا نمی‌کنم). |
| `otp-ratelimit-mutations` کامل | >۵ دقیقه (بوت سرور به‌ازای هر جهش)؛ با timeout قطع شد — M1–M6 کشته مشاهده شد، ولی «کامل سبز» ادعا نمی‌کنم. قطعِ میانی یک shadow + سرور یتیم گذاشت که **جاروب و پاک شد** (الگوی `node .*server/index\.js`). |
| استیجینگ/بنچ مقیاس (P0-5) | محیط چندنمونه‌ای در سندباکس نیست (تصمیم ناظر). |

---

## ۶) سنجهٔ بودجه — قبل/بعد

| لحظه | مؤثر (§۱) | فایل |
|---|---|---|
| شروع | ۳۹٫۶ MB | ۱۳۴۶ |
| پایان | ۴۹٫۴ MB | ۱۳۵۱ |

رشد = دانهٔ `payesh.json` (۷MB، لازمِ سوئیت‌ها) + اجراها. 🟢 زیر آستانه. `node_modules`/`.local`/`out` از بودجه مستثنی‌اند (§۱). PG دیتادیر در `/home/user/.local/pg-payesh` (خارج از snapshot، طبق §۵). در پایان نشست PG را stop و دیتادیر را حذف می‌کنم.

---

## ۷) COMMIT / SHA

| SHA | محتوا |
|---|---|
| `523c1f5` | `main` مبدأ (Merge PR #147) — `git ls-remote origin main` == HEAD تأیید شد. |
| این دور | شاخهٔ `arena/01a097f8-p2` — فقط مستندات ممیزی (این گزارش + بخش روزانه + HANDOFF + صف‌ها). صفر تغییر کد. |
