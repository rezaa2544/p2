# گزارش روزانه — 2026-09-13 (۲۲ شهریور ۱۴۰۵)

**نویسنده:** Arena — چت ۸ (بستهٔ اندازه‌پذیری بریدگی/resume گزارش‌ها — دورِ راستی‌آزمایی)
**شاخهٔ تحویل:** `feat/chat8-report-measurability` (تازه از `origin/main` @ `3b5a42f`)
**پایه (SHA_origin/main):** `3b5a42f095cb2548691a84a32f29f6b28e787a9f` (ثبت با `git ls-remote origin main`)

---

## خلاصهٔ اجرایی

مأموریت «بستهٔ اندازه‌پذیری بریدگی/resume گزارش‌ها» بود (۳ کانتر تله‌متری با کاردینالیتهٔ کران‌دار + هارنس بنچ مقیاسِ staging-ready). **قاعدهٔ ضدتکرار فعال شد:** تحویلاتِ همین مأموریت از قبل به‌صورت **PR [#133](https://github.com/rezaa2544/p2/pull/133)** (شاخهٔ `feat/p03-truncation-telemetry`، ۴ کامیت، stacked روی **PR [#129](https://github.com/rezaa2544/p2/pull/129)** `feat/p03-reports-envfix-bounded-cache`) موجود است. طبق قاعدهٔ مأموریت («اگر مشابهش موجود بود، به‌جای بازسازی، راستی‌آزمایی و گزارش بده») **هیچ بازسازی‌ای انجام نشد**؛ درختِ دقیق #133 در همین نشست راستی‌آزمایی شد (detached checkout، بدون worktree — برای ثبات بودجه): **گیت ۹/۹ + جهش ۹/۹ کشته + run 35/35 + smoke 547/547** و گیت‌های #129 روی همان درخت: **14/14 + 15/15 + جهش 10/10 + 10/10**. همهٔ گیت‌های پایه روی `main@3b5a42f` سبز (node v22.14.0).

## فاز ۰ — اتصال و محیط

| گام | نتیجه | شاهد |
|---|---|---|
| توکن‌ها | ۵ از ۶ معتبر (HTTP 200 از API)؛ ۱ مرده (HTTP 401) — هیچ توکنی در هیچ فایل/کامیت/لاگِ ریپو نوشته نشد؛ احراز با اسکریپت askpass موقت در `/tmp` (فقط `$GH_TOKEN` می‌خواند) | API واحد، 2026-09-13 |
| کلون کانونیک | `/home/user/p2` تازه (ورک‌اسپیس خالی بود؛ `P2_DIR_MISSING`) | `du -sh` = 97MB · `find` = 1329 فایل (بعد از کلون، measured) |
| SHA_origin/main | `3b5a42f…` | `git ls-remote origin main` |
| شاخهٔ کار | `feat/chat8-report-measurability` فقط از `origin/main` | — |
| npm ci | ۱۰۵ پکیج؛ `engines ≥22` ⇒ node **v22.14.0** در `.local` نصب شد (budget-exempt) — قرارداد CI: smoke روی 18/20 **خودِ خود می‌پزد** (fake-green) | `.github/workflows/node.js.yml` |
| گیت‌های پایه (درخت main) | smoke **547/547** · check-authz ✅ (394 اکشن، تطبیق کامل WRITE_PERMS) · secret-scan **11/11** · build --check ✅ | هر ۴ مورد exit=0 |
| دانه | `node server/seed.js` — users 1035 · grades 12855 · `payesh.json` ≈6.8MB روی دیسک (gitignored؛ طبق §۵ بهداشت نگه داشته شد) | `server/data/` |
| بهداشت — پایه | **94.0MB مؤثر / 1337 فایل (🟢)** | دستور سنجش §۱، پیش از هر کار |

## فاز ۱ — زمینه (فقط از API، نه حافظهٔ چت‌ها)

**PRهای باز (API `pulls?state=open`، 10 قلم):**
#134 `docs/daily-reports-bhmut` · **#133 `feat/p03-truncation-telemetry`** · #131 `feat/bh-mut-phase1` · **#129 `feat/p03-reports-envfix-bounded-cache`** · #128 `docs/daily-reports-p13` · #114 · #91 · #82 · #76 · #74

**به‌روزرسانی صف ناظر (endpoint تک‌PR — فیلد `merged` در endpoint فهرست معتبر نیست، درس §۱۰ سند بهداشت):** #130، #127، #125، #124 **مرج‌شده‌اند** (merged_at 2026-09-12، 20:51–20:54Z)؛ #126/#123/#120/#119/#118/#116/#115/#113/#112 نیز مرج‌شده.

**اسناد:** دوزیهٔ `REPORT_CACHE_ISOLATION_DOSSIER.md` (‏§۵) و مشخصهٔ `REPORT_PERF_GAP_SPEC.md` روی main **نیستند** — هر دو روی شاخهٔ #129 ساخته شده و توسط #133 مصرف/تکمیل شده‌اند. `NEXT_ACTIONS.md` = `docs/daily-reports/NEXT_ACTIONS.md`.

## فاز ۲ — راستی‌آزماییِ #133 (همهٔ اعداد measured، همین نشست)

**روش:** detached checkout از `3361154` (سرِ `feat/p03-truncation-telemetry`) داخل کلون کانونیک؛ شاخهٔ کاری من کامیت نداشت؛ پس از هر اجرا `git status` خالی (درختِ پس از جهش‌ها و rebuildهایشان تمیز — restore درست کار کرده). **بدون worktree** (بودجه: یک checkout دوم ≈۷۰MB مؤثر بود و بودجه را به 🟡 می‌برد).

| گیت | دستور | خروجی واقعی | وضعیت |
|---|---|---|---|
| گیت #133 | `node tests/truncation-telemetry.js` | `9 سبز / 0 قرمز ✅` (A: کانترها · B: خودآزمون هارنس) | **PASS** |
| جهش #133 | `node tests/truncation-telemetry-mutations.js` | **`9 کشته / 0 زنده ✅`** (TM1–TM9) | **PASS** |
| رگرسیون درخت #133 | `node tests/run.js` | `35/35 ✅` | **PASS** |
| smoke درخت #133 | `node tests/smoke.js` | `547/547 ✅` | **PASS** |
| گیت #129 (همان درخت) | `node tests/bounded-delta-resume.js` | `14/14 ✅` | **PASS** |
| گیت #129 (همان درخت) | `node tests/reports-bounded-cache.js` | `15/15 ✅` | **PASS** |
| جهش #129 (همان درخت) | `node tests/bounded-delta-resume-mutations.js` | **`10 کشته / 0 زنده ✅`** (RM1–RM10) | **PASS** |
| جهش #129 (همان درخت) | `node tests/reports-bounded-cache-mutations.js` | **`10 کشته / 0 زنده ✅`** (BM1–BM10) | **PASS** |

**تطبیق با بندهای مأموریت (قلم‌به‌قلم):**

- الف. سه کانتر با نام‌های دقیق (`payesh_pull_partial_collections_total` / `…_full_snapshot_required_total` / `…_resume_snapshot_total`) در `server/metrics.js` + سیم‌کشی در `server/pull.js` — ✅
- کران برچسب: `HEAVY_REPORT_COLS = ['attendance','grades','discipline','hw_submissions']` و هر نامِ دیگر ⇒ `other`؛ جهش TM4 (برچسب آزاد) کشته — ✅
- تست unit: افزایش (TM1–3 هر افزایش ⇒ قرمز) · کران برچسب (تست ۴) · نمایش در `/metrics` با HELP/TYPE (تست ۵) — ✅
- ب. هارنس `tools/bench-reports-scale.js`: خود-skip بدون DATABASE_URL (exit 0 + پیام صریح؛ TM9) · پرچم‌های `--roles/--concurrency/--iterations` · p50/p95 به تفکیک گزارش×نقش · خروجی ماشین‌خوان + **برچسب مقیاس measured اجباری** (Target ≠ Measured کدشده؛ TM8) — ✅
- اسناد: زیربخش «روش سنجش» در §۵ دوزیه (کانترها + آستانهٔ پیشنهادی: نرخ پایدار `full_snapshot_required` > ۱٪ کل pullها در ۷ روز) + رونبوک «پروتکل اجرای گزینهٔ ت» بلافاصله پس از گزینهٔ ت (پایان §۳، پیش از §۴) + ردیف ارجاع در NEXT_ACTIONS — ✅

### NOT-RUN (با دلیل صریح — سبز جعلی ممنوع)

| قلم | دلیل |
|---|---|
| اجرای واقعی `bench-reports-scale.js` (بنچ مقیاس در استیجینگ) | DATABASE_URL/PG در سندباکس نیست — خود-skip **به‌طور طراحی** (محل اجرا: استیجینگ، P0-5)؛ خودِ مسیر skip گیت‌دار است (TM9 + تست ۹) |
| CI روی GitHub Actions | انسداد بیلینگ (قید پابرجا) — گیت‌های محلی جایگزین |

## یافته‌ها (گزارش؛ صفر تغییر کد)

1. **ترتیب مرج:** #133 روی #129 stacked است ⇒ مرج #133 مستلزم مرج #129 (یا rebase) است. merge-baseٔ #129 با main = `b53a7a77` (نه نوکِ main). هر دو لایهٔ stack همین نشست راستی‌آزمایی شدند (جدول بالا) ⇒ **فنی‌اً merge-ready، منتظر تصمیم ناظر**.
2. **`USER_GUIDE.html`:** مشخصه‌های تعارضِ `<<<<<<< HEAD` / `>>>>>>> origin/main` **روی خودِ main وجود دارند** — پیش‌موجود و ناشی از #133 نیست (بیلدِ #133 فقط هش HEAD-side را داخل بلوکِ مارکر به‌روز کرده: `04ac4a3857a4` → `bd4fa8ae4a9e`). پیشنهاد: تمیزکاری به‌عنوان قلم مستقل.
3. **دامنهٔ بنچ:** رونبوک = **۶ کوئری academic/finance** (دامنهٔ مشخصه — عنوان `REPORT_PERF_GAP_SPEC.md`: «شکاف کارایی academic/finance»). عبارت «چهار گزارش» در متن مأموریت با دامنهٔ مشخصه یکسان نیست؛ اگر ناظر بخواهد attendance/teachers هم در همان هارنس باشد، یک دور پیگیری کوچک (افزودن builderها به `queriesFor`) کافی است.
4. **الگوی جهش:** جهش‌های #133 با الگوی in-place (restore در exit handler) است؛ در اجرای این نشست پس از هر اجرا درخت اندازه‌گیری‌شده تمیز بود. مهاجرت به الگوی امنِ `mutant-kit` (BH-mut فاز ۱) در فاز ۲ آن پروژه قابل‌ترتیب است — **فاز ۱ هنوز روی main نیست (PR #131 باز است)**.
5. **خاستگاه #133:** یادداشت کامیت «ff مین محلی به 3b5a42f» = دقیقاً پایهٔ همین نشست و یادداشت NEXT_ACTIONS با تاریخ 2026-09-13 — شاخه به‌احتمال زیاد از نشست قبلیِ همین مأموریت (یا چتِ دیگر) است؛ مالک/ناظر تأیید کند.

## بهداشت (طبق §۱ `WORKSPACE_HYGIENE.md`)

| لحظه | حجم مؤثر | فایل | وضعیت |
|---|---|---|---|
| پایه (پیش از کار) | **94.0MB** | 1337 | 🟢 |
| میانی (دانه + اسناد + refهای fetch، پیش از پاک‌سازی) | 102.3MB | 1341 | 🟡 (ستونِ «پاک‌سازی فوری») |
| پایانی (پس از پاک‌سازی دروازه‌دار، پیش از commit) | **92.9MB** | 1338 | 🟢 |

**پاک‌سازی دروازه‌دار (طبق §۸/§۵/§۷، همه با شاهد):**
- `git repack -adf --window=50 --depth=50` (دستورِ صریحِ §۸؛ window=250 در این سندباکس OOM می‌شود): `.git` از 65MB به **55MB**.
- حذفِ ۲ refِ موقتِ `refs/remotes/origin/c8/ref-p03-base` (‏= `de0005dda37…`) و `c8/ref-p03-telemetry` (‏= `3361154b038…`) — هر دو پیش از حذف با `git ls-remote` برابرِ همان refهای origin تأیید شدند (`feat/p03-reports-envfix-bounded-cache` و `feat/p03-truncation-telemetry`)؛ بازیابی = `git fetch origin <branch>`.
- حذفِ فایلِ scratch ریشهٔ ورک‌اسپیس (`.chat8_sha_origin_main` — SHA در همین گزارش و کامیت ثبت است)؛ askpassِ موقت هم از `/tmp` حذف شد.
- **نگه‌داشتنی‌ها (§۵):** `server/data/payesh.json` ≈6.8MB (دانهٔ نمایشی، gitignored، لازمِ سوئیت‌های وابسته؛ بازتولید: `node server/seed.js`). `node_modules` و `.local` (node 22) از بودجه مستثنی‌اند (§۱) و شمرده نشدند.

## باز است / گام بعدی

- **ناظر:** ① ترتیب مرج **(#129 → #133)** ② دامنهٔ بنچ (۶ کوئری academic/finance طبق مشخصه vs عبارت «چهار گزارش») ③ تمیزکاری `USER_GUIDE.html` ④ زمان‌بندی اجرای استیجینگِ گزینهٔ ت (محیط P0-5).
- پس از مرج #129/#133: اجرای واقعی بنچ در استیجینگ ⇒ اعداد measured برای تصمیم MV (گزینهٔ الف — معیار: p95 > بودجهٔ ناظر، جای‌دار 500ms).
- چت ۸ خود هیچ PRی باز نمی‌کند و به هیچ PR دیگری دست نزده است (خط قرمز).

---

## بخش چت ۸ — دور ۲: تمیزکاری `USER_GUIDE.html` + دوزیهٔ دامنهٔ بنچ (2026-09-13)

**شاخه:** `fix/user-guide-conflict-markers` (پایه: `origin/main` = `ad7ce5c` — پس از مرجِ #136/#134/#131)
**دامنه (طبق قاعدهٔ ضدتداخل):** فقط `USER_GUIDE.html` + دوزیهٔ بنچ — mutant-kit (چت ۲)، `reports-sql.js` (چت ۶) و PRهای محدودهٔ #118..#1 (چت ۳) دست‌نخورده.

### فاز ۱ — ریشه‌یابی دقیق (همه measured)

| قلم | نتیجه |
|---|---|
| خطوط marker | خطوط ۶/۸/۱۰ در `USER_GUIDE.html` (۳ خط)؛ **تنها فایلِ** درختِ main با marker (`git grep` روی `origin/main@ad7ce5c`) |
| کامیتِ واردکننده | `609ad23` (2026-09-06، «پُلِ دوره‌ایِ زنگ §۱۳.۴»، author "Payesh Dev") — تنها کامیتی که حضور marker را در تاریخچهٔ این فایل تغییر داده (`git log -S`) |
| مسیر ورود به main | مرج `b153f0f` (**PR #1** ui-improvements) |
| محتوای بلوک | طرف HEAD = stamp `04ac4a3857a4` / طرف origin/main = stamp `136c3d6aed4a` — دو تگِ **یکسان** (بنابراین union معنادار نیست؛ انتخاب دوتایی لازم است) |
| داوری | مکانیک `build.js:syncGuide`: `--check` فقط **اولین** stamp را با `buildHash(index.html)` مقایسه می‌کند؛ سنجش مستقیم: `buildHash(index.html فعلی) = 04ac4a3857a4` ⇒ **stamp طرف HEAD درست است**؛ stamp دوم **اثباتاً کهنه** |

### فاز ۲ — رفع red-first (شواهد)

- **RED (پیش از رفع):** ① گیتِ نشست «بدون marker در آرتیفکت‌های بیلد» = **FAIL** (۳ خط). نکته: `build --check` روی main سبز بود چون stampِ نخست به‌صورت تصادفی درست است — marker بی‌سروصدا از همهٔ گیت‌ها رد می‌شد (دلیلِ نیاز به گیتِ صریح). ② stamp دوم ناهم‌خوان با `buildHash` (measured).
- **رفع:** ۴ خط حذف (۲ مارکر + stamp کهنه + `=======`)، ۰ خط افزودن؛ stampِ معتبر حفظ شد.
- **GREEN (پس از رفع):** صفر marker در کل درخت · `build.js` کامل ×۲ **بایت‌پایدار** (بیلد نه `USER_GUIDE.html` و نه `index.html` را بازنویسی نکرد — `writeIfChanged`) · **smoke 547/547** · **secret-scan 11/11** · **build --check** ✅ (شامل «راهنما همگام با index.html است» + «خروجی build با index.html بیت‌به‌بیت یکسان است»)
- کامیت: `5c8d42d` (۱ فایل، −۴ خط)

### فاز ۳ — دوزیهٔ دامنهٔ بنچ (صفر تغییر کد)

> **تضاد با جدولِ الگوییِ پرامپت:** الگوی پرامپت «attendance/teachers در رونبوک #133» را نشان می‌داد — دقیقاً خلافِ واقعیتِ اندازه‌گیری‌شده (دور ۱ + بازخوانیِ کدِ این دور). جدولِ زیر بر پایهٔ کدِ stack @ `09c5578` و مشخصه است:

| گزارش | در رونبوک #133 (`bench-reports-scale`) | ابزار جداگانهٔ موجود | مأموریت چت ۸ («چهار گزارش») | در `REPORT_PERF_GAP_SPEC` |
|---|---|---|---|---|
| academic (۳ کوئری: page/totals/trend) | ✅ | — | ✅ | ✅ (یکی از ۶ کوئری شکاف) |
| finance (۳ کوئری: tuitions/installments/scholarships) | ✅ | — | ✅ | ✅ (یکی از ۶ کوئری شکاف) |
| attendance | ❌ | ✅ `bench-reports-attendance.js` (Wave 23، PG زنده) + در بارِ ترکیبی `bench-reports-concurrency.js` | ✅ | ❌ (خارج از دامنه) |
| teachers | ❌ | فقط در بارِ ترکیبی `bench-reports-concurrency.js` (ابزار مستقل ندارد) | ✅ | ❌ (خارج از دامنه) |

**توصیه:** رونبوک #133 با دامنهٔ مشخصه **هم‌خوان** است (شکافِ measured در academic/finance — خودِ عنوان مشخصه). attendance از قبل با ابزار موجود پوشش دارد. اگر ناظر همهٔ ۴ گزارش را در یک هارنس بخواهد (عبارتِ «چهار گزارش» مأموریت)، یک PR پیگیریِ کوچک (افزودن builderهای attendance/teachers به `queriesFor`) کافی است — بدون تغییر معماری. **توصیه: حفظ رونبوکِ فعلی (هم‌خوان با مشخصه) + PR پیگیریِ اختیاری؛ تصمیم: ناظر.**

### به‌روزرسانی صف — API (endpoint تک‌PR، 2026-09-13)

- **#133 مرج‌شده** (merged_at 2026-09-12T21:41:55Z) — **به شاخهٔ #129** (کامیت `aa936e4`)، نه مستقیم به main: stack **تک‌یک** شده؛ کانترها/هارنس اکنون روی `feat/p03-reports-envfix-bounded-cache` @ **`09c5578`** (که خودش با main تازه در `ea17c70` merge شده). **با ACCEPTِ #129 به main می‌رسند.**
- **#131 مرج‌شده** (`670be18` — BH-mut فاز ۱ حالا روی main) · **#134 مرج‌شده** (`c504fb0`).
- **باز (۸ قلم):** #137 (همان `feat/chat8-report-measurability` @ af25819 — دور ۱ چت ۸؛ PR توسط چت ۸ باز نشده بود) · **#129** (حالا stackِ تک‌یک‌شده) · #128 · #114 · #91 · #82 · #76 · #74 · + PRِ همین دور (تمیزکاری USER_GUIDE).
- **باز-راستی‌آزماییِ headِ تک‌یک‌شدهٔ `09c5578` (اجرای همین نشست):** truncation-telemetry **9/9** · run **35/35** · smoke **547/547** · bounded-delta-resume **14/14** · reports-bounded-cache **15/15** ✅ — تک‌یک شدن فایل‌های راستی‌آزمایی‌شده (metrics/pull/bench/telemetry) را لمس نکرده (measured) ⇒ شواهد جهشِ دور ۱ (9/9 + 10/10 + 10/10) معتبر می‌مانند.
- **ترتیب مرج ساده شد:** کافی است یک ACCEPTِ **#129** @ `09c5578` — کل stack (#129+#133) را می‌آورد.

### NOT-RUN (دلیل صریح)

- اجرای واقعیِ بنچ (استیجینگ): DATABASE_URL/PG در سندباکس نیست — self-skip به‌طور طراحی (P0-5).
- CI GitHub Actions: انسداد بیلینگ.
- دو سوئیت جهشِ #129 (10+10) روی headِ تک‌یک‌شده **دوباره اجرا نشدند**: تک‌یک شدن فایل‌های جهش‌پذیر را لمس نکرده (measured) و شواهدِ دور ۱ معتبرند؛ در صورت خواستِ ناظر برای اجرای کامل، یک نشست کافی است.

### تعهدات دور ۲

- `reza-mirror-check.js`: ✅ همهٔ آینه‌های زنده برابر (‏`reza/USER_GUIDE.html` اسنپ‌شاتِ مستندشدهٔ BH-5 است — فقط گزارش، همگام‌سازی نشد؛ آینهٔ تازه ساخته نشد)
- Push + `ls-remote == HEAD`: انجام شد و تأیید شد ✅ (SHAها در گزارشِ پایانِ نشست)
- بهداشت (سنجهٔ §۱): پایانِ دور ۱ `92.9MB/1338` (🟢) → پایانِ دور ۲ **`94.9MB`/`1410`** (🟢 — افزایش از fetchِ refهای main تازه و اسنادِ همین بخش)
- هیچ PRی مرج نشد (خط قرمز) · توکن فقط در env (askpass موقت، پس از کار حذف) · اسناد فارسی/UTF-8 · بلوک‌های ماشینی فقط با ابزار (stats-sync)
