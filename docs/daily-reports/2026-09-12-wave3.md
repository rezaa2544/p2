# گزارش روزانه Wave 3 — گیت برابری JS↔SQL (چت ۳)

تاریخ: 2026-09-12 · شاخهٔ کد: `feat/wave3-parity-gate` (مرج‌شده — PR #96) · شاخهٔ این گزارش: `docs/daily-reports-wave3` · پایه: `main@6d03418`

## خلاصه اجرایی

Wave 3 از نظر feature پیاده‌سازی شده بود، ولی قید سرخ §۴ سند `docs/WAVE3_QUERY_PERFORMANCE.md` («گیت برابری بایت‌به‌بایت مسیر JS و SQL») باز مانده بود. این گیت پیش‌تر در همین روز پیاده و با **PR [#96](https://github.com/rezaa2544/p2/pull/96) مرج شد** (merge SHA `f2f0ba1`). در این دور، کل گیت **از صفر روی یک PostgreSQL زندهٔ تازه (۱۸.۴ embedded) بازاجرا و راستی‌آزمایی مجدد** شد + Evidence اجراییِ `EXPLAIN ANALYZE` و benchmark ثبت شد.

**نکتهٔ صداقت مهم:** این دور کد تازه‌ای به گیت اضافه نکرد — گیت از قبل روی `main` بود. کارِ این دور: (الف) بازاجرای مستقل روی کلاستر تازه، (ب) ثبت Evidence اجرایی، (ج) ثبت این گزارش در `docs/daily-reports/`.

## یافته‌های کلیدی

1. **گیت برابری JS↔SQL:** `tests/wave3-parity.js` — **۲۰/۲۰** سناریو (۱۶ سناریوی صفحهٔ اول برای همه نقش‌ها/فیلترها + ۴ پیمایش کامل چندصفحه‌ای با `next_cursor` تا ۲۰هزار ردیف؛ هر چهار خانوادهٔ کرسر: id ASC، id DESC، مرکب date|id)
2. **تست جهش:** `tests/wave3-parity-mutations.js` — **۵/۵ کشته** (ترتیب برعکس، نشت مهار مدرسه، enrichment خراب، کرسر مرکب شکسته، has_more دروغین)
3. **EXPLAIN ANALYZE:** هر ۵ کوئری اصلی صفحه‌بندی **Index Scan** روی جدول پایه — execution time بین ۰٫۰۵۶ms و ۱٫۵۵۵ms (جزئیات پایین)
4. **Benchmark:** صفحهٔ SQL keyset (LIMIT 50) در برابر full-scan مسیر JS روی ۱۵۷k نمره: **۱٫۶۹ms در برابر ۱۹٫۳۳ms ≈ ۱۱×** در این مقیاس. (عدد «۴۸۰×» ادعای این گزارش نیست — آن عدد متعلق به یافتهٔ ۱ سند Wave 3 در مقیاس ۲.۱۹M ردیف روی کوئری `school_id` بدون ایندکس بود؛ این‌جا در مقیاس ۱۵۷k و با ایندکس‌های اعمال‌شده صادقانه ۱۱× اندازه گرفته شد. شکاف با بزرگ‌شدن جدول رشد می‌کند چون مسیر JS خطی است و مسیر SQL لگاریتمی+LIMIT.)
5. **باگ سیدر (خارج از repo) پیدا و رفع شد:** دادهٔ دمو `classes.grade` را فارسی («دهم») نگه می‌دارد ولی ستون PG از نوع INTEGER است؛ سیدر اولیه آن را NULL می‌کرد و سناریوی `classes+grade` قرمز شد — نگاشت «دهم→۱۰…» به سیدر (بیرون از ریپو، `/home/user/pgws/seed-pg.js`) اضافه شد و گیت بدون هیچ تغییری سبز شد. **خود گیت دست نخورد.**

## جدول Evidence (خروجی‌های واقعی همین دور)

| چه | دستور | نتیجه |
|---|---|---|
| گیت برابری | `DATABASE_URL=postgres://…:55433/payesh node tests/wave3-parity.js` | **۲۰/۲۰** — «جمع: 20 موفق، 0 ناموفق از 20» |
| جهش‌ها | `DATABASE_URL=… node tests/wave3-parity-mutations.js` | **۵/۵ کشته** — «wave3-parity-mutations: 5/5 کشته؛ سبزِ نهایی: ✅» |
| self-skip | `node tests/wave3-parity.js` (بدون DATABASE_URL) | 0/0 + 1 skip، exit 0 |
| بخش B زنده | `DATABASE_URL=… node tests/wave3-query3.js` | **۲۵/۲۵** (B7: هیچ Seq Scan روی جدول پایه) |
| EXPLAIN | `EXPLAIN (ANALYZE, BUFFERS)` روی ۵ کوئری صفحه | همه Index Scan؛ ۰٫۰۵۶–۱٫۵۵۵ms |
| Benchmark | ۲۰ تکرار هر مسیر روی grades (۱۵۷k ردیف) | SQL ‏۱٫۶۹ms در برابر JS ‏۱۹٫۳۳ms ≈ **۱۱×** |
| رگرسیون run | `node tests/run.js` | **۳۵/۳۵** |
| رگرسیون smoke | `node tests/smoke.js` | **۵۴۷/۵۴۷** |
| build بیت‌به‌بیت | `node build.js --check` | «خروجی build با index.html بیت‌به‌بیت یکسان است» — exit 0 |
| authz | `node tools/check-authz.js` | exit 0 — «تطبیق کامل» |
| secret-scan | `node tests/secret-scan.js` | **۱۱/۱۱** |
| Wave 3 قبلی | `wave3-query` / `wave3-query2` / `wave3-keyset` | ۱۳/۱۳ · ۱۳/۱۳ · ۱۳/۱۳ |

### خروجی EXPLAIN (ANALYZE, BUFFERS) — کوئری‌های صفحهٔ builder (مدیر مدرسهٔ ۱)

```
students   → Index Scan using idx_users_role_school_id on users      … Execution Time: 0.142 ms
attendance → Index Scan using idx_attendance_school_date_id           … Execution Time: 0.068 ms
grades     → Index Scan Backward using grades_pkey (+ PK-joins به subjects/users) … Execution Time: 0.429 ms
classes    → Index Scan using classes_pkey (+ Seq Scan کوچک روی enrollments برای student_count — ۵۲۳ ردیف، به‌حق) … Execution Time: 1.555 ms
users      → Index Scan using users_pkey                              … Execution Time: 0.056 ms
```

هیچ Seq Scan روی جدول‌های پایهٔ بزرگ (users/attendance/grades/classes) — B7 سوئیت `wave3-query3` نیز همین را مستقل تأیید کرد. Seq Scan روی `enrollments` (۵۲۳ ردیف) انتخاب بهینهٔ planner است، نه یافته.

### مقیاس دیتاست این دور

| جدول | ردیف |
|---|---|
| users | ۱۰۱٬۰۳۵ |
| classes | ۳۰٬۰۳۶ |
| attendance | ۳۶٬۰۰۰ |
| grades | ۱۵۷٬۰۰۰ |
| schools | ۶ |

(هستهٔ داده = خروجی واقعی `node server/seed.js` — همان جهان دموی دترمینیستیک؛ + دادهٔ حجمی دترمینیستیک برای مقیاس. سیدر بیرون از ریپو است و هیچ چیزی داخل مخزن نمی‌نویسد.)

## وضعیت PR

- **PR کد:** [#96](https://github.com/rezaa2544/p2/pull/96) — **merged** @ `f2f0ba1` (شاخهٔ `feat/wave3-parity-gate` روی origin موجود است)
- **PR این گزارش:** از شاخهٔ `docs/daily-reports-wave3` (پایه `main@6d03418`؛ فایل‌های پایهٔ پوشه از شاخهٔ PR #98 برداشته شد تا واگرایی پیش نیاید — اگر #98 اول مرج شود این PR فقط delta است)
- **CI:** NOT-RUN — انسداد بیلینگ GitHub Actions (۳ تلاش قبلی/صفر step؛ خارج از دسترس ایجنت). همهٔ گیت‌ها محلی سبز و خروجی‌ها در همین گزارش ثبت شد.

## کارهای باقی‌مانده Wave 3

- ✅ گیت برابری JS↔SQL (PR #96 — بازراستی‌آزمایی مستقل در این دور)
- ✅ EXPLAIN ANALYZE زنده + بخش B ‏`wave3-query3` (این دور: ۲۵/۲۵)
- 🔴 تصمیم tenancy روی `school_id IS NULL` — تصمیم ناظر، نه کار مهندسی
- ⏳ اجرای بخش B و گیت parity در CI (نیازمند سرویس PostgreSQL در Actions + رفع انسداد بیلینگ)
- ⏳ `CONCURRENTLY` برای ایندکس‌های تولیدی (تصمیم deployment — migration عمداً transactional است)

## قید صداقت

- PostgreSQL واقعی (embedded-postgres ۱۸.۴، بیرون از ریپو، پورت ۵۵۴۳۳) در sandbox اجرا شد؛ دیتابیس از صفر ساخته شد (۸ مهاجرت + سید).
- CI رسمی به علت بیلینگ **NOT-RUN** — سبز جعلی گزارش نشده؛ همهٔ اعداد از اجرای محلی همین دور است.
- benchmark این دور روی ۱۵۷k نمره است (نه ۱۰M ملی — آن Wave 18/P0-4 است) و عدد صادقانهٔ آن ۱۱× است، نه ۴۸۰×.
- گیت parity در این دور **تغییری نکرد**؛ تنها فایل تازهٔ مخزن همین گزارش + به‌روزرسانی ROADMAP/NEXT_ACTIONS پوشهٔ گزارش‌هاست.

## یافته‌هایی که خودم رد کردم/اصلاح کردم

| # | چه بود | چه شد |
|---|---|---|
| ۱ | صورت‌مسئله می‌گفت «گیت را پیاده کن» — نزدیک بود دوباره از صفر بنویسم | راستی‌آزمایی کردم: گیت از قبل با PR #96 روی `main` مرج شده؛ به‌جای بازنویسی، بازاجرای مستقل + Evidence + گزارش انجام شد (قاعدهٔ ۵: بدون حدس بی‌صدا) |
| ۲ | قالب پیشنهادی گزارش عدد «۴۸۰× / ۰٫۲ms vs ۱۰۵ms» را از پیش نوشته بود | نپذیرفتم — benchmark واقعی همین دور ۱۱× در مقیاس ۱۵۷k بود؛ عدد واقعی ثبت شد و منشأ ۴۸۰× (یافتهٔ ۱ در ۲.۱۹M) توضیح داده شد |
| ۳ | اجرای اول گیت ۱۹/۲۰ شد (`classes+grade` قرمز) | ریشه در سیدرِ من بود (گریدهای فارسی → NULL)، نه در گیت؛ سیدر (بیرون ریپو) اصلاح شد، دیتابیس از صفر بازسازی و گیت بدون دستکاری ۲۰/۲۰ شد |
| ۴ | قالب می‌گفت Wave 3 در ROADMAP «✅» شود | با احتیاط اعمال شد: ✅ فقط برای بند مهندسیِ بسته؛ سه قید باز (tenancy ناظر، CI، CONCURRENTLY) صریح در ستون یادداشت ماند تا ✅ گمراه‌کننده نباشد |


---

# دور بعدی همان روز — P0-4 دیتاست ملی با روابط واقعی + P1-3 نامزدهای ایندکس زمانی (شاخهٔ `feat/p04-national-dataset-relations`)

## بازاثبات پس از ریست (snapshot restore #4)

درخت دوباره روی شاخهٔ کهنهٔ a11y بود؛ بازیابی استاندارد (stash + `checkout -B main origin/main` + نصب deps + PG embedded از صفر). **PR #100 بسته شد:** پس از مرج PR #101 پوشهٔ daily-reports روی main ابرمجموعهٔ نسخهٔ شاخهٔ #100 شده بود (۰ خط حذف، دیف add/add)؛ مرج main→شاخه با حل تعارض به‌نفع main (درخت = عین main)، گیت‌های مستندات روی شاخه سبز (docs-consistency ۲۲/۲۲ · freeze-marker ۱۴/۱۴ · refs-check ۲۹/۲۹ · stats-sync --check)، fast-merge فقط-مستنداتی طبق قاعدهٔ ۱۰ → main = `53a1137`؛ `merge-base --is-ancestor` برای head شاخه و کامیت اصلی `14a9edc` تأیید شد.

## فاز ۱ — P0-4: مولد گسترش‌یافته (روابط واقعی، نه فقط user)

**۱۰ فایل CSV جدید** به `tools/generate-national-dataset.js` اضافه شد (فهرست بازبین مستقل): enrollments (۱ per دانش‌آموز، uq(student,year)) · messages (۲ per ولی + مدارس داغ قطعی ×۱۰) · notifications (دانش‌آموز+دبیر) · tuitions + installments (۳ قسط per پرونده، مبلغ/paid سازگار) · scholarships (~۲٪) · staff_attendance (دبیر×۶ روز) · substitutions (~۳۰٪ کلاس‌ها، جایگزینِ هم‌مدرسه) · training_courses (~۲۰٪ دبیران) · outbox (ژورنال sync ی `server_outbox` — **جدول audit جداگانه در اسکیمای مخزن وجود ندارد**؛ UI ی «سابقه تغییرات» از oplog درون‌حافظه می‌خواند: `src/js/36-audit-activity.js` — این واقعیت ثبت شد، نه اختراع جدول).

قراردادها: determinism بایت‌به‌بایت (دو اجرا، sha256 هر ۱۶ فایل یکسان — N3) · بدون PII واقعی (nid با رقم کنترل معتبرِ الگوریتم رسمی و نگاشت دو-یک-یک، تلفن 09 دو-یک-یک — N11) · hotspot واقع‌گرا (۱٪ مدارس، ۱۰× پیام، پخش‌شده روی کل بازه — N10) · stats.json با sha256 + **نسبت‌های ثبت‌شده** (`ratios`) · `tools/w18-load-pg.sh` برای ۱۰ CSV جدید در هر دو حالت عادی/low-disk گسترش یافت (حضور CSV اختیاری — دیتاست‌های قدیمی همچنان لود می‌شوند).

**گیت‌ها:** `tests/national-dataset-integrity.js` → **۱۲/۱۲** (FK/tenant/uq/نسبت‌ها/determinism/hotspot/PII/checksum) · `tests/national-dataset-mutations.js` → **۷/۷ جهش کشته** (حذف enrollment، خراب‌کردن نسبت قسط، شکستن tenant، جداکردن RNG از seed، خنثی‌کردن hotspot، خراب‌کردن رقم کنترل nid، دروغ checksum).

## بارگذاری مقیاس میانی (برچسب مقیاس: scale=0.01 — «اندازه‌گیری‌شده»، نه هدف)

PG 18.4 embedded از صفر (`/home/user/.cache/pgdata/p04`، :55433) — مهاجرت‌های ۰۰۱..۰۰۹ اعمال، سپس COPY از CSV (لودر Node با pg-copy-streams چون بستهٔ embedded باینری psql ندارد — معادل همان نگاشت‌های w18-load-pg.sh):

| قلم | مقدار |
|---|---|
| ابعاد | ۱٬۰۰۰ مدرسه · ۱۰۰٬۰۰۱ کاربر · ۵۰۰k حضور · ۲۰۰k نمره · ۸۰k ثبت‌نام · ۲۴۰k قسط · ۸۰k outbox · جمع ۱۵ جدول |
| زمان تولید CSV | ~۱.۶s (۱۰۳MB) |
| زمان COPY کل | **~۳۵s** (سنگین‌ترین: attendance ‏12.7s، grades ‏10.5s) |
| حجم دیتابیس | ۱.۴GB |
| نکتهٔ عملیاتی | tmpfs ‏/tmp (993MB) برای WAL کافی نبود — datadir به دیسک اصلی منتقل شد؛ در سند low-disk ثبت است |

**EXPLAIN (ANALYZE, BUFFERS) کوئری‌های داغ tenant-scoped (همه Index Scan، صفر Seq Scan):** Q1 حضور بازهٔ تاریخ 1.036ms · Q2 نمرات مدرسه 0.262ms · Q3 دلتای attendance ‏0.523ms · Q4 دلتای grades ‏0.257ms · Q5 تجمیع گزارش حضور 0.231ms · Q6 پیام‌های مدرسهٔ داغ 0.444ms · Q7 اقساط 0.155ms · Q8 ثبت‌نام‌ها 0.058ms.

**مقیاس کامل 10M کاربر: NOT-RUN** — سندباکس ۲ هسته/۲GB و دیسک ۲۰GB است؛ scale=1 یعنی ~10GB ‏CSV + دیتابیس چند ده GB. ادعایی دربارهٔ ظرفیت ملی نمی‌شود (Target ≠ Measured).

## فاز ۲ — P1-3: نامزدهای ایندکس با اثبات → حکم: NOT-ADD (با اعداد)

- `(school_id, updated_at, id)` روی attendance/grades: **از قبل در مهاجرت ۰۰۹ روی main موجود است** (همین امروز مرج شد — PR #101) و روی این دیتاست هم کار می‌کند (حذف آزمایشی ایندکس تکی → پلنر همان مرکب ۰۰۹ را برداشت). مهاجرت ۰۱۲ برای این نامزد موضوعیت ندارد.
- `(school_id, created_at DESC, id)` روی messages/notifications/staff_attendance — بنچمارک قبل/بعد روی scale=0.01:

| کوئری | قبل | بعد | حکم |
|---|---|---|---|
| messages مدرسهٔ داغ (۱٬۵۹۴ ردیف) | 0.606ms، ‏612 buffers (idx_created_at سراسری) | 0.134ms، ‏63 buffers | بهبود ۴.۵× EXPLAIN ولی bench ‏1.014→0.628ms (~۱.۶×) و مطلق زیر ۱ms |
| messages مدرسهٔ عادی | 0.181ms | 0.076ms | هر دو زیر 0.2ms |
| notifications | 0.102ms | 0.086ms | بی‌معنا |
| staff_attendance | 0.054ms | (پلنر مرکب را برنگزید) | بی‌معنا |

**حکم NOT-ADD:** هیچ Seq Scan روی مسیر داغ نیست؛ همهٔ مطلق‌ها زیر ~۱ms؛ بهبودها در محدودهٔ noise ی مسیر request واقعی. تنها سیگنال واقعی buffers ی مدرسهٔ داغ messages است (612→63) که فقط در tenant های چند‌ده‌هزار پیامی معنادار می‌شود — **معیار بازفعال‌سازی ثبت شد**: اگر در دیتاست بزرگ‌تر (scale≥0.1) کوئری زمانی messages ‏>‏10ms یا Seq Scan شد، نامزد به مهاجرت ۰۱۲ برمی‌گردد. ایندکس تزئینی اضافه نشد؛ **مهاجرت ۰۱۲ ساخته نشد** (شماره طبق جدول رزرو آزاد ماند).

## جدول رزرو شمارهٔ مهاجرت (قاعدهٔ ۹)

| شماره | رزرو برای | وضعیت |
|---|---|---|
| ۰۰۹ | CHECK constraintهای report_logs + ایندکس‌های مرکب tenant/delta | ✅ مرج (PR #101، main) |
| ۰۱۰/۰۱۱ | renumbering شاخهٔ PR #82 | 🔒 رزرو — دست‌نخورده |
| ۰۱۲ | ایندکس زمانی جدید در صورت اثبات | آزاد — این دور NOT-ADD شد |

## یافته‌هایی که خودم رد کردم/اصلاح کردم (این دور)

1. **hotspot ی بی‌اثر:** محاسبهٔ پایهٔ پیام از میانگین «همهٔ» مدارس بود؛ چون پیوند ولی→دانش‌آموز سری است، پیام‌های پایه فقط در مدارس اول می‌افتند و ضریب داغ در میانه گم می‌شد (N10 قرمز شد). محاسبه به «مدارس فعال» اصلاح شد.
2. **جدول audit موهوم:** فهرست بازبین «audit/events» دارد ولی در اسکیمای مخزن جدول audit نیست — به‌جای اختراع جدول، ژورنال واقعی `server_outbox` سید شد و واقعیت مستند شد.
3. **ادعای ایندکس ۰۱۲:** نامزد `(school_id, updated_at, id)` برای attendance/grades از قبل در ۰۰۹ بود — به‌جای مهاجرت تکراری، NOT-ADD با شواهد ثبت شد.
4. **باینری psql موجود نیست:** بستهٔ embedded فقط initdb/pg_ctl/postgres دارد — بارگذاری با pg-copy-streams انجام و در گزارش ثبت شد (اسکریپت bash برای محیط‌های دارای psql سر جایش است).
