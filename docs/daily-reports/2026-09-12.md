# گزارش روزانه — 2026-09-12 (۲۱ شهریور ۱۴۰۵)

**نویسنده:** Arena — Wave 18 (National Load Testing) + راه‌اندازی پوشهٔ گزارش روزانه
**شاخهٔ تحویل:** `feat/wave18-load-test` @ `9cae5cf` · ‏PR [#94](https://github.com/rezaa2544/p2/pull/94)
**شاخهٔ این پوشه:** `docs/daily-reports-init` (این کامیت)

---

## خلاصهٔ اجرایی

Wave 18 (تست بار ملی) روی محیط production-like سندباکس اجرا و بسته شد: PostgreSQL 17.11 با دیتاست ملی مقیاس ۰٫۳ (۳٬۰۰۰٬۰۰۱ کاربر / ۱۵M حضور / ۶M نمره) + Redis 8.0.2 + k6 v2.2.0؛ هر ۵ سناریوی طرح (بار ۵۰۰ / اوج ۲۰۰۰ / فشار →۵۰۰۰ / جهش ۵۰۰→۳۰۰۰ / سوک ۱۰۰۰×۱۵m) اجرا شد. **نتیجهٔ سلامت: صفر خطای نوشتن در هر ۵ سناریو — ۱۹٬۰۴۲ عملیات sync همگی اتمیک در PG؛** حتی لحظه‌های OOM هیچ داده‌ای از دست نرفت (بازشماری PG سالم). سقف ظرفیت تک‌جعبه ~۶۳۰ rps اندازه‌گیری شد و سه ایراد معماری مقیاس‌پذیری با کمّی‌سازی ریشه‌یابی و رفع/پیشنهاد ثبت شد. پوش انجام و با `git ls-remote` تأیید شد؛ PR #94 باز و mergeable.

## جدول گیت‌ها (اجرای امروز روی کلونِ تازهٔ شاخهٔ پوش‌شده — HEAD = `9cae5cf`، مطابق SHAی remote)

| گیت | دستور | خروجی واقعی | وضعیت |
|---|---|---|---|
| smoke | `node --expose-gc --max-old-space-size=2048 tests/smoke.js` | `تست دودی: 547/547 موفق — بدون خطا ✅` | **PASS** |
| check-authz | `node tests/check-authz.js` | `exit=0` | **PASS** |
| secret-scan | `node tests/secret-scan.js` | `secret-scan: 11/11 سبز ✅` | **PASS** |
| build check | `node build.js --check` | `✅ تطبیق کامل: هر اکشنِ نویسنده، نقش‌هایش را در WRITE_PERMS سرور دارد.` | **PASS** |
| wave18-load | `node tests/wave18-load-test.js` | `38/38 موفق — بدون خطا ✅` | **PASS** |
| wave18-stress | `node tests/wave18-stress-test.js` | `34/34 موفق — بدون خطا ✅` | **PASS** |
| wave18-spike | `node tests/wave18-spike-test.js` | `22/22 موفق — بدون خطا ✅` | **PASS** |

> این اجرا با `git clone -b feat/wave18-load-test` از remote و `npm ci` (۱۰۵ پکیج) روی همان درختِ پوش‌شده انجام شد — نه با اتکا به اجراهای قبلی.

## وضعیت Wave 18 — هر ۵ سناریو (اعداد واقعی k6)

| سناریو | rps | درخواست | p95 | خطای HTTP | نوشتن | خطای نوشتن | سرور |
|---|---|---|---|---|---|---|---|
| بار عادی ۵۰۰/s ×۳m | ۶۲۸ | ۱۱۴٬۸۱۶ | ۹۴۴ms | **۰** | ۳٬۷۰۴ | **۰** | زنده — PASS |
| اوج ۲۰۰۰/s ×۳m | ۴۵۹ | ۸۵٬۶۰۳ | ۱٬۳۸۷ms | **۰** | ۲٬۷۵۹ | **۰** | زنده — PASS (SLO تأخیر: FAIL) |
| فشار رَمپ ۱۰۰→۵۰۰۰/s ×۱۰m | ۱۹۴ | ۱۴۶٬۶۶۴ | ۱٬۷۸۷ms | ۰٫۰۹٪ | ۴٬۵۸۳ | **۰** | مرگ OOM (exit 137) در فاز پایدار — FAIL-by-design (نقطهٔ شکست) |
| جهش ۵۰۰→۳۰۰۰ + بازگشت | ۶۰۴ | ۱۵۹٬۰۰۰ | ۹۸۶ms | **۰** | ۴٬۹۶۰ | **۰** | زنده + بازیابی کامل — PASS (SLO تأخیر: FAIL) |
| سوک ۱۰۰۰/s ×۱۵m (فشردهٔ ۲۴h) | ۹۶ | ۹۳٬۳۴۷ | ۵٬۲۸۵ms | ۱٫۵۱٪ | ۳٬۰۳۶ | **۰** | مرگ OOM در انتها — FAIL (زیر ۶٫۸× اضافه‌بار) |

- SLOها: failed<0.1% ✓ (بجز سوک) · p95<300ms ✗ (اشباع جعبه) · sync p95<400ms ✗ · write_errors<0.1% ✓ **در همه**.
- **SKIP/NOT-RUN صادقانه:** سوکِ ۲۴ ساعتِ واقعی = NOT-RUN (نسخهٔ فشردهٔ ۱۵m اجرا شد؛ اسکریپت ۲۴h آماده: `SCENARIO=soak SOAK_DURATION=24h`) · فشار ۵۰۰۰ = ۵۰۰۰ iter/s عرضه با سقف مولد ۳۰۰ VU (۵۰۰۰ VU هم‌زمان روی جعبهٔ مشترک ۲GB معنا نداشت) · CI گیت‌هاب = NOT-RUN (انسداد billing؛ همهٔ jobها steps=0/0 — سند: HANDOFF ویو ۲۳) · اعداد ملی ۱۰M کاربر/۲۰k rps = NOT-RUN (نیازمند استیجینگ چندنمونه‌ای؛ سوئیت آماده).

## سه یافتهٔ معماری (کمّی‌شده) + ریشه‌یابی OOM و رفعِ بوت

1. **هیدراتاسیون کامل بوت ⇒ OOM گارانتی‌شده.** ریشه: `hydrateStoreFromPg` با `SELECT *` کلِ هر SCHEMA_TABLES را در RAM می‌ریزد — دیتاست ملی ۲۴M سطر؛ در ۲GB، FatalProcessOutOfMemory در همان بوت. **رفع (اعمال‌شده):** envهای `PAYESH_PG_HYDRATE_LIMIT=tbl:cap` (با `ORDER BY id LIMIT`) و `PAYESH_PG_HYDRATE_SKIP=tbl,…` در `server/db.js` — پیش‌فرضِ کد دست‌نخورده (رفتار قبلی حفظ شد؛ کانترکت تست‌ها تغییر نکرد).
2. **~۱MB حافظهٔ زنده به‌ازای هر درخواستِ هم‌زمانِ فعال** (سنجش تجربی: ۳۵۰ درخواست موازی ⇒ هیپ ۳۱→۳۴۶MB). عامل اصلی: snapshot دو‌فازیِ sync با `JSON.parse(JSON.stringify(store[c]))` به‌ازای هر درخواست — ‏O(کلِ کالکشن) نه O(batch)؛ در اشباع با صدها درخواستِ کندِ هم‌زمان ⇒ عبور از سقف هیپ ⇒ exit 134/137. پیشنهاد: snapshot با marker/version یا حذف در حالت PG-live.
3. **رشد نامحدود آینهٔ store در مسیر نوشتن (۱۰,۴۸۸ بایت/نوشتن سنجیده شد؛ store[c].push + __processed_uids) + auth با اسکن خطیِ آینه و نبود ایندکس `users.phone`** ⇒ در مقیاس ۱۰M کاربر مسیر لاگین O(N) می‌شود. پیشنهاد: قطع آینه از مسیر نوشتن در PG-live + ایندکس phone + auth از PG.

یافته‌های فرعی: tracing پیش‌فرض always_on بدون collector (برای بار: sampling/غیرفعال) · سقف‌های نرخِ تک‌IP مولد با env رسمی تنظیم شد (`PAYESH_LOGIN_IP_LIMIT/PAYESH_SMS_IP_LIMIT`) · pgbouncer روی این پروفایل لازم نبود · public-report از آینهٔ مقیّد تقریب می‌گیرد (باید به aggregation سمت PG برود).

## وضعیت پوش (با Evidence)

```
$ git ls-remote origin feat/wave18-load-test main
9cae5cfd66d56fde4c50170df526b53cb4512277   refs/heads/feat/wave18-load-test
10bebf9bb197bbf47140d58e228be1f9dd6d9d23   refs/heads/main   ← دست‌نخورده (پیشرفتِ دیگران)
```

- شاخهٔ `feat/wave18-load-test`: ۲ کامیت (`60d85c1` کار اصلی + `9cae5cf` ثبت PR) — بازسازی‌شده روی main@`20ed3e8` برای PR بدون conflict.
- PR [#94](https://github.com/rezaa2544/p2/pull/94): open · **mergeable: True** · ۹ فایل · ‏+۷۴۴/−۵۵.
- ریسکِ دوباره‌اجرای کورکورانهٔ اسکریپت بازپخش با پایهٔ قدیمی (main@4cf53af): شاخه را به conflict برمی‌گرداند و کامیت docs را می‌زد — **رد شد** (به‌روزرسانی اسکریپت با پایهٔ صریح `W18_BASE` انجام شد).

## مراجع ابزار

- `/home/user/w18-push-rebuild.sh` — بازپخش/پوش شاخهٔ Wave 18 بدون اتکا به `.git` محلی (کلون تازه → انشعاب از پایه → بازپخش ۹ فایل → کامیت → پوش → ls-remote). با `W18_BASE`/`W18_FORCE`/`W18_ORIGIN` (تست‌پذیر offline) و خواندن توکن فقط از آرگومان/env — **بدون ذخیرهٔ توکن**.
- `/home/user/w18-commit-message.txt` — پیام کامیت اصلی Wave 18.
- `/home/user/replay-push.sh` — تعمیم‌یافته برای هر شاخهٔ مانیفستی (از جمله همین شاخهٔ `docs/daily-reports-init`).

## باز است / گام بعدی

- مرج PR #94 (با بازبین) · اجرای ۵ سناریو روی استیجینگ چندنمونه‌ای مجزا برای اعداد ملی واقعی · سه اصلاح معماری §یافته‌ها (به‌ترتیب اولویت) — جزئیات در `NEXT_ACTIONS.md`.

---

## قالب Arena

```
TASK: Wave 18 — تست بار ملی (محیط production-like + ۵ سناریو + ریشه‌یابی) + راه‌اندازی پوشهٔ گزارش روزانه
FILES CHANGED: server/db.js · server/index.js · tools/w18-load-pg.sh · tests/performance/suites/national-load-test.js · tests/wave18-stress-test.js (جدید) · tests/wave18-spike-test.js (جدید) · docs/WAVE18_LOAD_TEST_REPORT.md (جدید) · docs/NATIONAL_ROADMAP_PROGRESS.md · docs/HANDOFF.md · docs/daily-reports/* (جدید، این شاخه)
WHY: گام ۱۸ نقشهٔ ملی — سنجش ظرفیت/پایداری زیر بار ملی و کشف گلوگاه‌های معماری پیش از استقرار تولید؛ و قرارداد گزارش‌دهی یکپارچه برای بازبین مستقل.
ARCHITECTURAL IMPACT: هیدراتاسیون بوت مقیّدشدنی با env (پیش‌فرض بدون تغییر)؛ کشفِ O(collection) snapshot در مسیر sync و ~۱MB/درخواستِ هم‌زمان — هر دو مستند با پیشنهاد اصلاح؛ سوئیت k6 به پروفایل واقع‌گرایانه (استخر کاربران واقعی، idهای دیتاست) ارتقا یافت.
DATABASE IMPACT: بدون تغییر شِما؛ بارگذاری دیتاست ۰٫۳ (۷٫۶GB) در PG 17.11؛ ۱۹٬۰۴۲ insert اتمیک از مسیر sync — بدون از‌دست‌رفتِ داده در هیچ سناریو (حتی OOM). ایندکس‌های موجود کفایت کردند؛ یافته: users.phone بدون ایندکس.
SECURITY IMPACT: بدون سطح حملهٔ جدید؛ توکن فقط env/آرگومان (قاعدهٔ ۱۱ — ذخیره ممنوع)؛ secret-scan 11/11؛ سقف‌های نرخِ OTP/لاگین برای مولدِ تک‌IP با env رسمی تنظیم و مستند شد.
PERFORMANCE IMPACT: سقف تک‌جعبهٔ ۲vCPU/۲GB: ~۶۳۰ rps (ترکیب ۸:۱)؛ p95 تا ~۱s در اشباع؛ صفر خطای نوشتن تا ۲۰۰۰/s عرضه؛ مرگ OOM در فشار پایدار ۳×+ — منحنی کامل در گزارش.
NEW TESTS: tests/wave18-stress-test.js (۳۴ چک) · tests/wave18-spike-test.js (۲۲ چک) — کانترکت سناریوهای stress/spike و گزارش.
ALL TEST RESULTS: smoke 547/547 PASS · check-authz exit 0 PASS · secret-scan 11/11 PASS · build --check PASS · wave18-load 38/38 PASS · wave18-stress 34/34 PASS · wave18-spike 22/22 PASS (همه روی کلونِ HEAD=9cae5cf از remote، 2026-09-12).
KNOWN LIMITATIONS: سوک ۲۴h واقعی NOT-RUN (۱۵m فشرده) · فشار ۵۰۰۰ = iter/s با سقف مولد ۳۰۰ VU · دیتاست مقیاس ۰٫۳ · همه‌چیز روی یک جعبه (اعداد = حد پایین) · CI گیت‌هاب NOT-RUN (billing، steps=0/0).
MIGRATION REQUIRED: خیر.
ROLLBACK PLAN: revert کامیت‌های شاخه (بدون شِما/داده)؛ envهای جدید اختیاری‌اند و پیش‌فرض رفتار قبلی است — حذفشان rollback کامل است.
EVIDENCE: جدول گیت‌ها (بالا) با خروجی واقعی؛ جدول ۵ سناریو؛ ls-remote با SHA؛ ‏docs/WAVE18_LOAD_TEST_REPORT.md §۲–§۶.
COMMIT: feat(wave18): national load test — staging run, bounded hydration, stress/spike suites (+ docs(wave18): ثبت PR #94)؛ و docs(reports): init daily-reports folder… (این شاخه)
SHA: 60d85c1 → 9cae5cf (Wave 18) · این شاخه: HEAD این کامیت
CI RUN: NOT-RUN — انسداد billing گیت‌هاب (۶ job، steps=0/0)؛ به‌جایش هر ۷ گیت محلی روی همان درخت اجرا و سبز شد.
RAW EVIDENCE: خروجی‌های کامل در لاگ جلسهٔ 2026-09-12؛ فایل‌های خام k6 (r1–r5) پس از بازیافت sandbox از /var/tmp حذف شدند — اعداد کلیدی همه در docs/WAVE18_LOAD_TEST_REPORT.md §۲ ثبت‌اند.
```


---

# بخش چت ۳ — دور ۸۷ (Wave 10 live) + مأموریت مهاجرت ۰۰۹ / ایندکس‌های tenant-delta / دوزیهٔ tenancy

**شاخه‌ها:** `feat/wave10-pg-live-gate` (مرج — PR #99) · `feat/migration-009-check-constraints` (این مأموریت) · CI: **NOT-RUN** (بیلینگ GitHub Actions)

## دور ۸۷ — خلاصه (مرج‌شده @ `a8bc038`)

جفت PostgreSQL 18.4 زنده (primary + **streaming replica واقعی** با cold-copy + `standby.signal`؛ بستهٔ embedded باینری `pg_basebackup` ندارد). گیت `tests/wave10-pg-live.js` → **۱۹/۱۹**: P1..P4 رپلیکیشن (streaming، انتشار WAL، رد write با 25006)، D1..D4 ‏`server/db.js` روی URLهای زنده (queryRead→replica، query→primary، fallback بی‌خطا، بازگشت خودکار S3-1)، T1..T8 ‏DDL پارتیشن‌بندی §۳.۲ (RANGE(created_at)، PK(id,created_at)، مهاجرت از heap، pruning، دیده‌شدن روی replica). جهش ۵/۵ کشته. ردیف‌های ملی ۶/۷/۱۰/۱۱/۱۳ از «⏳» به 🟡 با شواهد به‌روز شد.

## مأموریت این دور — نتایج

### فاز ۱: مهاجرت ۰۰۹ (CHECK constraints روی report_logs) — P1-2

enumها **از کد** استخراج شد، نه حدس: `kind` از کلیدهای `REPORT_ROLES` ‏(reports.js:62) = attendance/academic/finance/teachers · `format` از تنها نویسنده `rptLog()` ‏(77-reports.js: فقط `'csv'` و `'pdf'`؛ **«screen» فقط در کامنت 008 بود — هیچ مسیر کدی نمی‌نویسدش، عمداً مجاز نشد**) · `status` = generated/synced (کلاینت generated می‌نویسد؛ synced حالت پساهمگام‌سازی قرارداد 008/راهنما) · `generated_by` = digits-only یا NULL ‏(`String(S.user.id)`).

الگو: `NOT VALID` + `VALIDATE` — روی دیتابیس دارای داده هم امن اعمال می‌شود و زبالهٔ موجود را **با صدای بلند** رد می‌کند (fail-closed)، نه سبز ساکت.

گیت زنده `tests/migration-009-live.js` → **۱۳/۱۳**: زنجیرهٔ ۰۰۱..۰۰۹ روی DB خالی؛ ۱۶ ترکیب معتبر پذیرفته؛ نامعتبرها ⇒ 23514؛ down واقعی (قید حذف، نامعتبر موقتاً پذیرفته)؛ re-apply روی DB دارای ۱۷ ردیف؛ **M8c: apply روی DB دارای زباله ⇒ VALIDATE قرمز**؛ رفت/برگشت/رفت؛ M10: اجرای دوباره بدون down ⇒ 42710 (سیاست زنجیره). جهش `migration-009-live-mutations.js` → **۵/۵ کشته**. `migration-sequence` ‏۱۹/۱۹ (پس از ردیف §۸ راهنما) و `db-engineering` ‏۱۴/۱۴.

### فاز ۲: ایندکس‌های tenant/delta — با اثبات (P1-3)

بنچمارک روی PG زندهٔ سیدشده (report_logs ‏60k، attendance ‏36k، grades ‏157k)، ‏`EXPLAIN (ANALYZE, BUFFERS)` + ۳۰ تکرار:

| کوئری | قبل | بعد از `(school_id, updated_at, id)` | حکم |
|---|---|---|---|
| report_logs لیست tenant ‏(ORDER BY updated_at DESC, id DESC LIMIT 50) | 0.135ms / bench ‏1.16ms | 0.073ms / bench ‏0.53ms | ✅ ADD |
| attendance دلتای tenant ‏(school+changed>since ORDER updated_at,id LIMIT 101) | **Seq Scan ‏15.2ms** | **Index ‏0.121ms** (~۱۲۵×) | ✅ ADD |
| grades همان الگو | **Parallel Seq ‏47.2ms** | **Index ‏0.101ms** (~۴۶۷×) | ✅ ADD |
| report_logs دلتای **سراسری** (الگوی فعلی `pull.js` — بدون school_id در SQL) | 10.2ms | 9.5ms (planner ایندکس قدیم را نگه می‌دارد) | ❌ **NOT-HELPED — صادقانه ثبت شد** |

هر سه ایندکس داخل خود مهاجرت ۰۰۹ آمدند؛ بهرهٔ اصلی برای الگوی «دلتای per-tenant» است که مسیر pull به آن خواهد رفت (الان فیلتر tenant در JS انجام می‌شود).

### فاز ۳: دوزیهٔ tenancy ‏`school_id IS NULL` — بدون تغییر کد (تصمیم با ناظر)

**شمار NULLهای واقعی seed:** grades=۰، attendance=۰، classes=۰؛ **users=۵** (superadmin + ۴ edu_office — نقش‌های سراسری/منطقه‌ای، به‌حق بدون مدرسه).

| الگو | با `OR IS NULL` | بدون OR | اختلاف |
|---|---|---|---|
| grades صفحه‌بندی مدیر (157k) | 0.107ms / bench ‏1.08ms | 0.045ms / bench ‏0.68ms | ~۱.۶× |
| grades + subject_id | 5.01ms | 4.62ms | ناچیز |
| **attendance کرسر مرکب (36k)** | **Seq Scan ‏14.1ms** | **Index ‏0.074ms** | **~۱۹۰×** |

(عدد ۱۰۵ms→۰.۲۲ms سند Wave 3 مال ۲.۱۹M ردیف است؛ الگوی پلن این‌جا هم همان است: `OR IS NULL` ایندکس prefix ‏school_id را می‌کشد.)

**گزینه‌ها:**
- **الف) backfill + NOT NULL روی جدول‌های دادهٔ per-school ‏(grades/attendance/classes/…):** هزینهٔ backfill در seed = ۰ ردیف؛ حذف `OR IS NULL` از `syncdelta.tombstonesSql` و `pull.js` را ممکن می‌کند؛ **ریسک:** اگر در دادهٔ تولیدیِ واقعی ردیف NULL باشد باید اول تعیین‌تکلیف مالکیت شود؛ users را شامل نمی‌شود (NULL آن‌جا معنادار است).
- **ب) وضع موجود + ایندکس جزئی `WHERE school_id IS NULL`:** بدون تغییر قرارداد؛ سند Wave 3 نشان داد فقط 177→128ms می‌دهد — گلوگاه می‌ماند.
- **پیشنهاد فنی:** الف برای جدول‌های تراکنشی (نه users)، همراه CHECK ‏`school_id IS NOT NULL` با NOT VALID+VALIDATE در مهاجرت ۰۱۰ پس از تأیید ناظر. **هیچ تغییری در scope/dbquery/pull در این دور اعمال نشد.**

### دوزیهٔ counselor (بند ۱.۳ فاز ۱ — بدون تغییر مجوز)

**ناسازگاری مستند:** `model.json` ‏counselor را در `ins` ‏report_logs دارد و `REPORT_ROLES.academic` هم counselor را می‌پذیرد؛ **ولی منوی کلاینت (05-router.js) برای counselor صفحهٔ `reports` را ندارد** — پس counselor عملاً نمی‌تواند از UI گزارش بگیرد و `rptLog` هم برایش اجرا نمی‌شود، در حالی که مجوز نوشتن sync را دارد.
- **گزینهٔ A (بستن):** حذف counselor از `ins` ‏report_logs + از `REPORT_ROLES.academic` — هم‌راستا با UI فعلی؛ ریسک: اگر محصول بخواهد مشاور گزارش تحصیلی بگیرد، باید برگردد.
- **گزینهٔ B (گشودن):** افزودن `reports` به منوی counselor (فقط تب academic) — هم‌راستا با مجوز فعلی؛ ریسک: سطح دسترسی جدید در UI، نیازمند تست tenant-isolation برای مشاور.
- شواهد هر دو سمت ثبت شد؛ **تصمیم با ناظر؛ هیچ مجوزی تغییر نکرد.**

## باز است (این دور)

- تصمیم ناظر: tenancy (بالا) + counselor (بالا)
- PgBouncer (لایهٔ استقرار) · Wave 13 live-DAST = **NOT-RUN بدون استیجینگ** · بیلینگ CI (۶ job با steps=0)
- PR #93 (DB-native reports) هنوز باز است — **این دور عمداً واردش نشد** (قاعدهٔ ۱۱: بدون موازی‌کاری)؛ مهاجرت ۰۰۹ با آن تداخل فایلی ندارد (فایل‌های #93: reports-sql.js/routes/tests — نه migrations)

---

# بخش چت ۵ — پ۵: تعیین تکلیف صف باگ‌هانت (#76 · #74 · #80) + قفل rc34

**نویسنده:** Arena — چت ۵ (باگ‌هانت و ادغام) · **تاریخ:** 2026-09-12
**مبنای کار:** `main@c91c788` (HEAD پیش از این دور) · **شاخه‌ها:** `fix/bughunt-s7-split` @ `bf833d8` · `docs/p5-bughunt-queue` (این سند)
**قاعدهٔ حاکم:** هیچ شاخهٔ PRِ بازِ دیگران بازنویسی/force-push نشد؛ کار نو فقط روی شاخه‌های تازه از `main`.

## خلاصهٔ اجرایی

سه PRِ بازی که صف باگ‌هانت را قفل کرده بودند تعیین تکلیف شدند: **#80 و #84 و #81 مرج‌شده‌اند** (و روی `main` راستی‌آزمایی شدند)، **#76** با شاهد بندبه‌بند **superseded** اعلام شد (سه ارزشِ یکتا مستند شد، بدونِ هیچ اقدامِ بستن بدونِ تأیید مالک)، و **#74** شکافت خورد: رفع‌های غیرتوکنیِ نشست ۷ روی شاخهٔ تازه `fix/bughunt-s7-split` بازسازی و با تستِ قرمز-اول اثبات شدند (**PR #106**). هم‌زمان قفلِ مستندات از `rc33` به **`rc34`** بامپ شد چون `main` پیش از این دور **۱۱/۱۴** بود (یک سندِ تازهٔ جاافتاده + دو هشِ کهنه).

## جدول گیت‌ها (اجرای محلی روی درختِ کامیت‌شده — CI: NOT-RUN)

| گیت | دستور | خروجی واقعی | وضعیت |
|---|---|---|---|
| smoke | `node --expose-gc --max-old-space-size=2048 tests/smoke.js` | `تست دودی: 547/547 موفق` | **PASS** |
| route-url-guard (بدون رفع، روی `main`) | `node tests/route-url-guard.js` (در worktree جدا) | `route-url-guard: 0/5 — 5 FAILED` + `RG-1: process exited: exitCode=1` | **FAIL (قرمزِ اول)** |
| route-url-guard (با رفع) | همان، روی `bf833d8` | `route-url-guard: 9/9 ✅` | **PASS** |
| route-url-guard (بدونِ seed) | `mv server/data/payesh.json …` سپس اجرا | `⏭️ SKIP — دانهٔ نمایشی موجود نیست` · `exit=2` | **SKIP (صریح)** |
| sync-del-mirror | `node tests/sync-del-mirror.js` | روی `main` `5/7` ❌ → روی `bf833d8` `sync-del-mirror: 7/7 موفق ✅` | **PASS** |
| idb-migration-queue | `node tests/idb-migration-queue.js` | روی `main` `6/7` (M6) → `جمع: 7 موفق، 0 ناموفق از 7` | **PASS** |
| idb-persistence | `node tests/idb-persistence.js` | `نتیجه تست‌های IndexedDB: 16/16 تست موفق` | **PASS** |
| جهش‌های شکافت | `node tests/pr74-mutations.js` | `جهش‌های PR #74: 3/3 — همه کشته شدند ✅` | **PASS** |
| unit suite | `node tests/run.js` | `نتیجه: 35/35 تست موفق` | **PASS** |
| secret-scan | `node tests/secret-scan.js` | `secret-scan: 11/11 سبز ✅` | **PASS** |
| check-authz | `node tools/check-authz.js` | `exit=0` | **PASS** |
| build check | `node build.js --check` | `exit=0` | **PASS** |
| a11y-modal-focus (+جهش) | `node tests/a11y-modal-focus.js` | `9/9 ✅` · جهش‌ها `5/5` | **PASS** |
| a11y-regressions | `node tests/a11y-regressions.js` | `جمع: 54 قبول، 0 رد` (شمارِ جاری `main`؛ از ۴۶ رشد کرده) | **PASS** |
| a11y-keyboard | `node tests/a11y-keyboard.js` | `جمع: 92 قبول، 0 رد` (پس از نصبِ playwright + chromium) | **PASS** |
| docs-freeze-marker | `node tests/docs-freeze-marker.js` | پیش از بامپ: `11 موفق / 3 ناموفق` ❌ → پس از `rc34`: `14 موفق / 0 ناموفق` | **PASS (پس از بامپ)** |
| CI گیت‌هاب | Actions | بیلینگ غیرفعال — هیچ نتیجه‌ای برداشت نشد | **NOT-RUN** |
| مانور زندهٔ WAL | `node tests/wal-disk-full.js` (بدونِ `--skip-live`) | نیازمند PostgreSQL + tmpfs + root | **NOT-RUN (قید محیط)** |

## فاز ۱ — حکم PR #76: **superseded** (با شاهد بندبه‌بند)

`#76` (`feat/wave19-wal-drill`، tip `57661d87`، `mergeable=false`/`dirty`، ۴ کامیت، ۷ فایل، +۸۹۲/−۲) واریانتِ **قدیمی‌ترِ** مانورِ WAL است؛ موجِ بعدی (#78 مرج‌شده + #81 مرج‌شده + اصلاحاتِ مالک روی همان مانور) همان کار را کامل‌تر روی `main` دارد:

| قلمِ #76 | وضعیت روی `main` | حکم |
|---|---|---|
| `tests/wal-disk-full.js` (۵۵۰ خط) | نسخهٔ `main` (۶۰۹ خط) ابرمجموعه است: W1–W5 + بررسی‌های ایستای S1–S9 + `--skip-live` + گاردها | superseded |
| `docs/WAVE19_WAL_DRILL_REPORT.md` (۱۴۱ خط، «زنده» با ابزار `setup-pg.sh`) | نسخهٔ `main` (۱۵۸ خط، ۷ بخش، شامل §۶ «سه سبزِ کاذب») کامل‌تر است | superseded |
| `docs/DR_RUNBOOK.md` | نسخهٔ `main` بخشِ §۸ مفصل‌تری دارد (سه چهرهٔ خطا + ترتیبِ اجباریِ بازیابی)؛ نسخهٔ #76 آن را به یک ردیفِ جدول کوتاه می‌کند | superseded (نسخهٔ #76 **پس‌رفت** است) |
| `docs/PRODUCTION_READINESS_CHECKLIST.md` | ردیفِ WAL drill روی `main` از #78 **✅** است؛ نسخهٔ #76 آن را به ⏳ برمی‌گرداند | superseded (پس‌رفت) |
| `.gitignore` | `main` نسخهٔ ترمیم‌شدهٔ خودش را دارد و آزمونِ تجربی نشان داد فایل‌های untracked پنهان نمی‌شوند (`?? _probe/` دیده شد) | superseded |
| `HANDOFF.md` | `main` ورودی‌های بعدی (#78 + مرج‌ها) را دارد | superseded |
| `tools/wal-drill/setup-pg.sh` (۱۷۰ خط) | **روی `main` وجود ندارد** | **ارزشِ یکتا (۳ قلم)** |

**ارزشِ یکتای باقی‌مانده از #76 (فقط مستند، بدونِ اقدام — تصمیم مالک):**

1. `tools/wal-drill/setup-pg.sh` — بوت‌استرپِ جایگزین با سه قابلیتِ غایب در `infra/wal-drill/bootstrap.sh`: (الف) ساختِ کاربرِ اجراییِ اختصاصی وقتی `postgres` نیست، (ب) بازکردنِ تراورسِ `/home/user` به `0711` برای همان کاربر، (پ) `archive_mode=on` + `archive_command` محلی (`cp %p …`) که اجرای تجربیِ مسیرِ آرشیو/PITR را ممکن می‌کند.
2. نکتهٔ صداقتیِ گزارشِ #76 §۳-۱: در آن اجرا شاهدِ `PANIC` از سمت **کلاینت** ثبت شد، نه لاگِ سرور (روی `main` این قید نیست).
3. یافتهٔ §۴-۲: پر شدنِ مقصدِ `archive_command` **به‌تنهایی** دیتابیس را متوقف نمی‌کند (۰ ارجاع در گزارشِ `main`).

**توصیه (بدونِ اقدام):** #76 به‌عنوان superseded بسته شود؛ اگر مالک بخواهد، سه قلمِ بالا به‌صورتِ پچِ کوچک روی `main` منتقل شوند. **هیچ بستن/تغییری انجام نشد** (قاعدهٔ ۳ ابلاغ).

## فاز ۲ — شکافت PR #74 → `fix/bughunt-s7-split` @ `bf833d8` (PR #106)

شاخهٔ `bug-hunt-session7` **دست‌نخورده** ماند (نه rebase، نه force-push، نه کامیتِ تازه). شاخهٔ تازه از `main` ساخته شد و فقط چهار قلمِ در دامنهٔ ابلاغ در آن نشست:

- **S7-1** گاردِ DoS بی‌احراز هویت: `new URL(req.url, …)` بالای هندلر async و بیرونِ try/catch + نبودِ rejection handler روی wrapper ⇒ یک خط درخواستِ خام (`GET //[`) پروسه را با `exit 1` می‌کشت. حالا پارسِ دفاعی (۴۰۰) + wrapperِ fail-safe.
- **S7-5** مهاجرتِ صفِ IndexedDB: کلیدهای کهنه با «اولی-برنده» خوانده می‌شدند ⇒ با وجودِ کلیدِ جاری، صف‌های قدیمی هرگز خوانده و عملیاتِ معلق بی‌صدا گم می‌شد. حالا هر سه کلید قدیمی→جاری ادغام می‌شوند + markerِ v3 برای «دقیقاً یک ادغامِ کامل» در نصب‌هایی که با منطقِ قدیم «مهاجرت‌کرده» بودند.
- **S7-8** آینهٔ Background Sync: حذفِ دستی و بازگشت از DLQ آینهٔ IndexedDB را هم‌گام نمی‌کردند ⇒ رویدادِ بعدیِ BGSync عملی را می‌فرستاد که کاربر حذف کرده بود.
- **آزمون‌ها:** `tests/route-url-guard.js` (جدید؛ مهاجمِ سوکتِ خام) + `tests/sync-del-mirror.js` (جدید) + `tests/idb-migration-queue.js` (M6/M7) + `tests/idb-persistence.js` + `tests/pr74-mutations.js` (۳ جهش). در نبودِ دانهٔ نمایشی، `route-url-guard` صریحاً **SKIP** و `exit 2` می‌دهد (نه FATAL) — هم‌قاعده با `wal-disk-full`.
- **بیرونِ دامنه (طبق ابلاغ):** بازنویسیِ `tests/secret-scan.js`، هر تغییر در `setup-deps.ps1`، و بقیهٔ کامیت‌های نشست ۷ (S7-2 runner · S7-3 pins · S7-4 authz-model · S7-6 wave3-keyset · S7-7 arena5 · سوئیتِ cursor). **تصمیمِ توکن باز است**؛ توکن‌ها نه حذف و نه ابطال شدند.

## فاز ۳ — تعیین تکلیف #80: **مرج‌شده، راستی‌آزمایی شد**

`#80` (مودالِ تودرتو، WCAG 2.4.3) نه فقط mergeable بود بلکه **قبلاً مرج شده بود**: `mergeable=true` · `merged=true` · merge commit `9eea4df` · و `git merge-base --is-ancestor 493ef75 origin/main` ✅. شاخهٔ `fix/a11y-modal-focus-s9` روی `c469f70` نیرویِ تازه خورد؛ HEAD نهایی روی `main` است. گیت‌های a11y روی همین `main` اجرا و ثبت شدند (جدولِ بالا): modal-focus **۹/۹** · جهش **۵/۵** · regressions **۵۴/۵۴** · keyboard **۹۲/۹۲** · smoke **۵۴۷/۵۴۷**. هیچ rebase/مرجِ تازه لازم نبود.

## فاز ۴ — قفل، رجیستری و آینهٔ `reza/`

- **قفل:** `main` پیش از این دور **۱۱/۱۴** بود: مانیفستِ `rc33` ‏۳۱۱ ردیف داشت در برابرِ ۳۱۲ سند، `WAVE18_LOAD_TEST_REPORT.md` جا افتاده بود، و هشِ `docs/HANDOFF.md` و `docs/NATIONAL_ROADMAP_PROGRESS.md` کهنه بود. با این دور `rc34` ساخته شد (۳۱۳ ردیف، هم‌گام با دیسک) و `rc33` بنرِ تاریخی گرفت.
- **رجیستری:** ردیفِ باندلِ این دور با `sha256` و رأسِ بسته‌شده ثبت شد (قاعدهٔ ۳ و ۴ همان سند؛ رأس = کامیتِ پیش از کامیتِ ردیف، چون ردیف `sha256` باندلِ خودش را حمل می‌کند).
- **آینهٔ `reza/`:** فهرستِ موجود اول خوانده شد. از اسنادِ لمس‌شدهٔ این دور، تنها `docs/NATIONAL_ROADMAP_PROGRESS.md` آینهٔ محتوایی در `reza/` دارد (هشِ یکسان) و همان **هم‌گام شد**. بقیهٔ اسنادِ لمس‌شده (گزارش روزانه، ROADMAP روزانه، NEXT_ACTIONS، BUNDLE_REGISTRY) **آینه ندارند** و طبقِ ابلاغ ساخته نشدند. سه فایلِ `reza/index.html` · `reza/payesh.html` · `reza/USER_GUIDE.html` از 2026-09-08 (کامیت `367a4d2`) **اسنپ‌شاتِ بستهٔ بسته‌بندی‌اند**، نه آینهٔ زندهٔ build فعلی، و در این دور **دست‌نخورده** ماندند (تصمیم با مالک — ردیفِ باز در NEXT_ACTIONS).

## یافته‌هایی که خودم رد کردم/اصلاح کردم

1. **فرض «CI سبز»:** در حالتِ راستی‌آزمایی، `check-runs` مربوط به PR #84 هفت jobِ `in_progress` نشان می‌داد. چون بیلینگ غیرفعال است و هیچ نتیجهٔ کامل‌شده‌ای نیست، در این گزارش همه‌جا **NOT-RUN** ثبت شد، نه «سبز».
2. **ادعای «#76 کاملاً superseded»:** با diffِ محتوایی رد شد — سه قلمِ ارزشِ یکتا واقعاً روی `main` نیست (بندِ §۱). حکم به «superseded با سه استثنا» اصلاح شد.
3. **`a11y-keyboard` ابتدا `exit=3` داد** (playwright نصب نبود). ادعای ۹۲/۹۲ فقط **پس از** نصبِ playwright + `install-deps` و اجرای واقعی ثبت شد؛ پیش از آن NOT-RUN.
4. **`exit=3` را «سبز» نگرفتم:** آزمونِ مرورگری بدونِ مرورگر یعنی NOT-RUN، و همان‌جا صریح ثبت شد.
5. **درختِ کاریِ آلوده در آغاز:** `git status` هفتادوهشت فایلِ تغییرکِرده نشان داد که ۵۵ فایلش با `origin/main` و بقیه با شاخهٔ بستهٔ نشستِ ۹ یکی بود (بازماندهٔ snapshot). پیش از هر کار، دیف و tar پشتیبان گرفته شد (`/home/user/state-backup-2026-09-12/`) و سپس `reset --hard origin/main` اجرا شد.
6. **آزمونِ «red-first» در محلِ اصلی اجرا نشد:** اثباتِ قرمز در **worktree جدا** (`/tmp/wt-red` روی `origin/main`) انجام شد تا درختِ شاخهٔ در حالِ کار آلوده نشود.

## وضعیت پوش (`git ls-remote`)

| شاخه | HEAD محلی | `git ls-remote` | وضعیت |
|---|---|---|---|
| `fix/bughunt-s7-split` | `bf833d8` | `bf833d8749622f6da57f1ce7cec602d00aac1858` | ✅ برابر (PR #106 باز) |
| `docs/p5-bughunt-queue` | `ab6049b` | `ab6049b6…` | ✅ برابر، سپس **مرج شد** (PR #107) |
| `fix/a11y-modal-focus-s9` | — (شاخهٔ محلی در بازیابیِ snapshot نبود) | `493ef75b…` | ✅ مرج‌شده؛ `merge-base --is-ancestor 493ef75 origin/main` ✅ |
| `fix/wave19-wal-drill-s9` | — | `5e2fbf5…` | ✅ مرج‌شده (مرج `9e7da2c`) |
| `bug-hunt-session9` | — | `45e32cc…` | ✅ مرج‌شده (مرج `886e2da`) |

**`main` قبل و بعد این دور:** قبل `c91c7888ee434fdb5b08dd0297b31983c2265af2` · بعد `b0132c5fa9736bb0489c4a182f7f23ff3bd59208` (مرجِ PR #107، فقط-مستنداتی طبق قاعدهٔ ۱۰). هیچ پوشِ مستقیمی روی `main` انجام نشد.

**شاهدِ پس از مرج:** نشانگرِ قفل روی `main` تازه = `نتیجه: 14 موفق / 0 ناموفق (از 14)` (پیش از این دور: `11 موفق / 3 ناموفق`).

**به‌روزرسانیِ پایانی:** کامیتِ همین بخش (شاهدِ پوش) در PR #108 مرج شد؛ `main` پایانی این دور = `a800560d5a235b22af20646eebaafa1f1c258e82`.
> **تصحیحِ خودم:** در نسخهٔ نخستِ همین بخش، حجمِ باندل اشتباه نوشته شده بود (۵۵٬۳۹۸٬۴۰۹ به‌جای ۵۸٬۰۷۶٬۴۲۱ بایت — عددِ درست با `stat -c %s` سنجیده و این‌جا اصلاح شد).

**باندل:** `chat5-p5.bundle` (۵۸٬۰۷۶٬۴۲۱ بایت ≈ ۵۵٫۴ مگابایت — `stat -c %s`) رأسِ `docs/p5-bughunt-queue` را در `11febdb` — یعنی **پیش از** کامیتِ ردیفِ رجیستری و کامیتِ شاهدِ پوش — ثبت کرده است؛ همان رأسِ درج‌شده در ردیفِ رجیستری. کامیت‌های بعدی (`ab6049b` و مرجِ `b0132c5`) روی `origin` هستند و از طریق `main` بازیابی می‌شوند.

## باز است / گام بعدی

- **تصمیمِ توکن** (حذف/ابطالِ توکنِ `setup-deps.ps1`) — فقط مالک؛ تا آن زمان دو فایلِ بیرون‌دامنه دست‌نخورده می‌مانند.
- **تعیین تکلیف #76** (پیشنهاد: بستن به‌عنوان superseded؛ انتقالِ سه قلمِ یکتا به‌صورتِ پچِ کوچک در صورتِ تمایل).
- **#74** تا تعیین تکلیفِ توکن باز می‌ماند؛ رفع‌های غیرتوکنی‌اش در PR #106 پیگیری می‌شوند.
- **معیار بازفعال‌سازی ایندکس ۰۱۲** (`(school_id, created_at DESC, id)`): مطابق `2026-09-12-wave3.md` §… و ردیفِ NEXT_ACTIONS — فعلاً «آزاد/در انتظارِ اثباتِ Seq Scan روی مسیر داغ».
- **اسنپ‌شاتِ `reza/*.html`** (۲۰۲۶-۰۹-۰۸) — نیازمندِ تصمیمِ مالک برای بازتولید یا تثبیتِ نسخه.

---

# بخش چت — مأموریت P0-3 (آفلاین E2E) + P1-1 (PDF E2E) · 2026-09-12

**شاخهٔ فیچر:** `feat/p03-offline-e2e-pdf` (مبنا: main@805a04e) · **CI RUN = NOT-RUN (بیلینگ GitHub Actions)** — همهٔ سنجه‌ها اجرای محلی sandbox.

## P0-3 — زنجیرهٔ کاملِ آفلاین E2E

**تست جدید:** `tests/offline-e2e.js` — ۲۳/۲۳ سبز + ۲ NOT-RUN ثبت‌شده.

زنجیره (همه در یک اجرا): OFFLINE (fetch قطع) → تولید op → صف (`sms_syncq_v1`) → **restart واقعی مرورگر** (دو نمونهٔ jsdom مستقل با بک‌استورِ دیسکِ مشترکِ تزریقی — نه حافظهٔ مشترک؛ نمونهٔ اول بی‌فرصتِ flush بسته می‌شود) → ONLINE → sync خودکار → POST `/api/sync` به **server/sync.js واقعی** (فیلدگیت/authz/OCC/idempotency) → persistence (`persistOpsBatch`) → reconciliation کلاینت → dedupe (بازپخشِ دسته ⇒ `duplicate_ignored` + اثرِ تکی) → retry+backoff نمایی روی 503 گذرا (4000ms→سقف 300000ms) → conflict (سرور v4 در برابرِ base_version=2 ⇒ `conflict_preserved` + رکورد دست‌نخورده + notification داوری مدیر + status=conflict ماندگار در دیسک) → گاردِ tenant در هر push (نوشتنِ بین‌مدرسه‌ای ⇒ `school_mismatch`/`out_of_scope`؛ جعلِ by ⇒ رد).

**باگِ واقعیِ کشف و رفع‌شده (کامیت جدا):** صفِ آفلاین در بوت پیش از `loadQueue` بازنویسی می‌شد — هر `enqueueOp` مولدِ دموی بوت، `sms_syncq_v1` نشستِ قبل را له می‌کرد ⇒ تغییراتِ آفلاین پس از restart مرورگر بی‌صدا گم می‌شدند (E4 قرمزِ اولیه). رفع: پرچم `_QUEUE_LOADED` + ادغامِ uid ی قلم‌های دیسکی در `saveQueue` پیش از اولین نوشتن (src/js/27-sync.js).

**جهش‌سنجی:** `tests/offline-e2e-mutations.js` — **۷/۷ kill** + baseline-green:
| جهش | چه چیزی حذف شد | کشنده |
|---|---|---|
| M1 | dedupe سرور (هر دو لایه: چکِ validate + ادعای اتمیک apply) | E11/E12 |
| M2 | retry کلاینت (گذرا ⇒ rejected) | E13 |
| M3 | persistence صف (saveQueue ننوشت) | E2/E4 |
| M4 | گاردِ tenant سرور (هر سه لایه: fieldGate + op-level + inScope) | E21/E22 |
| M5 | بلعِ conflict (OCC خنثی) | E17/E18 |
| M6 | backoff نمایی (تأخیر ۰) | E15 |
| M7 | گاردِ ادغامِ پیش-از-load (رگرسیون باگ بوت) | E4 |

**NOT-RUN صادقانه:** (۱) Service Worker Background Sync با تبِ بستهٔ واقعی — jsdom ‏SW ندارد؛ منطقِ آینهٔ IDB در `tests/bgsync.js` (۱۴/۱۴) پوشش دارد. (۲) persistence روی PG زنده — DATABASE_URL در این اجرا ست نبود؛ persistence روی لایهٔ `persistOpsBatch` (همان قراردادی که db.js پیاده می‌کند) سنجیده شد؛ مسیرِ PG زنده در تست‌های wave10 موجود است.

## P1-1 — PDF E2E از مسیرِ واقعیِ UI

**تست جدید:** `tests/reports-pdf-e2e.js` — **۸/۸ سبز، صفر SKIP** (playwright-core + chromium-headless-shell + pdftotext در sandbox نصب شد).

مسیر: روت `reports` → گارد `canRoute` → `render()` → **کلیکِ واقعیِ** دکمهٔ `data-act="rpt-print"` → `rptPrint()` → `printableDoc()` (HTML عیناً از قلابِ window.open — نه سندِ تست‌ساخته) → Chromium → `page.pdf()` → امضای `%PDF-` + حجم → `pdftotext` → **نشانگرِ tenant**: نام مدرسهٔ مدیر در متنِ PDF حاضر و نام مدرسهٔ دیگر غایب.

- **سناریو A (مدیر مدرسهٔ ۱، مجاز):** A1 canRoute+دکمه در DOM · A2 کلیک ⇒ سند A4 واقعی + `report_logs(format='pdf')` · A3 سند tenant-scoped · A4 PDF واقعی (‎%PDF، ~۴۷KB) · A5 متنِ PDF نشانگرِ tenant دارد/نشت ندارد.
- **سناریو B (student، غیرمجاز — رد پیش از تولید):** B1 `canRoute('reports')=false` · B2 دکمهٔ چاپ در DOM نیست و renderRoute صفحهٔ ممنوع است · B3 حتی صدای مستقیم `rptPrint` هم `rptSchools()=[]` (بدون دادهٔ هیچ مدرسه‌ای).
- قاعدهٔ صداقت (الگوی reports-export): بدون playwright-core یا pdftotext ⇒ SKIP صریح، نه سبزِ جعلی. `tests/reports-export.js` موجود **دست‌نخورده** (۴ سبز + ۱ SKIP playwright کامل — چون فقط playwright-core نصب است، نه playwright).

## Evidence (اجرای محلی، درختِ کامیت‌شده)

| گیت | نتیجه |
|---|---|
| tests/offline-e2e.js | ۲۳/۲۳ ✅ (+۲ NOT-RUN ثبت‌شده) |
| tests/offline-e2e-mutations.js | ۷/۷ kill · baseline-green ✅ |
| tests/reports-pdf-e2e.js | ۸/۸ ✅ (A: مجاز، B: رد پیش از PDF) |
| tests/reports-export.js (دست‌نخورده) | ۴ سبز + ۱ SKIP ✅ |
| tests/run.js | ۳۵/۳۵ ✅ |
| tests/smoke.js | ۵۴۷/۵۴۷ ✅ |
| build --check + check-authz | ✅ |
| secret-scan | ۱۱/۱۱ ✅ |
| sync-queue-caps | ۳۶/۳۶ ✅ |
| sync-sending-revive / wave7-offline-queue / sync-dup-claim / bgsync / sync-dlq-retry / sync-lastsync | ۷/۷ · ۷/۷ · ۷/۷ · ۱۴/۱۴ · ۷/۷ · ۸/۸ ✅ |
| wave3-parity | self-skip بدون DATABASE_URL (exit 0) — طبق طراحی |
| CI GitHub Actions | **NOT-RUN — بیلینگ** |

## وضعیت PR

- **PR #105** (feat/p04-national-dataset-relations@ee3a838): باز — منتظر ACCEPT بازبین؛ دست‌نخورده.
- این دور: PR فیچر جدید از `feat/p03-offline-e2e-pdf` (باز می‌ماند تا ACCEPT) + PR فقط-مستنداتی (fast-merge قاعدهٔ ۱۰).

---

# بخش چت ۵ — پ۰: بهداشت ورک‌اسپیس سندباکس (۲۳۲MB → ۸۹٫۴MB) + قفل `rc35`

**تاریخ:** ۲۰۲۶-۰۹-۱۲ · **شاخه:** `docs/p0-workspace-hygiene` از `main@86c0fe1` · **CI گیت‌هاب:** NOT-RUN (بیلینگ)
**سقف snapshot:** ۱۲۸MB / ۱۰٬۰۰۰ فایل · **هدف:** ≤۱۰۰MB / ≤۸٬۰۰۰ فایل

## خلاصهٔ اجرایی

ورک‌اسپیس با **۲۳۲MB** و بالای سقفِ snapshot بود (خطرِ «files were not saved»). پس از
موجودیِ فقط‌خواندنی، حذفِ **دروازه‌دار** (هر `rm` با یک خط شاهد: sha256/ردیف رجیستری/`ls-remote`)،
هرسِ refها و `repack`، به **۸۹٫۴MB / ۱٬۳۴۱ فایل** رسید ⇒ **PASS**. هیچ قلمِ بی‌شاهد حذف نشد؛
دو فایلِ یگانه که در هیچ ref نبودند **حفظ** شدند. رویهٔ ماندگارِ این کار در
`docs/WORKSPACE_HYGIENE.md` ثبت و کتابخانه با `rc35` دوباره منجمد شد.

## فاز ۰ — موجودیِ فقط‌خواندنی (پیش از هر حذف)

| مسیر ریشه | حجم | فایل | × |
|---|---:|---:|---|
| `bandle/` (باندل/patch، بدون `.git`) | ۱۰۲M | ۱۸ | — |
| `p2/` (کلون کانونیکال؛ `node_modules` غایب، `.git`=۲۰M) | ۶۰M | ۱۳۹۰ | — |
| `state-backup-2026-09-12/` (شاملِ تارِ ۱۲M) | ۱۲M | ۳ | — |

- **کلون‌های تکراری ادعاشده وجود نداشتند** (`p-`، `wave22-*`، `stagin`، `verify-*`، `replay-manifest`، … = ۰ مورد).
- `fetch` همهٔ refها برای راستی‌آزماییِ پوشش: `.git` ۲۰M→۷۹M (راوِ کل: ۲۳۲M).
- هر ۵ باندل: `git bundle verify` OK و `sha256` ≡ مانیفست؛ رأس‌ها جدِ refهای `origin` بودند.
- رجیستری گیت: ۵۴ شاخهٔ محلی — **هر ۵۴** «عیناً روی origin یا جدِ آن»؛ ۳ تگِ محلی (روی `origin` نیستند ⇒ **نگه‌داشته شدند**).

## فاز ۱–۲ — طبقه‌بندی و حذفِ دروازه‌دار

هر قلم یک خط شاهد گرفت (`/tmp/deletion-evidence.log`، ساعت ۱۵:۲۶). خلاصه (تفصیلِ شاهد و «بازیابی» در
`docs/BUNDLE_REGISTRY.md §۷`):

| دسته | اقلام | حجم | شاهد |
|---|---|---:|---|
| باندل‌های loose | `chat5-full-history` · `chat5-work` · `chat5-session7` · `-continued` · `-final` | ۸۰M | `verify` OK + sha256 ≡ مانیفست + رأس‌ها روی `origin` (۱۰۵ ref عیناً + ۷ جد) |
| تاربال‌های worktree | `chat5-session9-worktree.tar.gz` · `chat5-worktree-backup.tar.gz` · `state-backup/p2-worktree-dirty.tar.gz` | ۳۶M | ۱۱۷۳+۱۱۲۰+۱۱۷۱ مسیر با blobهای موجود در refها؛ فقط ۲ خروجیِ gitignored بی‌اهمیت |
| کش بیلد | `.build-cache.blob` · `.build-cache.meta.json` | ۱٫۶M | `.gitignore` خط‌های ۶۷/۶۸ |
| refها | ۱۴۶ از ۱۵۹ ref محلی/ریموتی | — | «عیناً روی origin یا جد» برای ۵۴/۵۴ شاخه؛ فهرست در `state-backup-2026-09-12/dropped-refs-local.txt` |

**قاعدهٔ توقف اجرا شد:** دو فایلِ `src/js/00-migration.js` و `src/js/27-sync.js` در هیچ ref محلی/ریموتی
نبودند (NOT-FOUND) ⇒ حذف **نشدند**؛ به `state-backup-2026-09-12/preserved-unpushed/` منتقل شدند.

## فاز ۳ — دایرکتوری سنگین و عملیات

- **checkout ها:** ≤۲ در همهٔ لحظات؛ ورک‌تری‌ها (`/tmp/wtmain2`، `/tmp/wtfinal`، `/tmp/wt-red`، ورک‌تریِ اثباتِ این دور) حذف شدند ⇒ اکنون `git worktree list` = **۱**.
- **`node_modules`:** در موجودی غایب بود؛ برای شاهدِ گیت‌ها `npm ci` (۱۰۵ پکیج) اجرا شد. این دایرکتوری در snapshot ذخیره **نمی‌شود** ⇒ بازیابی در نشست جدید: `npm ci`.
- **`repack`:** `--window=50 --depth=50` ⇒ `.git` = ۵۴M. تلاشِ تهاجمی‌تر (`window=250`) با `pack-objects died of signal 9` (OOM) کشته شد؛ تکرار نشود.

## فاز ۴ — سند بهداشت + قفل `rc35`

- **`docs/WORKSPACE_HYGIENE.md`** (سند تازهٔ ریشه): ۹ قاعده — بودجه‌سنجیِ مؤثر (فهرستِ دایرکتوری‌های معاف)، کلونِ یگانه/≤۲ checkout، push و باندل‌سازی پیش از عملیات پرریسک، چهار شرطِ حذفِ باندل، قاعدهٔ تولید→لود→حذف، سیاستِ `node_modules`، «گزارش فقط داخل ریپو»، بهداشتِ `.git`، و جدولِ الگوی همین دور.
- **`BUNDLE_REGISTRY.md §۷`**: جدولِ ۱۱ قلمِ حذف‌شده + دستور بازیابی (طبق §۶ بند ۳: افزودن ردیف با بامپ).
- **`DOCS_INDEX.md`**: ردیفِ سند بهداشت (ضدیتیم: ۲ سند به آن ارجاع می‌دهند) · **`DOCS_METRICS.md`**: اعدادِ تازه (۳۱۵ ردیف/۳۱۶ سند ریشه/۳۳۶ درخت).
- **قفل `rc35`**: **۳۱۵** ردیفِ مانیفست = **۳۱۵** سند روی دیسک (بیرون از خودِ سند)؛ `rc34` بنرِ تاریخی گرفت و **مانیفستش دست‌نخورده** ماند (`sha256` خودِ `rc34` در مانیفستِ تازه ثبت شد).
- **تغییر تست:** تنها ۴ ارجاعِ نسخهٔ قفل در `tests/docs-freeze-marker.js` از `rc34` به `rc35` به‌روز شد. **هیچ تستی ضعیف، حذف یا استثنا نشد** (سنجه‌های قفل ۱۴/۱۴).

## جدول گیت‌ها (اجرای محلی روی شاخهٔ حاویِ همهٔ تغییرها · CI: NOT-RUN)

| گیت | دستور | نتیجه |
|---|---|---|
| قفل مستندات | `node tests/docs-freeze-marker.js` | **۱۴/۱۴** ✅ |
| هم‌خوانی اسناد | `node tests/docs-consistency.js` | **۲۲/۲۲** ✅ |
| پوششِ نمایه | `node tests/docs-index-coverage.js` | **۷۵/۷۵** ✅ |
| سلامت اسناد | `node tests/docs-health.js` | **۹/۹** ✅ |
| سنجه‌های اسناد | `node tests/docs-metrics.js` | **۱۱/۱۱** ✅ |
| چکِ هم‌خوانی | `bash tools/docs-consistency-check.sh` | **۴۹ هماهنگ / ۰ تعارض** ✅ |
| اسکنِ راز | `node tests/secret-scan.js` | **۱۱/۱۱** ✅ |
| اجراکنندهٔ تست | `node tests/run.js` | **۳۵/۳۵** ✅ |
| تست دودی | `node tests/smoke.js` | **۵۴۷/۵۴۷** ✅ |
| فرادادهٔ اسناد | `node tests/docs-metadata.js` | **۱۶/۱ ❌ — قرمزِ ارثی از `main`** |

### قرمزِ ارثی `docs-metadata` (بدون دخالتِ پ۰)

شاهد: ورک‌تریِ فقط‌خواندنی روی `origin/main@86c0fe1` **دست‌نخورده** همین تک‌شکست را داد:
سه سندِ یتیم `docs/daily-reports/2026-09-12-p04-national-dataset.md`، `…-p06-review-fixes.md`،
`…-p06-sync-oom-arch.md` (کامیت‌های `01a9387` و `ebf8852` — کارِ چت‌های دیگر). مالکیت: همان چت‌ها؛
پ۰ این اسناد را تغییر نداد. (ورک‌تری پس از اثبات حذف شد.)

## بودجه — قبل/بعد

| مقطع | سنجه | حجم | فایل |
|---|---|---:|---:|
| شروع (پیش از `fetch`) | راوِ `du` | ≈۱۷۴MB | ~۱٬۴۱۱ |
| پس از `fetch` همهٔ refها | راوِ `du` | ۲۳۲MB | ۱٬۵۱۹ |
| پس از حذفِ باندل‌ها و تاربال‌ها | راوِ `du` | ۱۳۲MB | ۱٬۵۱۲ |
| پس از حذفِ تارِ state + کشِ بیلد + `repack` | **مؤثر** | **۹۰٫۵MB** | ۱٬۳۳۹ |
| **پایانی (پس از پاک‌سازیِ خروجی‌های تولیدیِ تست‌ها)** | **مؤثر** | **۸۹٫۴MB** | **۱٬۳۴۱** |

**حکم: PASS** (≤۱۰۰MB و ≤۸٬۰۰۰ فایل). «مؤثر» = بدونِ دایرکتوری‌های معافِ snapshot
(`node_modules`، `.cache`، `dist`، `build`، `coverage`، `.venv`، …)؛ همین معیار در سندِ بهداشت مستند شده است.

## صداقت و محدودیت‌ها

1. **CI گیت‌هاب = NOT-RUN** (بیلینگ فعال نیست)؛ هیچ‌جا «CI سبز» ادعا نشده.
2. `docs-metadata` روی `main` قرمز است و **قرمزِ ارثی** است (شاهدِ بالا)؛ رفعش به چت‌های مالک مربوط است.
3. `main` زنده حرکت می‌کند: این شاخه از `86c0fe1` زده شد؛ پیش از مرج، نمونهٔ تازهٔ `ls-remote` لازم است.
4. باندلِ `chat5-p5.bundle` روی دیسک نیست (ریستِ پیشین)؛ **ادعای وجودش نمی‌شود** — محتوایش روی `origin` است.
5. `node_modules` پس از ریستِ سندباکس غایب است ⇒ گیت‌های وابسته تا `npm ci` = NOT-RUN (گیت‌های pure-node مثل قفل، بدونِ آن هم سبزند).

---

# بخش چت ۵ — پ۵ب: دورِ پس از بهداشت (سبزکردنِ قرمزهای ارثی + آینه‌های `reza/` + صف ادغام)

**تاریخ:** ۲۰۲۶-۰۹-۱۲ · **شاخه:** `docs/p5b-orphans-reza-sync` از `main@c04955c` · **CI گیت‌هاب:** NOT-RUN (بیلینگ)
**کامیت‌ها:** `214332b` (مستندات + قفل `rc36`) · `1aa37ef` (آینه‌های `reza/`) · آخرین: بخشِ همین گزارش

## خلاصهٔ اجرایی

`main` جاری **چهار گیتِ قرمزِ ارثی** داشت، نه یکی. همه با «تستِ قرمز → رفع → تستِ سبز» بسته شدند
(بدونِ هیچ ضعیف/حذف/استثنا کردنِ تست). هم‌زمان دو آینهٔ رانش‌کردهٔ `reza/` همگام و صفِ ادغام
از نو راستی‌آزمایی شد: **#106 مرج شده بود** (کشفِ تازه) و **#76** همچنان باز/`dirty` و فقط ثبت شد.

## فاز ۱ — قرمزهای ارثی: baseline ← رفع ← شاهدِ سبز

| گیت | baseline روی `main@c04955c` | ریشه | رفع | پس از رفع |
|---|---|---|---|---|
| `tests/docs-metadata.js` | **۱۶/۱** — سه سند یتیم | پوشهٔ `daily-reports/` در نمایه ثبت نبود | §۲.۱۳ تازه در `DOCS_INDEX.md` (۱۰ سند) | **۱۷/۱۷** ✅ |
| `tests/documentation-map-coverage.js` | **۲۰/۳** — ۳۳۹≠۳۴۶ · ۳۱۴≠۳۱۶ · «صفر یتیم» | عددهای لحظهٔ `rc34` | `node tools/docs-stats-sync.js` + رفعِ یتیمی | **۲۳/۲۳** ✅ |
| `tools/docs-stats-sync.js --check` | **۹ ردیفِ کهنه** | همان | بازتولیدِ اعداد (نقشه/سنجه‌ها/پوششِ تست) | **۰ کهنه** ✅ |
| `tests/docs-consistency.js` | **۱۹/۳** | بازهٔ مهاجرت روی دیسک `۰۰۱–۰۱۰`، در سند `۰۰۱–۰۰۹` | به‌روزرسانیِ `DOCS_INDEX.md` + `RELEASE_NOTES.md` | **۲۲/۲۲** ✅ |
| `tools/docs-consistency-check.sh` | **۴۷ هماهنگ / ۲ تعارض** | همان | همان | **۴۹ / ۰** ✅ |
| `tests/migration-sequence.js` | **۱۵/۴** | `010_users_phone_auth.sql` بدونِ `BEGIN/COMMIT` و بدونِ ردیفِ §۸ | تراکنشی‌کردنِ forward+down (قرارداد §۳۰) + ردیفِ ۰۱۰ در `MIGRATION_GUIDE.md` | **۱۹/۱۹** ✅ |
| `tests/db-engineering.js` | **exit ۱** | همان علت | همان | **۱۴/۱۴** ✅ |
| `tests/load-test-results-coverage.js` | **۷۹/۱** | عبارتِ قراردادیِ وضعیت حذف شده بود | بازگرداندنِ «هنوز هیچ اجرای واقعی انجام نشده» (بدونِ تغییرِ معنا) | **۸۰/۰** ✅ |

**نکتهٔ صداقتیِ مهم:** دو قلمِ این فهرست (تراکنشی‌کردنِ `migrations/010_*.sql`) **کدِ چتِ دیگر** است که PR #115 مرج کرده بود.
رفعش مکانیکی و مطابقِ قراردادِ خودِ مخزن بود (گیتِ `migration-sequence` آن را می‌خواست) و **هیچ تغییرِ معنایی** ندارد
(`CREATE INDEX IF NOT EXISTS` ⇒ idempotent). اگر مالکِ P1-1 بخواهد، در NEXT_ACTIONS ثبت شد.

**تعارضِ دو گیت که خودم کشف و رفع کردم:** `tests/docs-metrics.js` عددِ «درخت سه‌زیرپوشه‌ای» (**۳۳۶**) را می‌خواست و
ابزارِ `docs-stats-sync.js` «درختِ کامل» (**۳۴۶**) را می‌نوشت ⇒ هر بار یکی قرمز می‌شد. ردیفِ `docs-total-row`
ابزار حالا **هر دو عدد را با تفکیک** می‌آورد (سه زیرپوشهٔ پایه ۲۰ + `daily-reports/` ۱۰). با `--check` تأیید شد که idempotent است.

## فاز ۲ — آینه‌های `reza/`: موجودی، رانش، همگام‌سازی

موجودی: **۵۰ فایل / ۸٫۳MB** در ۵ پوشه. برای هر فایل sha256 + تاریخِ آخرین کامیت گرفته شد (شاهدِ کامل در §گزارش نهایی).

| دسته | شمار | وضعیت |
|---|---:|---|
| آینهٔ زندهٔ `docs/<name>.md` | **۲۱** | ✅ همه بیت‌به‌بیت برابر (۱۹ از قبل برابر) |
| آینهٔ رانش‌کرده — **همگام شد** | **۲** | `ROADMAP.md` (a79c42d378c5 → **9855f4c01a5c**؛ ۴۷۴ خط تفاوت) · `WAVE9_PERFORMANCE.md` (69722d9f5a2b → **c0742bc27925**؛ ۲۰ خط) |
| اسنپ‌شاتِ نقطه‌ای (BH-5) | **۳ یکتا / ۴ مسیر** | `index.html` · `payesh.html` · `برنامه_نویس/index.html` **یک اسنپ‌شاتِ یکسان** (sha `817e3950f53e6ce9`) + `USER_GUIDE.html` (sha `8258a49ea27e45b0`) — همه ۲۰۲۶-۰۹-۰۸ و قدیمی‌تر از build زندهٔ ریشه |
| اسنادِ مستقلِ پکیج (بی‌همتا در `docs/`) | **۲۶** | بدون تغییر (شاملِ ۵ `README.md` پوشه‌ها — با دلیلِ مستند در ابزار مستثنا) |

- **`reza/README.md`**: بخشِ تازهٔ «وضعیتِ آینه‌ها و اسنپ‌شات‌ها» با جدول کامل (فایل · منبع · حالت · تاریخ · مالکِ بازسازی).
- **`tools/reza-mirror-check.js`** (ابزارِ تازه): اولین گیتِ `reza/` در مخزن — `exit 1` روی رانش، `--json` برای ماشین.
  (`tests/` عمداً دست نخورد تا شمارِ تست‌ها و قراردادِ `TEST_COVERAGE_REPORT` جابه‌جا نشود؛ پیشنهادِ اتصال به گیت در NEXT_ACTIONS.)

## فاز ۳ — صفِ ادغام (راستی‌آزمایی، بدونِ بستن/مرجِ بی‌مالک)

| قلم | نتیجهٔ راستی‌آزمایی | شاهد |
|---|---|---|
| **#106** | ✅ **مرج‌شده — بسته** (تغییرِ وضعیت نسبت به گزارش پیشین) | `merged=true · 2026-09-12T15:09:23Z · merge_commit=5b96897b64`؛ `bf833d87` جدِ `origin/main` (`git merge-base --is-ancestor` ✅) |
| گیت‌های محتوای #106 روی `main` | ✅ سبز | `route-url-guard 9/9` · `sync-del-mirror 7/7` · `idb-migration-queue 7/7` · **`idb-persistence 16/16`** (در راستی‌آزماییِ امروز اجرا شد) · `pr74-mutations 3/3` (۳ جهش کشته) |
| **#76** | 🟠 باز/`dirty` — **بسته نشد** | `open · mergeable=false · head=57661d87 · ۴ کامیت · ۷ فایل · +۸۹۲/−۲` |
| حکمِ #76 | **superseded با سه قلمِ یکتا** — ثبتِ ساختاری | (۱) `tools/wal-drill/setup-pg.sh` · (۲) قیدِ صداقتیِ §۳-۱ (PANIC از کلاینت) · (۳) یافتهٔ §۴-۲ (`archive_command`) |
| **BH-5** | ✅ **بسته به‌صورتِ «مستندشده»** | `reza/README.md` + `tools/reza-mirror-check.js` |
| **BH-6** | بدون تغییر («NOT-ADD» تا اثباتِ `EXPLAIN`) | همین گزارش + `NEXT_ACTIONS.md` |
| **توکن‌ها** | نگه‌داشته‌شده، بیرونِ ریپو — صفر تغییر | `node tests/secret-scan.js` ⇒ **۱۱/۱۱** |

## جدول گیت‌ها (اجرای محلی روی شاخه · CI: NOT-RUN)

| گیت | دستور | نتیجه |
|---|---|---|
| قفل مستندات | `node tests/docs-freeze-marker.js` | **۱۴/۱۴** ✅ (`rc36`) |
| هم‌خوانی اسناد | `node tests/docs-consistency.js` | **۲۲/۲۲** ✅ |
| پوشش نمایه | `node tests/docs-index-coverage.js` | **۷۵/۷۵** ✅ |
| سلامت اسناد | `node tests/docs-health.js` | **۹/۹** ✅ |
| فراداده (یتیم‌ها) | `node tests/docs-metadata.js` | **۱۷/۱۷** ✅ |
| سنجه‌های اسناد | `node tests/docs-metrics.js` | **۱۱/۱۱** ✅ |
| نقشهٔ مستندات | `node tests/documentation-map-coverage.js` | **۲۳/۲۳** ✅ |
| پوشش تست‌ها | `node tests/test-coverage-report-coverage.js` | **۳۳/۳۳** ✅ |
| عددهای مشتق | `node tools/docs-stats-sync.js --check` | **۰ کهنه** ✅ |
| هم‌خوانی (shell) | `bash tools/docs-consistency-check.sh` | **۴۹ / ۰** ✅ |
| مهاجرت‌ها | `node tests/migration-sequence.js` | **۱۹/۱۹** ✅ |
| مهندسی DB | `node tests/db-engineering.js` | **۱۴/۱۴** ✅ |
| آزمون بار (سند) | `node tests/load-test-results-coverage.js` | **۸۰/۰** ✅ |
| اسکن راز | `node tests/secret-scan.js` | **۱۱/۱۱** ✅ |
| اجراکننده | `node tests/run.js` | **۳۵/۳۵** ✅ |
| تست دودی | `node tests/smoke.js` | **۵۴۷/۵۴۷** ✅ |
| آینه‌های `reza/` | `node tools/reza-mirror-check.js` | **۲۱/۲۱ برابر · ۰ رانش** ✅ |
| منابع مرجع | `node tools/docs-refs-check.js` | **exit 0** ✅ |

## بودجه — قبل/بعد

| مقطع | سنجه | حجم | فایل |
|---|---|---:|---:|
| شروعِ این دور | مؤثر | ۸۹٫۵MB | ۱٬۳۵۷ |
| پایانی (پس از پاک‌سازیِ خروجی‌های تولیدی + `repack`) | مؤثر | **۸۹٫۶MB** | **۱٬۳۵۲** |

حکم: **PASS** (🟢 ≤۱۰۰MB / ≤۸٬۰۰۰ فایل). افزودن‌ها: `rc36` + `tools/reza-mirror-check.js` + دو سندِ به‌روزشده؛ کاسته‌ها: `.build-cache.blob` + `docs/_metadata.json` + `docs/_search-index.json` (**۳٫۱MB**، خروجیِ بازتولیدشدنیِ gitignored) و `repack` (`.git` = ۵۴M · ۰ شیءِ loose · `git fsck` ۰ خط). سنجهٔ هر دو مقطع با همان اسکریپتِ §۱ سندِ `WORKSPACE_HYGIENE.md`.

## صداقت و محدودیت‌ها

1. **CI گیت‌هاب = NOT-RUN** (بیلینگ)؛ همهٔ اعداد اجرای محلی‌اند.
2. `main` زنده حرکت می‌کند (این شاخه از `c04955c`؛ نمونه‌های تازهٔ `ls-remote` در فاز push ثبت می‌شود).
3. رفعِ قراردادِ تراکنشیِ `migrations/010_*.sql` **کدِ چتِ دیگر** بود — مکانیکی و مطابقِ گیتِ خودِ مخزن؛ در NEXT_ACTIONS برای اطلاعِ مالک ثبت شد.
4. `node_modules` در snapshot ذخیره نمی‌شود ⇒ در نشست بعد تا `npm ci`، گیت‌های وابسته NOT-RUN اند.
5. `chat5-p5.bundle` روی دیسک نیست (ریست پیشین)؛ محتوایش روی `origin` است — ادعای وجودِ فایل نمی‌شود.
# بخش ۶ — چت ۳ · تکمیل P0-1: گزارش‌های DB-native برای academic/finance/teachers + P2 + هم‌زمانی

**شاخه:** `feat/wave23-db-native-complete` (main + merge رأس PR #93 چت ۶، بدون بازنویسی) · **CI:** NOT-RUN (بیلینگ)

## چه شد

- **سه گزارش باقی‌ماندهٔ موج ۲۳ DB-native شدند** طبق طرح‌های §۴ سند و دقیقاً با دکترین endpoint حضورِ چت ۶: سازندهٔ خالص در `reports-sql.js` · فقط با `db.isPostgres()` · مسیر حافظه بایت‌به‌بایت دست‌نخورده · همهٔ خواندن‌ها `queryRead` · keyset `LIMIT n+1` · مهار اجاره‌ای در SQL.
- **رفع باگ تأییدشدهٔ `schoolHasTuition`** (red-first): ردیف PG-شکل `{type:'shahed'}` قبلاً بی‌شهریه دیده می‌شد و 400 می‌گرفت؛ حالا نوع از هر دو شکل (`school_type ?? type`) خوانده می‌شود؛ درخواست صریح مدرسهٔ بدون‌شهریه همچنان 400. جهش M8 (برگرداندن باگ) کشته می‌شود.
- **اعتبارسنجی استاندارد (P2)** روی هر ۴ endpoint (شامل حضور چت ۶): ورودی خراب ⇒ 400 در هر دو مسیر.
- **هم‌زمانی (P1-7 سبک):** ۵۰ درخواست هم‌زمان ترکیبی روی PG زنده — خطا 0/50؛ اعداد با برچسب مقیاس در §۹.۲ سند.

## Evidence (درخت کامیت‌شده، محلی)

| گیت | نتیجه |
|---|---|
| tests/wave23-reports-sql.js | **۱۱۰/۱۱۰** (۵۱→۱۱۰) ✅ |
| tests/wave23-reports-pg.js (PG 18.4 زنده) | **۷۳/۷۳** (۳۰→۷۳؛ ۳۰ قبلی دست‌نخورده) ✅ |
| tests/wave23-reports-mutations.js (جدید) | **۱۰/۱۰ کشته** ✅ |
| EXPLAIN (ANALYZE, BUFFERS) کوئری‌های جدید | grades/tuitions/installments/staff_attendance همه Index Scan؛ یافتهٔ ثبت‌شده: Seq Scan روی classes (۲۰۰ ردیف، درست)؛ ایندکس جدید **NOT-ADD** ✅ |
| tools/bench-reports-concurrency.js | 50 هم‌زمان: خطا 0/50 هر دو مسیر؛ mem p95≈185–238ms، db p95≈364–416ms @200k (یافتهٔ منفی صریح در سند) |
| reports-basic / tenant-isolation / offline | ۹/۹ · ۱۱/۱۱ · ۱۰/۱۰ ✅ |
| CI GitHub Actions | **NOT-RUN — بیلینگ** |

## دو جهش که اول زنده ماندند (و تست قوی‌تر شد)

- **M2** (حذف تطبیق مدرسه از JOIN نمره): دادهٔ parity سازگار بود ⇒ سنجهٔ `W23C-JOIN` با نمرهٔ ناسازگارِ تزریقی اضافه شد.
- **M5** (حذف LIMIT از صفحهٔ معلمان): پاسخ slice می‌شد و parity سبز می‌ماند ⇒ سنجهٔ «bounded در خودِ PG» (شمار ردیف‌های برگشتی از دیتابیس ≤ limit+1) اضافه شد.

## NOT-RUN / محدودیت

- **scale=0.01 (~100k کاربر/1.4GB):** RAM سندباکس ~1GB — NOT-RUN؛ بنچ روی 200k/60k با برچسب Measured.
- CI — بیلینگ. Wave 13 — NOT-RUN (دست‌نخورده).

## اسناد لمس‌شده

`docs/WAVE23_DB_NATIVE_REPORTS.md` (§۸ قلم‌ها بسته + §۹ جدید) · `docs/REPORTING_SYSTEM_GUIDE.md` (P2 + حالت دوگانه) · آینهٔ `reza/` سند متناظر ندارد (هر دو سند فقط در docs/).

## بخش ۶ — ادامه: رسیدگی به بازبینی مستقل PR #120 (دو flag بازبین Devin)

- **Flag 2 (تأیید ✅ رفع red-first):** صفحهٔ آخر پیمایش، مدارس صفحات قبل را با ردیف خالی تکرار می‌کرد — بازتولید قرمز با مثال بازبین؛ رفع یکسان در هر سه گزارش‌ساز DB-native (شامل کد حضور چت ۶ همین شرط را داشت)؛ ۳ سنجهٔ `walkNoEmpty` (pg 73→**76**) + جهش M11 (mutations 10→**11/11 کشته**).
- **Flag 1 (تأیید؛ خارج از دامنه با دلیل):** ۴۰۳ برای مدرسهٔ بیرونِ آینهٔ hydration سقف‌دار — pre-existing (کد حضور چت ۶ روی main همین رفتار را دارد)، جابه‌جایی scopedSchools به SQL = تغییر مجوزی (بدون ناظر ممنوع). ریسک مهارشده (env فقط sandbox، هشدار بوت، persist خاموش). **قلم باز برای ناظر** در §۹.۲ب سند.
