# Mission Report — Chat9 — 2026-09-14

## Mission

- **Mission ID:** `Daily Mission Queue — Chat9` (M1→M2→M3→M4), `Date: 2026-09-14`, `Status: ACTIVE`, `Owner: Chat9 — Behavioral simulation / audit`
- **Source of the Mission text:** blob `fee8973ad657db763c020e1f2312a35cb9865b2c` (size 1374) at ``daily-missions/Chat9/ACTIVE.md`` on branch **`arena/01a0a0e2-p2`** (= head of **PR #221**, state **OPEN**, not merged)
- **✔ Authorization update (پس از انتشارِ Mission روی main، `e4415d60`، size 1888):** F-2 حل شد — جزئیات و اجرایِ ادامه‌دار در «§A» پایین. متنِ اولیهٔ این گزارش برای حالتِ «Mission روی main نیست» نوشته شده بود و **بازنویسی/پنهان‌کردنِ آن وضعیت نکردم** (تاریخچۀ تصمیم خودش شاهد است).
- **⚠ Authorization caveat (زمانِ نوشتنِ بخش‌هایِ بالا):** ``daily-missions/Chat9/ACTIVE.md`` **روی `origin/main` وجود ندارد** — شواهد: `git ls-tree -r --name-only origin/main -- docs/daily-missions` ⇒ `Chat1, Chat10, Chat2, Chat3, Chat4, Chat5, Chat6, Chat7, Chat8` (**Chat9 تنها gap**) و `gh api …/contents/`daily-missions/Chat9/ACTIVE.md`?ref=docs/daily-reports-2026-09-14` ⇒ `Not Found`. طبقِ «Mission مفقود ⇒ BLOCKED» من **هیچ تغییرِ کد/محصولی** شروع نکردم؛ آنچه اجرا شد **فقط** بندهای read-only/شبیه‌سازیِ محلیِ همان Mission است (M1/M2/M3 + المان M4)، چون پروتکل می‌گوید «اگر فقط یک بخش متوقف شد، مراحل مستقل را ادامه بده»
- **Canonical docs reviewed:** هر ۷ سندِ canonical روی main **موجود** و خوانده/سنجیده شدند (از راهِ `git cat-file -e origin/main:<path>`): `docs/ROADMAP.md` · `docs/NATIONAL_ROADMAP_PROGRESS.md` · `docs/P0_BLOCKER_TRACKER.md` · `docs/ARCHITECTURE_REVIEW.md` · ``EXECUTION_CONTROL_PROTOCOL.md`` · ``ARENA_EXECUTION_MODEL.md`` · ``ARENA_REGISTRY.md`` ⇒ **EXISTS ×۷** (تعارضِ محتواییِ جدیدی که مانعِ این دور باشد ثبت نشد؛ تعارضِ «نبودِ Mission» در بالا)
- **Base / repo state at start:** sandbox **re-clone + HEAD reset** به `0a208f8` با 136 ورودیِ dirty؛ recovery: `git fetch origin refs/heads/arena/01a09a73-p2…` + `git reset --hard origin/arena/01a09a73-p2` ⇒ `HEAD = 049ee71`، `git status = ۰`. `origin/main = f852f2921fc06a726923865c93165b226e8fdfb1`

## Result

- **Status: PARTIAL** (M1–M4 اجرا شد؛ **۰** تغییرِ کد — طبقِ خودِ Mission: «do not fix outside an explicit implementation Mission») + **BLOCKED** برای هر کارِ نوشتنیِ خارج از گزارش (Mission روی main merge نشده)
- **Scope completed:**
  - **M1** بازخوانیِ ادعاهایِ Chat9 و تطبیقِ آن‌ها با mainِ فعلی ⇒ **ادعاها پایسته‌اند** (جز دو موردِ stale که اصلاح شد — پایین)
  - **M2** اجرایِ شبیه‌سازی‌هایِ سطح‌مدرسهٔ موجود در مخزن ⇒ **66 سنجه سبز** (48+4+6+8)
  - **M3** سناریوهایِ مرزی/invalid-state/mutation/recovery-adjacent ⇒ **44 سبز + 3 NOT-RUNِ معلل**
  - **M4** ماتریسِ پوششِ رفتاری (proven / failed / NOT-RUN / environment-dependent) ⇒ در همین فایل
- **Scope not completed:** هیچ fix/تغییرِ کد/تغییرِ سندِ دیگران؛ هیچ P0/P1 renumber/closure؛ هیچ National GO

## Changes

- **Files changed:** فقط همین فایل (``daily-reports/Chat9/2026-09-14.md``) — **۰ فایلِ دیگر** (هیچ test/src/infra دست‌نخورده؛ همهٔ اجرا در `/tmp/main-wt` = خروجیِ `git archive origin/main`)
- **Commit SHA:** پس از commit در همین branch ثبت/تأیید می‌شود (`git log -1`)؛ خودِ فایل نمی‌تواند SHAِ خودش را بیاورد (خودارجاعی)
- **Branch:** `arena/01a09a73-p2` (شاخۀ رسمیِ همین نشست؛ `docs/daily-reports-2026-09-14` شاخۀ نشستِ دیگری است و دست‌نخورده)
- **Push:** `git push origin arena/01a09a73-p2` ⇒ وضعیتِ واقعی در § «Push verification» همین فایل و در پیامِ نشست
- **PR:** **PR #211 OPEN** (`arena/01a09a73-p2 → main`) · `additions 1285 / deletions 0 / changedFiles 2` · `mergeable = CONFLICTING` · `mergeStateStatus = DIRTY` ⇒ گزارش‌هایِ دورهایِ قبلِ من هنوز روی main نیستند (نه بازنویسی شد، نه force-push)

## Tests

| # | Test (executed on `main = f852f29`) | Result | Evidence | Status |
|---|---|---|---|---|
| T1 | `node /tmp/sim-accept-probe.js --tag main@f852f29 --json …` (پذیرشِ SIM-01..04، باندلِ واقعی در jsdom) | **PASS 24 / FAIL 3 / NOT-RUN 3** — برابرِ اندازۀ دور ۱۶ روی `bb5068b` ⇒ **پایدار در ≈۳۰ مرج** | `/tmp/probe-main-f852f29.json` · `VERDICT: RED (SIM-04/8-4.A, 8-4.B, 8-4.C)` · census users=1036/active=1016/guard=1/lib_staff=1/asset_staff=1/edu_office=4 | TESTED LOCALLY |
| T2 | بازتولیدِ خودِ هارنس از آرشیو (استخراجِ source از بلوکِ کدِ گزارشِ دور ۵) | 566 سطر / 39 561 B / `sha256 2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8` ⇒ **مطابقِ ادعایِ دور ۵** | خروجیِ `hashlib.sha256` روی استخراجِ دوم در این نشست | TESTED LOCALLY |
| T3 | `node tests/simulation.js` | **48/48 سبز** (چالش ۹/۰، ماژول‌سازی ۱۳/۰) | `شبیه‌سازی جامع: 48/48 سناریو سبز ✅` · exit 0 | TESTED LOCALLY |
| T4 | `node tests/schoolmode.js` · `node tests/schoolmode2.js` | **4/4** و **6/6** (V5 صف اولیا 1645 ms؛ V6 نوار «مسدود است») | tail های اجرا | TESTED LOCALLY |
| T5 | `node tests/school-type.js` | **8/8** (ST5 پیامدِ منو با نوعِ تنها؛ ST6 enumِ سرور؛ ST7 مدل+write-perms) | tail اجرا | TESTED LOCALLY |
| T6 | `node tests/school-type-mutations.js` | **5/5 mutant کشته شد** (SM2 پیش‌فرضِ fail-closed، SM3 وارونه‌شدنِ تقدم، SM4 تهی‌شدنِ enum، SM5 شکستِ flip) | `جهش‌های نوع مدرسه: 5/5 کشته` | TESTED LOCALLY |
| T7 | `node tests/drills2.js` | **10/10** (D8 ماتریس canAction هر ۵ اکشن؛ D9 شمار ۰..۱۰۰۰۰۰؛ D10 جداسازی) — با ۱ هشدارِ بی‌ضررِ jsdom: `Not implemented: window.scrollTo` | tail اجرا | TESTED LOCALLY |
| T8 | `node tests/offline-sync-drill.js` | **29 موفق / ۰ ناموفق / ۳ NOT-RUN** در 7.8s؛ شاهدِ per-scenario: `S4 skew_tested_min=[5,30] integrity=true` · `S5 queue_depth=120 push_ms=6 heap_delta_mb=5.1` · `S6 error_propagation_ms=2` | خروجیِ stdout | TESTED LOCALLY |
| T9 | `node tests/seed-completeness.js` (گیتِ چت ۳) | **FAILED TO START — ENOENT** `/tmp/main-wt/`server/data/payesh.json`` | trace: `code: 'ENOENT', path: '…/`server/data/payesh.json`'` · علت: `.gitignore:29` ⇒ `server/data/` در git نیست | **NOT-RUN — ENVIRONMENT/REPRODUCIBILITY LIMITATION** (از clean checkout قابل‌اجرا نیست ⇒ F-1) |
| T10 | CI / PR checks | — | — | **NOT-RUN** (بیلینگ؛ `RISK-O-007`) |

## Evidence State

- **CLAIMED:** — (چیزی که فقط ادعا باشد در این گزارش نیست)
- **IMPLEMENTED:** ۰ تغییرِ کد (عمداً؛ خارج از مجوز)
- **TESTED LOCALLY:** T1–T8 (روی `main = f852f29` در `/tmp/main-wt`؛ jsdom@25.0.1 در `/tmp/sim-probe-env`) + T9 با شکستِ مشخص
- **COMMITTED:** فقط همین گزارش (SHA در § Push verification)
- **PUSHED:** `arena/01a09a73-p2` ⇒ `ls-remote == HEAD` (سنجیده می‌شود؛ اگر برقرار نبود این سطر PUSH FAILED می‌شد)
- **PR OPEN:** #211 (CONFLICTING/DIRTY) — گزارش‌هایِ Chat9 دورهایِ ۵/۱۵/۱۶/۱/۱۰ هنوز روی main نیستند
- **MERGED:** — (هیچ PR من merge نشده؛ PR #220/#219/#217/#215 مالِ چت‌های دیگر و merge شده‌اند)
- **VERIFIED ON MAIN:** فقط برای **محتوایِ موجود روی main** (مثلاً `git grep`/`ls-tree` برای censusِ دانه و وجودِ ۶ سناریوی chaos-drill)؛ **نه** برای گزارش‌هایِ خودِ من
- **VERIFIED IN STAGING:** — · **PRODUCTION PROVEN:** — (هر دو: نیازمندِ محیطِ واقعی؛ `docker: ABSENT`, `id -u=1001`, RAM 3939 MB)

## Coverage matrix (M4)

| سناریو | وضعیت | شاهد |
|---|---|---|
| SIM-01 ورودِ نگهبان (`guard1`, id=1025) + `vis-save/out` با `registered_by=1025` | **PROVEN (PASS×۷)** | T1 `8-1.A…H` روی `f852f29` |
| SIM-02 کتابداری (`lib-lend=true`) | **PROVEN (PASS×۵)** | T1 `8-2.*` |
| SIM-03 تحویلدار (`as-status=true`، cross-tenant=false) | **PROVEN (PASS×۵)** | T1 `8-3.*` |
| SIM-04 نقش‌هایِ اشتباه/غیرمجاز (سه حالتِ طراحی) | **FAILED (3)** — سطحِ طراحی، نه دانه | T1 `8-4.A/B/C` · `8-4.D` NOT-RUN |
| قطعیتِ seed (B.1) و رگرسیونِ suites (B.2) | **NOT-RUN** | B.1: `node server/seed.js ×۲ + sha256sum` خروجیِ gitignored را بازنویسی می‌کند؛ B.2: به دانهٔ غنی‌شده گره‌خورده + ممنوعیتِ بازاجرا |
| حالت‌هایِ مدرسه / نوع مدرسه / جهش‌ها / مانورِ آفلاین | **PROVEN (66 + 49 سبز)** | T3–T8 |
| Service Worker BGSync واقعی · قطعِ شبکهٔ واقعی (iptables/tc) · PG/Redis زنده | **NOT-RUN — ENVIRONMENT LIMITATION** (سه مورد، صراحتاً در خروجیِ T8) | T8 |

## Findings / Blockers

- **F-1 (defect, verification-only, مالک: چت ۳ / حاکمیتِ گیت‌ها):** `tests/seed-completeness.js` از **clean checkout** اجراپذیر نیست: `ENOENT `server/data/payesh.json`` (مسیرِ gitignored در `.gitignore:29`) ⇒ به‌جای `SKIP/NOT-RUN` با **stack trace** می‌میرد. پیشنهادِ اصلاح (انجام‌نشده، خارج از مجوز): اگر فایل نبود ⇒ خطِ `NOT-RUN (data file absent; run server/seed.js first)` و exit 0.
- **F-2 (Blockerِ من):** Missionِ Chat9 روی main merge نشده (`arena/01a0a0e2-p2` / PR #221 OPEN) ⇒ هر کارِ نوشتنیِ خارج از گزارشِ خودم مجوز ندارد. رفع: mergeِ PR #221 (یا قرار دادنِ ACTIVE.md روی main توسط Coordinator).
- **F-3:** **PR #211 CONFLICTING/DIRTY** از دورهایِ قبل (append-only؛ تعارض فقط EOF در `docs/daily-reports/2026-09-14.md` که چند نشست همزمان به انتهایش می‌نویسند) ⇒ سابقۀ رسیدگی/ممیزیِ Chat9 روی main نیست. رفع: `git rebase origin/main arena/01a09a73-p2` توسط صاحبِ ادغام («keep both»). من rebase نکردم: tipِ شاخه کامیتِ نشستِ دیگر (`ab933c1` = دور ۶) را هم حمل می‌کند.
- **F-4 (environment/provisioning):** اصلاحِ `remote.origin.fetch` (افزودنِ refspecِ شاخۀ نشست، دور ۱۰) **در re-clone زنده نمی‌ماند** (`.git/config` در snapshot نیست) ⇒ سنجشِ این دور: `git config --get-all remote.origin.fetch` = فقط `+refs/heads/main:…` و HEAD دوباره روی `0a208f8` ریست شده بود (چهارمین «کامیتِ گمشدۀ» ساختگی). رفعِ ماندگار باید در provisioning/bootstrapِ مخزن باشد (مالکیت: چت ۶ / Coordinator)، نه config محلی.
- **F-5:** اعدادِ S3/S4 در بریف‌هایِ Coordinator (RTO/RPO و «۶ رویداد تا ابد pending») **پیش‌ازرفع**‌اند (`#185` MERGED، `703f93a`: F1/F2/F3)؛ عددِ پس‌ازرفع ثبت‌شده‌ای در مخزن نیست (`docs/DR_RUNBOOK.md` §۶ سطر 129 فاقدِ ردیفِ S1..S5) ⇒ «HISTORICAL/UNSOURCED» بماند تا اجرایِ مجدد + ثبتِ امضاشده (این بند در §۱/§۲ گزارشِ دور ۱۰ من روی شاخه آمده و تکرار نشد).

## Out of Scope (دیده شد، انجام نشد)

- هر رفعِ کد/سنجه/آلارم (شامل ۴ خلأِ p50 · OTP-avail · sync-ratio · آستانۀ 5xx که در دور ۱/۱۰ سند شده) — نیازمندِ Missionِ اجراییِ صریح
- ساختِ سه فایلِ موقتِ بریف‌هایِ قبلی — رد با دلیل (بدهیِ ارجاع + حکمِ «۰ فایلِ تازه»)
- rc bump (مالک: چت ۶) · آینهٔ تازهٔ reza (فقط چک) · ویرایشِ `RISK_REGISTER/NEXT_ACTIONS/HANDOFF` ریشه · force-push · بازاجرای سوئیت‌هایِ استیجینگِ چت ۸/۵
- هر کاری روی `arena/01a0a0e2-p2`، `docs/daily-reports-2026-09-14` و سایر شاخه‌ها (فقط خوانده شد)
- **۶ PAT فاش‌شده ⇒ revoke توسط مالک** (من هیچ‌گاه استفاده/ذخیره/echo نکردم) — همچنان باز

## Next Action Requested

1. **Coordinator/Auditor:** merge یا جای‌گذاریِ ``daily-missions/Chat9/ACTIVE.md`` روی main (PR #221) تا مجوزِ نوشتنیِ Chat9 رسماً صادر شود — تا آنجا Chat9 فقط read-only/تأییدی کار می‌کند.
2. **صاحبِ ادغام:** رفعِ تعارضِ **PR #211** (rebase = keep-both) تا سوابقِ ممیزیِ Chat9 (دور ۵/۱۵/۱۶/۱/۱۰) روی main بیاید؛ سپس «VERIFIED ON MAIN» برای آن بخش‌ها قابل‌ثبت است.
3. **چت ۳ (مالکِ گیت):** F-1 (اجراپذیر نبودنِ `seed-completeness` از clean checkout).
4. **حاکمیتِ محیط (چت ۶/Coordinator):** F-4 — refspec/branch-tracking در provisioning تا الگوی «re-clone ⇒ HEAD reset» تکرار نشود.
5. **Auditor:** تصمیمِ «الزامات پایش» و «الگوی A» همچنان باز است؛ با ACCEPT، کارِ Chat9 از ماتریسِ همین فایل (ردیف‌هایِ NOT-RUN/FAILED) شروع می‌شود، نه از صفر.

## Gates (مسیرِ تغییر، یک‌بار روی درختِ نهایی، همه با `timeout`)

- `node tools/docs-stats-sync.js --check` ⇒ ✅ «آمارِ مستندات با دیسک یکی است» (۳۴۸ ریشه + ۵۸ زیرپوشه = ۴۰۶ درخت · فعال ۳۴۰ + زنده ۳ + منجمد ۴۳ + آرشیو ۲۰ · قفل **rc43**) ⇒ **۰ rc bump** (مالک: چت ۶) و نیازی به `docs-stats-sync` نوشتنی نبود
- `node tests/docs-freeze-marker.js` ⇒ **۱۴ موفق / ۰ ناموفق (از ۱۴)** ✅
- `node tests/docs-metadata.js` ⇒ **۱۷ موفق / ۰ ناموفق** · «هیچ سند یتیمی وجود ندارد» ✅ (فایلِ تازهٔ من یتیم شمرده نشد)
- `node tools/docs-refs-check.js` ⇒ ۱۱۳ کهنه در ۶۵ سند = baseline ۷۵ + تازه **۴۱ (وراثتی)** + ۵ تولیدیِ ردشده · **۰ ارجاعِ کهنۀ تازه از این فایل** — ابتدا `+۴` داد (ارجاع‌هایِ `docs/…` به فایل‌هایی که روی این branch نیستند) و بازنویسی شد؛ ارجاعِ `server/data/payesh.json` عمداً مانده و چک‌ر آن را «تولیدی/ردشده با .gitignore» می‌شمارد
- `node tests/secret-scan.js` ⇒ **12/12 سبز** ✅ (۰ توکن در فایل/argv/کامیت/گزارش)
- `node tools/reza-mirror-check.js` ⇒ ✅ «همهٔ آینه‌های زنده با منبعِ docs/ برابرند» — **۰ همگام / ۰ اختراعِ آینه / ۰ cp دستی**
- CI / PR checks ⇒ **NOT-RUN** (بیلینگ، `RISK-O-007`) · سوئیت‌هایِ استیجینگ/پایشِ چت‌های دیگر ⇒ **۰ بازاجرا**
- **Push verification:** یک کامیت با `git add` صریح (فقط همین فایل)، بدونِ `git add -A`، بدونِ force-push؛ `git ls-remote origin refs/heads/arena/01a09a73-p2` با `git rev-parse HEAD` مقایسه و نتیجه در پیامِ پایانِ نشست اعلام می‌شود (این فایل SHAِ خودش را نمی‌تواند بیاورد — خودارجاعی)

- `node tools/docs-stats-sync.js --check` · `node tests/docs-freeze-marker.js` · `node tests/docs-metadata.js` · `node tools/docs-refs-check.js` · `node tests/secret-scan.js` · `node tools/reza-mirror-check.js` — نتایجِ عددی در همین بند پس از اجرا ثبت می‌شوند (اگر آمارِ سند به‌خاطر فایلِ تازه قرمز می‌شد، تنها مسیرِ مجاز `node tools/docs-stats-sync.js` بدونِ `--check` بود که ردیف‌هایِ OWNED را همگام می‌کند؛ **rc bump نکردم** — مالکِ چت ۶).

## Budget / hygiene

- **معیارِ مؤثرِ §۱ (بدونِ `node_modules`/`.cache`):** پیش **100.93 MB / 1554 فایل** → پس **100.94 MB / 1555 فایل** 🟢 — دلتا = **15 047 B و دقیقاً +۱ فایلِ ردیابی‌شده** (همین گزارش) · `.git` ثابت 68.7 MB؛ همهٔ اجرایِ تست‌ها در `/tmp`؛ **۰ فایلِ scratch در مخزن**؛ `git status` در پایانِ اجراهایِ T3–T9 = **۰** (مخزنِ کاری دست‌نخورده، چون main به‌صورت `git archive` به `/tmp/main-wt` استخراج شد).

---

# §A — ادامهٔMission پس از صدورِ مجوز (همان روز، بعد از بازگشتِ auth)

## A/1) وضعیتِ مجوز (تغییرِ مهم نسبت به بخشِ بالا)
- `docs/daily-missions/Chat9/ACTIVE.md` **روی main نشست** ⇒ blob `e4415d60` (size **1888**، نسخهٔ on-main از متنِ ۱۳۷۴ بایتیِ شاخۀ `arena/01a0a0e2-p2`) ⇒ **F-2 برطرف شد؛ Statusِ بلوکه‌شدنِ نوشتنی لغو**.
- بندِ جدیدِ «Common rules» در نسخۀ main: «The Queue is a **minimum path, not a stop condition** … For Mission-scoped **reports, fixtures, tests** or other authorized artifacts that are **safe to merge**, **Chat9 owns commit → push → checks → merge → main verification**. Do not merge feature remediation or anything that changes P0/P1 status without an explicit implementation/governance decision.» ⇒ دامنهٔ من: **گزارش/fixture/test**؛ **نه** remediation، **نه** بستنِ P0/P1.
- بردِ ریزتر از Coordinator: `docs/daily-mission-boards/2026-09-14/Chat9.md` = **C9-01…C9-16** («One task at a time; Chat1 assigns. Each completed task requires report + commit + push.») ⇒ ماتریسِ §A/2 همان ۱۶ ردیف را پوشش می‌دهد.

## A/2) ماتریسِ C9-01…C9-16 (اجرا روی درختِ merge‌شده، در `/tmp/mm`؛ صفرِ نوشتن در مخزن)

| task | artifact اجراشده | نتیجه | سطحِ Evidence |
|---|---|---|---|
| C9-01 baseline | بازیابیِ هارنسِ Chat9 از آرشیو + اجرایِ دوباره روی main | `sha256 2e204c92…40c8` **مطابقِ دور ۵** · **PASS 24 / FAIL 3 / NOT-RUN 3** (پایسته در ≈۳۰ مرج) | TESTED LOCALLY |
| C9-02 enrollment/registration | `tests/schoolmode.js` (حالت مدرسه + ثبت) | **4/4 ✅** (S3 نوار ۶ نما، S4 مودال) | TESTED LOCALLY |
| C9-03 attendance | `tests/att3.js` · `tests/att2.js` | **9/9 ✅** (دور ۷۵ تایمری) · **6/6 ✅** (حالت‌های زمان‌دار) | TESTED LOCALLY |
| C9-04 grading | `tests/multi-grade.js` · `tests/gradeavg2-mutations.js` | exit 0 ✅ · «M3 جمعِ نمره → شمارشِ ردیف کشته شد» | TESTED LOCALLY |
| C9-05 vocational | `tests/vocational-grades-mutations.js` | **5/5 جهش کشته ✅** | TESTED LOCALLY |
| C9-06 practical score | `tests/region-scorecard.js` · `tests/school-type.js`(ST5 منو با نوعِ تنها) | **14/14 ✅** · 8/8 ✅ | TESTED LOCALLY |
| C9-07 school-type | `tests/school-type.js` + mutations | **8/8 ✅** · **5/5 کشته ✅** | TESTED LOCALLY |
| C9-08 sync | `tests/offline-sync-drill.js` | **29 موفق / ۰ ناموفق / ۳ NOT-RUN معلل** (7.8s) + S4 skew [5,30] / S5 queue 120 push 6ms heap +5.1MB / S6 propagation 2ms | TESTED LOCALLY |
| C9-09 offline | همان drill + `tests/drills2.js` | 10/10 ✅ (D8 canAction، D9 شمار ۰..۱۰۰۰۰۰، D10 جداسازی) | TESTED LOCALLY |
| C9-10 authorization | `tests/authz-model.js` · `tests/session-revocation.js` | exit 0 ✅ (announcements 7 فیلد) · **16/16 ✅** | TESTED LOCALLY |
| C9-11 tenant isolation | `tests/reports-tenant-isolation.js` | ✅ «بدون احراز هویت: هر چهار گزارش 401» + cross-tenant false در `8-3.*` (هارنس) | TESTED LOCALLY |
| C9-12 negative/expected rejection | `tests/att2-mutations.js` · `tests/school-type-mutations.js` | **5/5 کشته ✅** · **5/5 کشته ✅** | TESTED LOCALLY |
| C9-13 data consistency | `tests/seed-completeness.js` (A1–A3) + census | A1–A3 ✅، **A4 SKIP صریح** (`is_head` — قرارداد چت ۲) · seed: users 1036 / schools 6 / classes 36 / parent_links 527 | TESTED LOCALLY |
| C9-14 audit reproduction | استخراجِ source از گزارشِ دور ۵ و تطبیقِ sha + اجرایِ مجددِ ادعاهای SIM-01..04 | تطبیقِ **دقیق** sha · سه FAILِ SIM-04 بدون تغییر ⇒ ادعایِ دور ۵ بازتولید شد | TESTED LOCALLY |
| C9-15 focused regression test | **انجام نشد** | دلیلِ صریح: تنها artifactِ قابل‌افزودنِ من «گزارش» است؛ افزودنِ test جدید برای اینکه *واقعاً* CI-safe باشد نیازمندِ حلِ §A/4 (bootstrap) است که خارج از دامنهٔ من است ⇒ **NOT-RUN — scope/dependency** (نه بی‌کاریِ بی‌دلیل) | NOT-RUN |
| C9-16 edge case | `tests/school-type-mutations.js` SM2/SM3/SM4/SM5 + `8-4.*` (هارنس) | ۴ جهشِ مرزی کشته · سه نقضِ طراحیِ باز (SIM-04) | TESTED LOCALLY |

جمعِ سنجه‌هایِ این دور: **24+3+3 (هارنس) + 48 + 4 + 9 + 6 + 8 + 5 + 5 + 5 + 10 + 29 + 16 + 14 + 22 + …** ⇒ بدونِ شمارشِ تکراری: **۲۱ سوئیت اجرا، ۰ خطای واقعی جدید، ۳ FAILِ شناخته‌شدۀ SIM-04 (سطح طراحی)، ۳ NOT-RUN معلل (محیط)، ۱ SKIPِ صریح (A4)**

## A/3) PUSH FAILED (دورِ قبل) و بازگشتِ auth — سند
- دورِ قبل پس از commit (`3aadc2c`) دو تلاشِ push: (۱) `fatal: could not read Username for 'https://github.com'` (۲) با `credential.helper='!gh auth git-credential'` ⇒ `remote: Invalid username or token. Password authentication is not supported`؛ `gh auth status` ⇒ «**The github.com token in GH_TOKEN is no longer valid**» و حتی `git ls-remote` هم می‌افتاد ⇒ ثبت شد: **PUSH FAILED** و `NOT-RUN — NETWORK/API LIMITATION` (کامیتِ 3aadc2c به‌خاطر re-clone از دست رفت؛ فایلِ گزارش روی دیسک ماند و همین‌جا بازیابی شد).
- ریشۀ کمکی که در همان دور ثبت کردم: `git config --get credential.helper` ⇒ **خالی** (helper در `.git/config` بود که با snapshot بازگردانی نمی‌شود) ⇒ همان F-4.
- این دور: `gh auth status` ⇒ «Logged in as arena-ai-coding-agent[bot]» ✅ و `ls-remote` کار می‌کند ⇒ push/merge انجام می‌شود و نتیجه‌اش در A/5 ثبت می‌شود (اگر شکست بخورد: PUSH FAILED، نه سبزِ جعلی).

## A/4) FINDING‌هایِ تازه (سندشده، اصلاح‌نشده — خارج از «do not fix»)
- **F-6 (پیش‌نیازِ bootstrap، با بازتولید):** از clean checkout، `server/data/payesh.json` تولید نشده است (`.gitignore:29 server/data/`) و خودِ `node server/seed.js` هم با **«jsdom not installed»** می‌افتد ⇒ سه آزمونِ ردیابی‌شده (`tests/seed-completeness.js`, `tests/authz-model.js`, `tests/reports-tenant-isolation.js`) با **stack trace ENOENT** می‌میرند. با `npm install --no-save jsdom@25` + `node server/seed.js` ⇒ seed «✅ store written (5683 KB)» و **هر سه سبز**. پس خلأ «کد» نیست، **نبودِ preflight** است. ⚠ ناسازگاریِ قرارداد: `att3/att4` همان شرایط را با «⏭️ jsdom نصب نیست — تست رد شد» و **exit 0** رد می‌کنند؛ این سه نه ⇒ پیشنهاد (مالک: چت ۳ برای seed-completeness، مالکینِ authz/reports برای بقیه): الگوی «skip+NOT-RUN» یا یک `pretest` که install+seed کند. **F-1 را همین F-6 اصلاح می‌کند**: تشخیصِ اولیه («گیت خراب است») با بازتولید، به «کمبودِ bootstrap + قراردادِ ناهمگون» تصحیح شد — و همین، دلیلِ NOT-RUN بودنِ C9-15 است (تستِ جدیدِ من بدونِ حلِ preflight، CI را قرمز می‌کرد درحالی‌که CI اینجا NOT-RUN است و قابل‌آزمون نیست).
- **F-7 (بدهیِ governance روی main، با isolation test):** قرمزی‌هایِ docs-gate **از من نیستند**: روی `origin/main = d804155` خالی هم دقیقاً همین‌ها قرمزند — `docs-stats-sync --check` ❌ «`docs/DOCS_METRICS.md [docs-total-row]` کهنه»، `docs-freeze-marker` **11/14** (ردیف ۳۴۷ ↔ ۳۵۷ سند؛ جاافتاده‌ها: `ARENA_AGENT_PROMPT.md`, `ARENA_CONTINUATION_POLICY.md`, `ARENA_CONTINUITY_AUTHORIZATION.md`, `ARENA_CONTROL_PLANE…`؛ hashِ ناهمخوان: `PR_MERGE_PLAN.md`)، `docs-metadata` **16/17** (یتیم‌ها: `docs/ARENA_MISSION_TEMPLATE.md`, `docs/daily-audits/2026-09-14.md`). ⇒ **rc/freeze را لمس نکردم** (مالک: چت ۶)؛ اگر mergeِ من این‌ها را قرمزتر کند، retract می‌کنم — سنجه در A/5.
- **F-8 (مثبت):** `docs-refs-check` روی main خالی: **fresh = 61** · روی درختِ merge‌شدۀ من: **fresh = 59** ⇒ گزارشِ `docs/daily-reports/Chat9/2026-09-14.md` **۲ ارجاعِ کهنه را حذف کرد** (سطرهایِ board به مسیرِ گزارشِ من اشاره می‌کردند) — یعنی اولیـن ارجاعِ ورودیِ این فایل از همین board آمده، مطابقِ حکم «سندِ تازه باید ارجاعِ ورودی بگیرد».

## A/5) Git/merge/verification (پر می‌شود پس از اجرا)
- merge: `fb64a015` «merge: origin/main (d804155) into arena/01a09a73-p2 — رفعِ تعارض با union» · تعارض فقط EOF بود · resolve: `git merge-file --union` (base 48 / ours 446 / theirs 478 → **873**) · سلامت: **۰** بخشِ main جاافتاده و **+۵** بخشِ من در `2026-09-14.md`؛ `2026-09-13.md` = ۲۰۵۷ + ۸۸۷ = ۲۹۴۴ · `git diff --numstat origin/main HEAD` ⇒ **887/0 + 395/0** (append-only، صفر حذف)
- **push / PR #211 merge / main-verification ⇒ PUSH FAILED** (سند در §A/6) ⇒ «checks» روی درختِ محلی انجام شد، «merge» و «main verification» **NOT-RUN — NETWORK/API LIMITATION**.


## A/6) PUSH FAILED — سندِ کامل (نه سبزِ جعلی)

- تلاش‌ها (همه با `timeout`): (۱) `git push origin arena/01a09a73-p2` ⇒ `fatal: could not read Username for 'https://github.com': terminal prompts disabled` · (۲) `git -c credential.helper='!gh auth git-credential' push …` ⇒ `remote: Invalid username or token. Password authentication is not supported for Git operations.` · (۳) `gh auth setup-git` سپس push ⇒ همان خطا · (۴) تلاشِ جایگزین با **Contents API** (`gh api -X PUT repos/…/contents/docs/daily-reports/Chat9/2026-09-14.md`) ⇒ **HTTP 401 `Bad credentials`** · (۵) تلاشِ نهایی پس از نوشتنِ همین بند (پایانِ دور).
- سنجشِ وضعیتِ auth در دو لحظه: `19:55Z` ⇒ «✓ Logged in to github.com as arena-ai-coding-agent[bot] (GH_TOKEN)» و `ls-remote`/`gh pr view` سالم · حدود `20:3xZ` ⇒ «✗ github.com: authentication failed — **The github.com token in GH_TOKEN is no longer valid**» و **حتی readها هم 401** (`gh api repos/rezaa2544/p2` ⇒ 401؛ `git ls-remote` ⇒ خطای Username).
- **F-9 (findingِ محیطی، مالک: provisioningِ Arena / Coordinator):** توکنِ `GH_TOKEN` کوتاه‌عمر است (کمتر از طولِ یک دورِ کاری) و هیچ `credential.helper` در `.git/config` نیست (در snapshot نمیماند) ⇒ هر دوری که commit را دیر push کند، **معلق** می‌ماند. راهکارهایِ پیشنهادی (انجام‌نشده، خارج از دامنه): (الف) refreshِ توکن پیش از push یا تمدیدِ عمرش، (ب) نوشتنِ helper در provisioning تا با re-clone پاک نشود، (ج) convention: push **بلافاصله** بعد از هر commit (که من هم از این پس اولویتِ اولم قرار می‌دهم).
- **آنچه سالم است:** tipِ remote شاخه = `049ee71` (کارِ دورهایِ ۵/۱۵/۱۶/۱/۱۰ + #211 روی remote) ⇒ **هیچ محتوایی از دست نرفته**؛ دو کامیتِ این دور محلی‌اند و روی دیسک زنده‌اند: `fb64a015` (merge) و `7f1b9c8d` (گزارش). برای کاربر/Coordinator پس از وصل‌شدنِ دوبارهٔ GitHub: `git push origin arena/01a09a73-p2 && gh pr merge 211 --merge` (تعارضِ PR به‌خاطرِ همین merge محلی حل شده و باید `CLEAN` شود).
- **Evidence State نهاییِ این دور:** COMMITTED (محلی) · **PUSH: FAILED** · PR OPEN (#211، بدونِ تغییرِ این دور) · MERGED: نه · VERIFIED ON MAIN: نه · TESTED LOCALLY: بله (۲۱ سوئیت) · CI: NOT-RUN (بیلینگ) · **هرچه در §A/2 سبز است در سطحِ «TESTED LOCALLY» است، نه «VERIFIED ON MAIN»** — و همین تفاوت، دلیلِ اینکه merge در انتظارِ رفعِ auth است.
