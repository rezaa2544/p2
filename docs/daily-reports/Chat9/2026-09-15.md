# Mission Report — Chat9 — 2026-09-15 — C9-17 Failure-path simulation

## 0) Mission و Base

- **Mission ID:** `C9-17` (ردیفِ عین Board: «Failure-path simulation | Non-destructive failure test.»)
- **منبع Assignment:** پرامپتِ اختصاصی Chat1 + پکتِ رسمی `docs/daily-reports/Chat1/2026-09-15.md` §R2.4 (سطرهای ۵۸۷–۵۹۹) · Board مجاز: `docs/daily-mission-boards/2026-09-14/Chat9.md` (۲۰ ردیف C9-01…C9-20) · تاریخ اجرا: 2026-09-15
- **Base re-verification و drift (ثبت‌شده، طبق R2.3):**
  - BASE تخصیص‌یافته: `8feb9e3d168e27a03c9055e5887ed1a7ae504e78`
  - `git ls-remote origin refs/heads/main` (سنجشِ اول): `328bdedf…` ⇒ **drift** · پس از fetch (طی همین مأموریت): `7017033fef42c699b5be9b5e393354433d141465` ⇒ **drift دوم** (مخزن حین کار در حرکت بود — PR #268/#269 merge شدند)
  - `8feb9e3` **جدِ `7017033`** است (سنجش با `git merge-base --is-ancestor` پس از `git fetch --deepen` — مجاز طبق R2.3؛ استنتاج نشد)
  - `git diff --name-only 8feb9e3..7017033 -- src/ server/ tests/ tools/ package.json package-lock.json migrations/` ⇒ **خالی** ⇒ بین base تخصیص‌یافته و درختِ سنجش **هیچ تغییرِ کدِ محصولی** نیست (فقط docs/ و .github/) ⇒ اندازه‌گیری‌های رفتاری روی `7017033` معادلِ base است.
- **بازیابیِ workspace (شفافیت):** snapshot وضعیتِ pre-commit دورِ قبل را برگردانده بود (تغییراتِ recovery به‌صورت uncommitted دوباره حاضر شدند)؛ محتوای آن دور **از قبل روی main** بود (PR #260 MERGED در `2026-09-15T19:13:31Z`؛ blob-level تأیید شد) ⇒ duplicates محلی discard شد و شاخهٔ نشست با `git merge --ff-only origin/main` به `7017033` رفت (fast-forward؛ بدون بازنویسیِ تاریخچه؛ push هم fast-forward).
- **نکتهٔ شاخه:** شاخهٔ نشستِ من (`arena/01a0a656-p2`) پیشتر توسط Chat8 برای C8-01 استفاده شده بود (برخوردِ شاخهٔ مشترک — F12 چت ۱)؛ tipِ remote (`7be63da`) کاملاً در main merge شده بود (تأیید ancestry) ⇒ ادامهٔ کار روی همین شاخه بدون از دست رفتنِ هیچ چیزی امن است.

## 1) بازتولید ۳ FAIL بازِ SIM-04 (قلبِ C9-17) — نتیجهٔ واقعی

- **هویتِ هارنس (بازتولید بدون اتکا به حافظه):** استخراج از بلوکِ کدِ گزارشِ دور ۵ (`docs/daily-reports/2026-09-13.md`، «پیوستِ الف»، خطوطِ ۲۲۷۴–۲۸۳۷) ⇒ `/tmp/sim-accept-probe.js` · **۵۶۶ سطر** · `sha256 = 2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8` — **تطبیقِ دقیق با سابقه** · تعدیلِ مستند: عددِ «۳۹۵۶۱ B» در گزارش‌های قبلی در واقع **شمارِ کاراکتر** است (بایتِ UTF-8 = ۴۵۶۹۴)؛ مرجعِ برابریِ محتوا sha256 است که مطابق است.
- **درختِ هدف:** `git archive 7017033` ⇒ `/tmp/c917` (بدون `.git` — علتِ خطای بی‌ضررِ `fatal: not a git repository` در خروجیِ پروب: پروب برای tag فرمان git می‌زند).
- **محیط (ثبتِ کامل):** `npm install --no-save jsdom` ⇒ **jsdom 30.0.1** نصب شد (exit 0 — برابر پینِ post-#240 در `package.json:42`) · `node server/seed.js` ⇒ «✅ store written: 5683 KB · users: 1036 | schools: 6 | classes: 36 | parent_links: 527» (exit 0).
- **فرمانِ اجرا:** `PAYESH_ROOT=/tmp/c917 SIM_JSDOM=/tmp/c917/node_modules/jsdom node /tmp/sim-accept-probe.js --tag main@7017033 --mode baseline --json /tmp/probe-c917-main.json`
- **نتیجهٔ اندازه‌گیری‌شده:** **۳۰ سنجه · PASS=24 · FAIL=3 · NOT-RUN=3 · `VERDICT: RED (SIM-04/8-4.A, SIM-04/8-4.B, SIM-04/8-4.C)` · exit 1**
- **پایداری (سابقه در برابر اکنون):** همان ۲۴/۳/۳ روی `bb5068b` و `f852f29` (دور ۱۵/۱۶)، `cc58531c` (بازبینیِ بازیابی) و اکنون **`7017033`** ⇒ پایدار در ۳+ ماهِ مرج — سطحِ ادعا: **TESTED LOCALLY روی main@7017033** (نه CI/staging).
- **سنجه‌های قرمز (actual values از خروجی):**
  - `8-4.A`: actual «۰ هیت · 0 هیت در src/+server/+migrations+authz»
  - `8-4.B`: actual «0 هیت»
  - `8-4.C`: actual «office_head=0 · is_head(محصول)=0»
  - `8-4.D`: **NOT-RUN ← 8-4.C** (SKIP صریح؛ actual: `is_head(truthy)=0` · کلیدِ `is_head` در ۰/۱۰۳۵ کاربر موجود)
- **غیرمخرب‌بودن:** پروب read-only است، رکوردهای `_probe` درون‌حافظه‌ای را پاک می‌کند و «دانهٔ دیسکی هرگز لمس نشد» (خروجیِ خودِ پروب)؛ **صفر تغییر کد/داده** در این Mission.

## 2) Characterisation دقیقِ مسیرِ شکست (کجا · چرا · چه ورودی/نقشی · file:line)

### 8-4.A — `is_head` در مدلِ authz تعریف نشده
- **کجا:** `authz/model.json` → `collections.users.fields` = **۳۶ فیلد** (بخشِ `users` از خطِ **۲۱۶۸**؛ فهرستِ الفبایی ~۲۱۷۰–۲۲۰۵؛ نمونه‌ها: `asset_staff` **:2189**، `lib_staff` **:2191**، `office_id` **:2194**) — **نه `is_head` و نه `office_head`** در whitelist هست.
- **چرا مسیرِ شکست است:** این مدل، قراردادِ فیلدهای مجازِ کاربر است و `tests/authz-model.js` آن را نگهبانی می‌کند: **:40** («test-written fields must exist in the authz model») و **:147–:189** (فیلدِ اضافهٔ client/server ⇒ پرچم‌گذاری؛ تنها استثنا `users.password` در **:154**) ⇒ حتی اگر seed مقدارِ `is_head` بنویسد، **دادهٔ مرده** است — whitelist مدل آن را رد می‌کند.
- **ورودی/نقشِ درگیر:** کاربرِ `role:'edu_office'` (نمونهٔ دانه: `edu_kurdistan` id=1027) — هیچ فیلدی برای تمایزِ «رئیس» از «کارشناس» در مدل نیست ⇒ مجوزِ ویژهٔ رئیس اصلاً قابلِ بیان نیست.
- **تنها پیش‌نویسِ موجود:** `tools/seed-office-data.js` — سرآیندِ **:6** («فرض‌های پیش‌نویس (قابل تغییر توسط چت ۲): پرچم `is_head` برای تمایز رئیس از کارشناس») و نمونه‌کاربران **:31–34** (`head_province901` `is_head:1` · `expert_province901` `is_head:0` · `head_district90111` · `expert_district90111`) — **مصرف‌کننده ندارد**.

### 8-4.B — صفرِ مصرف‌کنندهٔ `is_head` در کدِ محصول
- **کجا (اندازه‌گیری):** `grep -rn is_head` در `src/js/*.js` + `server/**` + `migrations` + `authz` = **۰ هیت** (تنها ارجاع‌های مخزن: پیش‌نویسِ `tools/seed-office-data.js` + مستندِ SKIP در `tests/seed-completeness.js:9,40,42`).
- **الگوی متضاد — پرچم‌هایی که واقعاً فرود آمدند (شش لایه، با file:line):**
  | لایه | lib_staff | asset_staff |
  |---|---|---|
  | دانه | `src/js/02-demo-data.js:459` | `src/js/02-demo-data.js:460` |
  | منو/UI | `src/js/07-shell.js:60` | `src/js/07-shell.js:65` |
  | اکشن‌ها | `src/js/19-actions-core.js:476` | `src/js/19-actions-core.js:532` |
  | authz کلاینت | `src/js/30-authz.js:73` | `src/js/30-authz.js:81` |
  | ماژول | `src/js/54-library.js:63,68` | — |
  | **گیتِ fail-closed سرور** | `server/policy.js:273` (`lib_staff !== 1 ⇒ false`) | `server/policy.js:281` (`asset_staff !== 1 ⇒ false`) |
  ⇒ برای `is_head` **هیچ‌یک از این شش لایه وجود ندارد** — «seed کردنش بی‌اثر است».
- **مسیرِ شکست با ورودی/نقش:** login هر کاربرِ `edu_office` (هر ۴ نمونهٔ دانه) → هیچ `canAction`/`canRoute`/`inScope` ای به `is_head` نگاه نمی‌کند ⇒ کاربرِ «رئیس» و «کارشناس» رفتارِ یکسان دارند؛ سناریوی رئیس-specific هیچ جایی از سیستم را لمس نمی‌کند.

### 8-4.C — قراردادِ «رئیس اداره» انتخاب نشده (`office_head` در برابر `is_head`)
- **کجا (اندازه‌گیری):** `office_head` = **۰ هیت در کل درخت** (`*.js`/`*.json`، خارج از node_modules) · `is_head` در کدِ محصول = **۰ هیت** ⇒ هیچ‌یک از دو نامِ کاندید در مدل/محصول وجود ندارد.
- **سمتِ دانه:** `src/js/24-edu-office.js:44–51` — تابعِ `mk()` برای هر اداره **دقیقاً یک** کاربرِ «کارشناس» می‌سازد (`role:'edu_office'`, `full_name:'کارشناس …'`, بدون هیچ پرچمِ ریاست)؛ ۴ فراخوانی **:54–58** (`edu_kurdistan` · `edu_baneh` · `edu_sanandaj1` · `edu_tehran` ⇒ ids 1027–1030، office_id 1–4).
- **سمتِ تست:** `tests/seed-completeness.js:9` و **:40–42** — معیارِ A4 با **SKIP صریح** رد می‌شود: «تا تثبیتِ قراردادِ چت ۲ … tools/seed-office-data.js پیش‌نویس است؛ policy/UI مصرف نمی‌کند».
- **مسیرِ شکست:** تا انتخابِ قرارداد، هر سناریوی رفتاریِ «رئیس اداره» **بی‌موضوع/NOT-RUN** است — `8-4.D` صراحتاً به آن وابسته است (SKIP).

## 3) مالکانِ پیشنهادی (برای route شدن توسط Chat1 — من مالکِ remediation نیستم)

| FAIL | مالکِ پیشنهادی | دلیل (شاهد) |
|---|---|---|
| `8-4.A` (فیلد در مدل) | **Chat2** | مالکِ قراردادِ طراحی؛ تصمیمِ «office_head در برابر is_head» در سه نقطه به چت ۲ نسبت داده شده: `tools/seed-office-data.js:6` · `tests/seed-completeness.js:9,42` · متنِ why خودِ هارنس؛ افزودنِ فیلد به `authz/model.json` کارِ feature/design است (ARENA_REGISTRY: Chat2 = Feature/application execution) |
| `8-4.B` (مصرف‌کننده) | **Chat2** — **هم‌مالکِ اختیاری: Chat3** | سیم‌کشیِ مصرف در `src/js` (منو/اکشن/authz کلاینت) به الگوی lib_staff/asset_staff = کارِ Chat2؛ اگر مصرف‌کنندهٔ انتخابی گیتِ fail-closed سمتِ سرور باشد (`server/policy.js:273/281` الگوی موجود)، بخشِ سرور به Chat3 (مالکِ write-path سرور طبق ARENA_REGISTRY) سپرده شود |
| `8-4.C` (انتخابِ قرارداد) | **Chat2** | تصمیم‌گیرندهٔ نام/قرارداد؛ پس از آن `8-4.D` (دانه ≥۱ edu_office با پرچمِ ریاست) قابلِ اجرا می‌شود |

**یادداشت:** هر سه FAIL سه چهرهٔ **یک ریشه**اند: «قراردادِ رئیس‌ادارهٔ چت ۲ هرگز فرود نیامد». route واحد به Chat2 (با هم‌دستیِ اختیاری Chat3 برای گیتِ سرور) منطقی‌ترین مسیر است؛ تصمیمِ نهایی با Chat1.

## 4) بازسنجی F-6 (قلمِ دوم — تصحیحِ evidence) — VERDICT: هنوز بازتولید می‌شود

همهٔ گام‌ها روی درختِ تمیزِ `main@7017033` در `/tmp/c917` اجرا و ثبت شد:

| # | گام | فرمان | نتیجهٔ واقعی | exit |
|---|---|---|---|---|
| 1 | clean checkout | `git archive 7017033` | `node_modules` غایب · `server/data/payesh.json` غایب · `.gitignore` همچنان `server/data/` را لا-کامیت می‌کند | — |
| 2 | سه آزمون بدون پیش‌نیاز | `node tests/seed-completeness.js` | **ENOENT stack-trace** (`server/data/payesh.json`) | **1** |
| 3 | | `node tests/authz-model.js` | **ENOENT stack-trace** | **1** |
| 4 | | `node tests/reports-tenant-isolation.js` | **ENOENT stack-trace** (copyFile) | **1** |
| 5 | seed بدون jsdom | `node server/seed.js` | «**jsdom not installed** — run: npm i --no-save jsdom» | **1** |
| 6 | قراردادِ متضاد | `node tests/att3.js` / `att4.js` | «⏭️ jsdom نصب نیست — تست رد شد.» | **0 / 0** |
| 7 | نصب | `npm install --no-save jsdom` | **jsdom 30.0.1** نصب شد (برابر پینِ post-#240) | 0 |
| 8 | seed با jsdom | `node server/seed.js` | «✅ store written 5683 KB · users 1036 · schools 6 · classes 36 · parent_links 527» | 0 |
| 9 | سه آزمون پس از bootstrap | همان سه فرمان | **5 سبز/0 قرمز/1 SKIP صریح** · **255/255** · **11 سبز/0 قرمز** | **0 · 0 · 0** |

- **VERDICT: «F-6 هنوز بازتولید می‌شود» — با #240 حل نشد.** PR #240 (`49cd65f`) فقط نسخهٔ devDependency را `25.0.1 → 30.0.1` برد (`package.json:42` = `"jsdom": "^30.0.1"` اندازه‌گیری شد)؛ **هیچ preflight/bootstrap ای اضافه نکرد**؛ `seed.js` همچنان بدون jsdom سخت‌می‌افتد؛ سه آزمون همچنان با stack-trace می‌میرند درحالی‌که att3/att4 همان شرایط را با skip و **exit 0** رد می‌کنند — **ناسازگاریِ قرارداد دست‌نخورده**.
- با bootstrap دستی همه سبز است — این بار با jsdom **30.0.1** (قبلاً 25) ⇒ #240 نسخهٔ کارکردی را به‌روز کرد اما خلأِ bootstrap را نه.
- **مالکِ پیشنهادی F-6 (ثابت):** چت ۳ برای `seed-completeness` + مالکانِ `authz-model`/`reports-tenant-isolation` (پیشنهادِ قبلی: الگوی «skip+NOT-RUN» به‌سبکِ att3/att4 یا یک `pretest` که install+seed کند).

## 5) تست‌های الزامیِ پکت — نتایجِ واقعی (NOT-RUN ≠ PASS)

| تست | نتیجه | exit | سطح |
|---|---|---|---|
| `/tmp/sim-accept-probe.js` (SIM-01..04) | **PASS 24 · FAIL 3 · NOT-RUN 3 · VERDICT: RED** | 1 | TESTED LOCALLY روی main@7017033 |
| `node tests/school-type-mutations.js` | **5/5 جهش کشته** | 0 | TESTED LOCALLY |
| `node tests/donations2.js` | **10/10 موفق** (N8/N9/N10؛ پینِ `:55` دست‌نخورده) | 0 | TESTED LOCALLY |
| `node tests/chat9-behavior-regression.js` | 8/0/1 (session-recovery همین روز) — **تکرار نشد** (دوباره‌کاری ممنوع) | 0 | VERIFIED ON MAIN (پیشتر) |

## 6) گیت‌های مستند — مسیرِ ابزار + exit (baseline روی main@7017033)

| ابزار (مسیر دقیق) | exit | نتیجه | وضعیت |
|---|---|---|---|
| `node tests/docs-freeze-marker.js --check` | **0** | 14/14 معتبر | برابر سنجشِ Chat1 |
| `node tools/docs-stats-sync.js --check` | **1** | `[docs-total-row]` کهنه — «جمع ۴۲۴» | **پیش‌موجود** (F13 چت ۱)؛ fix نشد (خارج از مأموریت) |
| `node tools/docs-refs-check.js` | **1** | **۱۱۴ ارجاعِ کهنه** | **پیش‌موجود** (کمّی‌سازیِ R4 چت ۱)؛ مالکِ اصلاح: چت ۷؛ دست نخورد |
| `node tests/secret-scan.js` | **0** | 12/12 سبز | برابر سنجشِ Chat1 |

- **دلتای درختِ من (اندازه‌گیریِ مستقیم، نه حدس):**
  - `tools/docs-stats-sync.js --check` ⇒ خروجیِ درختِ من با درختِ main **بیت‌به‌بایت یکسان** (`diff` ⇒ exit 0) ⇒ **دلتا = ۰** (متریکِ total-row فقط فایل‌های سطحِ ریشهٔ `daily-reports/` را می‌شمارد — ۳۸ در هر دو درخت؛ فایلِ من در زیرپوشهٔ `Chat9/` است)؛ گیت پیش‌موجودِ RED دست‌نخورده (F13 چت ۱).
  - `tools/docs-refs-check.js` ⇒ main: **۱۸۶ کهنهٔ کل / ۱۱۴ تازه** → درختِ من: **۱۸۵ کل / ۱۱۳ تازه** ⇒ **دلتا = −۱ (بهبود)** — فایلِ من **هیچ ارجاعِ کهنهٔ تازه‌ای نمی‌سازد** (۰ مورد `Chat9/2026-09-15.md → …` در خروجی) و یکی را رفع می‌کند: ارجاعِ پکتِ Assignment چت ۱ به مسیرِ همین گزارش (`docs/daily-reports/Chat9/2026-09-15.md`) حالا موجود است.
  - `tests/docs-freeze-marker.js --check` ⇒ روی درختِ من هم **14/14 · exit 0** (فایلِ من قفل را نمی‌شکند).
  - `tests/secret-scan.js` ⇒ روی درختِ من هم **12/12 · exit 0**.
  - شمارِ مستقیمِ فایل: `docs/**/*.md` = ۴۵۴ → ۴۵۵ (+۱ فقط همین گزارش)؛ `daily-reports/` = ۴۸ → ۴۹.

## 7) NOT-RUN‌ها با دلیل

- **8-4.D**: SKIP صریحِ پروب — مشروط به قراردادِ 8-4.A/B/C (actual ثبت شد: `is_head(truthy)=0`، کلید در ۰/۱۰۳۵ کاربر).
- **B.1 قطعیتِ seed**: اجرای دوبارهٔ `server/seed.js` خروجیِ gitignored را بازنویسی می‌کند؛ این Mission «صفر تغییر داده» است (همان دلیلِ پروب).
- **B.2 رگرسیونِ suites روی دانهٔ غنی‌شده**: دانهٔ غنی‌شدهٔ چت ۳ وجود ندارد؛ بازاجرا ممنوع.
- **CI روی PR**: پیش از باز شدنِ PR قابل‌سنجش نیست ⇒ پس از push سنجیده و نتیجهٔ واقعی (سبز/قرمز/در جریان) در §۹ ثبت شد.
- **staging/production**: محیط در دسترس نیست؛ هیچ ادعایی ثبت نمی‌شود.

## 8) Forbidden-scope compliance

- SIM-04 **fix نشد** · دانه/seed **دست‌نخورد** (پروب read-only + پاک‌سازیِ اثرِ درون‌حافظه؛ «دانهٔ دیسکی هرگز لمس نشد») · `tests/donations2.js:55` (پینِ 1036) دست‌نخورد · فایل/گزارش/شاخهٔ Arenaهای دیگر دست‌نخورد (حتی برای F-13/F-14 چت ۱) · `git add -A` **نه** (مسیرِ صریح) · force-push **نه** · بازنویسیِ تاریخچه **نه** (فقط `--ff-only`) · P0/P1 دست‌نخورد · National GO/NO-GO اعلام نشد (وضعیتِ ایستاده: NO-GO) · C9-18/C9-19/C9-20 شروع **نشد**.

## 9) تحویل (پس از اجرا پر/تأیید می‌شود)

- **Files changed:** فقط همین فایل `docs/daily-reports/Chat9/2026-09-15.md` (جدید — قاعدهٔ ضد-collision طبق R2.2)
- **Commit SHA / push / PR / checks / merge:** در hand-back همین Mission با SHA و نتیجهٔ دقیق ثبت می‌شود (اگر push شکست بخورد: PUSH FAILED = NOT-PUSHED).

---

# C9-18 — Evidence quality — الحاق به همین فایل — 2026-09-15

**Mission ID:** `C9-18` (ردیف عین Board: «Evidence quality | Tie simulation output to exact source/test.») · **BASE:** `891bfe3fd91ba86219ca7ee26dbbd9234d2d255f` (خودم بازسنجی کردم: `git ls-remote` ⇒ آخرین مختصاتِ گزارش‌شده `b5360fe` → **drift دو کامیت**: #273 چت۴ `tests/redis-key-audit.js` و #272 چت۶ `tests/config-audit.js` — فقط tests/؛ `git diff --name-only b5360fe..891bfe3` ⇒ صفر همپوشانی با فایل‌های evidence من). **همهٔ اعدادِ این بخش به BASE `891bfe3` لنگر شده‌اند** (عدد بدون BASE SHA = غیرقابل‌استناد). انضباط exit: `out=$(node … 2>&1); rc=$?` — بدون pipe.

## ۱) بند ۵ — بازراستی‌آزمایی تحویلِ C9-17 پیش از الحاق (ثبت)

- `out=$(git show origin/main:docs/daily-reports/Chat9/2026-09-15.md | sha256sum)` ⇒ `92004c8d042f3e6080158b5fce8e179bd9e9d24ecf9a69479da1b98b1bdbf780` == sha256 ثبت‌شده در hand-back من ✓ · ۱۲۸ سطر ✓
- `git merge-base --is-ancestor 33afce8 origin/main` ⇒ rc=0 (YES) ✓
- ⇒ الحاق به همین فایل انجام شد؛ فایل جدید ساخته نشد (قاعدهٔ ضد-collision).

## ۲) ممیزیِ اعداد — تأییدها (۱۲ مورد، هرکدام با فرمانِ بازتولیدپذیر)

| # | عددِ ثبت‌شده | محلِ ثبت | فرمانِ سنجش | مقدارِ سنجیده | حکم |
|---|---|---|---|---|---|
| V1 | sha256 هارنس `2e204c92…40c8` | ۱۰ ارجاع: `2026-09-13.md:2087,2156,2265,2891,2943` · `2026-09-14.md:109` · `Chat9/2026-09-14.md:35,123` · همین‌فایل §۱ | استخراج تازه از blob main + `sha256sum` | `2e204c929744d2faa2bfd64f050ce03414865072b900729b835efcc0b0fe40c8` | **CONFIRMED** (و کپی C9-17 == استخراج C9-18: `cmp` ⇒ exit 0) |
| V2 | ۵۶۶ سطر | همان ارجاعات | `wc -l` | 566 | **CONFIRMED** |
| V3 | baseline دور ۵: «۳۰ سنجه · PASS=14 · FAIL=6 · NOT-RUN=10 · exit=1» · users=1035 · guard=0 | `2026-09-13.md` §۲ (سطر ۲۰۹۳) | re-run روی `git archive 0a208f8` با jsdom **25.0.1** (محیطِ ثبت‌شدهٔ دور ۵) | «۳۰ سنجه · PASS=14 · FAIL=6 · NOT-RUN=10» · «دانه: 1035 کاربر · guard=0 · lib_staff=0 · asset_staff=0» · rc=1 | **CONFIRMED دقیق** |
| V4 | کنترلِ تفکیک‌پذیری دور ۵: PASS=23 · FAIL=4 · NOT-RUN=3 | `2026-09-13.md:2152` | re-run `--mode simulated-seed` روی 0a208f8 | PASS=23 · FAIL=4 · NOT-RUN=3 · VERDICT: RED (D.1 + 8-4.A/B/C) · rc=1 | **CONFIRMED دقیق** |
| V5 | دور ۱۵: PASS 24 / FAIL 3 / NOT-RUN 3 + census | `Chat9/2026-09-14.md:34` (T1) | re-run روی `git archive f852f29` با jsdom 25.0.1 | 24/3/3 · census: users=1036 · active=1016 · guard=1 · lib_staff=1 · asset_staff=1 · edu_office=4 · teacher=72 · manager=12 | **CONFIRMED** |
| V6 | دور ۱۶: 24/3/3 | ارجاع T1 | re-run روی `git archive bb5068b` با jsdom 25.0.1 | 24/3/3 · users=1036 · guard=1 | **CONFIRMED** |
| V7 | C9-17: 24/3/3 روی 7017033 | همین‌فایل §۱ | بازاجرا نشد؛ عدمِ تغییر اثبات شد: `git diff --name-only 7017033..891bfe3` ⇒ فقط tests/ بی‌ارتباط | — | **CONFIRMED (بدون بازاجرا — با diff)** |
| V8 | (لنگرِ تازه) | — | اجرای پروب روی BASE `891bfe3` با jsdom **30.0.1** | «جمع: 30 سنجه · PASS=24 · FAIL=3 · NOT-RUN=3» · VERDICT: RED (SIM-04/8-4.A, 8-4.B, 8-4.C) · rc=1 · census برابر V5 | **MEASURED** |
| V9 | خروجی seed: «store written 5683 KB · users 1036 · schools 6 · classes 36 · parent_links 527» | همین‌فایل §۴ | `node server/seed.js` روی کارستِ BASE | سطرِ شاهد: «✅ store written: … (5683 KB) / users: 1036 \| schools: 6 \| classes: 36 \| parent_links: 527» · rc=0 | **CONFIRMED** |
| V10 | لنگرهای authz: ۳۶ فیلد · users:2168 · asset_staff:2189 · lib_staff:2191 · office_id:2194 | همین‌فایل §۲ | python روی `authz/model.json` کارستِ BASE | 36 · 2168 · 2189 · 2191 · 2194 · is_head/office_head غایب | **CONFIRMED** |
| V11 | blob `c2bb26bd` برای `tests/chat9-behavior-regression.js` | `docs/daily-audits/2026-09-15-session-01a0a656-state-recovery.md:25` | `git ls-tree origin/main` | `c2bb26bdf4697d4ce45f078ba89aaa415355c60b` | **CONFIRMED** |
| V12 | edu ids ۱۰۲۷–۱۰۳۰ (C9-17) در برابر ۱۰۲۶–۱۰۲۹ (دور ۵) | همین‌فایل §۱ و `2026-09-13.md` D.2 | census JSON هر دو اجرا | 1027–1030 امروز · 1026–1029 روی 0a208f8 | **CONFIRMED** — هر دو رکورد با دورهٔ خود سازگار؛ جابه‌جایی +۱ = اثرِ درجِ نگهبانِ SIM-01 (پین 1035→1036 در PR #230) |

## ۳) اصلاح‌های ثبتی (بدون بازنویسیِ تاریخچه — اصلاح اینجا ثبت می‌شود، متنِ قدیم دست‌نخورده می‌ماند)

| # | رکوردِ نادرست | محل | مقدارِ اصلاح‌شده + فرمان |
|---|---|---|---|
| **R1** | «566 سطر / **39 561 B**» | `Chat9/2026-09-14.md:35` (T2) | واحد «B» غلط است: 39561 = **شمارِ کاراکتر** (`LC_ALL=C.UTF-8 wc -m` ⇒ 39561 · python `len` ⇒ 39561)؛ **بایت = 45694** (`wc -c` ⇒ 45694). مرجعِ برابریِ محتوا sha256 است (V1). ⚠ تلهٔ locale: `wc -m` با locale پیش‌فرضِ C نیز 45694 می‌دهد (= بایت) — شمارشِ کاراکتر نیازمندِ localeِ UTF-8 است. |
| **R2** | «خطوطِ **۲۲۷۴–۲۸۳۷**» برای بلوکِ source هارنس | همین‌فایل §۱ (C9-17) — و همین بازه در متنِ حکمِ Chat1 نیز آمده | بازهٔ واقعی: **۲۲۷۳–۲۸۳۸** (fenceِ ` ```js ` در خط ۲۲۷۲ و fenceِ بستن در ۲۸۳۹ — `grep -n`). خطای one-off دوطرفه در « نقلِ بازه »؛ خودِ استخراج همیشه fence-محور بود (هرگز خط‌محور) ⇒ هیچ خروجیای آلوده نشد. |
| **R3** | «پایدار در **۳+ ماهِ مرج**» | همین‌فایل §۱ (C9-17) | **غلطِ زمانی**: بازهٔ 2026-09-13/14 → 2026-09-15 = **۲ روز**. فاصلهٔ بازتولیدپذیر: `f852f29→891bfe3` = **۲۱۲ کامیت** · `bb5068b→891bfe3` = **۲۵۲ کامیت** (GitHub compare API، `ahead_by`). |
| **R4** | «پایدار در **≈۳۰ مرج**» | `Chat9/2026-09-14.md:34,123` | تقریبِ تاریخی؛ به‌عنوان فاصلهٔ lineage **بازتولیدپذیر نیست**: `0a208f8` (پایهٔ دور ۵) از lineage فعلیِ main واگراست (compare ⇒ 1852 ahead / 1453 behind تا BASE). جانشینِ قابل‌استناد: لنگرهای دقیقِ V3–V8. |

## ۴) بند ۲ — یکسان‌سازیِ هویتِ هارنس

- هر **۱۰** ارجاعِ sha256 در مخزن یکسان‌اند (فهرست در V1)؛ **نسخهٔ واگرا یافت نشد**؛ شمارِ سطر (۵۶۶) در همهٔ ارجاعات یکسان است.
- کپیِ C9-17 (`/tmp/sim-accept-probe.js`) و استخراجِ تازهٔ C9-18 از blobِ main (`/tmp/sim-accept-probe-v2.js`) **بیت‌به‌بایت برابر** (`cmp` ⇒ exit 0) ⇒ یک هارنسِ واحد، یک هویت.

## ۵) بند ۳ — پیش‌شرطِ دقیقِ بازکنندهٔ سه NOT-RUN

| NOT-RUN | چه چیزی باید land شود | مالک (طبق مسیریابی رسمی Chat1) | فرمانِ اجرایی پس از land |
|---|---|---|---|
| **8-4.D** (دانه ≥۱ edu_office با پرچمِ ریاست) | قراردادِ رئیس‌اداره در C2-07: (الف) انتخابِ نام (`office_head` یا `is_head`) · (ب) افزودنِ فیلد به `authz/model.json` → `collections.users.fields` · (ج) ≥۱ مصرف‌کنندهٔ محصول (کلاینت و/یا گیتِ `server/policy.js`) · (د) دانهٔ ≥۱ کاربرِ edu_office با پرچم | **چت۲** (مسیر رسمی: C2-07 پس از hand-backِ C2-02)؛ چت۳ فقط اگر مصرف‌کنندهٔ انتخابی گیتِ fail-closed سرور باشد (الگوی `server/policy.js:273/281`) | رویهٔ §۶ همین بخش |
| **B.1** (قطعیتِ seed: دو اجرا ⇒ بایت‌برابر) | مجوزِ اندازه‌گیری در Mission صریح — اجرای دوبارِ seed در **کارستِ یک‌بارمصرف** (خروجی gitignored است؛ اثری بر مخزن ندارد، ولی انضباطِ «صفر تغییر داده» تا مجوز برقرار است) | پیشنهاد: **چت۵** (C5-17 — دامنهٔ قراردادِ هارنس/QA طبق مسیریابی F-6) با هم‌دستیِ **چت۳** (مالکِ seed) | در worktree یک‌بارمصرف: `node server/seed.js && sha256sum server/data/payesh.json && node server/seed.js && sha256sum server/data/payesh.json` — دو hash باید برابر باشند |
| **B.2** (رگرسیونِ suites روی دانهٔ غنی‌شده) | دانهٔ غنی‌شدهٔ چت۳ (اکنون **وجود ندارد** — سنجش دور ۵: صفر شاخه/PR دانه؛ پیش‌شرطِ مفقود، بازسنجی نمی‌طلبد) | **چت۳** | اجرای suites مقید (run/smoke/authz-model) روی دانهٔ غنی‌شدهٔ land شده + `--mode simulated-seed` پروب به‌عنوان کنترل |

## ۶) بند ۴ — رویهٔ آمادهٔ 8-4.D (فقط آماده‌سازی؛ **اجرا نشد**)

پس از land شدنِ C2-07 (چت۲)، به همین ترتیب اجرا شود:

1. کارستِ تمیز از main تازه: `git archive <SHA> | tar -x -C /tmp/wt-postc207` سپس `npm install --no-save jsdom` (پینِ `package.json` — امروز 30.0.1).
2. اجرای پروب: `PAYESH_ROOT=/tmp/wt-postc207 SIM_JSDOM=<مسیر jsdom> node /tmp/sim-accept-probe.js --tag main@<SHA> --mode baseline --json /tmp/probe-postc207.json`
3. ترتیبِ خواندن: ابتدا ردیف‌های **8-4.A/B/C باید PASS شده باشند**؛ سپس **8-4.D** (SKIPِ آن مشروط به 8-4.C است و با سبز شدنِ C خودبه‌خود باز می‌شود) + census (پرچمِ ریاست ≥۱ روی edu_office).
4. **⚠ نکتهٔ حیاتیِ آماده‌سازی:** چکِ 8-4.D هارنس به نامِ `is_head` پین است (`census.is_head_truthy`). اگر چت۲ در C2-07 نامِ `office_head` را انتخاب کند، هارنسِ فعلی 8-4.D را **نمی‌سنجد** ⇒ در آن حالت: (الف) راستی‌آزماییِ نام‌آگنوستیکِ دستی با شمارشِ کاربرانِ edu_office دارایِ پرچمِ انتخابی از db باندل؛ (ب) به‌روزسازیِ هارنس برای نامِ تازه = **Mission/artifact تازه** (ویرایشِ خاموشِ هارنسِ ارجاع‌شده ممنوع — sha256 آن در ۱۰ سند لنگر است).
5. شواهدِ جانبی: `grep` نامِ انتخابی در `authz/model.json` و `src/js` + `server` (باید >0 مصرف‌کننده) + ثبتِ BASE SHA برای هر عدد.

## ۷) گیت‌ها (مسیرِ دقیق + rc — الگوی out/rc، بدون pipe)

- **pre-append** (درختِ من = BASE `891bfe3`): `node tests/docs-freeze-marker.js --check` ⇒ rc=0 · «۱۴ موفق / ۰ ناموفق» · `node tests/secret-scan.js` ⇒ rc=0 · «12/12 سبز» · `node tools/docs-stats-sync.js --check` ⇒ rc=1 (پیش‌موجود F13) · `node tools/docs-refs-check.js --json` ⇒ rc=1 · `totalMissing=185 · grandfathers=75 · fresh=113 · resolvedSinceBaseline=3` · **صفر fresh از اسنادِ Chat9**.
- **post-append**: در §۸ با عددِ واقعی (هدف: دلتای صفر — هیچ مسیرِ ناموجودی در این متن نوشته نشده).

## ۸) تحویلِ C9-18

- **Files changed:** فقط همین فایل (الحاقِ بخشِ C9-18 — append-only).
- **نکتهٔ انطباق:** گیتِ freeze فقط `docs/*.md` سطحِ ریشه را می‌سنجد (`readdirSync` غیربازگشتی در `tests/docs-freeze-marker.js:37`) ⇒ این الحاق خارج از دامنهٔ قفل است و F-C1-8 را نقض نمی‌کند (هیچ شمارِ تست/سند تغییری نکرد).
- Commit SHA / push / PR / merge / verify: در hand-back با سطرِ شاهد ثبت می‌شود (PUSH FAILED = NOT-PUSHED).
