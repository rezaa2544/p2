# 🔬 تحلیل سه قرمزِ wave5-authz (T18/T20/T21) — O-2
**تاریخ:** ۲۰ شهریور ۱۴۰۵ (2026-09-10) · **تحلیل‌کننده:** چت ۷ · **مبنا:** `main @ c7eee52` · **ارجاع:** چت ۵ (Bug Hunt)
**روش:** بازتولید زنده + خوانش کد + شبیه‌سازی policy + `git blame`/`git log -S`

---

## گام ۱ — بازتولید (خروجی کامل سه قرمز)

```
node tests/wave5-authz.js  →  exit 1 · 34/37
❌ T18 — extra: «403/out_of_scope»
❌ T20 — extra: «201/403 {"ok":false,"code":"forbidden","message":"این دانش‌آموز در کلاس‌های شما نیست"}»
❌ T21 — extra: «403/403/403»
```
فیکسچرهای سنجش (استخراج زنده از seed): T1=دبیر ۴ (کلاس‌های ۱،۲ — school 1) · ST=دانش‌آموز ۱۶ (کلاس ۱) · outsiderKid=۶۴ (کلاس ۳، هم‌مدرسه) · رکورد T18: attendance#561 (دانش‌آموز ۶۴، کلاس ۳).

---

## T18 — «دبیر: PATCH حضورِ شاگردِ بیرون‌کلاس — REST رد و sync رد (یکپارچه)»

**چه چیزی assert می‌شود؟** دبیر T1 رکورد حضورِ دانش‌آموزِ هم‌مدرسهٔ بیرون از کلاس‌هایش را PATCH کند ⇒ REST باید **404** بدهد و sync باید **رد (out_of_scope/role_denied)** بدهد (ترجیحِ ویو ۵: 404 ضد شمارش).

**خروجی واقعی:** REST = **403** (`forbidden` · «این رکورد در کلاس‌های شما نیست») · sync = **out_of_scope ✓** (نیمهٔ sync پاس می‌شود — فقط REST فرق دارد).

**ریشه:** کد سرور مشکل ندارد — **رفتار عمداً تغییر کرده است.** کامیت `4895f51` (**BUG-4**، باگ‌هانت چت ۵، ۰۹:۴۱ امروز) بایند دبیر→کلاس را در REST attendance/grades اعمال کرد و تصمیمِ مستندش: *«دبیرِ هم‌مدرسه ولی خارج از کلاس → 403 (نه 404)؛ رکوردِ ناموجود یا مدرسهٔ دیگر → 404 (عدم افشا)»* — یعنی افشای وجودِ رکورد به هم‌مدرسه‌ها مجاز است (school_id داخل مدرسه راز نیست)، افشای بین‌مدرسه‌ای نه. ترتیب دفاع در `updateAttendance`: اول `policy.inScope` (بیرون محدوده ⇒ 404)، بعد گارد BUG-4 همان‌مدرسه ⇒ 403. تستِ قدیمی فقط حالت 404 را می‌شناخت.

**کامیت مسئول:** `4895f51` — «fix(authz): enforce teacher class-binding in REST attendance/grades writes (BUG-4)» (همراه ۲۷ خط تست رگرسیون در `tests/api/attendance.test.js` که **سبز** است).

**دسته:** 🟦 **تست outdated** — عقدِ جدید (403 همان‌مدرسه / 404 بین‌مدرسه یا ناموجود) مستند، تست‌شده و منطقی است.

**مسیر رفع پیشنهادی (چت ۵):** در `tests/wave5-authz.js` انتظارِ T18 را به قرارداد BUG-4 به‌روز کنید:
`rDeny.status === 403 || rDeny.status === 404` (یا دقیق‌تر: 403 برای همان‌مدرسه) + نگه‌داشتن شرط sync. یک تست اضافی برای حالتِ بین‌مدرسه (404) ارزشمند است.

---

## T20 — «دبیر: POST حضور — دانش‌آموزِ خودی بله، بیگانه خیر»

**چه چیزی assert می‌شود؟** دبیر T1: (الف) POST برای دانش‌آموزِ خودی (ST در کلاس ۱) ⇒ **201** · (ب) POST برای outsiderKid (کلاس ۳، هم‌مدرسه) ⇒ **403** با کدِ **`out_of_scope`**.

**خروجی واقعی:** (الف) **201 ✓ پاس** · (ب) **403 ✓ از نظر وضعیت** اما کد = **`forbidden`** («این دانش‌آموز در کلاس‌های شما نیست»). تنها mismatch برچسبِ کد است؛ نه وضعیت، نه رفتار.

**ریشه:** همان کامیت `4895f51` (**BUG-4**) — گاردِ بایند دبیر→کلاس در `createAttendance` با `403/forbidden` رد می‌کند. تستِ ویو ۵ برچسبِ `out_of_scope` را (قرارداد قدیمی restCreateScopeOk در پایین‌دست، که حالا هرگز به آن نمی‌رسیم) انتظار داشت. هر دو برچسب «رد» را می‌رسانند؛ فقط واژه متفاوت است.

**کامیت مسئول:** `4895f51` (BUG-4) — تغییر برچسب از `out_of_scope` به `forbidden` بدون به‌روزرسانی wave5.

**دسته:** 🟦 **تست outdated** — با یک توصیهٔ دلبخواه برای چت ۵: اگر یکدستیِ برچسب با sync مطلوب است، سرور در همین گارد `code: 'out_of_scope'` برگرداند (sync همان را می‌دهد)؛ در غیر این صورت تست به `forbidden` به‌روز شود. هر دو مسیر یک‌خطی است.

**مسیر رفع پیشنهادی (چت ۵):** ساده‌ترین: در تست، `DENY = ['out_of_scope','role_denied']` را برای این نقطه به `['out_of_scope','forbidden']` گسترش دهید؛ یا برچسب سرور را به `out_of_scope` هم‌راستا کنید (ترجیحِ ما — پیام کد هم‌خوان با sync می‌شود). فقط یکی از دو طرف، نه هر دو.

---

## T21 — «دانش‌آموز: PATCH خودش — فول‌نیم بله، تلفن/نقش خیر»

**چه چیزی assert می‌شود؟** دانش‌آموز ST خودش را PATCH کند: `full_name` ⇒ **200** · `phone` ⇒ **403 field_denied** · `role` ⇒ **403**.

**خروجی واقعی:** **403/403/403** — پای اول (full_name=200) شکست می‌خورد؛ کلِ مسیر از همان ابتدا 403 `forbidden` («ویرایش کاربر فقط توسط مدیریت مجاز است») می‌دهد.

**ریشه:** کامیت `78da091` (**BUG-3**، باگ‌هانت چت ۵، ۰۹:۳۹ امروز) `PATCH /api/users/:id` را **فقط-مدیر** کرد — هم‌راستا با `authz/model.json` (users.upd: manager/superadmin) و گاردِ نقشِ sync (خودبه‌روزرسانیِ غیرمدیر = role_denied؛ phone/national_id/status/active فقط-مدیریتی‌اند). پیش از BUG-3، مسیر به هر نقشی اجازه می‌داد رکوردِ خودش — شامل همان فیلدهای حساس — را تغییر دهد (حفرهٔ واقعی که BUG-3 بست). حالا خودویرایشِ full_name **در هیچ مسیری** (نه REST، نه sync) وجود ندارد — تست به فیچری اسیر شده که عمداً حذف شده است.

**کامیت مسئول:** `78da091` — «fix(authz): REST users update is manager-only, matching sync/model (BUG-3)» (+۳۰ خط تست رگرسیون `tests/api/users.test.js::USR6` — **سبز**).

**دسته:** 🟦 **تست outdated** — با دو یادداشت برای تصمیم آینده:
1. **کد مرده:** `SELF_EDIT_FIELDS.users = ['full_name']` و منطقِ allowlist/`field_denied` در `users.js` (~L213-224) برای غیرمدیرها حالا **غیرقابل‌دسترس** است (BUG-3 زودتر برمی‌گرداند) — کاندیدای پاک‌سازیِ کد مرده یا بذرِ endpoint جداگانهٔ «پروفایل» اگر روزی خودویرایشِ full_name مطلوب شد.
2. اگر محصول خودویرایشِ full_name را می‌خواهد، این **شکاف طراحی** است که فقط سرپرست تصمیم می‌گیرد — نه باگ.

**مسیر رفع پیشنهادی (چت ۵):** تست T21 را به قرارداد BUG-3 به‌روز کنید: هر سه پای ⇒ 403 `forbidden` (هم‌ارزِ sync role_denied)؛ یا در صورت تصمیم سرپرست به بازگشتن خودویرایش، endpoint پروفایل جدا با allowlist موجود بسازید و تست را به آن بنگرانید.

