> ## 🔴 هم‌راستاسازی با برنامه جدید — 2026-09-24
> این tracker همچنان برای موانع P0/Go-Live معتبر است، اما ترتیب اجرای sweep سراسری عیب‌یابی اکنون در `docs/CURRENT_WORK_EXECUTION_PLAN.md` تعریف شده است.
> Atria ابتدا Critical/High، سپس Medium و سپس Low را بررسی می‌کند. بعد از آن validation سراسری همهٔ عامل‌ها اجرا می‌شود.
> بنابراین «رفع کدی» و «اثبات عملیاتی/اجرایی» همچنان دو مفهوم جدا هستند.

---

# 🔴 ردیاب موانع پ0 — مسیر زندهٔ نو-گو → گو

**تاریخ پایه:** 2026-09-10 · **مالک سند:** چت ۶ (مستندساز و مهندس انتشار) · **بازبینی:** روزانه تا گو
**مبنا:** `docs/GO_LIVE_PACKAGE.md` §۵/§۸ + `docs/PRODUCTION_READINESS_CHECKLIST.md` (شمارش ✅۱۲ · ⏳۱۵ · ❌۱۳ از ۴۰) + تاریخچهٔ `HANDOFF.md` + `docs/PR_MERGE_PLAN.md` + `docs/NATIONAL_ROADMAP_PROGRESS.md`

> **قاعدهٔ سند زنده:** این سند تنها نقطهٔ پیگیری مسیر نو-گو → گو است.
> هر تغییر وضعیت باید در **سه‌جا** اثر بگذارد: کارتِ مانع در همین سند +
> جدول §۸ و ماتریس §۵ در `docs/GO_LIVE_PACKAGE.md` + ورودی `HANDOFF.md`.
> هیچ ادعای پیشرفتی بدون شاهد (کامیت/پی‌آر/سند) ثبت نمی‌شود.
>
> **فاز بلافاصله پس از گو:** طرح پایلوت پیش از استقرار ملی — `docs/PILOT_ROLLOUT_PLAN.md`
> (شروعش منوط به نتیجهٔ گو و چهار امضاست؛ معیارها و محرک‌های بازگشتش همان گیت‌های اینجا را می‌سنجد).

---

## ۱) داشبورد مدیریتی

| شاخص | مقدار |
|---|---|
| کل موانع پ0 | **۶** |
| رفع‌شده ✅ | **۱** (پ0-۱: PostgreSQL منبع واحد حقیقت — گیت موج ۱ کامل و سبز) |
| در حال رفع 🟡 | **۴** (پ0-۲، پ0-۳، پ0-۵ — زیرساخت/کد مرج شده، اجرای زنده باقی است؛ پ0-۶ — ۷ از ۸ پی‌آر بلاک‌شده مرج شد) |
| بلاک‌شده 🔴 | **۱** (پ0-۴ — منتظر استیجینگ/محیط) |
| گام‌های تکمیل‌شده | **۱۷ از ۳۰** گامِ تعریف‌شده در «مسیر رفع» کارت‌ها |
| **درصد کلی تا گو** | **۵۷٪** — ۱۷ گام کامل از ۳۰ گام؛ میانگین سادهٔ گام‌ها بدون وزن‌دهی |

| مانع | وضعیت | یک‌خطی |
|---|---|---|
| پ0-۱ پستگرس تنها منبع حقیقت | ✅ | موج ۱ کامل: ادغام PR #47 و PR #48، مهاجرت 004، گیت 35/35، مسدودسازی فال‌بک در تولید |
| پ0-۲ آزمون بار/فشار/ضربه/خیس‌خوردن | 🟡 | پلن + زیرساخت کی‌شش مرج؛ دیتاست ملی و اجرای استیجینگ باقی است |
| پ0-۳ آشوب + بازیابی زنده | 🟡 | ابزار و طرح مرج (موج ۱۹ + اچ‌ای/پی‌آی‌تی‌آر)؛ اجرای زنده و مانور باقی است |
| پ0-۴ دی‌ای‌اس‌تی + پنتست خارجی | 🔴 | چک‌لیست و سایداست مرج؛ استیجینگ واقعی برای اجرا وجود ندارد |
| پ0-۵ رصدپذیری زنده | 🟡 | کد کامل مرج (ویو ۱۴)؛ بالاآوردن استک روی میزبان + وب‌هوک + دریل باقی است |
| پ0-۶ مرج پی‌آرهای بلاک‌شده | 🟡 | بستهٔ هفت‌گانه (#۵۱/#۳۴/#۳۳/#۳۱/#۳۵/#۴۶/#۳۲) مرج شد (2026-09-10 @ cd484c3)؛ #۲۹ در انتظار + قلم‌های محلی باقی (جدول کارت) |

---

## ۲) کارت‌های کامل شش مانع

> شماره‌گذاری کارت‌ها طبق ابلاغ تیمی است؛ فیلد «معادل §۸» ردیف متناظر در
> `docs/GO_LIVE_PACKAGE.md` §۸ را نشان می‌دهد تا منبع حقیقت دونگانی نشود.

### پ0-۱: ادغام نهایی موج ۱ — پستگرس تنها منبع حقیقت (پی‌جی‌اس‌او‌تی)

| فیلد | مقدار |
|------|-------|
| معادل §۸ | ردیف ۱ (پستگرس تنها منبع حقیقت) |
| توضیح | پیاده‌سازی کامل موج ۱ تثبیت شد: مهاجرت ۰۰۴ (ستون version و توالی outbox)، روت‌های ۵گانه و همگام‌سازی PG-اول با تراکنش اتمیک، حل تعارض Chat1/Chat3 با حکم معماری C1-02، و ایزولاسیون کامل Fail-Closed در محیط Production (عدم امکان استفاده از JSON در محیط واقعی). تمام تست‌های دو نمونه‌ای و گیت ۳۵گانه سبز است. |
| تأثیر | معیارهای «منبع حقیقت یکتا»، «بدون سریال‌سازی کل انباره» و «ای‌پی‌آی بی‌وضعیت» برآورده شدند؛ مسیر استیجینگ برای پ0-۲/پ0-۳ باز است. |
| مسئول | چت ۱ (مالک موج ۱) + چت ۵ (دروازهٔ کیفیت) |
| مهلت | پیش از هر انتشار (بند ۱ جدول §۸) |
| وضعیت | ✅ رفع‌شده (کامل) — ۲۰۲۶-۰۹-۱۷ |
| وابستگی | برطرف شد: حل تعارض C1-02 + ادغام PR #47 و PR #48 در main + قفل fail-closed در server/index.js و server/db.js |
| شاهد پیشرفت | پی‌آر #۴۸ و پی‌آر #۴۷ مرج‌شده · `tools/wave1-gate.js` (۳۵/۳۵ سبز) · `tests/wave1-multi-instance.js` (۳۳/۳۳ سبز) · `tests/wave1-reads.js` (۱۸/۱۸ سبز) · `tests/wave1-writes.js` (۱۵/۱۵ سبز) · گزارش `docs/daily-reports/Chat1/2026-09-17-wave1-canonical-delivery.md` |
| مسیر رفع | ۱. تکمیل پیاده‌سازی روی شاخه ✅ → ۲. تعیین‌تکلیف دو پیاده‌سازی با ناظر (C1-02) ✅ → ۳. ادغام نهایی در `main` ✅ → ۴. اجرای `tools/wave1-gate.js` روی `main` مرج‌شده ✅ → ۵. حذف کد فال‌بک حافظه‌ای/جی‌سان از مسیر تولید (مسدودسازی fail-closed در production) ✅ (۵/۵) |
| خطر باقی‌مانده | مرتفع شد. هرگونه اجرای پروداکشن در غیاب PostgreSQL متوقف می‌شود و هیچ واگرایی رخ نمی‌دهد. |

### پ0-۲: اجرای زنده لود / استرس / اسپایک / سوک

| فیلد | مقدار |
|------|-------|
| معادل §۸ | ردیف ۵ — بخش بار/فشار/ضربه/خیس‌خوردن |
| توضیح | طرح رسمی `docs/LOAD_TEST_PLAN.md` و زیرساخت کی‌شش (`tests/performance/`: شش سناریو + چهار سوئیت + `run-benchmarks.sh`) روی `main` مرج‌اند (پی‌آر #۴۷). دیتاست ملی (`seed-national.js`) با این نام هنوز در `main` نیست و هیچ استیجینگ چندنودی وجود ندارد. **هماهنگی ممیزی ۲۰۲۶-۰۹-۱۰:** مولد هم‌ارز `tools/generate-national-dataset.js` روی `main` موجود است؛ واگرایی نام‌گذاری و ابعاد (قلم باز ۲ و ۳ در `docs/DOCS_CONSISTENCY_REPORT.md`) پیش از اجرای رسمی تعیین‌تکلیف شود. |
| تأثیر | بند ۴ ماتریس §۵ (نتایج بار در `LOAD_TEST_RESULTS.md`)؛ سند §۳۰ اکنون با قالب آماده موجود است و فقط پر شدن اعداد باقی است؛ بدون اجرا، سقف ۱۰میلیون کاربر/۲۰هزار درخواست‌برثانیه اثبات نشده است. |
| مسئول | چت ۵ (مالک موج ۱۸) + چت ۴ (زیرساخت) |
| مهلت | تی−۳ روز (بند ۵ جدول §۸) |
| وضعیت | 🟡 در حال رفع — کد و پلن مرج؛ اجرا بلاک |
| وابستگی | مرج دیتاست ملی (قلم ۳ جدول پ0-۶) · استیجینگ چندنوده · تکمیل پ0-۱ (بار باید روی پی‌جی-محور بسنجد) |
| شاهد پیشرفت | `docs/LOAD_TEST_PLAN.md` · `tests/wave18-load-test.js` ‏38/38 روی main · `docs/CAPACITY_MODEL.md` (مدل ظرفیت) · شش سناریوی کی‌شش مرج · قالب `docs/LOAD_TEST_RESULTS.md` آمادهٔ پر شدن (چت ۶، ۲۰۲۶-۰۹-۱۰) |
| مسیر رفع | ۱. طرح رسمی ✅ → ۲. زیرساخت کی‌شش روی main ✅ → ۳. مرج دیتاست ملی ⬜ → ۴. پروویژن استیجینگ چندنوده ⬜ → ۵. اجرای چهار سناریو + پر کردن قالب `docs/LOAD_TEST_RESULTS.md` ⬜ (۲/۵) |
| خطر باقی‌مانده | سورپرایز مقیاس در روز اوج (مهر): کندی یا فروپاشی در پیک ثبت حضور — بدترین سناریوی حیثیتی برای پروژهٔ ملی. |

### پ0-۳: آشوب + بازیابی زنده (چائوس + ریکاوری)

| فیلد | مقدار |
|------|-------|
| معادل §۸ | ردیف ۳ (پایداری داده) + ردیف ۵ — بخش آشوب |
| توضیح | پنج سناریوی آشوب (کیوسک‌ای‌پی‌آی/ردیس‌دان/پی‌جی‌دان/نت‌لیتنسی/دیسک‌فول) با `tools/chaos-test.sh` (حالت امن دی‌آر‌آی‌ران + `--live`) و جدول پذیرش `docs/WAVE19_CHAOS_PLAN.md` مرج‌اند؛ زیرساخت‌های اچ‌ای پستگرس/ردیس و ابزارهای پی‌آی‌تی‌آر/فیلاور (`infra/postgres`، `infra/redis`، `tools/pitr-restore.sh`، `tools/failover-postgres.sh`) نیز کد شده‌اند. هیچ‌کدام روی محیط واقعی اجرا/دریل نشده‌اند. |
| تأثیر | بندهای پایایی چک‌لیست (دریل ری‌استور/فیلاور ❌، آر‌پی‌او/آر‌تی‌او سنجیده‌نشده ⏳) + بند ۳ ماتریس §۵. |
| مسئول | چت ۵ (مالک موج ۱۹) + چت ۴ + چت ۲ (پایداری داده) |
| مهلت | تی−۳ روز (بند ۵ جدول §۸ + بند ۳ برای دریل‌ها) |
| وضعیت | 🟡 در حال رفع — بیشترین پیشرفتِ کدی بین شش مانع؛ اجرا باقی |
| وابستگی | استیجینگ با رپلیکای زنده + آرشیو دبلیو‌ای‌ال (پی‌آی‌تی‌آر) · تکمیل پ0-۱ · ادغام قلم‌های پ0-۶ |
| شاهد پیشرفت | `tests/wave19-chaos.js` ‏28/28 · تست‌های اچ‌ای/پی‌آی‌تی‌آر ۹۲/۹۲ + ۳۸/۳۸ + جهش ۷/۷ (ردیف ویو ۱۶ نقشهٔ پیشرفت) · `docs/DR_RUNBOOK.md` + `docs/DISASTER_RECOVERY.md` |
| مسیر رفع | ۱. طرح آشوب ✅ → ۲. ابزار و تست روی main ✅ → ۳. زیرساخت اچ‌ای/پی‌آی‌تی‌آر به‌صورت‌کد ✅ → ۴. اجرای زندهٔ پنج سناریو روی استیجینگ ⬜ → ۵. مانور ری‌استور + فیلاور و سنجش آر‌پی‌او/آر‌تی‌او ⬜ (۳/۵) |
| خطر باقی‌مانده | طرح روی کاغذ در لحظهٔ حادثهٔ واقعی شکست می‌خورد؛ آر‌تی‌اوی ۱۵دقیقه‌ای تضمین‌نشده = ریسک قطعی ملی بیش از حد مجاز. |

### پ0-۴: دی‌ای‌اس‌تی + پنتست خارجی روی استیجینگ واقعی

| فیلد | مقدار |
|------|-------|
| معادل §۸ | ردیف ۶ (امنیت اجرایی) |
| توضیح | سایداست اجباری و سبز در `‎.github/workflows/security.yml`؛ دی‌ای‌اس‌تی در سی‌آی «بهترین‌تلاش» و هرگز زنده اجرا نشده؛ `docs/PEN_TEST_CHECKLIST.md` (سناریوها، به‌روزرسانی پی‌آر #۴۷) آماده ولی پنتست اجرا نشده؛ دبلیو‌ای‌اف فقط-تشخیص است و حالت اجرا (بند ۶ جدول §۸) هنوز فعال نیست. |
| تأثیر | بندهای ۴/۵/۶ محور امنیت چک‌لیست + بند ۶ ماتریس §۵ — تأییدیهٔ مسئول امنیت بدون این سه ممکن نیست. |
| مسئول | چت ۱ (امنیت) — پنتست خارجی نیازمند تیم/پیمانکار بیرونی |
| مهلت | تی−۷ روز (بند ۶ جدول §۸) |
| وضعیت | 🔴 بلاک‌شده — بدون استیجینگ واقعی هیچ‌کدام قابل اجرا نیست |
| وابستگی | پروویژن استیجینگ (مسیر مشترک با پ0-۲/پ0-۳) · تصمیم ناظر برای دبلیو‌ای‌اف اجرا-محور · دسترسی پیمانکار پنتست |
| شاهد پیشرفت | `docs/THREAT_MODEL.md` + `docs/PEN_TEST_CHECKLIST.md` · سوئیت `tests/wave13-security.js` ‏23/23 استاتیک · دبلیو‌ای‌اف تشخیصی مرج (پی‌آر #۲۱) |
| مسیر رفع | ۱. چک‌لیست پنتست ✅ → ۲. سایداست اجباری سبز ✅ → ۳. استیجینگ واقعی ⬜ → ۴. اجرای دی‌ای‌اس‌تی زنده + رفع یافته‌ها ⬜ → ۵. پنتست خارجی + فعال‌سازی دبلیو‌ای‌اف اجرا ⬜ (۲/۵) |
| خطر باقی‌مانده | کشف آسیب‌پذیری بحرانی پس از انتشار ملی؛ دادهٔ دانش‌آموزان (پی‌آی‌آی) بدون آزمودنِ مهاجم‌گونه محافظت می‌شود. |

### پ0-۵: رصدپذیری زنده (پرومتهوس/گرافانا با ترافیک واقعی)

| فیلد | مقدار |
|------|-------|
| معادل §۸ | ردیف ۴ (رصد عملیاتی) |
| توضیح | کل کد مرج شده (ویو ۱۴، پی‌آر #۴۹): متریک‌های `/metrics` با دروازهٔ توکن، استک کامل کمپوز (پرومتهوس/گرافانا/آلرت‌منیجر/لوکی/پرومتیل/او‌تی‌ال‌پی/جیگر)، هفت قانون بحرانی آلارم، داشبوردهای پروویژن خودکار. باقی‌مانده: بالاآوردن استک روی میزبان تولید، وب‌هوک واقعی آلارم، دریل اعلان و دیدن سیگنال زیر ترافیک واقعی. |
| تأثیر | بندهای ۴/۵ محور رصدپذیری + بند ۴ ماتریس §۵؛ بدون آن کشیک نابیناست و هیچ مانعی از جمله خودِ انتشار قابل تشخیص نیست. |
| مسئول | چت ۲/چت ۴ (طبق بند ۴ جدول §۸) |
| مهلت | تی−۷ روز (بند ۴ جدول §۸) |
| وضعیت | 🟡 در حال رفع — کد ۱۰۰٪ مرج؛ استقرار باقی |
| وابستگی | دسترسی به میزبان(های) تولید/استیجینگ + سکرت‌های وب‌هوک (پیام‌رسان/ایمیل آن‌کال) |
| شاهد پیشرفت | `server/metrics.js` (تست‌های کانفیگ ۵۵/۵۵ · داشبورد ۳۰/۳۰ · جهش ۶/۶) · `infra/observability/` (alert-rules.yml + دو داشبورد) · `docs/OBSERVABILITY.md` + `docs/OBSERVABILITY_DEPLOYMENT.md` + پلی‌بوک‌های `docs/INCIDENT_RESPONSE.md` |
| مسیر رفع | ۱. کد متریک/لاگ/تریس مرج ✅ → ۲. قوانین آلارم و داشبورد به‌صورت‌کد ✅ → ۳. بالاآوردن استک روی میزبان ⬜ → ۴. وب‌هوک واقعی + دریل اعلان ⬜ → ۵. اعتبارسنجی زیر ترافیک واقعی استیجینگ ⬜ (۲/۵) |
| خطر باقی‌مانده | کوری عملیاتی در هفته‌های اول؛ حادثه‌ای که کاربر قبل از کشیک ببیند = شکست اعتماد ملی. |

### پ0-۶: مرج پی‌آرهای بلاک‌شده (۸ قلم — ۷ قلم در بستهٔ 2026-09-10 مرج شد)

| فیلد | مقدار |
|------|-------|
| معادل §۸ | افقی — پیش‌نیاز ردیف ۱ و کل زنجیره |
| توضیح | ابلاغ تیمی از «۸ پی‌آر بلاک‌شده» یاد می‌کند. وارسی محلی این نشست (دسترسی ریموت بسته است؛ فهرست زندهٔ گیت‌هاب قابل تأیید نیست) این **هشت قلم مرج‌نشده/تحویل‌نشده** را یافت — پی‌آر #۴۷ چت ۳ در همین فاصله مرج شد (`4926041`) و از شمار خارج است. |
| تأثیر | همهٔ مانع‌های دیگر به این گلوگاه وابسته‌اند: دیتاست ملی (پ0-۲)، ابزارهای مرج‌نشده، سند‌های نشستِ بسته و کارهای پخش‌نشدهٔ دیگر چت‌ها. |
| مسئول | ناظر (مرج نهایی — اصل هشتم: هیچ مرج بدون تأیید صریح) + صاحبان هر قلم |
| مهلت | فوری — پیش‌نیاز زنجیرهٔ بحرانی |
| وضعیت | 🟡 در حال رفع — ۷ پی‌آر در بستهٔ 2026-09-10 مرج شد؛ #۲۹ در انتظار + قلم‌های پوش‌نشدهٔ محلی باقی است |
| وابستگی | دسترسی گیت‌هاب (نشست جاری بسته است: پوش/پی‌آر/ال‌اس‌ریموت ممکن نیست) · رفع تعارض‌های مستند · دروازه‌های مرج (دود ۵۴۷ + مجوز + راز + بیلد) |
| شاهد پیشرفت | `docs/PR_MERGE_PLAN.md` (وارسی زندهٔ چت ۱) · مرج‌شدن پی‌آر #۴۷ در `4926041` · **بستهٔ هفت‌گانهٔ 2026-09-10: #۵۱ رصدپذیری، #۳۴ کارآموزی (ئ.۲)، #۳۳ گیمیفیکیشن (ئ.۳)، #۳۱ شاخص سلامت (ج.۱)، #۳۵ ب.۳/د.۲-د.۴، #۴۶ موج‌های ۱۴/۱۶/۱۷/۱۸، #۳۲ اموال (ئ.۵) — ابلاغ تیمی: سرِ `main` = cd484c3** · باندل‌های آمادهٔ انتقال |
| مسیر رفع | ۱. وارسی و برنامهٔ ادغام ✅ → ۲. مرج پی‌آر #۴۷ ✅ → ۳. مرج باقی پی‌آرهای باز ✅ (هفت‌گانه: #۵۱/#۳۴/#۳۳/#۳۱/#۳۵/#۴۶/#۳۲ @ cd484c3؛ #۲۹ در انتظار) → ۴. پوش کارهای محلیِ نشست‌های بسته ⬜ → ۵. رگرسیون نهایی روی `main` یکپارچه ⬜ (۳/۵) |
| خطر باقی‌مانده | گم‌شدن/کهنگی کار مرج‌نشده با هر بار جلورفتن `main` (تعارض زنجیره‌ای — تجربهٔ چهار ریبیسِ پی‌آر #۴۷). |

**فهرست هشت قلم مرج‌نشده/تحویل‌نشده (وارسی محلی 2026-09-10):**

> **به‌روزرسانی 2026-09-10 (پس از ادغام):** ابلاغ تیمی مرج‌شدن هفت پی‌آر را تأیید کرد؛ `main` به `cd484c3` رسید و فقط پی‌آر #۲۹ در انتظار ماند. شماره‌های این فهرست با فهرست ابلاغ یک‌به‌یک منطبق نیست (این فهرست پیش از آن و از وارسی محلی ساخته شد) — قلم‌های قابل تأیید محلی در جدول علامت خوردند و فهرست رسمی ابلاغ در «شاهد پیشرفت» ثبت است.

| # | قلم | صاحب | وضعیت | مدرک |
|---|---|---|---|---|
| ۱ | پی‌آر #۲۲ — ردیابی جیگر (۶ یافتهٔ بازبینی باز) | چت ۳ | 🟡 باز، بدون تعارض، سی‌آی سبز | `docs/PR_MERGE_PLAN.md` §۱–۳ |
| ۲ | پی‌آر #۳۵ — منطقه/کمبود نیرو (۱۲ فایل متعارض، نقشهٔ حل دارد) | چت ۴ | ✅ مرج شد (2026-09-10) | ابلاغ تیمی + پیوست `docs/ROADMAP.md` |
| ۳ | ابطال نشست (شن) — اصلاً پی‌آر/شاخهٔ راه‌دور ندارد | چت ۴ | 🔴 پوش‌نشده | `docs/PR_MERGE_PLAN.md` §۱ |
| ۴ | دیتاست ملی `seed-national.js` — مولد هم‌ارز `generate-national-dataset.js` با موج ۱۸ (پی‌آر #۴۶) مرج شد؛ تعیین‌تکلیف نام/ابعاد = قلم باز ۲ گزارش هماهنگی | چت ۳/چت ۵ | ✅ جایگزین مرج شد | پی‌آر #۴۶ + `docs/DOCS_CONSISTENCY_REPORT.md` قلم ۲ |
| ۵ | تعیین‌تکلیف `writes` چت ۳ در برابر موج ۱ چت ۱ (پ0-۱) | ناظر | 🔴 منتظر تصمیم | `docs/NATIONAL_ROADMAP_PROGRESS.md` ویو ۱ |
| ۶ | ۱۶ کامیت محلی این نشست (مأموریت‌های ۴–۷: اسناد §۳۰ + بستهٔ انتشار + ردیاب) | چت ۶ | 🔴 پوش‌نشده — نشست بسته | باندل `/home/user/go-live-package.bundle` |
| ۷ | کارهای شاخهٔ اسنپ‌شات چت ۳ (موج‌های ۶/۱۱/۱۵ تست‌های ناپوشیده) | چت ۳ | 🟡 در `backup/workspace-snapshot-20260910` محفوظ | یادداشت ویو ۴ در `HANDOFF.md` |
| ۸ | یافتهٔ ۶ پی‌آر #۲۲ — سیاست وابستگی‌های ران‌تایم (تصمیم ناظر) | ناظر | 🔴 باز | `docs/PR_MERGE_PLAN.md` §۲ ردیف ۶ |

---

## ۳) مسیر بحرانی (Critical Path)

ترتیب وابستگی‌ها — هیچ گامی نمی‌تواند زودتر از پیش‌نیازش کامل شود:

```
پ0-۶ (مرج‌ها + پوش) ──► پ0-۱ (پی‌جی‌اس‌او‌تی نهایی) ──► پروویژن استیجینگ چندنوده
        │                          │                            │
        │                          ▼                            ▼
        │                    پ0-۴ (دی‌ای‌اس‌تی/پنتست)     پ0-۲ (بار چهارگانه)
        ▼                                                       │
پ0-۵ (موازی — استک رصد) ─────────────────────────────────► پ0-۳ (آشوب + دریل بازیابی)
                                                                │
                                                                ▼
                                                   جلسهٔ گو/نو-گو + چهار امضا
```

| گام | محتوا | پیش‌نیاز | تخمین (با منابع فعلی) |
|---:|---|---|---|
| ۰ | پوش ۱۶ کامیت این نشست + مرج پی‌آر #۲۲ و #۳۵ + پی‌آر ابطال نشست + مرج دیتاست ملی (پ0-۶) | نشستِ با دسترسی گیت‌هاب + تأیید ناظر | ۲ روز |
| ۱ | تعیین‌تکلیف دو پیاده‌سازی موج ۱ + ادغام نهایی + گیت موج ۱ روی `main` (پ0-۱) | گام ۰ | ۳ روز (هم‌پوشانی با گام ۰) |
| ۲ | بالاآوردن استک رصد + وب‌هوک + دریل اعلان (پ0-۵) | میزبان | ۳ روز (موازی با گام ۱) |
| ۳ | پروویژن استیجینگ چندنوده با رپلیکا و آرشیو دبلیو‌ای‌ال | گام ۱ | ۳ روز |
| ۴ | دی‌ای‌اس‌تی + پنتست خارجی + دبلیو‌ای‌اف اجرا (پ0-۴) | گام ۳ | ۴ روز |
| ۵ | اجرای لود/استرس/اسپایک/سوک + نتایج (پ0-۲) | گام‌های ۱ و ۳ | ۴ روز (موازی با گام ۴) |
| ۶ | آشوب زنده + مانور ری‌استور/فیلاور + سنجش آر‌پی‌او/آر‌تی‌او (پ0-۳) | گام‌های ۳ و ۵ | ۳ روز |
| ۷ | بافر + جلسهٔ گو/نو-گو با ماتریس به‌روز + چهار امضا | گام‌های ۴/۵/۶ | ۲ روز |

**تاریخ تخمینی تی-صفر (در صورت ادامهٔ روند و بدون کمبود منابع): ≈ 2026-09-27** — این تاریخ صرفاً برآورد مهندسی است و در جلسهٔ تصمیم با ناظر قطعی می‌شود. هر لغزش در گام ۰ (دسترسی گیت‌هاب) کل زنجیره را عقب می‌اندازد.

---

## ۴) دفتر ثبت تصمیم‌ها (Decision Log)

| تاریخ | تصمیم | تصمیم‌گیر | دلیل | تأثیر |
|---|---|---|---|---|
| 2026-09-10 | **نو-گو** — انتشار تا رفع هر شش مانع پ0 ممنوع | چت ۶ (ماتریس §۵ بستهٔ انتشار) | چهار بند ماتریس قرمز/نارنجی؛ اسناد §۳۰ = ۱۳/۱۴ | توقف همهٔ برنامه‌های انتشار؛ این ردیاب ساخته شد |
| 2026-09-10 | این سند تنها نقطهٔ پیگیری نو-گو→گو است؛ هر تغییر در سه‌جا (اینجا + §۸/§۵ + هندآف) | ابلاغ تیمی / چت ۶ | پراکندگی موانع بین اسناد | حذف منبع حقیقتِ دونگانی |
| 2026-09-10 | ادغام پی‌آر #۴۷ (مگاشاخهٔ چت ۳: ویوهای ۱۲/۱۳، موج ۹/۱۵/۱۸/۱۹، دبلیو‌ای‌اف) در `main` @ `4926041` | ناظر | چهار ریبیس با قاعدهٔ «هر دو سمت» + سی‌آی ۷/۷ سبز | ابزار آشوب/بار/سلامتی به `main` رسید؛ قلم اصلی پ0-۶ بسته شد |
| 2026-09-08 | اصل هشتم: هیچ مرج بدون تأیید صریح ناظر | کاربر/ناظر | حکمرانی ادغام‌ها در پروژهٔ ملی | همهٔ مرج‌های پ0-۶ منوط به تأیید ناظر |
| 2026-09-09 | دبلیو‌ای‌اف در وی۱ فقط-تشخیص؛ حالت اجرا پیش از تولید فعال می‌شود | ناظر | اجتناب از مسدودسازی ترافیک سالم پیش از کوک شدن قواعد | تبدیل به زیرمجموعهٔ پ0-۴ با مهلت تی−۷ |
| 2026-09-10 | تدوین بستهٔ انطباق قانونی ایران (`docs/IRAN_COMPLIANCE_PACKAGE.md`): هفت محور الزامی + ۱۲ قلم چک‌لیست + خط زمانی تی−۶ ماه تا تی−۰ | چت ۶ (ابلاغ تیمی) | پیش‌نیاز تصمیم‌های کارفرما برای گو-لایو ملی | اقلام کسب‌وکاری ردیاب به این سند ارجاع شد |
| 2026-09-10 | مرج بستهٔ هفت‌گانهٔ پی‌آرهای بلاک‌شده (#۵۱/#۳۴/#۳۳/#۳۱/#۳۵/#۴۶/#۳۲) در `main` @ `cd484c3` — فقط #۲۹ در انتظار | ناظر (ابلاغ تیمی چت ۷) | صف‌زدایی گلوگاه پ0-۶ | گام ۳ مسیر رفع بسته شد؛ بندهای ب.۳/د.۲-د.۴/ج.۱/ئ.۲/ئ.۳/ئ.۵ + موج‌های ۱۴/۱۶/۱۷/۱۸ به `main` رسیدند |
| 2026-09-10 | انتقال کار نشستِ بسته از طریق باندل گیت (`go-live-package.bundle`) به‌جای از‌دست‌دادن ۱۶ کامیت | چت ۶ | نشست جاری دسترسی ریموت ندارد | قلم ۶ جدول پ0-۶ تعریف شد |

---

## ۵) ثبت ریسک‌ها (Risk Register)

> **نمای جامع:** ثبت ریسک‌های مانع‌محورِ این جدول زیرمجموعهٔ `docs/RISK_REGISTER.md` است
> (تی-۰۰۱ تا تی-۰۰۸)؛ رجیستر جامع، همهٔ ابعاد فنی/امنیتی/قانونی/مالی/عملیاتی/انسانی/وابستگی
> را با ماتریس ۵×۵ و ده‌تای بحرانی پوشش می‌دهد و هفتگی به‌روز می‌شود.

| ریسک (مانع مبدأ) | احتمال | تأثیر | کاهش ریسک |
|---|---|---|---|
| تداخل دو پیاده‌سازی موج ۱ به ادغام ناقص منجر شود (پ0-۱) | متوسط | بحرانی — واگرایی داده در تولید | گیت `tools/wave1-gate.js` روی `main` مرج‌شده پیش از هر استقرار؛ بوت دیتابیس-اول + پایش واگرایی تا رفع کامل |
| استیجینگ چندنوده به‌موقع پروویژن نشود (پ0-۲/۳/۴ مشترک) | بالا | بحرانی — کل گام‌های ۳ تا ۶ مسیر بحرانی می‌خوابد | شروع پروویژن موازی با مرج‌ها (گام ۰/۱)؛ اعلان کمبود منابع به ناظر حداکثر تا پایان هفتهٔ اول |
| اجرای بار، گلوگاه پیش‌بینی‌نشده را نشان دهد و بازطراحی بخواهد (پ0-۲) | متوسط | بالا — عقب‌افتادن تی-صفر | اجرای زودهنگام لود پایه پیش از سوک کامل؛ معیار ردِ صریح در `docs/LOAD_TEST_PLAN.md` § پذیرش |
| دریل بازیابی شکست بخورد و آر‌تی‌اوی ۱۵ دقیقه نقض شود (پ0-۳) | متوسط | بحرانی — کل مدل پایایی زیر سؤال می‌رود | دریل خشک (بدون ترافیک) پیش از دریل کامل؛ مستندسازی ریشه و تکرار تا پاس شدن (`docs/DR_RUNBOOK.md` §۶) |
| پنتست خارجی آسیب‌پذیری بحرانی بیابد (پ0-۴) | متوسط | بحرانی — توقف انتشار یا انتشار ناامن | رفع پیش از هر ترافیک واقعی؛ ریت‌لیمیت سخت‌گیرانه + پایش دستی لاگ به‌عنوان پوشش موقت |
| استک رصد زیر ترافیک واقعی سیگنال ندهد یا وب‌هوک خاموش باشد (پ0-۵) | کم | بالا — کوری در لحظهٔ حادثه | دریل اعلان اجباری پیش از تی-صفر؛ کشیک انسانی شیفتی به‌عنوان پشتوانه تا فعال‌شدن |
| باندل/کامیت‌های محلی نشست‌های بسته هرگز پوش نشوند (پ0-۶) | کم | بالا — ۱۶ کامیت سند و تست گم می‌شود | باندل‌ها در `/home/user/` نسخهٔ پشتیبان دارند؛ هندآف و رفلو وضعیت را ثبت کرده‌اند؛ اولین نشستِ با دسترسی، پوش می‌کند |
| کهنگی زنجیره‌ای مرج‌نشده‌ها با هر مرج تازه (پ0-۶) | بالا | متوسط — ریبیس‌های مکرر (تجربهٔ چهاربارهٔ #۴۷) | ترتیب گام ۰: کوچک‌ها زودتر مرج شوند؛ ریبیس روزانهٔ شاخه‌های باز تا بسته‌شدن |

---

## ۶) اقدام‌های هفتهٔ پیش رو (۲۰۲۶-۰۹-۱۱ تا ۲۰۲۶-۰۹-۱۷)

| چه کسی | اقدام | مانع | مهلت |
|---|---|---|---|
| ناظر / چت با دسترسی گیت‌هاب | پوش باندل `go-live-package.bundle` و ۱۶ کامیت این نشست؛ مرج پی‌آرهای بازِ مانده (#۲۲ و #۲۹) طبق `docs/PR_MERGE_PLAN.md` — #۳۵ در بستهٔ هفت‌گانه مرج شد | پ0-۶ | ۱۲ سپتامبر |
| چت ۱ + ناظر | تعیین‌تکلیف پیاده‌سازی `writes` چت ۳ در برابر موج ۱ چت ۱ و ادغام نهایی | پ0-۱ | ۱۴ سپتامبر |
| چت ۲ / چت ۴ | پروویژن میزبان رصد + بالاآوردن استک کمپوز + اتصال وب‌هوک واقعی و دریل اعلان | پ0-۵ | ۱۴ سپتامبر |
| چت ۲ | پروویژن استیجینگ چندنوده: پستگرس + رپلیکا + آرشیو دبلیو‌ای‌ال + ردیس | پ0-۳ (و پیش‌نیاز ۲/۴) | ۱۵ سپتامبر |
| چت ۳ / چت ۵ | ساخت پی‌آر دیتاست ملی (`seed-national.js`) و مرج پس از گیت‌ها — تعیین‌تکلیف با مولد موجود `generate-national-dataset.js` (قلم باز ۲ گزارش هماهنگی) | پ0-۲ | ۱۵ سپتامبر |
| چت ۴ | پوش و پی‌آر «ابطال نشست» | پ0-۶ | ۱۶ سپتامبر |
| چت ۱ | هماهنگی تیم/پیمانکار پنتست + برنامه‌ریزی دی‌ای‌اس‌تی روی استیجینگ تازه | پ0-۴ | ۱۷ سپتامبر |
| چت ۵ | اجرای آزمایشی (درای‌ران) سناریوهای آشوب روی استیجینگ — آماده برای هفتهٔ اجرا | پ0-۳ | ۱۷ سپتامبر |
| چت ۶ | به‌روزرسانی روزانهٔ این ردیاب + همگام‌سازی §۸ و §۵ بستهٔ انتشار با هر تغییر وضعیت | همه | روزانه |
| کارفرما + چت ۶ | اقلام کسب‌وکاری/حقوقی گو-لایو (اینماد، مجوز آموزشی، میزبانی داده، پیامک، استعلام) طبق `docs/IRAN_COMPLIANCE_PACKAGE.md` — ۷ تصمیم دی-۱ تا دی-۷ در انتظار امضا؛ فرآیند انتخاب وندورها: `docs/VENDOR_PLAYBOOK.md` | پ0-۶ (افق کسب‌وکار) | شروع فوری (مسیر مجوز آموزشی بحرانی‌تر از مسیر فنی است) |

---

## یادداشت صحت‌سنجی

- وضعیت‌ها از اسناد داخل مخزن استخراج شده‌اند (چک‌لیست آمادگی، بستهٔ انتشار، طرح ادغام، نقشهٔ پیشرفت، هندآف) — نه از حافظه؛ در نشست جاری دسترسی به گیت‌هاب دور برای تأیید زندهٔ پی‌آرهای باز وجود ندارد و موارد وابسته صریحاً علامت خورده‌اند.
- شماره‌گذاری شش‌گانهٔ ابلاغ تیمی با ردیف‌های §۸ بستهٔ انتشار تطبیق داده شده است (فیلد «معادل §۸» در هر کارت + جدول §۸) تا هر دو سند هم‌زمان به‌روز بمانند.
- درصد ۴۰٪ = ۱۲ گام کامل از ۳۰ گام تعریف‌شده در «مسیر رفع» شش کارت؛ با تغییر گام‌ها، عدد به‌صورت دستی در همین‌جا به‌روز می‌شود.
