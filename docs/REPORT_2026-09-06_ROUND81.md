# گزارش تجمیعی دور ۸۱ — دادهٔ نمونهٔ پایلوت + استقرارِ تولید + قراردادِ درگاه — ۱۵/۰۶/۱۴۰۵

> دورِ «آماده‌سازیِ عملیات»: هر سه قدمِ بعد از پایلوت (دادهٔ پایه، استقرار،
> درگاهِ پیامک) یا **کاملاً کار آمدند** یا **قراردادِ دقیقِ کارِ باقی‌مانده**
> قفل شدند.

## ۱. خلاصه

۴ بند — ۵ کامیت — ۲ سندِ تازه + ۱ فایلِ آزمونِ تازه (۱۱ بررسی + ۴ جهش) +
۵ سندِ به‌روز — بستهٔ کاملِ آزمون سبز.

## ۲. جدولِ بندها

| # | بند | نتیجه | کامیت |
|---|---|---|---|
| ۱ | **فایل‌هایِ نمونهٔ ورودِ اطلاعات + سئوتِ سرتاسری** | ✅ `docs/samples/students_sample.csv` + `teachers_sample.csv` (ستون‌هایِ جابه‌جاشده، ارقامِ فارسی، کد ملیِ معتبرِ checksum) · `tests/import2.js` **11/11** از مسیرِ واقعیِ ویزارد: ۴ دانش‌آموز (۳ کلاسِ موجود) + ۵ ولی + ثبت‌نام + **جایگزینیِ شمارهٔ پدر وقتی دانش‌آموز شماره ندارد** + ۳ دبیر + خطایِ نامِ الزامی + خطایِ کد ملیِ تکراری · جهش‌ها **4/4** · یادداشتِ نمونه‌ها در PILOT_ONBOARDING §۵ | `05d460b` |
| ۲ | **`DEPLOY.md` — دستورِ کارِ استقرارِ تولید** | ✅ سخت‌افزار/پیش‌نیاز · نصب · جدولِ متغیرهایِ محیطیِ **دقیق از کد** (۱۰ متغیر با «چرا» — مهم‌ترین: `PAYESH_DEMO_CODE=0`) · TLS با certbot (پیش‌فرض: درون‌پروسه) · systemd + hardening · بکاپ/بازیابی + **کپیِ بیرونیِ هفتگی به آروان‌کلاود** (طبقِ قفل) · بروزرسانی · **چک‌لیستِ ۱۰ بندیِ go-live** · `server/README.md` همگام (ادعایِ قدیمیِ «رزرو/Express» باطل شد — صفرِ وابستگی) | `290a532` |
| ۳ | **`PLAN_SMS_GATEWAY.md` — قراردادِ درگاه (طراحیِ قفل)** | ✅ مرزِ دقیقِ شبیه‌سازی شناسایی شد (تنها `notifyApprove` — نوشتنِ `sms_log` به‌جای زنگِ درگاه) · ۴ شکاف با شواهد (صف در `WRITE_PERMS` نیست؛ ندایِ `/api/sms/*` نیست؛ مالکیتِ کیف باید سرور باشد؛ درگاهِ خارجی) · معماریِ هدف قفل شد: **ارسالِ فقطِ سمتِ سرور** + سازگارِ قابلِ تعویض + ایدمپوتانس (`queue_id+parent_id`) + «همه یا هیچ» + سقفِ روزانه + dry-run + ترازِ هفتگی · switchoverِ ۵ گامه + طراحیِ آزمونِ server11 (S1–S8 + جهش‌ها) · **قفلِ ۸۱.۱ در AD** · کدِ درگاه عمداً بدونِ قرارداد نوشته نشد | `ad7a30d` |
| ۴ | بستهٔ کاملِ آزمون + HANDOFF + گزارش | ✅ همهٔ سبز (جدولِ §۴) | (این کامیت) |

## ۳. کد در برابرِ مستندات

- **کد/آزمونِ تازه:** فقط بند ۱ — `tests/import2.js` + `import2-mutations.js` + دو فایلِ نمونه (کدِ محصول **دست‌نخورده** — نمونه‌ها فقط از مسیرِ موجود گذشتند).
- **فقطِ طراحی/تصمیم (عمداً بدونِ کد):** بندهای ۲ و  — DEPLOY و PLAN هرکدام **قراردادِ دقیق** برایِ کاری‌اند که به خرید/قراردادِ خارجی وابسته است.

## ۴. آزمون‌ها — بستهٔ کامل (بیلدِ تازه)

| گروه | خروجی |
|---|---|
| smoke | ۵۴۶/۵۴۶ ✅ |
| run.js | ۳۳/۳۳ ✅ |
| server1–10 | ۱۵۴/۱۵۴ ✅ |
| sim_full2 / sim_full3 | ۵۸/۵۸ · ۲۴/۲۴ ✅ |
| **import2 (تازه)** | **۱۱/۱۱** + جهش‌ها **۴/۴** ✅ |
| trendcmp2 / underpriv2 / reporttpl2 / gradeavg2 / pathway2 | ۷/۷ ✅ (+ جهش‌ها) |
| report2 | ۱۰/۱۰ ✅ |
| officesupp2 | ۵/۵ ✅ |
| uiclick (+ جهش‌ها) | ۴/۴ (+ ۵/۵) ✅ |
| att2 / nudge2 / bell2 / subs2 / workdays | ۶/۶ · ۹/۹ · ۸/۸ · ۹/۹ · ۶/۶ ✅ |

## ۵. پیداکردهایِ مهم (ثبت در HANDOFF)

1. `commitImport(st)` انتظار **`st.entity` + `st.preview`** دارد — صدا زدنِ مستقیم با
   فقطِ `preview`، دانش‌آموز را با `role='teacher'` می‌سازد (تستِ I5 گرفت).
2. صف/کیف/لاگِ پیامک در `WRITE_PERMS` نیستند (fail-closed) — شکافِ ۱ِ قراردادِ درگاه.
3. دامِ سلولِ CSV: یک ستونِ جابه‌جا = ولیِ «نامش شمارهٔ تلفن» می‌شد — سئوتِ I4/I6 گرفت.

## ۶. چی موند (نقشهٔ عملیاتیِ تازه)

| قدم | وضعیت | سند |
|---|---|---|
| مدرسهٔ پایلوت + تاریخ | **تصمیمِ کاربر** — حالا با: PILOT_KICKOFF + نمونه‌هایِ داده + ONBOARDING | §۱ |
| قراردادِ درگاهِ پیامک | بیرونی — وقتی امضا شد: **گامِ ۱ِ PLAN** (کدِ server/sms.js + server11 + endpoint) | §۲ |
| سرور + دامنه | بیرونی — وقتی خرید شد: **DEPLOY** (۲–۳ ساعت) | §۳ |
| حسابِ Play + ۱۲ آزمایش‌کننده | بیرونی | PLAY_STORE_CHECKLIST |

**ترتیبِ منطقی (قفل در DEPLOY §۹):** درگاه ← سرور+دامنه ← go-live.

## ۷. GitHub

۵ کامیتِ دور ۸۱: `05d460b` ← `290a532` ← `ad7a30d` ← (این گزارش).
push + `git ls-remote` در پایانِ دور.

---
*گزارشِ تجمیعیِ دور ۸۱ — طبقِ اصلِ کارفرما این فایل مستقیم ارائه شد.*
