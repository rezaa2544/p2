# طرح: نمای زندهٔ زنگ برای سه نقش + تلنگر حضور و غیاب

> **وضعیت: طرح — منتظر تأیید. هیچ کدی نوشته نشده.**
> دور ۵۷ · کامیت مبنا: `8ccc312`

---

## ۱. چهار خواسته

| # | خواسته | شدنی است؟ |
|---|---|---|
| ۱ | ولی ببیند فرزندش الان کلاس است یا تفریح | ✅ آسان |
| ۲ | دبیر با رسیدن هر زنگ، کلاس همان زنگ را ببیند | ✅ نیمی آماده |
| ۳ | مدیر ببیند کدام کلاس حضور و غیاب کرده و کدام نه | ✅ متوسط |
| ۴ | تلنگر مدیر ⇒ **نوتیفیکیشن روی گوشی خاموش دبیر** | 🔴 **در معماری فعلی ناممکن** |

---

## ۲. آنچه از قبل هست 🟢

دورهای ۴۸ و ۵۰ زیرساخت را ساختند:

```js
currentSlot(schoolId, now)     // الان زنگ چندم؟ تفریح؟ تعطیل؟
teacherNowClass(tid, sid, now) // این دبیر الان کدام کلاس؟
bellNowBar(now)                // نوار نمایشی — فقط دبیر
bellAutoAllowed(sid)           // آیا مدرسه زمان‌بندی ثبت کرده؟
```

⚠️ **`bellBarApplies()` امروز فقط `role === 'teacher'` را
می‌پذیرد** (`46-bell-now.js:252`). برای ولی و مدیر باید گسترش
یابد.

---

## ۳. خواستهٔ ۱ — نمای ولی

### چه چیزی نمایش داده شود

```
┌────────────────────────────────────────┐
│ 👨‍👩‍👦 علی رضایی — دهم تجربی ۱          │
│                                        │
│ 🔔 زنگ سوم (۰۹:۲۵ تا ۱۰:۱۰)            │
│    درس: ریاضی · دبیر: آقای محمدی        │
│    ✅ امروز حاضر بوده                   │
└────────────────────────────────────────┘
```

**حالت‌های دیگر:**
| وضعیت | نمایش |
|---|---|
| تفریح | `☕ زنگ تفریح — زنگ بعدی ۱۰:۲۰` |
| پیش از مدرسه | `🕗 مدرسه هنوز شروع نشده` |
| پس از مدرسه | `🌆 ساعت مدرسه تمام شده` |
| تعطیل | `🌙 امروز روز درسی نیست` |
| چند فرزند | یک کارت برای هر فرزند |

### 🔴 دو تصمیم حریم خصوصی

**الف) آیا ولی باید نام دبیر را ببیند؟**
پیشنهاد من: ✅ بله — اطلاعات عمومی برنامهٔ هفتگی است.

**ب) آیا ولی باید وضعیت حضور **همان لحظه** را ببیند؟**
🔴 پیشنهاد من: **با احتیاط.** اگر دبیر هنوز حضور و غیاب نکرده،
نمایش «غایب» غلط و نگران‌کننده است. سه حالت:

```
✅ حاضر               ← ثبت شده
❌ غایب               ← ثبت شده
⏳ هنوز ثبت نشده      ← نه «غایب»!
```

⚠️ **این مهم‌ترین نکتهٔ این بخش است.** ولی که ببیند «غایب» و
بعد معلوم شود دبیر فقط دیر ثبت کرده، اعتمادش به سامانه می‌رود.

**ج) دیوار پرداخت:** پنل ولی `parentLocked()` دارد. آیا این
کارت هم پشت اشتراک باشد؟ پیشنهاد من: **بله**، مثل بقیهٔ پنل.

---

## ۴. خواستهٔ ۲ — نمای دبیر 🟢 نیمی آماده

`bellNowBar()` از دور ۵۰ همین کار را می‌کند:

```
🔔 زنگ ۱ (۰۷:۳۰ تا ۰۸:۱۵) — دهم ریاضی فیزیک · فارسی ۱
```

### آنچه کم است

**الف) دکمهٔ میان‌بر.** نوار فعلی فقط نمایش است. باید دکمهٔ
«ثبت حضور و غیاب این کلاس» داشته باشد که مستقیم به صفحه با
کلاس **پیش‌گزینش‌شده** برود.

⚠️ **این همان گام ۳ طرح `PLAN_BELL_AUTOCLASS.md` است** که معلق
مانده. اگر آن اجرا شود، این خودکار حل می‌شود.

**ب) نشان «ثبت شده / نشده».**
```
🔔 زنگ ۳ — دهم تجربی · ریاضی    ⚠️ حضور و غیاب ثبت نشده
                                  [ ثبت کنم ]
```

---

## ۵. خواستهٔ ۳ — مانیتور زندهٔ مدیر

### نما

```
📡 وضعیت زنده — زنگ سوم (۰۹:۲۵ تا ۱۰:۱۰)

┌─────────────────┬──────────────┬────────┬─────────┐
│ کلاس            │ دبیر         │ حضور   │ اقدام   │
├─────────────────┼──────────────┼────────┼─────────┤
│ دهم تجربی ۱     │ محمدی        │ ✅ ۲۸/۳۰│    —    │
│ دهم ریاضی ۲     │ احمدی        │ ⏳ ثبت‌نشده│ [تلنگر] │
│ یازدهم انسانی   │ —            │ 🚫 بی‌دبیر│    —    │
└─────────────────┴──────────────┴────────┴─────────┘

✅ ۴ کلاس ثبت شده · ⏳ ۲ کلاس مانده
```

### منطق

```js
liveClassStatus(schoolId, now) → [{
  classId, className, teacherId, teacherName,
  attendanceDone,      // آیا برای این کلاس و امروز رکورد هست؟
  present, total,      // شمار حاضران
  nudgedAt             // آخرین تلنگر — جلوی اسپم
}]
```

**منبع داده:** تقاطع `schedule` (روز جاری + زنگ جاری) با
`idxAttByClassDate()` که از قبل هست.

⚠️ **دام کارایی:** برای هر کلاس نباید کل `attendance` پیمایش
شود. `idxAttByClassDate` کلید `cid|date` دارد — همان را به‌کار
ببرید.

### تلنگر

```js
nudgeTeacher(classId, teacherId) → {
  ok, reason   // 'sent' | 'too-soon' | 'already-done'
}
```

⚠️ **سه گارد لازم است:**
| گارد | چرا |
|---|---|
| فاصلهٔ ۱۰ دقیقه بین دو تلنگر | جلوی اسپم مدیر |
| اگر ثبت شده، تلنگر بی‌معناست | `already-done` |
| فقط برای زنگ **جاری** | تلنگر برای زنگ گذشته بی‌فایده است |

---

## ۶. 🔴 خواستهٔ ۴ — نوتیفیکیشن روی گوشی خاموش

### چرا در معماری فعلی ناممکن است

سنجش کد نشان داد:

```
Service Worker : ❌ ثبت نمی‌شود
manifest.json  : ❌ وجود ندارد
Push API       : ❌ به‌کار نرفته
سرور           : ❌ فقط پوشهٔ خالی رزروشده
```

**نوتیفیکیشن روی گوشی خاموش سه چیز لازم دارد که هیچ‌کدام نیست:**

۱. **Service Worker** — کد پس‌زمینه‌ای که وقتی صفحه بسته است
   اجرا می‌شود. نیازمند میزبانی روی **HTTPS** است؛ روی
   `file://` کار نمی‌کند.
۲. **Push Server** — سروری که پیام را به Firebase (اندروید) یا
   APNs (آی‌اواس) بفرستد.
۳. **اشتراک Push** — کاربر باید اجازه بدهد و مرورگر یک
   `endpoint` یکتا بسازد که روی سرور ذخیره شود.

🔴 **پایش امروز یک فایل HTML تک است که آفلاین کار می‌کند.**
دقیقاً همان چیزی که Push را ناممکن می‌کند.

### سه راه — با هزینهٔ واقعی

| راه | چه می‌دهد | هزینه | محدودیت |
|---|---|---|---|
| **الف) اعلان درون‌برنامه‌ای** | دبیر وقتی برنامه باز است می‌بیند | 🟢 ۰٫۵ روز | گوشی خاموش ⇒ نمی‌بیند |
| **ب) پیامک** | 🟢 روی گوشی خاموش هم می‌رسد | 🟡 ۰٫۵ روز + هزینهٔ پیامک | هزینه‌بر · تأخیر · آزاردهنده |
| **ج) PWA + Push** | ✅ دقیقاً خواستهٔ شما | 🔴 **۵ تا ۸ روز + سرور + HTTPS** | معماری عوض می‌شود |

### 🟢 پیشنهاد من: الف + ب با هم

**سازوکار دو مرحله‌ای:**

```
تلنگر مدیر
   ↓
۱. اعلان درون‌برنامه‌ای فوری (رایگان)
   ↓
۲. اگر دبیر تا ۵ دقیقه ندید ⇒ پیامک
```

**چرا این بهتر است؟**
- ۹۰٪ موارد دبیر برنامه را باز دارد (وسط کلاس است) ⇒ رایگان حل
  می‌شود
- ۱۰٪ باقی‌مانده پیامک می‌گیرند ⇒ هزینه کنترل‌شده
- **زیرساخت پیامک از دور ۴۲ آماده است** (`notifyRequest` با
  صف تأیید و سقف روزانه)

⚠️ **ولی صادقانه بگویم:** این «نوتیفیکیشن روی گوشی خاموش» نیست.
پیامک است. اگر واقعاً نوتیفیکیشن می‌خواهید، راه ج ناگزیر است و
باید **پس از** اتصال سرور انجام شود.

### اگر راه ج را خواستید — پیش‌نیازها

```
۱. سرور با HTTPS و دامنهٔ ثابت
۲. manifest.json + آیکن‌ها
۳. service-worker.js (خارج از index.html — نمی‌تواند inline باشد)
۴. کلید VAPID برای Web Push
۵. جدول push_subscriptions
۶. 🔴 iOS فقط از نسخهٔ ۱۶.۴ به بعد و فقط اگر کاربر
   «افزودن به صفحهٔ اصلی» بزند
```

🔴 **بند ۳ با معماری تک‌فایلی در تضاد است.** Service Worker باید
فایل جدا باشد. یعنی «خروجی نهایی همیشه یک `index.html`» — که
قاعدهٔ ثابت پروژه است — شکسته می‌شود.

⚠️ **این تصمیم معماری است، نه فنی.** باید آگاهانه گرفته شود.

---

## ۷. گام‌های اجرا (بدون راه ج) — ۴ تا ۵ روز

| گام | کار | زمان |
|---|---|---|
| ۱ | گسترش `bellBarApplies` به ولی و مدیر | ۰٫۵ روز |
| ۲ | کارت زندهٔ ولی + سه‌حالتی «هنوز ثبت نشده» | ۱ روز |
| ۳ | `liveClassStatus()` + مانیتور مدیر | ۱٫۵ روز |
| ۴ | `nudgeTeacher()` + سه گارد | ۰٫۵ روز |
| ۵ | اعلان + زنجیرهٔ پیامک پس از ۵ دقیقه | ۰٫۵ روز |
| ۶ | آزمون و تست جهش | ۱ روز |
| ۷ | اسناد | ۰٫۵ روز |

**جمع: ۵٫۵ روز** ⇒ با حاشیه **۴ تا ۵ روز** اگر گام ۳ طرح زنگ
از قبل انجام شده باشد.

⚠️ **وابستگی:** گام ۲ این طرح روی گام ۳ `PLAN_BELL_AUTOCLASS`
سوار است (پیش‌گزینش کلاس). اگر آن معلق بماند، دکمهٔ میان‌بر
دبیر کار نمی‌کند.

---

## ۸. 🔴 هشدارها

| ریسک | توضیح |
|---|---|
| نمایش «غایب» پیش از ثبت | 🔴 باید «هنوز ثبت نشده» باشد |
| بار مانیتور مدیر | هر رندر کل کلاس‌ها را می‌سنجد — ایندکس لازم است |
| اسپم تلنگر | فاصلهٔ ۱۰ دقیقه + بررسی «قبلاً ثبت شده» |
| ساعت دستگاه | همان محدودیت دور ۴۸ — تا سرور نیاید |
| هزینهٔ پیامک تلنگر | از کیف مدرسه کم می‌شود؛ سقف روزانه اعمال شود |

⚠️ **یک نکتهٔ انسانی:** تلنگر خودکار می‌تواند فشار روانی بسازد.
پیشنهاد می‌کنم **دستی** بماند (مدیر دکمه را بزند)، نه خودکار.
دبیری که سه دقیقه دیر کرده شاید مشغول آرام‌کردن کلاس است.

---

## ۹. پرسش‌های باز

۱. 🔴 **راه الف+ب کافی است یا PWA (راه ج) لازم است؟**
   ⚠️ راه ج معماری تک‌فایلی را می‌شکند.
۲. **کارت ولی پشت دیوار پرداخت باشد؟** پیشنهاد: بله
۳. **تلنگر دستی یا خودکار؟** پیشنهاد: دستی
۴. **ولی نام دبیر را ببیند؟** پیشنهاد: بله

---

📄 مرتبط: `docs/PLAN_BELL_AUTOCLASS.md` (گام ۳ معلق) ·
`46-bell-now.js` · `44-sms-notify.js`
