# 📌 ثبتِ تصمیم‌های ناظر — ۲۰۲۶-۰۹-۱۳ (از طریق هماهنگ‌کننده)

**ثبت‌کننده:** چت ۶ (حاکمیت مستندات + ممیزی) · **پایه:** `main` @ `523c1f5` (مرجِ PR #147) · **شاخهٔ ثبت:** `arena/01a09801-p2`
**منشأ:** بستهٔ تصمیمِ هماهنگ‌کننده (۵ حکم) · **روش:** هر حکم پیش از ثبت، روی مخزن راستی‌آزمایی شد.

> **قاعدهٔ صداقت (Rule ۱):** این سند تصمیم‌ها را **کورکورانه** کپی نکرده است. سه مورد از متنِ مصوب
> با شواهدِ مخزن ناهمخوان بود ⇒ **با عدد/متنِ اصلاح‌شده** ثبت شده و در §۰ مستند گردید. ثبتِ عددِ
> ردشده، نقضِ هم‌زمانِ Rule ۱ و گیتِ `docs-consistency-check` (تعارض عددی بین اسناد) بود.

---

## ۰. تصحیح‌های پیش از ثبت (بسیار مهم — سه مورد)

| # | متنِ مصوبِ هماهنگ‌کننده | واقعیتِ راستی‌آزمایی‌شده در مخزن | اقدامِ ثبت |
|---|---|---|---|
| **C-1** | «شکاف **۲۵–۴۰×** academic/finance هنوز بسته نشده» | `docs/REPORT_PERF_GAP_SPEC.md` §۰ صریحاً همین ادعا را **رد** کرده: «هیچ‌کدام در اسناد این مخزن نیست». واقعیِ measured: **~۲–۳× کندتر زیر هم‌زمانی** (بنچ ۵۰ درخواستِ هم‌زمان @‏200k حضور/60k نمره: حافظه p50≈۱۰۴–۱۰۸ms در برابر DB-native p50≈۲۲۶–۳۲۱ms، p95≈۳۶۴–۴۱۶ms). EXPLAINِ measured: academic ‏**7.8ms** · finance ‏**1.3ms+1.3ms**. مقیاس ملی = **NOT-RUN** (هدف scale=0.01 ≈1.4GB در سندباکس ~1GB RAM اجرانشدنی) | ✅ ثبت شد — **با عددِ اصلاح‌شده** (۲–۳× زیر هم‌زمانی؛ نه ۲۵–۴۰×) |
| **C-2** | Tenancy: «حذف `OR IS NULL` یعنی **~۱۹۰×** روی کرسر مرکب» | عدد **~۱۹۰×** متعلق به **ایندکسِ کرسرِ مرکبِ attendance** است (Seq ‏14.1ms → Index ‏0.074ms)، *نه* tenancy. عددِ tenancy در `docs/WAVE3_QUERY_PERFORMANCE.md` §یافتهٔ ۱ برابر **~۴۸۰× روی میانه** است (بازسنجیِ ۵ اجرا، ۲۰۲۶-۰۹-۱۰): `(school_id IS NULL OR school_id=$1)` = **۱۰۵.۱۶۸ms** در برابر `school_id=$1` = **۰.۲۱۹ms**؛ `Rows Removed by Filter: 648,000` | ✅ ثبت شد — **با عددِ اصلاح‌شده** (~۴۸۰×) |
| **C-3** | Tenancy: «NULL در seed برای **grades/attendance/classes** صفر است» | فقط **grades** مستند است: **۰ ردیف از ۷۲۰,۰۰۰** (همان سند). برای `attendance`/`classes` هیچ شمارشی در مخزن نیست و سیدِ `server/data/payesh.json` در ریپو **کامیت نشده** ⇒ محلی غیرقابل‌سنجش | ✅ ثبت شد — پیش‌شرطِ ناظر (شمارشِ NULL روی replica) **الزامی** و تعمیم‌یافته به هر سه جدول |
| **C-4** | «دوزیهٔ counselor — حکم **A** (بستن مجوز مرده)» | **بازنگریِ دور پ۶:** این ردیف **خطای خودِ ثبت‌کننده** بود. دوزیه وجود دارد — اما به‌صورت **بخش** در `docs/daily-reports/2026-09-12.md` §«دوزیهٔ counselor (بند ۱.۳ فاز ۱)»، نه فایل مستقل. جستجوی دورِ قبل فقط نامِ فایل و PRها را پوشش داده بود | ⏸ تعلیقِ اولیه ⇒ ✅ **ثبت‌شده در §۶** (همین سند، دور پ۶) |

> **نکتهٔ روش‌شناختی (C-1):** تضادِ موردِ نخست **درونِ خودِ مستندات است** — `REPORT_CACHE_ISOLATION_DOSSIER.md`
> §۳-پ و §۵-۳ می‌گویند «۲۵–۴۰×» ولی `REPORT_PERF_GAP_SPEC.md` §۰ همان را رد می‌کند. تا رفعِ این تضاد،
> **مرجعِ عددی = مشخصه** (جدیدتر و measured) است. رفعِ تضاد در دوزیه هنگام ACCEPTِ #129 انجام می‌شود.

---

## ۱. تصمیمِ ۱ — دوزیهٔ جداسازی کش گزارشی از دادهٔ عملیاتی

**سندِ مبدأ:** `docs/REPORT_CACHE_ISOLATION_DOSSIER.md` (در شاخهٔ PR #129؛ روی `main` نیست)
**حکم:** **ب اکنون + پ مسیر هدف + الف فقط مشروط**

- [x] **گزینهٔ ب — وضع موجود + سخت‌سازی تدریجی** (اقدام فوری؛ مرج‌آماده در #129: بنر shell + دفاع دولایهٔ P0-2 + قرارداد resume)
- [x] **گزینهٔ پ — مسیر هدف:** سوئیچ نماهای سنگین (`77-reports.js`) به endpointهای DB-native موج ۲۳
      · **پیش‌نیاز (اصلاح‌شده):** بستنِ شکافِ **هم‌زمانیِ** academic/finance — measuredِ امروز **~۲–۳×** زیر بار
      (و **نه** ۲۵–۴۰×؛ مقیاس ملی هنوز NOT-RUN) — **مالک: چت ۳**
- [ ] **گزینهٔ الف — جداسازی کامل** (فقط مشروط؛ تریگرهای §۵ دوزیه)

**پایشِ تریگر:** شمارندهٔ نمایشِ بنر `data-rpt-partial-shell` + سه کانترِ `/metrics`
(`payesh_pull_partial_collections_total` · `payesh_pull_full_snapshot_required_total` · `payesh_pull_resume_snapshot_total`؛
آستانهٔ پیشنهادی: نرخِ `full_snapshot_required` پایدار > ۱٪ کلِ pullها در هفت روز — گیت ۹/۹ + جهش ۹/۹) + سقفِ ردیف.

**امضا:** ناظر (به‌واسطهٔ هماهنگ‌کننده) · **تاریخ:** ۲۰۲۶-۰۹-۱۳ · **ثبت:** چت ۶

> **محلِ ثبتِ §۶ دوزیه:** متنِ بالا **در خودِ دوزیه پر نشد** — چون دوزیه روی `main` نیست و فقط در شاخهٔ
> PR #129 (که `DIRTY` است و متعلق به چت ۸) وجود دارد. درجِ آن در آن شاخه مصداقِ تداخل با PRِ بازِ
> جاری و ایجادِ کپیِ واگرا بود. **متنِ آمادهٔ درج** در «پیوست الف» همین سند است و هنگام ACCEPTِ #129
> (توسط چت ۸ یا پس از مرج) درج می‌شود.

---

## ۲. تصمیمِ ۲ — Tenancy و `school_id IS NULL`

**حکم:** **الف** — backfill + `CHECK NOT NULL` با `NOT VALID` سپس `VALIDATE`، **به‌جز `users`**

**شاهد (measured، `docs/WAVE3_QUERY_PERFORMANCE.md` §یافتهٔ ۱):**

| حالت | min | **median** | max |
|---|---:|---:|---:|
| فعلی: `(school_id IS NULL OR school_id = $1)` | ۱۰۳.۶۲۷ ms | **۱۰۵.۱۶۸ ms** | ۱۱۳.۵۵۶ ms |
| فقط `school_id = $1` | ۰.۱۸۱ ms | **۰.۲۱۹ ms** | ۰.۳۴۴ ms |

⇒ **~۴۸۰× روی میانه** (نه ۱۹۰×)؛ plan: `Index Scan Backward using grades_pkey` با `Filter` (هرگز `Index Cond`)
و `Rows Removed by Filter: 648,000`. حذفِ بازوی `IS NULL` **تغییرِ معناست، نه بهینه‌سازی** — باید با
مسیرِ JS هم‌زمان تغییر کند وگرنه برابریِ `tests/wave3-query.js` می‌شکند.

**پیش‌شرط‌های لازم‌الاجرا (تأکیدِ ناظر + افزودهٔ چت ۶):**

1. کوئریِ شمارشِ NULL روی **replica تولید/استیجینگ** برای **هر سه جدول** `grades` / `attendance` / `classes`
   (شمارش برای `attendance`/`classes` در مخزن موجود نیست — C-3).
2. استثنای `users` ثبت شد (superadmin / edu_office فاقد `school_id` هستند — جداولِ داده‌ایِ مدرسه‌محور مشمول نمی‌شوند).
3. ترتیب: backfill ⇒ `ADD CONSTRAINT … NOT VALID` ⇒ `VALIDATE CONSTRAINT` ⇒ سپس حذفِ بازوی `OR IS NULL`
   از SQL **و** هم‌زمان از مسیر JS + عبورِ `tests/wave3-query.js`.

---

## ۳. تصمیمِ ۳ — توصیه‌های ممیزی PR (هر دو پذیرفته شد)

| توصیه | وضعیتِ اجرا | شاهد/محل |
|---|---|---|
| چکِ `docs-stats-sync --check` در **چک‌لیست مرجِ docs-only** | ✅ **اجرا شد** — «بخش ۵٫۱» به `docs/DOCUMENTATION_MAINTENANCE.md` افزوده شد | همین کامیت |
| **چک‌باکس در قالب PR** | ✅ **اجرا شد** — `.github/pull_request_template.md` ساخته شد (پیش‌تر وجود نداشت) | همین کامیت |
| مرجِ **#135** پس از بازبینی | ⚠️ **حکمِ ممیزی: به‌همین شکل مرج نشود** — ببینید §۴ | §۴ همین سند |

**ریشهٔ هر ۴ رانشِ آماری** (تأییدِ ناظر): fast-mergeِ PRهای docs-only بدون اجرای `docs-stats-sync`.
گیتِ `docs-freeze-marker` فقط *اعتبارِ قفل* را می‌سنجد، **نه هم‌خوانیِ شمارها** — و `stats-sync --check`
در CI هم NOT-RUN است (بیلینگ). با چک‌لیست و قالبِ تازه، این شکافِ گیت بسته می‌شود.

---

## ۴. یافتهٔ ممیزیِ #135 — «مرج نشود به‌همین شکل»

`PR #135` (`fix/pr-audit-stats-drift`، +۲۲۶/−۱۰، ۷ فایل) در وضعیت **CLOSED** است و شاخه‌اش
(`fix/pr-audit-stats-drift` @ `0c57bb9`) موجود ⇒ بازگشایی مقدور است. اما محتوایش دو بخشِ متفاوت دارد:

| بخش | فایل‌ها | حکمِ ممیزی |
|---|---|---|
| **الف) رفعِ رانشِ آمار** | `DOCS_FREEZE_v1.0.0-rc38.md` · `DOCS_METRICS.md` · `DOCUMENTATION_MAP.md` · `TEST_COVERAGE_REPORT.md` | 🔴 **منسوخ و بالقوه مخرب.** اعداد متعلق به عصرِ rc38 هستند (درخت ۳۵۸ · تست ۴۶۳) در حالی که main اکنون rc40 است (درخت **۳۶۱** · تست **۴۶۵**) و `docs-stats-sync --check` **سبز** است ⇒ مرج، گیت را **قرمز** می‌کند. افزون بر این، ویرایشِ دستیِ هش‌های مانیفستِ **rc38** خلافِ قاعدهٔ «قفل‌های تاریخی دست‌نخورده می‌مانند» (thaw: فقط ابزار مجاز به بازتولید/بنر است) |
| **ب) خروجیِ ممیزیِ دور ۱** | `docs/daily-reports/2026-09-13-pr-audit-list.md` (فهرست ۱۲۴ PR) · `2026-09-13-pr-audit-report.md` · بخشِ ممیزی در `2026-09-12.md` | 🟢 **ارزشِ یکتا — روی main نیست** (`ls docs/daily-reports/` فاقد هر سه مورد است). این گزارش‌ها سرمایهٔ ممیزی‌اند و نباید گم شوند |

**توصیهٔ ثبت‌شده:** بازگشاییِ #135 + **شکافت** — حذفِ ۴ فایلِ آمار خودکار (بخش الف) و نگه‌داریِ
بخش ب؛ سپس `node tools/docs-stats-sync.js --freeze` برای بامپِ قانونی (rc41→rc42) و مرج.
**مرج با صلاحدیدِ مالک است** (قاعدهٔ ۶) — چت ۶ آن را انجام نمی‌دهد.

**منابع مرتبط — ممیزیِ دور ۲ (روی `main` از طریق PR #151؛ متمایز از دور ۱ در #135):**
[`2026-09-13-pr-audit-final.md`](daily-reports/2026-09-13-pr-audit-final.md) (گزارش نهاییِ ۱۳۴ PR) ·
[batch ۱](daily-reports/2026-09-13-pr-audit-batch-1.md) ·
[batch ۲](daily-reports/2026-09-13-pr-audit-batch-2.md) ·
[batch ۳](daily-reports/2026-09-13-pr-audit-batch-3.md) ·
[batch ۴](daily-reports/2026-09-13-pr-audit-batch-4.md) ·
[batch ۵](daily-reports/2026-09-13-pr-audit-batch-5.md) ·
[batch ۶](daily-reports/2026-09-13-pr-audit-batch-6.md)

> این هفت سند پس از ادغامِ `main@23bf513` در شاخهٔ این دور، تنها یتیم‌های `docs-metadata` بودند
> (صفر ارجاعِ ورودی). با ارجاعِ بالا بسته شدند — **بدون ویرایشِ خودِ آن‌ها** (قاعدهٔ ضدتداخل).

---

## ۵. تصمیمِ ۴ — ایندکس ۰۱۲: بدون اقدام

**حکم:** **بدون اقدام** (تثبیتِ NOT-ADDِ `BH-6`) — آزاد با معیارِ بازفعال‌سازیِ صریح.
**معیار:** اثباتِ `EXPLAIN (ANALYZE, BUFFERS)` که Seq Scanِ مسیرِ داغ نشان دهد **یا** زمانِ پرس‌وجو
در `scale ≥ 0.1` از **۱۰ms** بیشتر شود ⇒ آن‌گاه بازفعال‌سازی با شاهدِ قبل/بعد بررسی شود.
تا آن زمان ایندکسِ رزرو باقی می‌ماند و `migrate-helper --check` شمارهٔ بعدی را `011` نگه می‌دارد.

---

## ۶. تصمیمِ ۵ — دوزیهٔ counselor (ثبت: دور پ۶، ۲۰۲۶-۰۹-۱۳)

**حکم:** **A — بستنِ مجوز** (حذفِ counselor از `ins`ـِ `report_logs` و از `REPORT_ROLES.academic`)

**منبعِ دوزیه (تصحیحِ C-4):** دوزیه فایلِ مستقل نیست؛ در
[`docs/daily-reports/2026-09-12.md`](daily-reports/2026-09-12.md) §«دوزیهٔ counselor (بند ۱.۳ فاز ۱ —
بدون تغییر مجوز)» (بخشِ چت ۳، دور ۸۷) است.

> **تصحیحِ صادقانه (C-4 بازنگری‌شده):** در کامیتِ `18cf2f6` نوشتم «دوزیه در مخزن یافت نشد». این
> **خطای جستجوی خودم** بود — جستجوی پیشین روی فایل‌های مستقل و نامِ فایل متمرکز بود و بخشِ درونِ
> گزارشِ روزانه را ندید. دوزیه وجود دارد و حکم اکنون با ارجاعِ دقیق ثبت می‌شود. (Rule ۱ دربارهٔ
> ادعاهای خودِ من هم صادق است.)

**صورتِ مسئلهٔ مستند (نقل از دوزیه):** `model.json` نقشِ counselor را در `ins`ـِ `report_logs` دارد و
`REPORT_ROLES.academic` هم آن را می‌پذیرد؛ **اما منوی کلاینت (`05-router.js`) برای counselor صفحهٔ
`reports` ندارد** ⇒ counselor عملاً نمی‌تواند از UI گزارش بگیرد و `rptLog` برایش اجرا نمی‌شود، در
حالی که مجوزِ نوشتنِ sync را دارد.

**دلیلِ انتخابِ A (مطابق حکمِ ناظر):** fail-closed و هم‌راستا با UIِ فعلیِ محصول؛ بازگشاییِ آینده
(گزینهٔ B) فقط با تغییرِ مجوز **و** تستِ tenant-isolation برای مشاور.

**قیدِ اجرایی:** **تا پیش از این ردیف، هیچ کدی تغییر نمی‌کند**؛ پیاده‌سازی مالکِ جدا دارد.
چت ۶ فقط ثبتِ تصمیم و ردیفِ `NEXT_ACTIONS` را ساخت.

| بُعد | مقدار |
|---|---|
| مالکِ پیاده‌سازی | **چت ۴ / چت ۳** (نه چت ۶) |
| معیارِ قبولی | حذف از `ins report_logs` + `REPORT_ROLES.academic` · fail-closed باقی بماند · تستِ tenant-isolation سبز |
| بازگشاییِ آینده (B) | فقط با تغییر مجوز + تستِ tenant-isolation |

---

## ۷. یافتهٔ جنبی — یک سندِ یتیم (پیش‌موجود، رفع شد)

`node tools/docs-metadata.js` پس از این ثبت، **۱ یتیم** گزارش کرد:
`docs/daily-reports/2026-09-13-chat6-final-delivery.md` — با **صفر ارجاعِ ورودی در کلِ ریپو**
(راستی‌آزمایی: جستجوی سراسری به‌جز `node_modules`/`.git`/`_metadata.json`). این یتیمی ناشی از تغییرِ
این دور نیست (تغییراتِ من فقط افزودن بود) و با ارجاعِ زیر از همین سند بسته شد.

**منابع مرتبط:** گزارشِ روزانهٔ حاکمیتِ چت ۶ در
[`docs/daily-reports/2026-09-13-chat6-governance.md`](daily-reports/2026-09-13-chat6-governance.md) ·
گزارشِ تحویلِ نهاییِ چت ۶ در
[`docs/daily-reports/2026-09-13-chat6-final-delivery.md`](daily-reports/2026-09-13-chat6-final-delivery.md) ·
گزارشِ بوتِ سشن در `CHAT6_SESSION_BOOT_REPORT_2026-09-12.md` (ریشهٔ ریپو، بیرونِ مانیفست).

## ۸. بدهیِ ارجاعیِ آگاهانه (۵ مورد — نه پنهان، نه در baseline)

`node tools/docs-refs-check.js` در پایانِ دور پ۶: «تازه = **۱۹**» (پیش از دورِ چت ۶: ۱۱).
تفکیکِ دقیق: **۱۱ پیش‌موجود** (مربوط به چت ۸ و قدیمی‌ترها) + **۸ آگاهانه از چت ۶**
(۵ مورد در کامیت `18cf2f6` + **۳ مورد در دور پ۶**). همه به اسنادی ارجاع می‌دهند که **فقط در شاخهٔ
PR #129 / در PR #135 وجود دارند و روی `main` نیستند**:

| ارجاع | منبع | دور | وضعیتِ انتظار |
|---|---|---|---|
| `docs/REPORT_CACHE_ISOLATION_DOSSIER.md` | این سند + `CHAT6_SESSION_BOOT_REPORT_2026-09-12.md` | قبل | با ACCEPT/مرجِ #129 موجود می‌شود |
| `docs/FLAG1_SCOPE_SQL_DOSSIER.md` | `CHAT6_SESSION_BOOT_REPORT_2026-09-12.md` + این سند (§۸) | قبل + پ۶ | همان |
| `docs/REPORT_PERF_GAP_SPEC.md` | این سند + `CHAT6_DECISION_REGISTRATION_REPORT_2026-09-13.md` + بخشِ چت ۶ در `2026-09-13.md` | قبل + **پ۶ (۲ مورد)** | همان |
| `docs/daily-reports/2026-09-13-pr-audit-list.md` | این سند (بخشِ ارزشمندِ #135) | قبل | با مرجِ بخشِ ب از #135 موجود می‌شود |

**چرا حذف/تغییرشان نکردم:** ثبتِ تصمیم باید منبعش را ارجاع بدهد؛ حذفِ ارجاع برای سبز نگه‌داشتنِ
یک ابزار، دقیقاً همان «fake green» است که Rule ۱ نهی می‌کند. **چرا به baseline نیفزودم:**
`tools/docs-refs-baseline.json` بدهیِ *تاریخی* است و طبق HO-1 دست‌کاری‌اش ممنوع (قلمروی چت ۸).

**رفعِ خودکار:** هر چهار ارجاع با ACCEPTِ #129 (و بازگشاییِ بخشِ ب از #135) خودبه‌خود موجود می‌شوند.

## پیوست الف — متنِ آمادهٔ درج در §۶ دوزیه (برای ACCEPTِ #129)

```markdown
## ۶. تصمیم ناظر — ثبت ۲۰۲۶-۰۹-۱۳ (از طریق هماهنگ‌کننده؛ ثبت‌کننده: چت ۶)

[x] گزینهٔ ب — وضع موجود + سخت‌سازی تدریجی (اقدام فوری)
[x] گزینهٔ پ — مسیر هدف: سوئیچ نماهای سنگین به endpointهای DB-native موج ۲۳
    (پیش‌نیاز: بستن شکافِ هم‌زمانی academic/finance — measuredِ امروز ~۲–۳× زیر بار؛
     تصحیحِ «۲۵–۴۰×» طبق REPORT_PERF_GAP_SPEC.md §۰ — مالک: چت ۳)
[ ] گزینهٔ الف — جداسازی کامل (فقط مشروط؛ تریگرها طبق §۵)

پایش تریگر: شمارندهٔ نمایش بنر data-rpt-partial-shell + کانترهای /metrics + سقف ردیف.
امضا: ناظر (به‌واسطهٔ هماهنگ‌کننده) · تاریخ: 2026-09-13
```

## پیوست ب — فرمان‌های بازتولیدِ این ثبت

```bash
node tools/docs-stats-sync.js --check                 # همگامی آمار (باید سبز بماند)
node tests/docs-freeze-marker.js                      # ۱۴/۱۴
sed -n '/یافتهٔ ۱/,/بازسنجی/p' docs/WAVE3_QUERY_PERFORMANCE.md   # ۴۸۰×
gh pr diff 135                                        # یافتهٔ ممیزیِ #135
```
