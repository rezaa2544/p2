# گزارش تجمیعی دور ۶۹ — ۱۴۰۵/۰۶/۱۵ (۲۰۲۶-۰۹-۰۶)

> دو بخش: (الف) گزارش وضعِ ۱۲ بندِ دورِ قبل (۲۰۲۶-۰۹-۰۵) · (ب) پنج بندِ
> این دور، پشت‌سرهم و بدونِ توقف.

## الف) وضعِ ۱۲ بندِ ۲۰۲۶-۰۹-۰۵

گزارشِ تجمیعیِ آن دور قبلاً نوشته‌شده: **`docs/REPORT_2026-09-05_ROUND65.md`**.
خلاصهٔ وضع (به‌روزشده با بازگشت‌آزمونِ امروز):

| # | بند | وضع | شواهد |
|---|---|---|---|
| ۱ | مالی | ✅ انجام‌شده | `finance2` ۱۸/۱۸ |
| ۲ | اشتراک ولی (دانش‌آموز-محور) | ✅ انجام‌شده | `subs2` ۹/۹ |
| ۳ | منو (گروه‌بندی) | ✅ انجام‌شده | `navgroups` ۹/۹ |
| ۴ | تقویم (روزهای کاری + جبرانی) | ✅ انجام‌شده | `workdays` ۶/۶ + جهش ۴ |
| ۵ | نمای زنده (زنگ زنده ۴۵ ثانیه) | ✅ انجام‌شده | `bell2` ۸/۸ · `server5` ۱۴/۱۴ |
| ۶ | مدارس کم‌برخوردار (سیگنال به رئیس اداره) | ✅ انجام‌شده | `officesupp2` ۵/۵ |
| ۷ | **بورسیه (۲.۴)** |  **انجام‌نشده** | فقط روی نقشه (فاز ۲) — این دور هم در برنامه نبود |
| ۸ | طراحی استورِ فایل | ⏸️ قفل/ON HOLD | تصمیم قفل‌شده در AD.md — منتظرِ پیاده‌سازی |
| ۹ | pull سرویس (۳۰-۶۰ ثانیه، نه websocket) | ✅ انجام‌شده | `server5` |
| ۱۰ | امنیتِ ورودِ بدونِ رمز | ✅ انجام‌شده | `server1` ۳۰ · `server2` ۲۵ |
| ۱۱ | پاک‌سازیِ کدِ مرده | ⚠️ جزئی | انجامِ عمده در دورِ ۶۷؛ باقی‌ماندهٔ جزئی |
| ۱۲ | سنجهٔ ۳۰ ثانیه (حضور) | ✅ انجام‌شده | سبزیِ `att2` + جهش‌ها |

## ب) پنج بندِ این دور (هرکدام commitِ جدا)

| بند | موضوع | commit | کد | تست |
|---|---|---|---|---|
| B1 | ۴.۲ هنرستان: نمرهٔ عملی/کارگاهی + ساعتِ کارآموزی | `f9b9a79` | ۱۳-grades, 17-student-record, 18-modals, 19-actions, 30-authz, 02-demo-data, server/sync.js | `workshop2` ۱۲ + جهش ۴/۴ |
| B2 | ۲.۲ IEP: برنامهٔ آموزشی فردی (فیلدِ آزاد) | `b561c8a` | 17-student-record, 18-modals, 19-actions, 30-authz, 02-demo-data, server/sync.js (`IEP_KEYS`/`iepUsersUpdate`) | `iep2` ۷ + `iep3` ۸ + جهش ۳/۳ |
| B3 | ۴.۴ قیف پیش‌ثبت‌نامِ رقابتی | `084fc54` | **ماژولِ تازه** 61-preapp.js + 02-demo-data + 05-router + 07-shell + 18-modals + 19-actions + 30-authz + server/sync.js + USER_GUIDE + AI_PROMPT | `preapp2` ۸ + `preapp3` ۹ + جهش ۸/۸ |
| B4 | معیارهای خروجِ فازها (فقط سند) | `00b04aa` | — (فقط `docs/ARCHITECTURE_DECISIONS.md` ضمیمهٔ ج) | — (ملاکِ قابلِ اندازه‌گیری، نه کد) |
| B5 | ۴.۹ مقایسهٔ نمره با میانگین کلاس | `6cb2374` | — (**نمایش قبلاً ساخته‌شده بود**؛ فقط قفل) | `gradeavg2` ۷ + جهش ۴/۴ |
| — | به‌روزرسانیِ الگوی M3 (محرکِ کهنه بعد از B2) | `b6adfdd` | — | `server-mutations` بازگشت به ۱۹/۱۹ |

### B1 — هنرستان (۴.۲)
- نوعِ نمرهٔ «عملی/کارگاهی» فقط در مدارسِ `has_workshop` (تک‌گزینه در فرمِ
  نمره، نشان در فهرست، ستون در کارنامه).
- **ساعتِ کارآموزی**: فقط پایهٔ آخر؛ رکورد = تاریخ/ساعت/مکان/تأیید. تأیید
  فقط دبیرِ کلاسِ دانش‌آموز یا مدیر (گارد در **لایهٔ داده**:
  `canApproveInternship` در 04-queries)؛ حذف فقط مدیر؛ دانش‌آموز/ولی
  فقط‌خواندنی.
- دمو: دانش‌آموزِ IZ-105 با ۲ رکورد (یکی تأییدشده، یکی در انتظار).

### B2 — IEP (۲.۲)
- با `has_iep` روی مدرسه: کارتِ «برنامهٔ آموزشی فردی» در تبِ پرونده.
  **دو فیلدِ آزاد** (یادداشت + کادرِ کمکی) + تاریخِ به‌روزرسانی —
  به‌صورتِ آزاد، نه فرمِ سفت (بدون جدولِ تازه: روی رکوردِ دانش‌آموز).
- ویرایش: فقط دبیر/مدیرِ همان مدرسه (آکشن `iep-save` در ACTION_ROLES).
- **سرور fail-closed با استثنأِ محدوده‌دار:** `iepUsersUpdate` — دبیر
  `upd` روی `users` فقط با کلیدهای `iep_notes/iep_staff/iep_updated`
  (دامنهٔ `inScope` جدا و کامل). insert یا هر فیلدِ دیگر → `role_denied`.
- دمو: دانش‌آموز ۱۶ (مدرسهٔ SH-101 با `has_iep`).

### B3 — قیف پیش‌ثبت‌نام (۴.۴)
- **جدولِ تازه `preapps`** — عمدتاً **جدا** از `pre_enrollments` (۰.۲ —
  پیش‌ثبت‌نامِ سالِ بعدی) و `enrollments` (ثبت‌نامِ رسمی): **هیچ تبدیلِ
  خودکاری نیست** (تستِ P7 قفلش کرده).
- چهار مرحله: تماسِ اولیه → بازدید → آزمونِ ورودی → ثبت‌نامِ قطعی.
  صفحه = ۴ شمارنده + جدول + «→ مرحلهٔ بعدی». **فقط جلو**: گارد در دو لایه
  (دکمه در رندرِ `61-preapp.js` + گاردِ اکشن در `19-actions.js`).
- فقط مدیر + دامنهٔ مدرسه خود (تستِ P2: ردیفِ مدرسهٔ دیگر دیده نمی‌شود).
  سرور: `preapps` در WRITE_PERMS مدیر (`preapp3` A1/A2 قفلش کرده).

### B4 — معیارهای خروج (فقط سند)
- ضمیمهٔ ج در AD.md: فاز ۰ = **خارج‌شده** (۴ بند قفل‌شده، همه با تستِ
  نگهبان). فاز ۱ = ۵ شرط: (۱) ۳ پایلوتِ واقعی با ۲۰+ روزِ حضور · (۲)
  ثبتِ حضورِ هر کلاس < ۳۰ ثانیه · (۳) ۱۰۰ پیامک ≥۹۵٪ روی درگاهِ واقعی ·
  (۴) ۵ تأییدِ کاذبِ مثبتِ ریسک + یک ارجاعِ مشاورِ واقعی · (۵) سبزیِ
  هر commit. **امروز: ۲ و ۵ سبز؛ ۱، ۳، ۴ منتظرِ دنیای واقعی** — یعنی
  فاز ۱ «کدِ کامل، تأییدِ میدانیِ ناقص» است، نه «تمام‌شده».

### B5 — میانگین کلاس (۴.۹)
- **کشف: قبلاً ساخته‌شده بود** — `classScoreContext` (04-queries) +
  بجِ «· کلاس: X.XX» کنارِ هر نمره در تبِ نمراتِ پرونده و پنلِ ولی.
- این commit فقط **قفل** کرد: دقتِ میانگین، **ناشناسی** (خروجی فقط
  avg/n/students — نه نام، نه شناسه)، بج در نمایِ مدیر و ولی، آستانهٔ
  «حداقل ۲ دانش‌آموز» (با گروهِ تک‌نفرهٔ مصنوعی).

## بازگشت‌آزمونِ کامل (دورِ ۶۹)

| suite | نتیجه | suite | نتیجه |
|---|---|---|---|
| run | ۳۳/۳۳ ✅ | server3 | ۱۹ ✅ |
| navgroups | ۹/۹ ✅ | server4 | ۱۶/۱۶ ✅ |
| bell2 | ۸/۸ ✅ | server5 | ۱۴/۱۴ ✅ |
| schoolmode2 | ۶/۶ ✅ | server6 | ۹/۹ ✅ |
| workdays | ۶/۶ ✅ | server7 | ۱۵/۱۵ ✅ |
| nudge2 | ۹/۹ ✅ | server8 | ۹/۹ ✅ |
| att2 | ۶/۶ ✅ | server9 | ۱۰/۱۰ ✅ |
| att2-mutations | ۵/۵ کشته ✅ | server10 | ۷/۷ ✅ |
| privacy | ۳/۳ ✅ | server-mutations | **۱۹/۱۹ کشته** ✅ |
| finance2 | ۱۸/۱۸ ✅ | server2 | ۲۵/۲۵ ✅ |
| subs2 | ۹/۹ ✅ | iep2 / iep3 | ۷/۷ · ۸/۸ ✅ |
| association2 | ۶/۶ ✅ | workshop2 | ۱۲/۱۲ ✅ |
| officesupp2 | ۵/۵ ✅ | preapp2 / preapp3 | ۸/۸ · ۹/۹ ✅ |
| gradeavg2 | ۷/۷ ✅ | جهش‌ها (workshop/iep/preapp/gradeavg) | ۴+۳+۸+۴ = ۱۹/۱۹ کشته ✅ |

**یک حادثه + رفع:** `server-mutations` بعد از B2، **۱۸/۱۹** شد — الگوی
محرکِ M3 (خطِ گاردِ نقش) با اضافه‌شدنِ استثنأ IEP تغییر کرده بود (محرکِ
کهنه، نه رگرسیون). با `b6adfdd` الگو به‌روز شد → ۱۹/۱۹.

## وضعیتِ GitHub
- ۷ commit در main: `f9b9a79` → `b561c8a` → `084fc54` → `00b04aa` →
  `6cb2374` → `b6adfdd` (+ این گزارش) — **push‌شده** و با
  `git ls-remote` سنجیده.

## کارِ باقی‌مانده
- **بازِ محصول:** بورسیه (۲.۴) هنوز فقط روی نقشه است؛ استورِ فایل (۱۳.۳)
  قفل/ON HOLD.
- **بازِ Play (خارجی/تصمیمی):** درگاهِ SMS + استعلامِ واقعی، TLS,
  ۱۴ روزِ closed test (≥۱۲ نفر), تلفنِ تماس, متن‌های ۳۰/۸۰/۴۰۰۰,
  content rating, فرمِ data-safety, دسترسیِ reviewer, D-U-N-S.
- **جزئی:** calloutِ کاربرگِ کارآموزی (B1 راهنما را نگرفت).
