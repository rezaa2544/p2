# گزارش جامع — از تحویل پروژه تا امروز (۲۰۲۶/۰۹/۵)

> **حیطه:** از اولین پیام راهبردیِ دورهٔ تازه (دستهٔ بندهای ۰.۱ تا ۱.۸)
> تا پایان همین روز — اصلِ سوپرادمین + تماسِ تلفنی + مِرجِ در‌جریان.
> دورهٔ پیش (دورهای ۱ تا ۴۷، زیرِ مدیریتِ رضا و احمد) در `HANDOFF.md`
> و گزارش‌های قدیمی مستند است و در این گزارش تکرار **نمی‌شود**.
>
> **قاعدهٔ گزارش:** آنچه واقعاً کد شد از آنچه فقط طرح/قفل شد جداست؛
> همهٔ اعداد با اجرایِ واقعی سنجیده‌اند، نه از حافظه.

---

## ۱. تصمیمات راهبردیِ این دوره (قفل)

| تصمیم | محتوا | ثبت |
|---|---|---|
| مشتری | **مدرسه‌ها — همهٔ مدل‌ها** (دولتی + غیردولتی). edu-office فقط برای لایسنس و اعتبار؛ مالی + مبانی مدرسهٔ دولتی اولویت | `ARCHITECTURE_DECISIONS.md` + `REPORT_2026-09-05_STRATEGIC_BATCH.md` |
| اصلِ واقعیت | همه‌چیز باید **واقعاً** پیاده باشد و تنها مرحلهٔ باقی‌ماندهٔ هر بخش «وصل به سرور و زیرسازی» باشد؛ نمایشِ ساده‌شده به‌جای واقعی، جایز نیست | «اصلِ واقعیت» در `ARCHITECTURE_DECISIONS.md` |
| نقشِ فایلِ تک | `index.html` تک‌فایلی = پوستهٔ دمو/عیب‌یابی؛ محصول همان سامانهٔ واقعی است (کلائنت‌سایدِ واقعی، آفلاین، بدون وابستگی) | همان‌جا + `AI_PROMPT.md` |
| ورودِ نسخهٔ آخر | بدون رمز — فقط احراز هویت (موبایل + کد ملی)؛ رمزهای کنونی فقط دمو | `PLAN_PHONE_AUTH.md` + قفل ۶.۱ |
| اصلِ سوپرادمین | سوپرادمین **هیچ محدودیتی** ندارد (مسیر + کنش، کنونی + آتی) — باطل‌کنندهٔ تفکیک دورِ ۴۷ | «اصلِ سوپرادمین بدون محدودیت» (جدید، این گزارش) |
| تماس | **فقط شمارهٔ تلفن** — ایمیل در کار نیست | «اصلِ تماسِ تلفنی» (جدید، این گزارش) |
| راهنما | `USER_GUIDE.html` **همیشه** همراهِ `index.html` به‌روز می‌شود (اصلِ پنجم) | `AI_PROMPT.md` + بند «اصلِ همگامیِ راهنما» |
| آمادگیِ گوگل‌پلی | همهٔ ملاک‌های رسمیِ ۲۰۲۶ از همین حالا اعمال/قفل شد تا روزِ آپلود فقط «ثبت» باشد | `PLAY_STORE_CHECKLIST.md` (۲۲ بند) |

---

## ۲. جدولِ کاملِ کَمیت‌ها (هر دو شاخه)

نقطهٔ آغاز: دستهٔ راهبردی (اولین کَمیت `d7a088a`). پایان: `a3706c3`.
**۵۱ کَمیت در هر شاخه** (ui-improvements دو کَمیتِ آخر را تازه دارد و هنوز
روی main نیست — مِرج در جریان است).

### دستهٔ ۱ — راهبردی (۱۰ بند + اسناد)

| کَمیت | بند | چیزی که **کد** شد |
|---|---|---|
| `d7a088a` | ۰.۱ | پروفایلِ قابلیتِ هر مدرسه (`has_*`) + پنهان‌سازیِ ماژول‌هایِ غیرمرتبط |
| `e442a7a` | ۰.۲ | سالِ تحصیلی موجودیتِ مستقل (جدول `school_years`) + قیفِ پیش‌ثبت‌نام |
| `8080f33` | ۰.۳ | منشأِ جلسهٔ امتحان: درسی/نهایی/جبرانی (فیلد `source`) |
| `866774d` | ۰.۴ | دادهٔ پایه برای ولی **رایگان**؛ دیوارِ پرداخت فقط ارتباط/درخواست |
| `85d1621` | ۰.۵ | `organization_id` ساختاری روی همهٔ مدارس (همیشه خالی — قفل ۵.۱) |
| `355e1aa` | ۰.۶ | فقط **سند**: طرح ورود با موبایل به قفل تبدیل شد |
| `229840d` | ۱.۸ | ویزاردِ اکسل چهار نوع می‌خواند (دانش‌آموز/دبیر/نمرات دورهٔ گذشته/حضور) |
| `d58c618` | ۱.۲ | برنامهٔ کلاسی با **دبیرِ پایه** |
| `8e2e829` | ۱.۵ | جابه‌جای دبیرِ غایب — تعیینِ موقتِ کلاس |
| `a00a91c` | ۱.۶ | گواهیِ نمرات — چاپ (PDF قفل است، قفل ۱.۶) |
| `c9c888a`+۳ | اسناد | گزارشِ دسته + ثبت SHAهای push + یادداشت |

گزارش: `REPORT_2026-09-05_STRATEGIC_BATCH.md` — smoke ۵۲۰/۵۲۰ در پایانِ دسته.

### دستهٔ ۲ — دورِ دوم (بند ۱.۷ + سرویس + طرحِ کلاس مجازی)

| کَمیت | محتوا |
|---|---|
| `05fc6f5` | **بند ۱.۷** (کارتِ امروز + سوییچرِ فرزندان + خلاصهٔ روزانه پیامکی) **و** سرویسِ مدرسهٔ بدون جی‌پی‌اس: نقشِ راننده + جداول `bus_routes`/`bus_students`/`bus_events` + پنلِ مدیر + پنلِ راننده — همه کد |
| `c04fa22` | کلاس مجازیِ سبک: **فقط** سنجشِ سازگاریِ IndexedDB + طرح (سند) |
| `8819bf2`+`9ce79a7` | گزارشِ دورِ دوم + بازنویسیِ بخشِ «خواسته‌های من» |

### دستهٔ ۳ — بچهٔ ده‌بندی (بندهای ۱ تا ۱۳ — هر بند کد + سئوت مستقل + جهش‌کُشی)

| کَمیت | بند | سئوت (در لحظه) |
|---|---|---|
| `83b2029` | ۱ کلاس مجازی (سبک): ماژول IndexedDB + نشست شاد/ویدیو + سؤالات | vclass ۱۱/۱۱ + ۴ جهش |
| `69bf2e0` | ۲ مدل چندپایهٔ زودهنگام — **فقط دادهٔ مدل** (قفل ۲.۱) | ۳ بخش + ۱ جهش |
| `2b7c3b5` | ۳ میانگینِ کلاس کنارِ هر نمره (پنهان‌نام) | classmodel ۶/۶ + ۲ جهش |
| `dbe388b` | ۴ تکالیف: بارگذاریِ فایل + تصحیحِ دیجیتال (خطِ خوددست روی تصویر) | homework ۸/۸ + ۶ جهش |
| `3cda352` | ۵ گیمیفیکیشنِ ابتدایی (مدلِ امتیازِ مدرسه‌پیکربند) | dojo ۶/۶ + ۵ جهش |
| `5ce70fa` | ۶ گواهیِ اشتغال + انتقالی: چاپ + کد احرازِ قطعی | certify ۶/۶ + ۵ جهش |
| `a36783b` | ۷ مدیریتِ مهمان‌ها | visitors ۵/۵ + ۵ جهش |
| `4579724` | ۸ کتابخانهٔ سبک (امانت/بازگشت/دیرکرد) | library ۵/۵ +  جهش |
| `8ab7f53` | ۹ املاک و موجودی (تجهیزات با وضعیت) | assets ۵/۵ + ۶ جهش |
| `8d004da` | ۱۰ اختلاف با سیدا (ورودِ دستی + مقایسهٔ خالص) | sidadiff ۵/۵ + ۸ جهش |
| `f2b9a06` | — | گزارشِ تجمیعیِ بندهای ۸–۱۰ |
| `1af5e4c` | ۱۱ سرویس ۲: پاسخِ اولیا + ثبتِ دوسویهٔ سوار/پیاده + مغایرت | bus2 ۵/۵ + ۶ جهش |
| `11269f0` | ۱۲ کلاس مجازی ۲ + تکالیف ۲ (حضورِ خودکار + رسانه/قفل/تایمر) | vclass2 ۴/۴ + homework2 ۵/۵ + ۹ جهش |
| `122c0fd` | ۱۳ مدرسه غیرحضوری (حالتِ روزانهٔ حضوری/مجازی) | schoolmode ۴/۴ + ۸ جهش |
| `a2fce22`+۲ | — | گزارشِ دستهٔ R1–R5 + اصلاحِ اعداد |

### دستهٔ ۴ — اصلِ واقعیت (بندهای ۱۴ تا ۶)

| کَمیت | بند | سئوت (در لحظه) |
|---|---|---|
| `6c5bb4e` | ۱۴ سرویس **واقعی**: جی‌پی‌اسِ واقعی + نقشهٔ مختصاتیِ واقعی + پیگیریِ واقعیِ مغایرت | bus3 ۷/۷ + ۸ جهش |
| `b89041b` | ۱۵ کلاس مجازی: **لینکِ اختصاصیِ هر دانش‌آموز** (توکن + گاردِ مالکیت روی داده) | vclass3 ۶/۶ + ۷ جهش |
| `c7f6f2b` | ۱۶ روزِ غیرحضوری: **عملِ واقعی** (گاردهای داده‌ای روی مسیرِ نوشتن + صفِ اولیا + نوارِ «مسدود است») | schoolmode2 ۶/۶ + ۸ جهش |
| `904424c` | — | ثبتِ اصل + بازنویسیِ قفل‌های ۱۱.۱/۱۲.۱/۱۳.۱ + ۱.۲ خارج شد |
| `3376c7b` (main) | — | هماهنگ‌سازیِ بیلد |

گزارشِ دسته: `REPORT_BATCH11-13.md` (ضمیمهٔ ۱۴–۱۶).

### دستهٔ ۵ — بررسی‌های باز + اسکرول + گرافیک

| کَمیت | شاخه | محتوا |
|---|---|---|
| `d63c90d` | main | `REPORT_OPEN_2026-09-05.md` — ۴ پرسشِ مسدودکننده + ۷ کارِ سروری قفل‌شده + ۳ طرحِ بدونِ کد (همه روی کد وارسی شده) |
| `92210ff` | main | `REPORT_UI_REVIEW_2026-09-05.md` — بررسیِ خط‌به‌خطِ شاخهٔ گرافیک + موضوعِ توکن |
| `f80e7b3` | main | اسکرول (رفع‌نشدنی ۱+۲): صفحهٔ ورود اسکرولِ عمودی گرفت؛ نوارِ افقیِ جدول **همیشه پایینِ مانیتور** — **فقط CSS** |
| `122237c` | main | اسکرول در حالتِ موبایل — فقط CSS |
| `70b0a7d` | ui | بهبودهای بصری (گلو، سایهٔ رنگی، ترنزیشن) — **فقط CSS** |
| `4bfba42`+`4171401` | ui | همان رفع‌نشدنی‌های اسکرول روی شاخهٔ گرافیک |

گزارش: `REPORT_SCROLL_FIX_2026-09-05.md`. صفر جاوااسکریپت؛ صفر تغییرِ کارکرد؛ باتریِ کامل سبز.

### دستهٔ ۶ — آمادگیِ گوگل‌پلی (دستور: «همهٔ ملاک‌ها را اعمال کن»)

| کَمیت | ui | main | محتوا |
|---|---|---|---|
| سیاستِ درونِ اپ | `9fca0c0` | `0b9a5d5` | ماژول `58-privacy.js` (سیاستِ ۸ بخشی داخلِ برنامه + لینکِ صفحهٔ ورود) + meta description + سئوت `tests/privacy.js` 3/3 |
| چک‌لیست + اسناد | `598c8c4` | `41d3d7a` | `PLAY_STORE_CHECKLIST.md` (**۲۲ ملاک: ۱۱ اعمال‌شده + ۱۱ قفلِ بسته‌بندی**) + `PRIVACY_POLICY.md` + `DATA_SAFETY.md` + بند ۹ در تصمیمات |

### دستهٔ ۷ — دو باگِ واقعی + راهنمای کاربر

| کَمیت | ui | main | محتوا |
|---|---|---|---|
| — | `58962ea` | — | HANDOFF |
| دیاگ | `4d4521d` | `8ab04e5` | دکمهٔ تعمیرِ کارت‌های دیاگ دوباره کار می‌کند (دسپاتچر شناسهٔ رشته را `Number` می‌زد ⇒ NaN) + سئوتِ **کلیکِ واقعی** D11 |
| IDB | `3e4d4dc` | `f277149` | استورِ دومِ پروندهٔ فایل (`hw_files`) حالا واقعاً ساخته می‌شود (خطای مرورگرِ واقعی) + fakeِ تست **هم‌رفتارِ واقعی** + سئوت `tests/idb2.js` 4/4 |
| راهنما | `29725eb` | `c1ddab2` | **`USER_GUIDE.html`**: ۲۰ تصویرِ واقعی (ورود/کلیکِ واقعی با مرورگرِ واقعی)، ۸ نقش، تک‌فایلی/آفلاین + **مکانیزمِ مُهر**: هر build راهنما را تازه می‌کند، `--check` راهنمایِ قدیمی = شکست |
| اسناد | `5483d98` | `a464410` | اصلِ پنجم + قفل‌های همگامی + گزارشِ دور |
| — | — | `77e626f` | مُهرِ راهنما برای بیلدِ main |

گزارش: `REPORT_2026-09-05_DIAG_GUIDE.md`. راستی‌آزمایی: لاگِ خطای **پروفایلِ تازهٔ Chrome خالی است** (پیش از رفع: ۱ خطای IDB در هر بار).

### دستهٔ ۸ — امروز: اصلِ سوپرادمین + تماس (تازه، در حالِ مِرج)

| کَمیت | شاخه | محتوا |
|---|---|---|
| `64330a0` | ui | **اصلِ سوپرادمین بدون محدودیت**: بای‌پسِ ساختاری در `canAction` و `canRoute` (هر دو دروازه، کنش‌های آتی را هم می‌پوشاند) + سئوتِ تازه `tests/superadmin.js` (۳ بخش: همهٔ کنش‌ها / همهٔ مسیرها / کلیکِ واقعی روی «خروجی») + به‌روزرسانیِ ۶ تستِ دورِ ۴۷ که انتظارِ معکوس می‌گرفتند |
| `a3706c3` | ui | **تماس = تلفن**: سه نقطهٔ تماس (سیاستِ درونِ اپ، سیاستِ منتشرشده، فرمِ داده) از «ایمیل/نامشخص» به «شمارهٔ تلفن» |

---

## ۳. چه چیزی **فقط طرح/قفل** است (هنوز کد نشده)

قیدِ صریح، طبقِ قاعده:

| مورد | وضعیت | زمانِ کامل‌شدن (قفل) |
|---|---|---|
| ورود با موبایل + احراز کد ملی (حذفِ رمز) | فقط سند + قفل | اولین مدرسهٔ واقعی با کاربران واقعی |
| PDF برای گواهی‌ها | HTMLِ چاپ‌شوندهٔ واقعی داریم؛ PDF قفل | اولین نیازِ رسمی (بایگانی/سپرد) |
| مدلِ چندپایه (UI) | فقط دادهٔ مدل + fallback | اولین کلاسِ چندپایهٔ واقعی |
| فرمت‌های غیرتصویریِ تکلیف + تحویلِ فایل به دستگاهِ دیگر | ساختارِ ردیف آماده (mime/size) | اولین قراردادِ غیرتصویری / مرحلهٔ سرور |
| نقش‌های جداگانهٔ پذیرش/کتابدار/انباردار | عمداً **نساخته** (مدیر مالک است) | اولین مدرسه با حسابِ جداگانه (قفل‌های ۷.۱/۸.۱/۹.۱) |
| وارداتِ خودکارِ سیدا | فقط ورودِ دستی + مقایسه (قفل ۱۰.۱) | اولین درخواستِ واردات |
| استریمِ لحظه‌ایِ سرویس + کاشیِ نقشهٔ خیابان | جی‌پی‌اس/نقشهٔ مختصات/پیگیری **واقعی** است (قفل ۱۱.۱) | اولین تعقیبِ زندهٔ بین‌دستگاهی |
| حضورِ خودکار از **داخلِ اتاق شاد** | ورود/خروجِ دکمه‌ای + لینکِ اختصاصی **واقعی** است (قفل ۱۲.۱) | یکپارچگیِ شاد |
| ارسالِ واقعیِ صفِ پیامک | صف + گاردها + متن **واقعی** است (قفل ۱۳.۱) | وصل به درگاه |
| بسته‌بندیِ اندروید (۱۱ بندِ بخشِ ب چک‌لیست) | طراحی قفل (AAB, targetSdk 36, امتیاز, تستِ ۱۴ روزه با ۱۲+ تستر, reviewer access, …) | روزِ انتشارِ گوگل‌پلی |
| ۷ کارِ سروریِ قفل (احراز، بررسیِ نقش سمت سرور، …) | در `REPORT_OPEN_2026-09-05.md` و `TODO_BEFORE_PRODUCTION.md` | مرحلهٔ سرور و زیرسازی |
| معماریِ چندمدرسه‌ای (مسیرِ ج) | فقط تحلیل + قفلِ `organization_id` | تصمیمِ باز (~۶ ماه) |

---

## ۴. آزمون و کیفیت (وضعیتِ امروز — همه سبز)

**باتریِ کامل — ۲۵ سئوت، همه سبز:**

| سئوت | نتیجه | سئوت | نتیجه |
|---|---|---|---|
| smoke (دودی) | **۵۳۶/۵۳** | homework | ۸/۸ |
| superadmin (تازه) | **۳/۳** | homework2 | ۵/۵ |
| simulation (جامع) | ۳۵/۳۵ | dojo | ۶/۶ |
| run (ایستا) | ۳۳/۳۳ | certify | ۶/۶ |
| security (پنتست) | سبز (۱۴ سناریو) | visitors | ۵/۵ |
| diag2 (دیاگ) | ۱۱/۱۱ | library | ۵/۵ |
| idb2 (IDB) | 4/4 | assets | 5/5 |
| privacy (حریم) | 3/3 | sidadiff | 5/5 |
| vclass | 11/11 | bus2 | 5/5 |
| vclass2 | 4/4 | bus3 | 7/7 |
| vclass3 | 6/6 | schoolmode | 4/4 |
| classmodel | 6/6 | schoolmode2 | 6/6 |

**جهش‌کُشی این دوره: ~۱۰۰ جهش عمدی — همه کُشته شدند، هیچ‌کدام زنده نماند.**
(هر منطقِ حساس: فیلتر، مالکیت، بازهٔ اعتبار، upsert، گاردهای روزِ غیرحضوری،
بای‌پس‌های سوپرادمین — حداقل یک جهشِ کُشته روی خودِ آن.)

**درِ حضور (بند کیفیت):** ۷ تا ۸ میلی‌ثانیه — **به‌وضوح** زیرِ حدِ چندصد
میلی‌ثانیه (سقفِ مطلق: ۳۰ ثانیه). پرشِ معناداری با هیچ تغییری ثبت نشد؛
هر پایش با سناریوی **یکسان** قبل/بعد انجام شد.

**پنتست:** ۱۴ سناریوی exploit-driven — سطحِ کاربر همه سبز (XSS ذخیره‌شده
fuzz شده، ۵ سطحِ بیرونی، ۲ نشت) + ۴ بلوک‌کنندهٔ سروری **مستند** (قرمز
مانده تا سرور بیاید — عمداً).

---

## ۵. امنیت (وضعیتِ صادقانه)

- **نسخهٔ کنونی دمو است** — کنترلِ دسترسی سمتِ مرورگر، مجوزِ واقعی نیست
  (اصل: «کنترلِ کلاینتی هرگز مجوز نیست»).
- دو لایهٔ دفاع **درونِ دستگاه** برای همهٔ ماژول‌های تازه: گاردِ اکشن +
  مالکیتِ **روی داده** (نقش + `school_id` + اعتبارسنجی داخلِ خودِ تابعِ داده).
- پنتست + ۴ بلوک‌کنندهٔ سروریِ مستند (`SERVER_SECURITY_CONTRACT.md`).
- **توکنِ نشت‌کرده:** با تصمیمِ صریحِ کاربر بسته شد («مهم نیست، همه‌چیز امن است»).
- هر منبعِ تازهٔ داده، قبل از کَمیت برای دسترسیِ خارج‌ازمحدوده وارسی می‌شود
  (قاعدهٔ امنیتیِ ثابت).

---

## ۶. آمادگیِ گوگل‌پلی — خلاصه

**اعمال‌شده (۱۱ بند):** سیاستِ حریمِ خصوصیِ **درونِ اپ** (سئوت‌شده) +
سیاستِ منتشرشده + فرمِ Data Safetyِ آماده + بررسیِ واقعیِ سطحِ شبکه
(فقط ۲ فراخوانی در لایهٔ داده — بدون SDK/آنالیز/هوش مصنوعی) + عدمِ
فروشِ داده + مخاطبِ غیرکودک + حذفِ حساب (طراحیِ قفل) + …

**قفل‌شدهٔ روزِ بسته‌بندی (۱۱ بند):** AAB + امضای Play, targetSdk 36,
احرازِ هویتِ توسعه‌دهنده (۲۵ دلار + D-U-N-S), تستِ بستهٔ ۱۴ روزه با ۱۲+
تستر (حساب‌های شخصیِ پس از ۲۰۳), امتیازِ محتوا, reviewer access (۷
حسابِ دمو آماده), Play Billing = B2B مستقیم (مستند), …

**تغییرِ امروز:** همهٔ نقاطِ تماس = **شمارهٔ تلفن** (دستورِ کاربر).
شمارهٔ واقعی فقط در زمانِ انتشار درج می‌شود (مکانِ ثبت از قبل مشخص).

---

## ۷. راهنمای کاربر

- `USER_GUIDE.html` — ۲.۲۶ مگابایت، **۲۰ تصویرِ واقعی** (ورود و کلیکِ
  واقعی با مرورگرِ واقعی، نه نقاشی)، ۸ نقشِ مفصل، تک‌فایلی و آفلاین.
- **مکانیزه شد:** هر build مُهر را تازه می‌کند؛ `--check` راهنمایِ قدیمی را
  رد می‌کند (هر سه حالت آزموده شد).
- ثبت: اصلِ پنجم + «اصلِ همگامیِ راهنما» (۴ بند) — دائمی.

---

## ۸. وضعیتِ گیت و مِرج

| شاخه | HEAD | وضعیت |
|---|---|---|
| main | `77e626f` | push شده، ls-remote تأیید |
| ui-improvements | `a3706c3` | دو کَمیتِ تازه (سوپرادمین + تلفن) **هنوز push نشده** — مِرج در جریان |

مِرج `ui-improvements` ← `main` با **تأییدِ صریحِ کاربر** («انجام بده»)
در جریان است: الگوی همین دوره (چری‌پیک + بازنشانیِ بیلد + باتری + push
هر دو + ls-remote).

**تعداد:** ۵۱ کَمیت در هر شاخه از آغازِ دوره؛ ۵۹ ماژولِ جاوااسکریپت +
۳ فایلِ سبک؛ خروجی: **۱.۴۸ مگابایت** تک‌فایلی، کاملاً آفلاین، بدون وابستگی.

---

## ۹. پرسش‌های کاربر در این دوره و پاسخ/اثرشان

| پرسش/دستور | پاسخ/اثر | وضعیت |
|---|---|---|
| توکنِ نشت‌کرده چه شود؟ | با تصمیمِ کاربر بسته شد («همه‌چیز امن است») | ✅ بسته |
| `ui-improvements` به main مِرج شود؟ | «انجام بده» | 🔄 در جریان |
| تماسِ سیاست/فروشگاه ایمیل باشد؟ | «ایمیل باهاش کار نمی‌کنیم — فقط شمارهٔ تلفن» | ✅ اعمال شد (۳ نقطه) |
| سوپرادمین «اجازه ندارید» می‌گیرد | ریشه: تفکیک دورِ ۴۷؛ اصلِ تازه = بای‌پسِ ساختاری + سئوت | ✅ اعمال شد + ۶ تست به‌روز |
| فایلِ جامعِ گزارش | همین سند | ✅ این فایل |

---

## ۱۰. تصمیماتِ تفسیریِ خودِ من در این دوره (شفاف)

| تصمیم | چرا | کجا |
|---|---|---|
| بای‌پسِ سوپرادمین **ساختاری** (یک خط در هر دروازه) به‌جای افزودنِ دستیِ او به فهرستِ هر کنش | اصل «بدون محدودیت» باید برای کنش‌های **آتی** هم برقرار باشد؛ فهرست‌محوری یعنی فراموشیِ احتمالی | `30-authz.js` + قفل س.۱ |
| مسیر جانشینی (`S.boss`) **ماند** به‌عنوانِ دیدِ پنلِ مدیر | کاربر گارد نمی‌خواست ولی پشتیبانیِ فنی همان مسیر است — حذفش خطری بود بدون فایده | قفل س.۲ |
| متنِ تماس: «در زمانِ انتشارِ رسمی تکمیل می‌شود» | شمارهٔ واقعی هنوز داده نشده؛ ساختارِ «تماس = تلفن» قفل است، عدد بعد | دو سیاست + ماژول |
| نقشهٔ سرویس: SVG با **مختصاتِ واقعیِ خودِ برنامه** (نه کتابخانه) | پوستهٔ دمو شبکه ندارد (قاعدهٔ بدون وابستگی) | قفل ۱۱.۱ |
| توکنِ لینکِ کلاس مجازی **قطعی** (حاشه از نشست+دانش‌آموز) | بدون سرور قابلِ بازسازی/آزمون است؛ شکلِ سرور بعداً همان است با توکنِ opaque | قفل ۱۲.۱ |
| گاردهای روزِ غیرحضوری **در توابعِ داده** (نه فقط دکمه) | کلیکِ مستقیم یا فراخوانیِ تابع بی‌دکمه را هم مسدود می‌کند | قفل ۱۳.۱ |

---

## ۱۱. باقی‌مانده (ترتیبِ پیشنهادی)

1. **تکمیلِ مِرج** ui-improvements ← main (در جریان همین دور) + push هر دو + ls-remote.
2. **مرحلهٔ سرور و زیرسازی** — تنها گامِ باقی‌مانده بر اساسِ اصلِ واقعیت:
   ۷ کارِ سروریِ قفل (احراز، تکرارِ دروازه‌ها سمت سرور، صفِ واقعیِ پیامک،
   استریم، کاشی، توکنِ opaque، …) در `REPORT_OPEN_2026-09-05.md`.
3. **بسته‌بندیِ اندروید + آپلودِ گوگل‌پلی** — ۱۱ بندِ بخشِ ب چک‌لیست +
   درجِ **شمارهٔ تلفنِ تماس** در سیاست/فروشگاه.
4. **ورود با موبایل** در اولین مدرسهٔ واقعی (قفل ۶.۱) — حذفِ رمز از دمو.
5. قفل‌های «اولین مدرسهٔ واقعی» (۲.۱/۴.۱ب/۵.۱/۶.۱ب/۷.۱/۸.۱/۹.۱/۱۰.۱/۱۱.۱/۱۲.۱/۱۳.۱) —
   هر کدام دقیقاً زمانی که مشتریِ اولِ آن قابلیت بیاید.
6. **معماریِ چندمدرسه‌ای** — تصمیمِ باز (مسیرِ ج پیشنهادشده؛ ~۶ ماه).

---

*این سند با بقیهٔ گزارش‌ها در `docs/` یک‌رشته است: هر بخشِ کار، گزارشِ
خود را دارد؛ این فایل فقط نخِ واحدِ دوره است.*
