# گزارشِ دور ۷۴ — یکپارچه‌سازیِ واقعیِ کلاینت/سرور + پایشِ دقیق‌نویسیِ حریم خصوصی

تاریخ: ۱۵/06/۱۴۰۵ (2026/09/۰۶) · وضعیت: **بسته شد** (۳ کامیت)

---

## ۱. درخواست

«continue» — ادامهٔ کار پس از بسته‌شدنِ دورِ ۷۳. دو باقی‌ماندهٔ ارزشمندِ داخلِ رپو:
۱. **بزرگ‌ترین درزِ تست‌نشدهٔ «اتصال به سرور»:** هیچ تستی، کلاینتِ واقعیِ ساخته‌شده
(همان `index.html` که به کاربر می‌رسد) را با سرورِ واقعی و شبکهٔ واقعی سنجیده نبود
(server2 فقط fetch را mock می‌کرد).
۲. **مطابق‌بودنِ صفحاتِ حریم خصوصی/حذفِ حساب با کد** (ملاکِ Data Safety و
Account Deletionِ گوگل‌پلی: سیاست باید دقیقاً همین‌طور باشد که برنامه عمل می‌کند).

## ۲. یکپارچه‌سازیِ واقعی — `tests/integration.js` (تازه)

بیلدِ `index.html` داخلِ jsdom + سرورِ واقعی روی storeٔ موقت + HTTP واقعی +
کوکیِ نشستِ واقعی (fetchِ کلاینت، URL نسبی، `credentials: include` — همهٔ
مسیرهایِ واقعیِ خودِ کلاینت؛ بدونِ هیچ mock):

| # | بررسی | نتیجه |
|---|---|---|
| I۱ | خودِ کلاینت، سرور را از راهِ `/api/health` تشخیص می‌دهد (`DATA_MODE=server`) | ✅ |
| I۲-a/b | ورودِ واقعی از مسیرِ کلاینت: send-code → login (superadmin) | ✅ |
| I۲-c | کوکیِ نشست `payesh_session` در «جارِ» کلاینت نشست | ✅ |
| I۲-d | `S.user` به همان شیوهٔ UI از پاسخِ سرور تنظیم شد (مهرِ `op.by`) | ✅ |
| I۳-a | `/me` با کوکیِ واقعی: نشست زنده است | ✅ |
| I۳-b | bellِ parent با HTTP خام + کوکی: فرزندِ ۱۶ را می‌بیند | ✅ |
| I۴ | یک `Data.create` واقعی از کلاینت → صفِ SYNC → `/api/sync` (صف خالی) | ✅ |
| I۵ | رکورد روی **diskِ سرور** نشست (flush + renameٔ اتمی) | ✅ |
| I۶-a/b | خروج از کلاینت → نشست در سرور سلب شد (`/me` = 401) | ✅ |

**۱۲/۱۲ سبز** + صفر خطای کنسولِ کلاینت در کلِ تست.

**موتانت‌هایِ قرارداد (کلاینت↔سرور):**
- کلاینت کوکی را همراهِ درخواست نفرستد (`credentials: omit`) ⇒ I۳-a شکست (۱ شکست) — کشته شد
- سرور کوکیِ نشست نسازد ⇒ I۲-c تا I۶ شکست (۶ شکست) — کشته شد

## ۳. پایشِ صفحاتِ حریم خصوصی و حذفِ حساب

**باگِ واقعی پیدا شد و رفع شد:** تاریخِ «آخرین به‌روزرسانی» در نسخهٔ وبِ سیاست
(`privacy.html`) **خراب** بود — بخشِ تاریخِ داخلِ پرانتز تکرار و بی‌معنا شده بود
(در سندِ قابل‌نمایشِ کاربر). اصلاح شد و با همین به‌روزرسانی تازه شد.

**سه دقیق‌نویسی** (در هر سه نسخه: سندِ رسمیِ `docs/PRIVACY_POLICY.md` + وب + داخلِ اپ):
۱. §۱: «شمارهٔ تلفنِ اولیا» → «اولیا (و گاه دانش‌آموز به‌عنوانِ شمارهٔ تماس)» — دقیقاً مطابقِ داده.
۲. §۶: نتیجهٔ پایشِ دورِ ۷۳ به سیاست اضافه شد (پاسخِ یک‌شکلِ send-code، log-outِ دائمی، 0600ِ فایل‌ها).
۳. §۷: صادق‌نویسیِ نگهداری — داده از دادهٔ زنده فوراً حذف می‌شود؛ نسخه‌هایِ محدودِ پشتیبانِ خودکار تا جایگزینیِ دوره‌ای ممکن است دادهٔ قبل از حذف را داشته باشند.

عبارت‌هایِ قفل‌شدهٔ هم‌گامیِ وب/اپ (سئوتِ P5) دست‌نخورده ماندند؛ `privacy.js` (۳/۳) و `server10` (۷/۷) سبز.

**`account-deletion.html` (صفحهٔ وبِ حذفِ حساب — ملاکِ Play):** پایش شد، بدونِ نقص:
همهٔ خروجی‌ها `esc`/textContent؛ کد/کد ملی فقط در فیلد و نه در متن؛ رفتارِ
send-code پس از S-۷۳-۲ سازگار است (ناشناخته → همان شکلِ معمول، بدونِ نشت)؛
جریانِ حذف مستقیم است (بدونِ مانعِ پنهان: شماره → کد → کد ملی → تأیید صریح).

## ۴. سنجش (همهٔ زنده)

| سوت | نتیجه |
|---|---|
| **integration.js (تازه)** | ۱۲/۱۲ ✅ (+ ۲ موتانتِ قرارداد کشته) |
| privacy · server10 | ۳/۳ · ۷/۷ ✅ |
| run · simulation · sim_full۲ | ۳۳/۳۳ · ۴۸/۴۸ · ۵۸/۵۸ ✅ |
| بقیهٔ سوت‌ها (از دورِ ۷۳، روی همین کد) | server۱-۱۰ · server-mutations ۱۹/۱۹ · cmsg۳ · security۲/client/seed · bell۲ · sim_full۳ — همه ✅ |

## ۵. کامیت‌ها

| کامیت | محتوا |
|---|---|
| (۱-م) | دور ۷۴-۱: `tests/integration.js` — یکپارچهٔ واقعیِ کلاینت/سرور |
| (۲-م) | دور ۷۴-۲: اصلاحِ تاریخِ خراب + ۳ دقیق‌نویسی در ۳ نسخهٔ سیاست + بیلد |
| (۳-م) | دور ۷۴-۳: این گزارش + HANDOFF |

## ۶. باقی‌مانده‌ها

کد: درزِ تست‌نشدهٔ «اتصال به سرور» (بخشِ ۲) و ناسازگاری‌هایِ سیاست (بخشِ ۳) بسته شدند.
بیرونی (زیرساخت/تصمیم): درگاهِ واقعیِ SMS/استعلام · TLSِ تولیدی · اشیاء‌نگاری (قفل ۱۳.۳) ·
WAF/مانیتورینگ · شمارهٔ تماسِ رسمی · ورودی‌هایِ فروشگاهِ Play و آزمونِ ۱۴ روزه.
