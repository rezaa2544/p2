# گزارش — بررسیِ گرافیکِ خارجی (Figma AI) و وضعیتِ branch `ui-improvements`

> **تاریخ:** 2026-09-05 · **کامیتِ main:** `d63c90d` (دست‌نخورده) ·
> **branch در بررسی:** `ui-improvements` — کامیت `70b0a7d`
> **نتیجه:** ✅ تغییرات فقط CSS و بدون اثر بر کارکرد است؛ باتری کامل سبز؛
> در انتظارِ تصمیمِ merge.

---

## ۱. چه اتفاقی افتاد

۱. کاربر، مخزنِ گیت‌هاب (با **توکنِ کاملِ پروژه**) را در اختیارِ یک عاملِ
   هوش‌منندِ خارجی (Figma AI) گذاشت تا «فقط گرافیک» را بررسی کند.
۲. آن عامل، تحلیلِ بصری داد (سایدبارِ تخت، کارت‌های آماریِ ساده،
   دکمه‌های خشکِ حضور، صفحهٔ ورودِ ساده) و یک پیش‌نمایش ساخت.
۳. کاربر از ما خواست: اگر روی گیت کار کند، برنامه خراب نمی‌شود؟ چه
   کار کنیم؟
۴. ما: **گزینهٔ A** (branch جدا، نه مستقیم main) را تأیید کردیم +
   پرامپتِ قوانینِ سخت را نوشتیم (فقط CSS، نه JS، نه HTMLِ دستی،
   بدون وابستگیِ خارجی، بدون جابه‌جاییِ چیدمانِ RTL، بدون push به
   main).
۵. Figma AI تغییرات را روی branch `ui-improvements` push کرد
   (کامیت `70b0a7d`) و اعتبارش تمام شد.

## ۲. ⚠️ موضوعِ امنیتی — توکنِ نشت‌کرده

- توکنی که در چتِ Figma AI گذاشته شده بود، **همان توکنِ زندهٔ پروژه
  بود** (با فایلِ `~/.payesh_gh_token` تطبیق داده شد).
- **حالت:** Revoke شدنِ آن هنوز تأیید نشده است. تا وقتی Revoke نشود،
  هر دستی که آن چت را دیده، دسترسیِ کامل به مخزن دارد.
- **اقدامِ لازم:** GitHub → Settings → Developer settings → Personal
  access tokens → **Revoke** همان توکن + ساختِ توکنِ تازه.
- در همین دوره، یک بار هم remoteٔ محلی از بین رفته بود و با همان
  توکنِ کهنه بازسازی شده — پس بعد از Revoke، remote باید با توکنِ
  تازه به‌روز شود.

## ۳. بررسیِ ما روی branch (خط‌به‌خط، روی کدِ واقعی)

| بازرسی | نتیجه |
|---|---|
| baseٔ branch | `904424c` — یعنی دو کَمیتِ آخرِ main (هماهنگ‌سازی `index.html` + گزارشِ باز) در آن نیستند؛ **merge سه‌طرفه هر دو را حفظ می‌کند** (گزارش در diff به‌نظر «حذف‌شده» می‌رسید که نیست) |
| فایل‌های تغییرکرده | فقط **دو**: `src/styles/base.css` (+۴۰ خط) و `index.html` (خروجیِ build). `src/js` و بقیه: **صفر تغییر** ✅ |
| محتوای CSS | فقط `transition`، `box-shadow`، `backdrop-filter` و یک `::before` دکوراتیو — هیچ `display`/`visibility`/`position`/`z-index`/`font-size` دست نخورده ✅ |
| وابستگیِ خارجی | هیچ لینکِ CDN/فونت/کتابخانهٔ جدید نیست ✅ |
| انتخابگرهای CSS با کد | کلاس‌های `on-present`/`on-absent`/`on-late`/`on-excused` با کلاس‌های واقعیِ دکمه‌های حضور در `12-attendance.js` **مطابق‌اند** — اثر، واقعی است ✅ |
| دایرهٔ دکوراتیوِ ورود | `.login-art` خودش `position:relative` + `overflow:hidden` است — دایره داخلِ پنل می‌ماند ✅ |
| build | اجرای `node build.js` روی branch: خروجی **دقیقاً** همان `index.html`ِ کَمیت‌شده — یعنی build واقعی زده، نه ویرایشِ دستی ✅ |

## ۴. تغییراتِ اعمال‌شده (فقط بصری)

1. سایدبار: glowِ ملایمِ سیان روی آیتمِ فعال + transition نرم
2. آواتار: حلقهٔ درخشانِ آبی/سیان
3. نوارِ بالا: جلوهٔ شیشه‌ای (blur)
4. آیکون‌های آماری: سایهٔ ملایم
5. دکمه‌های حضور: transition روان + سایهٔ رنگیِ وضعیت (سبز/قرمز/نارنجی/بنفش)
6. Badgeها: سایهٔ رنگیِ بسیار ملایم
7. صفحهٔ ورود: شکلِ دکوراتیوِ دوم (دایرهٔ سیان)
8. لوگوی برند: سایهٔ آبیِ بهتر

## ۵. آزمون‌ها (روی خودِ branch)

- **smoke: ۵۳/۵۳۶** ✅ + ۱۹ سئوتِ مستقل (vclass ۱۱ · vclass2 ۴ · vclass3 ۶ ·
  classmodel ۶ · homework ۸ · homework2 ۵ · dojo ۶ · certify ۶ · visitors ۵ ·
  library ۵ · assets ۵ · sidadiff ۵ · bus2 ۵ · bus3 ۷ · schoolmode ۴ ·
  schoolmode2 ۶ · diag2 ۱۰ · simulation ۳۵) + پنتست — **همه سبز** ✅
- **یک نکتهٔ شفاف‌سازی:** در اولین دورِ اجرا، `jsdom` از `node_modules`
  پاک شده بود و سئوت‌ها به‌صورت **خاموش** رد می‌شدند (exit 0 بدون اجرا).
  خودِ ما متوجه شدیم، `jsdom` را نصب کردیم و **دوباره واقعاً** اجرا
  کردیم — اعدادِ بالا از دورِ دومِ واقعی‌اند.

## ۶. وضعیتِ کنونی و قدم‌های بعد

| # | کار | وضعیت |
|---|---|---|
| ۱ | Revokeٔ توکنِ نشت‌کرده + ساختِ توکنِ تازه | 🔴 **باز — اقدامِ کاربر** |
| ۲ | mergeٔ `ui-improvements` به `main` + push | ⏸️ در انتظارِ تأییدِ کاربر (پیش‌نمایش دیده شد؛ اگر تأیید شود، merge می‌شود — با همان گاردها) |
| ۳ | تنظیماتِ ظریفِ CSS (سبک/سنگین‌تر) | ⏸️ اختیاری — بدون فایگما، مستقیم روی همین branch قابل انجام است |
| ۴ | بازسازیِ remoteٔ محلی با توکنِ تازه (بعد از ۱) | 🔴 وابسته به ۱ |

**اگر merge نشود:** هیچ چیز از دست نمی‌رود — branch روی گیت‌هاب
مانده و `main` (کامیت `d63c90d`) دقیقاً همان حالتِ قبل است.
