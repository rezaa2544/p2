# کارهای ناتمام — وضعیت زنده

> به‌روزرسانی: ۲۰۲۶-۰۹-۰۶ · کامیت مبنا: `0ace71b`
> این سند پاسخ به پرسش «اگر کار ناتمامی داریم بگو» است.
> ⚠️ وارسیِ ۲۰۲۶-۰۹-۰۶: جدولِ بخش ۲ زیر با کدِ فعلی تطبیق داده شد.

---

## ۱. آنچه ساخته شده و کار می‌کند ✅

| قابلیت | ماژول | دور |
|---|---|---|
| اطلاع‌رسانی پیامکی (۹ گام) | `44-sms-notify.js` | ۴۲ |
| سابقهٔ چندساله و یادداشت دبیر | `45-teacher-tools.js` | ۴۴ |
| هستهٔ زمان و نوار زنگ | `46-bell-now.js` | ۴۸ و ۵۰ |
| تفکیک نقش مدیر کل از مدیر مدرسه | `30-authz.js` | ۴۷ |
| خانوادهٔ چندفرزندی در دادهٔ نمونه | `02-demo-data.js` | ۴۶ |
| رفع پرش صفحه به بالا | `24-edu-office.js` | ۶۰ |

**وضعیت:** ۴۷ ماژول · ۳۳ آزمون ایستا · ۸۳ صفحه سالم.

---

## ۲. وضعیتِ طرح‌های نوشته‌شده (وارسیِ ۲۰۲۶-۰۹-۰۶ با کدِ فعلی)

| # | طرح | وضعیت فعلی | شواهد |
|---|---|---|---|
| ۱ | **حالت‌های زمان‌دار غیبت** (تأخیر، خروج، الگو) | 🟡 **نیمه:** لایهٔ وضعیت (present/late/absent/excused) + پیگیریِ الگو (`viewFollowup`) پیاده؛ بخشِ «خروجِ زمان‌دار» نیست | `02-demo-data.js` · `47-counselor.js` |
| ۲ | **زنگ خودکار** گام ۳ و ۴ (پیش‌گزینش کلاس) | ✅ پیاده | `bellAutoClass` + `bellAutoAllowed` در `46-bell-now.js` |
| ۳ | **نمای زندهٔ زنگ** سه نقش + تلنگر | ✅ پیاده (دور ۶۵) + تیکِ سروری (۱۳.۴) | `46-bell-now.js` · `tests/bell2.js` · `tests/server5.js` |
| ۴ | **تقویم و روزهای متغیر** + کلاس جبرانی | ✅ پیاده | `calendar` در NAV همهٔ نقش‌ها · `makeup_classes` + `attendance_modes` |
| ۵ | **ورود با موبایل** و احراز هویت | ✅ پیاده (کلاینت + سرور) | `19-actions.js` (شماره+کد+استعلام) · `server/auth.js` |
| ۶ | **دسته‌بندی منو** در همهٔ پنل‌ها | ✅ پیاده | NAVِ گروه‌بندی‌شده در `05-router.js` (همهٔ نقش‌ها) |

**باز:** فقط نیمهٔ طرح ۱ (خروجِ زمان‌دار) — با بقیهٔ مواردِ بخش ۴ِ
`OPEN_ITEMS.md` (وابسته به بیرون/تصمیم).

---

## ۳. ترتیب پیشنهادی

### دستهٔ الف — کوچک و بی‌وابستگی (۳ روز)

۱. **دسته‌بندی منو** (۱ روز) — کاربر دو بار خواسته، هنوز انجام
   نشده. فقط منوی مدیر در دور ۴۷ مرتب شد.
۲. **زنگ خودکار گام ۳ و ۴** (۲ روز) — زیرساختش از دور ۴۸ آماده
   است و گارد `bellAutoAllowed()` منتظر مصرف‌کننده مانده.

🟢 **چرا این‌ها اول؟** کوچک‌اند، به چیزی وابسته نیستند، و
گام ۳ زنگ پیش‌نیاز طرح نمای زنده است.

### دستهٔ ب — نظم مدرسه (۸ تا ۱۱ روز)

۳. **حالت‌های زمان‌دار غیبت** — تأخیر با زمان دقیق، خروج از
   کلاس، موجه کردن پس از ثبت، و **الگوی تکرار**.

⚠️ کاربر تأکید کرد این «خیلی مهم است در نظم مدرسه».

### دستهٔ ج — بزرگ و ساختاری

۴. **تقویم و روزهای متغیر** (۶ تا ۸ روز) — شامل کلاس جبرانی.
۵. **نمای زندهٔ زنگ** (۴ تا ۵ روز) — پس از گام ۳ زنگ.
۶. **ورود با موبایل** (۸ تا ۱۰ روز) — بزرگ‌ترین، و تا وقتی
   سرور نیست فقط نیمه‌کاره می‌ماند.

---

## ۴. پرسش‌های بی‌پاسخ

| پرسش | طرح | اهمیت |
|---|---|---|
| روز کاری پیش‌فرض مدارس موجود؟ | تقویم | کم |
| پیش‌گزینش فقط با زمان‌بندی ثبت‌شده — تأیید؟ | زنگ | متوسط |
| کارت ولی پشت دیوار پرداخت باشد؟ | نمای زنده | کم |
| ولی نام دبیر را ببیند؟ | نمای زنده | کم |
| درخواست تکراری نقش — به‌روز یا رد؟ | ورود | کم |
| مهلت درخواست ۳۰ روز؟ | ورود | کم |
| خروج، احراز هویت را پاک کند؟ | ورود | متوسط |

⚠️ **هیچ‌کدام مانع شروع کار نیستند** — پیشنهادهایم در طرح‌ها
نوشته شده و اگر نظری ندهید، همان‌ها اجرا می‌شوند.

---

## ۵. بدهی فنی شناخته‌شده

| مورد | توضیح | فوریت |
|---|---|---|
| کد ملی آزادانه قابل ویرایش | با ورود تلفنی خطرناک می‌شود | 🔴 پیش از ورود تلفنی |
| ورود با نام کاربری | روز تولید باید خاموش شود | 🔴 پیش از تولید |
| `DAYS[]` ثابت ۵ روزه | در پنج جا مصرف می‌شود | 🟡 با طرح تقویم |
| حجم پایگاه داده ۴٫۷ مگابایت | سقف حدود ۵ | 🟡 هر افزودنی بسنجید |
| چهار مسدودکنندهٔ تولید | رمز، احراز هویت، پایگاه داده، نقش | 🔴 عمدی، فاز دمو |

---

📄 مرتبط: `HANDOFF.md` · `TODO_BEFORE_PRODUCTION.md` ·
`docs/OPEN_ITEMS.md`
