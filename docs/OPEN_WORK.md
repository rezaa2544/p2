# کارهای ناتمام — وضعیت زنده

> به‌روزرسانی: ۲۰۲۶-۰۹-۰۶ · کامیت مبنا: `4ed0fdf` (پایانِ دور ۷۹)
> این سند پاسخ به پرسش «اگر کار ناتمامی داریم بگو» است.
> ⚠️ وارسیِ ۲۰۲۶-۰۹-۰۶ (دور ۸۰): بخش‌های ۲/۳/۴/۵ با کد و سندِ فعلی تطبیق داده شد —
> **جوابِ کوتاه: کارِ کدیِ ناتمامی در طرح‌ها نیست**; باقی‌مانده‌ها یا «وابسته به
> بیرون»‌اند (پایلوت، درگاهِ پیامک، حسابِ Play) یا «تصمیمِ کاربر» (مدرسهٔ پایلوت + تاریخ).

---

## ۱. آنچه ساخته‌شده و کار می‌کند ✅

| قابلیت | ماژول | دور |
|---|---|---|
| اطلاع‌رسانی پیامکی (۹ گام) | `44-sms-notify.js` | ۴۲ |
| سابقهٔ چندساله و یادداشت دبیر | `45-teacher-tools.js` | ۴۴ |
| هستهٔ زمان و نوار زنگ | `46-bell-now.js` | ۴۸ و ۵۰ |
| تفکیک نقش مدیر کل از مدیر مدرسه | `30-authz.js` | ۴۷ |
| خانوادهٔ چندفرزندی در دادهٔ نمونه | `02-demo-data.js` | ۴۶ |
| رفع پرش صفحه به بالا | `24-edu-office.js` | ۶۰ |

**وضعیت:** ۴۷ ماژول · ۳۳ آزمون ایستا · ۸۳ صفحه سالم.

---

## ۲. وضعیتِ طرح‌های نوشته‌شده (وارسیِ ۲۰۲۶-۰۹-۰۶ با کدِ فعلی)

| # | طرح | وضعیت فعلی | شواهد |
|---|---|---|---|
| ۱ | **حالت‌های زمان‌دار غیبت** (تأخیر، خروج، الگو) | ✅ پیاده (دور ۶۶): تأخیر/خروج با ساعت + دقیقه از زنگ مدرسه (`attTimeFields`)، موجه‌سازیِ مدیرانه پس از ثبت (ردپا)، الگوی «خروج مکرر» واقعی + پیامکِ خروج (پیش‌فرض روشن) | `12-attendance.js` · `44-sms-notify.js` · `47-counselor.js` · `tests/att2.js` |
| ۲ | **زنگ خودکار** گام ۳ و ۴ (پیش‌گزینش کلاس) | ✅ پیاده | `bellAutoClass` + `bellAutoAllowed` در `46-bell-now.js` |
| ۳ | **نمای زندهٔ زنگ** سه نقش + تلنگر | ✅ پیاده (دور ۶۵) + تیکِ سروری (۱۳.۴) | `46-bell-now.js` · `tests/bell2.js` · `tests/server5.js` |
| ۴ | **تقویم و روزهای متغیر** + کلاس جبرانی | ✅ پیاده | `calendar` در NAV همهٔ نقش‌ها · `makeup_classes` + `attendance_modes` |
| ۵ | **ورود با موبایل** و احراز هویت | ✅ پیاده (کلاینت + سرور) | `19-actions.js` (شماره+کد+استعلام) · `server/auth.js` |
| ۶ | **دسته‌بندی منو** در همهٔ پنل‌ها | ✅ پیاده | NAVِ گروه‌بندی‌شده در `05-router.js` (همهٔ نقش‌ها) |

**باز:** چیزی نیست — شش طرح هر شش‌شان پیاده شد؛ فقط مواردِ بخش ۴ِ
`OPEN_ITEMS.md` (وابسته به بیرون/تصمیم).

---

## ۳. ترتیب پیشنهادی — **بسته شد: هر شش مورد پیاده است**

(این بخش در دور ۸۰ بازنویسی شد؛ ترتیبِ قدیمی دیگر معتبر نیست — جدولِ بخش ۲
همین را با شواهد می‌گوید: هر شش طرح پیاده و قفل‌شده با آزمون است.)

---

## ۴. پرسش‌های بی‌پاسخ — **به‌روزرسانیِ دور ۸۰: اکثرِ آن‌ها جواب دارد**

| پرسش | جواب (منبع) |
|---|---|
| روز کاری پیش‌فرض مدارس موجود؟ | ✅ شنبه تا چهارشنبهٔ قابلِ تنظیمِ هر مدرسه (`workDaysOf` — دور ۶۵) |
| پیش‌گزینش فقط با زمان‌بندی ثبت‌شده — تأیید؟ | ✅ بله (قفلِ زنگ خودکار — دور ۴۸ به بعد) |
| کارت ولی پشت دیوار پرداخت باشد؟ | ✅ **نه** — کارت ولی رایگان می‌ماند (جوابِ کارفرما، دور ۶۵) |
| ولی نام دبیر را ببیند؟ | ✅ بله (دور ۶۵) |
| درخواست تکراری نقش — به‌روز یا رد؟ | ✅ به‌روز می‌شود (طرحِ ورود — پیاده) |
| مهلت درخواست ۳۰ روز؟ | ✅ بله (طرحِ ورود) |
| خروج، احراز هویت را پاک کند؟ | ✅ بله (سلبِ jti — `server/auth.js`) |

**واقعا بازِ واقعی فقط دو مورد:** مدرسهٔ/مدرسه‌هایِ پایلوت + تاریخِ شروع (تصمیمِ
کاربر) و درگاهِ پیامک/استعلام (قراردادِ خارجی).

---

## ۵. بدهی فنی شناخته‌شده

| مورد | توضیح | فوریت |
|---|---|---|
| ~~کد ملی آزادانه قابل ویرایش~~ | ✅ بسته شد (وارسیِ دور ۸۰): ویرایشِ کاربر (شاملِ کد ملی) فقط برای `manager`/`superadmin` است (`canEdit` در `10-users.js` + `user-del` در ACTION_ROLES)؛ خودِ دانش‌آموز فرمِ کاربر ندارد. باقی‌ماندهٔ عملیاتی: اعتبارسنجیِ کد ملی هنگامِ درج (۱۱ رقم) | 🟢 بسته (بدهیِ امنیتی) / 🟡 اعتبارسنجی |
| ~~ورود با نام کاربری~~ | ✅ باطل (وارسیِ دور ۸۰): `username` فقط **برچسبِ نمایشی** است، نه اعتبارِ ورود; ورود فقط تلفن + کد + کد ملی (بدونِ رمز — TODO ۱.۱) |  بسته (تغییرِ طراحی) |
| `DAYS[]` ثابت | 🟡 به‌روزرسانی: روزهایِ کاری حالا **قابلِ تنظیمِ هر مدرسه**‌اند (`workDaysOf`، دور ۶۵)؛ `DAYS[]` فقط آرایهٔ نمایشِ ستون‌هایِ هفته است | 🟢 عمدی (نمایش) |
| حجم پایگاه‌داده ۴٫۷ مگابایت | سقف حدود ۵ |  هر افزودنی بسنجید |
| ~~چهار مسدودکنندهٔ تولید~~ | ✅ هر چهار بسته شد (دورهایِ سرور — `OPEN_ITEMS.md` §۱) | 🟢 بسته |

---

📄 مرتبط: `HANDOFF.md` · `TODO_BEFORE_PRODUCTION.md` ·
`docs/OPEN_ITEMS.md`
