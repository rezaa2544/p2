Wave 4 (بازیافتی) — دلتا روی همهٔ مجموعه‌ها + سخت‌سازی فاز ۲

▸ پوششِ همهٔ مجموعه‌ها (دلتا)
  ✅ AC1 هر ۱۹ مجموعه در پاسخِ دلتا حاضرند
  ✅ AC2 دلتا فقط ردیف‌های تغییرکرده بعد از since (هر مجموعه)
  ✅ AC3 دلتای کهنه (>۷روز): همهٔ مجموعه‌ها کامل + پرچم
  ✅ AC4 اسنپ‌شاتِ اجباری فیلترِ زمانی را کنار می‌گذارد (ردیفِ قدیمی برمی‌گردد)

▸ انتخاب و پالایشِ مجموعه‌ها
  ✅ AC5 زیرمجموعهٔ collections respected
  ✅ AC6 نام‌های ناشناخته/خراب حذف می‌شوند (fail-closed)

▸ ایزولاسیون مستأجر
  ✅ AC7 مدرسهٔ ۲ به مدیرِ مدرسهٔ ۱ نشت نمی‌کند (دلتا)
  ✅ AC8 سوپرادمین هر دو مدرسه را می‌بیند (دلتا)
  ✅ AC9 تومب‌استون‌ها school-scoped هستند (دلتا)

▸ مسیر PG زنده (db جعلی) — همهٔ مجموعه‌ها
  ✅ AC10 هر مجموعه از builder دلتا می‌آید + fallback تک‌جدولی
ACTUAL_FORCED_FULL_SQL ["SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"users\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"classes\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"subjects\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"schedule\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"enrollments\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"attendance\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"grades\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"discipline\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"leaves\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"notifications\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"announcements\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"hw_submissions\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"counselor_refs\"","SELECT COALESCE(MAX(chg_id), 0) AS m FROM \"counselor_msgs\""]
  ❌ AC11 دلتای کهنه اصلاً به DB دلتا نمی‌زند (فقط خواندنِ کامل)
     no delta SQL must run on forced full, ran 14

▸ کرسرِ امضاشده روی همهٔ مجموعه‌ها
  ✅ AC12 کرسرِ معتبر برای همهٔ مجموعه‌ها دلتای درست می‌دهد
  ✅ AC13 شکلِ پاسخ: since/server_version/کلیدهای ثابت

────────────────────────────────────────────────────────
نتیجه Wave4 Sync (همهٔ مجموعه‌ها): 12/13  —  1 ناموفق
