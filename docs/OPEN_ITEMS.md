# موارد باز — جمع‌بندی وارسی‌شده

> **دامنه:** این سند فقط موارد **شناخته‌شدهٔ** باز را فهرست می‌کند و
> هر ادعا را با وارسی واقعی روی کد فعلی تأیید کرده است. جست‌وجوی
> موارد تازه انجام **نشده** است.
>
> تاریخ وارسی: دور ۴۱ · کامیت مبنا: `a70f73b`
> وضعیت آزمون‌ها هنگام وارسی: ۳۱/۳۱ و ۳۳۲/۳۳۲ ✅

---

## ۱. مسدودکننده‌های استقرار — هر چهار مورد همچنان باز

از `TODO_BEFORE_PRODUCTION.md` بخش ۱. وضعیت هرکدام با گرپ روی کد
امروز بررسی شد، نه از روی حافظهٔ سند:

| بند | مورد | وارسی امروز | وضعیت |
|---|---|---|---|
| ۱.۱ | هش رمز با bcrypt | `bcrypt` فقط در **کامنت** دو فایل (`02-demo-data.js`، `19-actions.js`) — هیچ پیاده‌سازی‌ای نیست؛ ۱۸ مورد رمز `123456` در کد | 🔴 باز |
| ۱.۲ | احراز هویت سمت سرور | ورود همچنان مقایسهٔ متن ساده در `19-actions.js` | 🔴 باز |
| ۱.۳ | پایگاه دادهٔ واقعی به‌جای `localStorage` | `Api` در `00-data-layer.js` یک `fetch` دارد ولی سروری پشتش نیست | 🔴 باز |
| ۱.۴ | بررسی نقش سمت سرور | `30-authz.js:165` علامت TODO دارد؛ منطق فقط سمت مرورگر | 🔴 باز |

**این چهار مورد عمدی‌اند.** کاربر در دور ۲۸ تصریح کرد پروژه در فاز
دمو است و بک‌اند و هش رمز در این مرحله لازم نیست. پس «باز بودن»
اینجا به معنی عقب‌ماندگی نیست — به معنی کاری است که روز اتصال سرور
باید انجام شود.

---

## ۲. مهم‌های پیش از کاربر واقعی

از `TODO_BEFORE_PRODUCTION.md` بخش ۲:

| بند | مورد | وضعیت |
|---|---|---|
| ۲.۱ | اعتبارسنجی دوبارهٔ سمت سرور | 🔴 باز (وابسته به ۱.۳) |
| ۲.۲ | محدودسازی نرخ درخواست | 🔴 باز (وابسته به سرور) |
| ۲.۳ | ثبت وقایع امنیتی | 🔴 باز |
| ۲.۴ | پشتیبان‌گیری و بازیابی | 🔴 باز |
| ۲.۵ | حل تعارض در همگام‌سازی | ✅ تصمیم گرفته شد |
| ۲.۶ | سرآیندهای امنیتی HTTP | 🔴 باز (وابسته به سرور) |
| ۲.۷ | هشدار خودکار شمردن شناسه | 🔴 باز |
| ۲.۸ | حذف نرم یا سخت | ✅ تصمیم گرفته شد |

⚠️ **وابستگی متقابل** (از `ARCHITECTURE_DECISIONS.md`): تصمیم ۴.۱
(شناسهٔ عددی) فقط تا وقتی قابل دفاع است که ۲.۳ و ۲.۷ هر دو پیاده
شده باشند. اگر یکی حذف شد، شناسهٔ عددی هم باید بازبینی شود. این
تنها وابستگی متقابل کل فهرست است.

---

## ۳. سه آزمون که عمداً وارونه‌اند

`tests/smoke.js` خطوط ۲۵۲۶ به بعد. این سه آزمون **وجود**
آسیب‌پذیری IDOR را تأیید می‌کنند، نه نبودش:

- مدیر مدرسه با شمردن شناسه به کاربران مدارس دیگر می‌رسد
- ولی با شمردن شناسه به دانش‌آموزان غیرفرزند می‌رسد
- دبیر با شمردن شناسه به نمرات کلاس‌های غیرتخصیصی می‌رسد

🔴 **روز اتصال سرور باید معکوس شوند.** اگر روزی یکی از این سه
خودبه‌خود شکست خورد، یعنی کسی محدوده را درست کرده و آزمون باید
به‌روز شود — نه اینکه تغییر عقب زده شود.

📄 `docs/SERVER_SECURITY_CONTRACT.md` بند ۱.۲: `byId` محدوده
نمی‌سنجد ⇒ پاسخ باید **۴۰۴** باشد نه **۴۰۳** (تا وجود رکورد لو
نرود).

---

## ۴. دو موردی که عمداً بی‌تصمیم مانده‌اند

| مورد | چه وقت تصمیم بگیرید |
|---|---|
| محل نگهداری داده (داخل/خارج کشور) | هنگام انتخاب ارائه‌دهندهٔ سرور |
| رضایت والدین | 🔴 **پیش از نخستین ثبت‌نام واقعی** — گرفتن رضایت پس‌از‌واقعه از هزاران خانواده ناممکن است |

مورد دوم تنها بند این سند است که **مهلتش زودتر از اتصال سرور**
می‌رسد.

---

## ۵. مواردی که وارسی شد و همچنان سالم‌اند ✅

ادعاهای بخش ۳ سند `TODO_BEFORE_PRODUCTION.md` دوباره سنجیده شدند
تا مطمئن شویم پس نرفته‌اند:

| ادعا | وارسی امروز | نتیجه |
|---|---|---|
| لایهٔ داده تنها دروازه است | گرپ `localStorage.`/`fetch(` در ۴۳ فایل به‌جز `00-data-layer.js` (با حذف کامنت‌ها) | **صفر تخطی** ✅ |
| هیچ دادهٔ واقعی نیست | تلفن غیر `0999`: صفر · کد ملی غیر `999`: صفر | ✅ |
| امن‌سازی XSS | `esc()`/`escAttr()` سرجایشان، آزمون نگهبان فعال | ✅ |
| ۸۲ صفحه سالم | بازاجرا با `renderRoute()` | **۸۲/۸۲** ✅ |

---

## ۶. مواردی از ارزیابی دور ۴۰ که در دور ۴۱ بسته شد

| مورد | وضعیت |
|---|---|
| `meeting_slots` خالی | ✅ بسته — ۱۲۰ نوبت (کامیت `061e0cf`) |
| `bell_schedules` خالی | ✅ بسته — ۵ زمان‌بندی |
| بنچمارک ظرفیت کهنه | ✅ بسته — بازسنجی با ستون قبل/بعد (کامیت `a70f73b`) |

**یک مورد باز ماند:** حجم `JSON.stringify(db)` = **۴٫۶۴MB** در برابر
سقف ~۵MB مرورگر. امروز خطری ندارد چون کل `db` ذخیره نمی‌شود (فقط
دفترچهٔ تغییرات)، اما **هر افزایش دادهٔ نمونه باید دوباره سنجیده
شود**. با بند ۱.۳ (پایگاه دادهٔ واقعی) خودبه‌خود منتفی می‌شود.

---

## ۷. خودآزمایی داخلی

`DIAG_CHECKS`: ۳۲ بررسی → ۲۹ ok · ۲ warning · ۱ critical · نمره **۷۵**.

- ⚠️ «رشتهٔ خارج از شاخهٔ مدرسه» — دادهٔ نمونه، نه نقص کد
- ⚠️ «رمز پیش‌فرض تغییرنیافته» — همان بند ۱.۱
- 🔴 «دسترسی به حافظهٔ مرورگر» — **اثر محیط jsdom** (`opaque origin`)،
  نه نقص محصول. در مرورگر واقعی سبز است.

---

📄 مرجع اصلی: `TODO_BEFORE_PRODUCTION.md`
📄 قرارداد سرور: `docs/SERVER_SECURITY_CONTRACT.md`
📄 تصمیم‌ها: `docs/ARCHITECTURE_DECISIONS.md`
