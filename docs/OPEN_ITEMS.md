# موارد باز — جمع‌بندی وارسی‌شده

> **دامنه:** این سند فقط موارد **شناخته‌شدهٔ** باز را فهرست می‌کند و
> هر ادعا را با وارسی واقعی روی کد فعلی تأیید می‌کند (نه از روی حافظهٔ سند).
>
> آخرین وارسی: ۲۰۲۶-۰۹-۰۶ · کامیت مبنا: `4ed0fdf` (پایانِ دور ۷۹) — به‌روزرسانی: §۳ (IDOR حالا اجرا/سبز) + §۴ (قفلِ آروان‌کلاود)
> وضعیت آزمون‌ها هنگام وارسی (دور ۷۹): smoke 546/546 · server1–10 154/154 · sim_full2 58/58 · sim_full3 24/24 ·
> server1 ۳۰/۳۰ · server2 ۲۵/۲۵ · server3 ۱۹/۱۹ ·
> server4 ۱۶/۱۶ · server5 ۱۴/۱۴ · server6 ۹/۹ · server7 ۱۵/۱۵ · server8 ۹/۹ ·
> run ۳۳/۳۳ · جهش‌ها: uiclick 5/5 · trendcmp2 5/5 · underpriv2 5/5 · reporttpl2 5/5 · gradeavg2 4/4 ✅

---

## ۱. مسدودکننده‌های استقرار — **هر چهار مورد بسته شد** (مرحله‌های ۱ و ۲ِ سرور)

| بند | مورد | وضعیت فعلی (با شواهد روی کد) | وضعیت |
|---|---|---|---|
| ۱.۱ | هش رمز با bcrypt | باطل شد به‌سبب تغییرِ طراحی: **اصلاً رمز نداریم** — ورود فقط با شماره+کد+استعلام (AD: اصلِ واقعیِ ورود). ستونِ `password` آرشیوی است (AD ن.۴) | ✅ بسته (تغییرِ طراحی) |
| ۱.۲ | احراز هویت سمت سرور | `server/auth.js` — JWT HS256 سفت‌وسخت (alg بدون هدر)، exp، jti سلب، محدودسازیِ ۱۰۰ تلاش با تأخیرِ تدریجی و زمانِ مساوی، کوکی HttpOnly/Lax(/Secure) | ✅ بسته |
| ۱.۳ | پایگاه دادهٔ واقعی به‌جای localStorage | storeٔ سرور در `server/data/payesh.json` + `POST /api/sync` (چهار چکِ قرارداد) + پشتیبان/بازیابی (۱۴.۳). انتخابِ طراحی: فایلِ JSON (نه RDBMS) — تا اولین استقرارِ واقعی | ✅ بسته (فازِ فعلی) |
| ۱.۴ | بررسی نقش سمت سرور | `server/sync.js` — `WRITE_PERMS` (fail-closed) + scopeٔ رکورد + نقش از توکن (نه بدنه)؛ `server/admin.js` — فقط superadmin | ✅ بسته |

---

## ۲. مهم‌های پیش از کاربر واقعی

| بند | مورد | وضعیت (شواهد) |
|---|---|---|
| ۲.۱ | اعتبارسنجی دوبارهٔ سمت سرور | ✅ `sync.js`: op.by با توکن (ساختگی → کلِ دسته 403 + آدیت)؛ user/school با توکن؛ نقشِ جدول؛ دامنهٔ رکورد (test: server1 S10–S20) |
| ۲.۲ | محدودسازی نرخ درخواست | ✅ ورود: تأخیرِ تدریجی بعد از ۱۰۰ شکست + ۵ کد/۱۰ دقیقه به هر شماره؛ sync: سقف ۵۰۰ عملیات در دسته |
| ۲.۳ | ثبت وقایع امنیتی | ✅ `audit.log` append-only و بهداشتی (بدون phone/nid): forge_by، clock_skew، enum_warn، account_deleted، backup/restore، bell_now |
| ۲.۴ | پشتیبان‌گیری و بازیابی | ✅ `server/admin.js` (AD ۱۴.): اسنپ‌شاتِ فقط‌داده، نگه‌داریِ ۱۰ نسخه، restore با اعتبارسنجی (test: server8 ۹/۹ + M16/M17) |
| ۲.۵ | حل تعارض در همگام‌سازی | ✅ تصمیم: uid idempotent (last-write-wins به ازای هر uid) |
| ۲.۶ | سرآیندهای امنیتی HTTP | ✅ CSP با nonce (بدون unsafe-inline، build.js + تزریقِ سرور)، HSTS روی https، no-store برای API |
| ۲.۷ | هشدار خودکار شمردن شناسه | ✅ `idor.js`: بعد از ۱۰۰۰ شناسهٔ ناشناس هشدار در آدیت + کندشدن (50ms) |
| ۲.۸ | حذف نرم یا سخت | ✅ تصمیم + پیاده‌سازی: حذفِ سخت (قفل ۹.۵، test: server7) |

⚠️ **وابستگی متقابل** (از `ARCHITECTURE_DECISIONS.md`): تصمیم ۴.۱ (شناسهٔ عددی)
وابسته به ۲.۳ و ۲.۷ بود — **هر دو حالا پیاده است**، پس قید رفع شد.

---

## ۳. سه آزمونِ وارونهٔ IDOR (سمتِ کلاینت) — **حالا اجرا و سبز**

`tests/smoke.js` (خط‌های ۲۹۶۴ به بعد: «IDOR ⚠️» برای مدیر/ولی/دبیر) تأیید می‌کنند
که هیچ‌کدام با شمردنِ شناسه خارج از دامنه نمی‌روند. سمتِ سرور لایهٔ جدا دارد
(`sync.js` + `idor.js`). ⚠️ یادداشتِ قدیمیِ «در sandbox اجرا نمی‌شوند (OOM)»
باطل شد: در محیطِ فعلی هر سه در بستهٔ smoke **اجرا و سبز** هستند (بخشِ smoke 546/546).

---

## ۴. آنچه واقعاً باقی مانده (وابسته به بیرون/تصمیم)

| مورد | مانع |
|---|---|
| درگاهِ واقعیِ پیامک + استعلامِ کد ملی | درگاه/قرارداد (امروز: کد در حافٔهٔ سرور + بازتابِ اختیاریِ دمو) |
| استورِ آبجکت (فایل‌نگهداری ۱۳.۳) | ✅ **تصمیم قفل شد** (دور ۷۹ بند ۱۰): آروان‌کلاود Object Storage (بستهٔ ۵ گیگِ رایگان + ۲۰ گیگ ترافیک/ماه، PAYG فراتر) — ضمیمهٔ حِ AD؛ بازنگری فقط با شرایطِ صریحِ ثبت‌شده (حجم/هزینه/نیازِ فنی) |
| TLSِ production (گواهیِ رسمی + پروکسی) | گواهی/استقرار |
| بکاپِ خودکارِ دوره‌ای (cron) | ✅ بسته شد (AD ۱۵.۲ — زمان‌بندیِ درون‌پروسه) |
| شمارهٔ تماسِ داده در سیاست (بخش ۹) | انتشارِ رسمی |
| بستهٔ ۱۴ روزهٔ گوگل با ۱۲+ آزمایش‌کننده (AD ۹.۸) | حسابِ Play + افراد |
| اسکرین‌شاتِ گوگل‌پلی از دستگاهِ واقعی | انتشار (الان: آیکون 512 + گرافیکِ 1024×500 در `store/` آماده‌اند) |

**بسته‌شده در دورهای اخیر (شواهد در AD):** صفحهٔ وبِ سیاستِ حریم خصوصی (ملاکِ Play) → AD ۱۵.۳ (روتِ `/privacy`) ·
آیکون + گرافیکِ فروشگاه → `store/` · بکاپِ دوره‌ای → AD ۱۵.۲.

---

## ۵. یافته‌های تازه — دورِ تستِ جامع (۱۷/۰۶/۱۴۰۵ · main a12c80b) — **در انتظارِ دستور**

جزئیاتِ کامل: `COMPREHENSIVE_TEST_REPORT.md` (ریشهٔ پروژه). این دور فقط تست بود — هیچ تغییری اعمال نشد.

### ۵.۱ P0 — باگِ واقعی: دو گواهی کاربرپسند (رفعِ پیشنهادی: یک خط در هر تابع)

| مورد | شواهد |
|---|---|
| `enrollmentCert(sid)` کرش می‌کند | `src/js/33-forms-sms.js:355` — `return` از `body` استفاده می‌کند ولی بدنه در `bodyClassic` ساخته شده → `ReferenceError: body is not defined` (اثباتِ زنده در jsdom) |
| `transferCert(sid)` کرش می‌کند | `src/js/33-forms-sms.js:389` — همان ریشه |
| باگ از چه زمانی | کامیتِ سازندهٔ `5ce70fa` — یعنی هر دو گواهی از روزِ اول کار نکرده‌اند |
| تأثیر | مدیر/کارشناس نمی‌تواند گواهیِ اشتغال به تحصیل یا انتقالی چاپ کند؛ کارنامه (`reportCardCert`) سالم است |
| رفعِ پیشنهادی | `body: body` → `body: bodyClassic` در هر دو تابع + rebuild + اجرایِ `certify` (هم‌اکنون ۳/۶ قرمز روی همین باگ) |
| **تایید شد (scratch):** رفع را در یک worktree جدا اعمال کردم → `certify` **6/6** ✅ و سه جهش (همین باگ، متغیرِ تعریف‌نشدهٔ دیگر، خطایِ رکوردِ صدور) هر سه را کشت. **فقط منتظرِ دستور** |

### ۵.۲ بدهیِ بهداشتِ تست (اپ سالم؛ همهٔ موارد بازمحاکقه و سبزِ تکی شدند)

| # | مورد | ریشه | رفعِ پیشنهادی |
|---|---|---|---|
| ۱ | ۱۵ سوئیتِ جهش در بارگذاری کرش می‌کنند (`ENOENT`) | هنوز `src/js/19-actions.js` (حذف‌شده در فاز ۲، کامیت `05d3d19`) را `readFileSync` می‌کنند | هدف‌گذاریِ دوباره روی پاره‌های `19-actions-*.js` یا منسوخ‌شماردن |
| ۲ | کرشِ سوئیتِ جهش، سوئیتِ بعدیِ توالی را قرمزِ دروغین می‌کند (۸ مورد در اجرایِ ۱۲۴تایی) | crash در میانهٔ فایل، `index.html` را در حالتِ جهش‌یافته رها می‌کند (restore فقط src را برمی‌گرداند) | rebuildِ `index.html` در `finally` در سوئیت‌هایِ جهشِ کلاینت |
| ۳ | `bell2` (B1/B2) قرمز است مگر REF روی شنبه/پنجشنبه نباشد | انتظارِ «دبیرِ زنگِ جاری» برای day 0 (شنبه) سفت‌کد شده؛ در دادهٔ دمو دبیرِ هر روزِ هفته متفاوت است | انتظار را از روزِ REF حساب کند |
| ۴ | `bus2` (B5) + `bus3` (G3) قبل از 07:42 UTC قرمزند | seedِ دمو (48-bus-service.js:413-418) «موقعیتِ امروز» را با timestamp سفت `07:40/07:42Z` می‌سازد → قبل از آن لحظه، رکوردِ دمو آینده است و «آخرین موقعیت» درست برگشت می‌زند | **تأییدِ زنده شد:** همان کد، 07:30Z قرمز و 08:29Z سبز (bus2 5/5، bus3 7/7). رفع: clampِ timestampِ دمو به ≤ now (یا اجرایِ تست‌ها بعد از 07:42Z) |
| ۵ | `security_seed` (Z2) در اجرایِ توالی‌ای قرمز است | storeٔ واقعی اثرِ فعالیتِ محیط را دارد پیش از اجرا (seed خود قطعی است — اثبات شد) | کپیِ store به tmp قبل از S2 (عزل) |
| ۶ | `homework2` (H2 + H4) بینِ ۲۳:۰۰ تا ۲۳:۵۹ UTC قرمز است | خودِ تست `today+'23:00'` را هم‌زمان به دو معنیِ متضاد به‌کار می‌برد: در H2 «پایانِ امروز» (باید هنوز نرسیده باشد) و در H4 «زمانی در آینده» (برای آزمونِ حالتِ before). بعد از ۲۳:۰۰ هر دو فرض وارونه می‌شوند — `hwIsOpen` درست کار می‌کند و رشتهٔ `2026-09-07T23:18 > 2026-09-07T23:00` را درست می‌سنجد | **تأییدِ زنده شد (دورِ ۸۹):** همان فایل با ساعتِ قفل‌شده روی ظهر ⇒ **۵/۵ سبز**؛ در ۲۳:۱۸ ⇒ ۳/۵. رفع: ساعتِ مرجع را به‌جای «الان» از یک ثابتِ تزریق‌شده بگیرد (`hwIsOpen` پارامترِ `nowIso` را همین حالا می‌پذیرد و استفاده نمی‌شود) |

### ۵.۳ یادداشتِ معماری (مستند شد، رفعِ گزینشی)

- هر نوشتنِ کلاینت، کلِ db (≈۴٫۷MB) را دوباره stringify و در localStorage می‌نویسد — با رشدِ داده، هزینهٔ نوشتن خطی با حجمِ کلِ db است (دلیلِ همین رفتار: فشردنِ لاگ در دور ۸۶ که زنده در شبیه‌سازی دیده شد). مرورگر سقفِ localStorage (≈۵MB) دارد؛ در شبیه‌سازیِ ۳۰ روزه اشغال ۱۱٪ شد.
- microbenchmarkِ ۱۰۰۰ نوشتنِ پشت‌سرهم در فرآیندِ node با heap پیش‌فرض (~860MB) OOM می‌کند؛ در مرورگرِ واقعی چنین سقفی نیست (محدودیتِ محیطِ تست، نه اپ).
