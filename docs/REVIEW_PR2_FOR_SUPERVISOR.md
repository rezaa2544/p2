# گزارشِ بازبینیِ چهار ویژگیِ ادغام‌شده در PR #2 — برای ناظرِ ارشد

**تاریخ:** ۲۰۲۶-۰۹-۰۸ (دورِ ۸۹) · **وضعیت:** 🟡 **بازبینی — نه تأیید، نه رد**
**موضوع:** شاخهٔ `feat/client-features` → ادغام‌شده در `0acd68e` (PR #2)
**دستورِ ناظر:** «فقط می‌خواهم قبل از تصمیم نهایی، کامل ببینمش.»

> این سند هیچ تصمیمی نمی‌گیرد. کد را همان‌طور که هست شرح می‌دهد، با
> شواهدِ اجراییِ زنده — تا تصمیمِ تأیید/رد/بازگردانی نزدِ ناظر بماند.

---

## ۱. شناسنامهٔ ادغام

| قلم | مقدار |
|---|---|
| کامیتِ ادغام | `0acd68e` — «Merge pull request #2 from rezaa2544/feat/client-features» |
| کامیتِ محتوا | `5b528ea` — نویسنده: **Payesh Dev** (یعنی خودِ عاملِ آرنا، نه انسان) |
| تاریخِ ادغام | ۲۰۲۶-۰۹-۰۷ ساعت ۲۱:۰۸ |
| انجام‌دهندهٔ ادغام | حسابِ `rezaa2544` در گیت‌هاب |
| حجمِ تغییر | ۷ فایل · ۵۳۴+ / ۶− |
| بازبینیِ خودکار | Devin AI — ۶ ادعا؛ نتیجه در `PR2_REVIEW_FIX_REPORT.md` |

**فایل‌های تغییریافته:**

| فایل | نوع | چه شد |
|---|---|---|
| `src/js/66-client-features.js` | ➕ تازه | ۱۶۶ سطر — کلِ منطقِ چهار ویژگی |
| `src/js/_order.json` | ✏️ | ثبتِ ماژولِ ۶۶ (بدونِ آن وارد بیلد نمی‌شد) |
| `src/js/19-actions-core.js` | ✏️ ۱ سطر | افزودنِ `CF_ACTIONS` به زنجیرهٔ دیسپچرِ کلیک |
| `src/js/17-student-record.js` | ✏️ ۵ سطر | دکمهٔ «موجه اعلام کنم» + فراخوانیِ کارت بالای پرونده |
| `tests/client-features.js` | ➕ تازه | ۱۹۰ سطر — ۱۱ بررسی |
| `index.html` | 🔄 بازتولید | ۱۷۳+ (خروجیِ `build.js`، دست‌ساز نیست) |
| `USER_GUIDE.html` | 🔄 | فقط مُهرِ بیلد |

---

## ۲. چهار ویژگی — چه می‌کنند

| # | ویژگی | تابع(ها) | ورودیِ داده |
|---|---|---|---|
| ۱ | چک‌لیستِ «فردا» | `tomorrowChecklistItems(sid)` | `db.schedule` + `db.exams` (فقط کلاسِ خودِ دانش‌آموز) |
| ۲ | شمارشِ معکوسِ امتحان | `nextExamOf(sid)` | `db.exams` نزدیک‌ترینِ آینده |
| ۳ | خروجیِ تقویم ICS | `icsForStudent(sid)` · `icsDownload(sid)` | امتحاناتِ کلاس + `db.calendar` مدرسه، افقِ ۳۰ روز |
| ۴ | موجه‌سازیِ سریعِ ولی | `quick-excuse` · `qe-save` | `insert('leaves', {status:'pending'})` |

**نکتهٔ مهمِ ویژگی ۴:** هیچ مسیرِ تازه‌ای برای تغییرِ داده باز نمی‌کند —
از همان جریانِ موجودِ «درخواستِ مرخصی → تأییدِ مدیر» استفاده می‌کند. ولی
**نمی‌تواند** غیبت را مستقیماً موجه کند؛ فقط درخواستِ `pending` می‌سازد.

---

## ۳. دسترسی‌ها (مجوزِ نقش) — یافتهٔ اصلی

### ۳.۱ هر سه اکشنِ تازه **بی‌برچسب** هستند

```
ics-export     → در ACTION_ROLES نیست  ⇒ canAction() = true برای هر نقشی
quick-excuse   → در ACTION_ROLES نیست  ⇒ canAction() = true
qe-save        → در ACTION_ROLES نیست  ⇒ canAction() = true
```

این با معماریِ موجود ناسازگار **نیست** — پروژه ۵۲ اکشنِ نویسندهٔ بی‌برچسب
دارد و `tools/check-authz.js` این را رسماً می‌پذیرد، چون **نگهبانِ واقعی
`WRITE_PERMS` سمتِ سرور است (fail-closed)**. ولی معنایش این است که در
حالتِ **کلاینتِ تنها** (وضعیتِ فعلیِ پایلوت، `demoMode:true`) هیچ گاردی جز
گاردهای دستیِ درونِ خودِ توابع وجود ندارد.

### ۳.۲ گاردهای دستیِ درونِ ماژول

| اکشن | گارد | کیفیت |
|---|---|---|
| `quick-excuse` | `cfCanExcuse(u, rec)` هنگامِ بازکردنِ مودال | ✅ درست |
| `qe-save` | `cfCanExcuse(u, rec)` **دوباره** هنگامِ ذخیره | ✅ درست — الگویِ صحیح |
| `ics-export` | ❌ **هیچ گاردِ مالکیتی ندارد** | 🔴 شکاف (بند ۴) |

`cfCanExcuse` درست نوشته شده: ولی فقط با `parent_links` خودش، دانش‌آموز فقط
خودش، و **از `activePersona()` استفاده می‌کند نه `u.role`** — یعنی کاربرِ
چندنقشی را درست مدیریت می‌کند.

### ۳.۳ سازگاری با دامنه‌بندیِ مدرسه (`WRITE_PERMS` سرور)

| مجموعه | نقشِ ولی مجاز است؟ | نتیجه |
|---|---|---|
| `leaves` | ✅ بله (سطر ۳۰ `server/sync.js`) | درخواستِ مرخصی از ولی پذیرفته می‌شود |
| `notifications` | ✅ در فهرست هست | **ولی** `inScope` آن را رد می‌کند (رکوردِ اعلان `student_id` ندارد) |

این تناقض قبلاً کشف و **سمتِ سرور** رفع شده بود (کامیت `8e06379`): سرور خودش
اعلانِ مدیر را می‌سازد. اعلانِ سمتِ کلاینت برای حالتِ بدونِ سرور حفظ شده و با
سرور به‌صورتِ opِ ردشده بی‌ضرر می‌ماند. **این رفع درست است.**

---

## ۴. 🔴 یافتهٔ تازهٔ این بازبینی — نشتِ دامنه در خروجیِ ICS

بازبینیِ Devin این را **ندیده بود**. با پروبِ زندهٔ jsdom کشف و اثبات شد.

### ۴.۱ ریشه

دکمه با `data-sid` ساخته می‌شود:

```html
<button data-act="ics-export" data-sid="1021">📥 تقویم (ICS)</button>
```

اما دیسپچرِ سراسری در `19-actions-core.js` فقط `data-id` را می‌خواند:

```js
const a = el.dataset.act, rawId = el.dataset.id;   // ← data-sid خوانده نمی‌شود
```

پس در استفادهٔ عادی `id` همیشه `undefined` است و کد به شاخهٔ امن می‌افتد:

```js
'ics-export'(el, id){
  var sid = id ? Number(id) : recordTargetId();   // ← فقط این شاخه گارد دارد
  if(sid) icsDownload(sid);
}
```

`recordTargetId()` گاردِ درستی دارد (ولی را به `parent_links` محدود می‌کند).
**ولی شاخهٔ `id` هیچ گاردی ندارد** — و مهاجم می‌تواند آن را فعال کند.

### ۴.۲ اثباتِ زنده (اجرا شد، حدس نیست)

مهاجم = ولیِ واقعیِ سامانه (شناسه ۲، مدرسهٔ ۶). در کنسول:

```js
var b = document.createElement('button');
b.setAttribute('data-act','ics-export');
b.setAttribute('data-id','16');       // ← دانش‌آموزِ بیگانه، مدرسهٔ ۱
document.body.appendChild(b); b.click();
```

نتیجهٔ واقعی:

```
canAction("ics-export") برای ولی → true      (اکشنِ بی‌برچسب)
پس از کلیک: Blob ساخته شد؟ → yes
ICSِ بیگانه: ۱۲۷۴ بایت، دارای رویداد
ICSِ فرزندِ خودم: ۸۵ بایت (خالی)
نشت: SUMMARY:امتحان دین و زندگی ۱ 08:00 | امتحان فارسی ۱ 10:30 | ...
```

فایلِ دانش‌آموزِ **مدرسهٔ دیگر** دانلود شد.

### ۴.۳ شدت — با انصاف

| سنجه | ارزیابی |
|---|---|
| چه چیزی نشت می‌کند | نامِ درس، تاریخ/ساعتِ امتحان، نامِ سالن، عنوانِ رویدادهای مدرسه |
| PII دارد؟ | ❌ **نه** — بدونِ نام، تلفن، کد ملی (با اجرا تأیید شد) |
| مرزِ مدرسه را می‌شکند؟ | ✅ **بله** — مدرسهٔ ۶ دادهٔ مدرسهٔ ۱ را گرفت |
| نیازمندِ چیست | حسابِ معتبر + دستکاریِ دستیِ DOM |
| در حالتِ سرور | همچنان کار می‌کند — **این خواندنِ حافظهٔ محلی است، نه درخواستِ شبکه**، پس `idor.js` جلویش را نمی‌گیرد |
| هم‌ردهٔ بدهیِ شناخته‌شده؟ | بله — از خانوادهٔ «IDOR در `04-queries.js byId`» در §۰.۵.۵ |

**ارزیابیِ صادقانه: متوسط، نه بحرانی.** برنامهٔ امتحاناتِ یک کلاس رازِ درجه‌یک
نیست و PII ندارد. اما **اصلِ نهمِ پروژه** («هر داده‌ای که نقش اجازهٔ دیدنش را
ندارد نباید به مرورگر برسد») را نقض می‌کند و مرزِ مدرسه را می‌شکند.

### ۴.۴ رفعِ پیشنهادی — **اجرا نشد، منتظرِ تأیید**

طبقِ اصلِ هشتم، من این را ننوشتم. رفعِ کمینه دو خط است:

```js
'ics-export'(el, id){
  var sid = recordTargetId();          // همیشه از مسیرِ گاردشده
  if(sid) icsDownload(sid);
}
```

`data-sid` هم بی‌مصرف است و باید حذف شود. اگر تأیید کنید، با تستِ جهش
(F5: دکمهٔ دست‌ساز با شناسهٔ بیگانه باید رد شود) اجرا می‌کنم.

---

## ۵. سازگاری با اصولِ موجود

| اصل | وضعیت | توضیح |
|---|---|---|
| `index.html` دستی ویرایش نشود | ✅ | بازتولیدشده با `build.js` |
| ثبت در `_order.json` | ✅ | ماژولِ ۶۶ ثبت شد |
| بدونِ وابستگیِ خارجی | ✅ | جاوااسکریپتِ خالص |
| متنِ فارسی | ✅ | همه فارسی |
| `esc()`/`escAttr()` | ✅ | روی همهٔ خروجی‌ها |
| فرارِ ICS (RFC 5545) | ✅ | `icsEsc` — درست‌تر از پیشنهادِ Devin که `esc()` گفته بود |
| تاریخِ ISO داخلی | ✅ | شمسی فقط در نمایش |
| تغییرِ داده با `insert` | ✅ | `insert('leaves',...)` |
| بدونِ جست‌وجوی خطی در رندر | 🟡 | `db.exams.filter` و `db.schedule.filter` — روی دادهٔ کوچکِ یک کلاس، ولی الگویِ ایندکسِ پروژه رعایت نشده |
| دامنه‌بندی در `29-scope.js` | ❌ | گاردها دستی و درونِ ماژول‌اند، نه در لایهٔ scope |
| اصل نهم (داده به مرورگر نرسد) | ❌ | بند ۴ |

---

## ۶. آزمون‌ها

`tests/client-features.js` — **۱۱/۱۱ سبز** (خودم اجرا کردم). پوشش:

- F1 روزِ درسی از `work_days` مدرسه می‌آید نه `day<=4`
- F3 قالبِ `DTSTART` (تمام‌روز در برابر ساعت‌دار)
- F3-b بارِ بدخواه در ICS خطِ مستقل نمی‌سازد
- F4-b تا F4-f گاردهای موجه‌سازی، از جمله:
  - **F4-e** جعلِ `S.__cfRid` رد می‌شود
  - **F4-f** کاربرِ چندنقشی با `persona` سنجیده می‌شود نه `role`

**شکافِ پوشش:** هیچ تستی سناریوی بند ۴ (جعلِ `data-id` روی `ics-export`) را
پوشش نمی‌دهد. تست‌ها `icsForStudent` را مستقیم صدا می‌زنند و از مسیرِ
دیسپچرِ کلیک عبور نمی‌کنند.

رگرسیونِ کامل پس از تغییراتِ من: **۱۲۵/۱۲۵ سبز** · `build --check` سبز.

---

## ۷. جمع‌بندی برای تصمیمِ ناظر

**کیفیتِ مهندسی بالاست.** گاردِ دوبارهٔ `qe-save`، `activePersona()` به‌جای
`role`، فرارِ صحیحِ RFC 5545، و دو موردی که تیمِ بازبینی صریحاً پیشنهادِ
غلطِ Devin را رد کرده (`esc()` برای ICS و افزودنِ `Z` به زمان) نشانهٔ کارِ
دقیق است.

**اما یک نشتِ دامنهٔ واقعی دارد** که هیچ‌کدام از بازبینی‌های قبلی ندیده بودند.

سه گزینهٔ پیشِ رو:

| گزینه | شرح |
|---|---|
| **الف — تأیید مشروط** | ویژگی‌ها بمانند، رفعِ بند ۴ (۲ خط + تستِ جهش) در نوبتِ بعد با تأییدِ شما |
| **ب — بازگردانی** | `git revert 0acd68e` — چون بدونِ تأییدِ شما ادغام شده بود. هزینه: ۴ ویژگی و ۱۱ تست از دست می‌رود |
| **ج — تعلیق** | کد بماند ولی کارتِ «فردا و امتحانات» تا تصمیمِ نهایی از پرونده برداشته شود |

**هیچ‌کدام اجرا نشد.** منتظرِ دستورِ شما.
