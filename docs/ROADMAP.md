# نقشه راه جامع مهندسی «پایش» تا آمادگی مقیاس ملی

> این سند مکمل الزامات معماری ملی است و اجرای Waves بدون رعایت آن مجاز نیست.
**تاریخ:** ۱۸ شهریور ۱۴۰۵ (۲۰۲۶-۰۹-۰۹)

**هدف:** تبدیل پایش به سامانه مدیریت مدرسه پایدار، سریع، امن، قابل نگهداری و مقیاس‌پذیر برای جمعیت بسیار بزرگ دانش‌آموزان، معلمان و اولیا.

## 1. تعریف هدف
«کاربر نامحدود» از نظر فنی وجود ندارد. هدف واقعی: **مقیاس‌پذیری افقی، ظرفیت قابل اندازه‌گیری و افزایش ظرفیت بدون تغییر معماری**. معیار موفقیت فقط تعداد کاربران ثبت‌شده نیست؛ باید Registered Users، DAU، Peak Concurrent، RPS، Writes/sec، DB TPS و Sync Rate اندازه‌گیری شوند.

اصل پایداری:
> هیچ failure شناخته‌شده‌ای نباید باعث از دست رفتن داده، نشت داده، corruption یا توقف کنترل‌نشده شود؛ failure ناشناخته باید با telemetry، alerting، isolation و recovery قابل تشخیص و مهار باشد.

---

# 2. معماری هدف

```text
Internet
  ↓
DNS / CDN / WAF / DDoS / TLS
  ↓
Load Balancer
  ↓
API #1 ... API #N  ← Stateless
  ↓
Service / Policy Layer
  ├── PostgreSQL = Source of Truth
  ├── Redis = Cache + Distributed Ephemeral State
  └── Outbox / Queue
          └── Workers → SMS / Notification / Report / Analytics

Browser
  └── UI + bounded cache + IndexedDB offline queue + Sync
```

**قانون:** در Production فقط PostgreSQL منبع حقیقت داده‌های اصلی باشد. JSON/in-memory store نباید source of truth باشد. Browser نباید دیتاست ملی را نگه دارد.

> **وضعیت «bounded cache» (پ۳ 2026-09-12):** قانون «Browser نباید دیتاست ملی
> را نگه دارد» اکنون گیت اجرایی دارد — کش کراندار گزارش‌ها با دفاع دولایه
> (کران ردیف/بایت در `server/pull.js` + کران مستقل کلاینت + TTL + نشانگر
> «دادهٔ جزئی»)؛ `tests/reports-bounded-cache.js` ‏۱۵/۱۵ + جهش ۱۰/۱۰.
> **تکمیل (پ۳-ادامه):** دلتای بریده دیگر ساکت گم نمی‌شود — سرور
> `full_snapshot_required_collections` اعلام و کلاینت خودکار resume می‌کند
> (snapshot کران‌دارِ فقط مجموعه‌های بریده + سنجهٔ همگرایی + حلقه‌شکن دولایه)؛
> قرارداد: `WAVE23_DB_NATIVE_REPORTS.md` §۹.۲د؛ گیت
> `tests/bounded-delta-resume.js` ‏۱۴/۱۴ + جهش ۱۰/۱۰.

---

# 3. Wave 0 — Baseline و Freeze

- نسخه فعلی tag شود و commit مبنا ثبت شود.
- همه تست‌های فعلی اجرا و نتیجه ثبت شود.
- CPU/RAM/heap/event-loop latency ثبت شود.
- latency و حجم پاسخ endpointهای اصلی اندازه‌گیری شود.
- queryهای DB، cache hit/miss و sync throughput اندازه‌گیری شود.
- dependency و deployment inventory ساخته شود.

**خروجی:** `docs/NATIONAL_BASELINE.md`

---

# 4. Wave 1 — PostgreSQL به عنوان Source of Truth [P0]

این مهم‌ترین کار است.

### کارها
1. تمام read/write pathهای فعلی را inventory کن.
2. تمام readهای Production را به PostgreSQL منتقل کن.
3. writeها را transaction-based کن.
4. `store` را از مسیر production data خارج کن.
5. persistence دوره‌ای JSON را از Production حذف کن.
6. migration داده فعلی به PostgreSQL را repeatable کن.
7. دو API instance را با یک DB تست کن:
   `write A → read B → update B → read A`

**Acceptance:** همه instanceها state واحد را از DB ببینند.
---

# 5. Wave 2 — Database Engineering

## Migration
ساختار versioned ایجاد شود:

```text
migrations/
001_initial.sql
002_indexes.sql
003_constraints.sql
...
```

Migration باید immutable، تست‌شده و قابل rollback/forward-recovery باشد.

## Constraints
برای invariantهای مهم از:
- PK
- FK
- UNIQUE
- composite UNIQUE
- CHECK
- NOT NULL

استفاده شود.

## ID
`max(id)+1` حذف شود؛ از PostgreSQL Identity یا UUID/ULID استفاده شود.

## Transactions
عملیات چندمرحله‌ای در transaction انجام شوند:

```text
BEGIN
  business changes
  audit
  outbox event
COMMIT
```

## OCC
برای داده‌هایی مثل grade/attendance:

```sql
UPDATE ...
WHERE id = $id AND version = $base_version
```

صفر row → `409 Conflict`.
---

# 6. Wave 3 — Query و Performance [P0]

هر endpoint پرترافیک باید DB-native باشد.

### ممنوع
```text
load all
filter in JS
sort in JS
slice
```

### مجاز
```text
SQL WHERE
SQL ORDER BY
INDEX
LIMIT
cursor
```

برای queryهای مهم `EXPLAIN ANALYZE` و benchmark قبل/بعد اجباری است.

## Pagination
Cursor/keyset واقعی:

```text
GET /students?cursor=...
```

و cursor پایدار بر اساس کلید ترکیبی مناسب، مثلاً `(updated_at,id)`.

## Index
Index فقط بر اساس workload و query plan واقعی ایجاد شود.
---

# 7. Wave 4 — Sync / A01


A01 حفظ شود اما database-native شود.

### Pull
به جای scan کل store:

```sql
WHERE updated_at > $since
  AND scope_condition
ORDER BY updated_at,id
LIMIT $limit
```

Delta باید در برابر timestamp برابر، pagination boundary و clock skew مقاوم باشد.

### Tombstone
جدول/ساختار استاندارد:

```text
entity
record_id
deleted_at
scope
version
```

Retention مشخص شود و قبل از حذف tombstone امکان full resync وجود داشته باشد.

### Push
- validation
- authorization
- idempotency
- OCC
- transaction

در یک مسیر استاندارد انجام شود.
ید مشخص شود:

```text
RPO = حداکثر داده قابل از دست رفتن
RTO = حداکثر زمان recovery
```

و داشته باشیم:

```text
Backup
Off-site copy
Encryption
PITR
Restore test
Failover test
Runbook
```

Backup داشتن بدون restore drill کافی نیست.
---

# 20. Wave 17 — Testing Pyramid

تست‌ها:

```text
Unit
Integration
Contract
E2E
Security
Concurrency
Load
Stress
Spike
Soak
Chaos
Recovery
```

تست‌های موجود باید حفظ شوند و ضعیف/حذف نشوند.

---

# 21. Wave 18 — National Load Testing
## 🟣 فاز ۲.۳/۲.۴ زیرساخت تولید — اتصال PostgreSQL و پارتیشن‌بندی

| # | کار | وضعیت | مسئول | سند/تست |
|---|-----|--------|-------|---------|
| Infra.1 | PgBouncer Connection Pooling | ✅ کامل | چت ۴ جدید | `docs/PGBOUNCER_SETUP.md` + `tests/pgbouncer-pooling.js` |
| Infra.2 | Weighted Partitioning برای مدارس شلوغ | ✅ کامل | چت ۴ | `docs/WEIGHTED_PARTITIONING.md` + `tests/weighted-partitioning.js` |

---

## 🟣 فاز Sync ملی — تعارض‌های Offline-First

| # | کار | وضعیت | مسئول | سند/تست |
|---|-----|--------|-------|---------|
| Sync.1 | Version Vectors برای Offline-First Conflicts | ✅ کامل | چت ۴ | `docs/VERSION_VECTORS.md` + `tests/version-vector*.js` |

---

## 🟡 فاز ۳ (C) – تکمیل نیازهای دانش‌آموز و اولیا

10M registered user به تنهایی کافی نیست.

Dataset آزمایشی باید شامل:

```text
10M users
schools
classes
enrollments
attendance
grades
messages
notifications
audit/events
```

با نسبت‌های نزدیک به workload واقعی باشد.

سپس اندازه‌گیری:

```text
API RPS
writes/sec
DB TPS
Redis ops/sec
sync records/sec
p50/p95/p99
CPU
RAM
network
```

سناریوهای اجباری:

### Load
بار عادی و peak

### Stress
افزایش بار تا نقطه failure

### Spike
افزایش ناگهانی بار

### Soak
چند روز اجرای مداوم برای کشف:
- memory leak
- connection leak
- queue growth
- cache growth
- GC degradation
- DB bloat

---

# 22. Wave 19 — Chaos / Failure Testing

عمداً تست شود:

```text
kill API instance
Redis outage
slow Redis
slow DB
DB connection exhaustion
network degradation
queue outage
disk full
certificate expiry
bad deployment
database failover
```

برای هر failure:

```text
Detect
→ Alert
→ Contain
→ Recover
→ Verify data integrity
```

---

# 23. Wave 20 — چهار Arena

## Arena 1 — Database/Core
مالک:
```text
PostgreSQL
schema
migrations
queries
indexes
transactions
OCC
```

## Arena 2 — Security
مالک:
```text
Auth
RBAC
Tenant isolation
session
rate limiting
security tests
```

## Arena 3 — Sync/Offline
مالک:
```text
A01
Pull/Push
tombstone
cursor
offline queue
conflict resolution
IndexedDB
```

## Arena 4 — Performance/Infra
مالک:
```text
Redis
cache
workers
observability
load tests
deployment
```
| # | کار | مسئول | زمان تخمینی |
|---|-----|-------|-------------|
| E.1 | نمره‌ی عملی/کارگاهی هنرستان | چت ۳ | ۱ روز |
| E.2 | ثبت ساعت کارآموزی هنرستان | چت ۳ | ۰.۵ روز |
| E.3 | گیمیفیکیشن رفتاری برای دبستان | چت ۳ | ۲ روز |
| E.4 | مدیریت کتابخانه‌ی مدرسه | چت ۱ | ۱ روز |
| E.5 | مدیریت اموال/انبار مدرسه | چت ۱ | ۱ روز |
| E.6 | تولید خودکار برنامه‌ی هفتگی | چت ۳ | ۳ روز |
| E.7 | گردش کار امتحانات شهریور/تجدیدی | چت ۲ | ۱ روز |
| E.8 | کلاس‌های تابستانی (ماژول سبک) | ✅ کامل توسط چت ۴ | `docs/SUMMER_CLASSES_MODULE.md` + `tests/summer*.js` |
| E.9 | مدیریت مراجعین (Visitor Management) | چت ۱ | ۰.۵ روز |
| E.10 | شاخص «سلامت مدرسه» (G.1 – کدنویسی) | چت ۴ | ۱ روز |
| E.11 | پایگاه دانش برای کاربر نهایی (USER_GUIDE) | چت ۳ | ۰.۵ روز |
| E.12 | صفحه‌ی وضعیت عمومی سرویس | چت ۲ | ۰.۵ روز |

---

# 24. قرارداد اجباری گزارش هر Arena

هر Arena بعد از هر task باید بدهد:

```text
TASK:
FILES CHANGED:
WHY:
ARCHITECTURAL IMPACT:
DATABASE IMPACT:
SECURITY IMPACT:
PERFORMANCE IMPACT:
NEW TESTS:
ALL TEST RESULTS:
KNOWN LIMITATIONS:
MIGRATION REQUIRED:
ROLLBACK PLAN:
COMMIT:
```

---

# 25. قوانین جلوگیری از خراب شدن معماری

هیچ Arena مجاز نیست:

- Data Layer را دور بزند.
- مستقیم `fetch`/`localStorage` را خارج از قرارداد پروژه اضافه کند.
- authorization مستقل و موازی بسازد.
- schema را بدون migration تغییر دهد.
- production fallback حافظه‌ای برای state حیاتی بسازد.
- business logic تکراری ایجاد کند.
- تست را برای سبز شدن ضعیف یا حذف کند.
- فقط برای افزایش سرعت cache بدون benchmark اضافه کند.
- دو source of truth ایجاد کند.

---

# 26. Definition of Done

هر تغییر مهم زمانی Done است که:

```text
Code
+ Tests
+ Security
+ Performance impact
+ Migration impact
+ Observability
+ Documentation
```

بررسی شده باشد.

---

# 27. Production Readiness Gate

## Data
- [ ] PostgreSQL تنها Source of Truth
- [ ] transactions
- [ ] constraints
- [ ] migrations
- [ ] OCC
- [ ] tombstones

## Security
- [ ] centralized authorization
- [ ] tenant isolation
- [ ] secret management
- [ ] SAST/DAST/SCA
- [ ] penetration testing
- [ ] abuse protection

## Performance
- [ ] SQL pagination
- [ ] indexed queries
- [ ] no whole-store serialization
- [ ] no request-path sync disk I/O
- [ ] cache strategy

## Distributed
- [ ] stateless API
- [ ] Redis distributed state
- [ ] idempotency
- [ ] graceful shutdown

## Reliability
- [ ] HA database
- [ ] backup
- [ ] PITR
- [ ] restore drill
- [ ] failover drill
- [ ] RPO/RTO

## Observability
- [ ] metrics
- [ ] logs
- [ ] traces
- [ ] dashboards
- [ ] alerts

## Testing
- [ ] integration
- [ ] concurrency
- [ ] load
- [ ] stress
- [ ] spike
- [ ] soak
- [ ] chaos
- [ ] recovery

---

# 28. Anti-patternهای ممنوع

### 1
```text
PostgreSQL اضافه شود ولی store باقی بماند
```
= دو source of truth.

### 2
```text
Redis برای پوشاندن query بد
```

### 3
```text
افزایش RAM به جای scale-out
```

### 4
```text
Kubernetes قبل از stateless architecture
```

### 5
```text
catch کردن خطا و سکوت
```

### 6
```text
سبز کردن تست با تغییر تست
```

---

# 29. SLO اولیه برای benchmark

برای endpointهای حیاتی می‌توان به صورت اولیه benchmark را با این اهداف شروع کرد:

```text
p50 < 100ms
p95 < 300ms
p99 < 1s
5xx < 0.1%
```

این اعداد هدف اولیه تست هستند و باید پس از capacity model رسمی نهایی شوند.

---

# 30. اسناد اجباری پایان پروژه

```text
docs/
  NATIONAL_ARCHITECTURE.md
  NATIONAL_BASELINE.md
  DATABASE_ARCHITECTURE.md
  AUTHORIZATION_MODEL.md
  SYNC_PROTOCOL.md
  DISASTER_RECOVERY.md
  SECURITY_MODEL.md
  OBSERVABILITY.md
  CAPACITY_MODEL.md
  LOAD_TEST_PLAN.md
  LOAD_TEST_RESULTS.md
  PRODUCTION_RUNBOOK.md
  INCIDENT_RESPONSE.md
  MIGRATION_GUIDE.md
```

---

# 31. ترتیب اجرای قطعی

```text
1. Baseline
2. PostgreSQL Source of Truth
3. DB reads/writes/transactions
4. Migrations + constraints + IDs
5. Unified authorization + tenant isolation
6. DB-native Pull/Push
7. Redis distributed state
8. Idempotency + OCC
9. SQL pagination/index/query optimization
10. Async workers/outbox
11. Offline queue hardening
12. HA + Backup + PITR + Restore
13. Observability
14. Deployment hardening
15. Security verification
16. 10M realistic dataset
17. Load + Stress + Spike + Soak
18. Chaos + Recovery
19. Final architecture/security/performance sign-off
20. Go-Live
```

**ترتیب را جابه‌جا نکنید مگر با Architecture Review.**

---

# 32. معیار نهایی GO / NO-GO

## NO-GO اگر:
- PostgreSQL source of truth نیست.
- state بین instanceها متفاوت است.
- full scan در مسیر پرترافیک وجود دارد.
- persistence failure silently موفق اعلام می‌شود.
- authorization بین endpointها متفاوت است.
- tenant isolation اثبات نشده.
- Redis critical state fallback محلی دارد.
- restore تست نشده.
- load/soak test واقعی انجام نشده.
- observability ناکافی است.
- recovery اثبات نشده.

## GO اگر:
تمام blockerهای بالا رفع شده باشند و functional، security، integration، concurrency، load، stress، spike، soak و recovery با acceptance criteria رسمی پروژه پاس شوند.

---

# 33. نتیجه مهندسی

پایش برای رسیدن به National Scale نیازمند «افزایش منابع» صرف نیست؛ باید به معماری زیر برسد:

```text
PostgreSQL
= authoritative data

Redis
= distributed ephemeral state/cache

Browser
= bounded local cache + offline queue

API
= stateless

Workers
= asynchronous heavy operations

Authorization
= centralized + tenant-aware

Observability
= metrics + logs + traces

Reliability
= HA + backup + PITR + tested recovery

Capacity
= proven by realistic load testing
```

هدف نهایی «تست سبز» نیست؛ هدف **اثبات ظرفیت، امنیت، درستی داده، پایداری و recovery با آزمایش واقعی** است.

---

# 34. Wave 21 — قابلیت‌های اجرایی آینده در مقیاس ملی [Planned]

> **وضعیت کل بخش:** 🟡 Planned — این ویو پس از جمع‌شدن ویو ۲۰ و تأیید ناظر ارشد
> وارد فازبندی اجرایی می‌شود. هیچ‌یک از بندهای این بخش به‌تنهایی مجوز شروع
> کد ندارند؛ هر بند برای شروع نیازمند طراحی مستقل، تأیید ناظر و تعریف
> Evidence است (قرارداد گزارشِ بخش ۲۴ برای همهٔ بندها برقرار است).
>
> **ارتباط با الحاقات:** این ویو مکمل `docs/NATIONAL_SCALE_FUTURE_UPGRADES_ADDENDUM.md`
> (فازهای ۸.۶ تا ۹.۲) است؛ موارد مشترک در هر بند ارجاع داده شده‌اند و
> هیچ‌کدام جایگزین دیگری نیست.

## W21-01 — Smart Notification Engine (موتور اعلان هوشمند) [Planned]

**هدف:** اعلان‌رسانی هوشمند به اولیا، دانش‌آموز و معلم با کمترین نویز.

**شرح کار:**
- موتور اعلان هوشمند برای سه نقش: اولیا، دانش‌آموز، معلم.
- کاهش پیام‌های غیرضروری (جلوگیری از خستگی اعلان).
- اولویت‌بندی و مدیریت هوشمند اعلان‌ها (تجمیع، زمان‌بندی، سقوطِ مؤدبانه در صف).

**ارزش معماری:** مصرف‌کنندهٔ طبیعیِ زیرساخت صف/اوت‌باکس است (ارجاع: فاز ۸.۸
الحاقات — صف مستقل اعلان)؛ اعلان‌ها باید از مسیر ناهمگام و ایدمپوتنت عبور
کنند، نه از درخواستِ اصلی کاربر.

**وضعیت:** 🟡 Planned

## W21-02 — School Digital Twin (دوقلوی دیجیتال مدرسه) [Planned]

**هدف:** مدل دیجیتال هر مدرسه برای دید لحظه‌ای وضعیت عملیاتی.

**شرح کار:**
- ساخت مدل دیجیتال مدرسه (کلاس‌ها، منابع، جریان‌های آموزشی).
- نمایش لحظه‌ای وضعیت کلاس‌ها، منابع و شاخص‌های آموزشی.

**ارزش معماری:** لایهٔ نمایش روی دادهٔ موجود (بدون Source of Truth جدید)؛
خواندن‌ها باید از مسیر ایندکس‌دار/کش‌شده باشند تا مسیر رندر و تراکنش‌ها
سنگین نشود.

**وضعیت:** 🟡 Planned

## W21-03 — Early Warning System (هشدار زودهنگام افت تحصیلی) [Planned]

**هدف:** شناسایی زودهنگام دانش‌آموزان در معرض افت، پیش از بحرانی‌شدن.

**شرح کار:**
- سیستم هشدار زودهنگام افت تحصیلی.
- تحلیل ترکیبی حضور، نمره، رفتار و فعالیت دانش‌آموز.

**ارزش معماری:** تحلیل باید روی کپی تحلیلی/بازه‌ای داده اجرا شود تا فشار
گزارش‌گیری سنگین به تراکنش‌ها نرسد (هم‌راستا با فاز ۸.۷ الحاقات)؛ خروجی
هشدار فقط به نقش‌های مجاز با شرط `school_id` نمایش داده می‌شود.

**وضعیت:** 🟡 Planned

## W21-04 — AI Assistant داخلی پایش [Planned]

**هدف:** دستیار هوشمند مدیریتی برای گزارش‌گیری و تحلیل.

**شرح کار:**
- دستیار هوشمند مدیریتی داخل سامانه.
- گزارش‌گیری و تحلیل با رعایت کامل Policy و RBAC.

**ارزش معماری:** دستیار هیچ دسترسی فراتر از نقشِ کاربرِ درخواست‌دهنده ندارد
(اصل شکست-بسته)؛ هر پرسش از لایهٔ محدوده (`scope`) عبور می‌کند و خروجی آن
نیز مانند هر خروجی دیگر با `esc()` پالایش می‌شود.

**وضعیت:** 🟡 Planned

## W21-05 — National Observability Platform [Planned]

**هدف:** رصدپذیری یکپارچه در مقیاس ملی.

**شرح کار:**
- معیارها (Metrics).
- ردیابی توزیع‌شده (Distributed Trace).
- داشبورد ممیزی (Audit Dashboard).
- پایش SLA.

**ارزش معماری:** تکمیل‌کنندهٔ گیت «رصدپذیری» بخش ۲۷ و سنجه‌های بخش ۲۹؛
هر ادعای مقیاس ملی بدون این چهار رکن غیرقابل دفاع است (ارجاع: فاز ۹.۲
الحاقات برای اعتبارسنجی بار).

**وضعیت:** 🟡 Planned

## W21-06 — Advanced Multi-Tenant Isolation [Planned]

**هدف:** جداسازی سخت‌تر داده در سطوح مدرسه/منطقه/استان.

**شرح کار:**
- تقویت جداسازی مدرسه/منطقه/استان.
- اعمال سیاست (Policy Enforcement) به‌صورت مرکزی.

**ارزش معماری:** ادامهٔ مستقیم اصول «جداسازی مستأجر» و محدوده‌بندی سرور
(بخش ۲۵ و میان‌افزار محدوده)؛ هر کوئری باید شرط سطحِ خود را داشته باشد و
سیاست‌ها نباید در لایهٔ نمایش تکرار شوند.

**وضعیت:** 🟡 Planned

## W21-07 — Advanced Backup & Disaster Recovery [Planned]

**هدف:** بازیابی عملیاتی‌شده با اهداف کمی.

**شرح کار:**
- پشتیبان‌گیری چندمنطقه‌ای (Multi Region Backup).
- مانور بازیابی دوره‌ای (Restore Drill).
- تعریف و پایش عملیاتی RPO/RTO.

**ارزش معماری:** ارتقای رکن «پایداری» بخش ۲۷ از طراحی به عملیات اثبات‌شده؛
ارجاع: فاز ۹ الحاقات (Disaster Recovery drills) — این بند آن را به اهداف کمی
RPO/RTO و چندمنطقه‌ای بودن ارتقا می‌دهد.

**وضعیت:** 🟡 Planned

## W21-08 — National Analytics Platform [Planned]

**هدف:** تحلیل ملی بدون آسیب به تراکنش‌ها.

**شرح کار:**
- جداسازی بارهای کاری تراکنشی و تحلیلی (OLTP و OLAP).
- طراحی انبار داده (Data Warehouse) آینده.
- تحلیل‌های ملی تجمیعی.

**ارزش معماری:** گسترش فاز ۸.۷ الحاقات (مسیر Read Replica و خط لولهٔ
رویداد)؛ `PostgreSQL` همچنان تنها Source of Truth باقی می‌ماند و انبار داده
فقط مقصد تحلیلی است.

**وضعیت:** 🟡 Planned

## W21-09 — Adaptive Load Management [Planned]

**هدف:** عبور سالم از پیک‌های ملی بدون فروپاشی.

**شرح کار:**
- مدیریت پیک مهر (بازگشایی مدارس).
- اعمال فشار معکوس (Backpressure) در صف‌ها.
- محدودسازی پویا (Dynamic Throttling).
- کنترل بار هوشمند در سطح ورودی.

**ارزش معماری:** مکمل سناریوهای بار/اسپایک فاز ۹.۲ الحاقات و زیرساخت
محدودسازی نرخِ توزیع‌شده؛ کنترل بار باید در لبه و درگاهِ سرور اعمال شود،
نه داخل منطق کسب‌وکار.

**وضعیت:** 🟡 Planned

## W21-10 — Zero Trust Security Expansion [Planned]

**هدف:** گسترش امنیت فراتر از احراز هویت کلاسیک، در مقیاس ملی.

**شرح کار:**
- اعتماد دستگاه (Device Trust).
- امتیازدهی ریسک نشست (Session Risk Scoring).
- رفتارشناسی ورود (لاگین).
- افزایش امنیت در مقیاس ملی.

**ارزش معماری:** ادامهٔ ارزیابی آمادگی اعتماد-صفر (فاز ۸) و لایهٔ لبهٔ
فاز ۸.۶ الحاقات؛ تصمیم‌های ریسک باید شکست-بسته باشند و دادهٔ رفتارِ ورود،
خود مشمول قواعد دادهٔ حساس است.

**وضعیت:** 🟡 Planned

---

# 35. Future Research — طرح‌های تحقیقاتی بلندمدت (خارج از فاز اجرایی)

> **وضعیت کل بخش:** 🔵 ثبت‌شده برای آینده — این موارد فقط به‌عنوان چشم‌انداز
> بلندمدت (Future Research / Long Term Vision) ثبت می‌شوند و وارد هیچ فاز
> اجرایی نمی‌شوند، مگر با تصمیم صریح ناظر ارشد. هیچ‌کدام مجوز طراحی تفصیلی
> یا کد ندارند.

## R-01 — Federated Learning آموزشی [تحقیقاتی]

- آموزش مدل‌های هوشمند بدون انتقال دادهٔ خام مدارس به مرکز.
- حفظ حریم خصوصی در مقیاس ملی.

## R-02 — Privacy Preserving Analytics [تحقیقاتی]

- تحلیل آماری بدون افشای اطلاعات شخصی.
- استفاده از حریم خصوصی تفاضلی (Differential Privacy).

## R-03 — Digital Identity ملی آموزشی [تحقیقاتی]

- هویت دیجیتال دانش‌آموز و معلم.
- اتصال احتمالی به سرویس‌های هویتی آیندهٔ ملی.

## R-04 — Blockchain Audit Ledger [تحقیقاتی]

- ثبت غیرقابل تغییر رویدادهای حساس.
- فقط برای سناریوهای حاکمیتی خاص (نه مسیر عمومی سامانه).

## R-05 — AI Curriculum Intelligence [تحقیقاتی]

- تحلیل کیفیت آموزش.
- پیشنهاد بهبود محتوای آموزشی.

## R-06 — Edge Computing برای مدارس کم‌اتصال [تحقیقاتی]

- اجرای بخشی از سرویس در خود مدرسه.
- همگام‌سازی امن با مرکز (سازگار با فلسفهٔ آفلاین‌محور فعلی).

## R-07 — National Simulation Environment [تحقیقاتی]

- شبیه‌سازی ۱۰ تا ۵۰ میلیون کاربر.
- تست پیش از هر تغییر بزرگ در مقیاس ملی.

---

# 36. Wave 22 — Educational Ecosystem Expansion (گسترش اکوسیستم آموزشی) [Planned]

**هدف ویو:** تبدیل پایش از «سامانهٔ مدیریت مدرسه» به «اکوسیستم آموزشی» با پوشش
ارزیابی، تدریس، سلامت روان، هدایت تحصیلی، مشارکت اولیا، حمل‌ونقل، تغذیه،
پروندهٔ مهارت، اتصال‌های حاکمیتی و زیرساخت رسانه — بر پایهٔ سند پژوهشی
نیازمندی‌های نظام آموزشی (نزدیک‌ترین سندِ موجود در مخزن:
`docs/RESEARCH_2026-09-05_UNIFIED_EDU_ROADMAP.md`).

**قابلیت‌ها:** ده بند `W22-01` تا `W22-10` (در ادامه همین بخش).

**ارزش معماری/محصولی:** هر بند یک دامنهٔ مستقل با مدل دادهٔ خود است و باید
بدون شکستن اصل‌های موجود (شکست-بسته، جداسازی مستأجر، آفلاین‌محور، تک‌فایلی)
پیاده شود؛ اشتراکِ همهٔ بندها در زیرساخت‌های مشترک است (اعلان، دسترسی،
ایدمپوتنسی) تا دوباره‌کاری نشود.

**وابستگی‌های سراسری ویو:**
- موتور اعلان (`W21-01`) برای اطلاع‌رسانیِ بندهای اولیا/حمل‌ونقل/تغذیه.
- زیرساخت دستیار هوشمند (`W21-04`) برای بندهای مبتنی بر هوش مصنوعی.
- مدل دسترسی نقش‌محور (`WRITE_PERMS`) — هیچ نقش جدیدی هاردکد نمی‌شود.
- زیرساخت فایل/رسانه (`W22-10`) برای هر بندِ دارای پیوست.

**وضعیت:** 🟡 Planned — شروع هر بند منوط به طراحی مستقل، تأیید ناظر ارشد و
قرارداد بخش ۲۴ است.

## W22-01 — Advanced Assessment Engine (موتور آزمون‌سازی پیشرفته) [Planned]

**هدف:** موتور آزمون‌سازی پیشرفته برای کاهش بار معلم و افزایش کیفیت ارزیابی.

**شرح کار:**
- بانک سؤال استاندارد.
- دسته‌بندی سؤالات بر اساس اهداف شناختی بلوم.
- برگزاری آزمون آنلاین.
- تحلیل دشواری و تمایز سؤالات.
- تصحیح پاسخ‌برگ با اسکنر `OMR`.

**ارزش معماری/محصولی:** بارِ تصحیح و تحلیل از دوش معلم برداشته می‌شود و
دادهٔ کیفیتِ سؤال (نه فقط نمره) برای اولین بار در سامانه شکل می‌گیرد.

**وابستگی‌ها:** ماژول امتحانات و نمرات فعلی؛ ذخیرهٔ تصویر پاسخ‌برگ از مسیر
`W22-10`؛ تحلیل دشواری/تمایز فقط روی دادهٔ مدرسهٔ خودش (شرط `school_id`).

**وضعیت:** 🟡 Planned

## W22-02 — AI Teaching Assistant (دستیار هوشمند معلم) [Planned]

**هدف:** دستیار هوشمند معلم برای آماده‌سازی تدریس.

**شرح کار:**
- تولید طرح درس روزانه و سالانه.
- ساخت تمرین و کاربرگ شخصی‌سازی‌شده.
- پیشنهاد روش تدریس بر اساس ضعف کلاس.

**الزام:** کاملاً تحت `RBAC` و حکمرانی سیاست‌ها (Policy Governance) — دستیار
هیچ داده‌ای فراتر از نقش معلمِ درخواست‌دهنده نمی‌بیند.

**ارزش معماری/محصولی:** نخستین مصرف‌کنندهٔ تخصصیِ زیرساخت دستیار؛ خروجی
دستیار «پیشنهاد» است، نه ثبتِ خودکار — تأیید نهایی همیشه با معلم است.

**وابستگی‌ها:** زیرساخت دستیار و مجوزها (`W21-04`)؛ دادهٔ نمره/حضور کلاس؛
بانک سؤال (`W22-01`) برای تولید تمرین.

**وضعیت:** 🟡 Planned

## W22-03 — Student Mental Health & Counselor Platform (سلامت روان و مشاوره) [Planned]

**هدف:** افزودن لایهٔ سلامت روان به اکوسیستم مدرسه.

**شرح کار:**
- غربالگری دوره‌ای.
- پروندهٔ محرمانهٔ مشاور.
- گزارش ناشناس قلدری.
- هشدارهای خطر اجتماعی.

**الزام:** سطح دسترسی بسیار محدود و **مجازی‌سازی سطح فیلد (Field-Level
Authorization)** الزامی است — حتی مدیر مدرسه نباید متن پروندهٔ مشاور را
ببیند؛ وجود یا فقدانِ رکورد هم می‌تواند حساس باشد.

**ارزش معماری/محصولی:** حساس‌ترین دادهٔ سامانه در این بند است؛ طراحی آن
الگوی دادهٔ محرمانه برای کل پلتفرم می‌شود.

**وابستگی‌ها:** مدل دسترسی (`AUTHORIZATION_MODEL`) با توسعهٔ مجوز سطح فیلد؛
قواعد دادهٔ حساس (§۰.۵)؛ نقش مشاور از مسیر پیکربندی دسترسی، بدون هاردکد.

**وضعیت:** 🟡 Planned

## W22-04 — Career Guidance & Talent Discovery (هدایت تحصیلی و استعدادیابی) [Planned]

**هدف:** هدایت تحصیلی هوشمند برای انتخاب مسیر.

**شرح کار:**
- رغبت‌سنجی.
- استعدادسنجی.
- تحلیل عملکرد چندساله.
- شبیه‌ساز انتخاب رشته.

**ارزش معماری/محصولی:** تصمیم‌سازی داده‌محور برای پایه‌های نهم و دوازدهم؛
خروجی جنبهٔ اطلاع‌رسانی دارد، نه مشاورهٔ رسمیِ جایگزینِ انسان.

**وابستگی‌ها:** تاریخچهٔ نمرات چندساله (بایگانی سال‌های تحصیلی)؛ زیرساخت
تحلیلی (`W21-03`/`W21-08`)؛ قواعد نمایش دادهٔ رشته‌ها (بند ۵.۱ سند پژوهشی).

**وضعیت:** 🟡 Planned

## W22-05 — Parent Engagement Portal (درگاه مشارکت اولیا) [Planned]

**هدف:** ارتقای ارتباط و مشارکت اولیا در مدرسه.

**شرح کار:**
- نوبت‌دهی ملاقات با معلم.
- رضایت‌نامهٔ دیجیتال.
- انتخابات انجمن اولیا و مربیان.
- شفافیت مالی مدرسه.

**ارزش معماری/محصولی:** بیشترین اثر را روی اعتمادِ عمومی مدرسه دارد؛
رضایت‌نامهٔ دیجیتال باید ممیزی‌پذیر (چه کسی، کِی) باشد.

**وابستگی‌ها:** موتور اعلان (`W21-01`)؛ ماژول مالی موجود (بند ۴.۱ سند
پژوهشی) برای شفافیت؛ احراز هویت ولی و اتصال به فرزندان.

**وضعیت:** 🟡 Planned

## W22-06 — Smart Transportation Platform (حمل‌ونقل هوشمند مدرسه) [Planned]

**هدف:** مدیریت سرویس مدرسه با ردیابی و اطلاع‌رسانی.

**شرح کار:**
- ردیابی موقعیت (`GPS Tracking`).
- نمایش زندهٔ مسیر.
- حصار جغرافیایی (Geofencing).
- اطلاع‌رسانی والدین.

**ارزش معماری/محصولی:** نسخهٔ پیشرفتهٔ ماژول سرویسِ فعلی؛ دادهٔ موقعیت،
دادهٔ حساسِ جریان‌دار است و باید کران‌دار و کوتاه‌عمر بماند.

**وابستگی‌ها:** ماژول سرویس مدرسهٔ موجود؛ موتور اعلان (`W21-01`)؛
زیرساخت زمان‌بلادرنگ/اتصال ثالث (طراحی جداگانهٔ وابستگی خارجی).

**وضعیت:** 🟡 Planned

## W22-07 — Smart Cafeteria & Student Wallet (تغذیه و کیف پول دانش‌آموز) [Planned]

**هدف:** مدیریت تغذیه و خرید دانش‌آموز.

**شرح کار:**
- کیف پول دانش‌آموز.
- کنترل خرید (سقف، اقلام مجاز).
- منوی سالم.
- ثبت و رعایت حساسیت غذایی.

**ارزش معماری/محصولی:** تراکنش مالیِ خُرد = نیاز قطعی به ایدمپوتنسی و
مغایرت‌گیری روزانه؛ الگوی تراکنش‌های مالیِ آیندهٔ سامانه را تثبیت می‌کند.

**وابستگی‌ها:** ماژول مالی/کیف‌پول موجود؛ موتور ایدمپوتنسی؛ دادهٔ حساسیت
غذایی مشمول قواعد دادهٔ حساس است.

**وضعیت:** 🟡 Planned

## W22-08 — Student Skill Portfolio (پروندهٔ مهارتی دانش‌آموز) [Planned]

**هدف:** ثبت و نمایش دستاوردهای بیرون از نمره.

**شرح کار:**
- مسابقات.
- المپیادها.
- پروژه‌ها.
- مهارت‌های خارج از کلاس.

**ارزش معماری/محصولی:** پروندهٔ مهارتی، ورودیِ طبیعیِ هدایت تحصیلی
(`W22-04`) و مکمل کارنامهٔ رسمی است.

**وابستگی‌ها:** ذخیرهٔ پیوست/مدرک از مسیر `W22-10`؛ مدل مالکیت رکورد بین
مدرسه/منطقه.

**وضعیت:** 🟡 Planned

## W22-09 — National Interoperability Layer (لایهٔ همکاری‌پذیری ملی) [Planned — فقط طراحی]

**هدف:** آماده‌سازی اتصال‌های حاکمیتی.

**شرح کار:**
- سامانه‌های هدف: `SIDA`، `MyMedu`، شاهکار، ثبت احوال.
- **در این فاز فقط طراحی معماری و قرارداد `API` ثبت می‌شود** — هیچ اتصال
  زنده‌ای ساخته نمی‌شود.

**ارزش معماری/محصولی:** هر اتصال حاکمیتی بدون قراردادِ از پیش طراحی‌شده،
ریسک امنیت و یکپارچگی دارد؛ ثبت قراردادها الان، هزینهٔ اتصالِ بعدی را
کم می‌کند.

**وابستگی‌ها:** احراز هویت شکست-بسته، ایدمپوتنسی، و ممیزی کامل هر تراکنشِ
برون‌سامانه‌ای؛ مجوزهای حاکمیتی خارج از کنترل فنی پروژه است.

**وضعیت:** 🟡 Planned (محدود به طراحی و قرارداد)

## W22-10 — Media Learning Infrastructure Preparation (زیرساخت رسانهٔ آموزشی) [Planned]

**هدف:** آماده‌سازی آیندهٔ آموزش چندرسانه‌ای.

**شرح کار:**
- ذخیره‌سازی شیئی (Object Storage).
- شبکهٔ توزیع محتوا (`CDN`).
- مدیریت فایل آموزشی.
- آماده‌سازی برای ویدیوی آموزشی.

**ارزش معماری/محصولی:** هیچ‌کدام از بندهای دارای پیوست (`W22-01`، `W22-08`)
بدون زیرساخت فایل، مقیاس‌پذیر نیستند؛ این بند پیش‌نیازِ مشترک است.

**وابستگی‌ها:** تصمیم‌نامهٔ `CDN`/لبه (فاز ۸.۶ الحاقات ملی)؛ اصل تک‌فایلی
نسخهٔ آفلاین نباید شکسته شود — رسانه فقط در مسیر سروری است.

**وضعیت:** 🟡 Planned

---

# 37. Future Research — الحاقات پژوهشی اکوسیستم آموزشی (خارج از فاز اجرایی)

> **وضعیت کل بخش:** 🔵 ثبت‌شده برای آینده — موارد زیر فقط چشم‌انداز بلندمدت
> هستند و وارد هیچ فاز اجرایی نمی‌شوند، مگر با تصمیم صریح ناظر ارشد. مواردی
> که پیش‌تر در بخش ۳۵ ثبت شده‌اند، اینجا با همان وضعیت حفظ و با ارجاع آمده‌اند
> (بدون حذف و بدون ادغام).

## R-08 — Learning Analytics ملی مبتنی بر هوش مصنوعی [تحقیقاتی]

**توضیح کوتاه:** تحلیل یادگیری در سطح ملی با مدل‌های هوشمند برای کشف الگوهای
افت و موفقیت.
**ارزش احتمالی:** سیاست‌گذاری آموزشی مبتنی بر شواهد در مقیاس کشور.
**دلیل عدم ورود فعلی:** پیش‌نیازهای داده‌ای (انبار دادهٔ `W21-08`، جداسازی
`OLTP/OLAP`) هنوز اجرایی نشده‌اند و مدل ملی بدون زیرساخت رصدپذیری ملی
(`W21-05`) قابل دفاع نیست.

## R-09 — Federated Learning برای حفظ حریم خصوصی [تحقیقاتی]

**توضیح کوتاه:** آموزش مدل‌های هوشمند بدون انتقال دادهٔ خام مدارس به مرکز.
**ارزش احتمالی:** حفظ حریم خصوصی در مقیاس ملی.
**دلیل عدم ورود فعلی:** ثبت‌شده به‌عنوان `R-01` (وضعیت بدون تغییر)؛ تا زمانی
که زیرساخت هوشمند مرکزی (`W21-04`) وجود ندارد، لایهٔ فدرال موضوعیت ندارد.

## R-10 — Differential Privacy در تحلیل آموزشی [تحقیقاتی]

**توضیح کوتاه:** تحلیل آماری بدون افشای اطلاعات شخصی با حریم خصوصی تفاضلی.
**ارزش احتمالی:** انتشار آمارهای ملی بدون ریسک بازشناسی افراد.
**دلیل عدم ورود فعلی:** ادامهٔ `R-02` (وضعیت بدون تغییر)؛ بدون پلتفرم تحلیل
ملی (`R-08`) کاربردی برای این لایه تعریف نمی‌شود.

## R-11 — Blockchain برای سوابق آموزشی [تحقیقاتی]

**توضیح کوتاه:** ثبت غیرقابل تغییر سوابق آموزشی روی دفتر کل توزیع‌شده.
**ارزش احتمالی:** اعتبارسناسی اسناد در سطح ملی.
**دلیل عدم ورود فعلی:** ثبت‌شده به‌عنوان `R-04` (فقط سناریوهای حاکمیتی خاص)؛
زیرساخت ممیزی فعلی نیاز را پوشش می‌دهد و هزینهٔ دفتر کل توزیع‌شده هنوز
توجیه ندارد.

## R-12 — Edge Computing برای مدارس کم‌اتصال [تحقیقاتی]

**توضیح کوتاه:** اجرای بخشی از سرویس در خود مدرسه با همگام‌سازی امن با مرکز.
**ارزش احتمالی:** پوشش مدارس کم‌برخوردار و روستایی.
**دلیل عدم ورود فعلی:** ثبت‌شده به‌عنوان `R-06` (وضعیت بدون تغییر)؛ نسخهٔ
تک‌فایلیِ آفلاین، بخش بزرگی از این نیاز را امروز پوشش می‌دهد.

## R-13 — Digital Twin کامل مدرسه [تحقیقاتی]

**توضیح کوتاه:** شبیه‌سازی کامل و زندهٔ مدرسه (نه فقط داشبورد وضعیت).
**ارزش احتمالی:** پیش‌بینی رفتار سامانه و سناریوسازی مدیریتی.
**دلیل عدم ورود فعلی:** نمای عملیاتیِ لحظه‌ای به‌صورت `W21-02` در ویو ۲۱
ثبت است و همان کافی است؛ «دوقلوی کامل» با شبیه‌سازی پیش‌بینانه، هنوز بلوغ
داده‌ای ندارد و تحقیقاتی می‌ماند.

## R-14 — شبیه‌ساز بار ملی ۱۰ تا ۵۰ میلیون کاربر [تحقیقاتی]

**توضیح کوتاه:** شبیه‌سازی جمعیت ۱۰ تا ۵۰ میلیون کاربر پیش از تغییرات بزرگ.
**ارزش احتمالی:** اعتبارسنجی ظرفیت در مقیاس واقعیِ کشور.
**دلیل عدم ورود فعلی:** ثبت‌شده به‌عنوان `R-07` (وضعیت بدون تغییر)؛ فعلاً
اعتبارسنجی با فاز ۹.۲ الحاقات (بار/اسپایک/سوک) انجام می‌شود و شبیه‌ساز
چندده‌میلیونی زیرساخت اختصاصی می‌خواهد.

---

## پیوست: وضعیت فاز ۵ (E) — تکمیل‌شده‌ها (از roadmap قدیمی، دور ۱۰۳–۱۰۴)

> یادداشتِ ریبیسِ دور ۱۱۲: این جدول از roadmapِ قدیمی (نسخهٔ `2a2b74f`)
> به این سندِ مهندسی منتقل شد تا وضعیتِ تکالیفِ فازِ E گم نشود.

| # | کار | مسئول | وضعیت |
|---|-----|-------|-------|
| E.1 | نمره‌ی عملی/کارگاهی هنرستان — قسمت‌های تئوری/عملی | چت ۳ | ✅ کامل (دور ۱۰۳) |
| E.2 | ثبت ساعت کارآموزی هنرستان | چت ۳ | ✅ کامل — PR #34 (بستهٔ هفت‌گانه @ cd484c3)؛ سند `docs/INTERNSHIP_MODULE.md`؛ تست `internship2` ‏4/4 + جهش ‏5/5 (راستی‌آزمایی محلی 2026-09-12) |
| E.3 | گیمیفیکیشن رفتاری برای دبستان | چت ۳ | ✅ کامل — PR #33 (بستهٔ هفت‌گانه @ cd484c3)؛ سند `docs/BEHAVIOR_GAMIFICATION.md`؛ تست `dojo` ‏6/6 (راستی‌آزمایی محلی 2026-09-12) |
| E.4 | مدیریت کتابخانه‌ی مدرسه | چت ۱ | ⏳ |
| E.5 | مدیریت اموال/انبار مدرسه | چت ۱ |  |
| E.6 | تولید خودکار برنامه‌ی هفتگی | چت ۳ | ✅ کامل — `src/js/74-schedgen.js` + سند `docs/SCHEDULE_GENERATOR.md`؛ تست‌ها `schedule-gen` ‏6/6 + جهش ‏5/5 + `schedconf2` ‏9/9 (راستی‌آزمایی محلی 2026-09-12) |
| E.7 | گردش کار امتحانات شهریور/تجدیدی | چت ۲ | ⏳ |
| E.8 | کلاس‌های تابستانی (ماژول سبک) | چت ۲ | ⏳ |
| E.9 | مدیریت مراجعین (Visitor Management) | چت ۱/۳ | ✅ کامل (دور ۱۰۴) |
| E.10 | شاخص «سلامت مدرسه» (G.1 – کدنویسی) | چت ۴ | ⏳ |
| E.11 | پایگاه دانش برای کاربر نهایی (USER_GUIDE) | چت ۳ | ✅ کامل (2026-09-12) — چهار پیشنهاد `docs/G3_USER_GUIDE_REVIEW.md` اجرا شد: عدد منو از `NAV_EXPECT` (۴۷)، فصل «تازه‌های سامانه» (۱۱ ماژول)، «یک روز کاری مدیر»، حذف بخش فنی، رفع ۸ غلط تایپی؛ گارد رگرسیون `tests/user-guide.js` ‏33/33 + جهش ‏5/5 |
| E.12 | صفحه‌ی وضعیت عمومی سرویس | چت ۲ | ⏳ |
| B.3 | PG منبعِ حقیقت — Wave 1 بخش ۲ (انتقالِ Writes + تراکنش‌ها) | چت ۳ | ✅ کامل (دور ۱۰۵ — اسنکواری در `docs/WAVE1_WRITES_INVENTORY.md`) |
| B.4 | PG/Redis: Wave 6 — Audit و تکمیلِ Distributed State (OTP/rate-limit/revocation/idempotency/cache/lock) | چت ۳ | ✅ کامل (`docs/WAVE6_REDIS_AUDIT.md`) |
| B.5 | کشینگ: Wave 11 — TTL، invalidation، stampede protection (Cache Hierarchy) | چت ۳ | ✅ کامل (`docs/WAVE11_CACHE_STRATEGY.md`) |
| B.6 | Health/Deployment: Wave 15 — Liveness/Readiness/Health + Graceful Shutdown + راهنمای Rolling Deployment/Rollback | چت ۳ | ✅ کامل (`docs/DEPLOYMENT_GUIDE.md`) |
| B.7 | تست بار ملی: Wave 18 — دادهٔ آزمایشیِ 10M کاربر + چهار سناریو (عادی/اوج/فشار/چند روزه) | چت ۳ | ✅ کامل — اجرا روی زیرساختِ زنده pending (`docs/WAVE18_LOAD_TEST_PLAN.md`) |
| B.8 | تست آشوب و شکست: Wave 19 — 5 سناریو (kill/redis/pg/latency/disk) + ابزار chaos-test.sh | چت ۳ | ✅ کامل — اجرای LIVE روی محیطِ چند-نمونه pending (`docs/WAVE19_CHAOS_PLAN.md`) |
| B.9 | نهایی‌سازی Arena 5: Wave 20 — سندِ QA/Reliability + Recovery Validation (32/32) + Release Gate + رفع دو نقصِ date-bound | چت ۳ | ✅ کامل — اجرای L3 واقعی (G1–G8) pending زیرساخت (`docs/ARENA5_QA_RELIABILITY.md`) |

## پیوست — وضعیت بندهای ب.۳/د.۲/د.۳/د.۴ (چت ۴)

> مرجع وضعیت بندهای در حال اجرا. جدیدترین وضعیت همیشه در بالای هر
> بخش می‌آید. تغییر وضعیت فقط پس از سبزبودن گیت‌ها (‏`smoke`،
> `check-authz`، آزمون‌های رفتاری و جهش همان بند) ثبت می‌شود.

**به‌روزرسانی:** ۱۸/۰۶/۱۴۰۵ (2026-09-09) — چت ۴

---

## بخش آ — بندهای سوپروایزر (موج فعلی)

| ردیف | بند | موضوع | وضعیت | شاخه / کامیت |
|------|------|--------|--------|--------------|
| A.1 | ب.۳ | ارزشیابی ناشناس معلم | ✅ کامل | `feat/b3-d234-chat4` @ `d868f3a` |
| A.2 | د.۲ | کارت امتیازی منطقه (۵ بعد + CSV) | ✅ کامل | `feat/b3-d234-chat4` @ `236e19e` |
| A.3 | د.۳ | اطلاعیه فوری/بحرانی اداره | ✅ کامل | `feat/b3-d234-chat4` @ `4266b1d` |
| A.4 | د.۴ | نمای کمبود نیروی انسانی | ✅ کامل | `feat/b3-d234-chat4` @ `7f7dc3e` |

> هر چهار بند در شاخهٔ `feat/b3-d234-chat4` پوش شده‌اند و **در انتظار
> تأیید صریح ناظر ارشد برای ادغام به `main`** هستند (اصل هشتم).
> جزئیات: `REPORT_B3_D234_CHAT4.md` و `HANDOFF.md`.
>
> **به‌روزرسانی 2026-09-10 (چت ۶، ممیزی هم‌راستایی):** پی‌آر #۳۵ (همین چهار بند)
> در بستهٔ هفت‌گانهٔ تیمی در `main` مرج شد (سر: `cd484c3`). اثر اسکیمایی: جدول
> `staff_posts` + ستون‌های `office_id`/`severity` روی `announcements` — ثبت‌شده در
> `server/schema.sql` (بدون مهاجرت؛ انحراف ثبت‌شده) و `docs/DATA_DICTIONARY.md`
> (بازتولید: ۹۳ جدول). وضعیت هر چهار ردیف: ✅ مرج‌شده.

### خلاصهٔ فنی بندهای کامل‌شده

- **A.1 — ب.۳:** پرسش‌نامهٔ ناشناس دانش‌آموز/ولی؛ هیچ فیلد هویتی در
  ذخیره‌گاه، پاسخ خطا یا آدیت ثبت نمی‌شود؛ `upd:[]/del:[]` تغییرناپذیر؛
  دروازهٔ `server/sync.js` بر مالکیت مدرسه/دانش‌آموز. آزمون: ۱۲/۱۲ رفتاری
  + ۱۳/۱۳ سرور + ۵/۵ جهش.
- **A.2 — د.۲:** امتیاز ۰ تا ۱۰۰ در ۵ بعد (مالی/داخلی/رشد/رضایت/مدیریت)
  با بازنرمال‌سازی وزن روی داده‌های موجود؛ «داده ناکافی» به‌جای صفر؛
  بازهٔ رنگی (سبز ≥۶۰، کهربایی ≥۳۵، قرمز <۳۵) + خروجی CSV؛ دامنه
  `officeScopeSchools`. آزمون: ۱۴/۱۴ + ۶/۶ جهش.
- **A.3 — د.۳:** `severity` ∈ normal/urgent/critical (نبود فیلد = عادی،
  سازگار با گذشته) + `office_id`؛ بنر قرمز/کهربایی، بج «اطلاعیهٔ اداره»،
  مرتب‌سازی بحرانی > فوری > تازه‌ترین؛ دروازهٔ سرور انتشار بین‌اداره‌ای
  را می‌بندد. آزمون: ۱۳/۱۳ + ۶/۶ جهش.
- **A.4 — د.۴:** مجموعهٔ تازهٔ `staff_posts {school_id, subject_id,
  required}`؛ موجود = معلمان متمایز هر درس از برنامهٔ هفتگی؛ بدون هنجار =
  «تعریف نشده» (هرگز صفر ساختگی)؛ تجمیع شهرستان/استان؛ ویرایشگر هنجار با
  اعتبارسنجی؛ دروازهٔ دامنهٔ اداره در سرور. آزمون: ۱۶/۱۶ + ۷/۷ جهش.

---

## سایر کارهای در جریان (وضعیت از گزارش سوپروایزر)

| چت | کار | وضعیت |
|-----|-----|--------|
| چت ۱ | فیلد «نوع مدرسه» (فاز ۰٫۱) | ⏳ در حال اجرا |
| چت ۳ | نمرهٔ عملی/کارگاهی هنرستان (E.1) | ⏳ در حال اجرا |

---

## گیت‌های سراسری (آخرین اجرا — ۲۰۲۶-۰۹-۰۹، سرِ `feat/b3-d234-chat4`)

| گیت | نتیجه |
|---|---|
| `tests/smoke.js` | ✅ ۵۴۷/۵۴۷ |
| `tools/check-authz.js` | ✅ خروجی ۰ |
| `tests/authz-model.js` | ✅ ۲۵۰/۲۵۰ |
| `tests/server16.js` | ✅ ۳۹/۳۹ |
| `build --check` | ✅ بیت‌به‌بیت + تطبیق راهنما |

---

## اسناد مرتبط

- سند جامع پژوهشی و فازبندی: `docs/RESEARCH_2026-09-05_UNIFIED_EDU_ROADMAP.md`
- اسکن نهایی نقشه‌راه در برابر کد: `docs/ROADMAP_FINAL_SCAN_2026-09-08.md`
- الحاقات مقیاس ملی (فازهای ۸.۶ تا ۹.۲ — مکمل بخش‌های ۳۴ و ۳۵): `docs/NATIONAL_SCALE_FUTURE_UPGRADES_ADDENDUM.md`
- پیش‌نویس‌های طراحی د.۲/د.۳/د.۴: `docs/D2_DRAFT.md`، `docs/D3_DRAFT.md`، `docs/D4_DRAFT.md`
- دفترچهٔ تحویل: `HANDOFF.md`
