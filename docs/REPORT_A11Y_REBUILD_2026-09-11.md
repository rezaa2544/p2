# گزارشِ تجمیعی: بازسازیِ دسترس‌پذیری WCAG 2.1 AA — 2026-09-11

شاخه: `feat/a11y-rebuild` (مبنا `main@ac70c4c`) · PR #69: https://github.com/rezaa2544/p2/pull/69

## چه شد

حسابرسیِ WCAG 2.1 AA رویِ ۵ نقش (مدیر، دبیر، ولی، دانش‌آموز، کارکنان) و ۱۰ محور انجام شد؛
۱۶ یافته (۸ سالمِ ازپیش‌موجود) ثبت و هر ۸ رفعِ P1 در کامیتِ جدا با تستِ قرمزِ اول و اثباتِ جهش
(MUTATED→قرمز→RESTORED→سبز) اعمال شد. رگرسیونِ جدید `tests/a11y-audit.js` با ۴۰ بررسی از
۱۰/۴۰ به **۴۰/۴۰** رسید.

## کامیت‌ها (ترتیب)

1. `77239b6` docs: یافته‌ها + تست (قرمزِ اول)
2. `04c0acd` skip-link + `main` + نامِ `aside` (2.4.1/1.3.1)
3. `937b693` توست `role=status` + `aria-live` (4.1.3)
4. `b0febe8` نامِ برگر/بازگشت/زنگ (4.1.2)
5. `cf8b154` اتصالِ `for` در `f()` + لیبل‌های لاگین (1.3.1/3.3.2)
6. `673b965` ‏`alt` تصاویر/رسانه (1.1.1)
7. `5f76fc1` سمانتیک dialog + فوکوس/تله/بازگردانی (4.1.2/2.4.3)
8. `b07b487` نامِ دکمه‌های ✕ (4.1.2)
9. `14be693` سرستون جداول audit (1.3.1)
10. `4dc0a93` جایگزین متنی canvas (1.1.1)
11. `6107bc1` مستندات (A11Y_GUIDE + §۱۷ راهنما + وضعیت نهایی audit)

## تست‌ها (نهایی)

a11y-audit **۴۰/۴۰** · smoke **۵۴۷/۵۴۷** · run **۳۵/۳۵** · check-authz exit 0 ·
secret-scan **۱۱/۱۱** · build --check سبز (index.html + مُهر راهنما + WRITE_PERMS).

## GitHub

- push: موفق (`6107bc1` — تأیید با `git ls-remote`)؛ باندل fallback در `Temp/a11y-rebuild.bundle` (verify شد).
- PR #69 به `main` باز است.
- احراز هویت: توکنِ `~/.payesh_gh_token` و توکنِ اولِ جدید مردود (`invalid credentials`)؛
  push با URL-token و PR با REST API انجام شد چون `gh` به اسکوپِ `read:org` گیر می‌دهد.

## چه می‌ماند (P2 — غیرمسدودکننده)

پالت کنتراست بج‌ها/تیتر منو (تصمیم بصری) · مهاجرت `panelPicker`/`parentGate` به `openModal` ·
نام/role اسپن `duty-del` · جایگزین صفحه‌کلیدِ بوم تصحیح · `aria-hidden` ایموجی‌ها.

## یادداشتِ محیطِ مشترک

درختِ کاری با نشستِ موازی مشترک است (تغییرِ خودکارِ شاخه، کامیت‌های `a8f750b`/`d133db2`،
ویرایش‌هایِ `secret-scan.js` و `server/routes/grades.js`): همه دست‌نخورده ماندند و هیچ‌کدام
stage/commit نشدند. جزئیات: `docs/A11Y_AUDIT.md`.
