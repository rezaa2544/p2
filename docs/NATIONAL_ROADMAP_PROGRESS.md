# پیگیری پیشرفت نقشه راه ملی پایش

**مرجع اصلی:** `docs/ROADMAP.md` — National Scale Master Roadmap

**مکمل الزام‌آور:** `docs/NATIONAL_ROADMAP_ARCHITECTURE_ADDENDUM.md` — اجرای Waves بدون رعایت آن مجاز نیست.

**قاعده Addendum:** فقط Status کافی نیست؛ هر ردیف باید Owner، Risk، Dependency و Evidence داشته باشد.

| Wave | عنوان | Owner | Status | Risk | Dependency | Evidence |
|---:|---|---|---|---|---|---|
| -1 | Architecture Discovery | Arena 5 / همه Arenaها | ✅ | High | پیش‌نیاز همه Waves | کامل شد: Dependency/Data/Auth/Sync Flow + docs/THREAT_MODEL.md + docs/BOTTLENECK_MAP.md |
| 0 | Baseline و Freeze | Arena 1 + Arena 5 | ✅ | Medium | Wave -1 کامل | Part 1: docs/NATIONAL_BASELINE.md؛ Part 2: docs/NATIONAL_BASELINE_PART2.md؛ Part 3: docs/NATIONAL_BASELINE_PART3.md؛ Part 4: docs/NATIONAL_BASELINE_PART4.md؛ tag national-baseline-start (Part 4 بازسازی شد — commit اولیهٔ 0245515 هرگز push نشده بود) |
| 1 | PostgreSQL Source of Truth [P0] | Arena 1 | ✅ | Critical | Wave -1 و Wave 2 (✅)؛ تعارض با C1-02 حل شد | P0 کامل و تثبیت‌شده: ادغام PR #48 و PR #47؛ بوت PG-authoritative، قفل fail-closed در تولید؛ tests/wave1-multi-instance.js ‏33/33‏، tools/wave1-gate.js ‏35/35‏، wave1-reads ‏18/18‏، wave1-writes ‏15/15‏ و جهش‌ها ‏5/5‏ سبز. رفع مانع پ0-۱ در P0_BLOCKER_TRACKER.md و گزارش تحویل 2026-09-17-wave1-canonical-delivery.md. |
| 2 | Database Engineering | Arena 1 | ✅ | High | Wave -1 برای کشف کامل؛ Wave 1 برای PG-only شدن تولید | docs/DATABASE_ARCHITECTURE.md؛ migrations/؛ tests/db-engineering.js |
| 3 | Query و Performance [P0] | Arena 1 + Arena 4 + چت ۳ | 🟡 | High | Wave 1/2 | builderهای DB-native (`server/dbquery.js`) + ۷ ایندکس (`007`) + EXPLAIN ANALYZE زنده روی PG 18.4 با 2.19M ردیف (چت ۲، PR #46)؛ **گیت برابری فیلد‌به‌فیلد JS↔SQL روی PG زنده — PR [#96](https://github.com/rezaa2544/p2/pull/96) مرج شد (2026-09-12 @ `f2f0ba1`)**: `tests/wave3-parity.js` ‏20/20 + جهش ‏5/5 + `wave3-query3` بخش B ‏25/25 (سید ~101k کاربر/36k حضور/157k نمره/30k کلاس)؛ ⚠️ CI مسدود بیلینگ — گیت‌ها محلی. باز: تصمیم tenancy روی `school_id IS NULL` (ناظر)، بخش B در CI، `CONCURRENTLY` در تولید |
| 4 | Sync / A01 | Arena 3 | 🟡 | High | Wave 1/2/5 | فاز ۴ همگام‌سازی با PR #71 (`e5c9b27`) مرج شد: backpressure، فشرده‌سازی delta، cursor warmup پایدار، sync metrics و cursor چندمنطقه‌ای؛ `delta-phase4` ‏23/23 و mutation ‏20/20. تکمیل باقی‌ماندهٔ Wave 4 و اجرای ملی هنوز باز است |
| 5 | Authorization و Tenant Isolation [P0] | Arena 2 | ✅ | Critical | Wave -1 و مدل واحد policy | مدل یکتای `server/policy.js` (Sync+REST+PG هم‌قرارداد)؛ `docs/WAVE5_AUTHZ.md` + `docs/AUTHORIZATION_MODEL.md`؛ wave5-authz 37/37، جهش‌ها 5/5، هم‌ارزی 93,024/۰، smoke 547/547، api 7/7 — commits 8472f17..d5230b7 |
| 6 | Redis و Distributed State [P0] | Arena 4 + چت ۳ | 🟡 | High | Wave 1 و سیاست production readiness | لایهٔ `server/redis.js` (standalone/cluster/sentinel + fallback درون‌حافظه‌ای) + rate-limit/lock/denylist توزیع‌شده؛ `tests/wave6-redis.js` ‏22/22 و `redis-fallback` ‏10/10؛ **قیدِ «ردیسِ زندهٔ هرگز تست‌نشده» بسته شد — PR [#97](https://github.com/rezaa2544/p2/pull/97) مرج (2026-09-12 @ `83ad521`)**: گیتِ `tests/wave6-11-redis-live.js` ‏16/16 رویِ Redis 7.4.2 واقعی (ساخت از سورس در سندباکس) + جهش ‏5/5 + `session-revocation` ‏18/18 بدونِ skip؛ سند: `docs/WAVE6_REDIS_AUDIT.md`. باقی: استقرارِ Redis مدیریت‌شده در تولید (لایهٔ استقرار) |
| 7 | Offline-first | Arena 3 + چت ۳ | 🟡 | Medium | Wave 4/6 | معماری آفلاین‌اولِ کلاینت پیاده و مستند است: صفِ آفلاین + retry/backoff + ادغامِ delta-sync (`docs/OFFLINE_FIRST.md`، `docs/CLIENT_OFFLINE_ARCHITECTURE.md`، `docs/WAVE7_OFFLINE_QUEUE.md`)؛ `tests/wave7-offline-queue.js` ‏7/7 و سوئیت‌هایِ `reports-offline`/`offline-optimization` سبز؛ موج ۲۱ (چندپایه) و موج ۲۳ (گزارش‌ها) نیز offline-first حفظ شدند. باقی: مانورِ قطعیِ طولانی end-to-end رویِ استیجینگ |
| 8 | Async Architecture | Arena 4 | ⏳ | Medium | Wave 1/2 و Outbox | در انتظار شروع |
| 9 | Application Performance | Arena 4 | 🟡 | High | Wave 3 و baseline عملکرد (Part 2/3 ✅) | شروع شد: heavy worker برایِ persist/backup/report + ممیزیِ پس‌زمینه + L1 محدود + کشِ استاتیک (docs/WAVE9_PERFORMANCE.md؛ tests/wave9-performance.js ‏39/39؛ بلاکِ event-loop بکاپ ~۴۶ms→۰٫۶ms). باقی‌مانده: ایندکسِ مسیرهای فیلترِ O(n) (وابسته به Wave 1) |
| 10 | Database Scale | Arena 1 + Arena 4 | 🟡 | High | Wave 1/2/3 و benchmark | شروع شد (دو نوبت): نوبتِ چت ۲ (PR #39) — read-replica routing (`queryRead` + `executePagedList`) + pool observability، تست 26/26. نوبتِ Arena (2026-09-11، `feat/db-scale-wave10`) — قراردادِ PgBouncer قفل (22/22)، **مهاجرتِ ۰۰۸ chg_id + ۱۴ ایندکسِ دلتا + سازندهٔ watermark + پریتیِ شکل** (31/31 + جهش 8/8)، طراحیِ نهاییِ پارتیشنِ grades/attendance با یافتهٔ مسدودکنندهٔ upsert (ON CONFLICT (id) روی جدولِ پارتیشن‌شده نمی‌تواند — بازنویسیِ persistOp پیش‌نیازِ فاز A است)؛ نوبتِ سومِ Arena (2026-09-11) — **اجرا و تأییدِ زنده بر PG 17.11: مهاجرتِ ۰۰۹ پارتیشن‌بندیِ grades/attendance (چهارفازی، 180k سطر، EXPLAIN pruning + وارون‌سازیِ کامل) + رفعِ مسدودکنندهٔ persistOp (env-flag، idempotency حفظ) + اتصالِ chg_id به کرسرِ v3 (فیدِ بدونِ clock-skew، سازگاریِ v1/v2، دو باگِ coercion فیکس)** — partitioning 42/42 · chg_id_cursor 33/33 · رگرسیون pull/cursor سبز؛ **نوبتِ چهارمِ Arena (2026-09-12) — اجرای ۰۰۹ در استیجینگ (جانشین: PG 17.11 محلی، ۱.۸M سطر، نویسندهٔ هم‌زمان): سه یافتهٔ هم‌زمانی (اسیرشدنِ نوشته‌ها در تک‌تراکنش؛ پرشِ تریگرِ chg حینِ کپی؛ بازنویسیِ معکوسِ پیشیکیتِ تساوی) هر سه فیکس (دو تراکنش + کچ‌آپ؛ DISABLE/ENABLE تریگر؛ شرطِ تازگی p.chg_id >= o.chg_id در ۶ ضدالحاق) — صفر خطا، توقفِ خواندن ~۲s؛ رصد: فیدِ chg ۱–۲ms؛ retention سالانهٔ ۵ ساله (ابزارِ fail-closed + تست 15/15 + cron)** — partitioning 55/55 · wave10-retention 15/15 · smoke 547؛ **ادامهٔ نوبتِ چهارم (مانورِ مقیاس ۲۵M): دو یافتهٔ سخت (قفلِ FK رویِ جدول‌هایِ والد تا پایانِ کپی — users پنج دقیقه بلاک؛ OOMِ کپیِ تک‌تراکنشه در ~۱۱M سطر) فیکس شد (FK با ALTER رویِ جدولِ خالی؛ کپیِ chunk-commitِ قابلِ ازسرگیری از MAX(id) + تستِ L11 قتل/ازسرگیری) — ۲۵M سطر با ترافیکِ هم‌زمان: ۱۹.۶ دقیقه، صفر خطا/گم‌شدگی، users حینِ کپی ۵.۲ms، پنجرهٔ swap ~۳۶s؛ فیدِ chg در ۲۵M سطر ۱ms** — partitioning ۶۱/۶۱؛ **پایانِ نوبتِ چهارم (۲۰۲۶-۰۹-۱۲): پنجرهٔ swap با سه رانِ تعقیبی از ~۳۶s به ۰.۱۱۵s/۱.۱۴s (بدترینِ خواندن/نوشتن) رسید — پیشیکیتِ chg باید ثابتِ زمانِ پلان باشد (`psql \gset` + لیترالِ `:w0`؛ زیرپلان ⇒ overestimate ⇒ Hash Anti-Joinِ کلِ جدولِ نو زیرِ قفل؛ `OR IS NULL` هم ایندکس را می‌کُشد) — رانِ پنجم: ۱۹ دقیقه، صفر خطا/گم‌شدگی، p95 ‏۵/۱۳ms، down ≈ ۲۰–۲۸s با گاردِ `*_recovered`** — partitioning ۶۲/۶۲؛ باقی: اجرا در تولیدِ واقعی (۵۰M+/۲۸۸M اجرا نشد — برون‌یابی مستند: ~۴۰ دقیقه/چند ساعت) · **(چت ۳ — 2026-09-12)** گیتِ `wave10-pg-live.js` ‏19/19 + جهش 5/5 رویِ PG 18 واقعی با streaming replica (قیدِ «replica/پارتیشن هرگز رویِ PG زنده» بسته) |
| 11 | Cache | Arena 4 + چت ۳ | 🟡 | Medium | Wave 3/6 و benchmark | سلسله‌مراتبِ L1/L2 (`server/cache.js`): TTL، single-flight، قفلِ NX ضدِ stampede، ابطالِ user/school + epochِ ضدِ خوانشِ کهنه (W11-2)؛ `tests/wave11-cache.js` ‏35/35؛ **قیدِ «فقط با fake اثبات شده» بسته شد — PR [#97](https://github.com/rezaa2544/p2/pull/97) مرج (2026-09-12 @ `83ad521`)**: C1..C5 رویِ Redis واقعی داخلِ همان گیتِ ‏16/16 (از جمله ۵۰ خوانندهٔ هم‌زمان ⇒ ۱ تولید)؛ سند: `docs/WAVE11_CACHE_STRATEGY.md`. باقی: بنچمارکِ hit-rate رویِ بارِ واقعی (استیجینگ) |
| 12 | Network / Edge | Arena 4 | ✅ | Medium | استقرار production و TLS/CDN/WAF | PR #43 مرج شد (463233c): بلاک‌های لبه nginx + اسناد CDN/WAF + ci/pending CodeQL + wave12 24/24 و جهش 5/5؛ رفعِ braceِ رگکس‌های map در nginx -t رانر؛ باقی‌مانده: اِعمال پچ CodeQL با توکن workflow |
| 13 | Security Program | Arena 2 + Arena 5 | 🟡 | Critical | Wave 5 و threat model | برنامهٔ امنیتی مستقر است: ممیزیِ ASVS (`docs/WAVE13_ASVS_AUDIT.md` + `tests/wave13-asvs-audit-coverage.js` ‏41/41)، کنترل‌هایِ زمانِ اجرا (`docs/RUNTIME_SECURITY.md`، `tests/wave13-security.js` ‏23/23)، ثبتِ یافته‌ها (`docs/SECURITY_FINDINGS_REGISTER.md`) و لاگِ رخداد (`docs/SECURITY_INCIDENT_LOG.md`)؛ secret-scan ‏11/11 و check-authz صفر-انحراف در هر PR. باقی: DAST زنده (`tools/dast-live.sh`) منوط به استیجینگ؛ تصمیمِ ناظر رویِ tenancy `school_id IS NULL` |
| 14 | Observability | Arena 4 + Arena 5 | 🟡 | High | Wave -1 و deployment model | فاز۱ tracing مرج (PR #22) + فاز۲ استقرارِ زنده: metrics.js (exporter بدونِ وابستگی، ۵۵/۵۵ config + ۳۰/۳۰ dashboards + جهش ۶/۶)، استک compose (Prometheus/Grafana/Alertmanager/Loki/Promtail/OTel/Jaeger)، ۷ قانونِ بحرانی + داشبوردها؛ **پی‌آر #۵۱ (رصدپذیری) مرج شد (2026-09-10، بستهٔ هفت‌گانه @ cd484c3)** — اجرایِ واقعیِ compose/تستِ webhook روی میزبان باقی |
| 15 | Health / Deployment | Arena 4 | 🟡 | High | Wave 14 و stateless API | شروع شد: **پی‌آر #۳۱ (شاخص سلامت مدرسه، بند ج.۱) مرج شد (2026-09-10 @ cd484c3)** — `GET /api/health-index` فقط-سوپرادمین، امتیاز ۰–۱۰۰ + رنگ + دلیل (`docs/HEALTH_INDEX_MODULE.md`)؛ استقرار/دیپلوی باقی |
| 16 | Disaster Recovery | Arena 4 + Arena 5 | 🟡 | Critical | Wave 10/15 و RPO/RTO | P0#3 زیرساخت-as-Code+Runbook آماده (infra/postgres HA، infra/redis sentinel، tools/pitr-*/failover-*، docs/DR_RUNBOOK.md با RPO/RTO مصوب RELIABILITY_DR_PLAN؛ تست 92/92 + 38/38 + جهش 7/7)؛ **پی‌آر #۴۶ (بستهٔ موج‌های ۱۴/۱۶/۱۷/۱۸) مرج شد (2026-09-10 @ cd484c3)** — اجرای مانور روی استیجینگ باقی |
| 17 | Testing Pyramid | Arena 5 | 🟡 | High | همه Waveها | شروع شد: **پی‌آر #۴۶ مرج شد (2026-09-10 @ cd484c3)** — سوئیت‌های موج‌های ۳/۶/۷/۱۸/۱۹ روی `main` (`tests/wave3-keyset.js`، `wave6-redis.js`، `wave7-offline-queue.js`، `wave18-load-test.js`، `wave18w19-multinode-live.js`، `wave19-chaos.js` + `tools/chaos-test.sh`/`dast-live.sh`)؛ **PR #77 (A11y Keyboard Navigation) در 2026-09-11 مرج شد (`2c7097d`)** و `a11y-keyboard` ‏92/92 + `a11y-regressions` ‏46/46 را افزود؛ **زیرساختِ نگهداریِ خودِ مستندات اضافه شد (2026-09-12، `tools/docs-stats-sync.js`):** شمارِ تست‌ها و اسناد دیگر دستی نوشته نمی‌شود — ابزار از دیسک می‌سازدشان و `--check` کهنه‌بودن را با exit 1 گزارش می‌کند؛ سه تستِ `docs-metrics`/`documentation-map-coverage`/`test-coverage-report-coverage` که با هر سند یا تستِ تازه قرمز می‌شدند، اکنون با یک دستور همگام می‌مانند؛ **بدهیِ مستندات جغجغه‌ای شد (2026-09-12، `tools/docs-refs-check.js`):** ارجاعِ سند به فایلِ ناموجود دیگر بی‌صدا نمی‌ماند — ۷۴ موردِ تاریخی در `tools/docs-refs-baseline.json` قفل شد و فقط ارجاعِ **تازه** گیت را قرمز می‌کند (`tests/docs-refs-check.js` ‏۱۹/۱۹)؛ **مهاجرتِ ۰۰۸ (موج ۲۳)** که ردیفِ §۸ راهنما و پینِ `docs-consistency-check.sh` را کهنه کرده بود رفع و آن پین‌ها از دیسک مشتق شد؛ سنجهٔ `a11y-regressions` از متنِ سورس به **رفتاری** تبدیل شد (۴۵/۱ → ۵۴/۵۴، پنج جهش کشته شد)؛ تکمیل هرم باقی |
| 18 | National Load Testing | Arena 5 + Arena 4 | 🟡 | High | Capacity Model (✅ `docs/CAPACITY_MODEL.md` — چت ۶)، Wave 3/10/14 | شروع شد: **پی‌آر #۴۶ مرج شد (2026-09-10 @ cd484c3)** — مولد دیتاست ملی `tools/generate-national-dataset.js` + `tests/wave18-load-test.js` ‏38/38 روی `main`. **اجرای واقعی روی شاخهٔ `feat/wave18-load-test` — [PR #94](https://github.com/rezaa2544/p2/pull/94) مرج شد (2026-09-12T14:29:49Z @ `c91c7888` — envهای `PAYESH_PG_HYDRATE_LIMIT/SKIP` روی `main` پیاده‌اند):** محیطِ production-like سندباکس — PG 17.11 با دیتاست مقیاس ۰٫۳ (۳M کاربر/۱۵M حضور/۶M نمره/۳۰k مدرسه؛ بارگذاری ۸m با `tools/w18-load-pg.sh`) + Redis 8 + k6 v2.2؛ هر ۵ سناریوی طرح اجرا شد (بار ۵۰۰/اوج ۲۰۰۰/فشار→۵۰۰۰/جهش ۵۰۰→۳۰۰۰/سوک ۱۰۰۰×۱۵m) — **صفر خطای نوشتن در هر ۵ سناریو (۱۹٬۰۴۲ sync اتمیک؛ سلامتِ PG پس از هر OOM بازشماری شد)**، سقفِ تک‌جعبه ~۶۳۰rps، مرگِ OOM (exit 137) در فشارِ پایدار ~۳×+ با ریشه‌یابی کمّی (**~۱MB حافظه به‌ازای هر درخواستِ هم‌زمان + snapshotِ O(collection) در مسیر sync + هیدراتاسیونِ کاملِ بوت** — سه یافتهٔ معماری با رفع/پیشنهاد)؛ رفعِ OOM بوت با envهای `PAYESH_PG_HYDRATE_LIMIT/SKIP` (پیش‌فرض دست‌نخورده)؛ سوئیتِ k6 ارتقا یافت (استخر ۴۰ کاربر واقعی، نوشتنِ حضورِ واقعی، SPIKE_BASE/TIME_SCALE/MAX_VUS/SUMMARY_FILE) + `tests/wave18-stress-test.js` ‏34/34 + `tests/wave18-spike-test.js` ‏22/22 + `docs/WAVE18_LOAD_TEST_REPORT.md`؛ گیت‌ها: smoke ‏547/547، authz 0، secret-scan ‏11/11، build ✓. ⚠️ باقی: تکرار روی استیجینگِ چندنمونه‌ایِ مجزا برای اعدادِ ۱۰M/۲۰k-rps (محدودهٔ صادقانهٔ این اجرا در §۶ گزارش)؛ واگرایی ابعاد مولد با §۲.۱ طرح بار (قلم ۲ `docs/DOCS_CONSISTENCY_REPORT.md`) · **(PR #91 — استیجینگ ۲ هسته/2GB؛ مرج شد 2026-09-13T01:21:42Z):** نقطهٔ شکست OOM در ~۶۰ rps؛ peak: ‏http_req_failed ‏19.07%، ‏p95 ‏147s، ‏OOM-kill ‏anon-rss≈911MB؛ ‏write_errors=0% در هر ۵ سناریو؛ دو سبز کاذب هارنس رفع (SCENARIO=peak خالی؛ spike ‏maxVUs<preAllocated) — گزارش: WAVE18_LOAD_TEST_REPORT.md |
| 19 | Chaos / Failure Testing | Arena 5 + Arena 4 | 🟡 | Critical | Wave 15/16 و Observability | PR #78 (WAL disk-full drill) در 2026-09-11 با merge commit `94d3ac2` مرج شد؛ `docs/WAVE19_WAL_DRILL_REPORT.md` و زیرساخت/هارنس افزوده شد؛ static gate ‏7/7 و mutation ‏2/2؛ **دنبالهٔ مانور** PR #81 (`5e2fbf5`) در 2026-09-12 مرج شد (مرج `9e7da2c`): گاردهای مسیرِ مخرب، کشفِ پویای migrationها و `die` روی migrationِ شکست‌خورده؛ و آلارمِ بیرونیِ پرشدگیِ WAL (`WALDiskHigh` ۷۰٪ / `WALDiskCritical` ۸۵٪) در `docs/OBSERVABILITY.md` مستند شد؛ اجرای زندهٔ مستقل در این sandbox به PostgreSQL 17 و root/tmpfs نیاز داشت و NOT-RUN ماند |
| 20 | چهار Arena + Arena پنجم | همه Arenaها | ⏳ | Medium | Addendum اعمال‌شده | Arena 5 ثبت شد؛ تقسیم مالکیت به‌روز شد |
| 23 | سیستم گزارش‌دهی پیشرفته | چت ۳ | ✅ | Medium | مدل‌های موجود grades/attendance/tuitions + زیرساخت CSV (۳۷) و چاپ A4 (۳۳) + صف آفلاین | **PR [#88](https://github.com/rezaa2544/p2/pull/88) مرج شد (2026-09-12 @ `0a45c13`؛ ⚠️ CI به‌دلیل انسداد بیلینگ گیت‌هاب اجرا نشد — ۳ تلاش/صفر step؛ به‌جای آن همهٔ گیت‌ها محلی روی head سبز شدند، سند: `docs/WAVE23_IMPLEMENTATION_REPORT.md`)**: چهار endpoint `/api/v1/reports/*` (`server/routes/reports.js`) با مهار اجاره‌ای fail-closed؛ مجموعهٔ همگام‌شوندهٔ `report_logs` (model+write-perms+EO_SCOPE_GATED+schema+migration 008±down)؛ صفحهٔ `reports` (`src/js/77-reports.js`) با ۴ تب و خروجی CSV/چاپ A4؛ ۳۵ تست تازه (`tests/reports-{basic,tenant-isolation,offline,export}.js` — شامل PDF واقعی با playwright)؛ سند `docs/REPORTING_SYSTEM_GUIDE.md`؛ گیت‌ها: run 35/35، smoke 547/547 |
| 24 | بهینه‌سازی عملکرد (Performance Optimization) | چت ۳ | ✅ | High | Wave 23 (✅) + baseline ثبت‌شده در `docs/WAVE24_PREP_REPORT.md` | **PR [#89](https://github.com/rezaa2544/p2/pull/89) مرج شد (2026-09-12 @ `7ee5237`؛ ⚠️ CI به‌دلیل انسداد بیلینگ اجرا نشد — همهٔ گیت‌ها محلی سبز؛ گیت: `node tests/performance/baseline.js --assert` → ۴/۴)**: `index.html` ‏2,257,641→**1,657,003B** (کوچک‌ساز محافظه‌کار `tools/minify-source.js`)؛ build ‏~100→**~60ms** (کش افزایشی + skip نوشتن بی‌تغییر)؛ پارس استور ~70→**~35-40ms** (قالب ASCII-escaped ‏`server/json-fast.js` + پارس Buffer)؛ p95 API درون‌پردازه **~5ms** (<150ms)؛ کلاینت: memoize ‏fa/jalali + کش queueBytes؛ ۵ فایل تست بریف در `tests/performance/{baseline,build-optimization,query-optimization,ui-optimization,offline-optimization}.js` ‏(۴+۲۶+۱۱+۲۰+۱۵ سبز)؛ آفلاین‌اول دست‌نخورده؛ سند: `docs/PERFORMANCE_OPTIMIZATION_GUIDE.md` + §۷.۱ `NATIONAL_ARCHITECTURE.md`؛ رگرسیون کامل سبز (run 35/35، smoke 547/547، a11y 92/0، reports 35، authz 37/37) |
| 21 | پشتیبانی از مدارس روستایی و چندپایه | چت ۳ | ✅ | Medium | مدلِ موجودِ has_multigrade/c.multigrade + لایهٔ پیش‌نویسِ حضور (دور ۴۲/۷۷) | **PR [#85](https://github.com/rezaa2544/p2/pull/85) مرج شد (2026-09-11 @ `d1a0bf2`، هر ۷ چک CI سبز)**: نمای ماتریسی `multigrade` (سطر=دانش‌آموز، ستون=پایه) برای حضور (حاضر/غایب/مرخصی + ثبتِ گروهیِ هر پایه با یک ضربه + فیلتر پایه + وضعیت فعلی) و نمرهٔ گروهی؛ Offline-First حفظ شد (پیش‌نویسِ Store + att-review→att-commit؛ نمره از insert→صف)؛ `src/js/76-multigrade.js` + `docs/MULTI_GRADE_GUIDE.md`؛ `tests/multi-grade.js` ‏34/34 + mutation ×۳؛ گیت‌ها: smoke ‏547/547، check-authz تطبیق کامل، secret-scan ‏11/11، a11y-keyboard ‏92/92، build --check سبز |
| 25 | لایه معنایی آموزشی (P0-EI-01) | Arena 5 + چت ۱ | ✅ | Critical | پیش‌نیاز کل فاز ۳ هوشمندی آموزشی (`docs/roadmaps/ROADMAP_V3_EDUCATIONAL_INTELLIGENCE.md`) | لایه معنایی تک‌منبعی (`server/analytics/semantic.js`) با استانداردسازی کسرها و مخرج‌ها در ۱۰ حوزه آموزشی (حضور تقویمی/خالص، غیبت مزمن، توزیع آماری نمرات، روند پیشرفت یادگیری، شاخص سلامت ۴بعدی با گارد No-Masking، رده تعامل، سختی/تمایز آزمون، تکمیل و خلاصه فعالیت)؛ ممیزی کامل هرس پارتیشن‌ها و گارد Fail-Closed ایزولاسیون؛ مستند جامع قرارداد داده (`docs/EDUCATIONAL_SEMANTIC_MODEL.md`)؛ ۱۴ سوئیت تست جامع لایه معنایی و تست‌های جهش (`tests/semantic-layer/runner.js` ‏14/14 سبز ۱۰۰٪). |
| 26 | خط زمانی طولی دانش‌آموز (P0-EI-02) | Arena 1 + چت ۱ | ✅ | High | لایه معنایی (Wave 25) + پارتیشن‌بندی نمرات/حضور | موتور تحلیلی چندساله (`server/analytics/timeline.js`) تجمیع‌کننده سوابق تاریخی (`student_archive`) و پارتیشن‌های زنده (`grades` و `attendance`) با کشف افت تحصیلی (`GROWTH_DIP`) و جهش رشدی (`GROWTH_SPURT`)؛ مجهز به اندپوینت RESTful امن `GET /api/v1/students/:id/timeline` در `server/routes/students.js` و `server/index.js` با گیت‌های IDOR و کنترل دسترسی PG-Authoritative؛ پوشش کامل با تست `tests/semantic-layer/longitudinal-timeline.test.js` درون گیت ۱4/۱4 لایه معنایی. |
| 27 | تحلیل‌های پایه سنجش و ارزشیابی (P0-EI-03) | Arena 5 + چت ۱ | ✅ | Critical | لایه معنایی (Wave 25) + Wave 26 + پارتیشن‌بندی نمرات | موتور سنجش و ارزشیابی (`server/analytics/assessment.js`) شامل سنجش پوشش ارزشیابی و استخراج غایبان سنجش، ارزیابی دیرکرد و به‌موقع بودن ثبت نمرات، کشف نمرات پرت با حصارهای توکی (Tukey's Fences)، محاسبه نمره ترکیبی کیفیت داده (DQS ۴بعدی)، گزارش تجمیعی ارزشیابی و سازنده کوئری با هرس پارتیشن‌ها؛ ارائه از طریق مسیر امن `GET /api/v1/grades/analytics`؛ پوشش کامل با سوئیت `tests/semantic-layer/assessment-analytics.test.js` و گیت ۱۵/۱۵ لایه معنایی (۱۰۰٪ سبز). |
| 28 | تحلیل‌های پایه حضور و غیاب (P0-EI-04) | Arena 5 + چت ۱ | ✅ | Critical | لایه معنایی (Wave 25) + پارتیشن‌بندی حضور | موتور تحلیل حضور و غیاب (`server/analytics/attendance.js`) با سنجش شاخص وقت‌شناسی و دقایق دیرکرد، کشف توالی غیبت‌های متوالی (هشدارهای ترک تحصیل زودهنگام)، تحلیل الگوهای هفتگی شنبه تا چهارشنبه، محاسبه نمره ترکیبی کیفیت داده حضور (DQS)، گزارش تجمیعی و کوئری‌ساز هرس پارتیشن؛ ارائه از مسیر امن `GET /api/v1/attendance/analytics`؛ پوشش با سوئیت `tests/semantic-layer/attendance-analytics.test.js` و گیت ۱۶/۱۶ لایه معنایی (۱۰۰٪ سبز). |
| 29 | داشبورد سلامت آموزشی مدرسه و مرکز اقدام روزانه (P0-EI-05) | Arena 5 + چت ۱ | ✅ | Critical | لایه معنایی (Wave 25) + تحلیل نمرات (Wave 27) + تحلیل حضور (Wave 28) | داشبورد تصمیم‌یار و اقدام‌محور مدرسه (`server/analytics/school-health-dashboard.js`) با ارزیابی نیمرخ چندبعدی سلامت، تضمین قاطع اصل No-Masking (آشکارسازی مسائل حاد حتی با میانگین‌های بالا)، تولید وظایف اولویت‌دار مرکز اقدام روزانه (Daily Action Center) همراه با پیشنهادات مداخله؛ ارائه از طریق مسیر امن `GET /api/v1/analytics/school-health` با گیت‌های سازمانی اداره و مدرسه؛ پوشش با سوئیت `tests/semantic-layer/school-health-dashboard.test.js` درون گیت ۱۷/۱۷ لایه معنایی (۱۰۰٪ سبز). |

## بندهای ادغام‌شده در بستهٔ هفت‌گانه (2026-09-10 @ cd484c3)

> هفت پی‌آر تیمی در `main` مرج شدند؛ بندهای زیر «موج» نیستند ولی در نقشهٔ ملی
> اثر دارند. مرجع جزئیات فنی: پیوست `docs/ROADMAP.md` + سند هر بند.

| بند | موضوع | پی‌آر | سند | وضعیت ادغام |
|---|---|---|---|---|
| ب.۳ | ارزشیابی ناشناس معلم | #۳۵ | پیوست `docs/ROADMAP.md` بخش آ | ✅ مرج |
| د.۲ | کارت امتیازی منطقه (۵ بعد + CSV) | #۳۵ | پیوست `docs/ROADMAP.md` بخش آ | ✅ مرج |
| د.۳ | اطلاعیه فوری/بحرانی اداره (`severity`+`office_id`) | #۳۵ | پیوست `docs/ROADMAP.md` بخش آ | ✅ مرج |
| د.۴ | نمای کمبود نیروی انسانی (جدول `staff_posts`) | #۳۵ | پیوست `docs/ROADMAP.md` بخش آ | ✅ مرج |
| ج.۱ | شاخص سلامت مدرسه (فقط-سوپرادمین) | #۳۱ | `docs/HEALTH_INDEX_MODULE.md` | ✅ مرج |
| ئ.۲ | کارآموزی هنرستان (روی جدول `internships`) | #۳۴ | `docs/INTERNSHIP_MODULE.md` | ✅ مرج |
| ئ.۳ | گیمیفیکیشن رفتاری دبستان (روی `discipline`+`dojo_types`) | #۳۳ | `docs/BEHAVIOR_GAMIFICATION.md` | ✅ مرج |
| ئ.۵ | اموال | #۳۲ | — (سندش در اسنپ‌شات محلی نیست؛ تأیید از ابلاغ تیمی) | ✅ مرج |
| ویو ۱۴/۱۶/۱۷/۱۸ | بستهٔ موج‌ها (رصد/بازیابی/هرم تست/بار ملی) | #۴۶ | ردیف‌های ۱۴–۱۸ جدول بالا | ✅ مرج |
| رصدپذیری | تکمیل کد رصد (مکمل ویو ۱۴) | #۵۱ | `docs/OBSERVABILITY.md` | ✅ مرج |

**قید صداقت:** شش پی‌آر از هفت‌گانه (همه جز #۳۱) پس از اسنپ‌شات محلیِ `origin/main`
مرج شده‌اند و کدشان در این کلون نیست؛ وضعیت‌های بالا از ابلاغ تیمی و آثار موجود در
اسنپ‌شات (اسناد بندها + پیوست نقشه) گرفته شده، نه از وارسی کدِ آن پی‌آرها.

## Arenaها

| Arena | مالکیت | وضعیت | توضیح | Evidence |
|---|---|---|---|---|
| Arena 1 — Database/Core | PostgreSQL، schema، migrations، queries، indexes، transactions، OCC | 🟡 | Wave 1 و 2 کامل؛ Wave 3/10 باقی | `docs/DATABASE_ARCHITECTURE.md` |
| Arena 2 — Security | Auth، RBAC، Tenant isolation، session، rate limiting، security tests | 🟡 | Wave 5 کامل (ویو ۵)؛ Wave 13 باقی | `docs/NATIONAL_ROADMAP_ARCHITECTURE_ADDENDUM.md` |
| Arena 3 — Sync/Offline | A01، Pull/Push، tombstone، cursor، offline queue، conflict resolution، IndexedDB | ⏳ | منتظر Wave 4/7 | `docs/ROADMAP.md` |
| Arena 4 — Performance/Infra | Redis، cache، workers، observability، load tests، deployment | ⏳ | منتظر Wave 6/8/9/14/15 | `docs/ROADMAP.md` |
| Arena 5 — QA / Reliability Engineering | Test Strategy، Regression، Load/Stress/Spike/Soak/Chaos، Recovery Validation، Release Gate | 🟡 | Wave 20 مرج شد (351bd10)؛ مالک quality gate همهٔ Waves (فعال) | `docs/ARENA5_QA_RELIABILITY.md` |

## راهنمای وضعیت

- ⏳ در انتظار شروع
- 🟡 در حال انجام
- ✅ کامل
- 🔴 مسدود/نیازمند تصمیم

## قرارداد به‌روزرسانی

پس از هر task مرتبط با یک Wave، همین فایل باید با وضعیت جدید، Owner، Risk، Dependency، Evidence، خلاصه اثر معماری/دیتابیس/امنیت/کارایی و لینک commit/PR به‌روز شود.
