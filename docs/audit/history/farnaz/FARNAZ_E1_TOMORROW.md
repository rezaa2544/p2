# گزارش E.1 فرناز — چک‌لیست «فردا چی لازم دارم» (چت ۳، آیتم ۱)

## چه ساخته شد (کلاینت‌فقط، بدون سرور)
- `tomorrowCard(sid,iso)` در `src/js/08-dashboard.js` + فراخوانی در `summaryBlock` (داشبورد خانواده).
- درس‌های فردا از همان برنامهٔ هفتگی (`addDaysISO(iso,1)` + `todayDow`)؛ وسیلهٔ خاص هر درس
  (ورزش→👕 لباس ورزش، هنر→🎨، آزمایشگاه→🥼)؛ بج 🔁 جابه‌جایی فردا؛ پیام «🎉 فردا کلاسی نیست» اگر خالی.
- تیک‌ها فقط حافظهٔ محلی (`Store`، کلید `payesh_tmr_<sid>_<iso>`) از طریق اکشن `tomorrow-check`
  با نقش‌های `student,parent` — بدون رندر مجدد.

## نتایج
| گیت | نتیجه |
|---|---|
| سئوت جدید `tests/tomorrow.js` | 15/15 ✅ |
| تست جهش `tests/tomorrow-mutations.js` | 5/5 کشته ✅ |
| `build --check` / `check-authz` | 0 / 0 ✅ |
| `tests/smoke.js` | 547/547 ✅ |

## سه نکتهٔ فنی (شفاف)
1. دیسپچر روی کلیک `preventDefault` می‌کند و تاگِلِ بومی چک‌باکس را لغو می‌کند — هندلر دستی تاگِل می‌زند.
2. jsdom (برخلاف مرورگر) `checked` را قبل و بعد از دیسپچ تاگِل می‌کند — اثبات‌شده با پروب ایزوله؛
   پس به‌جای کلیکِ نمایشی، گیتِ نقش (`canAction`) + فراخوانی مستقیم اکشن تست شد.
3. تست امنیتی «فقط لایهٔ داده» حتی واژهٔ localStorage در کامنت را رد می‌کند — یک رگرسیون اسموک
   (546/547) دقیقاً از همین بود؛ کامنت‌ها اصلاح شدند و کد از ابتدا فقط از `Store` استفاده می‌کرد.
