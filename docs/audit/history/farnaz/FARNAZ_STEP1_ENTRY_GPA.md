# گزارش قدم ۱ — S4: فیلد معدل ورودی (فاز فرناز)

**شاخه:** `feat/farnaz-phase1` · **تاریخ:** ۲۰۲۶-۰۹-۰۸
**حکم:** پیاده‌سازی کامل + تست + جهش — همه سبز ✅

## تغییرها
| فایل | تغییر |
|---|---|
| `authz/model.json` + `authz/write-perms.json` | `entry_gpa` به فیلدهای users (+۱ خط، بازتولید با ژنراتور) |
| `server/validate.js` | نگاشتِ `entry_gpa→score` (۰ تا ۲۰؛ null آزاد) |
| `src/js/18-modals.js` | فیلدِ `u_entry_gpa` در فرم کاربر |
| `src/js/19-actions-admin.js` | پارس/اعتبارسنجی (خالی=null، ۰–۲۰، ارقام فارسی) + ذخیره فقط برای دانش‌آموز |
| `src/js/17-student-record.js` | سطر «معدل ورودی» در کارت پرونده |
| `index.html` + `USER_GUIDE.html` | بازبیلد (مُهر راهنما هم تازه شد) |
| `tests/entry-gpa.js` (جدید) | ۱۹ ادعا: فرم، فارسی، ردِ ۳ مقدار، null، غیرِدانش‌آموز، نمایش، allowlist، قانون سرور، سینک زنده |
| `tests/entry-gpa-mutations.js` (جدید) | ۶ جهش (سقف، کف، NaN، گیت نقش، قانون سرور، null) |

## نتایج
- `tests/entry-gpa.js`: **۱۹/۱۹** ✅ (۴.۹ ثانیه)
- `tests/entry-gpa-mutations.js`: **۶/۶ کشته**، بیزلاین سبز، ۰ خطای محیطی ✅
- `build --check` exit=0 ✅ · `check-authz` exit=0 ✅ · `smoke` **۵۴۷/۵۴۷** ✅

## باگ‌های سئوت در مسیر (هر سه در تست بودند، نه محصول)
1. فیلد پاسخ سینک `results` است نه `res`.
2. کارت پرونده ارقام فارسی می‌زند (مقایسه با `fa(16.25)`).
3. سئوت روی موفقیت exit صریح نداشت و می‌ماند (الان `process.exit` + بستن jsdom/سرور).

**ثبت:** بخش S4 در `HANDOFF.md`.
**قدم بعد:** S5 (تمایز پانسیون از اقامت کامل).
