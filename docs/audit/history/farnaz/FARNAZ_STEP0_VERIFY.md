# گزارش قدم ۰ — راستی‌آزمایی S1–S3 (فاز فرناز)

**شاخه:** `feat/farnaz-phase1` از `4ca531c` · **تاریخ:** ۲۰۲۶-۰۹-۰۸
**حکم:** هر سه بند (قالب دوم کارنامه، گواهی اشتغال، گواهی انتقالی) روی main موجود، سیم‌کشی‌شده و سبز بودند — **بدون تغییر کد**.

| بند | شاهد کد | شاهد تست (روی همین شاخه) |
|---|---|---|
| S1 قالب دوم کارنامه | `reportCardCert` شاخهٔ `compact` (`33-forms-sms.js:128`) + سلکت `cert_tpl` (`17-student-record.js:86`) | `reporttpl2.js` ‏۷/۷ ✅ · `report2.js` ‏۱۰/۱۰ ✅ |
| S2 گواهی اشتغال | `enrollmentCert` (`33-forms-sms.js:331`) + اکشن `:1111` | `certify.js` ‏۸/۸ ✅ (C1) |
| S3 گواهی انتقالی | `transferCert` (`33-forms-sms.js:360`) + اکشن `:1120` | `certify.js` ‏۸/۸ ✅ (C2) |

**بیزلاین:** `build --check` exit=0 ✅ · `check-authz` exit=0 ✅ · `smoke` ‏۵۴۷/۵۴۷ ✅
**ثبت:** ۳ بخش در `HANDOFF.md` (فاز فرناز — قدم ۰).
**قدم بعد:** S4 (فیلد معدل ورودی) — پیاده‌سازی واقعی.
