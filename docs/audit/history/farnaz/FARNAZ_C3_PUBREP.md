# گزارش C.3 فرناز — گزارش عمومی قابل انتشار برای بیرون

## چه ساخته شد
- بلوک «📰 گزارش عمومی» در داشبورد مدیر با دو دکمه: چاپ رسمی و دانلود CSV.
- سه شاخص فقط‌تجمیعی: نرخ حضور (٪)، میانگین نمرات (از ۲۰)، تعداد رویدادها — بدون هیچ نام،
  برتر، مورد انضباطی یا دادهٔ فردی؛ صفحهٔ چاپ سلب «نسخهٔ عمومی — بدون دادهٔ حساس» دارد.
- دسترسی: دکمه فقط برای مدیر، اکشن با گیت نقش دولایه (هندلر + تابع چاپ)، دامنه فقط مدرسهٔ خود.
- کاملاً فقط‌خواندنی: بدون تغییر مدل، بدون تغییر سرور، بدون اکشن نوشتن.

## نتایج
| گیت | نتیجه |
|---|---|
| سئوت جدید `tests/pubrep.js` | 9/9 ✅ |
| تست جهش `tests/pubrep-mutations.js` | 6/6 کشته ✅ |
| رگرسیون meetingtype / boom / client-features | 8/8، 6/6، 12/12 ✅ |
| `build --check` / `check-authz` | 0 / 0 ✅ |
| `tests/smoke.js` | 547/547 ✅ |

## نکات فنی (شفاف)
1. دو جهش در دور اول زنده ماندند و هر دو ضعف هارنس بودند نه کد: M1 به‌خاطر دفاع عمقی واقعی
   (دو گیت) — هارنس به سنجش مجزای هر لایه ارتقا یافت (P6/P8، ششمین جهش M6)؛ M3 زیر گردکردن
   اعشار پنهان شد — آلودگی تست از ۱ به ۱۰۰ سطر رسید.
2. پوش به راه‌دور مسدود است (اعتبار گیت‌هاب در این سندباکس نیست) — هر سه کامیت C محلی‌اند و
   با وصل‌شدن اعتبار باید پوش شوند.
