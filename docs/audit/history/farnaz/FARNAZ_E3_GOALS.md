# گزارش E.3 فرناز — هدف‌گذاری شخصی نمره (چت ۳، آیتم ۳)

## چه ساخته شد (کلاینت‌فقط)
- **ثبت هدف:** ویرایشگر inline در کارت «روند نمرات» (وقتی یک درس انتخاب شده): ورودی عدد + دکمهٔ «ثبت هدف»
  (`data-act="goal-save"`)؛ ذخیره فقط در `Store` با کلید `payesh_goal_<دانش‌آموز>_<درس>` — بدون سرور.
- **اعتبارسنجی:** عدد ۰ تا ۲۰؛ ورودی نامعتبر/خالی/بیرون‌بازه با پیام خطا رد می‌شود (نه سکوت، نه clamp)؛
  ارقام فارسی پذیرفته می‌شوند.
- **خط مرجع:** تیک سبز `.goal-tick` در هر ستون نمودار (همان مختصات تیک میانگین کلاس) + لجند «🎯 هدف: X از ۲۰».
- **حریم:** دیدن و ویرایش فقط برای خود دانش‌آموز یا ولیِ لینک‌شده (`goalViewerOk` هم در رندر هم در اکشن؛
  مدیر/دبیر/هم‌کلاسی هیچ اثری از هدف نمی‌بینند). اگر هدفی نباشد: «هنوز هدفی تعیین نشده».

## نتایج
| گیت | نتیجه |
|---|---|
| سئوت جدید `tests/goals.js` | 16/16 ✅ |
| تست جهش `tests/goals-mutations.js` | 5/5 کشته ✅ |
| رگرسیون E.1/E.2 | 15/15 و 16/16 ✅ |
| `build --check` / `check-authz` | 0 / 0 ✅ |
| `tests/smoke.js` | 547/547 ✅ |

## نکتهٔ فنی (شفاف)
- یک جهش (حذف خواندن هدف) در دور اول زنده ماند چون لنگر تست سست بود (عبارت «🎯 هدف» در لیبل
  ویرایشگر هم وجود دارد) — لنگر به فرم دقیق لجند تغییر کرد و هر ۵ جهش کشته شدند.
