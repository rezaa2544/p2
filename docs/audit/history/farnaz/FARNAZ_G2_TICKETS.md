# گزارش G.2 فرناز — تیکت پشتیبانی سبک (چت ۳، آیتم ۷)

## چه ساخته شد (نسخهٔ اول، ساده)
- جدول `support_tickets`: `school_id` (اجباری)، `title`، `description`، `priority` (کم/متوسط/بالا)،
  `status` (باز/در حال بررسی/بسته)، `created_at`، `updated_at`.
- **مدیر:** روت «🎫 پشتیبانی» — دیدن تیکت‌های مدرسهٔ خود + «➕ درخواست جدید» (عنوان اجباری، اولویت معتبر).
- **سوپرادمین:** روت «🎫 تیکت‌های پشتیبانی» — همهٔ مدارس + دکمه‌های تغییر وضعیت (با bump شدن `updated_at`).
- مجوز: مدل (`ins=[manager]`، `upd=[superadmin]`، `del=[]`) + `ACTION_ROLES` + گیت نقش در نما؛
  `write-perms.json` صرفاً با مولد رسمی بازتولید شد (۸۱ مجموعه).

## نتایج
| گیت | نتیجه |
|---|---|
| سئوت جدید `tests/tickets.js` | 20/20 ✅ |
| تست جهش `tests/tickets-mutations.js` | 5/5 کشته ✅ |
| رگرسیون E.1–E.6 و `client-features` | سبز / 12/12 ✅ |
| `build --check` / `check-authz` | 0 / 0 ✅ |
| `tests/smoke.js` | 547/547 ✅ |

## نکات فنی (شفاف)
1. **رگرسیون واقعی اسموک:** تست «محتوای منوی هر نقش» فهرست را پین کرده بود و با افزودن روت،
   ۵۴۶/۵۴۷ شد — `NAV_EXPECT` برای مدیر و سوپرادمین به‌روزرسانیِ مشروع شد (نه ماسک: همان تست
   حالا رندرشدن روت جدید با همان نقش‌ها را هم نگهبانی می‌کند).
2. **دو شکار جهش:** لنگر T9b سست بود (چون `updated_at` بدو تولد هم امروز بود — کهنه‌سازی اضافه شد)؛
   T10 بدون `try/catch` کرشِ اکشن را به‌جای FAIL به FATAL تبدیل می‌کرد و verdict را ماسک می‌کرد.
3. **دیتای قدیمی امن است:** بوت همیشه شکل تازهٔ `db` را می‌سازد و لاگ را replay می‌کند، پس نبودِ
   آرایهٔ جدید در اسنپ‌شات‌های قدیمی خطا نمی‌دهد (و `applySnapshot` هم گارد دارد).
