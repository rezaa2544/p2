# گزارش E.4 فرناز — خروجی تقویم «دانلود همه» (چت ۳، آیتم ۴)

## چه ساخته شد (کاملاً کلاینت، بدون سرور و بدون وابستگی)
- دکمهٔ «📲 افزودن به تقویم گوشی (دانلود همه)» در صفحهٔ تقویم آموزشی (`viewCalendar`) +
  اکشن `ics-download`: امتحانات پیش‌رو + رویدادهای مدرسه در یک فایل `.ics` استاندارد (RFC 5545).
- فرمت: CRLF، تاشدن ۷۵ اکتتی (بدون شکستن کاراکتر چندبایتی)، UID پایدار (ایمپورت مجدد = به‌روزرسانی)،
  امتحان ساعت‌دار با DTEND از روی duration، رویداد تقویم به‌صورت تمام‌روز، بدون BOM.
- نام فایل: `payesh_calendar_<school>_<date>.ics`.
- دامنهٔ داده (کمینه‌سازی): دانش‌آموز→کلاس خود، ولی→کلاس فرزندان، دبیر/مدیر/سوپرادمین→مدرسه،
  سایر نقش‌ها→فقط همان رویدادهای قابل‌مشاهده در صفحه (بدون امتحانات).

## تصمیم‌های معماری (شفاف)
1. **عدم موازی‌کاری:** خروجی ICS تک‌دانش‌آموز از قبل وجود داشت (`ics-export` با نگهبان IDOR دور ۸۹) —
   `icsEsc` موجود reuse شد و فقط دامنهٔ نقش + دانلود همه‌گاهی اضافه شد (سئوت قدیمی 12/12 سبز ماند).
2. **مجوز بدون هاردکد نقش:** ورودی ACTION_ROLES ندارد (آن جدول معنای WRITE دارد و ابزار ۴ ناهماهنگی
   گزارش کرد)؛ گیت داخل اکشن با `canRoute('calendar')` است که تک‌منبع جدول روت است.
3. `authz/write-perms.json` صرفاً با **مولد رسمی** بازتولید و کامیت شد (ورودی `roles:null`، مطابق الگوی
   ۵۴ اکشن بی‌برچسب دیگر) — بدون دست‌کاری دستی.

## نتایج
| گیت | نتیجه |
|---|---|
| سئوت جدید `tests/ics.js` (شامل مسیر واقعی Blob) | 22/22 ✅ |
| تست جهش `tests/ics-mutations.js` | 5/5 کشته ✅ |
| رگرسیون E.1/E.2/E.3 و `client-features` قدیمی | سبز / 12/12 ✅ |
| `build --check` / `check-authz` | 0 / 0 ✅ |
| `tests/smoke.js` | 547/547 ✅ |
