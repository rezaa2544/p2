# دور ۹۰ — بستنِ باقی‌ماندهٔ ۲ و ۳ِ گزارشِ دور ۸۹

**تاریخ:** ۲۰۲۶-۰۹-۰۸ · **کامیت:** `492585d` روی main (پوش‌شده)
**گیت نهایی:** رجیسیون **125/125** (471s) · server12 **43/43** · smoke 546/546 · check-authz 0 · build سبز

## ۱) ویرایشِ اعلانِ خود = فقط پرچمِ read (باقی‌ماندهٔ ۲ِ R89)

در R89، `upd` روی `rec.user_id===u.id` برایِ همهٔ نقش‌ها همهٔ فیلدها را می‌گذراند. حالا:

- فقط **والد/دانش‌آموز** (نقش‌هایی که هیچ مسیرِ دیگری نداشتند) مشمول‌اند:
  `upd` اعلانِ خود فقط با داده‌ای که **تمام** کلیدهایش `read` باشد؛ هر فیلدِ دیگر
  (title/body/…) → `403 out_of_scope`.
- **مدیر/دبیر/مشاور/edu_office رفتارِ قبلی را بدونِ تغییر نگه داشتند** (مسیرِ سطحِ مدرسه) —
  قراردادِ موجود دست‌نخورده.
- تست: `S27` (upd با `title`ِ جعلی → 403) + `S27b` (عنوان روی disk دست‌نخورده) — server12 **43/43**.
- جهش: `M4` (حذفِ محدودیت) → S27 قرمز (ذخیرهٔ جعلی apply می‌شد) ✅ کشته شد.

## ۲) سخت‌سازیِ sim_full3-mutations (باقی‌ماندهٔ ۳ِ R89)

سوراخِ این سوئیت **برعکس** 15 سوئیتِ دیگر بود: کشتِ محیطیِ بدونِ خروجی (SIGKILL) در
`e.status || 1` تبدیل به «exit 1» و از آن **«جهشِ کشته شد»** می‌شد — **فولز-پوزیتیو**
(جهشِ واقعیِ زنده می‌توانست پنهان بماند چون شمارِ killed به 3 می‌رسید).

رفع:
- `runSim`: خروجیِ خالی → **retry یک‌باره**؛
- حلقه: خروجیِ خالیِ پایانی → `envFails` + مارکرِ صریح «خطا: فرآیند بدون خروجی کشته شد
  (محیط) — محسوب شد» — **نه** کشته/زنده؛
- خروجِ 0 فقط با: `killed===3 && backGreen && envFails===0`.
- اثبات: اجرایِ تکی بعد از سخت‌سازی → 3/3 کشته + سبزِ پایانی ✅.

## وضعیتِ باقی‌مانده (همهٔ گزارش‌هایِ R88/R89/R90)

| مورد | وضعیت |
|---|---|
| فیلترِ school در ICS | بسته (R88b — ادعای نادرست بود) |
| شکافِ اعلان/چت/تکلیف/نشان/ردِ فرزند | بسته (R89) |
| ویرایشِ اعلانِ خود | **بسته (R90)** |
| سخت‌سازیِ sim_full3-mutations | **بسته (R90)** |
| opِ ردشدهٔ demo در ردیفِ همگام‌سازی | آگاهانه (بی‌ضرر؛ پنهانشان انتخابِ طراحیِ R88 بود) |
| **پرداختِ خودِ والد** | **منتظرِ تصمیمِ محصول** — پرداختِ کلاینتی شبیه‌سازی‌شده است (`ref='SUB-'+ts`) و `parent_subscriptions`/`subscription_payments` در امتیازاتِ والد نیستند. مسیرِ پایلوتِ سالم = ثبتِ پرداخت توسطِ **مدیر**. دروازهٔ واقعیِ پرداخت / تصمیمِ «آیا پرداختِ والد باید persist شود؟» خارج از دامنهٔ فنیِ این سشن است |
