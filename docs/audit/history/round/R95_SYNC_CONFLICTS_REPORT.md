# گزارشِ دورِ ۹۵ — بند ۲.۵: حلِ تعارض در همگام‌سازی (base_version + حفظ + داوری)

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08)
**وضعیت:** کامل — کامیت‌های `465e9d3` + `b49210e` (داک) + `b862414` (بازنومری ۶۷→۶۸) + `3962247` (مِرج با origin/main)
**رجیسیونِ پایانی (درختِ نهاییِ مِرج‌شده + سخت‌سازی): 128/128 سبز، 0 قرمز (968s)** — شاملِ server15 (40 چک) · server15-mutations (6 جهش) · theme-picker · certify · finance2 (نشت‌هایِ DOM-id دور ۸۹). smoke 547/547 · check-authz 6/6 · صفرِ سرورِ نشتی.

**رجیسیون (پیش از مِرج):** **126/127 سبز (982s)** — سوئیت‌هایِ R95 در رجیسیون سبز: server15 (26s) · server15-mutations (283s). قرمزِ تنها: `server14-gc-mutations` (فازِ پایانیِ baseline درونِ سوئیت؛ خودِ جهش‌ها 4/4 کشته بودند) — **خطایِ محیطی**: پنجرهٔ ۳ ثانیهٔ ثابتِ sleep تحتِ بارِ موازی (دوِ لِین + لِینِ پورت) کوتاه‌تر از یک دورِ persist+GC می‌شد؛ تکرارِ تکی **کاملاً سبز**. همین خطا در رجیسیونِ درختِ مِرج‌شده تکرار شد، پس **سخت‌سازی شد** (`007ce34`): sleepِ ثابت با پُلِ ۱۵ ثانیه‌ایِ خودکار جایگزین — اگر GC مرده باشد (جهش M4 / باگ) پُل وقتش را می‌گیرد و چک‌ها همان‌طور که باید شکست می‌خورند. رجیسیونِ نهایی با این تغییر: **128/128 سبز**.

**🔀 مِرج با origin/main (PR#3):** در حینِ این دور، PR#3 (رفعِ نشت‌هایِ DOM-id
دور ۸۹ + انتخابگرِ پوستهٔ دور ۹۰) به main merge شده بود و ماژولِ
`67-theme-picker.js` نومریِ ۶۷ را گرفته بود. ماژولِ تازهٔ این دور به
**`68-sync-conflicts.js`** بازنومری شد و مِرج بدونِ برخوردِ منطقی انجام
شد (کدِ هر دو طرف ماند: نگهبانِ `certAllowedStudent` + سه نشتِ دیگرِ
دور ۸۹ و انتخابگرِ پوسته). رجیسیونِ کاملِ درختِ مِرج‌شده پس از آن اجرا شد (127/128 — قرمزِ تنها
همان server14-gc-mutations).

## مسئله

سیاستِ بند ۲.۵ از قبل **تصمیم گرفته** بود ولی پیاده نبود: وقتی دو کاربر
هم‌زمان یک رکوردِ حساس (نمره/حضور/انضباطی) را عوض کنند، بدونِ نسخه‌گذاری
«آخرین نوشتن» بی‌سروصدا برنده می‌شد و دادهٔ دیگری از بین می‌رفت. این دور
مکانیسمِ کامل را ساخت: **نسخه‌گذاری → حفظِ تعارض → داوریِ انسانی**.

## انجام شد

### سرور — `server/sync.js`
سیاستِ سه‌گانهٔ بر پایهٔ نوعِ داده (ثابت‌های `VERSIONED` / `STRUCTURAL` /
`VERSION_TRACKED`):

| نوع | مجموعه‌ها | رفتارِ عملیاتِ کهنه (base_version ≠ نسخهٔ سرور) |
|---|---|---|
| حفظِ تعارض | grades · attendance · discipline | اعمال **نمی‌شود**؛ در `sync_conflicts` با هر دو نسخه محفوظ + اعلان به مدیر |
| سرورِ مرجع | schools · classes · subjects · users · enrollments · schedule | رد با `stale_base` |
| آخرین نوشتن | بقیه (اعلان/یادداشت/پیام/…) | `base_version` بی‌اثر — بر پایهٔ **زمان دریافت سرور** |

- بررسیِ `base_version` در حلقهٔ عملیات، **پس از** idempotency و **پیش از**
  clock-skew؛ عملیاتِ بدونِ `base_version` (رایهٔ قدیمی) همچنان اعمال
  می‌شود — سازگاریِ کامل.
- نگه‌داشتنِ نسخه: `ins` → `version: 1`؛ `upd` → `version + 1`.
  رکوردهایِ seed بدونِ فیلدِ `version` به‌عنوانِ نسخهٔ ۱ (`||1`) تلقی
  می‌شوند — **بدونِ مهاجرت**.
- ثبتِ تعارض: رکوردِ `sync_conflicts` با `collection/record_id/school_id/
  base_version/server_version/server_state (رونوشت)/incoming {data,by,at,
  op_uid}/status:'open'` + ردِّ پایِ `sync_conflict_preserved` + اعلانِ
  «⚠️ تعارض همگام‌سازی» به مدیرِ همان مدرسه (الگویِ hookهایِ R88/R89).

### سرور — `server/conflicts.js` (ماژولِ تازه)
- `GET /api/sync/conflicts` — فهرست: مدیر فقط مدرسهٔ خود، سوپرادمین همه؛
  بازها اول، تازه‌ها اول، حداکثر ۵۰. (کلاینت دادهٔ سرور را نمی‌کشد، پس
  این endpoint تنها مسیرِ خواندنِ تعارض‌هاست.)
- `POST /api/sync/resolve-conflict` `{conflict_id, winner, reason?}` —
  داوریِ اتمیک:
  - `incoming` → دادهٔ کلاینت روی رکورد اعمال + نسخه +۱
  - `server` → رکورد دست‌نخورده
  - در هر دو: `status:'resolved'` + `resolved_by` + `resolved_at` +
    ردِّ پایِ `conflict_resolved`؛ داوریِ دوباره → `409 already_resolved`
- مجوز: فقط manager/superadmin؛ مدیر فقط تعارضِ مدرسهٔ خود
  (`403 out_of_scope`)؛ دبیر/والد → `403 role_denied`؛ بدونِ نشست → 401.
  (مجوز را کلاینت هم دارد — دفاعِ چندلایه.)

### کلاینت
| فایل | تغییر |
|---|---|
| `03-persistence.js` | applyOp: `ins` → `version:1` · `upd` → `op.base_version = it.version||1` **پیش از** اعمال + `it.version++` (همان مجموعهٔ سرور) |
| `27-sync.js` | `conflict_preserved` → وضعیتِ `conflict` (بدونِ دوباره‌ارسال؛ در پنلِ همگام‌سازی «تعارض») · `stale_base` → `rejected` · هر دو در `SYNC_DEAD_CODES` + شمارشِ «N تعارض» در toast |
| `08-dashboard.js` | کارتِ «⚖️ تعارض‌های همگام‌سازی» برایِ مدیر/سوپرادمین — فقط حالتِ سروری |
| `67-sync-conflicts.js` (تازه) | پُلِ ۳۰ ثانیه‌ای به endpoint؛ نمایشِ روبه‌رویِ همِ «وضعیتِ فعلیِ سرور» و «تغییری که همگام نشد» + دکمه‌های «پذیرشِ تغییرِ کلاینت» / «نگه‌داشتنِ وضعیتِ سرور» + بخشِ داوری‌هایِ اخیر |
| `19-actions-core.js` · `30-authz.js` | اکشنِ `conflict-resolve` + مجوز `['manager','superadmin']` |

## آزمون

**`tests/server15.js` — 40 چک، 40 سبز** (سرورِ واقعی، پورت‌هایِ 9001/9002):

| گروه | چک‌ها |
|---|---|
| نسخه‌گذاری | ins→1 · updِ یکسان→+1 · بدون base_version→اعمال (سازگاری) · حضور همان سیاست · ساختار (۱)→اعمال+نسخه |
| حفظِ تعارض | upd کهنه→`conflict_preserved` + `conflict_id` · رکورد دست‌نخورده · `sync_conflicts` روی disk (هر دو نسخه) · اعلان به مدیر · مدرسهٔ ۲ تعارضِ جدا |
| LWW | announcements با base_version کهنه → **همچنان اعمال** |
| ساختار | کهنه → `stale_base` + دست‌نخورده |
| فهرست/دامنه | مدیر ۱ ≠ مدیر ۲ ≠ سوپرادمین · دبیر 403 · بدونِ نشست 401 |
| داوری | بین‌مدرسه‌ای 403 · دبیر 403 · incoming→اعمال+نسخه · دوباره 409 · server→دست‌نخورده · reason ثبت |
| ردِّ پای | audit: `sync_conflict_preserved` + `conflict_resolved` |

**`tests/server15-mutations.js` — 6/6 جهش کشته شد:**

| جهش | چه چیزی کشت |
|---|---|
| M1 نگارشِ sync_conflicts حذف | C3b |
| M2 چرخشِ نسخه حذف | C2b |
| M3 داوری incoming اعمال نکند | C15b |
| M4 گاردِ نقش حذف | C14 |
| M5 دامنهِٔ مدرسه حذف | C13 |
| M6 base_version روی LWW الزام | C6b |

**smoke.js** — چکِ تازهٔ لایهٔ محلی (insert→1 · update→2 + `base_version`
در عملیاتِ صف · LWW بدونِ base_version · وجودِ ماژولِ 67) → 547/547.
**check-authz** — 6/6 (شاملِ خودآزمونِ انحراف).

## یافته

- **دامِ تست:** نگاشتِ wire در `syncOps` اول `base_version` را رها می‌کرد
  (op روی سیم بدونِ فیلد می‌رفت → همهٔ «کهنه‌ها» اعمال می‌شدند). آزمون حالا
  فیلد را صریح می‌کَنَد و هر claimِ «دست‌نخورده ماند» را **پس ازِ flushِ
  تضمین‌شده** (وجودِ رکوردِ تعارض روی disk) سنجش می‌کند، نه پیش از آن.
- **هم‌ترازیِ fallback:** کلاینت و سرور هر دو رکوردِ بدونِ نسخه را ۱
  (`||1`) می‌دانند — به‌همین‌دلیل base_versionِ صحیحِ رکوردهایِ seed = ۱
  (نه ۰).
- **معماری:** کلاینت دادهٔ سرور را pull نمی‌کند (آرکیتکتورِ آفلاینِ اول
  با آینهٔ محلی)؛ برایِ همین فهرستِ تعارض‌ها از endpointِ اختصاصی می‌آید
  و داوری **عملیاتِ همگام‌سازی نیست** (POST جدا، اتمیک).

## باقی‌مانده (بدونِ تغییر)

1. **تصمیمِ محصول:** دکمهٔ «پرداخت اشتراک» ولی در حالتِ سرور — تنها موردِ
   بازِ کلِ پروژه (پیشنهاد: پنهان + «ثبت از طریق مدیر»).
2. بک‌لاگِ پسِ ۲.۵: بند ۲.۷ (throttle + تصمیمِ UUID پیش از نخستین
   استقرارِ واقعی) · ۲.۱(a) سقفِ نرخِ ورودِ اکسل · ۲.۱(b) قواعدِ عمیقِ
   کسب‌وکار سمتِ سرور · SameSite Lax→Strict.
3. همگام‌سازیِ خواندن (pull/bootstrap) همچنان جداست — برایِ تعارض‌ها
   باِ endpointِ تازه پوشش داده شد.
