# گزارشِ گامِ ۳ — نقصِ ۳: سایه + نقشِ plan-save شهریه (P1)

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08) · **برنچ:** `fix/behavior-findings`
**کامیتِ این گام:** (پایین، پس از ثبت)

---

## ۱. ریشهٔ دوگانهٔ نقص (هر دو تأییدشده)

1. **سایه در دیسپچ:** `'plan-save'` در A ‏(`19-actions-finance.js:26` —
   قیمت‌گذاریِ اشتراک) نسخهٔ F7 ‏(`20-communication-finance.js:735` — طرحِ
   شهریه) را می‌پوشاند (دیسپچ: A اول، بعد F7). کلیکِ «ذخیرهٔ طرحِ شهریه»
   به گردانندهٔ اشتراک می‌رفت و طرح **هرگز** ذخیره نمی‌شد.
2. **کلیدِ تکراری در نقش‌ها:** `'plan-save'` دو بار در `ACTION_ROLES`
   بود (`30-authz.js:309` زیرِ «مالی» ‏[manager,superadmin] و `:332` زیرِ
   «پنل سوپرادمین» ‏[superadmin]) و در آبجکت‌لیترالِ جاوااسکریپت دومی می‌برد
   ⇒ مدیر عملاً اجازه نداشت (و مولدِ مجوز هم `tuition_plans` را به
   `plan-save`ِ سوپرادمین‌فقط نسبت می‌داد).

## ۲. رفع (۳ ویرایش + بازتولیدِ مجوز)

- `20-communication-finance.js:735`: گردانندهٔ F7 ← `'tuition-plan-save'`.
- `20-communication-finance.js:804` (`planModal`): دکمهٔ ذخیره ← نامِ تازه.
- `30-authz.js:309`: کلیدِ «مالی» ← `'tuition-plan-save'` (نقش‌ها بی‌تغییر:
  ‏[manager,superadmin])؛ ورودیِ `:332` (اشتراک، سوپرادمین‌فقط) دست‌نخورده.
- `authz/write-perms.json` بازتولید شد (**۱۰+/۱−**): ورودیِ تازهٔ
  `tuition-plan-save ← ‏[manager,superadmin] × tuition_plans` و اصلاحِ
  نگاشتِ `plan-save` از `tuition_plans` به `app_settings` (نسبتِ درستِ
  نسخهٔ اشتراک که زیرِ سایهٔ نام گم بود). اکشن‌ها ۳۳۷ ← **۳۳۸**،
  نویسندگان ۱۶۹ ← **۱۷۰**، بی‌برچسب‌ها ۵۴ (بی‌تغییر).

## ۳. آزمون‌ها

- **تازه:** `tests/tuition-plan.js` — T1 رجیستری‌ها (F7 تازه را دارد/کهنه را
  نه، A برعکس، دکمهٔ مودال به نامِ تازه) · T2 نقش‌ها (۱۰ حالتِ جدول + ۲
  کنترل با S.user واقعی) · T3 سرتاسریِ مدیر با **کلیکِ واقعی** (ساخت +
  ویرایشِ طرح، اشتراک دست‌نخورده) · T4 سرتاسریِ سوپرادمین با کلیکِ واقعی
  (ذخیرهٔ قیمت، شهریه دست‌نخورده). نتیجه: **17/17** ✅
- **جهش:** `tests/tuition-plan-mutations.js` — M1 برگشتِ نامِ گرداننده ←
  T1 · M2 برگشتِ دکمهٔ مودال ← T3 · M3 حذفِ مدیر ← T2 · M4 افزودنِ ناروایِ
  دبیر ← T2. نتیجه: **4/4 کشته، خطِ پایه سبز، ۰ خطایِ محیطی** ✅
- **ریشه‌یابیِ حینِ جهش‌مندی (باگِ سئوت، نه محصول — بسته شد):** نخست M1–M3
  به‌جایِ قرمزِ تمیز، FATAL می‌دادند (`created[0]` رویِ آرایهٔ خالی +
  `.click()` رویِ دکمهٔ ناموجود). سئوت دفاعی شد (شاخهٔ پیش‌نیاز + کمکِ
  `clickModalSubmit` که هر دکمهٔ ذخیره‌ای را کلیک می‌کند تا دیسپچِ اشتباهِ
  M2 هم اجرا و قرمز شود). قانونِ «کرش = env-fail» دست‌نخورده ماند و درست
  کار کرد (کرشِ سئوت را پنهان نکرد — ما را به ریشه رساند).

## ۴. دروازهٔ این گام

- [x] `node build.js --check` سبز (تطبیقِ کامل، ۱۷۰ نویسنده)
- [x] `node tests/smoke.js` سبز (**547/547** — شاملِ «پلان فقط سوپرادمین» بی‌تغییر)
- [x] سئوتِ تازه **17/17** + جهش‌ها **4/4**
- [x] دروازه‌هایِ دستوری: `tools/check-authz.js` ✅ (تطبیقِ کامل) ·
      `tests/authz-model.js` ‏**۲۳۰/۲۳۰** ✅
- [x] مرتبط‌ها: `finance2` ‏۲۰/۲۰ · `subs2` ‏۹/۹ · `run.js` ‏۳۴/۳۴

**نکتهٔ ثبت‌شده (معماریِ موجود، بدونِ اقدام):** دیسپچِ کلاینت نقش را
اجرا نمی‌کند (دکمه‌ها در رندر با canAction گیت می‌شوند)؛ اجرایِ واقعی
سمتِ سرور است (fail-closed). تستِ M3 همین را نشان داد (کلیکِ مستقیم
بدونِ نقش هم می‌رود) — رفتارِ طراحی‌شده است، نه باگِ این گام.

**فایل‌هایِ این کامیت:** `20-communication-finance.js` +
`30-authz.js` + `authz/write-perms.json` + ‏۲ سئوتِ تازه +
`index.html`/`USER_GUIDE.html` بازسازی‌شده + این گزارش.

**گامِ بعد:** گامِ ۴ — نقصِ ۴ (P1، سرریزِ چیدمان).
