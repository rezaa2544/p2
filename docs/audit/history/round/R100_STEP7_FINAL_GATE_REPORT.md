# گزارشِ گامِ ۷ — دروازهٔ نهایی (رگرسیونِ کامل + مستندات)

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08) · **برنچ:** `fix/behavior-findings`
**کامیتِ این گام:** (پایین، پس از ثبت — پیامِ دستوریِ R100)

---

## ۱. رگرسیونِ کامل: ۱۴۴ سبز + ۴ قرمز (۱۴۲۳ ثانیه)

`bash scripts/run-all-tests.sh` روی درختِ نهاییِ هر ۶ گام: **۱۴۴ سوئیت
سبز، ۴ قرمز**. هر ۴ ریشه‌یابی شدند؛ هر ۴ با اصلاحِ **فقط-تست** سبز
شدند (هیچ رفتارِ محصولی برایِ سبزکردن دست‌کاری نشد — `git diff` فقط
‏`tests/` + مستندات را نشان می‌دهد).

### قرمزِ ۱ — `simulation` (۴۷/۴۸؛ IDORِ ولیِ چندفرزند)

- **ریشه:** گامِ ۶ آن را آشکار کرد، نه ایجاد. نمایِ فرزندان چیپ‌هایِ
  ‏`trend-sub` دارد که `data-id`شان **شناسهٔ درس** است، نه دانش‌آموز؛ با
  آمدنِ نمراتِ سال‌گذشته چیپ‌ها (۹ ← ۱۳) بیشتر شدند و شناسهٔ عددیِ
  درس با شناسهٔ دانش‌آموزِ ولیِ دیگر تصادف کرد ⇒ پیمایشِ «نشت» مثبتِ
  کاذب داد. **نشتِ واقعی نیست** (اثبات: همان بوت، با حذفِ درون‌صفحه‌ایِ
  نمراتِ سال‌گذشته ⇒ چیپ‌ها ۹ و `foreign=[]`).
- **رفع (فقط سئوت):** چیپ‌هایِ `trend-sub` پیش از پیمایش از HTML حذف
  می‌شوند + کامنتِ توضیحی. (یک «Invalid regular expression flags»ِ
  میانی هم بود: `\/` در تمپلیت‌لیترال باید `\\/` باشد — اصلاح شد.)
- **پس از رفع:** **48/48** ✅

### قرمزِ ۲ — `libserial2-mutations` (M2: «نماد پیدا نشد»)

- **ریشه:** پیامدِ دستوریِ گامِ ۱ — الگویِ M2 به کدِ قدیمیِ
  ‏`add('lib_books', rec)` اشاره می‌کرد که به `insert` تغییر کرده بود.
- **رفع (فقط سئوت):** الگو `add` ← ‏`insert`؛ نیّتِ جهش (حذفِ `serial`
  از رکورد) بی‌تغییر. **پس از رفع:** **4/4** ✅

### قرمزِ ۳ — `dropout-mutations` (۴/۶؛ M5/M6 با EISDIR)

- **ریشه:** قطعی و **پیش‌موجود** (از فاز ۳): هارنس `server/*` را با
  ‏`copyFileSync`ِ تخت کپی می‌کند ولی `server/middleware/` و
  ‏`server/routes/` دایرکتوری‌اند ⇒ هر دو جهشِ سروری هرگز واقعاً اجرا
  نمی‌شدند (حفرهٔ پوششی، نه «زنده‌ماندنِ» واقعی).
- **رفع (فقط هارنس):** کپیِ بازگشتیِ `copyTree` (با حفظِ ردِ `data/`).
  **پس از رفع:** **6/6** ✅ — هر دو جهش حالا واقعاً اجرا و کشته می‌شوند.

### قرمزِ ۴ — `server-mutations` (۱۹/۲۰؛ M16: «الگو پیدا نشد»)

- **ریشه:** **پیش‌موجود** (بایت‌به‌بایت مشابه مبنا — این دور `server/`
  را لمس نکرد): کامیتِ سخت‌سازیِ حسابرسی شکلِ گارد را از تک‌خطی به
  آکولادی + لاگِ `audit` تغییر داده بود و الگویِ M16 کهنه شده بود.
- **رفع (فقط سئوت):** الگو به `if(s.role!=='superadmin'){` ← ‏`if(false){`؛
  نیّت (خاموش‌کردنِ گارد) بی‌تغییر. **پس از رفع:** **20/20** ✅

## ۲. حسابِ نهاییِ رگرسیون: **۱۴۸/۱۴۸**

درختِ محصول از اجرایِ کامل تا امروز **بایت‌به‌بایت بی‌تغییر** است
(اصلاح‌ها فقط در `tests/`) ⇒ هر ۱۴۴ سبزِ قبلی معتبر می‌ماند و ۴ سوئیتِ
اصلاح‌شده تکی تا سبز بازاجرا شدند. هیچ قرمزی پنهان/رد نشده.

## ۳. سه‌گانهٔ تأیید

- [x] `node build.js --check` سبز (تطبیقِ کاملِ authz)
- [x] `node --expose-gc tests/smoke.js` سبز (**547/547**)
- [x] `node tools/check-authz.js` سبز (خروجِ ۰)

## ۴. مستنداتِ این گام

- `docs/AI_PROMPT.md`: §۰.۵.۲۱ (هر ۶ قاعدهٔ ماندگار + نگهبان‌ها).
- `USER_GUIDE.html`: کالآوتِ «(دور ۱۰۰)» در بخشِ مدیر (نشانِ «بدون
  کلاس» + پشتیبانِ v3).
- `HANDOFF.md`: ورودیِ دورِ ۱۰۰ نهایی شد («کامل ✅»).

**فایل‌هایِ این کامیت:** ۴ اصلاحِ فقط-تست + ۳ مستندات + این گزارش +
‏`R100_FINAL_REPORT.md`. بدونِ هیچ تغییرِ محصول.
