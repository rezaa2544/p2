# گزارشِ دورِ ۹۲ — رفعِ زنجیرهٔ خطاهای زیرساختِ تست (forensics کامل)

**تاریخ:** ۱۸/۰۶/۱۴۵ (2026-09-08) — شب، ~03:00 به‌ساعتِ تهران
**وضعیت:** کامل — ۳ کامیت (`a8c7efd`, `ffa4b77`, `2353898`) — همه پوش‌شده
**رجیسیونِ پایانی:** **125/125 سبز، 0 قرمز، 663s** · smoke 546/546 · check-authz سبز · build سبز · **صفرِ سرورِ نشتی‌شده**

---

## ۱. خلاصهٔ زنجیره (چرا سه رجیسیونِ متوالی لازم شد)

| رجیسیون | نتیجه | قرمزها | ریشه |
|---|---|---|---|
| #1 (محیطِ بازیابی‌شده) | 124/125 | server-mutations (M18 «زنده ماند») | سرورهایِ زомبیِ پیشین روی 8994/8995 → مرگِ زودهنگامِ server9 قبل از A2 |
| #2 | 123/125 | homework2 (H2,H4) · summer2-mutations (M3) | **بمبِ زمانیِ UTC 23:00–23:59** + کلاسیِ مرگِ زودهنگام در summer2 |
| #3 | 122/125 | sim_full3 · sim_full3-mutations · reexam2-mutations | sweepِ وسطِ اجرا storeِ زندهٔ sim_full3 را پاک کرد → کرش → سرورِ نشتی → پترنِ pkill نادرست → آبشارِ 429/no_session |
| #4 (پایانی) | **125/125** | — | — |

هر رجیسیونِ قرمز، یک لایهٔ عمیق‌تر از همان مشکلِ «دروازه‌هایِ محیطیِ کاذب» را فاش کرد. هر سه لایه حالا بسته‌اند.

## ۲. یافته‌ها و رفع‌ها (شش مورد)

### ۲.۱ M18 — «زنده ماند»ِ کاذب در server-mutations (لایهٔ ۱)
- **چی شد:** سرورهایِ نشتی‌شدهٔ دو رجیسیونِ پیشین (PIDهایِ 11536/13816) پورت‌هایِ ثابتِ 8994/8995 را گرفته بودند. server9 (کشف‌کنندهٔ M18) روی A0a («سرور بالا آمد») می‌مرد، قبل از چاپِ A2 → منطقِ رأی `failed && out.includes('A2')` شکست → «زنده ماند»!
- **رفع (`a8c7efd`):** الگوی R92 در server-mutations (آخرین سوئیتِ ۱۶تاییِ بدونِ سخت‌سازی): مرگِ زودهنگام (❌ هست ولی چکِ موردِ انتظار هرگز چاپ نشده) → **retry یک‌بار → env-failِ صریح** («نه کشته و نه زنده شمرده شد») → خروجِ 0 فقط با `killed==20 && envFails==0`.
- **آزمایش:** محیطِ پاک: 20/20 exit 0 ✅ · شبیه‌سازیِ محیط (پورت‌ها مسدود با dummyِ پاسخ‌دهنده): M18 → مارکرِ محیطی، 19/20 + 1 env، **exit 1** ✅ (M14/security2 با دو-سرورهایی بودن، حتی با پورتِ مسدود کشته شد — مقاوم).

### ۲.۲ H2/H4 — بمبِ زمانیِ ساعتی (لایهٔ ۲، خطای واقعیِ تست)
- **چی شد:** H2/H4 بازه‌ها را از `new Date().toISOString()` می‌ساختند: «امروز 00:00 تا 23:00» و انتظار «باز» داشتند. در **UTC 23:00–23:59** (یعنی **02:30–03:29 به‌ساعتِ تهران** — دقیقاً همین حال!) `now > 23:00` → «after»/قبلِ‌بازِ معکوس → **هر شب** در آن ساعتِ یک‌ساعته قرمز می‌شد. رجیسیونِ R90 ساعتیِ پیش (قبل از 23:00 UTC) سبز بود.
- **رفع (`ffa4b77`):** H2 → زمانِ مرجعِ **ثابت** (`2026-06-15T12:00Z`) که به‌صورتِ صریح به `hwIsOpen(a, nowIso)` داده می‌شود (تابع از قبل `nowIso` می‌پذیرفت) — deterministic در هر ساعت. H4 → بازه‌های **نسبت به اکنون** (`now±1h`) — مستقل از ساعت.
- **آزمایش:** homework2 **5/5 درِ درونِ خودِ پنجرهٔ بمب** (UTC 23:35) اجرا شد ✅.

### ۲.۳ summer2-mutations — همان کلاسِ مرگِ زودهنگام (لایهٔ ۲)
- M3 (killed-check روی summer3/8994) تحتِ تداخلِ پورت: کشف‌کننده قبل از B1 مرد → «کشته نشد»ِ کاذب. **رفع:** همان الگوی R92 (retry + env-fail + تشخیصِ «تست کامل شد» از خطِ خلاصهٔ `بررسی —`).
- **آزمایش:** پاک: 5/5 ✅ · شبیه‌سازی (8994 مسدود): M1/M2 کشته + M3 → مارکرِ محیطی ✅.

### ۲.۴ ده سوئیتِ سبکِ قدیمی — طبقه‌بندیِ نادرستِ پورت (لایهٔ ۳، باگِ جدولي)
- **چی شد:** `uses_port` فقط فیلدهای `suite: 'tests/x.js'` را می‌خواند؛ ده سوئیتِ جهشِ سبکِ قدیمی، کشف‌کنندهٔ خود را با **آرگومانِ تابع** (`mutate(..., 'tests/reexam3.js', ...)`) راه می‌اندازند → تشخیص نمی‌شدند → در **لانِ موازی** اجرا می‌شدند درحالی‌که سرورِ پورتِ ثابت بالا می‌آوردند. **سه‌تادهٔ 8997:** server10 + sim_full3 + reexam3 همگی 8997! (تا اکنون صرفاً با شانسِ زمانی سبز می‌ماندند.)
- **رفع (`2353898`):** `uses_port` حالا هر ارجاعِ `'tests/*.js'` را می‌خواند → **۵۱ سوئیتِ پورت‌دار** در لانِ سریال (قبلاً ۳۹).
- همزمان: **hardeningِ R92 روی هر ۱۰ سوئیت** اعمال شد (پترن‌هایِ پنج‌گانهٔ متفاوت: standard/noBuild/سخت‌کد/stdout||stderr/بلاک‌هایِ درون‌خطیِ preapp2) — هر ۱۰ در رجیسیونِ پایانی **سبز** ✅.

### ۲.۵ sweepِ وسطِ اجرا — قتلِ storeِ زنده (لایهٔ ۳)
- **چی شد:** sweepِ `rm -rf /tmp/payesh-*` (برایِ پاک‌کردنِ نشتی‌هایِ 5.6MB) وقتی LANE_A تمام می‌شد، **storeِ زندهٔ sim_full3** (`/tmp/payesh-sim3-*/store.json`) را پاک می‌کرد → `ENOENT` وسطِ تست → کرشِ سوئیت. (ممنوعیتِ اولیه در R92 فقط `/tmp/mut-*` را با `-mmin +2` محافظت کرده بود — payesh-* فراموش شد. این بمب از قبلِ R92 وجود داشت و فقط تایمینگِ رجیسیونِ #3 منفجرش کرد.)
- **رفع (`2353898`):** هر دو (`mut-*` و `payesh-*`) با `-mmin +2` — کپی‌هایِ زنده محافظت، نشتی‌هایِ کهنه هنوز پاک می‌شوند.

### ۲.۶ pkill — پترنِ نادرستِ پاک‌سازی (لایهٔ ۳، عمیق‌ترین)
- **چی شد:** کرشِ sim_full3 (از ۲.۵) سروری را روی 8997 **نشتی داد**. sweepِ لایه (`pkill -f 'node server/index.js'`) هیچ‌وقت آن را نکُشت، چون سرورها با **مسیرِ مطلق** spawn می‌شوند (`node /home/user/p2/server/index.js`) و پترنِ relative نمی‌خورد. نشتی **۱۲+ دقیقه** زنده ماند و تمامِ کارهایِ بعدیِ 8997 را آبشاری شکست: reexam3 → EADDRINUSE/`no_session` · reproی دستی → **429 rate_limited** (شمارنده‌هایِ `__auth.code_rate` در storeِ همان سرورِ زومبی!).
- **رفع (`2353898`):** پترنِ `node .*server/index\.js` در **هر دو مکان** (self-healِ شروع + sweepِ بعدِ هر سوئیتِ لانِ پورت).
- **اثباتِ زنجیره:** PID 41952 — سروری با cwd `/home/user/p2` که ساعت 23:38 (اثناءِ رجیسیونِ #3) شروع شده بود و 12 دقیقه زنده ماند؛ بعد از kill، reexam3 همان لحظه **8/8** سبز شد.

## ۳. دروازه‌هایِ پایانی

| دروازه | نتیجه |
|---|---|
| رجیسیونِ کامل (#4) | **125/125 · 0 قرمز · 663s** |
| smoke | 546/546 ✅ |
| check-authz | 6/6 ✅ |
| build | سبز ✅ |
| سرورهایِ نشتی‌شده بعد از رجیسیون | **0** ✅ |

**هزینه:** طولِ رجیسیون 431–471s → **663s** (۱۲ سوئیتِ پورت‌دارِ تازه به لانِ سریال اضافه شدند). این قیمتِ درستیِ جدولة است: تا اکنون این تداخلها فقط با شانسِ زمانی سبز می‌ماندند.

## ۴. باقی‌مانده

| مورد | وضعیت |
|---|---|
| **دکمهٔ پرداختِ خودِ ولی در حالتِ سرور** | **منتظرِ تصمیمِ محصول** (بدل‌ها: پنهان+«ثبت از طریق مدیر» [پیشنهاد] / همان‌جوری / پایداریِ شبیه‌سازی / دروازهٔ واقعی) — تنها موردِ بازِ کلِ پروژه |
| ۱۷ سوئیتِ سبکِ قدیمیِ **غیرپورت** (jsdom) بدونِ hardeningِ مرگِ زودهنگام | ریسکِ کم (بدونِ سرور/پورت؛ ریسکِ اصلیِ R89 یعنی OOM_سرور ندارند) — ثبت شد، اجرایِ اختیاری |
| `539b305` از چتِ جدید | هنوز به گیت‌هاب نرسیده — پرامپِ آماده در چت منتظرِ اجراست |
