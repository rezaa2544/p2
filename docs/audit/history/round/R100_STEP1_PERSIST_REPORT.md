# گزارشِ گامِ ۱ — نقصِ ۲: درجِ بدونِ دفترچه/صف (P0، ماندگاری)

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08) · **برنچ:** `fix/behavior-findings`
**کامیتِ این گام:** (پایین، پس از ثبت)

---

## ۱. رفع (۷ نقطه — ۶ دستوری + ۱ یافتهٔ حینِ کار)

| # | فایل:خط | تابع (مسیرِ کاربر) | مجموعه |
|---|---------|-------------------|--------|
| ۱ | `53-visitors.js:46` | `visitorRegister` | visitors |
| ۲ | `54-library.js:78` | `libAddBook` | lib_books |
| ۳ | `54-library.js:141` | `libLend` | lib_loans |
| ۴ | `55-assets.js:55` | `assetAdd` | assets |
| ۵ | `56-sida-diff.js:91` | `sedasUpsert` (شاخهٔ درج) | sedascores |
| ۶ | `52-dojo.js:181` | `dojoSaveModel` (حلقهٔ انواعِ تازه) | dojo_types |
| ۷ | **`33-forms-sms.js:289`** | **`certRecord`** (ثبتِ صدورِ گواهی) | **certificates** |

هر ۷ مورد: `add(` ← `insert(` (تک‌واژه؛ امضا یکسان، رفتارِ خواندن یکسان،
افزوده: ثبت در دفترچه + صفِ همگام‌سازی).

**چرا نقطهٔ ۷ اضافه شد:** حینِ پیاده‌سازیِ نگهبان، کلِ `add(`هایِ بیرون از
`02-demo-data.js` رده‌بندی شد (اسکریپتِ حوزه‌یاب). `certRecord` تنها نمونهٔ
کاربریِ بیرون از ۶ مسیرِ دستوری بود — همان ردهٔ P0 (گواهیِ صادرشده پس از
بوت گم می‌شد). نگهبانِ دستوری («ممنوعیتِ add بیرون از دمو») بدونِ رفعِ آن
قرمز می‌ماند؛ پس با همان وصلهٔ تک‌واژه‌ای رفع و با همان سئوت پوشش داده شد
(پنهان‌سازی نشد — این سطر، سندِ آن است).

## ۲. نگهبانِ ایستا (`tests/run.js` — گروهِ «نگهبان add/insert»)

- قانون: فراخوانیِ سراسریِ `add('…'` فقط در `02-demo-data.js` یا مولدهایِ
  دادهٔ پایه (`*Demo*`، `generateP8…P12`، `generateExtras`، و `generatePriorYear`
  برای سازگاریِ پیش‌رو با گامِ ۶).
- دقتِ پیاده‌سازی: `add`ِ محلیِ سایه‌انداز (۱۲-attendance) فقط در همان حوزه
  مستثناست (نه کلِ فایل)؛ ذکرهایِ `add()` در نظر/رشته (بدون کوتیشنِ مجموعه)
  شمرده نمی‌شوند.
- **دامِ ابزار در همین گام:** دو ویرایشِ موازی روی یک فایل (`54-library.js`)
  مسابقهٔ خواندن-نوشتن داد و یکی گم شد — نگهبان همان را گرفت (قرمزِ بجا) و
  ویرایش به‌صورتِ ترتیبی بازاعمال شد. از اینجا به بعد: ویرایش‌هایِ یک فایل،
  همیشه ترتیبی.

## ۳. پیامدِ احراز (مثبت و موردانتظار)

- `authz/write-perms.json` بازتولید شد (**صرفاً افزایشی، +۴۴ خط**): ۶ اکشنِ
  نویسندهٔ تازه شناسایی شدند (`as-save`، `lib-save`، `lib-lend-save`،
  `vis-save` با نقشِ manager؛ `cert-enroll-print` و `cert-transfer-print`
  بدونِ برچسب).
- نویسندگان: ۱۶۳ ← **۱۶۹** · بی‌برچسب‌ها: ۵۲ ← **۵۴** · `check-authz`:
  **تطبیقِ کامل** ✅
- نکتهٔ مهمِ گواهی: `authz/model.json` از پیش به manager/superadmin روی
  `certificates` مجوز داده بود — یعنی سرور **آمادهٔ** این opها بود و باگ،
  نرسیدنِ op به سرور بود. پس از رفع: چاپِ مدیر همگام می‌شود؛ چاپِ
  خودخدمتیِ ولی/دانش‌آموز محلی می‌ماند (fail-closed، مانندِ ۵۲ موردِ
  پیشین) — بدونِ نیاز به تصمیمِ امنیتیِ تازه.

## ۴. آزمون‌ها

- **تازه:** `tests/persist-roundtrip.js` — برای هر ۷ مسیر: ساختِ رکورد +
  بودنِ op در دفترچه + بودنِ op در صف (P1–P7)، سپس شبیه‌سازیِ بوتِ تازه
  (بسیجِ دوبارهٔ مولدها به‌ترتیبِ بوت + بازپخش) و سنجشِ بقایِ هر ۷ رکورد
  با شناسه (P8)، و حسابرسیِ صف (P9). نتیجه: **9/9** ✅
- **جهش:** `tests/persist-roundtrip-mutations.js` — هر جهش یک نقطه را به
  `add` برمی‌گرداند و سئوت باید در همان بخش قرمز شود. نتیجه: **7/7 کشته،
  خطِ پایه سبز، ۰ خطایِ محیطی** ✅
- **ریشه‌یابیِ P8 (دامِ آزمودن، نه باگِ محصول):** نخستین اجرا در گواهی قرمز
  شد. علت: بازتولیدِ درون‌حافظه‌ای بدونِ `idxReset()` ایندکسِ IDX را برای
  مجموعه‌هایِ بی‌سید (گواهی/دوجو) کهنه می‌گذاشت و بازپخش، درج را «تکراری»
  می‌پنداشت (اثبات با شمارندهٔ نسخه: ver‏ ۰←۱ بدونِ push). بوتِ واقعی
  (هیپِ تازه) این اثر را ندارد و جریانِ بازیابیِ محصول (`38-plans-backup.js`)
  هم دورِ بازتولید `idxReset` می‌کند — همان الگو به سئوت اضافه شد و P8 سبز شد.

## ۵. دروازهٔ این گام

- [x] `node build.js --check` سبز (تطبیقِ کاملِ authz)
- [x] `node tests/smoke.js` سبز (**547/547**)
- [x] سئوتِ تازه **9/9** + جهش‌ها **7/7**
- [x] سئوت‌هایِ موجودِ مرتبط سبز: visitors 5/5 · library 5/5 · libserial2 ‏7/7 ·
      assets 5/5 · sidadiff 5/5 · dojo 6/6 · certify 8/8 · compact 19/19 · schoolmode2 ‏6/6
- [x] `node tests/run.js` سبز (**34/34**، شاملِ نگهبانِ تازه)

**فایل‌هایِ این کامیت:** ۶ فایلِ `src/js` ‏(۷ تک‌واژه) + `authz/write-perms.json` +
`tests/run.js` + ‏۲ سئوتِ تازه + ‏`index.html`/`USER_GUIDE.html` بازسازی‌شده + این گزارش.

**گامِ بعد:** گامِ ۲ — نقصِ ۱ (P0، chunking صف).
