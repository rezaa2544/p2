# گزارشِ دورِ ۹۴ — بازبینیِ آمادگیِ تولید (audit بندهایِ TODO_BEFORE_PRODUCTION)

**تاریخ:** ۱۸/۰۶/۱۴ (2026-09-08)
**وضعیت:** کامل — کامیت + داک‌کامیت (پوش‌شده) · رجیسیونِ R93 بلافاصله پیش: **125/125**

## چرا این دور؟

`TODO_BEFORE_PRODUCTION.md` پیش از ساختِ سرور (دورهایِ ۶۰–۹۳) نوشته شده بود و بخش‌هایِ «مسدودکننده» آن هنوز به زبانِ «همه‌چیز سمتِ مرورگر است» بودند. این دور هر بند را **با کدِ فعلی** بازبینی کرد تا فهرستِ واقعیِ «تا تولید چه می‌ماند» مشخص شود. (فهرستِ خلاصه به ابتدایِ خودِ سند هم اضافه شد.)

## یافتهٔ اصلی: مسدودکننده‌هایِ کلاسیک، قبلاً بسته‌اند

| بند | نتیجهٔ بازبینی | مدرک |
|---|---|---|
| **۱.۲** احرازِ هویتِ سرور | ✅ **پیاده** — ورودِ تلفن/کد/کد ملی، کوکیِ `HttpOnly`+`Secure`(در https)+`SameSite=Lax`، ابطالِ نشست (jti)، انقضا/تمدید، قفلِ DEMO_CODE | `server/auth.js` · جهش‌هایِ M1/M5/M7/M10/M20 · server1 (31 چک) |
| **۱.۴** RBAC + محدودهٔ داده | ✅ **پیاده در پروتکلِ sync** — `WRITE_PERMS` (سطحِ مجموعه) + `FIELD_ALLOWLISTS` (سطحِ فیلد) + scope مدرسه/فرزند/کلاس → `out_of_scope` به‌ازایِ هر عملیات. (توضیحِ پروتکلی: sync مجموعه‌ای است نه REST — به‌جای 404 برایِ IDOR، کدِ `out_of_scope` روی همان op می‌آید؛ طراحیِ ثبت‌شده) | `server/sync.js` · M2/M3/M9 · server12 (43 چک) / server13 |
| **۲.۲** محدودسازیِ نرخ | ✅ ورود: ۵ تلاش/۱۰ دقیقه + پاسخِ هم‌زمان (anti-enumeration) + بررسیِ active بعد از کد | `server/auth.js:97` (S-73-2) · server1 |
| **۲.۳** سابقهٔ امنیتی | ✅ سمتِ سرور، فقط‌افزودنی (`audit.jsonl`) — شاملِ send_code/backup/restore | `server/index.js` · چک‌هایِ audit در server9 |
| **۲.۴** پشتیبان | ✅ خودکار (`BACKUP_EVERY_MS`) + دستی (superadmin/manager) + **اعتبارسنجیِ بازیابی** + GC | server9 (A2/A4) · M17 · server14-gc |
| **۲.۶** سرآیندها + CSP | ✅ کامل‌تر از آنچه سند خواسته بود: CSP با **nonce به‌ازایِ هر درخواست** (build.js + سرور)، `X-Frame-Options: DENY`، nosniff، Referrer-Policy، Permissions-Policy، HSTS | `server/index.js:178` |

## آنچه واقعاً می‌ماند

### فنی (قابلِ اجرا در پروژه)
| مورد | توضیح | اندازه |
|---|---|---|
| **۲.۵ base_version + رابطِ داوریِ تعارض** | سیاستِ ترکیبی تصمیم گرفته شده اما `base_version` در sync نیست و صفحهٔ داوری ساخته نشده. **تنها فیدِ فنیِ بزرگِ باقی‌مانده.** | متوسط (فیلد + منطق + UI) |
| **۲.۷ هشدارِ تدریجیِ شمردنِ شناسه** | ثبت ⇒ تأخیر 500ms ⇒ 2s+هشدار ⇒ ابطال نشست — پیاده نیست. **همراهش تصمیمِ UUID-vs-عدد باید پیش از نخستین استقرار واقعی گرفته شود.** | کوچک–متوسط + یک تصمیم |
| **۲.۱(a)** سقفِ نرخِ ورودِ اکسل | ورودِ فایلِ اکسل هنوز سقفِ نرخ ندارد (ورودِ کاربری دارد) | کوچک |
| **۲.۱(b)** قواعدِ عمیقِ کسب‌وکار سمتِ سرور | ساختار/امتیاز/فیلد سروری ✅؛ قواعدی چون ظرفیتِ کلاس هنوز سمتِ کلاینت‌اند | متوسط (فهرست‌بندیِ تک‌تک) |
| **Lax→Strict** (SameSite) | سند `Strict` می‌خواهد؛ حالِ فعلی `Lax` است (سازگارتر با فرم‌هایِ خارجی) — سلیقهٔ امنیتی | یک کلمه |

### نیازمندِ منابع/تصمیمِ شما (خارج از کد)
| مورد | نیاز |
|---|---|
| **پرداختِ والد در حالتِ سرور** | ⏳ **تصمیمِ محصول** (پیشنهاد: پنهان + «ثبت از طریق مدیر») — **تنها موردِ بازِ کلِ پروژه** |
| دروازهٔ پیامکِ واقعی | حسابِ شاپرک/زرین‌پال (R65: شاد دستی) |
| مقیاس‌پذیری (PostgreSQL/مُلتی‌نود) | تصمیمِ استقرار — store فعلی برایِ پایلوتِ تک‌مدرسه کافی است |
| نگهداریِ پشتیبانِ خارجِ سرور + HTTPS + استادیng | عملیات |

## نتیجه

فهرستِ «تا تولید» از ~۱۲ بندِ «مسدودکننده/مهم» به **۵ موردِ فنیِ مشخص + ۴ موردِ منابعی** تقلیل یافت؛ شش بندِ کلاسیک (احراز، RBAC/محدوده، نرخ، سابقه، پشتیبان، سرآیندها/CSP) **قبلاً با آزمونِ جهشِ محکم‌کاری‌شده بسته** بودند و این بازبینی آن را با مدرکِ فایل/تست ثبت کرد.
