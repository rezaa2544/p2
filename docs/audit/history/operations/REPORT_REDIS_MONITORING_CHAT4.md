# گزارش چت ۴ — مستندسازی Redis Monitoring & Alerting

**تاریخ:** ۲۰ شهریور ۱۴۰۵ (2026-09-10) · **شاخه:** `feat/p2-redis-integrity-chat4`

## خلاصه

مستندات کامل پایش و هشداردهی ردیس تحویل شد — همراه با فایل‌های اجرایی واقعی
(قوانین هشدار پرومتئوس، داشبورد گرافانا، و جمع‌آور متریک صفر وابستگی) تا سند
«قابل اجرا» باشد نه فقط توصیفی. مینِ تازه (430c7c8 شامل PR18/PR19) نیز در
شاخه ادغام شد؛ کار قبلی فاز ۲ از طریق همین شاخه وارد مین شده است (تشکر از ادغام).

## جدول کامیت‌ها

| کامیت | موضوع | وضعیت |
|---|---|---|
| `d6126fb` | ادغام مین تازه (430c7c8) در شاخهٔ چت ۴ | ✅ |
| `334630a` | `docs: add redis monitoring and alerting documentation` | ✅ پوش شد |

## ۱) مستندات — `docs/REDIS_MONITORING_ALERTING.md`

| بخش خواسته‌شده | تحویل |
|---|---|
| ۱.۱ متریک‌های کلیدی | جدول ۱۴ متریک با منبع (اکسپورتر/جمع‌آور) و آستانه — شامل همهٔ موارد جدول پرامپت (`used_memory` >۸۰٪، اوج >۹۰٪، کلاینت‌ها >۱۰۰۰، مسدودها >۱۰، انحراف >۵۰٪ در نرخ عملیات، نرخ اصابت <۸۰٪، تأخیر کپی >۵ ثانیه، آپ‌تایم <۱ ساعت) + متریک‌های مخصوص پروژه |
| ۱.۲ یکپارچه‌سازی با پرومتئوس | معماری دو ردیفه: ردیف اول `redis_exporter` رسمی برای متریک‌های بومی؛ ردیف دوم جمع‌آور پروژه (`tools/redis-metrics.js --serve 9122`) برای متریک‌های لایهٔ اپ؛ به‌علاوه کد مرجع `prom-client` برای مسیر `/metrics` درون اپ (با ذکر دلیل توقف در حد مستند: فلسفهٔ کمینه‌وابستگی + کفایت ردیف‌های فعلی) |
| ۱.۳ قوانین هشدار | فایل واقعی `monitoring/alert-rules.yaml` — ۱۲ قانون (شامل هر سه قانون پرامپت با عبارت‌های دقیق و سازگار با اکسپورتر) + ران‌بوک هر هشدار |
| ۱.۴ داشبورد گرافانا | فایل قابل‌ایمپورت `monitoring/grafana-redis-dashboard.json` — ۱۰ پنل شامل دقیقاً پنج پنل خواسته‌شده: Memory Usage، OPS/sec، Hit Rate، Connected Clients، Replication Lag |
| اضافی | چک‌لیست استقرار پایش + ران‌بوک هشدارها |

## ۲) جمع‌آور متریک — `tools/redis-metrics.js` (صفر وابستگی)

- خروجی در قالب متنی پرومتئوس: یک‌بار (کرون/بررسی) یا سرویس با `--serve <port>`.
- متریک‌ها: `payesh_redis_up`، `payesh_redis_mode{mode}`، `payesh_redis_keys_total`،
  `payesh_redis_keys_no_ttl` (با `PAYESH_METRICS_KEYS=full`)، `redis_used_memory_bytes`،
  اوج حافظه، کلاینت‌ها، مسدودها، عملیات/ثانیه، آپ‌تایم، نرخ اصابت، تأخیر کپی‌سازی.
- با همان درایور `server/redis.js` وصل می‌شود — در محیط توسعه (درایور حافظه)
  هم خروجی معنادار دارد؛ `redis.info()` به درایور افزوده شد (در حافظه، معادل مصنوعی).

## ۳) تست — `tests/redis-metrics.js`: **۱۵/۱۵ موفق**

پارس INFO · جمع‌آوری در درایور حافظه (لایو بودن/برچسب حالت/شمار دقیق کلید) ·
نرخ اصابت · ممیزی بی‌TTL · صحت قالب پرومتئوس (HELP/TYPE/برچسب) · اجرای بالینی
ابزار با خروجی صفر.

## ۴) قوانین سخت — همه سبز

| آزمون | نتیجه |
|---|---|
| `node tests/smoke.js` | **۵۴۷/۵۴۷** ✅ |
| `node tools/check-authz.js` | خروجی **۰** ✅ |
| `node tests/secret-scan.js` | **۱۱/۱۱** ✅ |
| سوئیت‌های ردیس (متریک ۱۵، فیل‌اُوور ۸، فال‌بک ۱۰، ممیزی کلید ۱۷) | همگی سبز ✅ |

## ۵) تصمیم‌ها و یادداشت‌ها

- **وابستگی تازه اضافه نشد:** مسیر توصیه‌شده `redis_exporter` (باینری مستقل
  عملیاتی) + جمع‌آور صفر وابستگیِ خود پروژه است؛ `prom-client` فقط به‌صورت
  کد مرجع در مستند آمده و فعال‌سازی آن نیازمند تصویب وابستگی تازه است.
- **عبارت‌های هشدار با اکسپورتر واقعی سازگارند** (مثلاً `redis_memory_used_bytes /
  redis_memory_max_bytes * 100` به‌جای نام‌های انتزاعی) تا قوانین از روز اول کار کنند.
- **سیاست اخراج باید `noeviction` بماند** (کلیدهای حالت نباید بی‌صدا بپرند) —
  در سند تصریح شده که پر شدن حافظه = خطای نوشت و هشدارهای حافظه باید پیش از ۹۰٪ اقدام شوند.
- داشبورد و قوانین با اعتبارسنج ساختاری تأیید شدند (10 پنل، 12 قانون).
