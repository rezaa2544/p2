# چت ۴ — گزارش اتصال و پایش (بدون کد) — ۱۹/۰۶/۱۴۰۵ (2026-09-10)

**شاخهٔ نشست:** `arena/01a08a4e-p2` (بر پایهٔ `main` @ `40c5f96` — مرج PR #44) · درخت کار تمیز
**دامنهٔ نشست:** یکپارچه‌سازی — Performance / Infra / Deployment (آرنا ۴)

---

## ۱) گیت‌های پایه در ابتدای نشست

| گیت | نتیجه | نکته |
|---|---|---|
| `node tools/check-authz.js` | ✅ ۰ ناهمخوانی (۳۷۶ اکشن / ۱۸۳ نویسنده) | — |
| `node tests/secret-scan.js` | ✅ ۱۱/۱۱ | — |
| `node build.js --check` | ✅ بیت‌به‌بیت + راهنما همگام + write-perms یکسان | — |
| `node tests/smoke.js` | ⚠️ ۴۰۵/۵۳۰ در سندباکس | **باگ پنجشنبه** — رجوع بند ۳ |

## ۲) Ruflo — حافظهٔ مشترک

- `ruflo` نسخهٔ **v3.39.2** نصب شد (`npm install -g ruflo@3.39.2`)؛ بک‌اند `sql.js + HNSW`، اسکیمای حافظه 3.0.0.
- **یافتهٔ مهم:** سندباکس تازه بود و `/tmp/ruflo-unified` وجود نداشت → دیتابیس صفرورودی؛
  کلیدهای `p2/roadmap-status`، `p2/memory-branch-map`، `next_wave` در این محیط گم بودند.
- هر سه کلید **از منابع معتبر ریپو + gh بازسازی** و با برچسب `REBUILT 2026-09-10 by Chat 4` ذخیره شد؛
  به‌علاوهٔ `p2/chat4-new-session` طبق پروتکل. اکنون **۴ ورودی** با بازیابی معنایی سالم.
- اجرا همیشه از بیرون ریپو (`cd /home/user`) با `CLAUDE_FLOW_MEMORY_PATH=/tmp/ruflo-unified` و `CLAUDE_FLOW_DISABLE_BRIDGE=1`.

## ۳) ریشه‌یابی قرمزی smoke در سندباکس (مهم‌ترین کشف این دور)

- خطای زمان‌بارگذاری `TypeError: ... reading 'teacher_id'` در `src/js/02-demo-data.js` —
  حلقهٔ ساخت برنامهٔ هفتگی روزها را سخت‌کد `0..4` می‌پیمود، اما مدرسهٔ دمو `work_days=[0..5]` دارد
  و بلوک جابه‌جای برای پنجشنبه (`dow0=5`) اسلات روز ۵ می‌خواست → در پنجشنبه‌ها `db.schedule.find(...)` تعریف‌نشده و ۱۲۵ بررسی دودی پشت‌سرهم قرمز می‌شود.
- **تأیید:** CI راه‌دور روی `main` سبز است چون ران ۱۴ ساعت پیش (چهارشنبه UTC) اجرا شد؛ ران امروز سندباکس (پنجشنبه 2026-09-10) همان باگ را می‌بیند.
- **رفع موجود است:** PR #45 (`feat/wave20-chat4`) شامل `tests/demo-thursday.js` + فیکس `02-demo-data.js` (پیمایش طبق `work_days`) + تنظیم بند ۱.۵ smoke است — وضعیت GitHub: **CLEAN / MERGEABLE (+1338/−285)**.
- نتیجه: **صفر رگرسیون ناشی از این نشست**؛ این باگِ تقویمیِ شناخته‌شده است و مرج PR #45 آن را روی main می‌بندد.

## ۴) وضعیت شاخه‌ها/PRها (مشاهدهٔ زنده از gh)

- **باز و قابل ادغام:** #45 (Wave 20 آرنا ۴) ✅ آمادهٔ مرج · #43 (Wave 12 شبکه/لبه) mergeable=UNKNOWN · #35 (b3-d234) · #29–#34 (قابلیت‌های چت ۳).
- **تازگی‌های مرج‌شده:** #44 Wave9+دروازهٔ انتشار · #42 Wave8 اوت‌باکس · #41 Wave5 authz · #39 Wave1 (seam خوانش) · #38/#40 baseline Part2/3 · #37 فازهای ۰.۱–۰.۳ · #36 redis-cluster.
- **CI امنیتی main:** تنها قرمزی = job «WAF & nginx» در گام `Install nginx` (اختلال گذرای رانر apt) و DAST که عمداً skip می‌شود (بند ۵).
- **مقررات رعایت‌شده در این نشست:** بدون push روی main؛ بدون تغییر کد؛ کامیت مستندات فقط روی `arena/01a08a4e-p2`.

## ۵) شکاف Wave 13 (ZAP/DAST) — صحت‌سنجی شد

`security.yml` فعلی روی main: شش job (SAST · Secret · SCA · SBOM · DAST · WAF) با فیکس‌های ثبت‌شدهٔ
`env.SECURITY_TARGET_URL` (دور zero-job) و `npm sbom --sbom-format=spdx`. شکاف باقی‌مانده **کدی نیست**:
`SECURITY_TARGET_URL` تنظیم نشده ⇒ ZAP baseline همیشه skip (best-effort). گره‌گاه عملیاتی: آدرس staging زنده.

## ۶) نقشهٔ مالکیت آرنا ۴ (سند `docs/ARENA4_PERFORMANCE_INFRA.md` روی PR #45 — کامل خوانده شد)

Redis (memory/standalone/sentinel/cluster + گیت P0-13) · کش L1 محدود+LRU/TTL، L2 بوت TTL 5د، ابطال Pub/Sub ·
کارگر سنگین persist/backup/report (بلاک event-loop ‏46ms→0.6ms) · اوت‌باکس+worker (Wave 8) ·
رصدپذیری OTel→Jaeger (tracing-integration 14/14 پایدار زیر بار، `OTEL_BSP_MAX_QUEUE_SIZE=8192`) ·
زیرساخت k6 شش‌سناریویی + thresholds (خطا<۱٪ · p95<250ms · p99<500ms) · DEPLOY/systemd/Arvan ·
قانون خط‌کش: میانهٔ حضور ۳۶۴ms، آستانهٔ رجعت ۶۰۰ms. باقی‌مانده‌ها: متریک عددی+SIEM پس از استقرار، اجرای مرجع k6 روی staging، مدل ظرفیت پنج‌محوری.

## ۷) کارهای باز تیمی (به‌روزرسانی Ruflo: `next_wave`)

۱) مرج PR #45 (با تأیید ناظر — اصل هشتم) ⇒ پنجشنبه‌ها smoke روی main سبز می‌شود · ۲) تکمیل Wave 1 (PG-only خوانش بقیهٔ REST + راستی‌آزمایی PG زنده) · ۳) ریبیس+PR شاخه‌های چت ۲ (14/16/17) · ۴) ریبیس پرریسک چت ۳ (1p2/6/11/15/18/19) · ۵) فعال‌سازی لبه Wave 12 (عملیاتی).

---

**ثبت صداقت:** این دور فقط پایش بود؛ هیچ فایل کد/تست/اسکیما تغییر نکرد. تنها افزوده: همین گزارش.
محرمانگی: هیچ رمزی ذخیره/منتقل نشد (سیاست fail-closed؛ placeholderها در ریپو دست‌نخورده).
