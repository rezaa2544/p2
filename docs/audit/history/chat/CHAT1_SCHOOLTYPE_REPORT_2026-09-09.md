# گزارشِ چت ۱ — فیلد «نوع مدرسه» با پیامد واقعی (فاز ۰.۱)

- **تاریخ:** ۲۰/۰۶/۱۴۰۵ (2026-09-09)
- **شاخه:** `arena/01a0827b-p2` — رویِ گیت‌هاب به‌روز شد (push شد؛ §۶)
- **کامیت‌ها:** ۶ کامیت (C1–C5 کار + C6 همین گزارش و HANDOFF) — جدول در §۱
- **سندِ تحویلی:** `docs/SCHOOL_TYPE_GUIDE.md`

---

## §۰. انحرافِ شاخه (قفلِ نشست — غیرقابل‌مذاکره)

پرامپت شاخهٔ `feat/school-type-chat1` می‌خواست، ولی قفلِ نشستِ آرنا ساخت/سوییچ/پوش
به هر شاخه‌ای جز `arena/01a0827b-p2` را ممنوع می‌کند (همان محدودیتی که در بستهٔ
PR22 هم اعمال شد). همهٔ ۶ کامیت روی `arena/01a0827b-p2` است؛ ناظر می‌تواند همان‌ها
را cherry-pick یا fast-forward به شاخهٔ دلخواه ببرد — کامیت‌ها اتمیک و سبزبه‌سبزند.

---

## ۱. جدولِ کامیت‌ها

| # | کامیت | پیام | محتوا |
|---|--------|------|-------|
| C1 | `ce34c82` | `feat(authz): add school_type field to schools model` | `model.json` ‏(+۱) + بازتولید `write-perms.json` ‏(+۱)؛ تازه‌سازی قطعی مُهر راهنما و `cdn-manifest` کهنه در HEAD |
| C2 | `feb4dd0` | `feat(schools): school_type consequence matrix + capability fallback` | `SCHOOL_TYPE_DEFS` + سه تابع + fallback در `schoolCaps`؛ enum سرور در `validate.js`؛ ری‌بیلد |
| C3 | `5f64eb5` | `feat(ui): school_type selector in school modal with auto capability flip` | سلکت `m_school_type` + راهنما؛ ذخیرهٔ ضدعفونی‌شده؛ flip خودکار روی `change`؛ انسجام مودال تازه؛ ری‌بیلد |
| C4 | `c50b62e` | `test(schools): add school-type + mutation suites (8/8, 5/5 killed)` | `tests/school-type.js` ‏(ST0–ST7) + `tests/school-type-mutations.js` ‏(SM1–SM5) |
| C5 | `0f80592` | `docs: add school type guide (phase 0.1)` | `docs/SCHOOL_TYPE_GUIDE.md` (انواع، ماتریس، معماری، امنیت، محدوده، گیت‌ها) |
| C6 | همین کامیت | `docs: add chat 1 school-type report` | ورودی HANDOFF + همین گزارش |

ترتیبِ اجرا C1→C2→C3 بود (منطق پیش از UI) چون UI به توابعِ منطق وابسته است و هر
کامیت باید به‌تنهایی سبز باشد؛ در گزارش، نظمِ پرامپت (UI=۲، منطق=۳) حفظ شده است.

---

## ۲. چه چیزی پیاده شد

- **مدل:** `school_type` در `schools.fields` (الفبایی، بین `province_id` و `shift`)؛
  چون `FIELD_ALLOWLISTS` از مدل تولید می‌شود، همین یک خط دروازهٔ sync را هم باز کرد —
  بدون هیچ تغییری در `server/sync.js`.
- **ماتریس پیامد** (`SCHOOL_TYPE_DEFS` — ۹ نوع × ۶ ستونِ tuition/dorm/iep/multigrade/
  workshop/entrance_exam) دقیقاً مطابق جدول پرامپت؛ سیم‌کشی از طریق همان کلیدهای
  `capability` و گیت‌های `hasCap` — **صفر اکشن جدید**، صفر شرط تازه در ماژول‌ها.
- **زنجیرهٔ تقدم در `schoolCaps`:** قابلیت صریح > سطر نوعی (فقط اگر صریح و معتبر
  باشد) > `CAP_DEFAULTS`. نتیجه: مدارس قدیمی (بی‌نوع) **دقیقاً رفتار قبلی** را دارند.
- **UI:** سلکت ۹تایی در مودال مشترک مدیر/سوپرادمین (سطح دسترسی مودال عوض نشد)؛ با
  تغییر نوع، چک‌باکس‌ها از سطر پیامد flip می‌خورند ولی override دستی ممکن است؛
  `school-save` مقدار نامعتبر/خالی را به `governmental` می‌برد (fail-closed).
- **سرور:** `ruleFor('schools','school_type')` از نوع `enum` نه‌تایی؛ ناشناخته ←
  `bad_enum` و هرگز به store نمی‌رسد.

---

## ۳. گیت‌ها (پس از هر کامیت)

| گیت | C1 | C2 | C3 | C4 | C5 |
|---|---|---|---|---|---|
| `node tests/smoke.js` | 547/547 ✅ | 547/547 ✅ | 547/547 ✅ | 547/547 ✅ | 547/547 ✅ |
| `node tools/check-authz.js` | ۰ ✅ | ۰ ✅ | ۰ ✅ | ۰ ✅ | ۰ ✅ |
| `node tests/secret-scan.js` | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ |
| `node build.js --check` | سبز ✅ | سبز ✅ | سبز ✅ | سبز ✅ | — (docs) |
| `tests/authz-model.js` | 232/232 ✅ | 232/232 ✅ | 232/232 ✅ | — | — |
| سوئیتِ تازه | — | — | — | 8/8 + ۵/۵ کشته ✅ | — |
| رگرسیون مرتبط | migration 8/8، cdn 5/5 | minutes2 8/8 | پروب jsdom ✅ | boom2 8/8، eslint بدون خطا | — |

**رگرسیون کامل** (`scripts/run-all-tests.sh`، حدود ۳۰ دقیقه):
**سبز=۱۸۵، قرمز=۱۲** — هر ۱۲ قرمز با A/B روی worktree تمیزِ `c22d354` **عیناً**
بازتولید شدند (امضاهای یکسان از جمله `2✅ 6❌` در چهار سوئیت سروری)؛ یعنی
**تغییرات من صفر سوئیت را شکسته‌اند** و هر دو سوئیت تازه (`school-type` و
`school-type-mutations`) در هارنس کانونی سبزند. فهرست ۱۲ قرمزِ پیشینه (§۵).

---

## ۴. تصمیم‌های آگاهانه و محدودهٔ فاز ۰.۱

- **سازگاری عقب‌رو:** پیش‌فرض نمایشیِ مدارس بی‌نوع `governmental` است، ولی رفتار
  مؤثرشان از قابلیت‌های ذخیره‌شده می‌آید تا هیچ مدرسه‌ای ماژول از دست ندهد؛ با
  یک‌بار ثبت نوع واقعی در مودال، مدرسه یکدست می‌شود. (مستند در §۴ گاید.)
- **`has_workshop` ← `vocational`:** «نمره عملی/کارگاهی» جدول پرامپت همان کلید
  موجود کارگاه است — سیم‌کشی شد و تست دارد.
- **`entrance_exam` داده‌ای:** ماژول مستقلی ندارد؛ فقط در `schoolTypeFeatures`
  افشا و تست شده؛ سیم‌کشی به فاز ماژول آزمون ورودی موکول است.
- **`shahed`:** فعلاً همان `has_tuition` سطح مدرسه؛ تفکیک سطح دانش‌آموز به فاز مالی.
- **`has_second_term_exam`/`has_evening`:** بیرون جدول پرامپت‌اند؛ بی‌نگاشت ماندند.
- **مدیر در برابر سوپرادمین:** دکمه‌های مودال مدرسه از قبل فقط-سوپرادمین بودند؛
  چون «سطح دسترسی» خارج از محدودهٔ فاز ۰.۱ است، سلکت در همان مودال مشترک نشست
  (هر دو نقش هر جا مودال را ببینند، سلکت را می‌بینند) و authz دست نخورد.
- **محتوای `USER_GUIDE.html`:** فقط مُهر بیلد تازه شد؛ متن راهنما با تیم مستندات.

---

## ۵. مواردِ پیشینه (نامرتبط — با مدرک A/B)

| سوئیت | امضا (هم در شاخهٔ من، هم در `c22d354`) |
|---|---|
| `preapp3`/`reexam3`/`scholarship3`/`summer3` | ۸ بررسی — ۲✅ ۶❌ (یکسان) |
| `user-self-update` | ۱/۸ (USU8) |
| `server16` | ۱۱ سبز / ۲۸ قرمز |
| `server7` | ۱۴/۱۵ (C3a) |
| `preapp2`/`reexam2`/`scholarship2`/`summer2`-mutations | هرکدام ۱ جهش زنده |
| `server16-mutations` | خطا/کرش |
| `dropout-mutations` | در هارنس کانونی سبز (۴/۶ اولیه فقط قبل از seed بود — محیطی) |

همچنین در C1 مشخص شد مُهر `USER_GUIDE.html` و `cdn-manifest.json` در HEAD کهنه
بودند (build --check قرمزِ پیشینه) — با ری‌بیلد قطعی تازه و سبز شدند.

---

## ۶. تأییدیهٔ پوش

- **مقصد:** `origin/arena/01a0827b-p2` (تنها شاخهٔ مجاز نشست — §۰)
- **وضعیت:** هر ۶ کامیت پوش و با HEAD راه‌دور همگام شد ✅
- **درخت پس از پوش:** تمیز (`git status` خالی)

فاز ۰.۱ کامل تحویل شد. 🟢
