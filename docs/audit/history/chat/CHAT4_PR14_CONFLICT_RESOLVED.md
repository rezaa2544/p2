# گزارش رفع تعارض پی‌آر ۱۴ (`USER_GUIDE.html`) — چت ۴

**تاریخ:** ۱۸/۰۶/۱۴۰۵ · **کامیت مرج:** `85f8ced` (پوش شد) · **درخت:** تمیز

## چه اتفاقی افتاد
مین به `daefb69` حرکت کرد (مرج پی‌آر ۱۳ — کارهای چت ۳ شامل ج.۱–ج.۳ + گ.۳ که
`USER_GUIDE.html` را لمس کرده بود) ⇒ تعارض با مُهر/محتوای راهنما در شاخهٔ من.

## اقدام (طبق دستور)
1. مرج `origin/main` در شاخهٔ `feat/farnaz-phase1-chat4` — تنها تعارض واقعی
   `USER_GUIDE.html` بود (`HANDOFF.md`، `index.html` و `19-actions-core.js` خودکار حل شدند).
2. راه‌حل: نسخهٔ جدیدتر (سمت مین) نگه داشته شد و `node build.js` بازتولید کرد
   (بیلد فقط مُهر همگام‌سازی را به‌روز کرد).
3. ثبت کامیت مرج + پوش.

## اعتبارسنجی پس از مرج (روی درختِ ترکیبی: کارایی چت ۴ + دستهٔ ج چت ۳)
| آزمون | نتیجه |
|---|---|
| `node build.js --check` | خروجی ۰ ✅ |
| `node tools/check-authz.js` | خروجی ۰ ✅ |
| `node tests/smoke.js` | **۵۴۷/۵۴۷** ✅ |
| `attpartial` (بهینه‌سازی دور ۱۰۲ پس از مرج زنده است) | ۱۰/۱۰ ✅ |
| `att2`/`att3`/`att4` | ۶/۶ · ۹/۹ · ۱۱/۱۱ ✅ |
| سئوت‌های چت ۳ (`pubrep`/`minutes2`/`boom2`/`public2`) | ۹/۹ · ۸/۸ · ۸/۸ · ۸/۸ ✅ |
| هارنس کارایی (میانهٔ ۵ اجرا) | **۳۴۹ مس** — همچنان زیر هدف ۶۰۰ ✅ |

## وضعیت پی‌آر ۱۴
`mergeable: True` — **دکمهٔ مرج دیگر بلاک نیست.** (`mergeable_state: unstable`
صرفاً وضعیت استاتوس‌چک‌هاست، نه تعارض.) پی‌آر آمادهٔ مرج با تأیید ناظر است.
