# گزارش دور ۱۰۹ (چت ۳) — Wave 18: تست بار ملی (دادهٔ 10M + سناریوها)

**تاریخ:** ۲۰/۰۶/۱۴۵ (2026-09-09)
**شاخه:** `arena/01a08545-p2`
**مأموریت (ناظر):** طراحی و آماده‌سازی سناریوهایِ تستِ بارِ ملی (۱۰ میلیون کاربر) با دادهٔ آزمایشیِ واقع‌گرایانه.

---

## ۱. Audit وضعیتِ پیشین

| موجود | وضعیت |
|---|---|
| `tests/performance/` (فاز ۵) | کامل: 6 سناریویِ k6 (login/bootstrap/attendance/grades/notifications/sync-batch) + 4 سوئیت (spike-mehr/saturation/soak-24h/chaos-redis) + helpers (auth/metrics/payload) + config (thresholds/environments) + runner |
| `docs/LOAD_TESTING_PLAN.md` (فاز ۵) | کامل: مقیاس‌هایِ پله‌پله (10k→5M VUs) + SLOها + نمونهٔ k6 |
| **دادهٔ آزمایشیِ ملی** | **بود: خیر** — fixtures فقط 6 مدرسهٔ دمو؛ هیچ تولیدکنندهٔ 10M نمی‌بود ⇒ **شکافِ اصلیِ این موج** |
| **طرحِ یکپارچهٔ چهار سناریویِ الزامی** | بود: خیر ⇒ طراحی شد |

## ۲. `tools/generate-national-dataset.js` — دادهٔ ملی

### اعداد (scale=1) — نسبت‌هایِ واقعیِ ایران

| مجموعه | تعداد | مبنایِ نسبت |
|---|---:|---|
| schools | 100,000 | ≈ واقعیِ کشور |
| classes | 1,000,000 | 10/مدرسه |
| **users** | **10,000,000** | 8M دانش‌آموز (80/مدرسه ≈ میانگینِ واقعی) · 1M دبیر (1/کلاس) · 900k ولی (≈11٪ فعال‌سازیِ سالِ اول) · 99,900 مدیر · 99 edu_office · 1 superadmin |
| attendance | 50,000,000 | 6 روز + ۲۵٪ یک روزِ بیشتر (میانگین 6.25) |
| grades | 20,000,000 | 2 آزمون + 50٪ یک آزمونِ بیشتر (میانگین 2.5) |
| parent_links | 900,000 | 1/ولی |

### ویژگی‌هایِ مهندسی

- **قطعی:** seed ثابت ⇒ دو اجرا = **بایت-به-بایت یکسان** (sha256 در stats.json؛ تست T4)
- **پخش‌شده:** chunked، پیکِ حافظه ~50MB؛ مقیاسِ کامل ≈10GB در ~3 دقیقه؛ **فقط stdlib**
- **واقع‌گرا و امن:** 31 استانِ واقعی، نید با **رقمِ کنترلِ معتبر**، تلفن 12 رقمی — همه **مصنوعی و تکرارناپذیر**
- **CLI:** `--plan` (جدولِ ملی، پیش‌فرض) · `--scale` (پیش‌فرضِ تولید 0.001 ≈ 10k کاربر، زیر 1s) · `--seed` · `--out` · `--fast` · `--quiet`
- خروجی: 6 CSV (PG COPY-ready + k6) + stats.json + README (دستورالعملِ بارگذاری + هشدارِ امنیتی)

**باگِ پیدا و رفع‌شده در حینِ کار:** نسخهٔ نخستِ تلفن‌ها slice از 11 رقمِ مشتق بود — با birthday paradox در مقیاسِ 10M کولایز می‌زد (~3.5 برخوردِ منتظره در هر bucket). اصلاح: `'09' + 10 رقم` از تابعِ **دو-یک-یک** ((K + id·M) mod 10¹⁰ با M نسبت-اولِ 10¹⁰) ⇒ تکرارناپذیریِ **تضمین‌شده**؛ همان الگو برایِ نیدها (mod 10⁹).

## ۳. `docs/WAVE18_LOAD_TEST_PLAN.md` — چهار سناریویِ الزامی (مطابقت با k6ِ موجود)

| سناریو | اسکریپت | پارامترهایِ کلیدی | SLO |
|---|---|---|---|
| **بار عادی** (ورود روزانه) | scenarios 01/02/03 (40/40/20) | 100k VUs ≈ 15k RPS، 45 دقیقه | login p95<150ms، bootstrap p95<180ms، error<1٪ |
| **بار اوج** (اول مهر) | `suites/spike-mehr-test.js` | جهش 15→150 VU در 15s (10x) | در اوج: error<0.1٪، p95<500ms، بدونِ collapse |
| **فشار** (تا نقطهٔ شکست) | `suites/saturation-test.js` | پلهٔ 20٪ هر 5 دقیقه (15k→650k RPS) | guardrail: p95>1200 یا error>5٪ یا CPU>90٪ ⇒ ثبتِ نقطهٔ شکست + پروفایلِ bottleneck |
| **چند روزه** (نشت حافظه) | `suites/soak-24h-test.js` | 60٪ خطِ پایه، 24 ساعت | شیبِ heap در 6 ساعتِ آخر ≈ صفر؛ error<0.1٪ کلِ بازه |

+ SLOهایِ مرجع (thresholds.json) + نقشهٔ راهِ اجرا (staging → کلاستر → soak ملی) + **وضعیتِ صادقانهٔ pending**.

## ۴. تست‌ها — `tests/wave18-load-test.js` (38/38 سبز)

- T1–T2: ابزار + `--plan` اعدادِ **دقیقِ ملی** (10,000,000 / 100,000 / 1,000,000 / 50,000,000 / 20,000,000)
- T3: تولیدِ واقعیِ مقیاسِ کوچک: فایل‌ها/`stats.json` با سطرهایِ CSV سازگار، **همهٔ نیدها رقمِ کنترلِ معتبر** (1002/1002)، تلفن‌ها 12 رقمی، **نید/تلفن تکرارناپذیر**، FKهایِ homeroom/student/teacher، دامنهٔ وضعیت‌هایِ حضور
- T4: determinism (sha256 دو اجرا = یکسان)
- T5: زیرساختِ k6 کامل (6+4+3+2+runner)، همهٔ اسکریپت‌ها k6 معتبر، configها JSON سالم
- T6: مستندات (دو سند + چهار سناریو + ارجاع‌ها + pending)
- T7: READMEِ خروجی (PG COPY + k6 + هشدار + sha256)

## ۵. گیت‌ها + رگرسیون

| گیت | نتیجه |
|---|---|
| `tests/smoke.js` | **547/547** ✅ |
| `tools/check-authz.js` | **0** ✅ |
| `tests/secret-scan.js` | **11/11** ✅ |
| `build.js --check` | rc=0 ✅ |
| `tests/wave18-load-test.js` | **38/38** ✅ |
| رگرسیونِ سرور | لازم نبود — **هیچ کدِ سرور دست نخورد** (فقط ابزار/تست/مستندات) |

## ۶. Commitها

| Commit | محتوا |
|---|---|
| `5b0fb9a` | feat(wave18): generate-national-dataset.js + wave18-load-test.js + WAVE18_LOAD_TEST_PLAN.md + .gitignore (data/national/) |
| `a00c0c2` | docs(wave18): AI_PROMPT 0.5.34 + ROADMAP B.7 + HANDOFF |
| (این گزارش) | docs: گزارشِ دور ۱۰۹ |

**تأییدیهٔ پوش:** خروجیِ `git ls-remote origin arena/01a08545-p2` در پیامِ تحویل (hashِ خودِ کامیتِ گزارش داخلِ فایل قابلِ ثبت نیست).

## ۷. نکات برایِ سوپروایزر

- **pending (ثبت‌شده در طرح §5):** اجرایِ واقعیِ بارِ ملی = **در انتظارِ زیرساختِ زنده** (PG/Redis/k6) — مطابقِ نکتهٔ صادقانهٔ ناظر. چک‌لیستِ شروعِ مرحلهٔ ۱ (staging): dataset scale=0.001 → PG با migrations → کالیبراسیونِ SLO.
- **حجم:** مقیاسِ کامل ≈10GB — دایرکتوریِ خروجی `data/national/` در .gitignore است؛ برایِ CI همیشه از scale≤0.001 استفاده شود.
- **ادامهٔ طبیعی (Wave 19 آشوب):** سوئیتِ `chaos-redis-test.js` از قبل در `suites/` است — با Graceful Shutdownِ Wave 15 (drain + readiness 503) می‌توان سناریوهایِ «kill pod در حینِ ترافیک» و «قطعِ Redis در اوج» را مستقیم روی همان زیرساخت اجرا کرد.
