# گزارش دور ۱۰ — چت ۳ جدید: E.9 مدیریت مراجعین (visitors)

**تاریخ:** 18/06/1405 (2026-09-09)
**شاخه:** `arena/01a08545-p2` (ادامهٔ E.1)
**وضعیت:** ✅ کامل — پیاده‌سازی، تست، مستندات، کامیت و پوش

---

## ۱) پیشینه و تصمیمِ کلیدی

فامیلِ `visitors` از قبل به‌صورت «نسخهٔ سبک» (بند ۷) موجود بود:
فقط مدیر، دو فیلد (نام/هدف)، بدونِ جستجو و بدونِ کارتِ داشبورد.
E.9 همین زیرساخت (db/مسیر/جدولِ سرور/sync) را **به مشخصاتِ کامل
ارتقا داد** — نه ساختِ موازی.

**یافته + رفع (امنیت):** مسیرِ `#visitors` در نسخهٔ سبک **برای هر
نقشی با hashِ مستقیم باز بود** (گاردِ نقش نداشت). حالا
`viewVisitors` گاردِ نقش دارد و `canRoute` مسیر را فقط به
NAVِ مدیر/نگهبان می‌دهد.

## ۲) مدل داده (visitors)

| فیلد | وضعیت |
|---|---|
| `school_id`, `name`, `purpose`, `in_at`, `out_at`, `registered_by`, `created_at` | پیشین |
| `national_id`, `phone`, `visiting_person` | **تازه** (اختیاری) |
| `status` ('in'/'out') | **تازه** — رکوردهای کهنه از `out_at` مشتق می‌شوند (سازگاریِ عقب، بدونِ مهاجرت) |

## ۳) دسترسی (سه سطح)

| نقش | مشاهده | ثبت ورود/خروج |
|---|---|---|
| مدیر | ✅ | ✅ |
| نگهبان (`role=guard` — **نقشِ تازه**، تفویضی) | ✅ | ✅ |
| معاون (مدیرِ سطحی با عنوانِ «معاون») | ✅ فقط‌خوان | ❌ |
| سایر نقش‌ها | ❌ | ❌ |

«مجوزِ تفویضی» عملیاتی شد: سوپرادمین در فرمِ کاربر گزینهٔ
«نگهبان/پذیرش» را دارد (`USER_ROLES` سرور + `NAV.guard` هم به‌روز
شدند). گاردِ «معاون فقط‌خوان» روی داده است (`visitorWriteOk`) —
الگوی رایجِ کد.

## ۴) رابط کاربری

- **فرمِ ورود:** نام (اجباری) + هدف + فردِ ملاقات‌شونده + تماس + کد ملی
- **ثبت خروج:** دکمهٔ «خروج» در کنارِ هر ردیفِ حاضر؛ خروجِ تکراری رد
- **جستجو:** نام (زنده، `data-f`) + تاریخِ ورود (`vis_date`) + پاک کردن
- **داشبورد:** `visitorDashCard` — مراجعه‌های امروز + درِ حالِ حضور + «مشاهدهٔ همه»
- **دادهٔ نمونه:** ۳ رکوردِ **قطعی** (نسخهٔ پیشین `new Date()` داشت)

## ۵) کامیت‌ها (روی `arena/01a08545-p2`)

| # | هش | پیام |
|---|---|---|
| ۱ | `b2745e9` | `feat(visitors): E.9 visitor management — full spec (round 104)` |
| ۲ | `6b29aab` | `docs: E.9 visitor management — VISITORS_MODULE.md, AI_PROMPT 0.5.29, ROADMAP, USER_GUIDE, HANDOFF [2026-09-09]` |

(بالایِ همان سه کامیتِ E.1: `7c0aba2`/`c7574d8`/`29eaf40`)

> **نکتهٔ محیطی:** این سشن روی همان شاخه ادامه خورد ولی stateٔ
> محلی بینِ دورها از `main` بازیابی شده بود؛ قبل از پوش، شاخه
> روی HEADِ remote (E.1) سوار شد و E.9 به‌صورتِ دوتا کامیتِ تمیز
> دوباره ساخته شد — تاریخچهٔ شاخه در گیت‌هاب پیوسته و کامل است.

## ۶) دروازه‌ها (همه سبز)

| دروازه | نتیجه |
|---|---|
| `tests/smoke.js` | ✅ **۵۴/۵۴۷** |
| `tools/check-authz.js` | ✅ صفر (۱۸۳ اکشنِ نویسنده، نقشِ guard اضافه شد) |
| `tests/secret-scan.js` | ✅ ۱۱/۱۱ |
| `build.js --check` | ✅ تطبیقِ بایت‌به‌بایت (dist 2025.3KB، ۹۱ ماژول) |
| `tests/authz-model.js` | ✅ ۲۴/۲۴ |
| `tests/run.js` | ✅ ۳۵/۳۵ |
| `tests/visitors2.js` (تازه) | ✅ ۱۳/۱۳ (V0–V12) |
| `tests/visitors-mutations.js` (تازه) | ✅ ۵/۵ جهش کشته |
| `tests/workshop2.js` (رگرسیون E.1) | ✅ ۱۲/۱۲ |

## ۷) پوش — تأیید شده

```
git push origin arena/01a08545-p2
   29eaf40..6b29aab  arena/01a08545-p2 -> arena/01a08545-p2  (fast-forward)
```
تأیید با `git ls-remote`: remote `6b29aab632880abab02a74ea79845416656ca8cf`
= HEAD محلی. درختِ کاری پاک.

## ۸) تصمیم‌های باز (برای سوپروایزر)

1. **حذف/ویرایش رکورد:** فعلاً فقط ورود+خروج (del در مدل فقط
   مدیر/سوپرادمین، بدونِ UI).
2. **آمارِ مراجعین** (روزانه/هفته‌ای روی همان صفحه) — فعلاً نیست.
3. **diffِ بزرگِ `schema.sql`:** بازتولیدِ آن در هر seed،
   created_at‌هایِ کاملِ دادهٔ نمونه را با ساعتِ seed بازنویسی می‌کند
   (driftِ ذاتیِ طراحیِ دادهٔ نمونه) — تغییرِ واقعی فقط DDLِ visitors
   + ۳ رکورد است.
4. **PR به main:** طبقِ پیمان، فقط با تأییدِ صریحِ شما.
