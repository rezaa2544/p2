# 📨 پرامپتِ نشست بعدی — بستنِ شکافِ هم‌زمانی گزارش‌های DB-native (Wave 23)

> آمادهٔ کپی‌کردن به‌عنوان **اولین پیام** نشست بعد (چت ۳ — معماری/هم‌زمانی).
> نسخهٔ بازبینی‌شده توسط چت ۶ (حاکمیت مستندات) — مواردِ **اصلاح‌شده/افزوده** با 🔧 مشخص‌اند.

---

## ۰) زمینه (Context)

چت ۶ (حاکمیت مستندات) دور خود را با قفل **`rc42`**، قالبِ PR، و ممیزیِ PRها بست.
شاخهٔ چت ۶: `arena/01a09801-p2` @ `b7880f6` (شامل اسنادِ تصمیم و پچ‌های ممیزی).
`main` اکنون روی **`23bf513`** است.

شما مسئولِ بررسی و رفعِ «شکافِ کاراییِ گزارش‌های academic/finance» هستید.

## ۱) ⚠️ تصحیحِ پیش‌فرض‌های غلط (بسیار مهم)

بریف‌های قبلی ادعا می‌کردند این گزارش‌ها **۲۵–۴۰× کندتر**ند و Seq Scan دارند. **این اعداد غلط‌اند** —
`REPORT_PERF_GAP_SPEC.md` §۰ صریحاً همین ادعا را رد کرده است.

**واقعیتِ measured (مرجعِ عددیِ شما):**

| سنجه | مقدار |
|---|---|
| شکاف در تک‌درخواست | ایندکس‌ها کار می‌کنند — academic **7.8ms** · finance **1.3ms + 1.3ms** (EXPLAIN) |
| شکاف زیر بارِ هم‌زمانی | **~۲–۳×** — بنچ `tools/bench-reports-concurrency.js`، ۵۰ درخواستِ هم‌زمان @200k حضور/60k نمره، Pool max=10: مسیرِ حافظه **p50≈۱۰۴–۱۰۸ms** در برابر DB-native **p50≈۲۲۶–۳۲۱ms** و **p95≈۳۶۴–۴۱۶ms** |
| Seq Scan | تنها روی `classes` (۲۰۰ ردیف) که انتخابِ درستِ برنامه‌ریز است ⇒ **NOT-ADD عمدی** |

🔧 **دو تصحیح روی متنِ پیشینِ این بریف:**
- عددِ «p95≈400ms در برابر حافظه≈100ms» نادقیق است (مقایسهٔ p95 با p50). جدولِ بالا مرجع است.
- 🔧 **ریشهٔ کندی فقط CPUِ تجمیع نیست:** طبق مستندات، store از قبل در RAM است و مسیرِ DB **رفت‌وبرگشتِ شبکه/Pool** را هم اضافه می‌کند؛ CPU-bound روی ۲ هسته. یعنی راهکار باید هم **هزینهٔ تجمیع** و هم **هزینهٔ رفت‌وبرگشت/Pool** را نشانه بگیرد (مثلاً MV/Pre-aggregation برای اولی، و افزایشِ آگاهانهٔ Pool/کاهشِ round-trip برای دومی — با اندازه‌گیری، نه حدس).

**مقیاسِ ملی = NOT-RUN:** هدفِ scale=0.01 (≈1.4GB) در سندباکسِ ~1GB RAM اجرانشدنی بود. برون‌یابی خطی فقط «تخمینی» است، نه measured.

## ۲) 🛑 مرزهای مالکیت و قواعدِ ضدتداخل (Rules of Engagement)

1. **ممنوعیتِ دستکاریِ اسنادِ حاکمیتی:** `docs/SUPERVISOR_DECISIONS_2026-09-13.md`،
   `.github/pull_request_template.md`، `docs/DOCUMENTATION_MAINTENANCE.md`،
   `docs/DOCS_FREEZE_*.md` و `tests/docs-freeze-marker.js` قلمروِ چت ۶ هستند — **ویرایش ممنوع**.
2. **عدمِ Rebase شاخه‌های دیگران:** PR #129 و #133 را rebase نکنید. کار را روی یک شاخهٔ **تازه** از
   `main@23bf513` شروع کنید (۹ PR باز همگی `DIRTY`اند؛ از آن‌ها شاخه نگیرید).
3. **مدیریتِ پچ‌ها:** هر فایلِ `*.patch` را با `git add -f` ثبت کنید — خط ۶۶ `.gitignore` آن‌ها را پنهان
   می‌کند (چت ۶ در این تله افتاد و کامنتِ #135 را مجبور شد تصحیح کند).
4. **محدودیتِ PG:** سندباکس فاقد باینری/پردازه/لیسنرِ PostgreSQL است.
   🔧 **`pg-mem` فقط برای صحتِ منطقی/کوئری است، نه برای اندازه‌گیریِ p95 یا هم‌زمانی** — هیچ عددِ کارایی را
   با pg-mem گزارش نکنید (fake-green). اعدادِ هم‌زمانی یا روی PG واقعی/استیجینگ measured است یا صریحاً
   **`NOT-RUN`** با دلیل.
5. **هرگز تستِ اجرانشده را سبز اعلام نکنید؛ هرگز «CI سبز» ادعا نکنید** (بیلینگِ Actions مسدود است).
6. **پوشهٔ `reza/`:** اگر سندِ پایهٔ معماری‌ای که آینه دارد (مثل `WAVE23_DB_NATIVE_REPORTS.md`) را تغییر
   دادید، آینه را همگام کنید و `node tools/reza-mirror-check.js` را سبز تحویل بدهید. **آینهٔ تازه نسازید.**
7. 🔧 **تلهٔ بامپِ هم‌زمان (جدید — مهم):** هر سندِ جدید/ویرایش در `docs/` ⇒ `DOCS_METRICS.md` و
   `DOCUMENTATION_MAP.md` (هر دو **فهرست‌شده در مانیفست**) تغییر می‌کنند ⇒ **`node tools/docs-stats-sync.js --freeze`
   + بامپِ قفل الزامی است**. اما ایجادِ فایلِ `rcNN+1` روی **دو شاخهٔ موازی** ⇒ تعارضِ قطعیِ مرج
  (همان اتفاقی که برای rc40 افتاد). بنابراین: ابزار را اجرا کنید، ولی **ساختِ فایلِ rc بعدی را با چت ۶
   هماهنگ کنید** تا یک نفر بامپ را انجام دهد.

## ۳) 🔧 اصلاحِ مقصدِ خروجی (مهم)

`docs/REPORT_PERF_GAP_SPEC.md` **روی `main` نیست** — فقط در **شاخهٔ PR #129**
(`feat/p03-reports-envfix-bounded-cache`، مالک: چت ۸/۳) وجود دارد. ویرایشِ آن = کار روی PRِ بازِ دیگری
و ساختِ کپیِ واگرا. بنابراین:

- ✅ **سندِ تازه بسازید** روی شاخهٔ خودتان: `docs/REPORT_CONCURRENCY_SPEC_2026-09-13.md`
  (و در صورت نیاز به‌روزرسانیِ مشخصه در زمانِ ACCEPTِ #129، با مالک هماهنگ کنید).

## ۴) خروجیِ مورد انتظار

1. 🔧 `docs/REPORT_CONCURRENCY_SPEC_2026-09-13.md` — راهکارِ انتخابی (MV دوره‌ای / Pre-aggregation /
   بهینه‌سازیِ Pool) با **اعدادِ واقعیِ هم‌زمانی** (و برچسبِ دقیقِ measured در برابر تخمینی).
2. در صورت تغییرِ Schema: مهاجرتِ **`011_*.sql`** — 🔧 **به‌همراه `011_*.down.sql`** و ثبت در جدولِ §۸
   راهنما (`node tools/migrate-helper.js --check` باید سبز بماند؛ شمارهٔ بعدیِ درست = 011).
   برای هر `NOT NULL` تازه از **`NOT VALID` سپس `VALIDATE`** استفاده کنید.
3. گزارشِ روزانه در `docs/daily-reports/2026-09-13-chat3-concurrency.md` — 🔧 **حتماً به یک سندِ دیگر
   ارجاع بدهید** وگرنه `docs-metadata` آن را «یتیم» گزارش می‌کند (چت ۶ این دور ۸ یتیم را بست).
4. 🔧 گیت‌های الزامیِ پیش از PR:
   `node tests/run.js` (۳۵/۳۵) · `node tests/smoke.js` (۵۴۷/۵۴۷ — نیازمند `npm i`؛ بدون jsdom خودش skip
   می‌کند و **skip ≠ سبز** است) · `node tests/secret-scan.js` (۱۱/۱۱) · `node build.js --check` ·
   `node tools/reza-mirror-check.js` · `node tools/migrate-helper.js --check` ·
   `node tools/docs-stats-sync.js --check` · `node tools/docs-metadata.js` (یتیم ۰)

## ۵) قلم‌های باز در زمینِ ناظر (برای آگاهی، نه اقدامِ شما)

1. **ACCEPT #129** (بازکنندهٔ ۸ ارجاعِ آگاهانه + دوزیه‌های Flag-1/Cache) — اولویتِ اول.
2. تصمیم دربارهٔ شکافتِ **#135** (پچ‌ها در `tools/audit-patches/` آماده‌اند؛ باز/بست/مرج فقط با مالک).
3. پیاده‌سازیِ حکمِ **counselor** (مالک: چت ۴/چت ۳ — نه شما در این دور).
4. رفعِ DIRTYِ ۹ PR باز (صفِ ادغام: چت ۵/۷).

---

### پیوست — SHAها و مراجع

| SHA/مرجع | معنا |
|---|---|
| `main@23bf513` | پایهٔ پیشنهادیِ شاخهٔ شما |
| `arena/01a09801-p2@b7880f6` | کارِ چت ۶ (اسنادِ تصمیم + پچ‌ها + rc42) |
| `docs/SUPERVISOR_DECISIONS_2026-09-13.md` §۱/§۲ | ثبتِ رسمیِ تصمیمِ دوزیهٔ کش + tenancy (منبعِ اعدادِ بالا) |
| `docs/WAVE3_QUERY_PERFORMANCE.md` §یافتهٔ ۱ | منبعِ عددِ tenancy (~۴۸۰× روی میانه) |
| `tools/bench-reports-concurrency.js` | بنچِ هم‌زمانی (در شاخهٔ #129؛ روی main نیست) |
