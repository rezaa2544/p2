# گزارش پیاده‌سازی سیستم ۳ تم و بهبود لوگو

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08)
**مجری:** آرنا

## ۱. تغییرات انجام‌شده

| فایل | نوع تغییر |
|------|-----------|
| `src/styles/base.css` | اضافه شدن استایل‌های ۳ تم (کلاسیک/مینیمال/لوکس) + بهبود نمایش لوگو (پایانِ فایل) |
| `src/js/99-theme-loader.js` | فایل جدید: اعمال تم ذخیره‌شده در شروع + تابع سراسری `switchPayeshTheme` |
| `src/js/_order.json` | اضافه شدن `99-theme-loader.js` به انتهای آرایه (ماژول ۷۶) |
| `index.html` | بیلدِ تازه (خروجیِ خودکارِ `node build.js`) |

## ۲. نتیجه تست‌ها (اندازه‌گیریِ واقعی)

| تست | نتیجه |
|-----|--------|
| `node build.js` | ✅ موفق — 76 ماژول، 3 فایل CSS، تک‌فایلی/آفلاین |
| `node tests/smoke.js` | ✅ 546/546 |
| `node tools/check-authz.js` | ✅ خروجی ۰ (تطبیق کامل مجوزها) |
| رجیسیونِ کاملِ 125 سویت (رانِرِ موازی) | ✅ 125/125 — ~۷ دقیقه |
| تأییدِ زندهٔ jsdom (سوئیچ/پایداری/توست/مدار) | ✅ 12/12 — پیش‌فرض theme-2 · سوئیچ به theme-3 با toast · تم ذخیره‌شده بعد از reload اعمال می‌شود · تم نامعتبر رد می‌شود |

## ۳. نحوه استفاده

- تم پیش‌فرض: `theme-2` (مدرن مینیمال)
- تغییر تم در کنسول مرورگر: `switchPayeshTheme('theme-3')`
- تم‌های موجود: `theme-1` (کلاسیک سازمانی — همان ظاهرِ پیشین)، `theme-2` (مینیمال)، `theme-3` (استودیو لوکس)

## ۴. نکات و انحرافاتِ ثبت‌شده

1. **دسترسی از طریقِ لایهٔ داده (انحرافِ لازم از متنِ پرامپت):** کدِ ارسالی `localStorage` را مستقیم صدا می‌زد؛ قاعدهٔ قفل‌شدهٔ پروژه (00-data-layer + نگهبانِ smoke) اجازهٔ آن را به هیچ ماژولِ دیگری نمی‌دهد — اجرایِ اولیه 545/546 داد و دقیقاً همین نگهبان شکست را نشان داد. حل: همان کلید (`payesh_ui_theme`) از طریقِ `Store.get/set` (لایهٔ داده) — رفتارِ کاربر نهایی یکسان + مقاومت در برابرِ حالتِ ناشناسِ مرورگر. smoke به 546/546 رسید.
2. **تم پیش‌فرض `theme-2`** به‌صورتِ صریحِ دستور: ظاهرِ پیش‌فرضِ همهٔ کاربران تغییر می‌کند (مینیمال + سایدبارِ تیره). بازگشت: `switchPayeshTheme('theme-1')` یا پاک‌کردنِ کلیدِ `payesh_ui_theme`.
3. **سازگاریِ انتخابگرها تأیید شد:** همهٔ 13 متغیرِ CSS (مثل `--primary` و `--radius`) در `:root` موجودند و همهٔ کلاس‌ها (`.card` ×346، `.sidebar` ×27، `.att-btn` ×21، `.brand-logo` ×4، `.login-logo` ×4) در بیلد یافت شدند — تم‌ها واقعاً اعمال می‌شوند، نه فقط نوشته شده‌اند.
4. **صفرِ تغییر در منطقِ کسب‌وکار:** فقط استایل + یک ماژولِ نمایشی؛ check-authz = 0، smoke = 546/546، رجیسیون = 125/125.
5. **آزمایشِ دائمیِ جدید در ریپو ساخته نشد** (مطابقِ ممنوعیتِ پرامپت: فقط ۳ فایل) — تأییدِ زنده با اسکریپتِ جدا (12 بررسی) انجام شد؛ در دورِ بعد در صورتِ خواسته، می‌تواند به `tests/theme.js` + جهش تبدیل شود.

## ۵. کامیت

- هش کامیت: `3b106a3`
- پیام: `feat: add 3 dynamic UI themes (classic, minimal, luxury) and enhanced logo styling`
- برنچ: `main` (push + ls-remote ✓)
