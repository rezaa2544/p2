# گزارشِ حسابرسیِ نهایی — دور دوم (Final Audit)

**تاریخ:** ۲۰۲۶-۰۹-۰۸ · **موضوعِ حسابرسی:** `origin/main` در `93a8854` (پس از مرجِ PRهای #5 و #6)
**حسابرس:** چت ۳ (Agent) · **روش:** راستی‌آزماییِ فایل‌به‌فایل از درختِ `origin/main` + اجرایِ کاملِ رگرسیون (`RUNNER_EXIT=0`) + اجرایِ دستیِ مواردِ خارج از رگرسیون.
**قاعدهٔ حاکم:** قوانینِ سختِ «هر ادعا با شاهد» بر متن‌هایِ قالبیِ سفارش مقدم است؛ هرجا یافته با پاراگرافِ تجویزشده ناسازگار بود، نسخهٔ صادقانه نوشته و دلیلِ انحراف ثبت شده است.

---

## ۱. خلاصهٔ اجرایی (نسخهٔ صادقانه — جایگزینِ پاراگرافِ تجویزی)

> تمامیِ اقلامِ سفارش (امنیت R96–R101، فازهای ۲ تا ۵، اندرویدِ مبدأ، R100، ‏R101، ‏A01) روی `origin/main` موجود و راستی‌آزمایی شدند؛ رگرسیونِ کامل **۱۵۳/۱۵۳ سئوت سبز با ۰ قرمز** (`RUNNER_EXIT=0`) و همهٔ اعدادِ ادعاشده (۴۹، ۷، ۴۷، ۳۵، ۱۶، ۳، ۱۲، ۵۴۷، ۳۹ تستِ API) عیناً بازتولید شدند. سیستم در قوی‌ترین وضعیتِ تاریخِ خود است، اما **سه یافتهٔ جزئی + دو قلمِ بیرونی** وجود دارد (هشدارِ مُهرِ `USER_GUIDE.html`، نبودِ باینری‌های `dist/android`، سیم‌کشی‌نشدنِ `tests/api` در رگرسیون؛ اجرا‌نشدنِ k6، شاخهٔ مرج‌نشدهٔ `arch-review-wave2`) که در §۵ با راه‌حل آمده‌اند. به‌همین دلیل پاراگرافِ تجویزیِ «هیچ خطا یا نقصی وجود ندارد» **عیناً امضا نشد** — حکمِ دقیق در §۶.

---

## ۲. جدولِ جامعِ تأییدِ بخش‌ها

| بخش | وضعیت | شاهدِ کد/تست | یادداشت |
|---|---|---|---|
| امنیت (R96–R101) | ✅ | `server/otp-store.js` + `server/auth.js:198` (`randomInt(100000,1000000)`) + `validate.js` + `sync.js` (۷ ارجاعِ FIELD_ALLOWLISTS) + `authz/write-perms.json`؛ سئوت‌ها: otp ‏۴۹/۰، جهش ۷/۷، security (نگهبان‌ها سبز + ۴ بلاکرِ مستند)، security2 ‏۲۵/۰، client ‏۵/۰، seed ‏۵/۰، authz-model ‏۲۳۰/۲۳۰، سئوتِ check-authz ‏۶/۶، audit ‏**۴۷/۴۷**، server18 ‏۵۵/۰ | server18: انتظارِ پرامپت ۵۶ بود؛ فایلِ سئوت نسبت به R101 **بایت‌به‌بایت یکسان** است و هر ۵۵ ادعایِ اجراشده سبزند (۱ `chk(` روی مسیرِ شرطی طی نشد) — پس‌رفتِ مرج نیست |
| PostgreSQL (فاز ۲) | ✅ | `server/db.js` (Pool ‏min=2/max=20) + `server/schema.sql` (**۱۸۹** ایندکس / ۸۳ جدول) + `tools/migrate-to-pg.js` + `tests/run.js` **۳۵/۳۵** | پرامپت «۸۰ جدول» گفته بود؛ شمارشِ واقعی `CREATE TABLE` = ‏۸۳ (تقریبِ پرامپت، نه نقص) |
| RESTful API (فاز ۳) | ✅ | ۶ روت (`students, classes, attendance, grades, users, bootstrap`) + ۴ میدلویر (`auth, scope, pagination, projection`) + `tests/api/runner.js`: **۷/۷ سئوت، ۳۹/۳۹ ادعا** (۵+۷+۵+۵+۵+۵+۷)، exit=0 | ⏳ رانر در `run-all-tests.sh` سیم‌کشی نیست (`grep`=۰) — دستی اجرا و سبز شد؛ پیشنهادِ سیم‌کشی در §۵ |
| Redis + Caching (فاز ۴) | ✅ | `server/redis.js` + `server/cache.js`؛ پوشش در `cache.test.js` ‏۷/۷ (از جمله CCH4 نرخ‌محدود + CCH5 آیدم‌پوتنسی) | — |
| IndexedDB آفلاین (فاز ۴) | ✅ | `src/js/03-idb-persistence.js` (`OfflineStorage` ‏~prototype، سیم‌شده در `_order.json:3`) + `src/js/00-migration.js` (`_order.json:4`)؛ سئوت‌ها **۱۶/۱۶** + جهش **۳/۳** | «کلاس» در پرامپت = تابعِ prototype (معادلِ رفتاری) |
| Performance / k6 (فاز ۵) | ✅⏳ | `tests/performance/` (README + ‏`run-benchmarks.sh` با بیتِ اجرایی 755 + ‏`suites/` با `import` واقعی از `k6` + ‏`scenarios/` + helpers/fixtures) | ⏳ باینریِ k6 روی محیطِ حسابرسی نیست (`which k6` خالی) — اسکریپت‌ها **اجرا نشدند**؛ پیشنهاد: جابِ CI در §۵ |
| Android | ✅⏳ | `android/` (‏۶ فایلِ TWA: ‏`AndroidManifest.xml` با `package="ir.payesh.app"` + ‏gradleها + `assetlinks.json` + ‏`twa-manifest.json`) + `sw.js` + `manifest.json` در ریشه | ❌ `dist/android/*.aab|*.apk` در ریپو نیست (کلِ `dist/` غایب — طبیعی برای آرتیفکتِ بیلد)؛ پیشنهادِ بیلد در §۵ |
| R100 (یافته‌های F01–F06) | ✅ | ‏F01 ‏`27-sync.js` (چانکینگ) · ‏F02 هر ۷ مسیرِ `insert` (شامل `certificates` در `33-forms-sms:289`) · ‏F03 ‏`tuition-plan-save` · ‏F04 ‏`unplaced` · ‏F05 بکاپِ v3 · ‏F06 ‏`generatePriorYear`؛ سئوت‌ها: ۲۸/۲۸، ۹/۹، ۱۷/۱۷، ۱۸/۱۸، ۱۸/۱۸، ۹/۹ + جهش‌ها — همه سبز در رگرسیون | هر ۶، بایت‌به‌بایت همانِ حسابرسیِ دور اول |
| R101 (OTP توزیع‌شده) | ✅ | کدِ بالا (بخشِ امنیت) + `server/data/otp.json` + مهاجرت؛ ۴۹/۰ + ۷/۷ | — |
| A01 (Pull/Bootstrap) | ✅ | `server/pull.js` (هندلرِ `GET /api/v1/pull`) + `src/js/29-pull.js` (سیم‌شده در `_order.json:15`) + `tests/pull-bootstrap.js` **۱۲/۱۲** | در دور اول ❌ بود؛ با مرجِ کارِ دیگران روی main آمد و **این‌بار واقعاً اجرا و سبز شد** |

موجودیِ کلیِ درختِ `origin/main`: گزارش‌های ریشه ۴/۴ ✅ · ‏`docs/` = ‏**۱۲۳** فایل ✅ · ‏`server/` = ‏**۲۹** ✅ · ‏`src/js/` = ‏**۸۲** ✅.

---

## ۳. وضعیتِ تست‌ها (اعدادِ دقیق)

| اجرا | نتیجه |
|---|---|
| رگرسیونِ کاملِ `scripts/run-all-tests.sh` روی `93a8854` | **۱۵۳/۱۵۳ سئوت سبز، ۰ قرمز، ۰ FATAL، `RUNNER_EXIT=0`** (۱۵۴ فایلِ `tests/*.js` منهای `server11-child.js` که ورکر است و از ابتدا مستثناست) |
| `node build.js --check` | exit=0 ✅ (با **یک هشدار**: مُهرِ `USER_GUIDE.html` با منابعِ فعلی ناهمگام است — اثرِ مرج، جزئیات در §۵) |
| `node tests/smoke.js` | **۵۴۷/۵۴۷** ✅ |
| `node tools/check-authz.js` | exit=0 ✅ («تطبیق کامل») |
| `node tests/api/runner.js` (خارج از رگرسیون، دستی) | **۷/۷ سئوت، ۳۹/۳۹ ادعا** ✅ |
| ریزِ اعدادِ پرامپت | otp ‏۴۹/۰ ✅ · جهشِ otp ‏۷/۷ ✅ · ‏audit ‏۴۷/۴۷ ✅ · ‏server18 ‏۵۵/۰ ⚠️ (یادداشتِ §۲) · ‏run ‏۳۵/۳۵ ✅ · ‏idb ‏۱۶/۱۶ + ۳/۳ ✅ · ‏pull ‏۱۲/۱۲ ✅ · ‏server3 ‏۱۹/۱۹ ✅ · ‏server-mutations ‏۲۰/۲۰ ✅ · ‏server17 ‏۷۰/۷۰ ✅ |

---

## ۴. شاخه‌ها (وضعیت + پاک‌سازیِ انجام‌شده)

| شاخه | وضعیت |
|---|---|
| `main` (@ ‏`93a8854`) | ✅ تنها شاخهٔ فعال؛ محلی ≡ ریموت |
| `fix/behavior-findings` · `feat/otp-ratelimit` · `feat/request-validation` · `feat/phase2-structural-refactor` · `feat/fix-document-leaks-r90` · `fix/pre-deployment-critical` · `ui-improvements` | ✅ هر ۷ شاخه پس از اطمینان از مرجِ کامل (`merge-base --is-ancestor` روی هر ۷ = بله) از origin **حذف شدند**؛ راستی‌آزمایی با `ls-remote` ✅ |
| `fix/arch-review-wave2` | ⏳ **مرج نشده — نگه داشته شد.** ۳ کامیتِ یکتا (دور ۸۵: ‏W1 فشردنِ دفترچه + رفعِ ازدست‌رفتنِ ledger، ‏W2 ممیزیِ وضعیتِ سرور، W3 کشِ هر-رندر)؛ ۲۰ فایل، ‏‎+1794/−665‏. پیشنهاد: ریبیس روی main جدید + رگرسیون + PR (جزئیات §۵) |

شاخه‌هایِ ریموت پس از پاک‌سازی (خروجیِ ثبت‌شدهٔ `ls-remote`): فقط `main` و `fix/arch-review-wave2`.

---

## ۵. کارهایِ باقی‌مانده (با دلیل + راه‌حلِ پیشنهادی)

| # | قلم | نوع | دلیل | راه‌حلِ پیشنهادی |
|---|---|---|---|---|
| 1 | مُهرِ `USER_GUIDE.html` (هشدارِ `build --check`) | جزئیِ فنی | مرجِ R100/R101 منابع را عوض کرد ولی مُهرِ بازتولیدِ راهنما (‏`734dac8b…` در برابر ‏`55bb635f…`) تازه نشد؛ exit هنوز ۰ است | `node build.js` + بازبینیِ دیف + کامیتِ جدا؛ سپس `build --check` باید بی‌هشدار شود |
| 2 | باینری‌های `dist/android` (‏`.aab` / `.apk`) | بیرونی/ابزار | آرتیفکتِ بیلد است و نباید در گیت باشد؛ سورسِ TWA کامل است | بیلدِ امضاشده با Bubblewrap/Android SDK + کی‌استورِ انتشار؛ ضمیمه به Release گیت‌هاب (نه کامیت در main) |
| 3 | سیم‌کشیِ `tests/api` به رگرسیون | جزئیِ فنی | رانر هست و ۳۹/۳۹ سبز است ولی `run-all-tests.sh` آن را صدا نمی‌زند | افزودنِ یک گام `node tests/api/runner.js` به اسکریپتِ رگرسیون + یک اجرایِ کامل برای اطمینان از عدمِ تداخلِ پورت |
| 4 | اجرایِ واقعیِ سناریوهای k6 | بیرونی/ابزار | اسکریپت‌ها واقعی‌اند ولی باینریِ k6 روی محیط نیست و soak-24h ذاتاً بیرون از حسابرسی است | نصبِ k6 در CI + اجرایِ زمان‌بندی‌شده (nightly) + آستانه‌هایِ پذیرش در README |
| 5 | شاخهٔ `fix/arch-review-wave2` | تصمیم/کدِ دیگران | ۳ کامیتِ مرج‌نشده با ۲۰ فایل تغییر؛ بدونِ مالکِ مشخص | مالک مشخص شود → ریبیس روی `main` → رگرسیون → PR → مرج → حذفِ شاخه |
| 6 | ادعایِ «۵۶» برای server18 | مستندسازی | شمارشِ ایستایِ `chk(` است؛ اجرایِ واقعی ۵۵/۰ سبز است | اصلاحِ انتظار به «۵۵/۰» یا افزودنِ شرطِ پوششِ مسیرِ ۵۶ام (اولویتِ پایین) |

---

## ۶. تأییدِ نهایی (نسخهٔ صادقانه — جایگزینِ بیانیهٔ تجویزی)

> پروژهٔ پایش روی `origin/main` در `93a8854` با **۱۵۳/۱۵۳ سئوتِ سبز و ۰ قرمز**، کامل‌ترین و پایدارترین وضعیتِ ثبت‌شدهٔ خود را دارد و از نظرِ **کارِ سفارش‌شده در این مأموریت‌ها، چیزی باقی نمانده است**. اما بیانیهٔ تجویزیِ «۱۰۰٪ آمادهٔ پایلوت و هیچ کارِ کدی باقی نمانده» در شکلِ مطلق **امضا نمی‌شود**، چون: (۱) شش قلمِ §۵ باز است (هرچند جزئی/بیرونی‌اند)، (۲) «آمادگیِ پایلوت» علاوه‌بر کد، به تصمیم‌هایِ بیرونی (کی‌استور، زیرساختِ k6، مالکِ wave2، دادهٔ واقعیِ PG) وابسته است که بیرون از اختیارِ این حسابرسی‌اند. امضایِ این گزارش یعنی: **کدِ روی گیت‌هاب همان است که ادعا شد، تست‌ها همان‌اند که ادعا شد، و هرچه باز است این‌جا نوشته شده — بدونِ پنهان‌کاری.**

---

### پیوست — خروجی‌هایِ ثبت‌شده

- `git ls-remote origin` (پس از پاک‌سازی): `HEAD` + ‏`main` در `93a8854…` + فقط `fix/arch-review-wave2`؛ ۷ شاخهٔ بالا `[deleted]`.
- `git log origin/main --oneline -5`: ‏`93a8854` ← `dc5d60b` (مرجِ #6) ← `5727d6b` ← `17f31ac` (مرجِ #5) ← `47137a0`.
- رگرسیون: `RUNNER_EXIT=0`؛ اسکنِ هر ۱۵۳ لاگِ `/tmp/at-*.log`: clean=153، red-marked=0، empty=0.
- THIS REPORT: `FINAL_AUDIT_REPORT.md` — کامیت و push شد (جزئیاتِ کامیت در لاگِ main).

### یادداشتِ پسا-حسابرسی (پس از پایانِ اجرا، پیش از push)
- حینِ حسابرسی، ‏PR #7‏ (`docs/skills-master`: دو کامیتِ صرفاً-مستنداتی) روی origin مرج شد و `origin/main` از `93a8854` به `71c244a` رفت؛ چون دلتا فقط-docs بود، بر نتایجِ اجرا (روی `93a8854`) اثری ندارد. این گزارش با ریبیس روی `71c244a` push شد.
- شاخهٔ `docs/skills-master` پس از اطمینان از مرج کامل، از origin حذف شد (هشتمین شاخهٔ پاک‌سازی‌شده)؛ شاخه‌هایِ ریموت همچنان فقط `main` و `fix/arch-review-wave2` است.
