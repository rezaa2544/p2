# دفترچهٔ تحویل کار — پایش

> **قاعده:** در پایان هر سشن کاری، پیش از بستن مکالمه، یک ورودی
> تازه **بالای** این فایل اضافه کنید (جدیدترین اول).
>
> پس از رسیدن به ۲۰ ورودی، قدیمی‌ترها به
> `docs/HANDOFF_ARCHIVE.md` منتقل می‌شوند.
>
> پیام کامیت: `docs: update HANDOFF.md with session summary [تاریخ]`

---

## Handoff — ۱۴۰۵/۰۶/۱۴ (۲۰۲۶-۰۹-۰۵) — از احمد به نفر بعدی

### چی انجام شد

**۱. تفکیک نقش سوپرادمین** — `src/js/30-authz.js` · کامیت `7ee6c25`

**۵۱ کنش مدرسه‌محور** از سوپرادمین گرفته شد (از ۸۸ کنش به ۳۷).

```
سوپرادمینِ خودش:  imp-commit=false · att-set=false · grade-save=false
از راه S.boss:     imp-commit=true  · att-set=true  · grade-save=true
```

**۲. طرح کلاس خودکار بر اساس زنگ** — `docs/PLAN_BELL_AUTOCLASS.md`
کد نوشته نشد؛ منتظر تأیید.

### تصمیمات مهم گرفته‌شده

**الف) معیار حذف: صفحه، نه سلیقه.** فقط کنش‌هایی حذف شدند که
صفحه‌شان در منوی سوپرادمین **نیست**. برای همین `user-save`,
`subject-save`, `ann-save`, `plan-save` **نگه داشته شدند** —
صفحه‌هایشان (`users`, `subjects`, `announcements`, `plans`)
واقعاً در منوی سوپرادمین هستند و حذفشان آن‌ها را می‌شکست.

⚠️ بررسی دور پیش گفته بود «۴۷ مورد»؛ عدد واقعی **۶۶ نامزد** بود
که ۵۱ تای آن‌ها حذف امن داشتند.

**ب) خط قرمز `S.boss` وارسی شد پیش از هر تغییری.** در
`19-actions.js:74` مقدار `S.user` **خودِ مدیر** می‌شود، پس
`activePersona()` هم `'manager'` برمی‌گرداند — یعنی جانشین واقعاً
به‌جای مدیر عمل می‌کند. حذف نقش از `ACTION_ROLES` آن مسیر را
نمی‌شکند.

**ج) پیش‌گزینش نرم تأیید شد، با یک افزوده:** پیش‌گزینش فقط وقتی
اعمال شود که مدرسه **واقعاً `bell_schedules` ثبت کرده باشد**.
دلیل: `bellOf()` نبودِ رکورد را با `BELL_PRESETS` جبران می‌کند،
یعنی ساعت **خیالی** برمی‌گرداند. بدون این شرط، مدرسه‌ای که
زنگ‌هایش را تنظیم نکرده با ساعت پیش‌فرض کار می‌کند و کاربر
نمی‌فهمد چرا کلاس اشتباه است.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### ⚠️ سه آزمون کهنه وارونه شدند

سه آزمون موجود انتظار داشتند سوپرادمین **داشته باشد**:
```js
['superadmin','imp-preview',true]    → حالا false
['superadmin','sms-topup-ok',true]   → حالا false
['superadmin','tr-send',true]        → حالا false
```
اینها معماری پیشین را تثبیت می‌کردند. به‌روز شدند با کامنت
توضیحی. **اگر کسی بعداً آن‌ها را عقب زد، یعنی تفکیک را نفهمیده.**

#### 🔴 یک جهش عمداً نگرفت — و درست است

جهش «`school-enter` نقش را عوض نکند» هیچ آزمونی را نینداخت. علتش
ضعف آزمون نیست: آن جهش فقط رفتار **UI** را عوض می‌کند نه منطق
مجوز. آزمون‌های ما `S.boss`/`S.user` را مستقیم می‌سازند، پس از
مسیر دکمه نمی‌روند.

**اگر کسی خواست کاملش کند:** آزمونی با کلیک واقعی روی
`data-act="school-enter"` لازم است. من ننوشتم چون آن کنش
`askConfirm` باز می‌کند و آزمونش پیچیده‌تر از ارزشش بود.

#### 🔴 ریسک شناخته‌شده در طرح زنگ (بخش ۶ سند)

دادهٔ نمونه فقط **۵ روز × ۴ زنگ** دارد، ولی `BELL_PRESETS`
پیش‌فرض **۹ بازه** دارد. یعنی اگر پیش‌گزینش پیاده شود، برای زنگ ۵
به بعد دبیر «الان زنگ درسی نیست» می‌بیند — فنی درست، ولی در دمو
گیج‌کننده. باید یا دادهٔ نمونه گسترش یابد یا الگو کم شود.

#### ⚠️ دام نگاشت روز هفته (پیش از پیاده‌سازی بخوانید)

`schedule.day` عددی ۰ تا ۴ است (شنبه تا چهارشنبه) ولی
`new Date().getDay()` یکشنبه‌محور است. نگاشتشان **یکی نیست** و
اشتباهش یعنی کلاس روز دیگری نمایش داده شود.

### قدم بعدی پیشنهادی

**منتظر تأیید رضا برای سه پرسش انتهای `PLAN_BELL_AUTOCLASS.md`:**
۱. افزودهٔ «فقط وقتی مدرسه زنگ ثبت کرده» پذیرفتنی است؟
۲. گام‌های ۱ و ۲ (فقط نمایش، ریسک صفر) جدا منتشر شوند؟
۳. دادهٔ نمونه گسترش یابد یا الگوی زنگ کم شود؟

**اگر تأیید آمد:** گام‌های ۱ و ۲ کم‌ریسک‌ترین شروع‌اند — نوار
«زنگ سوم — دهم تجربی» بدون تغییر هیچ رفتاری.

**کار باز از دور پیش:** مسیر معماری (الف/ب/ج در ضمیمهٔ الف
`ARCHITECTURE_DECISIONS.md`) هنوز تصمیم‌گیری نشده.

---

## Handoff — ۱۴۰۵/۰۶/۱۳ (۲۰۲۶-۰۹-۰۴) — از احمد به نفر بعدی

### چی انجام شد

**۱. دادهٔ نمونهٔ خانوادهٔ چندفرزندی** — `src/js/02-demo-data.js`
کامیت `cf36757`

| | پیش | پس (سنجیده) |
|---|---|---|
| توزیع فرزند/ولی | `{1: 521, 3: 1}` | **`{1: 309, 2: 103, 3: 3}`** |
| کل ولی | ۵۲۲ | ۴۱۵ |
| حجم `db` | ۴٫۷۵۵MB | **۴٫۶۹۷MB** |

انتخاب **قطعی** است نه تصادفی (هر دومین از هر چهارتا) چون
`generate()` با `SEED` ثابت اجرا می‌شود.

**۲ تا ۴: سه بررسی/سند** — کامیت بعدی. جزئیات پایین.

### تصمیمات مهم گرفته‌شده

**الف) ادغام خانواده حجم را کم می‌کند نه زیاد.** هر ادغام یک
رکورد `users` (۲۲۳ بایت) حذف می‌کند و فقط `parent_links`
(۵۹ بایت) می‌ماند. نگرانی سقف ۵MB بی‌مورد بود.

**ب) دو مورد چندفرزندیِ عمدی دست‌نخورده ماندند:**
`parent_multi` («کاظم رستمی») با فرزندان در سه مدرسه برای صفحهٔ
`family`، و یک کاربر نقش‌`teacher` با پیوند ولی. هر دو نام
خانوادگی و مدرسهٔ ناهمخوان دارند که **درست** است؛ آزمون‌ها
کنارشان می‌گذارند.

**ج) مسیر ج در تحلیل معماری، کم‌پشیمانی‌ترین است** — ولی تصمیم
نهایی با رضاست.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### 🔴 دام تازه: دیوار پرداخت آزمون ولی را دروغین می‌اندازد

آزمون «صفر دکمهٔ سوییچ» می‌گرفت در حالی که سنجش دستی **۲ دکمه**
نشان می‌داد. علت: **هیچ ولیِ دوفرزندی اشتراک فعال ندارد** —
`generateP10` اشتراک‌ها را پیش از تغییر من می‌سازد. بدون اشتراک،
`parentLocked()` صفحه را می‌بندد.

helper `withSubscribedParent()` در `tests/smoke.js` ساخته شد.

⚠️ **نکتهٔ ظریف:** `subOf()` با `find` نخستین رکورد را برمی‌دارد.
رکوردی که `push` کنید نادیده می‌ماند — باید ردیف‌های پیشین را
حذف و تازه را `unshift` کرد. دو بار گمراهم کرد.

#### ✅ اثر جانبی مثبت روی توزیع خودکار

`keepSiblings` (دور ۳۸) تا امروز روی دادهٔ نمونه **هرگز فعال
نمی‌شد** چون خواهر/برادری نبود. حالا **۲۶ خانوادهٔ هم‌پدر** فقط
در مدرسهٔ نخست هست. آزمون‌های توزیع را جداگانه سنجیدم:
**هیچ‌کدام نشکست** (۳۳/۳۳ ایستا، ۸۳/۸۳ صفحه).

#### 🔴 بررسی ۲: تفکیک نقش سوپرادمین — مغایرت جدی هست

**سوپرادمین ۸۸ کنش از ۹۳ کنش کل را دارد.** ۴۷ تای آن‌ها
مدرسه‌محورند و طبق تعریف درست باید فقط مال مدیر مدرسه باشند:

| دسته | نمونه |
|---|---|
| ورود اکسل | `imp-preview` · `imp-commit` · `imp-back` · `imp-reset` |
| استخراج | `export-csv` |
| تعریف کاربر | `user-save` · `user-del` · `user-toggle` |
| کلاس و درس | `class-save` · `class-del` · `subject-save` |
| آموزشی | `att-set` · `grade-save` · `disc-save` |
| پیامک | `sms-new` · `sms-send` · `sms-topup` · ۹ کنش `notify-*` |

**`ACTION_ROLES` هیچ کنشی ندارد که مدیر داشته باشد و سوپرادمین
نداشته باشد** — یعنی تفکیکی وجود ندارد.

**اما یک نکتهٔ مهم که شدت را کم می‌کند:** گارد **صفحه** سخت‌گیرتر
از گارد **کنش** است:

```
imp-commit:  کنش=true  صفحه(import)=false
export-csv:  کنش=true  صفحه(formssms)=false
att-set:     کنش=true  صفحه(attendance)=false
class-save:  کنش=true  صفحه(classes)=false
```

سوپرادمین این صفحه‌ها را در منو ندارد و مستقیم بازشان کند
«دسترسی مجاز نیست» می‌گیرد. پس در عمل نمی‌تواند اکسل وارد کند.

🔴 **ولی `canAction('imp-commit')` برایش `true` برمی‌گرداند.** اگر
کسی دکمه را دستی بسازد، گارد کنش جلویش را نمی‌گیرد. **دفاع تک‌لایه
است، نه دولایه.**

⚠️ **پیش از اصلاح، این را در نظر بگیرید:** سوپرادمین از راه
`S.boss` می‌تواند به نقش مدیر مدرسه برود (جابه‌جایی نقش). اگر
کنش‌های مدرسه‌محور را از او بگیرید، **باید مطمئن شوید آن مسیر
همچنان کار می‌کند** وگرنه پشتیبانی فنی فلج می‌شود.

**طبق دستور، اصلاح نکردم.** فقط گزارش.

#### 🔴 بررسی ۳: رفتار خودکار کلاس بر اساس زنگ — پیاده نشده

هر سه بخش خواسته‌شده **وجود ندارند**:

| خواسته | وضعیت |
|---|---|
| کلاس زنگ جاری خودکار نمایش داده شود | ❌ `S.filters.class \|\| cls[0].id` — همیشه کلاس اول |
| حضور/نمره محدود به کلاس زنگ فعلی | ❌ هیچ محدودیتی نیست |
| زنگ تفریح کلاس را ببندد | ❌ مفهوم «زنگ فعال» وجود ندارد |

**آنچه هست:** ماژول `43-bell-schedule.js` با `bellTimeline()` و
`bellTimeOf(schoolId, period)` که ساعت هر زنگ را برمی‌گرداند، و
جدول `schedule` با `{class_id, teacher_id, day, period}`.

🔴 **حلقهٔ مفقوده: هیچ‌جای برنامه ساعت جاری را با زمان زنگ‌ها
مقایسه نمی‌کند.** گرپ `getHours` فقط یک نتیجه دارد، آن هم در
`27-sync.js` برای نمایش زمان همگام‌سازی. `bellOf` بیرون از ماژول
۴۳ **اصلاً مصرف نمی‌شود**.

**برآورد کار: متوسط، ~۳ تا ۵ روز.**
۱. `currentPeriod(schoolId)` — ساعت جاری → شمارهٔ زنگ یا «تفریح»
۲. `teacherNowClass()` — تقاطع زنگ جاری با `schedule` دبیر
۳. پیش‌گزینش در `viewAttendance`/`viewGrades` + نوار «زنگ سوم —
   دهم تجربی»
۴. حالت تفریح: پیام «بین دو زنگ» به‌جای کلاس

⚠️ **دو تصمیم سیاستی لازم است پیش از کد:**
- آیا محدودیت **سخت** باشد (دبیر خارج از زنگ نتواند ثبت کند) یا
  فقط **پیش‌گزینش**؟ سخت‌گیری یعنی دبیری که غروب می‌خواهد غیبت
  صبح را وارد کند نمی‌تواند.
- ساعت **دستگاه کاربر** مبناست. اگر ساعت گوشی اشتباه باشد، کلاس
  اشتباه نشان داده می‌شود. بدون سرور راه دیگری نیست.

#### 📄 بررسی ۴: سند تحلیل معماری نوشته شد

ضمیمهٔ الف در `docs/ARCHITECTURE_DECISIONS.md`.

🔴 **یافتهٔ اصلی:** `officeScopeSchools()` در دمو کار می‌کند
**فقط چون همهٔ مدارس در یک `db` واحدند**. در استقرار واقعی هر
مدرسه `localStorage` جدا دارد و مرورگر رئیس اداره **هیچ راهی**
به دادهٔ مدارس ندارد. این باگ نیست — محدودیت ذاتی است.

سه مسیر با برآورد:

| | زمان | ریسک پروژه | حل مسئله | بار حریم خصوصی |
|---|---|---|---|---|
| **الف** سرور کامل | ۲–۴ ماه | 🔴 بالا | ✅ کامل | 🔴 سنگین |
| **ب** استخراج دستی | ۱–۲ هفته | 🟢 کم | ⚠️ نیمه | 🟡 متوسط |
| **ج** تجمیع خودکار آمار | ۳–۵ هفته | 🟢 کم | ✅ برای اداره | 🟢 ناچیز |

**مسیر ج** فقط شمارنده‌ها را می‌فرستد (نه نام، نه کد ملی، نه
نمره)، پس مسائل رضایت والدین اصلاً پیش نمی‌آید و اگر بعداً الف
لازم شد دورریز نیست.

⚠️ **در هر سه مسیر:** تا وقتی رمزها متن ساده است، هیچ داده‌ای
نباید از دستگاه خارج شود. حتی آمار تجمیعی به احراز هویت حداقلی
سمت سرور نیاز دارد وگرنه هر کسی می‌تواند آمار جعلی بفرستد.

### قدم بعدی پیشنهادی

**منتظر تصمیم رضا:**
۱. مسیر معماری (الف/ب/ج) — پرسش کلیدی: آیا در ۶ ماه آینده کاربر
   واقعی می‌آید؟
۲. تفکیک نقش سوپرادمین — با توجه به هشدار `S.boss`
۳. دو تصمیم سیاستی رفتار زنگ (محدودیت سخت یا نرم؟)

**کم‌ریسک و آمادهٔ اجرا:**
- افزودن `superadmin` به گارد صفحه‌های مدرسه‌محور **حذف نشود**،
  بلکه کنش‌ها هم‌تراز صفحه‌ها شوند (دفاع دولایه)
- `currentPeriod()` به‌تنهایی — بدون اعمال محدودیت، فقط نمایش
  «الان زنگ چندم است» در داشبورد دبیر

---

## Handoff — ۱۴۰۵/۰۶/۱۳ (۲۰۲۶-۰۹-۰۴) — از رضا به احمد

### 👋 احمد، از اینجا شروع کن

خوش آمدی. سه چیز را به‌ترتیب بخوان، بقیه را وقتی لازم شد:

| # | فایل | چرا |
|---|---|---|
| ۱ | `docs/AI_PROMPT.md` | **سند مرجع رسمی.** هر تصمیم و هر دام ثبت‌شده اینجاست. حجیم است؛ فهرستش را ببین و بخش مربوط به کارت را بخوان |
| ۲ | `CONTRIBUTING.md` | راهنمای عملی: چطور صفحه اضافه کنی، چطور آزمون بنویسی، چه دام‌هایی هست |
| ۳ | `docs/OPEN_ITEMS.md` | چه چیزی هنوز باز است (وارسی‌شده، نه از روی حافظه) |

**راه‌اندازی در سه دستور:**
```bash
cd /home/user/payesh
npm install jsdom --no-save     # هر نشست تازه لازم است
node build.js && npm test
```

### پنج اصلی که در هر تغییر رعایت می‌شوند

۱. **همگامی مستندات** — `docs/AI_PROMPT.md` و `CONTRIBUTING.md`
   در **همان نوبت** به‌روز شوند، با جزئیات: چرا آن تصمیم، چه
   عددی سنجیده شد، چه دامی هست.
۲. **تفکیک اصولی کد** — برنامه‌نویس تازه‌وارد بدون پرسیدن بخش
   موردنظرش را پیدا کند. نام فایل محتوایش را بگوید، هر صفحه یک
   تابع `view`، هر ماژول سرفصل فارسی.
۳. **گیت‌هاب همیشه به‌روز** — پس از هر تغییر پوش، و **حتماً** با
   `git ls-remote` تأیید کن (توضیحش پایین‌تر).
۴. **کمترین حجم ممکن** — ولی **هرگز** فایل اصلی/حساس حذف نشود.
   در صورت شک، نپرس‌ونکن؛ بپرس.
۵. **بازبینی امنیتی** — پیش از هر کامیت چهار پرسش
   (`CONTRIBUTING.md` بخش «اصل پنجم در عمل»).

⚠️ **قاعدهٔ نگارش:** همهٔ توضیح‌ها و اسناد **فارسی**. نام انگلیسی
فقط داخل قطعه‌کد، مسیر فایل یا سلول جدول.

### روال کاری که جواب داده

این ترتیب در ۴۴ دور کاری آزموده شده و **هر بار** باگ واقعی پیدا
کرده:

```
۱. کد موجود را بخوان (نه حدس بزن) — گرپ کن، اجرا کن
۲. طرح بنویس، تأیید بگیر
۳. کد بزن
۴. سنجش واقعی در jsdom — با شمارندهٔ خروجی، نه فقط نبود خطا
۵. آزمون دائمی بنویس
۶. 🔴 تست جهش: عمداً کد را خراب کن، ببین آزمون می‌افتد یا نه
۷. اسناد + کامیت + پوش + تأیید ls-remote
۸. ورودی HANDOFF
```

🔴 **گام ۶ را جدی بگیر.** بارها آزمونی نوشتیم که سبز بود ولی
**هیچ چیز نمی‌سنجید**. فقط تست جهش لوشان داد.

### 🔴 پنج دامی که بیشترین وقت را گرفتند

**۱. آزمونی که بی‌صدا رد می‌شود.** الگوی `if (r.skipTest) return;`
باعث شد آزمونی ماه‌ها سبز باشد و هرگز اجرا نشود. اگر ناگزیری،
دست‌کم چاپش کن.

**۲. آزمون و کد از یک منبع می‌خوانند.** انتظار آزمون را از **یک
لایه پایین‌تر** بساز، نه از همان تابعی که می‌سنجی.

**۳. `git push` پیام «Everything up-to-date» می‌دهد وقتی کامیت
شکست خورده.** همیشه با `git ls-remote` تأیید کن.

**۴. هویت گیت در هر نشست تازه پاک می‌شود.** اگر
`fatal: not a git repository` دیدی، مخزن سالم است — فقط
`.git/config` و `.git/refs/` گم شده‌اند. رفعش در
`CONTRIBUTING.md` بخش «پشتیبان مستقل از `.git`».

**۵. `node_modules` بین نشست‌ها نمی‌ماند.** نشانه: تست دودی بی‌صدا
رد می‌شود با کد خروج **صفر**.

📄 فهرست کامل دام‌ها: `CONTRIBUTING.md` و بخش «Errors» در
`docs/AI_PROMPT.md`.

---

### چی انجام شد (خلاصهٔ کل پروژه تا امروز)

پروژه یک **سامانهٔ مدیریت مدرسه** است که خروجی نهایی‌اش همیشه یک
`index.html` تک‌فایلی است (~۱٫۱MB، ۴۶ ماژول، صفر وابستگی تولیدی).

**وضعیت امروز:** ۸۳ صفحه · ۶ نقش · ۲۰ آزمون ایستا + ۳۶۴ آزمون
دودی · بیلد بیت‌به‌بیت تکرارپذیر.

#### فازهای اصلی (۴۴ دور کاری)

| دوره | چه ساخته شد |
|---|---|
| ۱–۸ | هستهٔ سامانه: نقش‌ها، کلاس‌ها، نمرات، حضور، مالی، خانواده |
| ۹–۱۳ | پنل سوپرادمین، سابقهٔ تغییرات، پلان فروش، پشتیبان‌گیری |
| ۱۴–۱۹ | اشتراک مشترک پدر/مادر، چرخهٔ سال تحصیلی، اتصال گیت‌هاب |
| ۲۰–۲۷ | شاخه/رشتهٔ متوسطه دوم، لایهٔ offline-first، دیاگ خودکار |
| ۲۸ | **فاز دمو:** جداسازی لایهٔ داده، `esc()` سراسری، `TODO_BEFORE_PRODUCTION.md` |
| ۲۹–۳۵ | ممیزی امنیت، قرارداد سرور، تصمیم‌های معماری |
| ۳۶–۳۹ | **کارایی:** توزیع خودکار کلاس، ورود اکسل ۲۱× سریع‌تر |
| ۴۰–۴۱ | خودآزمایی ۳۲ بررسی، دادهٔ نمونهٔ جلسات و زنگ، بازسنجی ظرفیت |
| **۴۲** | **اطلاع‌رسانی پیامکی — ۹ گام کامل** |
| **۴۳** | اختیار مدیر، نمودار روند نمرات، پیام اداره، بازچینش منو |
| **۴۴** | سابقهٔ چندساله + یادداشت خصوصی دبیر |

#### چهار دستاورد که بیشترین ارزش را داشتند

**۱. کارایی ورود اکسل: ۲۷۱۹ms → ۱۲۹ms (۲۱×).**
`buildClassIndex` + `placeIdx` + رفع باطل‌سازی ایندکس در هر درج.
جزئیات: `docs/CAPACITY_SIM.md`.

**۲. سامانهٔ پیامک با چهار لایهٔ دفاعی** (دور ۴۲، ۹ گام):
پیش‌نویس حضور و غیاب → مرور نهایی → پنجرهٔ مهلت ۲۰ دقیقه →
اصلاحیهٔ خودکار. طرح کامل: `docs/SMS_NOTIFY_PLAN.md`.

**۳. باگ دفترچهٔ تغییرات.** `applyOp` شیءِ **زندهٔ** رکورد را در
دفترچه می‌گذاشت، پس هر `update` گذشته را بازنویسی می‌کرد. یعنی
صفحهٔ «سابقهٔ تغییرات» **از روز اول** داده‌های نادرست نشان می‌داد.
رفع: رونوشت سطحی در `03-persistence.js`.

**۴. ۲۱ جهش عمدی در سه دور اخیر** — ۱۸ گرفته شد، ۳ شکاف واقعی در
آزمون‌ها لو داد.

#### فایل‌های کلیدی

```
index.html              خروجی توزیعی — 🔴 هرگز مستقیم ویرایش نشود
build.js                node build.js  ·  --check صحت بیت‌به‌بیت
src/js/_order.json      ترتیب الزامی ۴۶ ماژول
src/js/00-data-layer.js Store/Api/Data — تنها دروازهٔ حافظه
src/js/01-helpers.js    esc() خط ۸ · escAttr() خط ۹
src/js/03-persistence.js دفترچهٔ تغییرات + batchWrites
src/js/30-authz.js      ACTION_ROLES — هر کنش تازه اینجا ثبت شود
src/js/44-sms-notify.js اطلاع‌رسانی پیامکی (۱۴ بخش)
src/js/45-teacher-tools.js سابقهٔ چندساله + یادداشت خصوصی
tests/run.js            ۲۰ آزمون ایستا (سریع، بدون jsdom)
tests/smoke.js          ۳۶۴ آزمون دودی (jsdom)
```

### تصمیمات مهم گرفته‌شده

**۱. فاز دمو، نه تولید.** کاربر تصریح کرد بک‌اند کامل و هش رمز در
این مرحله لازم نیست. اما چهار اصل رعایت می‌شود: لایهٔ داده جدا،
`esc()` همه‌جا، هیچ دادهٔ واقعی، و `TODO_BEFORE_PRODUCTION.md`.

**۲. عدد نمره در پیامک نمی‌آید.** تصمیم اخلاقی: پیامک روی گوشی
باز، جلوی چشم خواهر/برادر یا همکلاسی، مسئلهٔ کرامت دانش‌آموز است.
ولی با ورود به سامانه عدد دقیق را می‌بیند.

**۳. سقف روزانهٔ پیامک: ترمز نه دیوار — مگر در حالت خودکار.** در
مسیر دستی مدیر آگاهانه رد می‌شود؛ در حالت خودکار کسی نیست که
تصمیم بگیرد، پس دیوار است.

**۴. پیش‌نویس حضور و غیاب در `Store` است نه `S`.** تعویض کلاس یا
بستن مرورگر کار دبیر را نمی‌برد. کلید `uid|cid|date` — هر سه جزء
لازم است (با جهش تأیید شد).

**۵. اصلاحیه بر پایهٔ «آخرین پیام ارسال‌شده» است نه «پیام اصلی».**
در سناریوی غایب→حاضر→غایب، با مبنای پیام اصلی خانواده با اطلاع
غلط می‌ماند.

**۶. یادداشت خصوصی دبیر با مدرسه می‌ماند نه دبیر.** و هشدار صریح
دارد: «خصوصی یعنی ولی نمی‌بیند، نه اینکه در بازرسی قابل مطالبه
نباشد.»

**۷. سابقهٔ چندساله فقط نمرات و حضور — بدون انضباط.** تا دبیر با
قضاوت پیشینی وارد کلاس نشود.

📄 ۴۴ تصمیم معماری در `docs/ARCHITECTURE_DECISIONS.md`.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### 🔴 وضعیت امنیتی: NOT SAFE FOR PRODUCTION (آگاهانه)

چهار مسدودکننده که **عمداً** بازند (تصمیم کاربر، فاز دمو):

| بند | مورد |
|---|---|
| ۱.۱ | رمزها متن ساده‌اند — bcrypt فقط در کامنت |
| ۱.۲ | احراز هویت سمت مرورگر |
| ۱.۳ | `localStorage` به‌جای پایگاه دادهٔ واقعی |
| ۱.۴ | بررسی نقش فقط سمت مرورگر |

**جداسازی کاربران: FAIL.** سه آزمون IDOR در `tests/smoke.js`
عمداً *وجود* آسیب‌پذیری را تأیید می‌کنند و **روز اتصال سرور باید
معکوس شوند**. `byId` محدوده نمی‌سنجد.

⚠️ اگر روزی یکی از آن سه آزمون خودبه‌خود شکست، یعنی کسی محدوده
را درست کرده — آزمون را به‌روز کن، تغییر را عقب نزن.

#### محدودیت‌های فنی

**حجم `db` = ۴٫۷۵MB از سقف ~۵MB مرورگر.** هر افزایش دادهٔ نمونه
باید سنجیده شود. دو بار از سقف رد شدیم و برگرداندیم.

**سندباکس کند است.** تست دودی گاهی ۶ تا ۱۲ دقیقه می‌گیرد، ولی
`user 13s` در برابر `real 6m31s` نشان می‌دهد پردازنده بیکار است —
مشکل از اشباع میزبان است نه کد. آزمون ایستا در کمتر از یک ثانیه
اجرا می‌شود؛ برای بازخورد سریع از آن استفاده کن.

**منوی مدیر با ۲۶ آیتم به مرز رسیده.** پیش از افزودن سومین آیتم
تازه، گروه‌ها باید بازبینی شوند.

#### بخش‌های تست‌نشده

- **مرورگر واقعی:** همهٔ سنجش‌ها در jsdom است. `Store.available()`
  در jsdom `false` می‌شود و یک بررسی دیاگ کاذبانه بحرانی است.
- **چاپ:** `printableDoc()` پنجرهٔ تازه باز می‌کند؛ در jsdom
  آزموده نشده.
- **همگام‌سازی با سرور:** `Api` و `enqueueOp` نوشته شده‌اند ولی
  سروری پشتشان نیست.

#### یک مورد بلاتکلیف

**`New folder.rar` (۳٫۷MB)** در ریشهٔ پروژه. در گیت نیست و روی
گیت‌هاب هم نه. `.gitignore` می‌گوید محتوایش در `reference/`
استخراج شده ولی آن پوشه وجود ندارد. **تصمیمش با رضاست** — تا آن
موقع طبق اصل چهارم دست نزن.

### قدم بعدی پیشنهادی

**احمد، اگر می‌خواهی گرم شوی، از این‌ها شروع کن (کم‌ریسک):**

۱. **دادهٔ نمونهٔ خانوادهٔ چندفرزندی.** از ۵۲۲ ولی، فقط **یکی**
   چند فرزند دارد. قابلیت سوییچ بین فرزندان کار می‌کند ولی در
   دمو تقریباً نامرئی است. کار کوچکی است در `02-demo-data.js`.

۲. **دو مورد باقی‌ماندهٔ کشف‌پذیری** از
   `docs/DISCOVERABILITY_R43.md`: برچسب پویای «پروندهٔ N فرزند» و
   روشن‌کردن تفاوت `family` و `children`.
   ⚠️ ریسک دارد: `allowedRoutes()` از `NAV` ساخته می‌شود، پس حذف
   آیتم از منو **دسترسی مسیر را قطع می‌کند**.

**کارهای بزرگ‌تر که منتظر تصمیم رضا هستند:**

- **#۹ تکالیف کلاس** — پرسش باز: آیا چیزی جدا از اطلاعیه است؟ اگر
  فقط «اطلاعیهٔ کلاس‌محور با تاریخ تحویل» باشد، دو فیلد به
  `announcements` بسیار ارزان‌تر از جدول تازه است.
- **#۱۰ راهنمای انتخاب رشتهٔ نهم** — من پیشنهاد دادم **نسازیم**.
  مسئلهٔ محتوایی است نه فنی؛ اطلاعات هدایت تحصیلی باید منطبق با
  آیین‌نامهٔ روز باشد وگرنه بر آیندهٔ دانش‌آموز اثر می‌گذارد.

**و یک کار که هر وقت خواستی:** پوشش آزمون برای مرورگر واقعی
(نه jsdom). امروز کور است.

---
