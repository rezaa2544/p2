# دفترچهٔ تحویل کار — پایش

> **قاعده:** در پایان هر سشن کاری، پیش از بستن مکالمه، یک ورودی
> تازه **بالای** این فایل اضافه کنید (جدیدترین اول).
>
> پس از رسیدن به ۲۰ ورودی، قدیمی‌ترها به
> `docs/HANDOFF_ARCHIVE.md` منتقل می‌شوند.
>
> پیام کامیت: `docs: update HANDOFF.md with session summary [تاریخ]`

---

## Handoff — ۱۴۵/۰۶/۱۵ (۲۰۶-۰۹-۰۵) — نویسنده: هوش مصنوعی — دایره‌های آنالوگِ سلامت

### چی انجام شد

* با دستور رضا: پارامترهای اندازه‌گیریِ صفحهٔ سلامت به شکل
  عقربه/آنالوگ/دماسنجی، با انتخاب شیوه توسط کاربر و گرافیک جالب.
* کارت «دایره‌های آنالوگ» بالای صفحهٔ سلامت: ۴ دایره (حافظه، نرخ
  اصابت ایندکس با منطقِ رنگِ معکوس، صف ارسال، کل رکوردها) + سه
  رندرکنندهٔ SVG خالص: عقربه‌ای (ناحیه‌های رنگی + جارؤ انیمیشنی
  عقربه)، آنالوگ (قوسِ سرگرد + نقطهٔ دمیده)، دماسنجی (ستون مایع +
  حبابِ نبض‌دار). انتخاب در payesh_health_gauge_v1 ماندگار؛ مقادیر
  خارج از حد سنجاق می‌شوند؛ prefers-reduced-motion رعایت شد.
* کد: ۳۵-superadmin.js (رندرکننده‌ها + کارت)، ۱۹-actions.js
  (health-gauge-style)، base.css (کلاس‌های hgt-* + ۴ انیمیشن).
* سئوت: ۲ سئوت تازه در smoke (شیوه‌ها + ماندگاری + سنجاق‌سازی).

### وضعیت سئوت‌ها

* دودی ۵۰۱/۵۰ · ایستا ۳/۳۳ · شبیه‌سازی ۳۴/۳۵ (bell-del) ·
  پنتست ۱۰/۱۰ + ۴ سروری · دیاگ ۱۰/۱۰
* ماتاسیون: صفرکردن زاویهٔ عقربه → سئوت قرمز (کشته شد)

### کدهای کامیت (همه push + ls-remote شده)

| کامیت | موضوع |
|---|---|
| b25df1f | دایره‌های آنالوگ + سئوت + سند |

### چه چیزی باز است (برای رضا)

1. انتخاب شیوه حالا سراسری است — خواستی هر دایره شیوهٔ جداگانه
   داشته باشد، بگو (یک کنشِ کوچک دیگر).
2. (قدیمی: bell-del — F-۱؛ بیلد سروری؛ سقف لاگِ خطا.)

### نکته برای ادامه

* رندرکننده‌های دایره فقط از HGT_TONES و hgtArcPath استفاده می‌کنند؛
  افزودن شیوهٔ چهارم = یک تابع + یک ردیف در HGT_STYLES.

---

## Handoff — ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۹-۰۵) — نویسنده: هوش مصنوعی — دیاگ فوق‌هوشمند (موتور خودتعمیر)

### چی انجام شد

* با دستور رضا: «دیاگ فقط نشان می‌دهد کجا خطاست؛ می‌خواهم فوق‌هوشمند باشد:
  خطاهای کل سامانه را تشخیص دهد و رفع کند، حتی مشکل کدنویسی؛ سناریوهای
  مشکل و نحوهٔ رفع را تعریف کند — داخل برنامه و ارتباط سرور/هاست/زیرساخت.»
* **پاسخ اسکرین‌شات:** دو هشدار = (۱) ۱۸ کلاس با رشته‌ای که مدرسه‌اش
  اعلام نکرده (ناتوانی مولد دمو در تطبیق رشته با شاخه‌های مدرسه)،
  (۲) ۱۷ کاربر مدیریتی با رمز پیش‌فرض ۱۲۳۴۵۶. هر دو حالا رفع خودکار دارند.
* **موتور خودتعمیر** در ۴۲-self-diagnostics.js:
  - ۱۲ رفع خودکارِ تازه/بهبودشده؛ مهم‌تر: تکمیل فهرست رشتهٔ مدرسه،
    رمز تازه + اعلان به حساب، ظرفیت = واقعیت.
  - **تاریخچهٔ تعمیرات پایدار** با «حالت قبل» هر رکورد + دکمهٔ بازگردانی.
  - **دام‌گیر خطای زمان‌اجرا** (syslog پایدار) — خطای کد دیگر در
    بی‌خبری نمی‌ماند.
  - **پنهان‌سازی خودکار گزینهٔ شکستهٔ منو** (route/عنوان/مجوز) +
    نوار هشدار + دکمهٔ بازگردانی؛ تازه‌بارگیری برای توابع گمشده.
  - **خانوادهٔ سوم زیرساخت** (۶ آزمون): حالت اجرا، پروب /api/health
    با تأخیر/نسخه/اختلاف ساعت، آنلاین/آفلاین، حجم کل حافظه. در حالت
    تک‌فایل صادقانه «اجرا نمی‌شود» — آمادهٔ اولین اتصال به سرور.
  - **کتابچهٔ عملیات درون‌برنامه**: ۳۹ سناریو با «وقتی/رفع خودکار/
    نحوهٔ رفع/مسئول».
  - رفع باگ: آزمون رشته حالا فقط مدرسه‌های با فهرست صریح را می‌سنجد
    (کلاس‌های «عمومی»ِ مدارس عادی دیگر صوری علامت نمی‌خورند).
* **لایهٔ داده:** Store.keys() و httpGetJson() افزوده شد — لمسِ
  حافظه/شبکه فقط در لایهٔ داده (قاعدهٔ دودی).
* **سئوت جدید:** tests/diag2.js — ۱۰ سناریو (هر عیب: ساخت → تشخیص →
  تعمیر → راستی‌آزمایی داده → بازگردانی). smoke هم ۲ سئوت تازه گرفت
  و هارنسش Promise-aware شد.

### وضعیت سئوت‌ها

* دودی ۴۹۹/۴۹ · ایستا ۳۳/۳۳ · شبیه‌سازی ۳۴/۳۵ (bell-del) · پنتست
  ۱۰/۱۰ + ۴ سروری · موتور خودتعمیر ۱۰/۱۰
* ماتاسیون: غیرفعال‌کردن رفع رمز → D2 قرمز (کشته شد)

### کدهای کامیت (همه push + ls-remote شده)

| کامیت | موضوع |
|---|---|
| b96f432 | دیاگ فوق‌هوشمند + سئوت + اسناد |

### چه چیزی باز است (برای رضا)

1. **بیلد سروری:** پروب‌ها آماده‌اند — زمان شروع سرور/API رسیده؟
2. **بایگانی لاگ خطا به فایل:** سقف ۱۰۰ در مقیاس مدارس کافی؟
3. (قدیمی: bell-del — F-۱؛ auto-login — SEC-۳.)

### نکته برای ادامه

* ۴۲-self-diagnostics.js حالا ~۱۹۰۰ خط است؛ آزمون‌ها را با id بشناسید.
* diagFix/diagFixAll/Promise می‌دهند — در سئوت await کنید.
* منو از navFor می‌آید؛ گزینهٔ پنهان‌شده فقط از منو می‌رود، روتش هنوز
  قابلِ دسترسی مستقیم است (مستند در کتابچه).

---

## Handoff — ۱۴۰۵/۰۶/۱۵ (۲۰۲۶-۰۹-۰۵) — نویسنده: هوش مصنوعی — پنتست (فرض: نسخهٔ سروری)

### چی انجام شد

* با دستور رضا: «ببین کجاها از بیرون می‌توان نفوذ پیدا کرد؛ باگ‌های
  امنیتی را پیدا کن؛ اگر می‌توانی راه‌ها را ببند؛ گزارش را بده.»
* **tests/security.js (جدید):** ۱۴ سناریوی exploit-driven روی باندل
  واقعی در jsdom — ۵ سطح بیرونی (فرم ورود، auto-login، جعل S.user،
  اعتبارنامه در باندل، برون‌فهمی)، ۷ fuzz سیاه‌باکس XSS ذخیره‌شده با
  نگهبان صوری‌نبودی، ۲ نشت داده. دو دسته: client (باید سبز؛ کد خروج
  با قرمزش می‌شکند) و server (بلوک‌کننده‌های شناخته‌شده با exploit
  زنده — تا کار سروری قرمز می‌مانند).
* **۲ رفعِ امنیتی (SEC-۱ و SEC-۲):**
  ۱. پیش‌پر superadmin/123456 از فرم ورود حذف شد (یک کلیک = ورود
     سوپرادمین). دکمه‌های «حساب‌های نمونه» دست‌نخورده ماندند.
  ۲. buildBackup() کلید password را در عملیات کاربران به [redacted]
     تبدیل می‌کند؛ localStorage و بازیابیِ همان مرورگر دست‌نخورده است.
* **ماتاسیون-تست:** برداشتن esc از عنوان اطلاعیه → سناریوی B1 قرمز
  شد (نگهبان زنده است)؛ کد برگشت.
* **گزارش کامل:** docs/REPORT_2026-09-05_SECURITY.md — جدول یافته‌ها
  با exploit و وضعیت + ۴ تصمیم خودم + ۴ سؤال باز برای رضا.

### وضعیت سئوت‌ها

* پنتست: سمت کاربر ۱۰/۱۰ سبز + ۴ بلوک‌کنندهٔ سروریِ مستند
* دودی: ۴۹۷/۴۹۷ · ایستا: ۳۳/۳۳ · شبیه‌سازی: ۳۴/۳۵ (قرمزِ
  تنها = باگ عملکردیِ معلوم bell-del، بدون تغییر)

### کدهای کامیت (همه push + ls-remote شده)

| کامیت | موضوع |
|---|---|
| 685429f | پنتست + ۲ رفع + گزارش + اسناد |

### چه چیزی باز است (برای رضا)

1. **SEC-۳:** auto-login از localStorage — بماند یا reload با رمز؟
   (نسخهٔ سروری با کوکی HttpOnly خودبه‌خود حل می‌شود.)
2. **بلوک‌کننده‌های سروری (SEC-۳ تا ۶):** عین TODO ۱٫۲/۱٫۴/۲٫۲ —
   پیشنهاد: یک دور اختصاصی «آماده‌سازی بیلد سروری» (flag حذف دمو،
   حذف auto-login در بیلد server).
3. **CSP nonce / clickjacking:** بند 🔴 ۲٫۶ برای بیلد — چه زمانی؟
4. (قدیمی: باگ عملکردی bell-del — F-۱ شبیه‌سازی.)

### نکته برای ادامه

* tests/security.js را کنار smoke هر بار که به ورود/نشست/خروجی
  (پشتیبان) دست زد، اجرا کن.
* در سئوت‌های jsdom، رندر کامل با render() است (نه renderRoute) —
  محتوا در div.content بدون id می‌نشیند؛ آزمون‌ها باید حضور محتوا را
  در DOM راستی‌آزمایی کنند تا صوری نشوند.
* پنتست، فرم ورود را پس از سناریوی برون‌فهمی «وارد» می‌گذارد —
  سناریوهای بعدی با نشست زندهٔ سوپرادمین ادامه دارند.

---

## Handoff — ۱۴۰/۰۶/۵ (۲۰۲-۰۹-۵) — نویسنده: هوش مصنوعی — شبیه‌سازی جامع (در غیاب رضا)

### چی انجام شد

* رضا در دسترس نبود → به‌جای توقف، شبیه‌سازی جامع اجرا شد:
  `tests/simulation.js` — ۳۵ سناریو روی باندل واقعی در jsdom (بوت،
  ورود ۷ نقش، IDOR، جریان‌های روزمره، حالت‌های زنگ، چالش‌ها).
  نتیجه: **۳۴/۳۵ سبز** — تنها قرمز، یافتهٔ واقعی است.
* یافته‌ها (گزارش کامل: `docs/REPORT_2026-09-05_SIMULATION.md`):
  - **P1 (تأییدشده):** `bell-del` بازهٔ اشتباه را حذف می‌کند —
    `data-i` روی دکمه است ولی اکشن از ردیف می‌خواند → همیشه بازهٔ
    اول همان روز. اصلاح یک‌خطی ماتاسیون-تست‌شده (با اعمال ۳۵/۳۵)؛
    طبق تصمیم «غیاب رضا = نپچ»، کد دست‌نخورده ماند.
  - **P3:** فیلتر دبیر در `viewGrades` با `|| true` خنثی است (۸۴
    ردیف خودش / ۵۸۸ دبیر دیگر — نشت بین‌مدرسه‌ای نیست).
  - **P3 (رفتار ثبت‌شده):** `manager6` (مدرسهٔ غیرفعال) وارد می‌شود —
    ورود `school.active` را چک نمی‌کند.
* ماتاسیون-تست شبیه‌سازی: سه ماتاسیون (اصلاح bell-del، شکستن
  نگهبان IDOR، شکستن esc) — همه کشته شدند؛ بعد از برگشت، اپ
  دست‌نخورده (smoke ۴۹۷/۴۹۷ · run ۳۳/۳۳ · index.html بیت‌به‌بیت).
* مستندات در همان دور: AI_PROMPT (بند شبیه‌سازی جامع)،
  CONTRIBUTING (فهرست تست‌ها + اعداد به‌روز ۳۳/۴۹۷)، DEVELOPMENT
  (اجرای شبیه‌سازی).
* کامیت `008f8b7` — پُش و تأیید ls-remote شد.

### قدم بعدی پیشنهادی

1. **F-1 (bell-del):** اصلاح یک‌خطیِ تأییدشده را اعمال کنیم؟
   (با آن شبیه‌سازی ۳۵/۳۵ می‌شود.)
2. **F-2:** حذف `|| true` در `viewGrades`؟
3. **F-3:** مسدود کردن ورودِ مدرسهٔ غیرفعال یا حفظ رفتار کنونی؟
4. بازهای کهنه بی‌تغییر: پاسخ Claude Code برای مهارت‌های ① و ③،
   `New folder.rar`، معماری چندمدرسه‌ای، اتصال gradeModal به زنگ جاری.

---

## Handoff — ۱۴۰۵/۰۶/۱۵ (۲۰۲۶-۰۹-۰۵) — نویسنده: هوش مصنوعی — اصل تازه: گزارش‌های تجمیعی به‌صورت فایل

### چی انجام شد

دستور صریح رضا: **«گزارش رو تو فایل بذار. این اصل همیشه یادت
باشه.»**

* گزارش تجمیعی این دور به فایل درآمد:
  `docs/REPORT_2026-09-05_SKILLS.md` (کامیت `9b8af88`) — نخستین
  گزارش فایلی؛ پیش‌تر گزارش هفت‌بند هم فایلی بود
  (`docs/REPORT_2026-09-05.md`) ولی «همیشه بودنش» ثبت نشده بود.
* اصل در سه جا ماندگار شد:
  1. `docs/AI_PROMPT.md` — اصل نهم: گزارش تجمیعی همیشه در
     `docs/REPORT_*.md` + commit/push؛ چت فقط اشارهٔ کوتاه؛
     نقطهٔ مبنای گزارش‌های بعدی به `9b8af88` به‌روزرسانی شد.
  2. `.claude/skills/payesh-standards/SKILL.md` — بند تازه در
     «فرمت گزارش پایان هر سشن».
  3. همین ورودی HANDOFF.
* مرز اصل: **گزارش تجمیعی** («گزارش کلی») فایلی است؛ گزارش
  کوتاه پایان هر پاسخ (اصل هفتم) همان‌طور که همیشه بود، در چت
  می‌ماند.

### قدم بعدی پیشنهادی

همان: پاسخ سؤال Claude Code (احمد/رضا) برای دو اسکیل باقی‌مانده
(superpowers، frontend-design) + سایر موارد باز گزارش.

---
## Handoff — ۱۴۰۵/۰۶/۱۵ (۲۰۲۶-۰۹-۰۵) — نویسنده: هوش مصنوعی — دو اسکیل اختصاصی پایش

### چی انجام شد

بعد از گزارش Agent Skills، تصمیم مشترک: **دو اسکیل اختصاصی** طبق
ترتیب پیشنهادی ساخته شدند. superpowers و frontend-design **ساخته
نشده‌اند** — منتظر جواب سؤال «آیا ریپو با Claude Code هم کار می‌شود
یا فقط Arena؟» (از احمد/رضا).

**۱. `payesh-standards` — کامیت `8e98e3b`:**
* `.claude/skills/payesh-standards/SKILL.md` — قوانین ثابت پروژه
  در یک فایل: قانون طلایی «تأیید کورکورانه نکن» + ۱۵ قانون
  اجباری (تست جهش اجباری با روال بکاپ/جهش/بیلد/smoke/بازگردانی،
  اجرای واقعی در jsdom، esc()/escAttr همه‌جا، داده فقط از راه
  journal/applyOp، token هرگز در فایل پروژه، تاریخ ISO/جلالی،
  index.html دست‌نخورده (خروجی بیلد)، کامیت جدا + push +
  ls-remote برای هر تغییر، مستندات در همان کامیت، HANDOFF بالای
  فایل، **بازبینی فساد اعداد فارسی با کد‌پوینت**، New folder.rar
  دست‌نخورده، دمو فقط با add()، بدون فریم‌ورک، کنترل کلاینتی =
  مجوز واقعی نیست) + چهار نقض شناخته‌شدهٔ دور ۲۸.
* frontmatter با کلمه‌های محرک برای Claude Code (smoke.js،
  build.js، applyOp، esc، commit، push، HANDOFF، docs).
* ارجاع در `docs/AI_PROMPT.md` (جدول اسکیل‌ها + بند کوتاه) تا در
  Arena هم به‌عنوان مرجع شناخته شود.

**۲. `security-review-payesh` — کامیت `913afc0`:**
* `.claude/skills/security-review-payesh/SKILL.md` — بازبینی
  امنیتی **کلاینت‌ساید** (واقعیت پروژه: وانیلی، localStorage،
  authz شبیه‌سازی‌شده):
  * اصل حاکم (الگوی Dilaz): **«بدون exploit کارکردن، یافته
    نداریم»** — exploit = آزمون jsdomِ اجراپذیر در smoke.js که
    آسیب‌پذیری را واقعاً نشان می‌دهد (همان فلسفهٔ تست جهش)؛
  * چک‌لیست اجباری هفت‌ماده: نشت بین‌مدرسه‌ای (فیلتر school_id /
    کوئری‌های visible*، PoC با کاربر مدرسهٔ ۱ روی دادهٔ مدرسهٔ ۲)،
    esc/escAttr روی همهٔ ورودی (PoC با مقدار `<img onerror>`)،
    نبود token/رمز در فایل، یکپارچگی journal/applyOp، ورودی
    اکشن‌های تازه در 30-authz (بدون authz = P0)، مرز نقش روی
    نماها، ورودی به journal مطابق تأیید UI؛
  * روش ۹ مرحله‌ای security-expert به‌عنوان مکمل؛
  * فهرست «نقض‌های شناخته‌شده و عمداً پذیرفته‌شده» دور ۲۸
    (bcrypt، احراز سرور، دیتابیس واقعی، بررسی نقش سمت سرور —
    در TODO_BEFORE_PRODUCTION.md) مستقیماً از AI_PROMPT کپی
    شد تا هشدار کاذب ندهد؛
  * فرمت یافته: شدت P0–P3 + **exploit اجباری** + حلقهٔ بسته
    (آزمون exploit پس از فایکس سبز بماند).
* بند تازه در جدول اسکیل‌های AI_PROMPT.

### تصمیمات مهم گرفته‌شده

* **superpowers و frontend-design منتظر ماندند** — تصمیم با سؤال
  Claude Code گره خورده (در Arena، روال superpowers نهادینه است
  و frontend-design نیازش را payesh-standards بهتر پوشش می‌دهد).
* **قالب پرامپت، نه GitHub Action:** خودِ Action نیاز به
  workflow PR + سرور + CLAUDE_API_KEY دارد («روز سرور»)؛ قالب
  قابل‌حمل همان است که ساخته شد.
* **الگوی Dilaz از سه اسکیل امنیتی جامعه انتخاب شد** — تنها
  گزینه‌ای که مفهوم exploit با harnessِ jsdom ما می‌خواند
  (jeffallan = SAST‌محور، joaquimscosta = وابسته به subagent).
* **جدول اسکیل‌های AI_PROMPT برای هر دو به‌روز شد** (ارجاع برای
  payesh-standards صریحاً خواسته شده بود؛ برای security-review
  هم به‌دلیل یکپارچگی ثبت شد).

### مشکلات باز / نکات برای نفر بعدی

* **سؤال اصلی:** آیا ریپو با **Claude Code** هم کار می‌شود؟ جواب
  بله ⇒ زیرمجموعهٔ superpowers (TDD/دیباگ/verification/
  anti-patterns؛ ۳۰–۶۰ دقیقه) و تطبیق frontend-design
  (۶۰–۹۰ دقیقه) فعال می‌شوند. جواب «فقط Arena» ⇒ هر دو بی‌ارزش
  می‌مانند (محتوایشان در AI_PROMPT کافیه).
* بقیهٔ موارد باز همان‌اند: `New folder.rar`، معماری چندمدرسه‌ای
  (الف/ب/ج)، data-i حذف زنگ، وصل gradeModal به زنگ.
* دو اسکیل جدید در Arena **خودکار فعال نمی‌شوند** — با ارجاع
  (جدول AI_PROMPT) می‌خواند؛ در Claude Code از روی description
  خودکار می‌شوند.

### قدم بعدی پیشنهادی

۱. گرفتن جواب سؤال Claude Code (احمد/رضا).
۲. هر مورد باز دیگر که رضا دستور دهد.

---
## Handoff — ۱۴۰/۰۶/۵ (۲۰۲۶-۰۹-۰۵) — نویسنده: هوش مصنوعی — طرح زنگ، گام ثبت نمره

### چی انجام شد

بعد از گزارش تجمیعی فهرست روزانه، کار روی طرح زنگ ادامه پیدا کرد:
**گام ۴ — پیش‌گزینش ثبت نمره** (بعد از «continue» رضا). کامیت
`5845a2c` (پیش‌تر `aace241`).

* `viewGrades` (13-grades): همان الگوی گام ۳، با یک مهم‌تر —
  **درس هم** از برنامهٔ هفتگی پیش‌گزینش می‌شود (`subject_id` همان
  ردیف روز×زنگ). منبع واحد همان `bellAutoClass` است.
* **قاعدهٔ جفت از یک منبع:** پیش‌گزینشِ درس منوط به
  `!S.filters.class` است — اگر دبیر کلاسی دستی انتخاب کرده، درس
  زنگِ قبل نمی‌چسبد.
* **نشان** «🔔 انتخاب خودکار بر اساس زنگ» فقط وقتی **هر دو** (کلاس
  و درس) خودکار باشند؛ انتخاب دستیِ یکی، نشان را خاموش می‌کند.
* **خروج:** دکمهٔ «همهٔ کلاس و درس» = `grade-reset-auto`
  (19-actions + 30-authz: teacher/manager) — کلاس به اولین کلاس
  قابل‌دید + درس به «همه دروس» (مقدار خالی، نه شناسهٔ قبلی).
* ۴ آزمون تازه + ۵ جهش گرفته‌شده: تقدم فیلتر کلاس، حذف
  پیش‌گزینش درس، نشان با انتخاب دستیِ درس، روز هفته (هستهٔ
  مشترک با گام ۳)، جابجایی کلاس/درس.
* مستندات: AI_PROMPT §۵.۳۸ · CONTRIBUTING (بخش تازه) ·
  PROJECT_NOTES.
* **وضعیت پایانی:** smoke ۴۹۳/۴۹۳ · static ۳۳/۳۳ · build --check سبز ·
  push و ls-remote تأیید شد.

### تصمیمات مهم گرفته‌شده

**الف) درس پیش‌گزینش می‌شود ولی کورکورانه نه:** پیش‌گزینشِ درس
فقط وقتی که کلاس هم از زنگ آمده باشد (جفت از یک منبع). ترکیب
«کلاس دستی + درس خودکار» عمداً ساخته نمی‌شود.

**ب) نشان یعنی «هر دو خودکار است»:** اگر یکی دستی باشد، نشان
خاموش است ولی همان نیمهٔ خودکار هنوز اعمال می‌شود (کلاس خودکار
با درسِ دستی می‌ماند).

**ج) ریست، درس را «همه دروس» می‌کند نه «همان درس قبلی»:** وگرنه
بعد از ریست، فیلتر درس پنهان می‌ماند و کاربر فکر می‌کند هنوز
خودکار است.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

* 🔴 **دام پنل فیلتر:** انتخابگر «درس» داخل `filterPanel` است که
  پیش‌فرض **بسته** است (`S.fopen[route]` در 25-filters). پیش‌گزینش
  روی ردیف‌ها اثر دارد، ولی انتخابِ درس فقط وقتی پنل باز باشد دیده
  می‌شود. آزمون‌ها `S.fopen.grades=true` می‌گذارند — بدون آن،
  انتخابگر اصلاً در HTML نیست.
* **فرم «ثبت نمره» (gradeModal) هنوز از زنگ استفاده نمی‌کند** —
  پیش‌گزینش فقط در نماي فهرست است (همان دامنهٔ طرح). اگر خواستید
  دکمهٔ «ثبت نمره» هم کلاس/درس را از زنگ بگیرد، کار جداست.
* **مغازهٔ سبزِ گام ۶ طرح:** بند ۵ (آزمون/جهش) با همین بندها پوشیده
  شد؛ بند ۶ (اسناد) انجام شد؛ قلاب 27-sync هنوز لازم نیست (هیچ
  داده‌ای نوشته نمی‌شود).
* بقیهٔ موارد باز همان‌اند که در ورودی قبل بودند: گام بعدی طرح
  ندارد (۴ گام طرح زنگ تا اینجا: ۱، ۲، ۳، ۴ — همه ✅)؛ پرسش‌های
  گزارش تجمیعی (Skills، `New folder.rar`، معماری، data-i) منتظر
  رضا.

### قدم بعدی پیشنهادی

۱. هر کدام از پرسش‌های باز که رضا جواب بدهد.
۲. در غیر این صورت، کاندیدای طبیعی بعدی: وصل کردن فرم «ثبت
   نمره» به زنگ جاری (دامنهٔ تازه، نیاز به تصمیم رضا) یا هر
   فهرست روزانهٔ جدید.

---

## Handoff — ۱۴۰۵/۰۶/۱۵ (۲۰۲۶-۰۹-۰۵) — نویسنده: هوش مصنوعی — بند ۶ (وصل گام ۳ زنگ)

### چی انجام شد

بند ۶ فهرست روزانه: **گام ۳ طرح زنگ — پیش‌گزینش حضور و غیاب** پیاده
شد و push شد. کامیت `9e5bb3d` (پیش‌تر `0885928`).

* `bellAutoClass(teacherId, schoolId, now)` در 46-bell-now — منبع واحد،
  روی `teacherNowClass`. `null` یعنی رفتار قدیم: مدرسه
  `bell_schedules` ندارد، زنگِ حال درس نیست (تفریح/پیش از شروع/پایان/
  تعطیل)، یا دبیر در آن روز و زنگ کلاسی ندارد.
* `viewAttendance` (12-attendance): انتخاب دستی **همیشه** مقدم است —
  `S.filters.class` پیش از زنگ خوانده می‌شود. بدون انتخاب دستی و وقتی
  کلاس زنگِ جاری در کلاس‌های قابل‌دید دبیر است، آن کلاس پیش‌گزینش
  می‌شود + نشان آبی «🔔 انتخاب خودکار بر اساس زنگ».
* `att-reset-class` (19-actions + 30-authz: teacher/manager) — دکمهٔ
  «همهٔ کلاس‌ها»: فیلتر کلاس را روی اولین کلاس قابل‌دید می‌گذارد
  (انتخاب صریح). مقدار خالی بی‌مفید است چون پیش‌گزینش را دوباره
  تحریک می‌کند.
* **باگ بند ۲ رفع شد:** `currentSlot` بدون روز هفته از
  `bellTimeline` صدا می‌زد و خط زمانی همهٔ روزها شنبه بود — حالا
  `out.day` منتقل می‌شود.
* ۵ آزمون تازه (زمان‌دار با `S.bellNow` + ساخت روز هفته در سمت
  صفحه) + ۴ جهش گرفته‌شده: اولویت فیلتر، روز هفته، hasSchedule،
  جابجایی classId/subjectId.
* مستندات: AI_PROMPT §۵.۳۷ · بخش تازهٔ CONTRIBUTING ·
  PROJECT_NOTES بند ۶.
* **وضعیت پایانی:** smoke ۴۹۳/۴۹۳ · static ۳۳/۳۳ · build --check سبز ·
  push و ls-remote تأیید شد.

### تصمیمات مهم گرفته‌شده

**الف) خروج از خودکار = انتخاب صریح، نه «پاک کردن» فیلتر:** مقدار خالی
پیش‌گزینش را همان لحظه دوباره تحریک می‌کرد و دکمهٔ «همهٔ کلاس‌ها»
بی‌اثر می‌شد؛ پس اولین کلاس قابل‌دید گذاشته می‌شود (انتخابی که دبیر
می‌بیند و می‌تواند عوض کند).

**ب) `clock.ok` در `bellAutoAllowed` لایهٔ تکراری است (جهش مرده):** حذف
آن هیچ آزمونی نمی‌افتد — چون `currentSlot` برای ساعت نامعتبر
`kind:'unknown'` برمی‌گرداند و `teacherNowClass` فقط lesson می‌پذیرد.
به‌عنوان بیمهٔ دوگانه ماند و در مستندات ثبت شد.

**ج) باگ روز هفتهٔ بند ۲ در همین بند رفع شد** (نه بند جدا) — چون
مستقیماً گام ۳ را می‌شکست (مثلاً خط زمانی چهارشنبه به‌جای خودش،
شنبه را می‌خواند).

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

* **گام ۴ طرح (پیش‌گزینش ثبت نمره) شروع نشده** — بند بعدی
  `PLAN_BELL_AUTOCLASS.md` (فایل 13-grades). الگو آماده است: همان
  `bellAutoClass` را در نماي نمره صدا بزنید؛ انتخاب دستی همان‌جا هم
  مقدم باشد.
* بند ۵ و ۶ طرح (آزمون/جهش و مستندات) با بند ۶ این فهرست پوشیده شد؛
  قلاب 27-sync معنا ندارد چون گام ۳ داده نمی‌نویسد.
* data-i برای bell-del هنوز باز است (از سشن‌های پیش).
* گزارش Agent Skills تحویل شد — تصمیم افزودن اسکیل با رضاست؛ هیچ
  تغییری به‌خاطرش نشده است.

### قدم بعدی پیشنهادی

۱. **گام ۴:** پیش‌گزینش ثبت نمره (همان الگو، در نماي نمره).
۲. یا هر بند بعدی که رضا در فهرست روزانه بدهد.

---

## Handoff — ۱۴۰۵/۰۶/۱۵ (۲۰۲۶-۰۹-۰۵) — نویسنده: رضا — ده‌مرحله‌ای

### چی انجام شد

**ابهام «دو رشتهٔ موازی» حل شد.** با تاریخچهٔ گیت تأیید شد:
**گزینهٔ (الف)** درست است — هر ۴ کار احمد **تمام شده بود**، بعد
پروژهٔ زنگ شروع شد. ترتیب کامیت‌ها:

```
cf36757  چندفرزندی        ← کار ۱ احمد
7ee6c25  تفکیک سوپرادمین  ← کار ۲ احمد (اصلاح شد نه فقط بررسی)
a899143  زنگ گام ۱
527d949  ضمیمهٔ ب معماری
474f618  زنگ گام ۲ ← این نشست
```

**کار واقعی این نشست: مرحلهٔ ۲** — گام ۲ زنگ + دادهٔ ۶ زنگ.
کامیت `474f618`.

### تصمیمات مهم گرفته‌شده

**الف) نوار زنگ شش حالت دارد و در سه حالت عمداً خالی است:**
کاربر دبیر نیست · مدرسه `bell_schedules` ندارد · (ساعت نامعتبر
⇒ به‌جای زنگ، **هشدار**).

**ب) `bellNowBar(now)` پارامتر زمان می‌گیرد** — همان الگوی بقیهٔ
ماژول. بدون آن آزمون به ساعت واقعی تکیه می‌کرد و جمعه‌ها متفاوت
می‌شد.

**ج) دادهٔ نمونه ۶ زنگ شد نه ۵** — الگو ۵ زنگ درسی دارد؛ ۶
حاشیهٔ امن می‌دهد اگر مدرسه‌ای الگوی بلندتر تعریف کند.
`schedule`: ۷۲۰ → **۱۰۸۰** ردیف · حجم `db`: **۴٫۷۲۵MB**.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### ✅ مرحلهٔ ۷ (پیگیری `applyOp`) — پوشش کامل تأیید شد

هشت جدول جدا سنجیده شد، **همه ✅ گذشته محفوظ**:
`grades · discipline · transactions · users · installments ·
attendance · teacher_notes · notify_queue`

⚠️ و یک نکتهٔ ظریف که تأیید شد: رونوشت **پیش از** `enqueueOp`
اجرا می‌شود، پس صف همگام‌سازی سرور هم دادهٔ درست می‌گیرد.

#### 🔴 دام که این نشست خوردم

اسکریپت سنجش من از `global` استفاده کرد که در jsdom وجود ندارد
(`ReferenceError`). به‌جای دور زدنش با ترفند، **کد محصول را
آزمون‌پذیر کردم**: `bellNowBar(now)` پارامتر گرفت. راه‌حل بهتری
بود چون آزمون دائمی هم از همان استفاده می‌کند.

#### ⚠️ گام ۳ و ۴ هنوز شروع نشده‌اند

طرح `PLAN_BELL_AUTOCLASS.md` شش گام دارد:
| گام | وضعیت |
|---|---|
| ۱ هستهٔ زمان | ✅ دور ۴۸ |
| ۲ نوار وضعیت | ✅ دور ۵۰ |
| **۳ پیش‌گزینش حضور و غیاب** | ⏸️ منتظر تأیید |
| **۴ پیش‌گزینش ثبت نمره** | ⏸️ پس از ۳ |

گارد `bellAutoAllowed()` از قبل آماده است و هنوز مصرف‌کننده
ندارد — گام ۳ باید از آن استفاده کند.

### قدم بعدی پیشنهادی

**۱. تأیید گام ۳** (پیش‌گزینش واقعی در حضور و غیاب). یادآوری
تصمیم تأییدشده: **پیش‌گزینش نرم** — `S.filters.class` همیشه بر
پیش‌گزینش مقدم است.

**۲. کارهای بازِ دورهای پیش:**
- مسیر معماری (الف/ب/ج ضمیمهٔ الف) — تصمیم‌گیری نشده
- `New folder.rar` (۳٫۷MB) — تصمیمش با رضاست

---

## Handoff — ۱۴۰۵/۰۶/۱۴ (۲۰۲۶-۰۹-۰۵) — بررسی معماری — به رضا و احمد

### چی انجام شد

پاسخ به سه پرسش مهندس ارشد امنیت دربارهٔ مدل داده تک‌مدرسه‌ای در
برابر چندمدرسه‌ای. **فقط تحلیل — هیچ کدی تغییر نکرد.**

سند تازه: `docs/ARCHITECTURE_DECISIONS.md` **ضمیمهٔ ب**

### تصمیمات مهم گرفته‌شده

🟢 **مدل داده از همین حالا چندمدرسه‌ای است — نگرانی موضوعیت ندارد.**

| شاهد سنجیده‌شده | نتیجه |
|---|---|
| جدول‌های دارای `school_id` | **۳۲ از ۴۳** |
| شناسه‌ها | **سراسری یکتا** — مدرسه ۱ کلاس ۱-۳، مدرسه ۲ کلاس ۱۰-۱۲ |
| سلسله‌مراتب | `province_id/county_id/district_id` روی **۱۰۰٪** مدارس |
| کوئری‌ها | هر سه تابع اصلی فیلتر `school_id` دارند |

🟢 **مهم‌ترین یافته:** `nextId` **سراسری** است نه per-school. اگر
per-school بود، دو مدرسه هر دو `classes.id=1` می‌ساختند و روز
ادغام در پایگاه دادهٔ مرکزی برخورد شناسه رخ می‌داد. این دقیقاً
همان چیزی است که نگرانش بودند — و از قبل درست است.

🔴 **`org_id` رد شد.** سلسله‌مراتب موجود همان کار را می‌کند.
افزودنش **دو منبع حقیقت** می‌سازد (اگر `org_id` با `district_id`
نخواند، کدام برنده است؟) و تا روز سرور مصرف‌کننده‌ای ندارد.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### 🔴 نقطهٔ قفل واقعی: ذخیره‌سازی، نه مدل داده

```js
const LOG_KEY = 'sms_log_v1';   // یک کلید برای همهٔ مدارس
```

در دمو همهٔ مدارس در یک `db` اند پس تجمیع کار می‌کند؛ در استقرار
واقعی هر مدرسه `localStorage` جدا دارد و
`officeScopeSchools()` **صفر** برمی‌گرداند.

⚠️ **ولی این بازنویسی مدل داده نمی‌خواهد.** روز سرور فقط `Store`
به `fetch` تبدیل می‌شود — همان چیزی که `00-data-layer.js` از دور
۲۸ برایش طراحی شد. سه مسیر حل در ضمیمهٔ الف.

#### دو نکتهٔ کوچک

- `superadmin.school_id === null` است و کد تحملش می‌کند (۱۹ صفحه
  سالم). یعنی «کاربر بدون مدرسه» از قبل مفهوم معتبری است.
- `schoolStudents` گارد `null` صریح ندارد — آرایهٔ خالی می‌دهد نه
  خطا. قابل قبول، ولی بهتر است روزی صریح شود.

#### ⚠️ کارهای متوقف‌شده هنوز منتظرند

این بررسی جای گام ۲ را نگرفت. همچنان منتظر تأیید:
- **گام ۲ نوار وضعیت زنگ**
- **گسترش دادهٔ نمونه به ۶ زنگ** (تأییدشده، اجرا نشده)

### قدم بعدی پیشنهادی

**🟢 پاسخ به پرسش تعیین‌کنندهٔ مهندس امنیت: کارهای بعدی روی همین
مسیر ادامه یابند. اصلاح زودهنگام لازم نیست.**

سپس همان دو کار متوقف‌شده (گام ۲ و دادهٔ نمونه).

---

## Handoff — ۱۴۰۵/۰۶/۱۴ (۲۰۲۶-۰۹-۰۵) — تعویض شیفت — از احمد به نفر بعدی

### چی انجام شد

**گام ۱ طرح زنگ جاری: هستهٔ زمان** — کامیت `a899143`
فایل تازه: `src/js/46-bell-now.js` (پروژه به **۴۷ ماژول** رسید)

هفت تابع: `todayIndex` · `isSchoolDay` · `clockSanity` ·
`clockWarnText` · `currentSlot` · `teacherNowClass` ·
`bellAutoAllowed`.

⚠️ **این ماژول هیچ رفتاری را عوض نمی‌کند** — فقط می‌گوید «الان چه
خبر است». هیچ صفحه‌ای هنوز از آن استفاده نمی‌کند.

### تصمیمات مهم گرفته‌شده

**الف) نگاشت روز هفته: `(getDay() + 1) % 7`**

دو شمارش متفاوت در پروژه هست:
```
Date.getDay()  :  یکشنبه=۰ · دوشنبه=۱ … شنبه=۶
schedule.day   :  شنبه=۰   · یکشنبه=۱ … چهارشنبه=۴
```
عمداً **همان فرمولی** را برداشتم که از قبل در
`22-jalali-calendar.js:43` برای نام روز به‌کار رفته — تا دو منطق
موازی نداشته باشیم. هر هفت روز جدا سنجیده شد ✅.

**ب) مرز بازه `[from, to)` است** — شامل شروع، بدون پایان. با مرز
بسته، لحظهٔ دقیق پایان زنگ به **دو** بازه تعلق می‌گیرد و نتیجه به
ترتیب حلقه بستگی پیدا می‌کند.

**ج) سه لایهٔ دفاع در برابر ساعت اشتباه:**
۱. سال خارج از ۲۰۲۴–۲۰۳۵ ⇒ `kind:'unknown'` + هشدار فارسی
۲. `hasSchedule` — گارد تصمیم سیاستی رضا: پیش‌گزینش فقط وقتی
   مدرسه **واقعاً** `bell_schedules` دارد
۳. قلاب `SERVER_TIME_KEY` (آستانهٔ ۱۰ دقیقه) — **پیاده نشده**،
   فقط آماده است برای روز اتصال سرور

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### 🔴 گام ۲ و گسترش دادهٔ نمونه انجام **نشده‌اند**

در دستور رضا هر سه کار بود، ولی خودش نوشته بود «بعد از گام ۱،
همون‌جا گزارش بده و **منتظر تأیید برای گام ۲ بمون**». پس عمداً
متوقف شدم. باقی‌مانده:

| کار | وضعیت |
|---|---|
| گام ۱ هستهٔ زمان | ✅ تمام |
| **گام ۲ نوار وضعیت زنگ** | ⏸️ منتظر تأیید |
| **گسترش دادهٔ نمونه به ۶ زنگ** | ⏸️ منتظر تأیید |
| گام ۳ و ۴ (پیش‌گزینش واقعی) | ⏸️ پس از تأیید گام ۲ |

#### ⚠️ ناسازگاری دادهٔ نمونه با الگوی زنگ (هنوز باقی است)

`02-demo-data.js:127` فقط **۵ روز × ۴ زنگ** می‌سازد، ولی
`BELL_PRESETS` پیش‌فرض **۵ زنگ درسی** دارد (سنجش: ۹ بازه شامل
تفریح‌ها، `07:30` تا `12:00`).

یعنی زنگ ۵ هیچ کلاسی در جدول ندارد و `teacherNowClass` برای آن
`null` می‌دهد — فنی درست، ولی در دمو گیج‌کننده. **رضا تأیید کرده
که دادهٔ نمونه به ۶ زنگ گسترش یابد** (نه کاهش الگو).

⚠️ پس از آن تغییر، `renderRoute()` روی هر ۸۳ صفحه باید دوباره
اجرا شود.

#### 🔴 یک جهش که نگرفت و شکاف واقعی بود

حذف شرط `r.day === slot.day` از `teacherNowClass` هیچ آزمونی را
نمی‌انداخت. سناریوی واقعی: دبیری که فقط شنبه کلاس دارد، یکشنبه
هم همان کلاس را می‌دید.

آزمون تازه‌ای نوشتم که شنبه و یکشنبه را مقایسه می‌کند و حالا
می‌گیرد. **درس:** آزمون «کلاس درست برگشت» کافی نیست؛ باید «کلاس
روز دیگر برنگشت» هم سنجیده شود.

#### ⚠️ آزمون‌های وابسته به زمان

همهٔ آزمون‌های این ماژول با تاریخ **ثابت** (`2026-09-05`، شنبه)
کار می‌کنند. هرگز `new Date()` واقعی در آزمون نگذارید — نتیجه
جمعه‌ها یا شب‌ها متفاوت می‌شود. همهٔ توابع `46-bell-now.js`
پارامتر اختیاری `now` می‌گیرند دقیقاً برای همین.

### قدم بعدی پیشنهادی

**۱. تأیید رضا برای گام ۲** (نوار وضعیت زنگ در داشبورد دبیر).
ریسکش تقریباً صفر است — فقط نمایش، هیچ رفتاری عوض نمی‌شود.

**۲. گسترش دادهٔ نمونه به ۶ زنگ** — تأیید شده، فقط منتظر ترتیب
اجراست. تغییر کوچکی در `02-demo-data.js:127` است
(`p<=4` ⇒ `p<=6`) ولی باید اثرش بر حجم `db` (الان ۴٫۷MB از سقف
۵MB) و ۸۳ صفحه سنجیده شود.

**۳. سپس گام ۳ و ۴** — پیش‌گزینش واقعی در حضور و غیاب و نمره.
گارد `bellAutoAllowed()` از قبل آماده است.

**کارهای باز از دورهای پیش:**
- مسیر معماری (الف/ب/ج در ضمیمهٔ الف `ARCHITECTURE_DECISIONS.md`)
  — هنوز تصمیم‌گیری نشده
- `New folder.rar` (۳٫۷MB) — تصمیمش با رضاست، دست نزنید

---

## Handoff — ۱۴۰۵/۰۶/۱۴ (۲۰۲۶-۰۹-۰۵) — از احمد به نفر بعدی

### چی انجام شد

**۱. تفکیک نقش سوپرادمین** — `src/js/30-authz.js` · کامیت `7ee6c25`

**۵۱ کنش مدرسه‌محور** از سوپرادمین گرفته شد (از ۸۸ کنش به ۳۷).

```
سوپرادمینِ خودش:  imp-commit=false · att-set=false · grade-save=false
از راه S.boss:     imp-commit=true  · att-set=true  · grade-save=true
```

**۲. طرح کلاس خودکار بر اساس زنگ** — `docs/PLAN_BELL_AUTOCLASS.md`
کد نوشته نشد؛ منتظر تأیید.

### تصمیمات مهم گرفته‌شده

**الف) معیار حذف: صفحه، نه سلیقه.** فقط کنش‌هایی حذف شدند که
صفحه‌شان در منوی سوپرادمین **نیست**. برای همین `user-save`,
`subject-save`, `ann-save`, `plan-save` **نگه داشته شدند** —
صفحه‌هایشان (`users`, `subjects`, `announcements`, `plans`)
واقعاً در منوی سوپرادمین هستند و حذفشان آن‌ها را می‌شکست.

⚠️ بررسی دور پیش گفته بود «۴۷ مورد»؛ عدد واقعی **۶۶ نامزد** بود
که ۵۱ تای آن‌ها حذف امن داشتند.

**ب) خط قرمز `S.boss` وارسی شد پیش از هر تغییری.** در
`19-actions.js:74` مقدار `S.user` **خودِ مدیر** می‌شود، پس
`activePersona()` هم `'manager'` برمی‌گرداند — یعنی جانشین واقعاً
به‌جای مدیر عمل می‌کند. حذف نقش از `ACTION_ROLES` آن مسیر را
نمی‌شکند.

**ج) پیش‌گزینش نرم تأیید شد، با یک افزوده:** پیش‌گزینش فقط وقتی
اعمال شود که مدرسه **واقعاً `bell_schedules` ثبت کرده باشد**.
دلیل: `bellOf()` نبودِ رکورد را با `BELL_PRESETS` جبران می‌کند،
یعنی ساعت **خیالی** برمی‌گرداند. بدون این شرط، مدرسه‌ای که
زنگ‌هایش را تنظیم نکرده با ساعت پیش‌فرض کار می‌کند و کاربر
نمی‌فهمد چرا کلاس اشتباه است.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### ⚠️ سه آزمون کهنه وارونه شدند

سه آزمون موجود انتظار داشتند سوپرادمین **داشته باشد**:
```js
['superadmin','imp-preview',true]    → حالا false
['superadmin','sms-topup-ok',true]   → حالا false
['superadmin','tr-send',true]        → حالا false
```
اینها معماری پیشین را تثبیت می‌کردند. به‌روز شدند با کامنت
توضیحی. **اگر کسی بعداً آن‌ها را عقب زد، یعنی تفکیک را نفهمیده.**

#### 🔴 یک جهش عمداً نگرفت — و درست است

جهش «`school-enter` نقش را عوض نکند» هیچ آزمونی را نینداخت. علتش
ضعف آزمون نیست: آن جهش فقط رفتار **UI** را عوض می‌کند نه منطق
مجوز. آزمون‌های ما `S.boss`/`S.user` را مستقیم می‌سازند، پس از
مسیر دکمه نمی‌روند.

**اگر کسی خواست کاملش کند:** آزمونی با کلیک واقعی روی
`data-act="school-enter"` لازم است. من ننوشتم چون آن کنش
`askConfirm` باز می‌کند و آزمونش پیچیده‌تر از ارزشش بود.

#### 🔴 ریسک شناخته‌شده در طرح زنگ (بخش ۶ سند)

دادهٔ نمونه فقط **۵ روز × ۴ زنگ** دارد، ولی `BELL_PRESETS`
پیش‌فرض **۹ بازه** دارد. یعنی اگر پیش‌گزینش پیاده شود، برای زنگ ۵
به بعد دبیر «الان زنگ درسی نیست» می‌بیند — فنی درست، ولی در دمو
گیج‌کننده. باید یا دادهٔ نمونه گسترش یابد یا الگو کم شود.

#### ⚠️ دام نگاشت روز هفته (پیش از پیاده‌سازی بخوانید)

`schedule.day` عددی ۰ تا ۴ است (شنبه تا چهارشنبه) ولی
`new Date().getDay()` یکشنبه‌محور است. نگاشتشان **یکی نیست** و
اشتباهش یعنی کلاس روز دیگری نمایش داده شود.

### قدم بعدی پیشنهادی

**منتظر تأیید رضا برای سه پرسش انتهای `PLAN_BELL_AUTOCLASS.md`:**
۱. افزودهٔ «فقط وقتی مدرسه زنگ ثبت کرده» پذیرفتنی است؟
۲. گام‌های ۱ و ۲ (فقط نمایش، ریسک صفر) جدا منتشر شوند؟
۳. دادهٔ نمونه گسترش یابد یا الگوی زنگ کم شود؟

**اگر تأیید آمد:** گام‌های ۱ و ۲ کم‌ریسک‌ترین شروع‌اند — نوار
«زنگ سوم — دهم تجربی» بدون تغییر هیچ رفتاری.

**کار باز از دور پیش:** مسیر معماری (الف/ب/ج در ضمیمهٔ الف
`ARCHITECTURE_DECISIONS.md`) هنوز تصمیم‌گیری نشده.

---

## Handoff — ۱۴۰۵/۰۶/۱۳ (۲۰۲۶-۰۹-۰۴) — از احمد به نفر بعدی

### چی انجام شد

**۱. دادهٔ نمونهٔ خانوادهٔ چندفرزندی** — `src/js/02-demo-data.js`
کامیت `cf36757`

| | پیش | پس (سنجیده) |
|---|---|---|
| توزیع فرزند/ولی | `{1: 521, 3: 1}` | **`{1: 309, 2: 103, 3: 3}`** |
| کل ولی | ۵۲۲ | ۴۱۵ |
| حجم `db` | ۴٫۷۵۵MB | **۴٫۶۹۷MB** |

انتخاب **قطعی** است نه تصادفی (هر دومین از هر چهارتا) چون
`generate()` با `SEED` ثابت اجرا می‌شود.

**۲ تا ۴: سه بررسی/سند** — کامیت بعدی. جزئیات پایین.

### تصمیمات مهم گرفته‌شده

**الف) ادغام خانواده حجم را کم می‌کند نه زیاد.** هر ادغام یک
رکورد `users` (۲۲۳ بایت) حذف می‌کند و فقط `parent_links`
(۵۹ بایت) می‌ماند. نگرانی سقف ۵MB بی‌مورد بود.

**ب) دو مورد چندفرزندیِ عمدی دست‌نخورده ماندند:**
`parent_multi` («کاظم رستمی») با فرزندان در سه مدرسه برای صفحهٔ
`family`، و یک کاربر نقش‌`teacher` با پیوند ولی. هر دو نام
خانوادگی و مدرسهٔ ناهمخوان دارند که **درست** است؛ آزمون‌ها
کنارشان می‌گذارند.

**ج) مسیر ج در تحلیل معماری، کم‌پشیمانی‌ترین است** — ولی تصمیم
نهایی با رضاست.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### 🔴 دام تازه: دیوار پرداخت آزمون ولی را دروغین می‌اندازد

آزمون «صفر دکمهٔ سوییچ» می‌گرفت در حالی که سنجش دستی **۲ دکمه**
نشان می‌داد. علت: **هیچ ولیِ دوفرزندی اشتراک فعال ندارد** —
`generateP10` اشتراک‌ها را پیش از تغییر من می‌سازد. بدون اشتراک،
`parentLocked()` صفحه را می‌بندد.

helper `withSubscribedParent()` در `tests/smoke.js` ساخته شد.

⚠️ **نکتهٔ ظریف:** `subOf()` با `find` نخستین رکورد را برمی‌دارد.
رکوردی که `push` کنید نادیده می‌ماند — باید ردیف‌های پیشین را
حذف و تازه را `unshift` کرد. دو بار گمراهم کرد.

#### ✅ اثر جانبی مثبت روی توزیع خودکار

`keepSiblings` (دور ۳۸) تا امروز روی دادهٔ نمونه **هرگز فعال
نمی‌شد** چون خواهر/برادری نبود. حالا **۲۶ خانوادهٔ هم‌پدر** فقط
در مدرسهٔ نخست هست. آزمون‌های توزیع را جداگانه سنجیدم:
**هیچ‌کدام نشکست** (۳۳/۳۳ ایستا، ۸۳/۸۳ صفحه).

#### 🔴 بررسی ۲: تفکیک نقش سوپرادمین — مغایرت جدی هست

**سوپرادمین ۸۸ کنش از ۹۳ کنش کل را دارد.** ۴۷ تای آن‌ها
مدرسه‌محورند و طبق تعریف درست باید فقط مال مدیر مدرسه باشند:

| دسته | نمونه |
|---|---|
| ورود اکسل | `imp-preview` · `imp-commit` · `imp-back` · `imp-reset` |
| استخراج | `export-csv` |
| تعریف کاربر | `user-save` · `user-del` · `user-toggle` |
| کلاس و درس | `class-save` · `class-del` · `subject-save` |
| آموزشی | `att-set` · `grade-save` · `disc-save` |
| پیامک | `sms-new` · `sms-send` · `sms-topup` · ۹ کنش `notify-*` |

**`ACTION_ROLES` هیچ کنشی ندارد که مدیر داشته باشد و سوپرادمین
نداشته باشد** — یعنی تفکیکی وجود ندارد.

**اما یک نکتهٔ مهم که شدت را کم می‌کند:** گارد **صفحه** سخت‌گیرتر
از گارد **کنش** است:

```
imp-commit:  کنش=true  صفحه(import)=false
export-csv:  کنش=true  صفحه(formssms)=false
att-set:     کنش=true  صفحه(attendance)=false
class-save:  کنش=true  صفحه(classes)=false
```

سوپرادمین این صفحه‌ها را در منو ندارد و مستقیم بازشان کند
«دسترسی مجاز نیست» می‌گیرد. پس در عمل نمی‌تواند اکسل وارد کند.

🔴 **ولی `canAction('imp-commit')` برایش `true` برمی‌گرداند.** اگر
کسی دکمه را دستی بسازد، گارد کنش جلویش را نمی‌گیرد. **دفاع تک‌لایه
است، نه دولایه.**

⚠️ **پیش از اصلاح، این را در نظر بگیرید:** سوپرادمین از راه
`S.boss` می‌تواند به نقش مدیر مدرسه برود (جابه‌جایی نقش). اگر
کنش‌های مدرسه‌محور را از او بگیرید، **باید مطمئن شوید آن مسیر
همچنان کار می‌کند** وگرنه پشتیبانی فنی فلج می‌شود.

**طبق دستور، اصلاح نکردم.** فقط گزارش.

#### 🔴 بررسی ۳: رفتار خودکار کلاس بر اساس زنگ — پیاده نشده

هر سه بخش خواسته‌شده **وجود ندارند**:

| خواسته | وضعیت |
|---|---|
| کلاس زنگ جاری خودکار نمایش داده شود | ❌ `S.filters.class \|\| cls[0].id` — همیشه کلاس اول |
| حضور/نمره محدود به کلاس زنگ فعلی | ❌ هیچ محدودیتی نیست |
| زنگ تفریح کلاس را ببندد | ❌ مفهوم «زنگ فعال» وجود ندارد |

**آنچه هست:** ماژول `43-bell-schedule.js` با `bellTimeline()` و
`bellTimeOf(schoolId, period)` که ساعت هر زنگ را برمی‌گرداند، و
جدول `schedule` با `{class_id, teacher_id, day, period}`.

🔴 **حلقهٔ مفقوده: هیچ‌جای برنامه ساعت جاری را با زمان زنگ‌ها
مقایسه نمی‌کند.** گرپ `getHours` فقط یک نتیجه دارد، آن هم در
`27-sync.js` برای نمایش زمان همگام‌سازی. `bellOf` بیرون از ماژول
۴۳ **اصلاً مصرف نمی‌شود**.

**برآورد کار: متوسط، ~۳ تا ۵ روز.**
۱. `currentPeriod(schoolId)` — ساعت جاری → شمارهٔ زنگ یا «تفریح»
۲. `teacherNowClass()` — تقاطع زنگ جاری با `schedule` دبیر
۳. پیش‌گزینش در `viewAttendance`/`viewGrades` + نوار «زنگ سوم —
   دهم تجربی»
۴. حالت تفریح: پیام «بین دو زنگ» به‌جای کلاس

⚠️ **دو تصمیم سیاستی لازم است پیش از کد:**
- آیا محدودیت **سخت** باشد (دبیر خارج از زنگ نتواند ثبت کند) یا
  فقط **پیش‌گزینش**؟ سخت‌گیری یعنی دبیری که غروب می‌خواهد غیبت
  صبح را وارد کند نمی‌تواند.
- ساعت **دستگاه کاربر** مبناست. اگر ساعت گوشی اشتباه باشد، کلاس
  اشتباه نشان داده می‌شود. بدون سرور راه دیگری نیست.

#### 📄 بررسی ۴: سند تحلیل معماری نوشته شد

ضمیمهٔ الف در `docs/ARCHITECTURE_DECISIONS.md`.

🔴 **یافتهٔ اصلی:** `officeScopeSchools()` در دمو کار می‌کند
**فقط چون همهٔ مدارس در یک `db` واحدند**. در استقرار واقعی هر
مدرسه `localStorage` جدا دارد و مرورگر رئیس اداره **هیچ راهی**
به دادهٔ مدارس ندارد. این باگ نیست — محدودیت ذاتی است.

سه مسیر با برآورد:

| | زمان | ریسک پروژه | حل مسئله | بار حریم خصوصی |
|---|---|---|---|---|
| **الف** سرور کامل | ۲–۴ ماه | 🔴 بالا | ✅ کامل | 🔴 سنگین |
| **ب** استخراج دستی | ۱–۲ هفته | 🟢 کم | ⚠️ نیمه | 🟡 متوسط |
| **ج** تجمیع خودکار آمار | ۳–۵ هفته | 🟢 کم | ✅ برای اداره | 🟢 ناچیز |

**مسیر ج** فقط شمارنده‌ها را می‌فرستد (نه نام، نه کد ملی، نه
نمره)، پس مسائل رضایت والدین اصلاً پیش نمی‌آید و اگر بعداً الف
لازم شد دورریز نیست.

⚠️ **در هر سه مسیر:** تا وقتی رمزها متن ساده است، هیچ داده‌ای
نباید از دستگاه خارج شود. حتی آمار تجمیعی به احراز هویت حداقلی
سمت سرور نیاز دارد وگرنه هر کسی می‌تواند آمار جعلی بفرستد.

### قدم بعدی پیشنهادی

**منتظر تصمیم رضا:**
۱. مسیر معماری (الف/ب/ج) — پرسش کلیدی: آیا در ۶ ماه آینده کاربر
   واقعی می‌آید؟
۲. تفکیک نقش سوپرادمین — با توجه به هشدار `S.boss`
۳. دو تصمیم سیاستی رفتار زنگ (محدودیت سخت یا نرم؟)

**کم‌ریسک و آمادهٔ اجرا:**
- افزودن `superadmin` به گارد صفحه‌های مدرسه‌محور **حذف نشود**،
  بلکه کنش‌ها هم‌تراز صفحه‌ها شوند (دفاع دولایه)
- `currentPeriod()` به‌تنهایی — بدون اعمال محدودیت، فقط نمایش
  «الان زنگ چندم است» در داشبورد دبیر

---

## Handoff — ۱۴۰۵/۰۶/۱۳ (۲۰۲۶-۰۹-۰۴) — از رضا به احمد

### 👋 احمد، از اینجا شروع کن

خوش آمدی. سه چیز را به‌ترتیب بخوان، بقیه را وقتی لازم شد:

| # | فایل | چرا |
|---|---|---|
| ۱ | `docs/AI_PROMPT.md` | **سند مرجع رسمی.** هر تصمیم و هر دام ثبت‌شده اینجاست. حجیم است؛ فهرستش را ببین و بخش مربوط به کارت را بخوان |
| ۲ | `CONTRIBUTING.md` | راهنمای عملی: چطور صفحه اضافه کنی، چطور آزمون بنویسی، چه دام‌هایی هست |
| ۳ | `docs/OPEN_ITEMS.md` | چه چیزی هنوز باز است (وارسی‌شده، نه از روی حافظه) |

**راه‌اندازی در سه دستور:**
```bash
cd /home/user/payesh
npm install jsdom --no-save     # هر نشست تازه لازم است
node build.js && npm test
```

### پنج اصلی که در هر تغییر رعایت می‌شوند

۱. **همگامی مستندات** — `docs/AI_PROMPT.md` و `CONTRIBUTING.md`
   در **همان نوبت** به‌روز شوند، با جزئیات: چرا آن تصمیم، چه
   عددی سنجیده شد، چه دامی هست.
۲. **تفکیک اصولی کد** — برنامه‌نویس تازه‌وارد بدون پرسیدن بخش
   موردنظرش را پیدا کند. نام فایل محتوایش را بگوید، هر صفحه یک
   تابع `view`، هر ماژول سرفصل فارسی.
۳. **گیت‌هاب همیشه به‌روز** — پس از هر تغییر پوش، و **حتماً** با
   `git ls-remote` تأیید کن (توضیحش پایین‌تر).
۴. **کمترین حجم ممکن** — ولی **هرگز** فایل اصلی/حساس حذف نشود.
   در صورت شک، نپرس‌ونکن؛ بپرس.
۵. **بازبینی امنیتی** — پیش از هر کامیت چهار پرسش
   (`CONTRIBUTING.md` بخش «اصل پنجم در عمل»).

⚠️ **قاعدهٔ نگارش:** همهٔ توضیح‌ها و اسناد **فارسی**. نام انگلیسی
فقط داخل قطعه‌کد، مسیر فایل یا سلول جدول.

### روال کاری که جواب داده

این ترتیب در ۴۴ دور کاری آزموده شده و **هر بار** باگ واقعی پیدا
کرده:

```
۱. کد موجود را بخوان (نه حدس بزن) — گرپ کن، اجرا کن
۲. طرح بنویس، تأیید بگیر
۳. کد بزن
۴. سنجش واقعی در jsdom — با شمارندهٔ خروجی، نه فقط نبود خطا
۵. آزمون دائمی بنویس
۶. 🔴 تست جهش: عمداً کد را خراب کن، ببین آزمون می‌افتد یا نه
۷. اسناد + کامیت + پوش + تأیید ls-remote
۸. ورودی HANDOFF
```

🔴 **گام ۶ را جدی بگیر.** بارها آزمونی نوشتیم که سبز بود ولی
**هیچ چیز نمی‌سنجید**. فقط تست جهش لوشان داد.

### 🔴 پنج دامی که بیشترین وقت را گرفتند

**۱. آزمونی که بی‌صدا رد می‌شود.** الگوی `if (r.skipTest) return;`
باعث شد آزمونی ماه‌ها سبز باشد و هرگز اجرا نشود. اگر ناگزیری،
دست‌کم چاپش کن.

**۲. آزمون و کد از یک منبع می‌خوانند.** انتظار آزمون را از **یک
لایه پایین‌تر** بساز، نه از همان تابعی که می‌سنجی.

**۳. `git push` پیام «Everything up-to-date» می‌دهد وقتی کامیت
شکست خورده.** همیشه با `git ls-remote` تأیید کن.

**۴. هویت گیت در هر نشست تازه پاک می‌شود.** اگر
`fatal: not a git repository` دیدی، مخزن سالم است — فقط
`.git/config` و `.git/refs/` گم شده‌اند. رفعش در
`CONTRIBUTING.md` بخش «پشتیبان مستقل از `.git`».

**۵. `node_modules` بین نشست‌ها نمی‌ماند.** نشانه: تست دودی بی‌صدا
رد می‌شود با کد خروج **صفر**.

📄 فهرست کامل دام‌ها: `CONTRIBUTING.md` و بخش «Errors» در
`docs/AI_PROMPT.md`.

---

### چی انجام شد (خلاصهٔ کل پروژه تا امروز)

پروژه یک **سامانهٔ مدیریت مدرسه** است که خروجی نهایی‌اش همیشه یک
`index.html` تک‌فایلی است (~۱٫۱MB، ۴۶ ماژول، صفر وابستگی تولیدی).

**وضعیت امروز:** ۸۳ صفحه · ۶ نقش · ۲۰ آزمون ایستا + ۳۶۴ آزمون
دودی · بیلد بیت‌به‌بیت تکرارپذیر.

#### فازهای اصلی (۴۴ دور کاری)

| دوره | چه ساخته شد |
|---|---|
| ۱–۸ | هستهٔ سامانه: نقش‌ها، کلاس‌ها، نمرات، حضور، مالی، خانواده |
| ۹–۱۳ | پنل سوپرادمین، سابقهٔ تغییرات، پلان فروش، پشتیبان‌گیری |
| ۱۴–۱۹ | اشتراک مشترک پدر/مادر، چرخهٔ سال تحصیلی، اتصال گیت‌هاب |
| ۲۰–۲۷ | شاخه/رشتهٔ متوسطه دوم، لایهٔ offline-first، دیاگ خودکار |
| ۲۸ | **فاز دمو:** جداسازی لایهٔ داده، `esc()` سراسری، `TODO_BEFORE_PRODUCTION.md` |
| ۲۹–۳۵ | ممیزی امنیت، قرارداد سرور، تصمیم‌های معماری |
| ۳۶–۳۹ | **کارایی:** توزیع خودکار کلاس، ورود اکسل ۲۱× سریع‌تر |
| ۴۰–۴۱ | خودآزمایی ۳۲ بررسی، دادهٔ نمونهٔ جلسات و زنگ، بازسنجی ظرفیت |
| **۴۲** | **اطلاع‌رسانی پیامکی — ۹ گام کامل** |
| **۴۳** | اختیار مدیر، نمودار روند نمرات، پیام اداره، بازچینش منو |
| **۴۴** | سابقهٔ چندساله + یادداشت خصوصی دبیر |

#### چهار دستاورد که بیشترین ارزش را داشتند

**۱. کارایی ورود اکسل: ۲۷۱۹ms → ۱۲۹ms (۲۱×).**
`buildClassIndex` + `placeIdx` + رفع باطل‌سازی ایندکس در هر درج.
جزئیات: `docs/CAPACITY_SIM.md`.

**۲. سامانهٔ پیامک با چهار لایهٔ دفاعی** (دور ۴۲، ۹ گام):
پیش‌نویس حضور و غیاب → مرور نهایی → پنجرهٔ مهلت ۲۰ دقیقه →
اصلاحیهٔ خودکار. طرح کامل: `docs/SMS_NOTIFY_PLAN.md`.

**۳. باگ دفترچهٔ تغییرات.** `applyOp` شیءِ **زندهٔ** رکورد را در
دفترچه می‌گذاشت، پس هر `update` گذشته را بازنویسی می‌کرد. یعنی
صفحهٔ «سابقهٔ تغییرات» **از روز اول** داده‌های نادرست نشان می‌داد.
رفع: رونوشت سطحی در `03-persistence.js`.

**۴. ۲۱ جهش عمدی در سه دور اخیر** — ۱۸ گرفته شد، ۳ شکاف واقعی در
آزمون‌ها لو داد.

#### فایل‌های کلیدی

```
index.html              خروجی توزیعی — 🔴 هرگز مستقیم ویرایش نشود
build.js                node build.js  ·  --check صحت بیت‌به‌بیت
src/js/_order.json      ترتیب الزامی ۴۶ ماژول
src/js/00-data-layer.js Store/Api/Data — تنها دروازهٔ حافظه
src/js/01-helpers.js    esc() خط ۸ · escAttr() خط ۹
src/js/03-persistence.js دفترچهٔ تغییرات + batchWrites
src/js/30-authz.js      ACTION_ROLES — هر کنش تازه اینجا ثبت شود
src/js/44-sms-notify.js اطلاع‌رسانی پیامکی (۱۴ بخش)
src/js/45-teacher-tools.js سابقهٔ چندساله + یادداشت خصوصی
tests/run.js            ۲۰ آزمون ایستا (سریع، بدون jsdom)
tests/smoke.js          ۳۶۴ آزمون دودی (jsdom)
```

### تصمیمات مهم گرفته‌شده

**۱. فاز دمو، نه تولید.** کاربر تصریح کرد بک‌اند کامل و هش رمز در
این مرحله لازم نیست. اما چهار اصل رعایت می‌شود: لایهٔ داده جدا،
`esc()` همه‌جا، هیچ دادهٔ واقعی، و `TODO_BEFORE_PRODUCTION.md`.

**۲. عدد نمره در پیامک نمی‌آید.** تصمیم اخلاقی: پیامک روی گوشی
باز، جلوی چشم خواهر/برادر یا همکلاسی، مسئلهٔ کرامت دانش‌آموز است.
ولی با ورود به سامانه عدد دقیق را می‌بیند.

**۳. سقف روزانهٔ پیامک: ترمز نه دیوار — مگر در حالت خودکار.** در
مسیر دستی مدیر آگاهانه رد می‌شود؛ در حالت خودکار کسی نیست که
تصمیم بگیرد، پس دیوار است.

**۴. پیش‌نویس حضور و غیاب در `Store` است نه `S`.** تعویض کلاس یا
بستن مرورگر کار دبیر را نمی‌برد. کلید `uid|cid|date` — هر سه جزء
لازم است (با جهش تأیید شد).

**۵. اصلاحیه بر پایهٔ «آخرین پیام ارسال‌شده» است نه «پیام اصلی».**
در سناریوی غایب→حاضر→غایب، با مبنای پیام اصلی خانواده با اطلاع
غلط می‌ماند.

**۶. یادداشت خصوصی دبیر با مدرسه می‌ماند نه دبیر.** و هشدار صریح
دارد: «خصوصی یعنی ولی نمی‌بیند، نه اینکه در بازرسی قابل مطالبه
نباشد.»

**۷. سابقهٔ چندساله فقط نمرات و حضور — بدون انضباط.** تا دبیر با
قضاوت پیشینی وارد کلاس نشود.

📄 ۴۴ تصمیم معماری در `docs/ARCHITECTURE_DECISIONS.md`.

### مشکلات باز / نکاتی که نفر بعدی باید بدونه

#### 🔴 وضعیت امنیتی: NOT SAFE FOR PRODUCTION (آگاهانه)

چهار مسدودکننده که **عمداً** بازند (تصمیم کاربر، فاز دمو):

| بند | مورد |
|---|---|
| ۱.۱ | رمزها متن ساده‌اند — bcrypt فقط در کامنت |
| ۱.۲ | احراز هویت سمت مرورگر |
| ۱.۳ | `localStorage` به‌جای پایگاه دادهٔ واقعی |
| ۱.۴ | بررسی نقش فقط سمت مرورگر |

**جداسازی کاربران: FAIL.** سه آزمون IDOR در `tests/smoke.js`
عمداً *وجود* آسیب‌پذیری را تأیید می‌کنند و **روز اتصال سرور باید
معکوس شوند**. `byId` محدوده نمی‌سنجد.

⚠️ اگر روزی یکی از آن سه آزمون خودبه‌خود شکست، یعنی کسی محدوده
را درست کرده — آزمون را به‌روز کن، تغییر را عقب نزن.

#### محدودیت‌های فنی

**حجم `db` = ۴٫۷۵MB از سقف ~۵MB مرورگر.** هر افزایش دادهٔ نمونه
باید سنجیده شود. دو بار از سقف رد شدیم و برگرداندیم.

**سندباکس کند است.** تست دودی گاهی ۶ تا ۱۲ دقیقه می‌گیرد، ولی
`user 13s` در برابر `real 6m31s` نشان می‌دهد پردازنده بیکار است —
مشکل از اشباع میزبان است نه کد. آزمون ایستا در کمتر از یک ثانیه
اجرا می‌شود؛ برای بازخورد سریع از آن استفاده کن.

**منوی مدیر با ۲۶ آیتم به مرز رسیده.** پیش از افزودن سومین آیتم
تازه، گروه‌ها باید بازبینی شوند.

#### بخش‌های تست‌نشده

- **مرورگر واقعی:** همهٔ سنجش‌ها در jsdom است. `Store.available()`
  در jsdom `false` می‌شود و یک بررسی دیاگ کاذبانه بحرانی است.
- **چاپ:** `printableDoc()` پنجرهٔ تازه باز می‌کند؛ در jsdom
  آزموده نشده.
- **همگام‌سازی با سرور:** `Api` و `enqueueOp` نوشته شده‌اند ولی
  سروری پشتشان نیست.

#### یک مورد بلاتکلیف

**`New folder.rar` (۳٫۷MB)** در ریشهٔ پروژه. در گیت نیست و روی
گیت‌هاب هم نه. `.gitignore` می‌گوید محتوایش در `reference/`
استخراج شده ولی آن پوشه وجود ندارد. **تصمیمش با رضاست** — تا آن
موقع طبق اصل چهارم دست نزن.

### قدم بعدی پیشنهادی

**احمد، اگر می‌خواهی گرم شوی، از این‌ها شروع کن (کم‌ریسک):**

۱. **دادهٔ نمونهٔ خانوادهٔ چندفرزندی.** از ۵۲۲ ولی، فقط **یکی**
   چند فرزند دارد. قابلیت سوییچ بین فرزندان کار می‌کند ولی در
   دمو تقریباً نامرئی است. کار کوچکی است در `02-demo-data.js`.

۲. **دو مورد باقی‌ماندهٔ کشف‌پذیری** از
   `docs/DISCOVERABILITY_R43.md`: برچسب پویای «پروندهٔ N فرزند» و
   روشن‌کردن تفاوت `family` و `children`.
   ⚠️ ریسک دارد: `allowedRoutes()` از `NAV` ساخته می‌شود، پس حذف
   آیتم از منو **دسترسی مسیر را قطع می‌کند**.

**کارهای بزرگ‌تر که منتظر تصمیم رضا هستند:**

- **#۹ تکالیف کلاس** — پرسش باز: آیا چیزی جدا از اطلاعیه است؟ اگر
  فقط «اطلاعیهٔ کلاس‌محور با تاریخ تحویل» باشد، دو فیلد به
  `announcements` بسیار ارزان‌تر از جدول تازه است.
- **#۱۰ راهنمای انتخاب رشتهٔ نهم** — من پیشنهاد دادم **نسازیم**.
  مسئلهٔ محتوایی است نه فنی؛ اطلاعات هدایت تحصیلی باید منطبق با
  آیین‌نامهٔ روز باشد وگرنه بر آیندهٔ دانش‌آموز اثر می‌گذارد.

**و یک کار که هر وقت خواستی:** پوشش آزمون برای مرورگر واقعی
(نه jsdom). امروز کور است.

---
