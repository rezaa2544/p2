# راهنمای برنامه‌نویس — «پایش»

> **از دور ۷۳ (۲۰۲۶-۰۹-۰۶):** راهنمای برنامه‌نویس، پرامپتِ کلی و مستندات
> همه در **یک فایل** یکپارچه شده‌اند:
>
> ## 👉 `docs/AI_PROMPT.md`
>
> این فایل مرجعِ واحد است: هویت و معماری برنامه، مدل داده، نقش‌ها و
> دسترسی‌ها، وضعیتِ امنیتی، ساختار ماژول‌ها، قواعد کدنویسی، راهنمای
> «می‌خواهم فلان چیز را عوض کنم، کجا بروم؟»، روش‌های تست (از جمله
> تستِ جهش)، روال انتشار و HANDOFF — همه در یک جا.
>
> اگر چتِ تازه‌ای با هوش مصنوعی باز می‌کنید، کل `docs/AI_PROMPT.md` را
> به آن بدهید؛ از صفر کافی است.
>
> متن کاملِ راهنمای پیشین این فایل، حالا در **بخش ۹** همان سند
> («راهنمای برنامه‌نویس (یکپارچه)») زندگی می‌کند و از آن‌جا به‌روز می‌شود.


> ممیزی رصدپذیری ۲۰۲۶-۰۹-۲۲: برای ادعای صحت `promtool`/`amtool` و جریان Promtail فقط خروجی اجرای واقعی قابل قبول است؛ اجرای صف‌شده یا بررسی ایستا به‌تنهایی «تأییدشده» محسوب نمی‌شود.
