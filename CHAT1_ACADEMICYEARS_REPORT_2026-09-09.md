# گزارشِ چت ۱ — هم‌زمانی دو سال تحصیلی (فاز ۰.۲)

- **تاریخ:** ۲۰/۰۶/۱۴۰۵ (2026-09-09)
- **شاخه:** `arena/01a0827b-p2` — رویِ گیت‌هاب به‌روز شد (push شد؛ §۶)
- **کامیت‌ها:** ۶ کامیت (D1–D5 کار + D6 همین گزارش و HANDOFF) — جدول در §۱
- **سندِ تحویلی:** `docs/ACADEMIC_YEARS_GUIDE.md`

---

## ۱. جدولِ کامیت‌ها

| # | کامیت | پیام | محتوا |
|---|--------|------|-------|
| D1 | `61c83d4` | `feat(authz): formalize school_years model fields + schools.active_year_code` | `school_years.fields` ‏۲←۱۰‏ + `active_year_code` در schools + بازتولید write-perms |
| D2 | `5c08978` | `feat(schools): active academic year core (override + season + format rule)` | `activeYearOf`/`prevYearCode`/`isPreSeason`؛ سیم‌کشی state/funnel؛ pattern سرور؛ ری‌بیلد |
| D3 | `1d0b61b` | `feat(ui): academic year badge, modal selector, report filter, season banner` | بج هدر + سلکت مودال + ذخیره + بج جدول + فیلتر سابقه + بنر فصل؛ ری‌بیلد |
| D4 | `5fd8dbd` | `test(schools): add academic-years + mutation suites (9/9, 5/5 killed)` | `tests/academic-years.js` ‏(AY0–AY8) + `tests/academic-years-mutations.js` ‏(YM1–YM5) |
| D5 | `bac3bc2` | `docs: add academic years guide (phase 0.2)` | `docs/ACADEMIC_YEARS_GUIDE.md` (تحلیل، رفع DLQ، سال عملیاتی، فصل، امنیت، بهره‌برداری) |
| D6 | همین کامیت | `docs: add chat 1 academic-years report` | ورودی HANDOFF + همین گزارش |

---

## ۲. تحلیل وضع موجود (قلم ۱ پرامپت)

- موجودیت سال `school_years` است (ردیف به‌ازای هر مدرسه+`year_code`)؛ رشتهٔ
  `academic_year` در کد وجود ندارد؛ سال از تاریخ شمسی (شروع مهر) مشتق می‌شود.
- مهاجرت: بستن سال ← ارتقا/فارغ‌التحصیلی/بایگانی؛ چیدمان با `placement_year`؛ بدون کپی رکورد.
- **یافتهٔ کلیدی:** هم‌زمانی سال جاری + پیش‌ثبت‌نام سال بعد ساختاری از قبل کار
  می‌کرد (بند ۰.۲: `pre_enrollments` محصور در `year_code` + قیف وضعیتی) — کار
  فاز ۰.۲، رفع ۵ شکاف واقعیِ باقی‌مانده بود (§۳)، نه بازنویسی.

---

## ۳. چه چیزی پیاده شد

- **🔴 رفع DLQ بستن سال:** مدل `school_years` فقط ۲ فیلد داشت و `fieldGate` همهٔ
  عملیات سال را با `unknown_field` به dead-letter می‌فرستاد (بستن سال هرگز سینک
  نمی‌شد). مدل کامل شد؛ AY7 عبور `fieldGate` را رفتاری اثبات می‌کند.
- **سال عملیاتی (`active_year_code`):** override قابل‌/null روی مدرسه؛
  `activeYearOf` = معتبر وگرنه تقویمی؛ مصرف‌کننده فقط دامنهٔ schoolyear + هدر +
  مودال. بدون override، رفتار بایت‌به‌بایت قبلی.
- **UI:** بج `📅` در هدر (همهٔ نقش‌ها)؛ سلکت خودکار/پارسال/جاری/بعد در مودال
  مشترک مدیر/سوپرادمین؛ بج سال + `🔒 بسته` در جدول مدارس (نمای سراسری بدون روت
  تازه)؛ فیلتر سال در کارت سابقه (پیش‌فرض: همه)؛ بنر نرم فصل پیش‌ثبت‌نام.
- **سرور:** قانون فرمت `NNNN-NNNN` برای `year_code`/`active_year_code`؛ خالی‌ها
  طبق `isEmpty` می‌گذرند (patch جزئی).
- **صفر اکشن/روت/کالکشن تازه** — `check-authz` با ساختار صفر ماند.

---

## ۴. گیت‌ها (پس از هر کامیت + رگرسیون دامنه)

| گیت | D1 | D2 | D3 | D4 | D5 |
|---|---|---|---|---|---|
| `node tests/smoke.js` | 547/547 ✅ | 547/547 ✅ | 547/547 ✅ | 547/547 ✅ | 547/547 ✅ |
| `node tools/check-authz.js` | ۰ ✅ | ۰ ✅ | ۰ ✅ | ۰ ✅ | ۰ ✅ |
| `node tests/secret-scan.js` | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ | ۱۱/۱۱ ✅ |
| `node build.js --check` | سبز ✅ | سبز ✅ | سبز ✅ | سبز ✅ | — |
| `tests/authz-model.js` | 232/232 ✅ | 232/232 ✅ | 232/232 ✅ | — | — |
| سوئیتِ تازه | — | — | — | 9/9 + ۵/۵ کشته ✅ | — |
| رگرسیون مرتبط | — | uiclick 4/4، minutes2 8/8 | پروب jsdom ✅ | eslint بدون خطا | — |

رگرسیون پایانی دامنه: `uiclick` ‏4/4‏ + جهش ‏5/5‏، `boom2` ‏8/8‏ (رگرسیون
school-save)، `simulation` ‏48/48‏، `check-authz` خودآزما ‏6/6‏.
هیچ سوئیت سروری مستقیم `year_code` نمی‌نویسد (grep) پس قانون فرمت
نمی‌تواند آن‌ها را بشکند — اثبات با ساختار + smoke سبز.

نکتهٔ محیطی: یک اجرای موازیِ `academic-years` با `uiclick-mutations` (دو
rebuild هم‌زمان روی یک درخت) یک‌بار ۸/۹ داد؛ اجرای تکی: ۹/۹ ✅. همان
دلیلِ laneهای ایزوله در `run-all-tests.sh` — ربطی به کد نداشت.

---

## ۵. تصمیم‌های آگاهانه و محدوده

- **بدون ستون `status` جدا:** پرچم‌های `closed/promoted` همان وضعیت‌اند
  (state دوگانه ممنوع)؛ نگاشت واژگان در §۴ گاید.
- **فصل نرم، نه گیت سخت:** بیرون اسفند–شهریور فقط بنر (پذیرش زودهنگام
  غیردولتی‌ها + عدم‌شکست سوئیت‌ها)؛ گیت سخت تصمیم مالک محصول.
- **حضور/نمرات دست نخورد:** رکورد با تاریخ اکنون ساخته می‌شود و تاریخچه با
  `yearOfDate` سال‌بندی می‌شود — آلودگی بین‌سالی از قبل ناممکن بود.
- **مدیر در برابر سوپرادمین:** سلکت در مودال مشترک؛ authz مودال عوض نشد.
- **محتوای `USER_GUIDE.html`:** فقط مُهر بیلد (الگوی همیشگی).

---

## ۶. تأییدیهٔ پوش

- **مقصد:** `origin/arena/01a0827b-p2`
- **وضعیت:** هر ۶ کامیت پوش و با HEAD راه‌دور همگام شد ✅
- **درخت پس از پوش:** تمیز (`git status` خالی)

فاز ۰.۲ کامل تحویل شد. 🟢
