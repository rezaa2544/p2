# گزارشِ نهاییِ دورِ ۱۰۰ — رفعِ هر ۶ یافتهٔ رفتاری (F01–F06)

**تاریخ:** ۱۸/۰۶/۱۴۰۵ (2026-09-08) · **برنچ:** `fix/behavior-findings`
(از `main` در `078c6bc`؛ پوش به `origin/fix/behavior-findings`)
**طرحِ مرجع:** `docs/FIXES_ACTION_PLAN.md` (گام‌هایِ ۰ تا ۷، هر گام یک کامیت)

---

## ۱. جدولِ یافته‌ها

| یافته | شدت | گام/کامیت | ریشه | رفع |
|---|---|---|---|---|
| ماندگاری (۷ مسیرِ `add`) | P0 | ۱ · `c2a9fd1` | ثبتِ کاربر با `add` (بی‌دفترچه/بی‌صف) ⇒ گم‌شدن پس از بوتِ تازه | ‏`insert` در هر ۷ مسیر + نگهبانِ ایستایِ `add/insert` در `run.js` |
| همگام‌سازیِ حجیم | P0 | ۲ · `9558707` | ارسالِ یک‌جایِ صف ⇒ 413/تایم‌اوت | دسته‌هایِ ۲۰۰تایی + شکافتِ بازگشتیِ 413 + نامهٔ مرده + پیشرفت |
| نامِ اکشنِ طرحِ شهریه | P1 | ۳ · `a7a918f` | سایه/ناهماهنگیِ نام ⇒ ذخیره‌نشدنِ طرح | ‏`tuition-plan-save` یکتا + بازتولیدِ مجوزها |
| سرریزِ چیدمانِ خودکار | P1 | ۴ · `da4611b` | `buckets[0].push` هنگامِ پرشدن ⇒ جابه‌جاییِ بی‌صدا | قراردادِ `{buckets,unplaced}` + نشانِ «N نفر بدون کلاس» + اعمالِ جزئی با هشدار |
| پشتیبان/اسنپ‌شات | P2 | ۵ · `411fc5b` | validate اسنپ‌شات را رد می‌کرد + restore آن را نادیده می‌گرفت | فرمتِ v3 + بازپخشِ `applyLog` + سازگاریِ v1/v2 |
| تداخلِ `generateP12` | P2 | ۶ · `f8f3e21` | دو تعریفِ هم‌نام ⇒ سایه‌خوردنِ دادهٔ سال‌گذشته (کدِ مرده) | ‏`generatePriorYear` + فراخوانیِ دوگانه (بوت/بازیابی) + نگهبانِ یکتایی |

گزارشِ تفصیلیِ هر گام: `R100_STEP0..7_*_REPORT.md` (ریشه).

## ۲. آزمونِ تازه (۹۹ ادعا + ۳۰ جهش — همه سبز، ۰ خطایِ محیطی)

| سئوت | ادعاها | جهش‌ها | کشته |
|---|---|---|---|
| persist-roundtrip (+mutations) | ۹ | ۷ | ۷/۷ |
| sync-chunking (+mutations) | ۲۸ | ۵ | ۵/۵ |
| tuition-plan (+mutations) | ۱۷ | ۴ | ۴/۴ |
| placement-overflow (+mutations) | ۱۸ | ۵ | ۵/۵ |
| backup-snap (+mutations) | ۱۸ | ۵ | ۵/۵ |
| genp12 (+mutations) | ۹ | ۴ | ۴/۴ |

قاعدهٔ «قرمز = توقف و ریشه‌یابی» در همهٔ گام‌ها اجرا شد (۳ ریشه‌یابیِ
smoke/سئوت در گام‌هایِ ۴–۶ + ۴ ریشه‌یابیِ رگرسیون در گامِ ۷ — هیچ‌کدام
پنهان نشد؛ جزئیات در گزارشِ گامِ ۷).

## ۳. یافته‌هایِ جنبیِ حینِ کار

- **امنیتی (P0، بسته شد در گامِ ۵):** اسنپ‌شات رمزِ متن‌سادهٔ کاربران
  را حمل می‌کرد و رداکشن فقط opها را پوشش می‌داد ⇒ رداکشن به
  ‏`db.users[].password` درونِ اسنپ‌شات هم گسترش یافت (همان قاعدهٔ
  «رمز هرگز در فایلِ دانلود»).
- **نگهداشتِ سوئیت‌ها (گامِ ۷، فقط-تست):** کهنگیِ الگویِ M16
  (پس‌از سخت‌سازیِ حسابرسی) و کپیِ تختِ dropout-mutations (پس‌از فاز ۳)
  که دو حفرهٔ پوششی بودند — هر دو ترمیم و سبز شدند (۲۰/۲۰ و ۶/۶).
- **یادداشتِ خارج‌از-دامنه (بدونِ اقدام):** سطل‌هایِ چیدمان از صفر
  شروع می‌کنند (اشغالِ موجود دیده نمی‌شود) — در گزارشِ گامِ ۴ ثبت است.

## ۴. دروازهٔ نهایی

- رگرسیونِ کامل: **۱۴۸/۱۴۸ سبز** (۱۴۴ مستقیم + ۴ بازاجراشده پس‌از
  اصلاحِ فقط-تست؛ درختِ محصول در این میان بی‌تغییر).
- `build --check` ✅ · `smoke` **547/547** ✅ · `check-authz` (خروجِ ۰) ✅
- مستندات: `AI_PROMPT.md` §۰.۵.۲۱ · کالآوتِ «(دور ۱۰۰)» در راهنما ·
  ورودیِ نهاییِ `HANDOFF.md`.

## ۵. کامیت‌هایِ برنچ (۸)

`7ae96a4` (گامِ ۰) · `c2a9fd1` · `9558707` · `a7a918f` · `da4611b` ·
‏`411fc5b` · `f8f3e21` · کامیتِ نهاییِ R100 (این گزارش + گامِ ۷).

**باقی‌ماندهٔ کلِ پروژه (دست‌نخورده):** تصمیمِ محصولِ پرداختِ والد ·
۲.۷ (throttle + UUID) · ۲.۱(a) نرخِ اکسل · ۲.۱(b) قواعدِ عمیق ·
Lax→Strict · pull/bootstrapِ خواندن.
