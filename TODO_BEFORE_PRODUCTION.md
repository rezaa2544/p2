# کارهای الزامی پیش از اتصال به سرور واقعی

> ⛔ **این نسخه دموی نمایشی است.** تا وقتی همهٔ بندهای «مسدودکننده»
> در این سند تیک نخورده‌اند، سامانه **نباید** با دادهٔ واقعی دانش‌آموز،
> اولیا یا کارکنان اجرا شود.

وضعیت امروز: تمام داده در حافظهٔ مرورگر است و احراز هویت کاملاً سمت
کاربر انجام می‌شود. این‌ها برای نمایش عمدی و پذیرفته‌شده‌اند، ولی هیچ‌کدام
برای محیط واقعی قابل قبول نیست. (محصول هیچ رمز کاربری ندارد — ورود فقط
**تلفن + کد پیامکی + کد ملی** است؛ ستونِ `password` روی رکوردهایِ نمونه
فقط **آرشیوی** است و در ورود خوانده نمی‌شود. 📄 `docs/PLAN_PHONE_AUTH.md`)

---

📄 **تصمیمات معماری قفل‌شده** (خلاصهٔ یک‌صفحه‌ای):
`docs/ARCHITECTURE_DECISIONS.md`

📄 **قرارداد فنی دقیق سرور** (شکل ژتون، سنجش‌های محموله، رمزنگاری
داده): `docs/SERVER_SECURITY_CONTRACT.md`

---

## 📋 بازبینیِ R96 (2026-09-08) — وضعیتِ واقعیِ بندها در کدِ فعلی

> R94 هر بند را با کدِ آن زمان بازبینی کرد؛ **R96** کنترل‌های امنیتی را
> قطعی و جهش‌کشی‌شده کرد (جزئیات: `R96_SECURITY_PROGRAM_REPORT.md`).

| بند | وضعیتِ واقعی (R96) | مدرک |
|---|---|---|
| ۱.۲ احرازِ هویتِ سرور | ✅ سخت‌شده (JWT: iss/aud/iat/rotation/ابطال jti؛ OTP: CSPRNG+hash-only+سقف‌هایِ phone/IP/tries+enum-guard) | `server/auth.js` · server17 J/O |
| ۱.۳ پایگاهِ داده | 🟡 سرورِ فایلِ JSON (برایِ پایلوت کافی؛ PostgreSQL/مقیاس‌پذیری = تصمیمِ استقرار) | `server/index.js` · `docs/STORAGE_OPTIONS_2026-09-06.md` |
| ۱.۴ RBAC + محدوده | ✅ **یک منبعِ واحد** (authz/model.json 80 مجموعه × 3 عمل، build-time) + مجوزِ سطحِ فیلد (status/role/school_id/phone/مالکیت تزریق‌نشده) + محدوده در خوانش (404-not-403) | `authz/model.json` · `server/sync.js` fieldGate · server16 (39) + جهش‌ها 6/6 |
| ۲.۱ اعتبارسنجیِ سرور | ✅ صریح و به‌ازایِ endpoint (fieldGate + 413 + sms: فقطِ queue_ids + auth: فرمت/ترتیبِ active) — قواعدِ عمیقِ کسب‌وکار (ظرفیت کلاس و…) هنوز سمتِ کلاینت | server16 M3 · server17 S/J11 |
| ۲.۲ محدودسازیِ نرخ | ✅ ورود: cooldown+سقفِ روزانه+IP (phone و IP جدا) + کدِ کِلی — ⚠️ سقفِ نرخِ ورودِ اکسل هنوز نیست | `server/auth.js` · server17 O2–O6 |
| ۲.۳ سابقه امنیتی | ✅ فقط‌افزودنی + **خارج از store** + چرخش 10MB + بدونِ phone/nid کامل | server17 T6 · O7 |
| ۲.۴ پشتیبان | ✅ خودکار/دستی + اعتبارسنجیِ بازیابی + GC — نگهداریِ خارجِ سرور = عملیات | server9 A2/A4 · server14-gc |
| ۲.۵ تعارض (base_version + داوری) | ✅ پیاده (نسخه‌گذاری + حفظِ تعارض + صفحهٔ داوریِ مدیر) | `server/conflicts.js` · server15 + جهش |
| ۲.۶ سرآیندها + CSP nonce | ✅ nonce در build.js + سرور (هماهنگ، چکِ e2e) + HSTS/Frame/nosniff/Referrer/Permissions + XSS: صفرِ eval/Function، document.write فقط ۶ نقطهٔ مجاز | `server/index.js` · `xss-guard` (23) |
| ۲.۷ هشدارِ شمردنِ شناسه | 🟡 guardِ enum (رابطهٔ موجود/ناموجود) ✅ — تأخیر/ابطال + تصمیمِ UUID باز | `server/auth.js` O8 |
| ۱.۶ ورودِ خودکارِ دمو | 🟡 آگاهانه (فقط حالتِ دمو/پایلوت؛ فیلترِ F-3) — در حالتِ سرور نشست = کوکی | F-3 |
| رازها (P1-9) | ✅ .gitignore (.env*/توکن/کلید/داده) + اسکینِ راز در رجیسیون — ⚠️ Revokeٔ توکن‌هایِ نشت‌کرده = اقدامِ کاربر | `tests/secret-scan.js` (11) |
| TLS (P1-10) | ✅ production بی‌گواهی/پروکسی → exit(1)؛ self-signed در production رد؛ Secure+HSTS زیر HTTPS — گواهیِ واقعی = استقرار | `server/index.js` · server17 T1–T4 |
| پرداختِ والد | ⏳ **تصمیمِ محصول** (تنها موردِ بازِ کلِ پروژه) | R88b/R90 |

---

### ۱.۱ هش کردن رمز عبور با bcrypt — ✅ بسته شد (محصول رمز ندارد)

✅ **این بند دیگر مسدودکننده نیست.** محصول هیچ رمز کاربری ندارد: ورود
فقط **تلفن + کد پیامکی + کد ملی** است (📄 `docs/PLAN_PHONE_AUTH.md`) و
فرمِ نام‌کاربری/رمز در دور ۷۰ از محصول حذف شد. ستونِ `password` روی
رکوردها فقط **آرشیوی** است (ساختارِ رکورد/پشتیبان؛ مقدارِ ثابتِ
`123456` پرکنندهٔ دمو) و هیچ کدی آن را در ورود نمی‌خواند — آزمونِ smoke
این رفتار را قفل می‌کند («یادآور: محصول رمز کاربری ندارد»).

🔶 **بدهیِ مشروط** (فقط اگر روزی رمز دوباره به محصول معرفی شود):
- هش با **bcrypt** فقط سمت سرور، هزینهٔ حداقل ۱۲؛ رمز خام هرگز ذخیره/لاگ نشود.
- ⚠️ دام: bcrypt ورودی بیش از ۷۲ بایت را بی‌صدا می‌بُرد (فارسی هر حرف ۲ بایت) — پیش از هش طول بایتی را بسنجید یا رمز بلند را رد کنید.
- ⚠️ حداقل قدرت رمز (آزمونِ `weak-password` در دیاغ همین حالا شمارش می‌کند) + سقفِ نرخِ حدس (بند ۲.۲).

### ۱.۲ انتقال احراز هویت به بک‌اند

| | |
|---|---|
| وضعیت امروز | `06-login.js` جریانِ **تلفن → کد پیامکی → کد ملی** را در مرورگر شبیه‌سازی می‌کند (دروازهٔ شاهکار، بدون رمز) — کاملاً سمت کاربر |
| خطر | کاربر می‌تواند در کنسول مرورگر `S.user` را عوض کند و مدیر کل شود. هیچ چیز جلویش را نمی‌گیرد. |
| کار لازم | ورود از راه `POST /auth/login` (تلفن/کد/کد ملی — قرارداد در `docs/PLAN_PHONE_AUTH.md`) · نشست با کوکی `HttpOnly` و `Secure` و `SameSite=Strict` · انقضا و تمدید نشست · محدودیت تعداد تلاش ناموفقِ کد |

⚠️ ژتون نشست را در `localStorage` نگذارید. هر کد جاوااسکریپتی —
از جمله کتابخانهٔ آلوده — می‌تواند بخواندش. کوکی `HttpOnly` از دید
جاوااسکریپت پنهان است و همین تفاوت، مرز میان نشت و امنیت است.

⚠️ «جعل هویت» (`19-actions.js`) که مدیر کل را به حساب مدیر مدرسه
می‌برد، در حالت سروری باید عملیات مجزای سمت سرور با ثبت در سابقه
باشد، نه فقط عوض کردن `SESSION_KEY`.

### ۱.۳ جایگزینی حافظهٔ مرورگر با پایگاه دادهٔ سرور

| | |
|---|---|
| وضعیت امروز | همه‌چیز در `localStorage` زیر کلید `sms_log_v1` |
| محدودیت | سقف حدود ۵ مگابایت ⇒ عملاً حدود ۲٬۰۰۰ دانش‌آموز در هر مرورگر (سنجش در `docs/CAPACITY_SIM.md`) |
| کار لازم | پایگاه دادهٔ رابطه‌ای (پیشنهاد PostgreSQL) · حافظهٔ مرورگر فقط برای صف آفلاین بماند |

✅ **این بند تا حد زیادی آماده است.** فایل `src/js/00-data-layer.js`
تنها دروازهٔ برنامه به حافظه است و هیچ فایل دیگری `localStorage` را
مستقیم صدا نمی‌زند — آزمون خودکار در `tests/smoke.js` این را نگه
می‌دارد. برای اتصال کافی است `DATA_MODE` را `server` کنید و بدنهٔ
`Api.request` را باز کنید.

### ۱.۴ بررسی نقش کاربر سمت سرور

| | |
|---|---|
| وضعیت امروز | `30-authz.js` با `canRoute(route, role)` فقط منو را می‌بندد |
| خطر | این «قفل در ویترین» است. هرکس درخواست را مستقیم بسازد از کنارش رد می‌شود. کنترل دسترسی سمت کاربر فقط برای راحتی کاربر است، نه امنیت. |
| کار لازم | هر مسیر حساس سرور نقش را از نشست بخواند و **مستقل** بسنجد |

مهم‌تر از نقش، **محدودهٔ داده** است. یک مدیر مدرسه نقش درستی دارد
ولی فقط حق دیدن مدرسهٔ خودش را دارد. منطق `29-scope.js` باید عیناً
سمت سرور تکرار شود:

- مدیر مدرسه → فقط `school_id` خودش
- دبیر → فقط کلاس‌هایی که به او تخصیص یافته
- ولی → فقط فرزندان متصل به خودش
- کارشناس اداره → فقط مدارس منطقهٔ خودش

⚠️ **پرخطرترین نقطه — با عدد سنجیده‌شده:** مسیرهایی که شناسه
می‌گیرند. سه آزمون `IDOR ⚠️` در `tests/smoke.js` وسعت نشت فعلی را
اندازه گرفته‌اند: مدیر مدرسه به **۸۷۰ کاربر** خارج از مدرسه‌اش،
ولی به **۵۲۶ دانش‌آموز** غیرفرزند، و دبیر به **۱۱٬۵۹۲ نمرهٔ**
کلاس‌هایی که هرگز ندیده می‌رسد.

⚠️ برای رکورد خارج از محدوده `404` برگردانید نه `403` — پاسخ `403`
تأیید می‌کند رکورد وجود دارد و شناسه‌های معتبر را لو می‌دهد.

⚠️ **دام:** مسیرهایی که شناسه می‌گیرند.

⚠️ **کارایی:** سنجش محدوده رایگان نیست. سنجش ولی و دبیر جست‌وجو
لازم دارد و اگر ساده پیاده شود در حلقه رفتار درجه‌دوم می‌سازد —
ایندکس اختصاصی بسازید. بنچمارک‌های `docs/CAPACITY_SIM.md` پس از
این تغییر باید بازسنجی شوند. **هرگز برای سرعت، سنجش را حذف
نکنید**؛ راه‌حل ایندکس است نه استثنا.
📄 `docs/SERVER_SECURITY_CONTRACT.md` بند ۱.۲.۴ `GET /students/۵۰۰`
باید بررسی کند دانش‌آموز ۵۰۰ در محدودهٔ درخواست‌کننده هست یا نه.
بدون این، شمردن ساده از ۱ تا ۱۰٬۰۰۰ کل پایگاه داده را بیرون می‌کشد.

---

## ۲. مهم — پیش از پذیرش کاربر واقعی

### ۲.۱ اعتبارسنجی دوباره سمت سرور
هر قاعده‌ای که امروز در مرورگر بررسی می‌شود (کد ملی، ظرفیت کلاس،
یکتایی نام کاربری، تطابق رشته با شاخهٔ مدرسه) باید سمت سرور هم
بررسی شود. اعتبارسنجی مرورگر برای تجربهٔ کاربر است، نه درستی داده.

### ۲.۲ محدودسازی نرخ درخواست
ورود (تلاشِ کد پیامکی) و ورود اکسل باید سقف نرخ داشته باشند. بدون
آن، حدسِ کد با آزمون‌وخطا رایگان است.

⚠️ **قفل سخت حساب نگذارید** — مهاجم با ۵ تلاش عمداً غلط، معلم را
روز امتحان قفل می‌کند. تأخیر تصاعدی (۱، ۲، ۴، ۸ ثانیه) درست است.

⚠️ زمان پاسخ «تلفن ثبت‌نشده» و «کد غلط» باید یکسان باشد، وگرنه
فهرست تلفن‌های معتبر لو می‌رود. (🔶 مشروط — فقط با بازگشتِ رمز:
راه‌حلِ کلاسیک، اجرای bcrypt روی یک هش ساختگیِ از پیش ساخته‌شده با
**همان هزینه** است تا زمان‌ها یکسان بماند.)

⚠️ بررسی `active` باید **بعد از** تاییدِ کد/کد ملی بیاید، وگرنه مهاجم
می‌فهمد کدام حساب‌ها وجود دارند ولی غیرفعالند.

📄 جزئیات و آستانه‌ها: `docs/SERVER_SECURITY_CONTRACT.md` بند ۵.۵

### ۲.۳ ثبت وقایع امنیتی
سابقهٔ تغییرات (`36-audit-activity.js`) امروز در مرورگر است و کاربر
می‌تواند پاکش کند. سابقهٔ واقعی باید سمت سرور و فقط‌افزودنی باشد.

### ۲.۴ پشتیبان‌گیری و بازیابی
`38-plans-backup.js` امروز فایل می‌سازد. در محیط واقعی: پشتیبان
خودکار روزانه، نگهداری خارج از سرور اصلی، و **آزمون واقعی بازیابی**
— پشتیبانی که هرگز بازیابی نشده، پشتیبان نیست.

### ۲.۵ حل تعارض در همگام‌سازی ✅ تصمیم گرفته شد
سیاست **ترکیبی بر پایهٔ نوع داده**:
اعلان و یادداشت ⇒ آخرین نوشتن برنده ·
حضور و نمره و انضباطی ⇒ **تعارض نگه داشته می‌شود** ·
ساختار ⇒ سرور مرجع است.

✅ **بسته شد (دور ۹۵):** هر عملیاتِ `upd` روی مجموعهٔ نسخه‌دار
(نمره/حضور/انضباطی) حالا `base_version` می‌کَنَد؛ سرور آن را با
نسخهٔ خود می‌سنجد و در صورتِ کهنه‌بودن:

- **حفظ تعارض** (نمره/حضور/انضباطی): تغییر اعمال **نمی‌شود**، در
  `sync_conflicts` با هر دو نسخه (سرور/کلاینت) محفوظ می‌شود، مدیر
  مدرسه اعلان می‌گیرد و از داشبورد «⚖️ تعارض‌های همگام‌سازی» داوری
  می‌کند (`GET/POST /api/sync/conflicts` و `/api/sync/resolve-conflict` —
  `server/conflicts.js`). داوری `incoming` = اعمالِ دادهٔ کلاینت +
  نسخه +۱؛ `server` = رکورد دست‌نخورده.
- **سرورِ مرجع** (ساختار: کلاس/درس/کاربر/...): عملیاتِ کهنه با
  `stale_base` رد می‌شود.
- **آخرین نوشتن** (اعلان/یادداشت/پیام): `base_version` بی‌اثر است —
  بر پایهٔ **زمان دریافت سرور**، نه ساعتِ دستگاه.
- رکوردهایِ بدونِ فیلدِ `version` به‌عنوانِ نسخهٔ ۱ تلقی می‌شوند
  (`||1`) — بدونِ نیازِ بهِ مهاجرت. عملیاتِ بدونِ `base_version`
  (رایهٔ قدیمی) همچنان اعمال می‌شود.

آزمون: `tests/server15.js` (۴۰ چک) + `tests/server15-mutations.js`
(۶ جهش) + چکِ لایهٔ محلی در `tests/smoke.js`.

📄 `docs/SERVER_SECURITY_CONTRACT.md` بند ۵.۸.۲

### ۲.۸ حذف نرم یا سخت ✅ تصمیم گرفته شد
سابقهٔ تحصیلی و ساختار ⇒ **حذف نرم** (`deleted_at`) ·
اعلان و پیش‌نویس و صف پردازش‌شده ⇒ حذف سخت.

🔴 **کار لازم:** هر پرس‌وجو باید `WHERE deleted_at IS NULL` داشته
باشد. افزودنش پس از نوشتن صدها کوئری، بازنویسی گسترده است.

⚠️ قید یکتایی کد ملی و نام کاربری باید **جزئی** باشد
(`UNIQUE ... WHERE deleted_at IS NULL`)، وگرنه رکورد حذف‌شده مانع
ثبت دوباره می‌شود.

⚠️ مسیر جدا برای پاک‌سازی قطعی (حق فراموشی) لازم است.

📄 `docs/SERVER_SECURITY_CONTRACT.md` بند ۵.۸.۳

### ۲.۶ سرآیندهای امنیتی HTTP
`Content-Security-Policy` (مهم‌ترین لایهٔ دوم در برابر XSS) ·
`X-Content-Type-Options: nosniff` · `Strict-Transport-Security` ·
`X-Frame-Options` · `Referrer-Policy: same-origin`.

🔴 **کار لازم در `build.js`:** خروجی تک‌فایلی است و اسکریپت‌ها
درون‌خطی‌اند، پس سیاست محتوا به `'unsafe-inline'` نیاز پیدا می‌کند
که عملاً بی‌اثرش می‌کند. `build.js` باید برای هر بلوک `nonce`
تولید کند. این تغییر واقعی در ابزار ساخت است و باید زودتر
برنامه‌ریزی شود.

📄 مجموعهٔ کامل سرآیندها: `docs/SERVER_SECURITY_CONTRACT.md` بند ۵.۶

### ۲.۷ هشدار خودکار در برابر شمردن شناسه
سنجش مالکیت (بند ۱.۴) جلوی نشت را می‌گیرد ولی مهاجم را نشان
نمی‌دهد. کسی که ۵٬۰۰۰ بار `404` گرفته در حال نقشه‌برداری است.

پاسخ تدریجی: ثبت ⇒ تأخیر ۵۰۰ میلی‌ثانیه ⇒ تأخیر ۲ ثانیه + هشدار
⇒ ابطال نشست. قطع فوری نه، چون مدیری که گزارش سالانه می‌گیرد هم
صدها رکورد می‌خواند.

⚠️ تصمیم زودهنگام لازم: اگر شناسه‌ها قرار است UUID شوند تا اصلاً
قابل شمردن نباشند، باید **پیش از** نخستین استقرار واقعی تصمیم
گرفته شود؛ مهاجرت بعدی سنگین است.

📄 آستانه‌ها و جزئیات: `docs/SERVER_SECURITY_CONTRACT.md` بند ۵.۷

---

## ۳. آنچه همین حالا انجام شده ✅

این‌ها منتظر سرور نماندند چون همین امروز هم لازم بودند:

| کار | وضعیت | جزئیات |
|---|---|---|
| امن‌سازی خروجی در برابر XSS | ✅ | `esc()` برای متن، `escAttr()` برای صفت — ۱۲۱ صفت پویا، صفر مورد ناامن |
| جداسازی لایهٔ داده | ✅ | `00-data-layer.js` تنها دروازه؛ صفر دسترسی مستقیم در ۴۳ فایل دیگر |
| نبود دادهٔ واقعی | ✅ | تلفن با پیش‌شمارهٔ ۰۹۹۹ (تخصیص‌نیافته)، کد ملی با پیشوند ۹۹۹ (صادرنشده) |
| نگهبان خودکار | ✅ | آزمون‌های `tests/smoke.js` نمی‌گذارند این سه مورد پس بروند |

---

## ۴. ترتیب پیشنهادی اجرا

۱. پایگاه داده و مهاجرت داده (بند ۱.۳) — پایهٔ بقیه است
۲. احراز هویت سرور — جریانِ تلفن/کد/کد ملی (بند ۱.۲؛ بند ۱.۱ بسته است چون محصول رمز ندارد)
۳. کنترل دسترسی و محدودهٔ داده (۱.۴) — پرخطرترین بند
۴. اعتبارسنجی دوباره و محدودسازی نرخ (۲.۱ و ۲.۲)
۵. سابقه، پشتیبان، تعارض، سرآیندها

⚠️ بند ۱.۴ را به آخر نیندازید. رایج‌ترین اشتباه این است که تیم
احراز هویت را درست می‌سازد و بعد فرض می‌کند «کاربر وارد شده پس
مجاز است». ورود و مجوز دو چیز متفاوت‌اند.

---

## ۵. آزمون پذیرش پیش از استقرار

> R96 (2026-09-08): همهٔ بندها زیر آزمونِ خودکار قفل شدند — جزئیات
> `R96_SECURITY_PROGRAM_REPORT.md`.

- [x] ورود هیچ‌گاه ستونِ آرشیویِ `password` را نمی‌خواند (قفلِ smoke)
- [x] عوض کردن `S.user` در کنسول مرورگر هیچ دسترسی تازه‌ای نمی‌دهد (server16 M1: نقش از توکنِ سرور، نه بدنه)
- [x] درخواست مستقیم به مسیر مدیر با نشست دبیر رد می‌شود (role_denied per-op — server16 M1/M3)
- [x] مدیر مدرسهٔ الف با شناسهٔ دانش‌آموز مدرسهٔ ب رد می‌شود (404-not-403 — server16 M4/M5)
- [x] ولی نمی‌تواند پروندهٔ دانش‌آموزی که فرزندش نیست را ببیند (server16 M4 ownership)
- [x] قطع اینترنت وسط حضور و غیاب داده را از بین نمی‌برد (صفِ آفلاین + ایدمپوتانس uid)
- [x] بازیابی از پشتیبان روی سرور خالی آزموده شده است (server9 A4 + اعتبارسنجی)
- [x] ورود با نام کاربری حاوی تگ HTML اسکریپت اجرا نمی‌کند (xss-guard: esc/escAttr + e2e jsdom + CSP nonce)


---

## ۱.۵ ورود با نام کاربری ✅ بسته شد (افزوده در دور ۶۱، بسته شد در دور ۷۰)

این بند دیگر لازم نیست: فرمِ نام‌کاربری/رمز **از محصول حذف شده**
(دور ۷۰) — ورود فقط **تلفن + کد + کد ملی** است و پرچمِ `auth_mode`
دیگر وجود خارجی ندارد (smoke فیلدِ رمز را در فرمِ ورود قفل کرده:
«فیلدِ رمز در فرمِ ورود بازگشته»). ستون‌هایِ `username`/`password` روی
رکوردها آرشیوی‌اند و در ورود خوانده نمی‌شوند.

## ۱٫۶ ورود خودکار بدون رمز = فقط دمو (افزوده در دور ۶۲)

**اولویت: 🔴 مسدودکننده — همراه بند ۱٫۵**

امروز برنامه در بوت (بلوک `SESSION_KEY` در `24-edu-office.js`) کاربرِ
ذخیره‌شده را **بدون هیچ رمز یا تأیید هویتی** وارد می‌کند: اگر
`SESSION_KEY` در حافظه باشد، نشست از آن ساخته می‌شود. این رفتار
**صرفاً برای حالت دمو/آفلاین** است تا با حساب‌های نمونه راحت کار
کنیم و **بخشی از طراحی تولید نیست**؛ نباید به‌عنوان «حالت ورود
قانونی» معرفی شود.

در نسخهٔ نهایی:

- ورود فقط با **تلفن + کد پیامکی + کد ملی** از دروازهٔ شاهکار
  (جزئیات در `docs/PLAN_PHONE_AUTH.md`) انجام می‌شود — بند ۱٫۵ (بسته شد: فرمِ رمز از محصول حذف شده)
- نشست یک **کوکی `HttpOnly`/`Secure`/`SameSite=Strict`** است که سمت
  سرور صادر و باطل می‌شود (طراحی: قرارداد امنیت سرور، بند ۲٫۱؛
  کار لازم: بند ۱٫۲ همین فایل)
- بلوک ورود خودکار بالا **جایگزین نشستِ کوکی** می‌شود (فرمِ
  نام‌کاربری/رمز پیش‌تر حذف شده — دور ۷۰)

⚠️ تا آن روز، دو نکتهٔ عملی:

1. اگر کسی با مرورگرش صفحهٔ دمو را باز کند و `SESSION_KEY` در حافظه
   باشد، **به‌جای صفحهٔ ورود مستقیم وارد می‌شود** — در دمو قابل‌قبول،
   در محیط واقعی خطرناک.
2. نگهبان F-3 (مدرسهٔ غیرفعال، حتی نشست ذخیره‌شده را هم رد می‌کند)
   این بلوک را پوشش می‌دهد و آزمون‌های smoke/security آن را نگه می‌دارند.
