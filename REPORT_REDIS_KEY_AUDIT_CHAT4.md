# گزارش چت ۴ — ممیزی و بهینه‌سازی کلیدهای Redis و TTL

**تاریخ:** ۱۹ شهریور ۱۴۰۵ (2026-09-09) · **شاخه:** `feat/p2-redis-integrity-chat4` · **پایه:** ادغام‌شده با `56bbedf` (main)

## خلاصه

ماموریت «ممیزی و بهینه‌سازی کلیدهای Redis و TTL» کامل شد: موجودی کامل کلیدها
از دلِ کد استخراج شد، یک نشت حافظهٔ واقعی (کلیدهای بی‌TTLِ بازمانده از کرش)
یافته و بسته شد، ابزار پایش ساخته شد و مستندات ثبت شد. همهٔ قوانین سخت سبز.

| کامیت | موضوع |
|---|---|
| `267235c` | ادغام `origin/main` (56bbedf — PRهای چت۳: نرخ، WAF، اعتماد صفر) + حل کانفلیکت |
| `06da9c5` | `feat(redis): audit and optimize redis keys and ttl` |

## ۱) موجودی واقعی کلیدهای Redis

| الگوی کلید | مالک | TTL | وضعیت |
|---|---|---|---|
| `payesh:cache:bootstrap:<userId>` | `server/cache.js` | ۳۰۰ ثانیه | ✅ |
| `payesh:rl:<action>:<identifier>` | `server/cache.js` + `server/waf.js` | به‌اندازهٔ پنجره (۶۰–۹۰۰ ثانیه) | ✅ |
| `payesh:rl:waf:ip:<ip>` | `server/waf.js` | ۶۰ ثانیه (۱۰۰/دقیقه) | ✅ |
| `payesh:rl:waf:audit:<rule>` | `server/waf.js` | ۶۰ ثانیه (۵/دقیقه) | ✅ |
| `payesh:idempotency:<uid>` | `server/cache.js` ← `server/sync.js` | ۲۴ ساعت | ✅ |
| `payesh:lock:<name>` | `server/cache.js` (پی۰-۱۴) | ۵–۱۰ ثانیه | ✅ |
| `payesh:otp:state` | `server/otp-store.js` | دائمیِ مجاز (عمدی) | ✅ |
| `rate:otp:send:phone:day:<phone>` | `server/rate-limit.js` | ۲۴ ساعت | ✅ (پس از اصلاح) |
| `rate:otp:send:ip / send:phone / login:ip` | `server/rate-limit.js` | ۹۰۰ ثانیه | ✅ (پس از اصلاح) |

علاوه بر این‌ها، کانال `payesh:pubsub:inval` وجود دارد که کلید نیست
(Pub/Sub برای بی‌اعتبارسازی کش بین نمونه‌ها).

**تطبیق با جدولِ پرامپت:** دو کلیدِ `revoked:*` و `id:seq:*` در این مخزن
وجود ندارند — ابطال نشست در `store.__revoked_jti` نگهداری می‌شود (کار چت۲
هنوز ادغام نشده) و دنباله‌های شناسه سمت پستگرس‌اند (پی۰-۱۶)، نه ردیس.

## ۲) یافتهٔ اصلی و اصلاح — نشت حافظه در `rate:*`

**مسئله:** الگوی قدیمی «`INCR` سپس `EXPIRE`» بود. اگر فرایند میان این دو
عملیات می‌مُرد، کلید برای همیشه **بی‌TTL** می‌ماند — در مقیاس ملی یعنی نشت
آهستهٔ حافظهٔ ردیس.

**اصلاح:** `redis.incrWithTtl(key, ttlSeconds)` — یک اسکریپت لوآ که اینکریمِنت
و تضمین انقضا را اتمیک انجام می‌دهد (یک رفت‌وبرگشت):

```lua
local v = redis.call("INCR", KEYS[1])
if redis.call("TTL", KEYS[1]) < 0 then
  redis.call("EXPIRE", KEYS[1], ARGV[1])
end
return v
```

- کلیدهای تازه از همان نوشتِ نخست انقضا دارند.
- کلیدهای یتیمِ باقی‌مانده از قبل، در اولین برخوردِ بعدی **خوددرمانی** می‌شوند.
- `server/rate-limit.js` اکنون فقط از همین درگاه استفاده می‌کند.

## ۳) تصمیم دربارهٔ `payesh:otp:state` — دائمیِ عمدی

پیشنهاد جدولِ پرامپت، TTL پنج دقیقه بود. اجرا نشد و دلیلش فنی است:
این سند حامل **تأخیر تصاعدی بروت‌فورس** (`login_fail`) است؛ انقضای پنج‌دقیقه‌ای
یعنی صفرشدن شمارندهٔ تأخیر هر پنج دقیقه → ضعف امنیتی. اندازهٔ سند با هرسِ
درون‌سندی (کدها: عمر کد؛ بقیه: افق ۲۵ ساعت) کراندار است، پس نشتی ندارد.
در ابزار ممیزی در فهرست «دائمیِ مجاز» ثبت شده و پرچم نمی‌خورد.

## ۴) ابزار پایش `tools/redis-audit.js`

- اسکن امن با `redis.scan` (SCAN با مکان‌نما — هرگز `KEYS *`؛ تابع `scan`
  به `server/redis.js` افزوده شد).
- گزارش هر کلید: مقدار/وضعیت، TTL، مالک، توضیح قرارداد.
- پرچم‌ها: ⚠️ بی‌انقضای غیرمجاز (یتیم) و ❓ کلید ناشناخته (باید در `KEY_SPECS`
  ثبت شود).
- کد خروجی: ۰ سالم / ۲ نیازمند رسیدگی / ۱ خطای اجرا.
- بدون `REDIS_URL` روی درایور حافظه هم کار می‌کند (مناسب توسعه و تست).
- اجرا: `REDIS_URL=... node tools/redis-audit.js` (پیشنهاد: پس از هر استقرار + کرون دوره‌ای).

## ۵) نتایج تست و دروازه‌ها (قوانین سخت)

| آزمون | نتیجه |
|---|---|
| `node tests/smoke.js` | **۵۴۷/۵۴۷** ✅ |
| `node tools/check-authz.js` | خروجی **۰** ✅ |
| `node tests/secret-scan.js` | **۱۱/۱۱** ✅ |
| `tests/redis-key-audit.js` (تازه) | **۱۷/۱۷** ✅ |
| `tests/rate-limit-distributed.js` | ۹/۹ ✅ |
| `tests/rate-limit-mutations.js` | سبزِ نهایی ✅ (طبق معیار خود سوئیت) |
| `tests/otp-ratelimit.js` | ۴۹/۴۹ ✅ |
| شش سوئیت فاز ۲ (redis-fallback, lock-atomic, otp-redis, id-collision, tombstone, occ) | همگی سبز ✅ |

## ۶) ادغام مین در شاخه (پیش‌نیاز این کار)

مین از زمان فاز ۲ سه کار چت۳ را گرفته بود (نرخ توزیع‌شده، WAF/DDoS، اعتماد
صفر). ادغام انجام شد و تنها کانفلیکتِ معنایی این بود: چت۳ شمارنده‌های پنجره‌ای
را به `rate-limit.js` برده بود ولی پی۰-۱۵ من کل سند حالت را به ردیس می‌برد.
حل: **شمارنده‌های پنجره‌ای در ردیسِ چت۳** (`rate:*`) و **کدها/کول‌داون/تأخیرها**
با توزیع ردیسی پی۰-۱۵ (`payesh:otp:state`). تست `otp-redis` با شکل تازهٔ سند
به‌روز شد و ۱۶/۱۶ است.

## ۷) شفاف‌سازی‌ها

- یک قرمزیِ **پیش‌موجود** روی مین هست که به این تغییر مربوط نیست:
  `waf-ddos` — آزمون INT-b (همزیستی سرآیند با tracing). بدون هیچ تغییرِ من هم
  قرمز است (با `git stash` راستی‌آزمایی شد)؛ متعلق به حوزهٔ چت۳ است و دست نخورد.
- در `tests/rate-limit-mutations.js` یک جهش «زنده» می‌ماند اما خود سوئیت با
  همان وضعیت «سبزِ نهایی» گزارش می‌دهد (معیار داخلی‌اش)؛ رفتاری تغییر نکرد.

## فایل‌های این دور

- `server/redis.js` — +`incrWithTtl`، +`scan`
- `server/rate-limit.js` — استفاده از `incrWithTtl`
- `tools/redis-audit.js` (تازه)
- `tests/redis-key-audit.js` (تازه)
- `docs/REDIS_KEY_OPTIMIZATION.md` (تازه)
- `HANDOFF.md` — ورودی ممیزی
- ادغام مین: `server/auth.js`, `server/otp-store.js`, `tests/otp-redis.js`
