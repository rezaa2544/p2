# ارسال به گیت‌هاب

مخزن: `https://github.com/rezaa2544/p2.git`

## ✅ وضعیت: متصل و همگام

آخرین ارسال با موفقیت انجام شد. مخزن شامل **۷۱ فایل** است:

| بخش | تعداد |
|---|---|
| ماژول‌های `src/js` | ۴۱ |
| اسناد `docs/` | ۹ |
| آزمون‌ها | ۲ |
| فایل پیشین مخزن | `New folder.rar` (حفظ شد) |

---

## نکتهٔ مهم دربارهٔ ادغام

مخزن پیش‌تر فقط `New folder.rar` داشت. به‌جای پاک‌کردن تاریخچه با
`--force`، تاریخچهٔ دو طرف **ادغام** شد تا آن فایل از دست نرود.

---

## ارسال دستی (اگر لازم شد)

```bash
git push https://TOKEN@github.com/rezaa2544/p2.git HEAD:main
```

توکن در تنظیمات ذخیره **نمی‌شود**؛ هر بار باید در همان دستور بیاید.
این عمدی است تا کلید دسترسی در فایل‌های پروژه نماند.
